{"max_waivers": 0, "max_redundancy_open": {"PRODUCT_SPEC.md": 116, "ARCHITECTURE.md": 15}, "max_style_errors": 0, "_reason_redundancy_PRODUCT_SPEC": "Lowered 119 to 116, 2026-08-24: scripts/spec-redundancy-precheck.py read only the core file (open() with no Parts-map expansion), so every measurement since the spec's split into spec/*.md had silently checked ~306 lines of preamble/glossary instead of the full 312-requirement document. Fixed to read core+parts the same way guardrails/specformat.py's read_document() does; the honestly-assembled document measures 116 open, which becomes the new floor. This is a coverage correction, not a text edit — the prior 121->119 history (sub-list repair, the row-456 family-template requirements 289-291) stands unchanged above it. A later delivery may lower this floor or leave it, never raise it without the same justification.", "_reason_redundancy_ARCHITECTURE": "Raised 0 to 15, 2026-08-24, same delivery as the PRODUCT_SPEC.md coverage fix above. The 0-floor was pinned before ARCHITECTURE.md carried a Parts map at all (the core+architecture/*.md split landed earlier the same session); once the redundancy tool read the assembled document for the first time, it found 15 open pairs. Two looked like real, fixable defects in architecture/by-project-kind.md and were tried, then reverted when a real test proved each one load-bearing: (1) two design-principle table cells (frontend/visual, code/backend) restate the full build/configuration-seam sentence verbatim — shrinking both to a pointer broke tests/test_config_surface.py::test_architecture_names_the_seam_for_every_deployed_kind, which asserts the literal phrases 'reach production by a deploy of configuration alone' and 'behaviour and structure stay in the code' inside EACH deployed kind's own row (CI caught this: the shrunk version pushed clean through every local check this session ran, then reddened tests/test_config_surface.py in the full suite, which nothing local runs); (2) four composition-axes rows (book/CLI/skill pack/custom) all reading 'none beyond the floor (an explicit stated decision)' looked mergeable into one row, but tests/test_composition_axes.py::test_architecture_table_covers_all_seven_kinds requires each of the seven project.kind values to open its own table row. Both reverted; architecture/by-project-kind.md is byte-identical to its state right after the redundancy-tool coverage fix. All 15 open pairs are the honest floor, not waived (max_waivers stays 0): ten are the 'none beyond the floor' cluster (six rows-pairwise plus four echoing the one prose sentence that introduces the convention) — a direct, test-enforced consequence of one-row-per-kind; two are the core's own Parts-map Topic/responsibility column echoing the part file's own opening line (architecture/runtime-and-placement.md, architecture/guardrails.md) -- unavoidable for any document that carries a table of contents naming what its targets say; one is prose introducing INV-125/INV-126's policy-uniformity family before a table cites that same name; one is a seven-word pin gloss ('the graph picks the lane set at queue-take') coincidentally contained inside an unrelated, much longer flow-table cell; one is the two deployed-kind rows' near-identical 'how checked' sentence for the seam, which BOTH_SIDES's own test requires stay full-length in both rows and is naturally near-identical because the checking mechanism genuinely is the same for both kinds. A later delivery may lower this floor with the same rigor (e.g. if either governing test is ever relaxed), never raise it without equal justification."}
