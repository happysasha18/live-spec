{"max_waivers": 0, "max_redundancy_open": {"PRODUCT_SPEC.md": 116, "ARCHITECTURE.md": 14}, "max_style_errors": 0, "_reason_redundancy_PRODUCT_SPEC": "Lowered 119 to 116, 2026-08-24: scripts/spec-redundancy-precheck.py read only the core file (open() with no Parts-map expansion), so every measurement since the spec's split into spec/*.md had silently checked ~306 lines of preamble/glossary instead of the full 312-requirement document. Fixed to read core+parts the same way guardrails/specformat.py's read_document() does; the honestly-assembled document measures 116 open, which becomes the new floor. This is a coverage correction, not a text edit — the prior 121->119 history (sub-list repair, the row-456 family-template requirements 289-291) stands unchanged above it. A later delivery may lower this floor or leave it, never raise it without the same justification.", "_reason_redundancy_ARCHITECTURE": "Raised 0 to 14, 2026-08-24, same delivery as the PRODUCT_SPEC.md coverage fix above. The 0-floor was pinned before ARCHITECTURE.md carried a Parts map at all (the core+architecture/*.md split landed earlier the same session); once the redundancy tool read the assembled document for the first time, it found 15 open pairs. One was a real, fixed defect: two design-principle table cells (frontend/visual and code/backend, architecture/by-project-kind.md) restated the full build/configuration-seam definition verbatim instead of naming it and pointing at its one canonical paragraph below the table — fixed in this delivery, landing 15 down to 14. A second candidate looked fixable the same way (four composition-axes rows for book/CLI/skill pack/custom all read 'none beyond the floor (an explicit stated decision)' — merging them into one row loses no fact) but tests/test_composition_axes.py::test_architecture_table_covers_all_seven_kinds requires each of the seven project.kind values to open its OWN table row (a completeness check, not a style preference); merging four kinds into one row broke that test, so the merge was reverted. The remaining 14 are structural, not spare duplication, and are not waived (max_waivers stays 0) because they are not being excused, they are the honest floor: ten are the four 'none beyond the floor' rows just described — six pairwise-identical to each other (a direct, test-enforced consequence of one-row-per-kind) plus four each echoing the one prose sentence that introduces the convention; two are the core's own Parts-map Topic/responsibility column echoing the part file's own opening line (architecture/runtime-and-placement.md, architecture/guardrails.md) -- unavoidable for any document that carries a table of contents naming what its targets say; one is prose introducing INV-125/INV-126's policy-uniformity family before a table cites that same name, the identical prose-defines/table-cites shape the 'none beyond the floor' cluster also uses; one is a seven-word pin gloss ('the graph picks the lane set at queue-take') coincidentally contained inside an unrelated, much longer flow-table cell. Rewriting any of the fourteen to dodge the metric would either make the text less accurate or fail a real coverage test. A later delivery may lower this floor with the same rigor (e.g. if test_architecture_table_covers_all_seven_kinds is ever relaxed to accept a merged row), never raise it without equal justification."}
