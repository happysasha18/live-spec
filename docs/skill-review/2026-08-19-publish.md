# `SKILL-REVIEW` — publish, the skill-row's new checklist clause

Skill: publish. Date: 2026-08-19. Range: 228cbcda..cd2010f0 (skill's own last change is `b4799774`;
HEAD at review time is `cd2010f0`, unchanged under `skills/publish/` since `b4799774`).

Verdict: ALLOW.

## Why this record exists

`guardrails/check-skill-review.sh` (gate s, SPEC INV-208) reds this push for `publish` the same way it
does for `build-pipeline`: commit `b4799774` changed `skills/publish/SKILL.md` with a non-stamp
content line and no committed review record under `docs/skill-review/` named `publish` was fresh
enough. Confirmed by running the gate directly:

    FAIL (skill review): skill 'publish' is substantively changed in this push but no committed
      skill-creator review record under docs/skill-review/ names it with a verdict at least as new as
      the skill's own last change (SPEC INV-208). skill 'publish' last changed in
      b479977453f9742b490b371ea4514f91a56425b6.

## What `b4799774` actually changed here

Read via `git show b4799774 -- skills/publish/SKILL.md` directly. The change is one clause appended
to the `skill` row of the kind checklist table (current file, line 93):

    | skill | install line(s) that actually run · the commands/invocations, shown as they're typed ·
    when to USE it and when NOT (both, verbatim from the skill's own boundaries) · a one-scenario
    taste of it working (its eval scenario is the honest source, SPEC E-19) · a skill-creator review
    on record for the change (SPEC INV-208, `docs/skill-review/`, gate s) |

Only the `skill` row changed; the `infra / tool`, `product (visual)`, and `prose` rows are untouched
(checked by diffing the full table against `git show 228cbcda:skills/publish/SKILL.md`, the file's
prior state). Net: `SKILL.md` 160 lines total, and `guardrails/rule-census.json`'s
`skills/publish/SKILL.md` entry moved 13553 to 13649 bytes — checked against the file on disk with
`wc -c`: 13649, matching the census exactly.

## The review itself, applying skill-creator's method

**Frontmatter and description.** Untouched by `b4799774`. Read in full: `name: publish` is
unambiguous. `description` states a clear firing condition (before work leaves the machine publicly —
public repo, README push, release, shared skill) and an explicit boundary against the next step it
sits before (the human's own publish decision) — this matches skill-creator's guidance that
"when to use" belongs entirely in the description, not the body, and this commit adds no new trigger
condition, so nothing here needed to change and nothing did.

**Does the new clause fit its row, and does it contradict a neighbor?** I read the whole checklist
table (lines 91-96) and the two paragraphs immediately above it (the shared floor every kind owes,
lines 54-89). The new clause sits in the same list-with-middle-dot form as the other three clauses
already on the `skill` row (install lines, when-to-use/not, the eval-scenario taste), each ending
in a SPEC citation in parentheses the same way the eval-scenario clause does (`SPEC E-19`). The new
clause follows that exact shape: `(SPEC INV-208, `docs/skill-review/`, gate s)`. No other row or
paragraph in the file mentions skill-creator review, INV-208, or `docs/skill-review/` before this
change (checked with `grep -n` across the whole file), so there is no existing statement for the new
clause to duplicate or conflict with — this is genuinely new information in the file, stated once.

**Is the new clause's content accurate?** Verified independently of the commit message:
`guardrails/check-skill-review.sh` is the real gate; `guardrails/pre-push` line 227 and
`guardrails/README.md` line 45 both label it "gate s"; `docs/skill-review/` is a real, populated
directory; `TEST_MATRIX.md` row M-389 documents `SPEC INV-208` as exactly this law (a committed
record naming the skill, a `SKILL-REVIEW` marker, a `Verdict:` line, freshness against the skill's own
last change). The clause names all three correctly and nothing else needed correcting.

**Shape and readability.** Table cells in this row already run long (the row was one of the file's
longer lines before this change); the addition is a fifth middle-dot-separated clause in a row that
already had four, matching the existing punctuation and citation style exactly. I checked the row does
not overflow into a run-on that loses its own dots (read the raw line, all five clauses separated
cleanly by ` · `), so the addition does not degrade the row's readability below its three siblings'.

**Whole-skill re-walk.** Read the rest of the file end to end (all 160 lines) looking for anything
this addition should have touched but didn't: the "How it runs" section (lines 133-141) still says
"Walk the floor, then the kind's row" without needing to name every row clause individually, so it
needed no edit to stay accurate; the worked example (lines 142-154) is a README example, not a
skill-review example, and doesn't claim to demonstrate every row clause, so its silence on
skill-creator review is not a gap. No reference file exists under `skills/publish/` for this addition
to have orphaned (the skill carries no `references/` directory at all — confirmed by `ls
skills/publish/`).

## No defect found — what would have counted as one

Checked for: a restated fact the file already carried elsewhere (none, confirmed by `grep`); a broken
citation (none, all three parts of the parenthetical verified against the real gate, directory, and
matrix row); a byte-count/rule-census mismatch (none); a row-formatting break (none, the dot-separated
shape holds through all five clauses); a stale description that now undersells or oversells the skill
given the new clause (none — the description's scope is unchanged and the new clause is an internal
requirement, not a new user-facing capability). None of these produced a real flaw, so the skill file
was not modified by this review.

## What I ran

`git show b4799774 -- skills/publish/SKILL.md`; a full diff of the kind-checklist table against
`git show 228cbcda:skills/publish/SKILL.md`; `wc -c skills/publish/SKILL.md` against the
`rule-census.json` entry (`python3 -c` JSON read); `grep -n` for `INV-208`, `skill-creator`,
`skill-review` across the whole file; `grep -n "gate s"` in `guardrails/pre-push` and
`guardrails/README.md`; `ls skills/publish/` to confirm there is no `references/` directory to check
for orphaning; `git log --oneline -- skills/publish/SKILL.md` to confirm `b4799774` is the skill's
last change; `bash guardrails/check-skill-review.sh` run directly, output pasted above; and
`python3 -m pytest tests/ -q -k "skill_review or skill_count or build_pipeline or publish"` — 75
passed, 0 failed. No file under `skills/` was modified during this review.

Reviewer: this session, reading `~/.claude/skills/skill-creator/SKILL.md` for method and applying it
directly to the changed and surrounding text of `skills/publish/SKILL.md` at HEAD.
