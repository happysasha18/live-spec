# Skill review — publish

SKILL-REVIEW

Skill: publish

Date: 2026-08-26
Reviewer: skill-creator quality lens (Anthropic's skill-creator SKILL.md's own writing guide —
Progressive Disclosure, Anatomy of a Skill, frontmatter-description accuracy — applied by hand; the
tool's own eval/iterate loop is out of scope for a pack-wide pass over twelve skills, PLAN.md step 8)

Verdict: clean — no blocking findings.

## What changed

This is not a review of a specific edit. It is the plan-mandated pack-wide skill-creator pass over
every live-spec working skill ahead of PLAN.md step 8 ("Релиз наружу") and its push gate s, which
otherwise reds a substantive skill change with no matching record. `publish` is reviewed fresh here as
part of that full set.

## Findings

None blocking. Checked against the skill-creator checklist:

- **Frontmatter description** — states what (a pre-publish quality checklist) and when (before work
  leaves the machine publicly — a public repo, README push, release, shared skill), and draws its own
  scope boundary in the same sentence: "It finishes before the human's own publish decision and only
  prepares it." That last clause is exactly the kind of precision that stops the skill from being
  mistaken for the human's approval gate itself, and it matches the body's own "Work that belongs
  elsewhere" almost word for word.
- **Anatomy of a Skill** — a single 174-line `SKILL.md`, no `references/` directory. The content is one
  kind-checklist table plus per-target plugin sections; nothing here reads as needing offload at this
  length.
- **Progressive Disclosure** — 174 lines, well under the guideline.
- **Principle of Lack of Surprise** — nothing misleading; the skill is explicit that it "never sends
  anything itself" and that a publish "never turns into a push authorization" — both guard against a
  reader over-trusting this skill's authority.
- **Writing style** — imperative, with a genuinely useful worked before/after example (the track-coach
  README case) that shows rather than tells what the floor + kind-row checklist demands — a strong
  instance of skill-creator's own "examples pattern."
- **Reference-file consistency** — no `references/` directory exists; not applicable. Cross-file paths
  the body cites (`guardrails/check-shipped-language.sh`, `scripts/sweep-rendered.py`) were spot-checked
  and exist at the pack root.
