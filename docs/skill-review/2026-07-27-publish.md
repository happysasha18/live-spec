# Skill review — publish (a release sweeps the rendered pages)

`SKILL-REVIEW`

Skill: publish
Date: 2026-07-27
Reviewer: skill-creator (Anthropic)

Verdict: passes, with one defect folded — the new paragraph pointed into communicator at a rule number
that does not exist. The frontmatter description, the paragraph's placement, and its report duty all
read clean.

## What changed

The shopfront walk gained a paragraph: a release runs `python3 scripts/sweep-rendered.py` to sweep the
rendered pages that accumulated, and the delivery report carries the outcome in one line (SPEC INV-286).

## Findings

**F1 — folded. A dangling cross-reference into communicator.** The paragraph cited "communicator rule
23". That skill carries twenty-two rules, and the clearing clause landed inside rule 5, as a closing
sub-bullet of the rule that governs where a rendered page is put. A reader following the pointer at
number 23 finds nothing there. This file already cites communicator rule 5 twice, in its when-it-fires
section and in its when-not-to-use section, both for the in-session showing boundary, so rule 5 is also
the anchor a reader of this file has already met. Changed to "communicator rule 5" at
`skills/publish/SKILL.md:41`. `tests/test_rendered_sweep.py` stays green.

**F2 — reviewed, no change. The paragraph earns its place under the version-push trigger.** The
shopfront walk already fires at every push that ships a version. A release is the moment where pages
that escaped their own clearing get swept, so the sweep is a step of a walk that already runs at that
moment. Placing it there costs the reader no new trigger to remember.

**F3 — reviewed, no change. The command string appears in two files by design.** The lifecycle
reference in communicator documents both machines, the sweep and the check, with the dry-run flag. This
file states the one command a release runs. A publisher can run the release step without loading a
communicator reference, and each page keeps the level of detail its own reader needs.

**F4 — reviewed, no change. The frontmatter description already covers the new material.** A release is
named in the trigger list, and the description keeps the walk-shaped framing the body follows. Naming
the sweep in the description would add a step name to a field that lists occasions.

**F5 — reviewed, no change. The report duty matches its neighbour.** The shopfront paragraph above ends
by putting its outcome in one line of the delivery report. The sweep paragraph does the same and names
what that line says: which pages were cleared and where they come back from.
