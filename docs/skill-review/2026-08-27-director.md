# Skill review — director

SKILL-REVIEW

Skill: director

Date: 2026-08-27
Reviewer: skill-creator quality lens (Anthropic's skill-creator SKILL.md's own writing guide —
Progressive Disclosure, Anatomy of a Skill, frontmatter-description accuracy — applied by hand)

Verdict: PASS — no blocking findings. Real content: four new definitional clauses closing gaps in the
acts model, plus the pack-wide seat rename applied across `director`'s reference files. One
non-blocking finding, a small leftover of the rename in one reference file the diff otherwise renamed
consistently; one pre-existing non-blocking finding carried forward unchanged from the prior review.

## What changed

The prior record, `2026-08-26-director.md` (commit `c73d87cd`), covered commit `5db30805`. `director`'s
own last change is now `905a1e13` ("five skill-text clarifications and three fixture corrections close
7 of 9 red scenarios," 23:59) — later than `c73d87cd` and not covered by it, so the gate correctly
calls that record stale for this push. `git diff c73d87cd..HEAD -- skills/director/` (228 lines) adds:

1. **Four new definitional clauses in `SKILL.md`**, closing gaps between adjacent acts that the prior
   model left ambiguous: (a) both branches of a conditional request stay named as separate acts, but
   the outcome is exclusive — taken as work or shelved, never both; (b) a decision is a standing rule
   with force beyond the message that carried it, distinct from a same-turn request; (c) a correction
   is distinguished from a decision (a correction replans the work's goal; a decision settles a choice
   within work that keeps its plan) and from a caution (a caution paces the work without changing what
   it must do); (d) a halt is scoped to the session's own work in progress — a mid-procedure "stop the
   server" step or a request to change method rather than goal is not a halt. A fifth clause states
   when grounds given alongside an act count as their own second act (only when they say something the
   neighbouring act's own goal does not already carry).
2. **The seat rename** applied across `references/class-hunt.md`, `delegation-protocol.md`,
   `excuses-table.md`, `landing-law.md`, and `lanes-and-pen.md` — "senior" → "seat," and citations to
   now-retired `live-spec-base` rule numbers (14, 15, 32) replaced with descriptions.
3. **The specialist table's Product prover row** gains ", or shipped code needs a class-based defect
   hunt with no document to check it against" — the CODE-REVIEW mode wired in tonight's
   `product-prover-pack` change (reviewed separately in this session).

## Findings

None blocking.

1. **The four new clauses read as genuine closures, not padding** — each pairs a rule with a worked
   contrast ("stop the server" vs. a true halt; a standing "always deploy" vs. today's one deploy) in
   the file's existing register, and each is placed directly beside the act it disambiguates rather
   than collected in an appendix. Checked against the commit message's own claim ("close 7 of 9 red
   scenarios") only insofar as the four clauses map onto four named ambiguities — verifying the
   fixtures themselves is a test-suite question outside this skill-text review's scope.
2. **The Product prover row's new clause is cross-checked against its source** — `skills/
   product-prover-pack/SKILL.md`'s own Code mode section states the same boundary ("Director calls this
   when shipped code needs the same defect hunt a spec review runs, and no document governs the
   surface") — the two files describe the same handoff consistently in both directions.
3. **A rename leftover, not caused by this diff but sitting inside a file this diff otherwise renamed
   throughout** — `references/delegation-protocol.md` renames five of its seven "senior" occurrences to
   "seat" in this same diff, but two survive unchanged: line 9 ("judgment or design stays senior") and
   line 91 ("how much senior work it saved"). Same file, same pass, two instances missed — worth a
   one-line fix next time this file is touched; non-blocking since the file's meaning is still correct
   ("senior" and "seat" name the same session here) and no reader is misled.
4. **Reference-file reachability — the prior review's finding is unchanged, not worsened.**
   `references/` still holds thirteen files; `SKILL.md` still links only four of them
   (`build-craft.md`, `class-hunt.md`, `landing-law.md`, `verify-step-detail.md`). The nine unreachable
   files — `delegation-protocol.md`, `drafter-applier-example.md`, `excuses-table.md`,
   `footprint-read.md`, `guardrails-catalog.md`, `lanes-and-pen.md`, `mockup-first-entry.md`,
   `request-kind-table.md`, `work-kind-table.md` — include three (`delegation-protocol.md`,
   `excuses-table.md`, `lanes-and-pen.md`) edited in place by tonight's rename, but the edit does not
   make them reachable or newly orphaned — the same pre-existing pack-hygiene debt the 2026-08-26
   review already named, still open, still not this commit's to fix.
