# Skill review — product-prover (the no-redundancy cut, verified old against new)

`SKILL-REVIEW`

Skill: product-prover
Date: 2026-08-07 10:48
Reviewer: a fresh verifier session raised for the verification alone, with clean context. It did
not author the cut. A second fresh reader, the day's adversarial reviewer, spot-checked ten rules
of its own choosing afterward. Base rule 33 asks for that freshness, and both heads answer it.

Verdict: the cut stands after two repairs the verification forced. Every rule, anchor, path, date
and table value survives; the file falls 69,297 to 66,675 bytes at zero prose-census findings.

## What was reviewed

The whole rewrite of `skills/product-prover/SKILL.md` (row 570, "the rulebook cut"), old text
against new, by an inventory the verifier built itself from the old file.

## Findings and their disposition

- The first cut broke the file's cleared prose record: ten sentences merged past the 25-word cap.
  Repaired by splitting every one; the census reads zero for this file.
- One worked instance was dropped with no pointer, the transition-payload example. Restored
  byte-exact at its old position.
- The second reader's ten-rule spot check found nothing lost, weakened, or invented.
- Standing recommendation: the file still carries unpinned scan-time targets; they go with the
  task's next pass, named on the rulings page.
