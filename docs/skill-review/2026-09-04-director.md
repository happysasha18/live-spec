# Skill review — director ("What the human hears back" — a document change owes before/now/added)

SKILL-REVIEW

Skill: director

Date: 2026-09-04
Reviewer: skill-creator quality lens (Anthropic's skill-creator SKILL.md's own writing guide —
Anatomy of a Skill, Progressive Disclosure, frontmatter-description accuracy, Writing Style —
applied by hand against the working tree as re-read at write time; skill-creator's own
`quick_validate.py` re-run immediately before writing this record, quoted below)

Verdict: validator passes; the duplication this record flagged earlier tonight is now fixed on
disk — nothing outstanding is folded here. This record holds no edit authority over
`skills/director/SKILL.md`.

This record covers the working tree at the moment it was written, not a fixed commit — `git
status` shows `skills/director/SKILL.md` still uncommitted. Size read at that moment:
`skills/director/SKILL.md` is **30,185 bytes** (this record's own re-read at write time; the
coordinator's own count agrees). The whole skill directory (`SKILL.md` + thirteen `references/`
modules) is **75,017 bytes** total. This supersedes every earlier draft of this record, each stale
before it was read again.

## Validator

```
$ python3 ~/.claude/skills/skill-creator/scripts/quick_validate.py skills/director
Skill is valid!
```

## What changed since the last draft of this record

The new paragraph in `## What the human hears back` was cut back to only what the earlier draft
found genuinely new. It now reads: "A change to a document owes three more short lines beside the
document's name: what that part said before, what it says now, and what was added. The person can
then disagree with the edit while undoing it is still cheap..." The sentences the earlier draft
flagged as restating the section's own four-outcome line ("new work says a row is opening...") and
its own document-naming line ("a change to a document says which document...") are gone.

## Findings

1. **Partial duplication with the section's existing paragraphs — resolved.** Folded. Re-read
   against the three paragraphs already in `## What the human hears back`: the trimmed paragraph
   no longer restates "which of four things happened" (still only in the section's first
   paragraph) or "the sentence names what actually changed — which document" (still only in the
   third). What remains is exactly the before/now/added shape, which exists nowhere else in the
   skill — the one piece of the earlier paragraph this record called genuinely new.

2. **Description still matches the body.** The frontmatter's closing clause — "carry it through —
   checkpoint, verify, close, report" — covers this addition at the level a one-paragraph summary
   can; the trimmed paragraph is a refinement of what "report" already promised. No change owed.

3. **Anatomy, progressive disclosure, writing style — pass.** 444 lines (four lines shorter than
   the pre-trim draft's 448), still comfortably under the guide's <500-line target, and still the deepest
   reference layer in the pack (thirteen `references/` files). The paragraph carries no citation,
   matching the pattern of its neighbors in `## Execution` (a stated process discipline proven by
   the project's own eval harness rather than a machine check) — not a gap specific to this
   addition. No shouted MUST/ALWAYS/NEVER anywhere in the file.

## Carried forward from tonight's review of the tree (live-spec-base rule 36)

Not a director finding, but a true finding about the tree this same review pass turned up while
`skills/live-spec-base/SKILL.md` was open beside this file, kept on the record at the
coordinator's word rather than let it disappear with the rewrite: rule 36 of `live-spec-base` (who
the person is, and what they never see) now carries no trailing enforcement citation for any of
its three paragraphs. Checked directly: "blocked" narrowed to a real cause and the plan's five
status marks are covered, by `scripts/plan_checks.py`, `scripts/plan_checks_core.py`, and
`guardrails/check-board.py`. Two clauses are covered by nothing found in `guardrails/`, `scripts/`,
or `hooks/`: a "get his word" item never filed as a task or a status, and the "report a result
flat" ban on self-congratulatory phrases ("exactly where you said," "as expected") — the nearest
law, `r12` in `guardrails/language-rules.json` via `hooks/register-judge.py`, targets different
phrasing (evaluative adjectives, not completion phrases) and is armed only `"manual"`, not run on
its own. The full finding, with sources, is on record in
`docs/skill-review/2026-09-04-live-spec-base.md`.

No findings against the trimmed paragraph's own content — it is concrete, states one instruction,
and gives its own reason (a document edit reported only as "changed" gives the person nothing to
check against; this gives them exactly that).

## Follow-up, same night, after this record was written

One of the two uncovered clauses now has a reader. `tests/test_no_row_waits_on_the_person.py` reads
every open row's finish condition in `PLAN.md` and reds when the condition is somebody looking at
it, which is rule 36's ban on a task standing in for the person's word. It is red-proven against the
tree's own history rather than asserted: run over `PLAN.md` at `ead4a705` it catches three open
rows, among them `q-166`, whose acceptance read "No command decides this one; his own eye is the
check." The flat-reporting ban still has no reader, and none is planned — it governs chat, which no
file-reading check can see.
