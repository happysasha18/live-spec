# Prover record — 2026-08-19 server-only-reds

PUSH-REVIEW

Range: 8c58bdc2..bdf8ef14
- bdf8ef14 Fix two server-only reds: raw-substring gates.yml tests, and the unmarked HTML render
Files read: tests/test_guardrails_unit.py, scripts/measurements-table.py, guardrails/check-rendered-sweep.py, scripts/sweep-rendered.py, scripts/render-doc.py, .github/workflows/gates.yml, tests/test_measurements_html_optin.py, tests/test_rendered_sweep.py
Checks run: `python3 -m pytest -q tests/test_guardrails_unit.py tests/test_measurements_html_optin.py` — 43 passed. `python3 -m pytest -q tests/test_gates_manifest.py tests/test_ci_mirror.py tests/test_every_gate_can_fail.py tests/test_rendered_sweep.py` — 89 passed. `gh run view 32255385068 --repo happysasha18/live-spec --log-failed` and `--log`, read directly against the real server run rather than inferred: confirmed the actual raised assertion for `test_html_flag_builds_a_page_the_gate_reds_on_and_the_sweep_clears` is `AssertionError: 0 != 1 : the sweep gate stayed green with docs/MEASUREMENTS.html standing — it is not being read as the transient page it is`, not the `os.path.exists(HTML)` line first suspected (that line is pytest's printed CONTEXT above the actual failure, not the raised error — confirmed by locating the `>` marker and the final `E AssertionError:` line in the raw log). Grepped the full job log for `dubious|fatal:|safe.directory` — `actions/checkout@v5` explicitly ran `git config --global --add safe.directory /home/runner/work/live-spec/live-spec`, ruling out the dubious-ownership class. Reproduced the disease independent of any CI access: built a scratch, non-git directory carrying only `docs/MEASUREMENTS.md` and a hand-built `docs/MEASUREMENTS.html`; against the pre-fix (unmarked) page shape, `guardrails/check-rendered-sweep.py --root .` printed "OK (rendered sweep): no transient page stands" and exited 0 — a false green with a real transient page standing; against the post-fix (marked) shape, the same command exited 1, naming the page and quoting the mark. `git ls-files -- '*.html' '*.HTML'` on the real tree confirmed `docs/MEASUREMENTS.html` is not and was not tracked, ruling out the "already committed, so exempt" explanation. `python3 -c "import yaml; yaml.safe_load(...)"` on the current `.github/workflows/gates.yml` — parses, 29 steps.
Findings: (1) BREAKAGE ONE, this branch's own, from the prior range's gates.yml quoting fix. Two tests in `tests/test_guardrails_unit.py` — `test_the_server_runs_gate_g_on_every_push` and `test_the_server_still_runs_the_full_suite` — located a gates.yml step by raw substring: `workflow.index("name: gate g")` and `assertIn("name: test suite (gate b, full", workflow)`. Quoting every step name (this branch's `a45a32fc`, closing the YAML-parse hole gate af's own byte-for-byte sync opened) put a `"` directly after `name: ` on every step, so both substrings stopped matching — the workflow itself was correct and parseable, but two tests that read it as raw text rather than as YAML broke on formatting alone. The coordinator's report named only the gate-b one from the server; running the file locally found the gate-g one failed identically and had simply not yet reached a server run. Both rewritten to parse gates.yml with PyYAML (already a test-only dependency as of the prior range's fix) and locate the step by its parsed `name` field through one new shared helper (`_step_named`), so a step's exact formatting — quoted or bare, extra whitespace, a wrapped `run:` block — can no longer break either test; only the step's actual absence, or its `run`/`if` field's actual content, can. (2) BREAKAGE TWO, a neighbouring lane's `--html` opt-in feature, fixed here per the coordinator's assignment. Ground truth taken directly from the failing server run's own log (`gh run view --log-failed`), not inferred: the real raised assertion is that `guardrails/check-rendered-sweep.py` returned 0 (green) with `docs/MEASUREMENTS.html` standing, not that `--html` failed to build the page at all — the earlier two assertions in that test (`returncode == 0`, `os.path.exists(HTML)`) both pass on the server; only the third, the sweep gate's verdict, is wrong. Root cause, read from `guardrails/check-rendered-sweep.py`'s own `evidence()`: a page reads as transient by carrying the shared `live-spec render-doc` generator mark (the modern, robust signal every other renderer in the pack writes, per `scripts/render-doc.py`) OR, failing that, by a documented WEAKER fallback — its source `.md` standing beside it, gated on `git ls-files` succeeding. `scripts/measurements-table.py`'s `render_html()` wrote no mark at all, so the gate's verdict rested entirely on that weaker, git-dependent fallback; on the server that fallback evidently did not fire (evidence and reproduction above), on this machine it did, and the local/server split traces to exactly that dependency rather than to any deeper CI-only unavailability — repeated attempts to find a git-level failure in the server log (dubious ownership, a nonzero `ls-files`, a timeout) turned up nothing, so the finding is content: the renderer never gave the gate the strong signal every sibling renderer gives it, and leaned on a fallback the gate's own docstring already calls a heuristic that "runs only where the tracked-page guard can stand behind it." Fixed by importing `GENERATOR` from `scripts/render-doc.py` the same way `scripts/sweep-rendered.py` itself already does (one shared home for the string, never a second hand-typed copy) and embedding `<meta name="generator" content="live-spec render-doc">` in the page's `<head>`, matching `render-doc.py`'s own placement exactly. Detection no longer depends on git succeeding, on a source file standing beside the page, or on any environment property at all — proven by the scratch-directory reproduction above, which used no git repository whatsoever. Not silenced, not skipped, not weakened: the test's assertion is unchanged; the renderer now actually earns the green it claimed. (3) What this record does not claim: it does not have direct evidence of WHY `git ls-files` (or the legacy fallback more broadly) specifically failed to evidence the page on the GitHub-hosted runner — the server log carries no git-level error to point at, and the fix removes the dependency rather than diagnosing that one runner's exact behaviour, which is the correct scope for a fallback the gate's own author already documented as fragile.
Blocking: none
