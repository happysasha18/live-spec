# Push review — 2026-08-06 three landings and the edition measured

PUSH-REVIEW

Range: 9b3a666..706e6e6
Range note: the range pushed in two portions; the second portion's base 48ccd89 (the commit that first shipped this record) and its commits c0a6e3a and a4a9f0c all sit inside the reviewed range above. A third portion follows, based at 8276607, which carries this record alone; its one commit of its own is 0ce6fe7, reviewed below. A fourth portion follows it, based at dde72ac, which again carries this record alone. That fourth portion carries two commits of its own, b1e89e0 and 706e6e6, both reviewed below. I checked the tree rather than assuming a portion per commit: `origin/main` stands at dde72ac and the branch is two commits ahead of it, so both sit in one unpushed portion and there is no fifth base to name.

Reviewed commits (every commit in the range touches a file outside `docs/push-review/`):
- 706e6e6 The board row carries the evening's widening: frozen task wording and the validation step — one line in ROADMAP.md, row 166's Done-when column taking the second widening of the day
- b1e89e0 The work board moves to the queue's front, on Alexander's word of 19:17 — the decision on record in DECISIONS.md, the dated JOURNAL.md entry for the same word, and NEXT_STEPS.md putting the board at forward-queue item 1 with the rest renumbered 2 to 9
- 0ce6fe7 The spent work copies leave on Alexander's word, and the live state says what stays — a dated JOURNAL.md entry on the approved deletion of four work copies and five merged branches, and NEXT_STEPS.md item 8 rewritten to describe the three branches that stay
- a4a9f0c The four unregistered count surfaces state their real owners — NEXT_STEPS.md drops the claim that each of the four carries its own later row and points the undeclared-page class at row 563 and the other three at row 555's non-goal
- c0a6e3a The forward queue names the push review's four new rows — one new item in NEXT_STEPS.md naming rows 562 to 565, and a renumber of the two items below it
- fc57abe The push review's three blocking findings close, and the rest join the queue — the description-length figures state the measured 493, the skill-review record says the closing-sentence recommendation was taken, matrix row M-518 says the loading is unscored, queue rows 562 to 565 open, and the gate page gains its note for gate ad
- 996a405 The session's record: rows 555-557 landed, the suite's five reds repaired at their roots
- ce04fc6 The edition's README states what two real runs support, and the counts catch up
- 0c4224e The setup-entry tests ride every scoped push run
- 3ef7fc9 The public prover edition holds for any product kind and sits under its prose ceilings
- 704fdb0 Matrix rows M-505 to M-511 stand built; the registry files take their pins
- e68045f The three landings' shared documents, written in one pass (rows 555, 556, 557)
- d717f62 Every runnable file a skill body names has a record of what it is (row 556) — the merge
- fee77fb Saying "attach live-spec to this project" reaches a real setup walk (row 557)
- c238ca1 Every published count about this tree is measured, declared, and gated (row 555)
- 9a5df02 The registry records what each runnable file a skill body names is

Files read: PRODUCT_SPEC.md (Requirements 169, 306, 307, 308 and the generated Reference), ARCHITECTURE.md (templates node, guardrails node, the crossing table), TEST_MATRIX.md (the artifact inventory, rows M-494 to M-518, the generated anchor table), README.md, ROADMAP.md (rows 166, 552-561), JOURNAL.md (the 16:00-16:53 entry), NEXT_STEPS.md, docs/PROGRESS.md, docs/adoption.md, adopt/ADOPT.md, adopt/START.md, docs/push-review/README.md, docs/push-review/2026-08-05-day-of-readability-repairs.md, docs/prover/2026-08-06-setup-entry.md, docs/prover/2026-08-06-edition-two-run-comparison.md, docs/skill-review/2026-08-06-setup-entry.md, docs/handovers/2026-08-06-adoption-movement-handover.md, editions/product-prover/README.md, editions/product-prover/examples/sample-review-run-1.md, editions/product-prover/examples/sample-review-run-2.md, editions/product-prover/reference/stress-lenses.md, editions/product-prover/examples/sample-spec.md, evals/build-pipeline.md, guardrails/README.md, guardrails/pre-push, guardrails/check-push-review.sh, guardrails/check-freeze.sh, guardrails/check-push-reach.sh, guardrails/check-tree-counts.py, guardrails/check-named-checks.py, guardrails/tree-counts.json, guardrails/gate-red-proofs.json, guardrails/rule-census.json, guardrails/progress-baseline.json, .github/workflows/gates.yml, scripts/check-registry.json, scripts/gen-tree-counts.py, skills/build-pipeline/SKILL.md, skills/build-pipeline/references/project-setup.md, skills/build-pipeline/references/request-kind-table.md, skills/spec-author/SKILL.md, skills/text-audit/SKILL.md, templates/test_scaffold.template.py, templates/ (the whole directory listing), tests/test_setup_entry.py, tests/test_tree_counts.py, tests/test_published_counts.py, tests/test_check_registry.py.

Checks run: (each command, then its result)

- `python3 guardrails/check-tree-counts.py` — OK. Four of four rows matched over README.md and guardrails/README.md; counts read gate-roster, prover-records, scaffold-checks, skills-lines; 0.1 seconds.
- `python3 guardrails/check-named-checks.py` — OK. 32 entries read (21 checks, 3 libraries, 8 data files), 11 skill bodies scanned, 18 command-position paths found.
- `bash guardrails/check-cleanup-notice.sh` — OK. Every cleanup path that ends a process says what it ended.
- `python3 guardrails/check-handover-provenance.py` — OK. Six files read, three session handovers, two in scope, each naming its transcript, its extract and its writer.
- `python3 guardrails/check-doc-findings-bound.py` — OK. 114 live documents, 20 held at zero, none above its record.
- `bash guardrails/check-ci-mirror.sh` — OK. Every local pre-push gate is mirrored in CI or declared as a carve-out.
- `python3 guardrails/check-every-gate-can-fail.py` — OK. 31 gates checked, each carrying a known-red proof or a declared covered ride.
- `bash guardrails/check-prover-record.sh` — OK. The record commit is no older than the last PRODUCT_SPEC.md or ARCHITECTURE.md commit.
- `python3 scripts/rule-census.py` — 114 files measured, 4757 findings in all. This agrees with guardrails/progress-baseline.json and with docs/PROGRESS.md.
- `python3 -m pytest tests/test_tree_counts.py tests/test_published_counts.py tests/test_check_registry.py tests/test_setup_entry.py tests/test_catchup_walk.py tests/test_traceability.py -q` — 274 passed in 9.14s.
- `cat skills/*/SKILL.md skills/*/references/*.md | wc -l` — 6456. README.md publishes 6,456.
- `cat skills/*/SKILL.md | wc -l` — 5221. README.md publishes 5,221.
- `grep -oE -- '-- gate [a-z]{1,2}:' guardrails/pre-push | sort -u | wc -l` — 31. guardrails/README.md publishes 31, and the gate-can-fail meta-gate counted the same 31.
- `ls scaffold/guardrails/check_*.py | wc -l` — 4. README.md and guardrails/README.md publish four.
- `ls docs/prover/*.md | wc -l` — 342, above the floor of 300 the README sentence publishes.
- Finding counts re-measured by hand against the two run records: run 1's ledger totals 32 findings over F1-F32, run 2 numbers F1-F30 with five recommendation tags on F4, F24, F27, F28 and F29, and the match table in docs/prover/2026-08-06-edition-two-run-comparison.md leaves eight dashes, so 24 issues are reached by both. The edition README's "32 and 30 findings, and 24 of them were reached by both" holds.
- `wc -l editions/product-prover/examples/sample-spec.md` — 95. The edition README calls it a 95-line spec.
- `ls templates/ | wc -l` — 14. docs/adoption.md says fourteen and names all fourteen correctly.
- Description-field lengths measured over all eleven shipped SKILL.md files — text-audit 122, test-author 128, design-reviewer 202, publish 209, feedback-collector 246, feedback-intake 251, product-prover 293, spec-author 302, communicator 328, live-spec-base 383, build-pipeline 493. Ten of the eleven match the ladder published in docs/skill-review/2026-08-06-setup-entry.md. The eleventh disagrees. See finding 1.
- Registry composition recomputed from scripts/check-registry.json — 14 ships, 3 host-optional, 4 pack-only, and 14 argument roots, 2 working-directory, 5 own-tree. Commit 9a5df02's message states each of these figures and each one holds.
- Copy-table arithmetic in adopt/START.md re-counted — eleven rows, eight unconditional, three on the person's word. The walk's own sentence holds.

Re-run at fc57abe, after the repair of the three blocking findings:

- Description field re-measured at fc57abe — 493 characters, and it closes "A tiny reversible edit and pure research stay outside the pipeline." Every 477 and 493 occurrence across the three homes read and checked against it.
- `python3 guardrails/check-tree-counts.py` — OK. Four of four rows still matched after guardrails/README.md gained the gate ad note.
- `bash guardrails/check-freeze.sh` — GREEN over three files. The M-518 cell edit and the four new ROADMAP rows leave the guarded documents matching their baseline.
- `python3 guardrails/check-doc-findings-bound.py` — OK. Still 114 live documents, 20 at zero, none above its record.
- `python3 -m pytest tests/test_setup_entry.py tests/test_traceability.py tests/test_check_registry.py -q` — 219 passed in 6.87s. The eval and matrix edits break no test that reads them.

Run at c0a6e3a, over the forward-queue edit:

- Forward-queue numbering re-derived from NEXT_STEPS.md — items 1 through 8, no gap and no duplicate, and no text elsewhere in the file cites an item by its old number.
- Item 6's four descriptions read against ROADMAP.md — they name row 562's scoring run, row 563's counts outside the declared registry, row 564's re-implemented walk readers, and row 565's two ledger shapes, in that order. All four rows stand queued and dated 2026-08-06, and each cites the push review as its source.
- NEXT_STEPS.md re-measured — 85 lines against the 100-line digest cap, and one LIVE STATE block.
- `python3 guardrails/check-doc-bound.py` — OK, gate z. Four growable documents read, each within its declared bound.
- `python3 guardrails/check-doc-findings-bound.py` — OK. Still 114 live documents, 20 at zero, none above its record.
- ROADMAP swept for a row covering each of the four surfaces the counts registry leaves outside — only rows 555 and 563 touch the class at all. See finding 11.

Run at a4a9f0c, over the repair of finding 11:

- NEXT_STEPS.md read again — the phrase "each with its own later row" is gone. The paragraph now points the undeclared-page class at row 563 and the other three at row 555's non-goal. Row 563 stands queued and its body names counts on pages the registry does not declare. Row 555's non-goal names a chat number, a rendered-artifact number and a shipped-skill-body count, saying each stays unheld. Both pointers land on what the rows actually say.
- Forward queue re-derived — items 1 through 8 still, no gap and no duplicate, 86 lines against the 100-line cap, one LIVE STATE block.
- `python3 guardrails/check-doc-bound.py` — OK, gate z, four documents within their bounds.
- `python3 guardrails/check-doc-findings-bound.py` — OK. 114 live documents, 20 at zero, none above its record.
- `python3 guardrails/check-tree-counts.py` — OK. Four of four rows matched.

Run at 0ce6fe7, over the record of the removed work copies:

- `git worktree list` — one tree, the repository itself. `.claude/worktrees/` holds nothing. The journal's "four clean work copies were removed" matches the tree.
- `git branch -vv` — three branches stand beside main, exactly the three the journal names.
- `git rev-list --count main..backup-2026-08-06-before-relay` — 4, and `git log --date=iso` dates all four between 09:20 and 09:31 on 2026-08-06. The journal's "four commits of its own, a safety copy from this morning" holds.
- `git merge-base --is-ancestor relay backup-2026-08-06-before-relay` — false, which is what the branch name states. The backup is a snapshot taken before the relay, so it holds a parallel copy of the same work rather than relay's tip.
- `git branch --merged main` — main and relay. The journal's "relay (fully merged)" holds.
- `git rev-list --count main..wip/comms-naming-424` — 1, and its commit 55597bc is dated 2026-07-19. The journal's "one checkpoint commit of 2026-07-19" holds.
- The five deleted branches were counted against the queue item they replace, which named four work copies plus the merged registry branch. The journal's "the four copies' branches and one branch that had no copy" agrees with that accounting.

One limit of this check, stated rather than left silent: a deleted branch leaves no artifact, so the journal's "each held zero uncommitted files and zero commits ahead of main, checked before removal" cannot be reproduced after the fact. It rests on the session that ran the check and on Alexander's approval, which the entry records with its time.

Run at b1e89e0, over the work-board decision:

- `python3 guardrails/check-authority-anchor.py` — OK, gate r. Every live entry on a decision record names a real date, so the new entry's `2026-08-06 ~19:17` anchor parses and no attribution stands unanchored. The gate's own green line says a plausible fabricated date would still parse here and is caught at the read-back instead, so this result holds the anchor's presence rather than the words.
- The three files read against each other — DECISIONS.md, JOURNAL.md and NEXT_STEPS.md each name row 166, the time of about 19:17, the frozen task wording as an understandable name, description and plan spoken letter for letter once approved, and a validation step in front of the board. They tell one story with no clause in one file that the other two contradict. The journal's own heading reads 19:18, which is when the entry was written, against a word at about 19:17 and a commit at 19:19.
- Forward queue re-derived — items 1 through 9, no gap and no duplicate. The board stands at 1 and the eight previous items keep their text at 2 through 9. NEXT_STEPS.md is 91 lines against the 100-line cap, with one LIVE STATE block.
- ROADMAP row 166 read against the decision — see finding 12.

Run at 706e6e6, over the repair of finding 12:

- Row 166 parsed into its five columns — the new widening sits in the last one, the Done-when column, beside the widening of the same morning. That is the cell finding 12 named, since the Done-when is what stated the outgrown bar.
- The widening's text read clause by clause against the DECISIONS.md entry — the time of about 19:17 and the citation, the understandable name, description and plan, the freezing of approved wording, the seat speaking it letter for letter, all three moments of take-up, along the way and the close, the validation step in front of the board, and nothing entering work before it passes. Every clause of the decision reaches the row. The one clause left out is that the board is the next movement ahead of the rest of the queue, which is rank rather than work, and it stands at NEXT_STEPS forward-queue item 1.
- Scope checked — the commit touches ROADMAP.md alone, one line changed and one line removed, so no other row moved.
- `python3 guardrails/check-doc-bound.py` — OK, gate z, four documents within their bounds after the row grew.
- `python3 guardrails/check-doc-findings-bound.py` — OK. 114 live documents, 20 at zero, none above its record.
- `python3 guardrails/check-authority-anchor.py` — OK, gate r, still green over the decision record.

Findings: twelve. Three of them blocked, and all three closed in fc57abe, which I re-verified on the artifacts. Findings 1 to 10 describe the tree as I found it at 996a405. Their disposition after fc57abe: findings 1, 2 and 3 are repaired, and the Blocking section states what I checked for each. Finding 4 became queue row 563, finding 5 became row 564, and finding 7 became row 565. Finding 8 is repaired, since guardrails/README.md now carries the note for gate ad. Findings 6, 9 and 10 carry no disposition and stand as written. Finding 11 came out of the c0a6e3a check and is repaired in a4a9f0c, which I re-verified against rows 555 and 563. Finding 12 came out of the b1e89e0 check and is repaired in 706e6e6, which I read clause by clause against the decision entry.

The three landings are real work and most of what they publish survives re-measurement. Every count the new registry declares matches the tree, the census total matches the progress page, the registry composition figures in the commit messages are exact, and the two-run comparison's arithmetic holds row by row. The defects sit in the numbers and claims the new gates do not reach.

1. **A published count about this tree is wrong, in the push that lands the rule against exactly that.** Three shipped files state the build-pipeline description field at 477 characters. The shipped field is 493 characters, and it has been 493 since fee77fb first wrote it. The homes are `docs/skill-review/2026-08-06-setup-entry.md` at lines 98 and 102, `docs/prover/2026-08-06-setup-entry.md` at line 47, and `evals/build-pipeline.md` in the "When it re-scores" paragraph. The other ten figures in the same published ladder match the tree to the character, so the ladder was measured once against a draft and never re-measured after the field grew. Severity: high. Blocking.

2. **A review record says a recommendation was not taken, and the artifact took it.** `docs/skill-review/2026-08-06-setup-entry.md` Finding 5 recommends naming the pipeline explicitly in the description's closing sentence, then writes "Recorded rather than taken" and hands the decision to the design's owner. The base field at 9b3a666 closes "A tiny reversible edit and pure research stay outside it." The field this push sends closes "A tiny reversible edit and pure research stay outside the pipeline." That is the reviewer's own repair, landed in the same commit that ships the record saying it was not landed. Severity: high. Blocking.

3. **A matrix row reads built on a proof that does not exist.** Row M-518 names `test_the_eval_carries_every_scored_phrase` and adds "the loading itself is scored by the recorded eval run". No eval run is recorded. All ten result cells of Scenario C in `evals/build-pipeline.md` read "to be scored". The consequence reaches the front page: README.md's new paragraph tells a reader to say "attach live-spec to this project" and states that the pack then reads the tree and runs the setup walk, and the only evidence under that promise is a string present in a description field. The skill-review record's Finding 6 says the claim stands unverified until the scenario runs. The matrix row and the README do not say so. Severity: high. Blocking.

4. **Requirement 306's first criterion is universal and its gate reads a list.** R306.1 says the system shall declare each count this repository publishes about its own tree in `guardrails/tree-counts.json`. `guardrails/check-tree-counts.py` iterates that registry, so a count nobody declared is invisible to it. The delta ships several such counts: "fourteen starter files" in docs/adoption.md, "Eleven rows in all: eight ... and three" in adopt/START.md, the entry composition figures in commit 9a5df02's message, and the character ladder of finding 1. Every one I re-measured holds except the ladder, which is the proof that the unheld class is real. The registry's own comment says what no field holds is whether a count is worth publishing; it does not say that an undeclared count is outside the law the requirement states. Severity: medium.

5. **The founding proven by deed is driven by a re-implementation, and half of it is not read from the shipped walk.** `tests/test_setup_entry.py` carries `found()` and `resolve_pack_tree()`. `found()` is honest in its docstring about being phases 0, 2 and 3 of `adopt/START.md` driven mechanically, and it does read the Phase-2 copy table out of the shipped walk through `copy_table()`, so the copy set is genuinely held. `resolve_pack_tree()` duplicates the routing card's six reads in Python instead. `test_card_carries_six_ordered_reads_ending_in_one_action` asserts only that the card holds six numbered items and mentions `installed_plugins.json` and `CLAUDE_PLUGIN_ROOT`. Reordering reads 1 through 5 in the card leaves both tests green while the driver stops matching the card. Row M-517 says "the walk resolves the pack tree" and the prover record's F10 fate says "a founding executed on a throwaway tree". Both read stronger than what runs. Severity: medium.

6. **The readability ratchet let 343 findings into the live set in one push.** The two shipped example run records enter the census at 186 and 157 findings. The headline on docs/PROGRESS.md moves from 4,463 to 4,757. Gate aa holds each document against its own record, and a new document sets its own record on entry, so nothing held this. The promise the headline measures is that a reader gets through a document without stopping. A verbatim review transcript is a record of a run rather than a document a reader reads to learn the method, so either it belongs outside the measured set or the promise it moves is not the promise it appears to move. Severity: medium.

7. **The two shipped examples demonstrate two different shapes for the same promised artifact.** The edition README says the review writes a file carrying each finding's kind and a column for whether the fix was applied or rejected. Run 1 carries a thirty-five row ledger, one row per finding, each with its kind. Run 2 carries one row reading "F1-F30 | see each finding's tag". A reader comparing their own output against the examples gets two answers. Severity: low.

8. **The gate roster's notes cover the smaller of the two new gates.** guardrails/README.md gained a note for gate ae and none for gate ad. The section says it covers the gates whose behaviour takes more than one line. Gate ad carries an argument-list allowlist, a floor claim, a sentence-home arm and an uncommitted-path arm, so it takes more than one line. Severity: low.

9. **Gate k's green over this range proves nothing.** The baseline under `.spec-freeze/` was re-blessed at 16:14 on 2026-08-06. PRODUCT_SPEC.md's last change in this range landed at 16:12 and ARCHITECTURE.md's and TEST_MATRIX.md's at 16:13. So the freeze verifies the delta against a baseline taken from the delta. `guardrails/check-freeze.sh` documents the per-session blessing as its design, so this is a known reach limit rather than a new defect. It is written here because a reader of the push log would otherwise read gate k's green as coverage of these documents. Severity: low.

10. **The last commit's subject claims repairs the delta does not carry.** 996a405 says the suite's five reds were repaired at their roots. The JOURNAL entry beneath it names three installed-copy syncs, one hook reinstall and one freeze re-bless among the repairs. None of those is in the pushed tree. Two repairs are: the cleanup notice added to `scripts/gen-tree-counts.py` and the extract line added to the handover. A fresh clone reproduces neither the reds nor the other three repairs. Severity: low.

11. **The forward queue promises four later rows and three of them do not exist.** NEXT_STEPS.md's "Where the numbers live" section says four surfaces stay outside `guardrails/tree-counts.json`, "each with its own later row", then names them: a number a session writes into chat, a number in a rendered artifact, a count inside a skill body, and a count on an undeclared page. I swept all 191 ROADMAP rows. Only rows 555 and 563 touch the class. Row 555 names the first three in its Non-goal and says they stay unheld, citing no row. Row 563 covers the fourth. So three of the four surfaces have no later row, and when the sentence was written in e68045f none of the four did. The substance holds and the promise of coverage does not. This came out of the c0a6e3a check rather than from that commit, which touched only the forward queue's items. Severity: medium. Repaired in a4a9f0c: the coverage promise is dropped, and each surface now points at what really holds it.

12. **The row that owns the work board does not carry the decision that just moved it to the front.** b1e89e0 records Alexander's word of about 19:17 in DECISIONS.md, JOURNAL.md and NEXT_STEPS.md, and touches ROADMAP.md not at all. Row 166 is the row all three name. It carries neither the frozen task wording nor the validation step in front of the board, and it cites no decision entry. Its Done-when therefore states an acceptance bar that the movement has outgrown, and a session opening the row to run the movement, which is now the front of the queue, reads the morning's widening and none of the evening's. The house pattern points both ways and row 166 sits on the first: five rows carry an in-place widening and row 166 is one of them, having been widened in place earlier the same day for the morning ask; three other rows cite `DECISIONS.md` instead. This one does neither. Nothing is lost, since the decision stands on record in three files. Severity: low to medium. Repaired in 706e6e6: row 166's Done-when takes the second widening of the day, and every clause of the decision reaches it.

Two things I checked and cleared, recorded so the absence rests on the coverage rather than on silence. The rewrite of Requirement 169's criterion 4 replaces a stale four-check sentence with five bullets, and each bullet matches one test method in `templates/test_scaffold.template.py`, including the generated-Reference check that replaced the retired coverage checklist. The removal of the census and the doc-findings gate from `skills/text-audit/SKILL.md` leaves the body's three count claims agreeing with its own list, and the four checks that remain named in command position all carry a ships or host-optional kit in the registry.

Blocking: - finding 1, a published count about this tree is wrong at three homes — closed: fc57abe, re-verified by me on the artifacts at that commit. `evals/build-pipeline.md` line 100 states the field at 493 characters against the other ten ranging 122 to 383, dated 2026-08-06. `docs/prover/2026-08-06-setup-entry.md` lines 47 and 48 state 477 at review time and 493 as shipped, naming the closing-sentence repair as the cause. `docs/skill-review/2026-08-06-setup-entry.md` keeps 477 in its ladder, marks it in its own heading as the review-time reading, and states at line 103 that the shipped field measures 493 at the push of 2026-08-06. I re-measured the shipped field at fc57abe and it is 493 characters. No home now states a figure about the tree that the tree contradicts.
- finding 2, a review record contradicts the artifact shipped in the same commit — closed: fc57abe, re-verified by me. `docs/skill-review/2026-08-06-setup-entry.md` Finding 5's heading now reads "Recommendation; taken at the landing", and its closing paragraph says the orchestrating session took it and quotes the shipped closing, "stay outside the pipeline". I read the shipped description at fc57abe and it closes "A tiny reversible edit and pure research stay outside the pipeline." The record and the artifact agree.
- finding 3, matrix row M-518 reads built on an eval run nobody has run — closed: fc57abe, re-verified by me. The row's parenthetical now reads "the loading itself is unscored; queue row 562 owes the scoring run", so the row names no coverage that does not exist. ROADMAP row 562 stands queued and owes the ten scored cells from real sessions, the run's date and method in the eval file, and a repointed parenthetical. One residue is named rather than closed: README.md's setup paragraph still states plainly that saying the sentence runs the walk, carrying no hedge, and row 562 is what will measure it. That is the pack's own parked state for an unmeasured triggering claim, written here so a later reader sees which claim the row holds.
