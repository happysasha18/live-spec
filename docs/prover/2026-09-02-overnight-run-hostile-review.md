# Prover record — 2026-09-02, hostile review of the overnight run (six rows, two prose repairs, one renumber)

PUSH-REVIEW

Range: 11987b80..521f38f7
- 521f38f7 claim: row q-805 → in-work (lane/q-805-cut-invented-number-ratchets) — landed on main by another session at 01:44, while this review was running; named here for the gate's sake, not reviewed as part of the overnight run
- 667ac780 NEXT_STEPS.md: whole-night LIVE STATE for the overnight run, heals three INV-242 warns, fresh prompt for the next session
- 49b4813f Fix two prose regressions the quiet-tree suite caught: PRODUCT_SPEC.md's size-ratchet (q-48's new criteria), ARCHITECTURE.md's shout+redundancy (q-48/q-804's PLAN citations)
- 1c1d0800 q-804: fix M-621 row-id collision with q-48 (renumber to M-624/625/626); drop stale INV-244 map entry left over from pre-q-436 fork point
- f69f0340 q-804: two of the three lane-net arms ship for real, row stays open on the third
- 1467f480 sync-skills: repair q-803's own drift (communicator, live-spec-base, publish, text-audit-pack)
- d0bbc72b q-803: strip inline provenance citations from skill rule prose
- f79e74b9 sync-skills: repair q-537 drift caught after q-436's merge (product-prover-pack, spec-author)
- beaf953d q-501: front page accurate, project count dropped, July gap closed
- d6b52e35 claim: row q-803 → in-work (lane/q-803-strip-provenance-citations)
- b5914865 q-436 lands: the value-space in-between forcing step, beside q-437's axis-verdict sweep
- bf322e4c claim: row q-501 → in-work (lane/q-501-front-page-accuracy)
- 68539f6e q-48: pack-side automatic-fetch success-measure contract (spec + checker + tests)
- ef671c4a claim: row q-48 → in-work (lane/q-48-automatic-fetch-contract-spec-delta)
- 1b3d5b42 q-54: confirm live-spec's own profile is not a legitimate stand-in; leave row open
- b29231a3 Save tonight's productization-phase brief as a task for after PLAN.md closes
- 0a3fa8ec claim: row q-54 → in-work (lane/q-54-founding-line-live-spec)
- 26a36e17 claim: row q-804 → in-work (lane/q-804-lanes-net-for-real)
- cad8403f claim: row q-436 → in-work (lane/q-436-co-occurrence-value)
- 534cb16b Write tonight's overnight-run prompt: eight closeable rows, worktree-isolated, one honest report
- b0f8929c Closing adversarial review of tonight's session; sweep the [default]-tag-order defect to its 19 siblings
- f7382a15 The one-file-in-the-tree check learns to stand down in a git-less scratch copy
- 4f0b760c NEXT_STEPS.md: tail-end cleanup heals landing 0a9a431a, catches up on tonight's last five fixes
- 16d59df9 Re-point five pins that drifted again after 084c3eb4's communicator trim
- bb084ed5 docs/prover: fresh adversarial review record — q-385/q-804/q-436 reopened, criterion 15 narrowed
- 5c8ebb87 Fix shipped-language gate: allowlist plan_checks.py's real grep string, mark PLAN.md's direct quotes user-language
- 7c25768c plan_checks.py: back five done tasks, and drop pytest from q-802's key
- 0a9a431a PLAN.md: three findings from tonight's traceability/done-mark audit
- 084c3eb4 communicator's body drops back under its size ideal, q-536's rulings moved to references
- 16878f0c Cut the scissors contrast frame from q-398's new referral criterion
- 476f5246 Fix INV-196 trailing-tag order so its declaration paragraph is found
- 0d668348 config-health: routing hook installed, communicator's copy re-synced
- 4c95f679 NEXT_STEPS.md: name the real date behind plan-9's "Alexander's own word" deferral
- 7551744b docs/language-rule-coverage.md: rebuild off hooks/register_judge_core.py's source note
- fc5c2792 PRODUCT_SPEC.index.md: rebuild the generated code-to-location table off today's spec edits
- ce3a7e30 spec: Requirement 163's heading loses a stray [default] tag it never should have carried
- 6ea75939 architecture pins: re-point nine line pins that drifted when today's session edited skills/live-spec-base/SKILL.md and skills/communicator/SKILL.md
- 97799c24 NEXT_STEPS.md: full suite is red, 30 failures -- state it honestly
- 29faa996 NEXT_STEPS.md: re-heal two commits whose heal phrase wrapped across a line
- d35dc003 README rewritten short, product-prover's shape: what you get, then how, install last
- b1f0963c NEXT_STEPS.md catches up with today's full landing range
- 67bd98d1 q-386's convergence test lands
- 1280cd99 q-536's fourteen rulings land for real
- 287e019c q-163's M-620 matrix row lands for real
- 67f9ce6e q-802 lands: the snapshot's baseline only ever moves for what a delivery actually declared
- caa7f6a7 Old leftovers actually confirmed dead, not just old, are cut
- adceb60b q-166: board.html already answers the daily ask he remembered
- cf244b5b The false Known Issue claim gets a guard against a fourth return, not a fourth manual fix
- e3b745b1 q-501 / the front page's own false Known Issue closes, again
- 54bde341 Revert TEST_MATRIX.index.md, matrix/test-author.md, skills/communicator/SKILL.md from the q-398 commit
- e2a0e8c4 q-398 lands: a routing preamble now reaches an adopted project automatically
- 2858c023 q-800: close the playbook-repo row now that his permission this session is the read that was waited on
- c30491b9 q-48: the deferred trigger already fired 24.07 -- the row catches up
- 8905d7af q-536 lands: the fourteen communicator collisions each get their ruling, in the rule's own text
- 24152152 q-163: the wiring proves itself; the host field-leg still needs its own project's window
- 4f86dfd9 q-591 lands: M-313's cited home follows the requirement that actually moved
- d673c75c q-166 re-marked queued, not needs-his-eyes -- nothing built yet to look at
- 3147d6e2 Journal: plan-2 re-recorded, q-437 lands, sync catches up
- 14808ef2 plan-2 re-recorded: all thirty-five traces fresh, 32 of 35 hold
- 7e3f32e1 q-437 lands: the axis-verdict sweep runs at every level, not just siblings
- b9708261 plan-10 lands: every done mark now proves itself, none turned out false
- a488854f plan-10: sixteen done marks now carry a real check or a dated reading, not a typed mark
- bf319751 plan-10's own instrument: a done mark now has to prove itself
- 3d4b8ae4 q-803: a skill's rule states itself, the journal carries who said it
- 5aacf3aa Concurrency without a stated safety measure gets its own rule
- 62394f45 q-576 lands: the page it asked for exists, and the tree holds nothing ungrounded
- c8adff22 q-576: the twelve unlabelled survivors and one new constant get the same source-admission every other unproven number already carries; r14's stale note is corrected
Files read: `.live-spec/overnight-prompt-2026-09-01.md` (the brief and its bar), `PLAN.md` (q-436,
q-501, q-803, q-48, q-54, q-804, q-385 rows in full, plus the file's own "Words used here" mark
legend and §Blockers), `NEXT_STEPS.md` whole, `CLAUDE.md`, `spec/parallel-lanes.md` (Requirements
85, 86, 87, 88 as they now stand), `spec/success-measure-feed.md` (Requirement 318, before and
after the trim), `spec/design-spec-review.md` (Requirement 265 criteria 11–15),
`architecture/guardrails.md` (INV-324's owns entry, the pin list, the notes block),
`architecture/seams.md` (the two rewritten rows), `matrix/guardrails.md` (M-462, M-621),
`matrix/parallel-lanes.md` (M-375, M-381, M-624, M-625, M-626), `matrix/test-author.md` (M-620),
`TEST_MATRIX.index.md`, `README.md`, `skills/spec-author/references/facet-sweep.md`,
`skills/product-prover-pack/SKILL.md`, `skills/live-spec-base/SKILL.md` (rules 7 and 36, the
lane-open act), `skills/director/references/lanes-and-pen.md`, `skills/build-pipeline/SKILL.md` and
`references/project-setup.md`, `adopt/ADOPT.md`, `adopt/START.md`, `adopt/install-ratchet.sh`,
`adopt/install-scaffold.sh`, `MIGRATION.md`, `guardrails/pre-push`,
`guardrails/check-landing-next-steps.py`, `guardrails/check-config-health.sh` (the new arm),
`guardrails/check-merge-base.sh`, `guardrails/check-worktree-line.sh`,
`guardrails/check-prover-record.sh`, `scripts/plan_checks.py` (the three new keys),
`scripts/check-success-measure-feed.py`, `tests/test_row_id_uniqueness.py`,
`tests/test_no_inline_provenance_citation.py`, `tests/test_skill_count_agrees.py`,
`tests/test_host_count_agrees.py`, `tests/test_plan_done_marks_are_backed.py`,
`tests/test_lane_net_arms.py`, `tests/test_landing_next_steps.py`, `tests/test_convergence_locks.py`,
`~/tlvphotos/.live-spec/agent.md` and `.live-spec/agent.md` (q-385's revisit trigger),
`docs/prover/README.md` and the two most recent records (this record's own shape).

Checks run, each run here rather than taken from a commit message: (1) `bash
guardrails/check-config-health.sh` — OK, exit 0; (2) `python3 guardrails/check-matrix-reference.py
TEST_MATRIX.md TEST_MATRIX.index.md` — OK, 557 of 557 rows, committed Reference equals the fresh
build, 412 anchors agree; (3) `python3 guardrails/check-index-generated.py PRODUCT_SPEC.md
PRODUCT_SPEC.index.md` — OK, 405 of 405, all 33 parts named, 314 requirement numbers each claimed
once; (4) `python3 guardrails/check-architecture-reference.py ARCHITECTURE.md ARCHITECTURE.index.md`
— OK, 24 of 24 nodes (the ARCHITECTURE equivalent is this gate, not `check-index-generated.py`,
which refuses that pairing by design as a vacuous pass — a refusal, not a defect); (5) `python3
guardrails/check-size-ratchet.py PRODUCT_SPEC.md` — OK at 185.8 bytes/criterion against the
recorded bound 185.8, `guardrails/spec-ratchet.json` confirmed untouched in the range; (6) `python3
scripts/spec-style-lint.py --gate` over `ARCHITECTURE.md`, `PRODUCT_SPEC.md` and `NEXT_STEPS.md` —
0 errors, 0 warnings, 0 stale waivers each; (7) `python3 scripts/spec-redundancy-precheck.py
ARCHITECTURE.md` — 17 candidates / 15 open, identical to the pre-session baseline measured by
extracting `534cb16b` into a scratch tree and running the same command there, so the night added
no redundancy; (8) `python3 -m pytest -q` over the thirteen files covering everything touched —
`test_traceability`, `test_plan_done_marks_are_backed`, `test_composition_axes`,
`test_success_measure_feed`, `test_lane_net_arms`, `test_guardrail_fixture_proofs`,
`test_no_inline_provenance_citation`, `test_matrix_reference`, `test_index_generated`,
`test_architecture_reference`, `test_size_ratchet`, `test_convergence_locks`,
`test_host_count_agrees` — 288 passed, 2 skipped; (9) `python3 -m pytest -q
tests/test_readme_stance.py tests/test_skill_count_agrees.py tests/test_four_checks_contract.py` —
27 passed; (10) `python3 scripts/preshow-register-lint.py README.md`, `python3
guardrails/check-one-name.py README.md`, `python3 scaffold/guardrails/check_completeness.py` — all
three clean, q-501's own named commands; (11) `python3 -m pytest -q tests/test_landing_next_steps.py`
— **1 failed**, see finding 1; (12) `python3 -m pytest -q` over the whole tree, started at
`667ac780` and finished 17m21s later — 3 failed, 2736 passed, 5 skipped, 1 error. Each red read
rather than counted: `TestGateA_ProverRecord::test_real_repo_passes` is the gate this record exists
to satisfy; `test_real_repo_range_refreshes_next_steps` is finding 1, reproduced twice on its own
before and after; `TestGateShippedLanguage::test_gate_green_on_the_swept_tree` (4 Cyrillic offences
at `PLAN.md:1836-1839`) belongs to `521f38f7`, a claim commit for a new row `q-805` another session
landed on main at 01:44 while this run was still going — outside this record's range and none of the
overnight run's doing; and the single ERROR, `tests/test_worker_restore_run_scope.py`, passes clean
on its own re-run and is an artifact of the tree moving under a live suite. Against the reviewed
range the suite therefore carries exactly one real red, finding 1; (13) `bash
guardrails/check-worktree-line.sh .` exit 1 and `bash guardrails/check-merge-base.sh` exit 0 run
directly, to confirm the two new scripts carry real exit codes rather than printing JSON and
returning 0; (14) a full-tree grep for every caller of both new scripts, and for `M-621`–`M-627`,
archives included; (15) `python3 scripts/plan_checks.py` — exit 0, `python3
guardrails/check-board.py` — OK, and `bash guardrails/check-pin-drift.sh` — OK, 184 pins plus 39
range pins, so the night's skill-file edits did not re-break the pins `16d59df9` repaired earlier in
this range; (16) the grep q-803's own acceptance names, run against a scratch
extraction of `3d4b8ae4` and against the live tree, to measure the sweep rather than trust it;
(17) `git show --stat` on each of the five landing commits against its own message, looking for
scope creep — none found, every stat matches its subject line.

Findings: six. None repaired here — this review reports, the orchestrating session triages. Two
block a push as the tree stands.

1. **The INV-242 heal did not take, and the suite is red on `667ac780` because of it.**
   `guardrails/check-landing-next-steps.py`'s heal road requires the literal phrase `heals landing
   <shortsha>` in the healing commit's own MESSAGE. `667ac780`'s message reads "heals three INV-242
   warns" and names no sha; the three shas were written into `NEXT_STEPS.md`'s body text instead,
   where the checker never looks. `python3 -m pytest -q
   tests/test_landing_next_steps.py::test_real_repo_range_refreshes_next_steps` fails on HEAD,
   printing `severity: error` for `b5914865` (q-436), `beaf953d` (q-501) and `d0bbc72b` (q-803) —
   the three the note claims to have healed. Two consequences. First, `NEXT_STEPS.md`'s LIVE STATE
   opens with a false statement about its own commit ("Heals landing b5914865… beaf953d…
   d0bbc72b…"). Second, this checker rides the suite deliberately, and the suite is gate b, so the
   push is blocked until a later commit that touches `NEXT_STEPS.md` carries the three phrases in
   its message. This is the second occurrence of the same class: `29faa996` in this very range
   reads "re-heal two commits whose heal phrase wrapped across a line". **Stands.**

2. **Two of q-804's three shipped arms have no caller anywhere, and their spec criteria dropped
   `[target]` regardless.** `guardrails/check-merge-base.sh` and `guardrails/check-worktree-line.sh`
   both work and are honestly red-proven on hermetic scratch repos. Neither is invoked by anything
   except its own tests. A full-tree grep for both names returns only the scripts' own headers,
   prose in `PLAN.md`, `architecture/guardrails.md`, `architecture/seams.md` and
   `matrix/parallel-lanes.md`, and `tests/test_lane_net_arms.py` /
   `tests/test_guardrail_fixture_proofs.py` / one comment in `tests/test_traceability.py`. The
   landing walk names no merge-base call — not `skills/live-spec-base/SKILL.md`'s lane-open act or
   its one-row-per-landing rule, not `scripts/open-lane.sh` (which names exactly one guardrail,
   `fence-refresh.sh`), not `skills/director/references/lanes-and-pen.md`, not
   `guardrails/pre-push`. The adoption walk names no worktree-line call —
   `adopt/ADOPT.md`'s "Installing the gates" section runs three scripts and names four guardrails,
   none of them this one; `adopt/START.md`, `adopt/install-ratchet.sh` and
   `adopt/install-scaffold.sh` carry zero hits; and the catch-up walk, `MIGRATION.md`, names
   fourteen other scripts and not this one. Naming the path in an executable or a walk document is
   how every other check in this tree is wired: `guardrails/pre-push` invokes twenty-four checks by
   literal path, and `check-agent-card.py` — the exact precedent for an adoption-time gate — is
   named at `adopt/ADOPT.md:37` and run at `guardrails/pre-push:284`. Why it matters: Requirement 86
   criterion 5 now says the system *shall* red "ahead of the landing gate" and Requirement 88
   criterion 4 says the gate is "read at the adoption/catch-up walk", both with the `[target]` tag
   removed and both `TARGET_ROW_OWNERS` entries dropped — so the promise is recorded as kept while
   nothing performs it, and `architecture/guardrails.md:149` states outright that this node "runs
   three of their checks" when it runs one. The third arm is genuinely wired and is not part of this
   finding: `check-config-health.sh`'s primary-tree-holds-main arm is reached at
   `guardrails/pre-push:241` on every push, and I confirmed it reds by deed. **Stands.**

3. **`NEXT_STEPS.md`'s "Prompt for the next session" tells the next session the run "closed all
   eight of the prompt's own rows".** It did not, and the same file's LIVE STATE block sixty lines
   above says so correctly ("Six actually moved tonight; two stayed correctly untouched"). Three of
   the eight stand open on the board right now — `q-48` and `q-54` as honest partials, `q-804` on
   its third arm — and `q-163` and `q-385` were never touched. Verified against `bash
   scripts/state-probe.sh` and `grep -n "^### " PLAN.md`. The block is the one a fresh session with
   clean context is instructed to act on, and it is the half of the file most likely to be read
   alone. **Stands.**

4. **The law that should have caught tonight's `M-621` collision is a vacuous pass, and the night
   wrote a manual workaround into the next session's prompt rather than repairing it.**
   `tests/test_row_id_uniqueness.py::test_every_matrix_id_is_unique` scans `TEST_MATRIX.md` alone
   with the pattern `^\|\s*(M-\d+)\s*\|`. That file has carried zero matching rows since `35bc12e8`
   moved all of them into `matrix/*.md`; `grep -cE '^\|\s*M-[0-9]+\s*\|' TEST_MATRIX.md` returns 0
   and the same grep over `matrix/*.md` returns 557. The test therefore compares an empty set to an
   empty set and passes, with no non-empty guard — its own sibling `test_every_plan_id_is_unique`
   carries one (`assertGreater(len(pairs), 20)`), and this tree's `guardrails/nonempty_input.py`
   states the rule the file breaks. Its docstring describes the 2026-08-19 `M-547`/`M-548` incident
   it was written for; tonight's `M-621` collision is that incident repeating, and it reached the
   merge because nothing was holding the law. The renumber that followed is clean (finding-free, see
   below), but `NEXT_STEPS.md:214` now instructs the next session to run `grep -rhoE "^\| M-[0-9]+"
   matrix/*.md` by hand before trusting a new row number — a human procedure standing in for a
   broken check. The defect predates this range; carrying a workaround forward instead of naming it
   does not. **Stands.**

5. **The number 31 is stated twice and no command produces it.** `PLAN.md`'s q-803 row says a sweep
   found "31 hits for 'his word' / 'owner's word' across `skills/*/SKILL.md` and
   `skills/*/references/*.md`", and `NEXT_STEPS.md:35` repeats it as "31 inline… citations swept".
   Extracting `3d4b8ae4` — the commit that wrote the row — into a scratch tree and running the row's
   own grep there returns 20 (21 case-insensitively, and no variant spelling reaches 31: I tried
   occurrence counts, recursive reach, `his own word`, `owner's own word`, `his words`, and the
   installed mirror). The live tree returns 9. The note's own breakdown settles it against itself:
   11 removed + 7 left as behavioural + 2 exempted = 20, exactly the measured before-count, with the
   "1 stripped with no citation possible" a subset of the 11. The work is right; the headline number
   is not, and it is the kind of unsourced figure this project's own standing rule forbids. **Stands.**

6. **`q-804` is left on 🔄 with nothing in hand.** `PLAN.md`'s own legend reads "🔄 in hand". The
   lane branch `lane/q-804-lanes-net-for-real` is gone, `git worktree list` shows no lane, and no
   worker runs. The mark is the leftover from the claim commit `26a36e17`, which flipped the row
   ⬜→🔄 on opening; `git show 534cb16b:PLAN.md` confirms it stood ⬜ before the night. `q-48` — an
   identically-shaped honest partial from the same night — was flipped ⬜→🔄 at `ef671c4a` and
   returned to ⬜ at its landing `68539f6e`, so the two rows carry different marks for the same
   state. `bash scripts/state-probe.sh` prints q-804 as `<-- NEXT` on the strength of that mark, so
   the next session is pointed at a row nobody is working. **Stands.**

What the review checked and found clean, so the absence above rests on coverage rather than silence:

- **The `M-621` renumber is complete and correct.** `M-624`, `M-625`, `M-626` each name one row;
  `M-622`, `M-623` and `M-627` appear nowhere in the tree; `M-621` belongs to q-48 alone and
  `M-620` to q-163. Both cross-references were carried along — `matrix/parallel-lanes.md:14` inside
  M-375 now cites M-624, and `:21` inside M-381 now cites M-626. `TEST_MATRIX.index.md` is genuinely
  fresh: `check-matrix-reference.py` reports the committed Reference equal to the fresh build over
  all 557 rows, with INV-198→M-624, INV-199→M-625, INV-201→M-626 and INV-324→M-621. `attic/` and
  `docs/queue-archive/` carry no reference to any id in the block.
- **The two prose repairs lost no requirement content.** Read clause by clause against
  `git show 49b4813f`. Requirement 318's six trimmed criteria drop only redundant possessives and
  restatements — "one named experiment block" → "one named block" keeps its antecedent in the
  clause's own *when*; "the feed's generation timestamp" → "the feed's timestamp" names the only
  timestamp the feed has; clause 9's "the host pole Requirement 267 draws for host-specific data"
  → "Requirement 267's own host pole" still cites the pole by number. No *shall*, no red condition,
  no anchor changed. The `ARCHITECTURE.md` reword replaces a bare capitalised `PLAN` with
  `PLAN.md`'s row and re-points one restated phrase at the pin two lines above it; that pin,
  `architecture/guardrails.md:115`, still names the arm in full ("+primary-tree-holds-main arm
  INV-198"), so the pointer resolves. The ratchet file was not edited to make room.
- **q-436's ✅ holds.** The duty is real text in `skills/spec-author/references/facet-sweep.md`, the
  lens is real text in `skills/product-prover-pack/SKILL.md`, Requirement 265 criterion 15 describes
  a built step with its `[target]` and GAP line gone, and `scripts/plan_checks.py`'s new `q-436` key
  runs four greps plus `test_composition_axes.TestCooccurrenceValueForcingStep` rather than asking
  whether a file exists.
- **q-803's ✅ holds.** `tests/test_no_inline_provenance_citation.py` reds on phrase-plus-date over a
  real reach with one declared exemption guarded by a second test, and the four facts that lived
  nowhere else were written into `JOURNAL.md` in the same commit before their citations came out.
  Only the count in finding 5 is wrong.
- **q-501's ✅ holds, and every number on the page re-derives.** No project count appears anywhere in
  `README.md` or `OVERVIEW.md`. "Twelve working skills" is correct: `skills/` shows fourteen folders
  on this machine, but `product-prover` is an untracked external clone that
  `test_skill_count_agrees.py`'s structural `.git` fence excludes, leaving thirteen tracked folders
  and twelve working skills once the rulebook is set aside. "Twenty-two shared rules" matches
  `grep -cE "^[0-9]+\. " skills/live-spec-base/SKILL.md`. `matrix/guardrails.md`'s M-462 carries the
  narrowing and its date. The `Checked by reading on 02.09.` phrasing is one of the two shapes
  `tests/test_plan_done_marks_are_backed.py` accepts, and that test is green.
- **The two honest partials stay honest.** `q-48` claims only the pack-side contract, keeps
  `INV-21`'s `[target]` live, and names the host-side legs as clauses 9 and 10 of its own
  requirement; its twelve checker cases are green and the checker has no caller here on purpose,
  since clause 3 hands the call to the host. `q-54` claims nothing built and names the one line only
  a `~/tlvphotos` session can write; I confirmed the wish still sits unhandled in that project's
  inbox.
- **q-385's non-action is correct, not a rationalization.** Its revisit trigger is "the first host
  declaring a contract in its card", and both cards in reach say otherwise in their own words:
  `~/tlvphotos/.live-spec/agent.md` reads "None today" under *Contracts this agent publishes*, and
  this pack's own `.live-spec/agent.md` reads "None". Its definition of done also demands three arms
  "red-proven against a real producer and consumer", which no fixture can stand in for — the
  distinction from q-804, whose own definition of done demanded no real host, is written in the two
  rows themselves rather than invented to justify the skip.
- **The size-ratchet trim was lawful when it was made, and the owner has since retired the law.**
  Recorded so a later reader does not mistake the sequence: `49b4813f`'s trim was forced by
  `check-size-ratchet.py`'s recorded bound, and at 01:30–01:43 — after that commit and during this
  review — the owner ruled that class of gate out entirely, which `521f38f7` opened as row `q-805`.
  The trim is not a finding either way: it cost no requirement content, and the gate that demanded
  it is now scheduled for removal by his own word rather than by anything this review found.
- **No commit in the range creeps past its own message.** `git show --stat` on `b5914865`,
  `beaf953d`, `68539f6e`, `f69f0340` and `1c1d0800` matches each subject line, and the two
  `sync-skills` commits touch installed copies alone.

Blocking: two
- the unhealed INV-242 landings (finding 1): stands — the suite is red on HEAD, so the push cannot
  go until a later `NEXT_STEPS.md` commit carries `heals landing b5914865`, `heals landing
  beaf953d` and `heals landing d0bbc72b` in its own message.
- two orphaned arms with their `[target]` tags dropped (finding 2): stands — either the two walks
  are edited to name the scripts, or the two criteria go back to promising what is not yet
  performed. Recording it as blocking because the spec now states as done a thing the tree does not
  do, which is the exact condition this record's own requirement exists to catch.
