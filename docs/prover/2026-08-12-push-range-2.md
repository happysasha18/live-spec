# Prover record — 2026-08-12, the second morning range: stage-2 batch 2, stage 3, and the repairs the review demanded

PUSH-REVIEW

Range: e8900d9..29710be — 31 commits. `origin/main` resolves to e8900d9, so this is the range the
push sends. The adversarial read covered the 26 commits from e8900d9 to 493f094, which is the whole
of the morning pass; the five commits above 493f094 are this walk's own repairs, written at the seat
in answer to the review's blocking findings and to the push gate's own red, and each is named with
what it carries.

- 29710be The suite wall-time budget is re-derived at 1280 s, and the whole rise is traced to one file measuring 640 s
- 0a1c364 The push review's fourteen findings get their homes: eight new queue rows, one reopened, one closed
- 2d14354 The mirror sync reaches its own branches again, five more architecture pins land, the tree counts rebuilt, the morning pass gets its journal entry
- 999a28c Three skill reviews cover the skill bodies this push carries
- 46de0a6 The prose-findings ratchet goes green again, and two architecture pins that drifted five lines are repointed
- 493f094 Standing instructions carry the visible task list and the one-clean-suite rule
- 630722e The live block carries the morning pass
- 9e94f75 Every worker-restore finding says what the shell did with the command
- 6ed17ec The tlvphotos reply lands in the inbox
- 5b3cead Plan v3 marks batch 2's close and stage 3's C1/C2 close
- e8c4a3a Repoints thirteen ARCHITECTURE.md pins
- 05560d0 Batch 2's S5 closes with rule 32's verdict page
- e17eea9 Rule 7's four restorations, the retired rule-30 number named at the head
- 1cadbe6 Worker-restore counting start narrowed to 2026-08-12T06:06:00Z
- 0e95f45 The pin guard reaches the rule-price page's 53 range pins
- 4b20af9 Batch 2's S4 repoints five ARCHITECTURE.md pins
- 62447e8 Worker-restore gate: one dated finding carried as history
- deba91b S3 checks batch 2's rewrite of base rule 32
- dc78db9 Row 593's three rule-count copies point at one home
- 0ac3b19 Base rule 32 rewritten, 2,205 to 1,449 bytes
- 0bf9844 Batch 2's S1 records rule 32's price and requirements
- 4dfdc66 progress-report.py stops storing and printing a run-to-run comparison
- 39a8bb9 Stage 3 C2's verdicts land as records
- 7520a42 sync-mirrors.sh stops loudly when gh cannot answer
- 89cbcf7 The D8 entry gets the 3,095-runs figure's provenance
- 0b218bb The plan's volume row re-measures at 72,929 bytes
- 7521782 Matrix row M-313 drops its stale build-pipeline home
- 3aec8e7 Stage 3 C2: every check with no dated catch gets its verdict
- 7d59845 Stage 3 C1: an evidence line for each of the 25 checks
- a5b94f8 The resume block carries the batch-2 pass composition
- 3c5836b The second push stamp lands in the live block

Files read: `skills/live-spec-base/SKILL.md` at both ends of the range and at HEAD; `ARCHITECTURE.md`;
`ROADMAP.md`; `DECISIONS.md`; `NEXT_STEPS.md`; `TEST_MATRIX.md` row M-313; `README.md` line 74;
`JOURNAL.md`; `docs/PROGRESS.md`; `guardrails/rule-census.json`; `guardrails/progress-baseline.json`;
`guardrails/check-worker-restore.py`; `guardrails/check-pin-drift.sh`;
`guardrails/check-prover-record.sh`; `guardrails/check-skill-review.sh`;
`guardrails/check-doc-findings-bound.py`; `guardrails/pre-push`; `guardrails/README.md`;
`guardrails/archformat.py` output; `scripts/sync-mirrors.sh`; `scripts/progress-report.py` diff;
`scripts/rule-census.py`; `scripts/spec-freeze.py`; `skills/build-pipeline/SKILL.md` line 73;
`skills/communicator/SKILL.md` header block; `skills/communicator/references/words.md` line 30;
`skills/product-prover/SKILL.md` and `skills/product-prover/README.md` in full;
`tests/test_worker_restore.py`; `tests/test_compaction_discipline.py`; `tests/test_progress_report.py`;
`tests/test_clean_context_review.py`; `tests/test_resume_rederive.py`; `tests/test_doc_findings_bound.py`;
`tests/test_mirror_autosync.py`; `tests/test_mirror_editions.py`; `tests/test_class_hunt.py`;
`.live-spec/batch2-s1-rule32-2026-08-12.md`; `.live-spec/batch2-s3-rule32-check-2026-08-12.md`;
`.live-spec/batch2-verdicts-2026-08-12.md`; `.live-spec/stage3-check-evidence-2026-08-12.md`;
`.live-spec/stage3-verdicts-2026-08-12.md`; `.live-spec/r5-rule-prices-2026-08-11.md`;
`.live-spec/culling-plan-v3-2026-08-10.md` and its three deltas of 2026-08-12;
`inbox/2026-08-12-tlvphotos-reply-worker-restore-finding.md`; `docs/prover/README.md`;
`docs/prover/2026-08-12-batch1-range.md`; `docs/skill-review/` in full; `.gitignore` line 24.

Checks run: `git log --format='%h %ad %s' e8900d9..HEAD` and `git rev-list --count e8900d9..HEAD` —
30 commits; `git rev-parse --short=7 origin/main` — e8900d9, so the base the gate resolves is the
base named above. The rest, each with its result:
- The closing full suite, run alone with no worker writing the tree,
  `python3 -m pytest -q` — **3 failed, 2491 passed, 1 skipped in 993.31s**. The three:
  `tests/test_doc_findings_bound.py::test_the_real_repository_passes`, and the two gate arms that
  read it back, `tests/test_guardrails.py::TestGateA_ProverRecord::test_real_repo_passes` and
  `::TestGateB_Tests::test_real_content_passes`. All three are answered below.
- `python3 guardrails/check-doc-findings-bound.py` — red at the start of the walk, green after the
  repairs: **OK, 123 live documents, 22 held at zero, none above its record**.
- `python3 guardrails/check-tree-counts.py` — red at the start with three faults on `README.md`,
  green after `python3 scripts/gen-tree-counts.py`.
- `bash guardrails/check-skill-review.sh` — red at the start over three skills, green after the
  three records landed in 999a28c.
- `bash guardrails/check-pin-drift.sh` — OK over 209 pins, and OK over the 53 range pins of the
  rule-price page. Green both before and after the pin repairs, which is finding 6's whole point.
- `bash guardrails/check-freeze.sh` — GREEN over three files, re-baselined twice for the
  `ARCHITECTURE.md` pin repairs.
- `python3 guardrails/check-worker-restore.py` — OK, 2,633 command lines read, one finding stamped
  before the counting start carried as history, outcome `declined`.
- `python3 guardrails/check-board.py` — OK.
- `python3 -m pytest tests/test_worker_restore.py -q` — 97 passed.
- `python3 -m pytest tests/test_release_tier_rule.py tests/test_compaction_discipline.py
  tests/test_progress_report.py tests/test_clean_context_review.py tests/test_resume_rederive.py
  tests/test_architecture_format.py -q` — 59 passed.
- `python3 -m pytest tests/test_architecture_format.py tests/test_mirror_autosync.py
  tests/test_mirror_editions.py tests/test_tree_counts.py tests/test_doc_findings_bound.py -q` —
  88 passed, after the repairs.
- `python3 -m pytest tests/test_mirror_autosync.py -q` — 5 passed with the fix in place, and
  **2 failed** with the pre-fix two lines restored, which is the red proof of the new tests.
- A shell reproduction of the errexit defect:
  `bash -c 'set -euo pipefail; err="$(sh -c "echo boom >&2; exit 7" 2>&1 >/dev/null)"; st=$?; echo REACHED'`
  exits 7 and never prints REACHED; the guarded form prints `REACHED st=7`.
- A byte measure of rule 32's span,
  `awk 'NR>=560 && NR<=574' skills/live-spec-base/SKILL.md | wc -c` — **1449**, matching the claim.
- A verbatim substring test of all ten requirement quotations from
  `.live-spec/batch2-s1-rule32-2026-08-12.md` against the rewritten span — **ten of ten found**.
- The campaign's own volume command — **72466**, matching the S5 verdict exactly.
- All 21 `skills/live-spec-base/SKILL.md` pins in `ARCHITECTURE.md` read out with
  `python3 guardrails/archformat.py --pins ARCHITECTURE.md` and compared by hand against the rule
  or row each label names — **seven landed wrong, all seven now repaired and re-verified**.
- All 53 range pins in `.live-spec/r5-rule-prices-2026-08-11.md` compared against the lines they
  name — **53 of 53 land exactly**.
- A comparison of `guardrails/rule-census.json` before and against after the re-measure: **no
  ceiling rose**, `skills/live-spec-base/SKILL.md` fell 70 to 68, and one row was added for the
  tlvphotos deposit at its own seven findings.
- Gates run clean, exit 0 each: `check-named-checks.py`, `check-board.py`,
  `check-authority-anchor.py`, `check-doc-rotation.py`, `check-doc-bound.py`,
  `check-hooks-can-fire.py`, `check-judge-listed.py`, `check-agent-card.py`,
  `check-every-gate-can-fail.py`, `check-landing-next-steps.py`, `check-index-generated.py`,
  `check-ci-mirror.sh`, `check-config-health.sh`, `check-shipped-language.sh`,
  `check-skill-loadability.sh`, `check-prototype-fence.sh`, `check-cleanup-notice.sh`.
- `python3 scripts/preshow-register-lint.py skills/live-spec-base/SKILL.md` — OK.
- `python3 scripts/spec-style-lint.py --tier full` over the new journal entry's own lines — clean.

Findings: sixteen. Four blocked the push and all four are closed in this range. Twelve stand as
recorded debts, each with a queue row and a named owner. The batch's own work — rule 32's rewrite —
is the cleanest thing in the range and survived every check aimed at it.

**The four that blocked, and how each closed.**

1. Gate aa refused the push, and this range caused it. Four documents stood above their recorded
   prose ceilings: `NEXT_STEPS.md` had been repaired to zero and carried 21 sentences past the
   25-word cap after the rewrites of 09:51 and 09:53; `guardrails/README.md` had risen 48 to 55 on
   the fourteen lines added at 09:51; `ROADMAP.md` had risen 215 to 216 on one contrast frame in
   row 588; and the tlvphotos reply of 09:44 was live and unrecorded. Closed in `46de0a6`. The
   ceilings were held rather than raised: the resume file's sentences were split, with every hash,
   byte count, date, queue row and quoted word carried over, and the record was rewritten only
   after every repair had landed. Running that command first would have given the resume file a
   fresh ceiling of 21 and turned the gate green over an unrepaired document.

2. Gate ad refused the push, third range running. `README.md` published 6,440 and 5,197 line counts
   against a tree returning 6,435 and 5,197 — the ten lines rule 32 lost and the five rule 7 and
   the rulebook head gained. Closed in `2d14354` by `python3 scripts/gen-tree-counts.py`. The class
   repair the previous two ranges also asked for takes queue row 600: every shortening batch
   changes a skill body by construction, so the rebuild belongs in the batch recipe's closing step.

3. Gate s refused the push, over three skills. Closed in `999a28c` with three records written by a
   reviewer with clean context that authored none of the changes. Its own verdict on each skill is
   that the skill holds.

4. `scripts/sync-mirrors.sh:463` could reach none of its own branches. `7520a42` split the absence
   check into three answers so an authentication or network failure would stop the sync loudly, and
   the script runs under `set -euo pipefail`, where an assignment whose command substitution exits
   non-zero is itself the failing simple command. The loud message the commit exists to print was
   unreachable, and so was the pre-existing `no mirror repo yet` skip. Queue row 597 stood `*done*`
   on it and `NEXT_STEPS.md` published the claim as fact. Closed in `2d14354`: the status is seeded
   and the assignment guarded with `|| gh_view_status=$?`, and two tests read the script's own two
   lines and run them under a failing `gh`. Both were proven red against the pre-fix script. Row 597
   is reopened, corrected and closed a second time.

**What held, and it is the substance of the range.** Rule 32's rewrite is exact. All ten requirement
sentences from the batch's own inventory are present verbatim in the rewritten span, tested as
substrings rather than trusted from the S3 page. Nothing was softened. The span measures 1,449 bytes
against the claimed 1,449, the campaign's volume command returns 72,466 against the S5 figure of
72,466, and the decomposition reconciles: 756 bytes out of rule 32, 293 back in from the folded
restorations. Stage 3's twenty-five verdicts hold together with the evidence page, the decision log
and the resume file — 18 keep, 6 repair, 1 already removed, and six questions written for the person
who decides. The pin guard's own widening onto the rule-price page is real and its verdict word now
matches its findings.

**Twelve debts, each with a home.** Seven architecture pins landing five lines short, all repaired,
with the guard that passed them taking row 599. A batch's reached size held by no machine, row 601.
The migration chapter still walking a host to the cut rule 30, row 602. The rulebook's rule count
asserted by two hand-written literals and derived by nothing, so row 593 closed on a premise that is
not true, row 603. The worker-restore gate reading the refusal marker per record while mapping it
onto every tool result inside, row 604. A discard that executed in this tree on 2026-07-28 living in
a source comment alone, row 605. A dead `last_generation` block still publishing a stale figure, and
a progress page regenerated before the prose it describes, row 606. Row 595's S3 re-run has no
record and its per-rule byte accounting is off by 108 bytes, noted here and carried by row 601's
lock question. Row 588's acceptance line was already met and the row is now closed. And the freeze
baseline lives under `.gitignore`, so it is local and no reviewer can see what it compared against —
a standing property of the tree rather than a defect of this range.

**Why the suite would not have caught three of the four blockers.** Gate aa is the exception: a test
runs it over the real repository and reds. Gates ad and s have no such arm — the tree-counts test
exercises fixtures, and no test file runs the skill-review gate over this repository. Finding 4 had
no test at all until this walk wrote one. A reader taking the suite log as the push's answer would
have shipped a README publishing a count the tree does not carry, for the third range running.

**Two findings the push gate itself produced, after the review closed.** The gate chain was run
whole at 15:36 and returned `PUSH BLOCKED` on one red: the full suite measured 1,159.75 s against a
stated 800 s. The suite itself was green — 2,497 passed, no failures — so the red was the budget
alone. Rather than raise the number, the rise was measured: `tests/test_guardrails.py` costs
640.59 s of the run on a quiet machine, against the near-282 s the budget row had been claiming for
it since 2026-08-06. One file is 55% of every full run, and the morning's 800 s derivation had
inherited the stale term. The row is re-derived at 1280 s by its own slowest-plus-spread method in
`29710be`, with the measurement written beside it and queue row 553 carrying the fresh figure.

The same run exposed the gate's own skill-review check. It reported
`docs/skill-review/2026-08-12-build-pipeline-2.md` as the fresh record for all three changed
skills, because it matches a record to a skill with `grep -qw` over the whole body and that record
mentions communicator once inside a quoted sentence and names one live-spec-base file path. A
single record can green-light every skill it happens to name. The freshness half of that gate is
careful, after the 2026-08-09 stale-match finding; the identity half is a bare word search. This
push passes it honestly, since all three records exist and each was written for its own skill.
Queue row 607 holds the repair.

Blocking: none.
- closed: gate aa, the prose-findings ratchet — repaired in `46de0a6`, verified
  `OK (doc-findings-bound): 123 live documents, none above its record`.
- closed: gate ad, the published tree counts — repaired in `2d14354`, verified green once the page
  was committed.
- closed: gate s, the skill review records — repaired in `999a28c`, three records committed and
  fresh against their skills' own last change.
- closed: `scripts/sync-mirrors.sh:463` — repaired in `2d14354`, with two tests proven red against
  the pre-fix script and green after it.
- closed: the suite wall-time budget — re-derived in `29710be` after the rise was measured to one
  file, verified `OK (suite budget): measured 1159.75 s within the stated 1280 s`.

Verdict: the push road is clear. Every blocking finding is repaired inside this range, each repair
carries its own verification, and the twelve remaining findings are debts with named owners and
queue rows. The one habit this walk pays for, written down so the next pass skips it: a range that
closes without re-deriving the things that describe it — a README block, a census, a progress page,
a journal entry, the architecture pins, and the queue rows whose status the tree disagrees with —
leaves the next session to find all of it at the push gate.
