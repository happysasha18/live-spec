# Prover record — 2026-09-08 the window that never shuts, and two reds the release left

Reviewer: product-prover v1.6.0, pack bindings `skills/product-prover-pack/SKILL.md` v6.1.0.

PUSH-REVIEW

Mode: global
Scope: the release path and the base pack's lifecycle machinery — the hook installer
(`scripts/install-pack-hooks.sh`, `guardrails/judge-hooks.json`), admission and the spawn guard
(`scripts/task-admission.py`, `scripts/checkpoint.py`, `guardrails/worker-admission-guard.py`), and
the shared release path (`guardrails/run_modes.py`, `guardrails.config.json`,
`adopt/install-scaffold.sh`, `guardrails/check-acceptance-rerun.py`, `VERSION`, `MIGRATION.md`,
`.github/workflows/gates.yml`). The row changes a critical cross-cutting surface, which is the
second trigger a global read is owed on, so no owner's word was needed for it.
Range: 6b7a3fc..87eb7d0
- 87eb7d03 6.1.1 — two lifecycle faults, and the integration cap that named nothing
Files read: PRODUCT_SPEC.md, PRODUCT_SPEC.index.md, ARCHITECTURE.md, MIGRATION.md, VERSION, PLAN.md (q-829), .live-spec/checkpoints/q-829.md, .live-spec/readers/q-829-reader.md, scripts/task-admission.py, scripts/checkpoint.py, scripts/plan_checks.py, scripts/install-pack-hooks.sh, scripts/install-session-hooks.sh, guardrails/worker-admission-guard.py, guardrails/judge-hooks.json, guardrails/run_modes.py, guardrails/check-acceptance-rerun.py, guardrails/check-config-health.sh, guardrails/check-prover-record.sh, guardrails/pre-push, guardrails.config.json, adopt/install-scaffold.sh, matrix/build-pipeline.md, spec/queue-intake-priority.md, .github/workflows/gates.yml, hooks/, tests/test_release_6_1_1.py, tests/test_run_modes.py, tests/test_acceptance_rerun_reach.py, tests/test_closure_kernel_bypasses.py, tests/test_version_is_one_fact.py, docs/prover/README.md, skills/product-prover-pack/SKILL.md
Checks run: `python3 -m pytest -q tests/test_run_modes.py tests/test_acceptance_rerun_reach.py tests/test_release_6_1_1.py tests/test_checkpoint_mechanism.py tests/test_closure_kernel_bypasses.py` — 123 passed; `python3 -m pytest -q tests/test_task_admission.py tests/test_traceability.py tests/test_register_judge.py tests/test_hedge_arm.py tests/test_affirmation_arm.py tests/test_setup_entry.py tests/test_catchup_walk.py tests/test_statement_validation.py` — 390 passed, 2 skipped; `python3 -m pytest -q tests/test_chat_law_hook.py tests/test_conduct_judge.py tests/test_config_health.py tests/test_front_door_boundaries.py tests/test_guardrail_fixture_proofs.py tests/test_install_session_hooks.py tests/test_lean_orchestrator_arm.py tests/test_plan_is_not_executable.py tests/test_probe_reads_state.py tests/test_routing_preamble_hook.py tests/test_run_modes_install.py tests/test_scaffold_install.py tests/test_status_view_install.py tests/test_style_gate_kit.py tests/test_tasks_parser_finds_every_task.py tests/test_update_watcher.py tests/test_work_board.py tests/test_wind_down.py` — 250 passed; `python3 -m pytest -q tests/test_version_is_one_fact.py` — **2 failed, 3 passed**; `LIVE_SPEC_RUN_MODE=release LIVE_SPEC_DIFF_BASE=origin/main python3 guardrails/check-acceptance-rerun.py` — **BLOCKED, exit 1** (plan-11 and q-823 fail at this commit); q-823's own acceptance command run by hand — **FAIL** (the function count reads 9 where the key demands 8); `HOME=<scratch> sh scripts/install-pack-hooks.sh` twice — exit 0 both times, fifteen files placed on the first run, "already present" for all fifteen on the second, `conduct-law.md` and `language-laws.json` placed non-executable; the real `guardrails/worker-admission-guard.py` driven over five hand-built throwaway trees with a bare upstream (the landing window attacks under Findings); `python3 guardrails/check-index-generated.py`, `check-architecture-reference.py`, `check-matrix-reference.py`, `check-config-surface.py` — all OK.
Findings: Three blocking, listed under `Blocking:` below and not repeated here. What else the read covered and found clean: the run-mode change carries no surviving consumer of the old `integration` `max_targets` anywhere in the pack (`guardrails.config.json` keeps the key for `row` alone, `adopt/install-scaffold.sh` seeds it for `row` alone, `guardrails/run_modes.py` reads it for `row` alone), and no live prose anywhere still tells a host an integration run is capped at five — every surviving mention of the figure is written as history. The release path runs end to end in release mode: `guardrails.config.json` carries `core` with `core_version` `6.1.1`, so `guardrails/check-acceptance-rerun.py` under `.github/workflows/gates.yml`'s `LIVE_SPEC_RUN_MODE: release` is admitted rather than refused, and the two release-composition tests that hold the core against CI's own gate list pass. `guardrails/check-acceptance-rerun.py` prints the new unnamed-composition refusal as a verdict rather than a traceback, driven on its own path. The installer still installs everything it always installed on a machine that already has all of it, and its new refusal fires only on a file under `hooks/` that neither its own list nor another installer names — `dialog-warning-guard.py` and `worker-restore-guard.py` are correctly carved out, and the real `~/.claude/hooks` was left untouched by the run. `MIGRATION.md`'s 6.1.1 chapter is reached by the catch-up walk (the walk builds its work list as the chapter chain under "Migration chapters", which is by heading, and 6.1.1 is the last heading), and each of its two host instructions is true of the tree: `scripts/install-session-hooks.sh` chains to `scripts/install-pack-hooks.sh` and so does place the five newly-shipped files, re-running changes nothing, and no reader of `max_targets` for `integration` survives for a host to keep. `matrix/build-pipeline.md`'s rewritten M-658 and M-661 name four tests and all four exist and pass, and the two test names the rewrite dropped survive nowhere. The spec, the index, the matrix and the architecture carry no anchor, code or row pointing at anything the delta moved: their four generated-agreement gates are green, and none of `MODE_LAW`, `admit_targets`, `max_targets`, `close_is_unlanded`, `pre_spawn_check` or `update_checkpoint` is pinned in `PRODUCT_SPEC.md`, `PRODUCT_SPEC.index.md` or `ARCHITECTURE.md`. On `checkpoint.update_checkpoint(allow_closed=True)`: no other caller reaches it, now or by a near miss — the only route is `task-admission._write_anchor`, whose two callers are the definition-of-done anchor (which never sets it) and the brief token (which sets it only inside the window). It does, driven directly, let a closed checkpoint's DONE, IN PROGRESS and NEXT bodies all be rewritten wholesale; that is a latent widening no shipped caller reaches today, so it is deposited rather than blocked on. On the spawn guard's other legs: a prompt whose row has lost its acceptance key is still refused inside the window, and so is a prompt carrying no token at all — the two legs the window claims not to touch hold where I could reach them. The rest of what the read noticed and does not block on is one deposit, `inbox/2026-09-08-notes-beside-q829.md`.
Blocking: three, each one below
- The landing window does not shut when the push lands, so a row closed AND pushed admits a worker — `scripts/task-admission.py`, `close_is_unlanded`. The function answers True whenever the row's checkpoint file is dirty in the working tree or is touched by any commit not yet on the upstream, and `brief` itself writes that file (it appends the BRIEF-TOKEN line to the closed checkpoint), so using the window is what keeps the window from ever closing. Driven on the real hook `guardrails/worker-admission-guard.py` over a throwaway tree with a bare upstream: close the row, commit, mint the token inside the window, push the landing — the spawn is still ADMITTED after the push, and `close_is_unlanded` still reads True. Two neighbouring shapes fall the same way: a row closed and pushed with one uncommitted byte on its checkpoint is admitted, and a long-closed, long-pushed row is re-admitted by any later unpushed commit that touches its checkpoint file. This tree is in the first half of that state as it stands — `.live-spec/checkpoints/q-829.md` is dirty with the token line minted at 23:23:03, so q-829's window survives its own push. The row's definition of done says a spawn is "still refused on a row whose close has landed", and it is not; the guard's own refusal text, "its close has already landed on the remote", is false in that state. stands: the reviewer produced none of this work and repairs none of it; closing it is the holder's, and the window has to be bound to the close's own commit rather than to any modification of the checkpoint file.
- q-823's recorded acceptance command fails at this commit, and the range is what broke it — `scripts/task-admission.py`. The key in `scripts/plan_checks.py` reads `test "$(grep -cE '^def (correct|block|unblock|park|close|reopen|abandon|worker_brief)' scripts/task-admission.py)" = 8`. The new top-level `def close_is_unlanded` matches the `close` alternation, because the pattern anchors only its left side, so the count reads 9 at HEAD and 8 at `origin/main`. `.github/workflows/gates.yml` runs gate v with `LIVE_SPEC_RUN_MODE: release`, and q-823 is selected there because a file its acceptance command names changed in the pushed range, so this reds CI. Reproduced by running the key's own arm by hand and by running `guardrails/check-acceptance-rerun.py` in release mode over `origin/main..HEAD`. stands: the reviewer repairs nothing; the holder either anchors the pattern's right side or re-takes the figure, and the choice is the holder's.
- `VERSION` went to 6.1.1 without the version stamp, so the pack's one-version law is broken — `VERSION`, `skills/*/SKILL.md`. `tests/test_version_is_one_fact.py::test_every_skill_frontmatter_version_equals_pack_version` and `::test_every_base_reference_equals_pack_version` both fail at HEAD (every skill's frontmatter and every in-text `live-spec-base (v6.1.0)` reference still read 6.1.0 against a pack reading 6.1.1) and both pass at `origin/main`. CI runs the full `python3 -m pytest -q` as gate b, so this reds the push. The stamp that writes them is `scripts/stamp-versions.py`. stands: the reviewer produced none of this work and runs no stamp on it.
