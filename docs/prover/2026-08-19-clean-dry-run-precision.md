# Prover record — 2026-08-19 clean-dry-run-precision

PUSH-REVIEW

Range: 7ecff935..6524f288
- 6524f288 Record the git-clean dry-run fix: TEST_MATRIX row M-548, ROADMAP row 626 (landed)
- e70904da Teach the worker-restore gate git clean's own dry-run guarantee
- 9880bc32 Make measurements-table.py's HTML render opt-in, so a bare remeasure trips no sweep gate
Files read: scripts/measurements-table.py, tests/test_measurements_html_optin.py, docs/MEASUREMENTS.md, TEST_MATRIX.md, ROADMAP.md, docs/queue-archive/rotated-ROADMAP-2026-08.md, guardrails/check-rendered-sweep.py, scripts/sweep-rendered.py, spec/guardrails-freshness.md (Requirement 286), guardrails.config.json (rendered_pages), attic/MANIFEST.md, guardrails/check-worker-restore.py, tests/test_worker_restore.py, PRODUCT_SPEC.md (Requirement 298/299 territory via TEST_MATRIX M-476/M-477/M-546), skills/live-spec-base/SKILL.md, skills/build-pipeline/SKILL.md, skills/build-pipeline/references/delegation-protocol.md, templates/agent.template.md, scripts/open-lane.sh, guardrails/README.md, guardrails/check-matrix-reference.py, scripts/build-matrix-reference.py, docs/prover/README.md, docs/prover/2026-08-19-four-tails-dead-paths.md, docs/prover/2026-08-19-worker-restore-scope.md
Checks run: `python3 -m pytest tests/test_measurements_html_optin.py tests/test_measurements_table.py tests/test_rendered_sweep.py tests/test_worker_restore.py -q` — 177 passed. `python3 guardrails/check-matrix-reference.py TEST_MATRIX.md` — OK, 538/538 rows matched, 400 anchors agree body-to-table. `python3 guardrails/check-doc-rotation.py` — OK. `python3 guardrails/check-rendered-sweep.py` — OK, no transient page stands. `python3 guardrails/check-doc-findings-bound.py` — OK, 164 live documents, none above its recorded bound. `bash guardrails/check-shipped-language.sh` — OK, no Cyrillic/owner-name offences. `python3 guardrails/check-worker-restore.py --since-hours 24` — OK against this machine's own real transcripts (exit 0; 15 findings carried as another-project notices, 2 pre-counting-start history, 0 live findings against this project). Both fixes red-proven live, each by reverting the fix and confirming the specific test fails, then restoring: (1) `scripts/measurements-table.py` — reverted `main`'s HTML build to unconditional, `tests/test_measurements_html_optin.py::test_a_bare_remeasure_leaves_no_rendered_page` failed naming `docs/MEASUREMENTS.html` standing after a bare run; restored, `git diff` empty, green again. (2) `guardrails/check-worker-restore.py` — reverted the `elif sub == "clean":` branch to the bare `any(a.startswith("-") and ("f" in a or "x" in a) for a in rest)` check with no dry-run gate, ran `python3 -m pytest tests/test_worker_restore.py -k TestGitCleanDryRunIsNeverAFinding -q`: 10 of 12 `test_a_dry_run_form_passes` cases failed (the two bare `-n`/`--dry-run` cases with no `-f`/`-x` were never caught either way, so they still passed), each failure naming the still-reddening command; restored, `git diff` empty, full file green at 132/132. Manually confirmed the git semantics the fix rests on against a real repository (git 2.50.1, scratch dirs under this session's own scratchpad, outside this project's tree): `git clean -n -f`, `-f -n`, `--dry-run --force`, `--force --dry-run`, `-fn`, `-nf`, `-nx`, `-xn` each printed "Would remove" and deleted nothing; `--force` alone and `--force` after every dry-flag ordering above still deleted nothing until a bare `--force` with no `-n` anywhere was run, which did delete; `--dry` (an unambiguous prefix git itself accepts for `--dry-run`) also printed "Would remove" and deletes nothing, confirming the fix's own choice to require the exact spelling and let an abbreviation still red is a real narrowing of what the gate proves dry, not a hypothetical one.
Findings: (1) This range is two unrelated push-gate mines, found and closed the same session, kept as separate commits and reported separately here since they share no cause. (2) `scripts/measurements-table.py` wrote `docs/MEASUREMENTS.md` and `docs/MEASUREMENTS.html` together on every call, whether or not a person was about to read the page. `attic/MANIFEST.md` carries five separate sweep entries for this one page (2026-08-04 through 2026-08-19, two of them this session's own predecessor commits 063a55df and 33a983be) against at most two for any other rendered page in the project's history — the evidence this page alone was rendered far more often than read. No consumer of the page was found anywhere in the tree (no CI step, no skill, no script besides the generator itself references `docs/MEASUREMENTS.html`); the fix made the HTML build opt-in behind `--html` rather than removing the renderer outright, since the page carries real, non-trivial layout (per-column hover tooltips) that someone plainly built for reading and no evidence rules out a real future reader — this is stated caution, not a found consumer, and was said as such to the coordinator before landing. This matches the pack's own established pattern: every other page-producing path (`scripts/render-doc.py`) already renders on a deliberate, separate call rather than bundling a page into a routine data write; `measurements-table.py` was the one generator coupling the two. Checked for breakage: no `.github/workflows/*.yml`, skill, or other script calls `measurements-table.py` or opens `docs/MEASUREMENTS.html`; `NEXT_STEPS.md`'s own instruction ("Build it with `python3 scripts/measurements-table.py`") still holds, since the `.md` — the source of truth — is written on every call unchanged. (3) The `git clean` finding: this session's own coordinator ran the full `LIVE_SPEC_PUSH_FULL=1 bash guardrails/pre-push` dry run and read its gate-b failure by hand rather than accepting the red at face value — `tests/test_worker_restore.py::TestTheGateIsArmedWhereItSaysItIs::test_the_gate_runs_against_this_machines_own_transcripts` reddened on a real transcript: agent `a958ef6bcfb0b7018`, this same session (`279a25e4-6919-4347-965a-f49848119223`), ran `git clean -fn` at 2026-08-19T10:12:29Z while proving row 624's pre-shell hook against these very discard forms. The uncomfortable half, stated plainly rather than smoothed over: the finding came from this package's own agent, mid-build on the defence against these same forms, and the gate caught it correctly by the letter of its old code (`-f` present) and incorrectly by the meaning of its own stated law ("a worker runs no command that DISCARDS uncommitted work") — `-n` makes a `git clean` discard nothing, by git's own design, confirmed live above. (4) Scope held to exactly what was asked: only `git clean`'s dry-run recognition changed; every other member of the forbidden list (`git checkout --`, `git restore`, `git stash`, `git reset --hard`/`--merge`/`--keep`) and every other `git clean` form (`-f`, `-x`, `--force`, combinations without `-n`) reds exactly as before, proven by the paired `LIVE_TWINS` cases in the new test class and by the full 132-test file staying green. An abbreviated `--dry` is deliberately NOT recognized (doubt resolves to the finding, per the coordinator's own instruction), proven live above to still red. (5) The "one list, word for word" cross-file test (`test_the_five_homes_state_one_command_list`, `test_the_clause_is_present`) was checked before touching the module docstring: `CLAUSE_SENTENCES[2]`, the exact sentence mirrored across `guardrails/check-worker-restore.py`, `guardrails/README.md` and the five `CLAUSE_HOMES` files, was left byte-for-byte unchanged; the new dry-run exception paragraph was inserted as a new sentence immediately after it in the module docstring only (the one home the coordinator named — "абзац в шапке сторожа"), never touching the five skill/template homes, and the cross-file test suite (49 cases) stayed green throughout. (6) TEST_MATRIX.md's generated `## Reference` section was rebuilt with `scripts/build-matrix-reference.py` after each row addition (M-547 for the measurements fix, M-548 for the clean fix) rather than hand-spliced; `check-matrix-reference.py` confirms 538/538 rows matched. (7) Both ROADMAP rows (625, 626) were added live, then closed the same commit with `python3 scripts/rotate-doc.py --doc ROADMAP.md --close-row N`, the documented closing-commit path (INV-276) — never hand-edited into the archive file directly.
Blocking: none
