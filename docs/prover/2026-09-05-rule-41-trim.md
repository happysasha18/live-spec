# Prover record — 2026-09-05 rule 41's history moved to a reference

PUSH-REVIEW

Prover skill version: product-prover (installed under `skills/product-prover/`), read beside
`skills/product-prover-pack/SKILL.md` v6.1.0 (pack bindings) and `skills/live-spec-base/SKILL.md`.

Range: 2c1dde9b..7d5a8762
- 7d5a8762 Skill review for rule 41's history move, and the behavioural check that backs it (SPEC INV-208)
- b18e8d95 Rule 41's own history moves to references/rule-origins.md, proven safe on eight fresh reads (PLAN q-822)

Files read: skills/live-spec-base/SKILL.md (rule 41's body), skills/live-spec-base/references/
rule-origins.md (the new Rule 41 section), PLAN.md (q-822's own row), docs/skill-review/
2026-09-05-live-spec-base-rule-41-trim.md.

Checks run: `python3 -m pytest -q` — 2,844 passed, 6 skipped, 3 real fails traced and closed (below).
- Two config-health fails ("installed skill drifted from source: live-spec-base") were this
  session's own edit not yet mirrored to the installed copy — `bash scripts/sync-skills.sh`
  closed them (live-spec-base 6.1.0 -> 6.1.0, content refreshed).
- The prover-record gate's own fail ("today's review record exists on disk but none are committed")
  was this record's own file, written before this commit — closed by committing it here.
- The teardown error (`test_packet_a_does_not_move_the_counting_start...`) is the suite's own
  session-scoped checkpoint-cache write, the same known, accepted self-write prior records already
  name — not a new finding.
- Eight independent fresh-agent scenario runs (four pairs, full-vs-trimmed rule text) read by hand
  for the behavioural claim; `python3 scripts/spec-style-lint.py --gate PRODUCT_SPEC.md
  ARCHITECTURE.md` — both clean (this range touches neither document, so this is a repeat of the
  standing floor, not new evidence).

Findings: none blocking. One recommendation (R1), detailed below.

R1 (recommendation) — the eight-agent behavioural check this commit cites (four scenario pairs) is
real evidence that rule 41's specific cut changed no observed behaviour, but it is not the standing,
reusable measurement PLAN q-822's own "Done when" criterion asks for before further rulebook cuts:
the four scenarios were designed once, for this one rule, and are not committed anywhere as a
re-runnable suite (unlike `evals/director/scenarios.json`). A later session re-reading this record
cannot re-run the same check without reconstructing the scenarios from this record's own prose. Not
fixed here — PLAN q-822's own text already names this gap as the row's own open work, so it is not a
fresh finding this record invents; it is confirmed as still real by this pass's own read of what got
committed.
`recommendation · later · hard-to-operate (ops-ux)`

Class lens: no class. This is a narrow, single-rule prose move with a matching narrow behavioural
check; no sibling instance of "history paragraph inside a rule body" remains in `skills/
live-spec-base/SKILL.md` per this session's own earlier grep sweep (only single-clause dated
citations remain elsewhere, none extractable the same way).

Blocking: none
