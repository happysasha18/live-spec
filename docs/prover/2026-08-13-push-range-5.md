<!-- ARCHIVED 2026-08-17, the owner's word. This record was written on an unmerged branch and
     carried into the tree when that branch was deleted, so the reasoning it holds outlives the ref.
     It reviews a range this tree never took, it discharges no gate, and it authorizes no push. -->

# Prover record — 2026-08-13 the repair range: both blocking findings of push-range-4 re-certified closed

PUSH-REVIEW

This is a short-form re-check under `PRODUCT_SPEC.md:3261` (INV-61, matrix M-160): the delta is an
infra and prose kind with no new surface and no structure change, so it rides the short form of
previous records named, the delta in one line, and the verdict. The five fields below stay filled
because `guardrails/check-prover-record.sh` reads them on whichever record names the pushed range,
and after 284ab50 that record is this one. Short in what it claims, complete in its shape.

Range: be4e4f0..HEAD. `origin/main` resolves to be4e4f0. The reviewed commits — those touching a
file outside `docs/prover/` — are d835ecc and 284ab50; 8f18a8f and c9d3d35 carry records alone.

- 284ab50 The temporary reconciliation answers its review: the three missed modules and the skips' real reach
- c9d3d35 The reconciliation range meets its adversarial read: the sweep repaired three gates and left one module red
- d835ecc The tree stops pointing at a body that left it: the temporary external-prover reconciliation
- 8f18a8f The decoupling meets its adversarial read: nineteen findings, sixteen of them blocking

Previous records: `docs/prover/2026-08-13-decoupling.md`, the FULL pass whose nineteen findings opened
this territory, and `docs/prover/2026-08-13-push-range-4.md`, the adversarial push review that refused
the range on two blocking findings. Sixteen of the decoupling's findings stand open by design and are
untouched here. This record re-certifies the two that refused the push, and nothing else.

The delta in one line: 284ab50 gives the four red tests in `tests/test_prover_doc_homes.py` the same
named temporary skip the other modules carry, widens twenty-three skip reasons to name the
still-shipped files each one silences, corrects the false "26 modules green" claim in `JOURNAL.md`,
and opens `ROADMAP.md` rows 623 and 624 to own the debt this repair only declares.

Files read: `tests/test_prover_doc_homes.py` whole; the whole diff `c9d3d35..284ab50`;
`tests/test_review_record_class.py` lines 1-70; `tests/test_restructure_merge_gate.py` lines 24-80;
`tests/test_traceability.py` lines 582-615 and 1030-1040;
`tests/test_minor_gate_reconciliations.py` lines 85-100; `ROADMAP.md` rows 623 and 624 whole;
`JOURNAL.md` the 15:38-15:52 entry whole; `PRODUCT_SPEC.md` lines 3251-3262;
`TEST_MATRIX.md` line 248; `guardrails/check-prover-record.sh` lines 225-290.

Checks run: each at this seat, on the tree 284ab50 leaves.

- `python3 -m pytest tests/test_prover_doc_homes.py -q -rs` — **1 passed, 4 skipped in 0.06 s**,
  against 4 failed and 1 passed at c9d3d35. Each of the four reasons names its own cause: the canon
  carries neither the boundary sentence, the KIND-block split, the record-path line, nor the
  lifecycle lead-in the module pins.
- `python3 -m pytest` over the eleven modules the repair names — **213 passed, 34 skipped, 0 failed**
  in 4.74 s. `test_node_fitness_test.py` and `test_product_prover_standalone_release.py`, the two
  unguarded modules push-range-4 flagged and this repair left untouched, are green by name.
- `grep -rl` over `tests/` for the literal path against every mention of the prover — 26 modules name
  `skills/product-prover/` as a literal string, 30 mention the prover at all, and of the four
  difference two are prose-only mentions in module docstrings while two build the path from segments,
  `test_prover_doc_homes.py:12` and `test_product_prover_standalone_release.py:49`. So 26 literal
  readers plus 2 segment builders is 28, which reconciles the repair's accounting with push-range-4's
  count exactly. Both numbers were right about different sets, and d835ecc's error was the verdict
  rather than the arithmetic.
- A sweep resolving every reach clause against its own test body, following module helpers, `setUp`
  assignments and class attributes — 23 clauses, `PRODUCT_SPEC.md` named in 18, and one named file
  that the test carrying it does not read. That one is finding 1 below.
- `grep -rc "his word 2026-08-13 15:06" tests/*.py` summed over its per-file counts — **60** skip
  sites across **26** modules, exactly what row 623 states and the command row 623 names.

Findings: one, and it does not block. Both blocking findings of `docs/prover/2026-08-13-push-range-4.md`
are re-certified closed, each against its own stated closing condition.

**Finding 1 of push-range-4 — closed.** Its condition was that the module's four assertions be
re-pointed or skip-guarded in the same named form the other modules use, that the other two unguarded
modules be confirmed by name rather than by luck, and that the count read 28 rather than 26. All three
are met. The module returns 1 passed and 4 skipped; the other two return green when run by name; and
the journal states 28 with the decomposition that makes both earlier counts intelligible. The journal
also says plainly that the entry above it and d835ecc's own message state a count that is wrong, which
is the correction a false green owes.

**Finding 2 of push-range-4 — closed.** Its condition was that the tree's own declaration state the
reach with its counts. Twenty-three reasons now carry a reach clause naming the still-shipped files
that skip silences, and rows 623 and 624 carry the counts where this pack looks for open work. On the
two numbers this seat disputed with the repair, the repair is right and push-range-4 was wrong, and it
is worth saying plainly rather than leaving in a diff. `PRODUCT_SPEC.md` is silenced in 18 skipped
tests and not 17: `tests/test_review_record_class.py` reads the spec in `setUp` at line 20 and hands it
to every test in the class through `self.spec`, an indirection push-range-4's sweep did not follow.
`VERSION` is a real omission of that sweep for the same kind of reason — it matched filenames by
extension, so a file without one could not be found, and `tests/test_minor_gate_reconciliations.py:95`
reads `VERSION` directly. `LICENSE` is likewise real, with one imprecision noted below.

**The one thing this repair got wrong, non-blocking.** The reach clause on
`tests/test_traceability.py::test_public_skills_ship_readme_license_and_crossref` names three files,
and one of them, `docs/worker-liveness.md`, that test never reads. Its body checks
`skills/<skill>/README.md` and `skills/<skill>/LICENSE` for the prover and the design reviewer, then
reads the prover's README once; `docs/worker-liveness.md` is read by two other tests in the same file,
neither of them skipped. The same phantom is repeated in the journal's list of silenced files. The two
other names in that clause are real but loose: the still-shipped coverage lost is
`skills/design-reviewer/README.md` and `skills/design-reviewer/LICENSE`, and the clause names them
bare, as the test's own loop does. This over-states one clause where finding 2 was about under-stating,
so it does not reopen that finding — a reader who chases the phantom finds nothing at stake rather than
missing something that is. It is recorded so the next pass over these reasons repairs it instead of
propagating it, and it belongs to row 623, which owns the reasons and their re-pinning.

**Swept and clean.** Every other named file across the twenty-three clauses is genuinely read by the
test that names it. The three widened reasons spot-checked by hand each hold:
`test_review_record_class.py` names `PRODUCT_SPEC.md` and `skills/design-reviewer/SKILL.md`, both read
through `self.spec` and a direct read; the three in `test_restructure_merge_gate.py` name
`PRODUCT_SPEC.md` and `skills/build-pipeline/SKILL.md`, exactly the two surviving members of the
module's `HOMES` tuple; and `test_skills_inherit_base_pin` names `skills/live-spec-base/SKILL.md`,
which its first line reads. Rows 623 and 624 state the debt in the form the queue takes, each with the
command that measures it, and 624 carries the CI gap push-range-4 found under its non-blocking finding
4. The protected files are untouched and unstaged.

Verdict: ALLOW. Both blocking findings are closed against their own conditions, the repair's two
disputed counts are correct and push-range-4's were not, and the one inaccuracy found here is smaller
than the finding it sits inside and carries no push weight. The sixteen findings of the decoupling
record stand open as they did, owned by rows 623 and 624 and by the record itself.

Blocking: none.
