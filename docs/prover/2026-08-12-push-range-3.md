# Prover record — 2026-08-12, the evening range: the prover read, the install-defect row, and the repairs this review demanded

PUSH-REVIEW

Range: 4a0b982..6efcea3 — 10 commits. `origin/main` resolves to 4a0b982, so this is the range the
push sends. The adversarial read covered the seven commits from 4a0b982 to 88ca918. The eighth,
c74eee4, is this walk's own repair commit, written at the seat in answer to the review's six
blocking findings. The ninth, 1fabca9, answers the push gate's own refusal, recorded below as
finding 11. The tenth, 6efcea3, takes the commit count back out of the two surfaces that state this
range, since the ninth commit had already made that number stale. Each commit is named below with
what it carries.

- 6efcea3 The two surfaces that state this range stop stating a number that rots
- 1fabca9 The prover's two landed fixes get the tests that hold them
- c74eee4 The six blocking findings of this range's own review close inside it
- 88ca918 A queue row opens on the two files the install names and never delivers
- 0e4834c The plan page answers "what is happening" from its first screen, and the resume file tells the next session to ask rather than to start
- bea80c0 The resume file carries the two closed prover rows and the four commits the next push must cover
- c9e1886 Two of the prover read's five findings land: the record filename now matches the gate that reads it, and the lifecycle lead-in counts what follows it
- d6a6ea3 The resume file carries the prover read's five rows and names the two commits the next push owes a record for
- c9f4a16 The product-prover skill's full read lands with its five findings, and each takes a queue row
- b102281 The plan's four disagreeing cells are repaired and the resume file carries the pushed pass

Root: his word of 2026-08-12 at 18:45 — ship what is waiting. The review was run by a separate
reader with clean context, briefed to find reasons to refuse the push rather than reasons to allow
it, and its verdict was REFUSE until findings 1 through 6 closed. The seat then closed all six and
re-derived every number the reader disputed against the tree itself.

Files read: `NEXT_STEPS.md` whole; `ROADMAP.md` head 1-40 and rows 212, 229, 248, 254, 257, 260,
388-396; `README.md` lines 62, 74, 105; `ARCHITECTURE.md` lines 9, 368, 457, 629, 877, 895;
`PRODUCT_SPEC.md` lines 1630-1634 and 6328-6332; `DECISIONS.md` lines 490-540; `OVERVIEW.md` line
63; `VERSION`; `install.sh` whole; `docs/PROGRESS.md` diff; `docs/prover/2026-08-12-product-prover-full-read.md`
whole; `docs/skill-review/2026-08-12-product-prover.md` whole; `docs/prover/2026-08-12-push-range-2.md`
headings and line 118; `docs/prover/README.md`; `docs/roadmap-format.md` lines 29-72;
`inbox/2026-08-12-preshow-lint-script-missing.md` whole; `.live-spec/culling-plan-v3-2026-08-10.md`
whole; `.live-spec/plan-v3-delta-2026-08-12-4.md`; `.live-spec/plan-v3-delta-2026-08-12-5.md`;
`.live-spec/r5-rule-prices-2026-08-11.md` lines 1-20; `skills/product-prover/SKILL.md` lines 1-8,
296-302, 632-637, 737-742 and 965-975; `skills/product-prover/README.md` lines 90-94;
`skills/live-spec-base/SKILL.md` lines 261-265; `editions/product-prover/SKILL.md` lines 320-324;
`guardrails/pre-push` whole; `guardrails/check-prover-record.sh` whole at the seat and lines 125-135
at the reader; `guardrails/check-skill-review.sh` lines 18-154; `guardrails/check-skill-loadability.sh`
lines 39-57; `guardrails/rule-census.json`; `tests/test_class_hunt.py` whole; `tests/test_traceability.py`
lines 77-131 and 395-540.

Checks run: each with its result, the reader's set first and the seat's re-derivations after it.

- `git log --oneline origin/main..HEAD` — 7 commits at review time, b102281..88ca918.
- `git diff --stat origin/main..HEAD` — 10 files, 547 insertions, 168 deletions; the diff read whole.
- `git rev-parse --short origin/main` — 4a0b982.
- `git rev-list --count e8900d9..origin/main` — 33, which confirms the morning range the resume file claims.
- `git rev-list --count origin/main..HEAD` — 7, against the four and the two fives the resume file stated.
- `git cat-file -s origin/main:skills/product-prover/SKILL.md` — 67412; `wc -c` on the working copy — 67562.
- `wc -c PRODUCT_SPEC.md` — 702784, matching `docs/PROGRESS.md`.
- `python3 guardrails/check-doc-findings-bound.py` — FAIL before the repair on the unrecorded inbox note; OK after, 124 live documents, 22 held at zero, none above its record.
- `python3 guardrails/check-tree-counts.py` — 3 published-count faults before the repair; clean after `python3 scripts/gen-tree-counts.py`, once the regenerated README.md was committed.
- `python3 guardrails/check-doc-bound.py` — OK, 4 docs read.
- `python3 guardrails/check-board.py` — OK.
- `python3 guardrails/check-doc-rotation.py` — OK.
- `bash guardrails/check-freeze.sh` — SPEC-FREEZE GREEN, 3 files; no frozen document is in this range.
- `bash guardrails/check-pin-drift.sh` — OK, 209 pins plus the 53 range pins of the rule-price page.
- `bash guardrails/check-skill-loadability.sh` — OK, 11 skills.
- `bash guardrails/check-skill-review.sh` — OK, product-prover carries a fresh record.
- `bash guardrails/check-prover-record.sh --push` — FAIL at review time, by construction: the newest record commit predated the newest reviewed commit. This record is what closes it.
- `python3 -m pytest tests/test_traceability.py -q` — 178 passed in 8.14 s.
- `ls docs/prover/*.md | wc -l` — 362, of which 2 are not records (`README.md`, `architecture-prover-record.md`); 360 dated, 356 with a slug, 4 bare dates.
- `cat skills/*/SKILL.md skills/*/references/*.md | wc -l` — 6436 against the 6,435 README published; `cat skills/*/SKILL.md | wc -l` — 5198 against 5,197.
- The rulebook volume by the plan's own recorded command — 72466 exactly, so the plan's figure holds.
- `grep -rn "preshow-register-lint" skills/` — text-audit three times, its rewrite-meaning-check reference once, communicator once; `grep -rn "spec-coinages" skills/` — spec-author only.
- `python3 scripts/rule-census.py NEXT_STEPS.md` — 2 sentences past the cap after the seat's repairs, 0 after they were split; the file's record of zero held.
- `python3 scripts/rule-census.py --json guardrails/rule-census.json` — refused once against the resume file's ceiling, then wrote: 124 files, 4,874 findings.
- `python3 scripts/progress-report.py` — rewrote `docs/PROGRESS.md` on the refreshed census.
- `grep -rn '750\.36' --include='*.md' .` — one hit, the resume file itself, so the figure had no home.
- `python3 -m pytest -q` at 19:04, the pass's one clean full run with no worker writing the tree — 2 failed, 2495 passed in 1221.81 s. Both failures read back one cause, recorded as finding 11's neighbour below.
- `bash scripts/sync-skills.sh` — product-prover synced, every other skill unchanged; `python3 -m pytest tests/test_config_health.py -q` after it — 32 passed.
- `bash guardrails/pre-push < /dev/null` at 19:31 — one gate refused, gate h's tests-present arm, recorded as finding 11.
- `python3 scaffold/guardrails/check_tests_present.py` after 1fabca9 — OK, 2 user-facing changes travel with 1 test change.
- `python3 -m pytest tests/test_prover_doc_homes.py -q` — 5 passed, with the two new tests red-proven against the body at c9e1886~1.

Findings: eleven. Seven blocked the push and all seven are closed in this range. Four were
non-blocking and three of those are closed here as well; the fourth is a note about a lint that
cannot fire, recorded below. Six of the seven blocking findings came from the adversarial read of
18:58, and the seventh from the gate chain of 19:31. Three of the seven were gates already red in
the tree, and all three were caused by commits inside this very range.

**The seven that blocked, and how each closed.**

1. Gate aa refused: the inbox note added by 88ca918 was live and carried no census entry, measuring
   3 findings of its own. Closed in c74eee4 by rebuilding the census and the progress page. The live
   set grows to 124 documents and 4,874 findings.
2. Gate ad refused: `README.md` published 6,435 lines under `skills/` and 5,197 lines of skill body,
   while the tree held 6,436 and 5,198 after c9e1886 added one line to the prover's body. This is the
   defect queue row 600 already names, walked into a second time by the same recipe. Closed in
   c74eee4 by `scripts/gen-tree-counts.py`.
3. `NEXT_STEPS.md` stated the unpushed-commit count three times and was wrong three times — four at
   line 83, five at 171, five at 175, against seven in the tree. The file written for a session with
   clean context disagreed with itself inside one file. Closed in c74eee4: the live-state block now
   names the range 4a0b982..HEAD and what each of its commits carries, and the two other mentions
   send the reader to `git log` rather than to a number that rots between passes.
4. The prover full-read record, roadmap row 608 and the skill-review record all claimed
   `docs/prover/` holds 381 records with 380 slugs, and that only `2026-07-04.md` follows the skill
   as written. The tree holds 360 dated records, 356 with a slug, and four bare dates —
   `2026-07-04.md`, `2026-07-05.md`, `2026-07-19.md` and `2026-07-20.md`. The figure 381 matches
   nothing countable in the tree. This mattered beyond arithmetic: the "only one" claim is the
   evidence row 608's whole argument rests on, and three more files contradict it. The row's repair
   stands on its own merits, since the gate's repair line and the skill now name one string. Closed
   in c74eee4 in all three documents, with the four bare-date records named.
5. The plan page — the one shared surface — said five commits wait, in a head block that promises it
   is rewritten by every pass. Closed in c74eee4: the block names the range, its size, this review
   and its six findings, stamped 19:01, with `.live-spec/plan-v3-delta-2026-08-12-6.md` beside it.
6. `NEXT_STEPS.md` said the day's two full runs measured 1,159.75 s and 750.36 s, both inside the
   gate chain. `ARCHITECTURE.md` line 877, the home the 1280 s budget was derived from and untouched
   by this range, says 1,159.75 s inside the pre-push gate set and 993.31 s standalone while three
   worker sessions shared the machine. The resume file was the copy that drifted, and 750.36 has no
   home anywhere in the tree. Closed in c74eee4 by writing what the architecture row states.

11. Gate h refused at 19:31, the arm that asks whether tests travel with a change: `README.md` and
    `skills/product-prover/SKILL.md` changed with nothing under `tests/`. README carries generated
    tree counts that gate ad already holds against the tree; the prover's body carries the two fixes
    of c9e1886, and those had nothing holding them. Closed in 1fabca9 with two tests. The first pins
    the record path the skill prescribes to the one the gate demands, reading the gate's own printed
    repair line, so drift on either side reds — that is the very defect row 608 named. The second
    counts the lifecycle sweep's bullets against the lead-in that promises a parent plus five. Both
    were proven red against the body at c9e1886~1.

    Beside it, and closed before the gate chain ran: the pass's full run at 19:04 came back 2 failed
    and 2,495 passed. One cause read back by two tests — the installed copy of the prover skill in
    `~/.claude/skills` had drifted from source since c9e1886 edited the body. `scripts/sync-skills.sh`
    aligned it and `tests/test_config_health.py` went 32 passed. The repository tree was untouched by
    that repair, so the run's own result stands for the tree this push sends.

**The four that did not block.**

7. A paragraph in `NEXT_STEPS.md` had lost its subject: a sentence began lower-case with a dangling
   possessive, and no gate catches it, because the file's zero-findings state measures sentence
   length and register rather than grammar. Closed in c74eee4.
8. Row 613's status cell read `*open*`, a word outside the queue's closed vocabulary of five. It was
   the only such cell in the file. Closed in c74eee4 as `*queued*`. The lint that should hold this is
   disarmed: `tests/test_traceability.py` carries the vocabulary, and its queue arm stays unarmed
   while 25 offenders sit in the live body, ten of them `*done*` rows. That stands as a note for the
   session that arms it, and it belongs beside the existing stage-3 work on gates.
9. Row 613 attributed both named files to all four skill bodies it listed. Three name the register
   lint and `skills/spec-author/SKILL.md` names the coinage list. The row's substance held on every
   other point: both files exist here, `install.sh` copies the skill directory and nothing else, and
   the row's footprint and acceptance were sound. Closed in c74eee4.
10. `docs/PROGRESS.md` would have gone stale the moment the census was refreshed. Closed in c74eee4
    in the same commit as the census, which is what the row asked for.

**Classes the reader swept and found nothing in.** Line pins and paths: every pin cited in the new
documents lands, and the pin guard passes 209 plus 53. Repair text against code: the only body change
is `skills/product-prover/SKILL.md` at lines 635 and 969-973, and both read what row 608 and row 612
claim. Gate strings: the resume file's quoted verdict line matches what `guardrails/pre-push` prints,
and the record-matching glob accepts both the bare-date and the slug forms, as the skill review says.
Frozen documents: none of the three is in this range. The full-read record's other evidence — the
three absent terms, the undefined sweep, the string-presence test — verified true. The plan's twelve
debts across queue rows 599-607 verified against `docs/prover/2026-08-12-push-range-2.md` line 118,
which records sixteen findings with four closed in that range.

Blocking: seven, and all seven are closed inside the range.
- Finding 1, gate aa refused on the unrecorded inbox note — closed: c74eee4 rebuilt the census and the progress page, and the gate now reads 124 live documents with none above its record.
- Finding 2, gate ad refused on README.md's published line counts — closed: c74eee4 carries the counts rebuilt by `scripts/gen-tree-counts.py`, and the gate passes on the committed page.
- Finding 3, the unpushed-commit count stated three ways in NEXT_STEPS.md — closed: c74eee4 names the range and what each commit carries, and sends the file's two other mentions to `git log`.
- Finding 4, the 381-records claim standing in three documents — closed: c74eee4 writes 360 dated records with 356 slugs in all three, and names the four bare-date records.
- Finding 5, the plan's head block saying five commits wait — closed: c74eee4 rewrites the block to name this range, this review and its six findings, with delta page six beside it.
- Finding 6, NEXT_STEPS.md's suite figures against their home in ARCHITECTURE.md — closed: c74eee4 writes what the architecture row states, 1,159.75 s inside the gate set and 993.31 s standalone.
- Finding 11, gate h refused because two user-facing files changed with no test beside them — closed: 1fabca9 lands two tests over the prover's changed lines, both red-proven against the body at c9e1886~1, and the gate reads OK on the committed range.

The three non-blocking repairs are closed in the same commit. The one open note, the disarmed
queue-status lint of finding 8, carries no push weight and no gate reads it today.
