# Prover record — 2026-08-19 four-tails-dead-paths

PUSH-REVIEW

Range: d899093f..f289330a
- f289330a Guard measurements-table.py's reader-prompt citation against another silent move
- 33a983be Sweep a rendered docs/MEASUREMENTS.html left over from this session's remeasure
- 50afcd97 Re-range the prover record onto the merged main and read the merge itself
- 99f07c4d Merge remote-tracking branch 'origin/main' into fix/2026-08-19-four-tails
- 127f90d0 Prover record for the dead-path repairs and the three non-defect tails
- 3574cb2e Point TEST_MATRIX.md's retired-checker citations at their attic path
- 1de6fc14 Fix dead paths in docs/MEASUREMENTS.md after the text-audit/product-prover extractions
Files read: docs/MEASUREMENTS.md, guardrails/progress-baseline.json, scripts/measurements-table.py, guardrails/rule-census.json, TEST_MATRIX.md, attic/MANIFEST.md, guardrails/check-push-reach.sh, guardrails.config.json, spec/guardrails-freshness.md (Requirement 275), spec/doc-order-generated.md (Requirement 1), PRODUCT_SPEC.index.md, tests/test_traceability.py, tests/test_installed_copy_staleness_class.py, tests/test_forward_binding_and_infra_class.py, tests/test_guardrails_unit.py, tests/test_far_tier.py, tests/test_listener_tripwire.py, tests/test_description_field.py, tests/test_agent_channels.py, tests/test_vacuous_pass.py, tests/test_progress_report.py, skills/text-audit-pack/SKILL.md, skills/product-prover-pack/SKILL.md, ROADMAP.md rows 55 and 93, docs/queue-archive/rotated-ROADMAP-2026-07.md, docs/prover/2026-08-19-worker-restore-scope.md, guardrails/check-worker-restore.py, tests/test_worker_restore.py, ROADMAP.md (d899093f delta), DECISIONS.md (d899093f delta), guardrails/README.md (d899093f delta), /private/tmp/live-spec-control/push-tails.log, tests/test_rendered_sweep.py, guardrails/check-rendered-sweep.py, scripts/sweep-rendered.py, scaffold/guardrails/check_tests_present.py, guardrails.config.json (user_facing_globs), tests/test_measurements_table.py, attic/MANIFEST.md
Checks run: `python3 -m pytest tests/test_traceability.py tests/test_far_tier.py tests/test_listener_tripwire.py tests/test_description_field.py tests/test_agent_channels.py tests/test_vacuous_pass.py tests/test_config_surface.py tests/test_index_generated.py tests/test_matrix_reference.py tests/test_progress_report.py tests/test_guardrails_unit.py tests/test_worker_restore.py -q` — 555 passed. `python3 -m pytest tests/test_installed_copy_staleness_class.py tests/test_forward_binding_and_infra_class.py -q` — 9 passed. `python3 guardrails/check-matrix-reference.py TEST_MATRIX.md` — OK, 536/536 rows matched, 400 anchors agree body-to-table (536 = the 535 this range's own six repairs sit inside, plus row M-546 the merged-in first daily package added). `python3 scripts/spec-style-lint.py TEST_MATRIX.md --tier full` — 68 findings both before this range's edits (`git show f6fa13a8:TEST_MATRIX.md`) and now, so this range's edits added none (the merged-in package's own findings count is its own record's business, not re-litigated here). `python3 scripts/measurements-table.py` — the real regeneration command behind the MEASUREMENTS.md commit, output diffed against the shipped file before committing. `python3 -m pytest tests/test_rendered_sweep.py -q` — 42 passed. `python3 guardrails/check-rendered-sweep.py` — OK, no transient page stands. `python3 scaffold/guardrails/check_tests_present.py --base origin/main` — OK, 1 user-facing change travels with 1 test change. `python3 -m pytest tests/test_measurements_table.py -v` — red-proved live: passed on the real citation, failed naming the dead path once `docs/briefs/reader-prompt.md` was swapped back to `skills/text-audit/references/reader-prompt.md` in the script's source, passed again once reverted (`git diff scripts/measurements-table.py` empty after). `git diff --name-only f6fa13a8 d899093f` against `git diff --name-only f6fa13a8 127f90d0`, comm -12 on the two lists — the only file both sides of the merge touched is TEST_MATRIX.md. `git diff f6fa13a8 99f07c4d -- TEST_MATRIX.md`, read hunk by hunk (`grep -n '^@@'`, 7 hunks) against the same diff take separately for each parent — every hunk is exactly one side's known change (this range's six `-`/`+` row-pair repairs, or the merged-in package's one new row M-546 plus its Reference-table line), none overlapping, none merged into a third reading; `grep -rn '^<<<<<<<|^=======$|^>>>>>>>'` over the tree — no conflict markers anywhere.
Findings: (1) The pushed range now opens with a merge of origin/main's first daily package (base d899093f, eleven commits from c9e98308 to d899093f: the worker-restore own-session-scoping fix, its TEST_MATRIX.md row, and three rounds of prover-record-and-repair) into this branch. That package is someone else's already-reviewed work — its own prover record sits at docs/prover/2026-08-19-worker-restore-scope.md, dated and committed before it landed — so this record does not re-derive its findings; it only reads what the merge itself did. (2) The merge auto-resolved with no conflict, and only one file — TEST_MATRIX.md — was touched by both sides: the merged-in package added one row (M-546, the worker-restore gate's own-session scoping) and updated the INV-299 Reference-table line to name it alongside M-477; this branch had already repointed six unrelated rows (M-406, M-403, M-404, M-399, M-412, M-421) from their pre-attic path to guardrails/attic/. Read hunk by hunk against both parents' independent diffs: the seven hunks in the merged file split cleanly into the six single-row repairs (each still exactly this branch's before/after text) and the one addition plus the one Reference-line edit (each still exactly the merged-in package's text) — no hunk mixes the two, no row from either side is missing, doubled, or altered by the auto-merge. `guardrails/check-matrix-reference.py` confirms mechanically: 536 of 536 rows matched (535 + M-546), 400 anchors agree body-to-table. (3) Of the four tails handed down before the merge, three were not defects, each checked rather than taken on faith. M-336 (TEST_MATRIX.md, INV-180's installed-copy staleness class): Requirement 275 in spec/guardrails-freshness.md is fully written, its own installed-skills member already names the config-health skill-copy arm (INV-243/M-428, built), and ROADMAP's archived row 356 records the same closure — its three owning tests all pass and guardrails/check-matrix-reference.py reads the row's anchor cleanly. R1 (Requirement 1, criterion 4, spec/doc-order-generated.md): its `[target]` mark on the host-facing-gates/snapshot/design-sync trio is honestly tied to open ROADMAP rows 55 (`*queued*`) and 93 (`*deferred*`) through S-0's declared TARGET_ROW_OWNERS map in tests/test_traceability.py — the self-closing ownership test and the architecture pin-honesty test both pass, and the row is not the one carrying a stale defect comment elsewhere in that file (INV-160/M-307's sibling class does, in a different, untouched test — noted, not chased, since it names no dead path). The reach machine (guardrails/check-push-reach.sh) genuinely does send a spec-only diff to the FULL suite — confirmed live with `REACH_FILES=PRODUCT_SPEC.md` — but this is by design, not a bug: PRODUCT_SPEC.md is deliberately kept out of `guardrails.config.json`'s `prose_files` because hundreds of string-level tests scattered across the suite read its text directly, and there is no reliable static map from a changed spec line to the handful of tests it could break; the script's own header states this in words, and `test_tested_documents_stay_full_reach` in tests/test_guardrails_unit.py asserts it by name. No safe narrowing was found, so none was made; this is reported, not silently left as a suspected bug. (4) The two real repairs, both dead-path fixes discovered by walking every backtick-quoted path in the named files against the tree: docs/MEASUREMENTS.md cited `skills/text-audit/SKILL.md` and `skills/product-prover/SKILL.md` as measured rows in the reading queue, and a hardcoded `skills/text-audit/references/reader-prompt.md` — all three left the pack in the 2026-08-13/18 external-skill extractions; fixed by dropping the two dead members from guardrails/progress-baseline.json's priority groups (naming the move and the thin adapters each left behind), repointing the reader-prompt citation to its real current home docs/briefs/reader-prompt.md (confirmed by the language-reads records that already cite it), and regenerating docs/MEASUREMENTS.md with the real command rather than hand-editing numbers. TEST_MATRIX.md separately cited five guardrails scripts — check-wrong-referral.py, check-far-tier.py (twice, rows M-403/M-404), check-index-prose.py, check-listener-tripwire.py, check-description-field.py — at their pre-move path; all five parked to guardrails/attic/ on 2026-08-18 per attic/MANIFEST.md, still running from there, and their owning tests already import them from the attic path, so only the six TEST_MATRIX.md rows' prose was stale. Every other backtick-quoted path in TEST_MATRIX.md was checked against the tree and is not dead: two are template placeholders, one (M-461, hooks/midturn-chat-scan.py) already names its attic home in the same sentence, and four (skills/product-prover/{SKILL.md,README.md,LICENSE}, skills/text-audit/SKILL.md) are covered by the tracked external-skill-root contract tests/test_traceability.py already reads — a gitignored external clone, legitimately absent on a bare checkout. (5) The live edits this range carries, beyond the merge itself, are exactly four files: docs/MEASUREMENTS.md, guardrails/progress-baseline.json, scripts/measurements-table.py (commit 1de6fc14), and TEST_MATRIX.md (commit 3574cb2e). Nothing else moved. (6) The first push attempt of this range failed at the server's push gate on two real catches, both this branch's own oversights, named here rather than hidden: gate b's tests/test_rendered_sweep.py::test_gate_passes_the_real_tree reddened because docs/MEASUREMENTS.html — the reading companion page scripts/measurements-table.py also writes on every run — stood past its one reading; cleared with the named remedy python3 scripts/sweep-rendered.py (moved to attic/docs-MEASUREMENTS.html, manifest line added, base rule 10, commit 33a983be). This is the second time this exact page has needed sweeping after a measurements-table.py run (attic/MANIFEST.md already carried one entry from commit 063a55df, an earlier, unrelated session); every regeneration of docs/MEASUREMENTS.md writes the .html beside it, so every future regeneration will trip this same gate again unless the habit or the script changes — flagged to the coordinator, not fixed in this range. Gate h's tests_present check reddened because scripts/measurements-table.py changed (the reader-prompt path fix, commit 1de6fc14) with no change under tests/ — this script had no dedicated test at all before this range. tests/test_measurements_table.py now pins the fix itself, red-proved live (see Checks run) rather than added as a token touch, commit f289330a.
Blocking: none

Scope note: the coordinator narrowed the second delivery's perimeter to TEST_MATRIX.md's dead paths
alone. One adjacent staleness surfaced while walking the file — M-300's prose still claims
product-prover ships its own README/LICENSE inside the pack and that its README cross-references
design-reviewer, both reversed by the same 2026-08-13 decoupling (design-reviewer's README now
cross-references product-prover, confirmed by tests/test_traceability.py's own updated test and
comment) — but M-300 cites no path in backticks, so it sits outside "dead paths" as asked and was
left untouched, named here rather than fixed on my own word.
