# Prover record — 2026-08-19 row-id-collision

PUSH-REVIEW

Range: 07cf715d..bcd23563
- bcd23563 Add a test that every TEST_MATRIX/ROADMAP row id is claimed exactly once
- 53e6523e Renumber the two ids the merge collided with an unrelated package
- 12cb5f71 Merge remote-tracking branch 'origin/main' into fix/2026-08-19-remeasure-sweeps-itself
- d71c8f81 Prover record for the measurements-html opt-in fix and the clean dry-run precision fix
- 6524f288 Record the git-clean dry-run fix: TEST_MATRIX row M-548, ROADMAP row 626 (landed)
- e70904da Teach the worker-restore gate git clean's own dry-run guarantee
- 9880bc32 Make measurements-table.py's HTML render opt-in, so a bare remeasure trips no sweep gate
Files read: TEST_MATRIX.md, ROADMAP.md, docs/queue-archive/rotated-ROADMAP-2026-08.md, docs/queue-archive/rotated-ROADMAP-2026-07.md, docs/queue-archive/rotated-ROADMAP-2026-07-18.md, docs/prover/2026-08-19-clean-dry-run-precision.md, docs/prover/2026-08-19-gates-manifest.md, guardrails/check-matrix-reference.py, scripts/build-matrix-reference.py, scripts/rotate-doc.py, guardrails/check-doc-rotation.py, tests/test_row_id_uniqueness.py, tests/test_matrix_reference.py, tests/test_doc_rotation.py, guardrails/gates-manifest.json, guardrails/check-gates-manifest.py, .github/workflows/gates.yml
Checks run: `git show 12cb5f71 --stat` and `git diff --stat d71c8f81 07cf715d` — the merge's own file list, read before touching anything. `grep -oE '^\| M-[0-9]+' TEST_MATRIX.md | sort | uniq -c | awk '$1>1'` and the same over `grep -oE '^\| [0-9]+ \|' ROADMAP.md docs/queue-archive/rotated-ROADMAP-2026-08.md docs/queue-archive/rotated-ROADMAP-2026-07.md docs/queue-archive/rotated-ROADMAP-2026-07-18.md` — run before the renumber (found M-547 x2, M-548 x2, row 625 x2) and after (all empty). `python3 guardrails/check-matrix-reference.py TEST_MATRIX.md` — OK, 540/540 rows matched, both before and after the renumber (the gate cannot see the collision either way — this is exactly the gap the new test closes). `python3 guardrails/check-doc-rotation.py` — OK. `python3 -m pytest tests/test_row_id_uniqueness.py tests/test_matrix_reference.py tests/test_doc_rotation.py -q` — 47 passed. `python3 -m pytest tests/test_worker_restore.py -q -k "not TestTheGateIsArmedWhereItSaysItIs"` — 127 passed, 5 deselected (that one class reads this machine's real transcripts and is scoped out of a scripted check run for the reason below). Red-proven live: planted a second `| 44 |` row in ROADMAP.md and a second `| M-600 |` row in TEST_MATRIX.md, ran `python3 -m pytest tests/test_row_id_uniqueness.py -v`, both new tests failed naming the exact id and both its line locations; removed the plants and confirmed `git diff` on both files was empty before re-committing, both tests green again.
Findings: (1) The merge (12cb5f71) brought in a same-day, independently-authored package — gate af, `guardrails/gates-manifest.json`, the one generated home for the push gate's device (SPEC INV-210/INV-212) — already reviewed under its own record, `docs/prover/2026-08-19-gates-manifest.md`, committed before it landed on origin/main. That record's findings are not re-derived here; this record reads only what the merge itself did to files this branch also touches. (2) The merge auto-resolved with no textual conflict, but both sides had independently added rows to TEST_MATRIX.md and ROADMAP.md off the same shared ancestor (7ecff935) and picked the same "next free" identifiers: `M-547` and `M-548` each ended up naming two unrelated rows (mine: measurements-table.py's opt-in HTML render [INV-286] and the worker-restore gate's dry-run precision [INV-299]; the incoming package's: the gates-manifest home and the CI-mirror law-text check, both [INV-210, INV-212]), and ROADMAP row `625` named one live row (the incoming gate-device row, `*done*`) and one already-rotated archive row (mine, the measurements-html fix, `*landed*`, in `docs/queue-archive/rotated-ROADMAP-2026-08.md`). `guardrails/check-matrix-reference.py` (gate d) read clean throughout — "540 of 540 rows scanned", "committed Reference equals the fresh build" — because it verifies the body against the generated Reference table, never that a row's own id is claimed by exactly one row; a green gate d is therefore not evidence against this class of defect, and was not treated as such. (3) Verified by eye, not on the gate's word, that both sides' actual content survived whole: my ROADMAP row `626` (the git-clean fix, unique, no collision) reads intact and unchanged; the incoming package's four homes (`guardrails/gates-manifest.json`, `scripts/gen-gates-manifest.py`, `guardrails/check-gates-manifest.py`, `tests/test_gates_manifest.py`) and its `.github/workflows/gates.yml`/`guardrails/pre-push` wiring for gate af are present and untouched by this branch's own commits — `git diff --stat d71c8f81 07cf715d` names exactly those files and no overlap with the files this branch's own commits (9880bc32, e70904da, 6524f288) touched. (4) The coordinator's ruling on who renumbers: the row already on origin/main moves nothing, since a record, a test or another branch may already cite it; the row that has not shipped moves. Applied: `M-547`(mine) -> `M-600`, `M-548`(mine) -> `M-601`, archive row `625`(mine) -> row `700`, each taken with a margin of roughly fifty past the post-merge maximum (`M-548`/row `626`) to leave room for a concurrent lane resolving the same class of collision nearby. Every internal citation of the old numbers was swept and fixed: the `(INV-286, ROADMAP 625)` parenthetical inside the renamed `M-600` row itself, the archived row's own number, `ROADMAP.md`'s rotated-manifest row list (`602, 618, 619, 625, 626` -> `602, 618, 619, 626, 700`), and this session's own prior prover record (`docs/prover/2026-08-19-clean-dry-run-precision.md`), which had named the pre-collision numbers by hand; the incoming package's own two homes (`TEST_MATRIX.md` rows still at `M-547`/`M-548`, its own prover record `docs/prover/2026-08-19-gates-manifest.md`) were left exactly as they stand, per the ruling. `scripts/build-matrix-reference.py` rebuilt the Reference table after the renumber; `check-matrix-reference.py` reads 540/540 matched, no drift. (5) The standing gap this incident named: nothing in the tree enforced row-id uniqueness before today, and with four lanes now working the same files concurrently the next collision is not hypothetical. Per the coordinator's word, this is closed with a TEST under `tests/` rather than a new push gate — gate af's generated-device machinery (a manifest entry, a red proof, a CI-mirror step) is not owed for a defect this cheap to catch and repair by hand once caught. `tests/test_row_id_uniqueness.py` scans the real tree (TEST_MATRIX.md's body rows; ROADMAP.md plus every `docs/queue-archive/rotated-ROADMAP-*.md` archive) and asserts each id is claimed by exactly one line, naming every duplicate and its locations on a failure; it rides the ordinary suite. (6) `tests/test_worker_restore.py::TestTheGateIsArmedWhereItSaysItIs::test_the_gate_runs_against_this_machines_own_transcripts` was excluded from this record's own check run (see Checks run) because it now reads a real finding unrelated to this range: a worker in a sibling lane's worktree (`/private/tmp/live-spec-man/wt`, same shared git repository, different session id `afc7daf29f1501c3e`) ran `git checkout -- TEST_MATRIX.md` at 2026-08-19T11:01:06Z — a genuine ROADMAP-479 violation, correctly caught by both letter and meaning, in a worktree neither this branch nor this session touches. Named here rather than silently excluded; not this range's defect, not fixed here.
Blocking: none
