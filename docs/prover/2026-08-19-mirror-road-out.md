# Prover record — 2026-08-19 mirror-road-out

PUSH-REVIEW

Range: 5999b923..d7158520
- d7158520 Re-baseline the size ratchet after the mirror-road cull
- 969186db Close ROADMAP row 565: the mirror road it asked about is cut
- aea805d2 Retire Requirement 148 and 304 with the mirror road; trim 147 and 198
- 7b2980df Remove the mirror-sync mechanism: no standalone mirror repository ever received it
Files read: scripts/sync-mirrors.sh (deleted), .github/workflows/gates.yml, guardrails/pre-push, tests/test_mirror_editions.py (deleted), tests/test_mirror_release_history.py (deleted), tests/test_mirror_autosync.py (deleted), tests/test_made_with_attribution.py, tests/test_prover_adapter_contract.py, guardrails/check-config-health.sh, scripts/sync-skills.sh, scripts/shipped-language-allowlist.json, skills/product-prover-pack/SKILL.md, skills/text-audit-pack/SKILL.md, scripts/install-external-skills.sh, .gitignore, skills/design-reviewer/README.md, DECISIONS.md, ROADMAP.md, docs/queue-archive/rotated-ROADMAP-2026-08.md, PRODUCT_SPEC.md, PRODUCT_SPEC.index.md, spec/public-text-rules.md, spec/guardrails-freshness.md, spec/roles-and-agents.md, ARCHITECTURE.md, TEST_MATRIX.md, tests/test_formal_index.py, tests/test_traceability.py, guardrails/specformat.py, scripts/build-index.py, scripts/build-matrix-reference.py, guardrails/check-index-generated.py, guardrails/check-matrix-reference.py, guardrails/check-requirement-shape.py, guardrails/check-vocabulary.py, guardrails/rule-census.json, scripts/rule-census.py, docs/PROGRESS.md, scripts/progress-report.py, scripts/rotate-doc.py, guardrails/check-doc-rotation.py, guardrails/check-board.py, guardrails/check-ci-mirror.sh, guardrails/ci-mirror.json, guardrails/check-every-gate-can-fail.py, guardrails/check-named-checks.py, guardrails/check-pin-drift.sh, guardrails/check-tree-counts.py, guardrails/gate-red-proofs.json, scripts/check-registry.json, docs/prover/README.md, docs/prover/2026-08-19-invented-numbers-out.md (precedent), NEXT_STEPS.md, JOURNAL.md, docs/MEASUREMENTS.md, docs/handovers/2026-08-06-adoption-movement-handover.md
Checks run: `python3 -m pytest tests/test_traceability.py tests/test_formal_index.py tests/test_index_generated.py tests/test_made_with_attribution.py tests/test_prover_adapter_contract.py tests/test_ci_mirror.py -q` — 227 passed, 1 skipped. `python3 -m pytest tests/test_guardrails.py -q -k "not TestGateB_Tests"` — 1 failed before the record commit (TestGateA_ProverRecord::test_real_repo_passes, expected: no fresh record yet for this range), else green; TestGateB_Tests itself is the machine's declared known-red (test_real_content_passes) and was excluded per standing instruction. `python3 guardrails/check-index-generated.py PRODUCT_SPEC.md PRODUCT_SPEC.index.md` — OK, 393/393 rows matched. `python3 guardrails/check-matrix-reference.py TEST_MATRIX.md` — OK, 535/535 rows matched. `python3 guardrails/check-requirement-shape.py PRODUCT_SPEC.md spec/*.md` — OK, 1744 criteria across 307 requirements. `python3 guardrails/check-vocabulary.py PRODUCT_SPEC.md` — OK, every glossary term used, no banned coinage. `bash guardrails/check-ci-mirror.sh` — OK. `bash guardrails/check-pin-drift.sh` — OK, 206 pins. `python3 guardrails/check-every-gate-can-fail.py` — OK, 28 gates. `python3 guardrails/check-named-checks.py` — OK. `python3 guardrails/check-doc-rotation.py` — OK. `python3 guardrails/check-board.py` — OK. `python3 guardrails/check-tree-counts.py` — OK. `python3 guardrails/check-doc-findings-bound.py` — OK, 164 live documents, none above its record, after the rule-census rebaseline. `bash -n guardrails/pre-push`, `bash -n scripts/sync-skills.sh`, `bash -n guardrails/check-config-health.sh` — all OK. `python3 -c "import json; json.load(open('scripts/shipped-language-allowlist.json'))"` — OK. `bash guardrails/check-shipped-language.sh` — OK, no offences. YAML re-validated after the gates.yml edit (`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/gates.yml'))"` — one job, `gates`).
Findings: (1) Every piece of evidence the coordinator's decision rested on was verified against the live tree before cutting, not assumed: scripts/sync-mirrors.sh really does derive its mirror target as `${GITHUB_OWNER}/${skill_name}` from folder names under `skills/`, every one of which now carries a `-pack` suffix; commit 037e2329 (same day, earlier in this range's history) recorded in its own message "today's sync run reports every skill 'skipped (no mirror repo yet)'", confirming no live mirror repository exists for any of the eleven; product-prover and text-audit are genuinely sourced externally now (`scripts/install-external-skills.sh`, `skills/product-prover-pack/SKILL.md` and `skills/text-audit-pack/SKILL.md`'s `requires:` lines naming `github.com/happysasha18/product-prover` and `/text-audit`); and `editions/product-prover/` was already gone, retired the same day as dead weight by 037e2329, with `editions/` left with no other resident. No evidence line failed verification. (2) `skills/design-reviewer/README.md` — a live, tracked, non-external skill file — carries a stale mirror banner on its last line: "*Read-only mirror of one skill from the live-spec pack ... changes land in the pack and sync via `scripts/sync-mirrors.sh`.*", a leftover the sync script's own `banner_for()` used to stamp onto a real standalone mirror at some earlier point in the project's history. This is now inaccurate (the script it names no longer exists) but under `skills/`, which this task was explicitly told to leave untouched and to stop on rather than edit. No test reads or checks this line — `tests/test_traceability.py` only asserts the file mentions "product-prover" elsewhere — so nothing mechanically reds on it; it is reported here rather than silently left for a later session to rediscover. (3) `D-4` ("the pack is the source ...") looked at first read like a third invariant this cull would orphan, since its one MIRROR-shaped criterion sits in Requirement 198. A closer grep (past the exact-bracket `[D-4]` search that missed compound brackets) found it still anchored twice in `spec/guardrails-freshness.md`, both about the LOCAL installed-skill-copy sync under `~/.claude/skills` (`scripts/sync-skills.sh`) — a live, unrelated application of the same "repo is the source" decision. Only Requirement 198's mirror-specific criterion was removed (renumbering the requirement's remaining four); D-4 itself was correctly NOT added to `tests/test_formal_index.py::EXPECTED_GAPS`, and the fresh `build-index.py` run confirms it keeps its `R251.3, R275.5` anchors. (4) Requirement 147 criterion 3 was a compound sentence — one live clause ("apply the line to each built-with project through its own queue") and one dead clause ("stamp it onto each standalone mirror ... at every sync") joined by "and". Only the dead half came out; INV-96 keeps its three live criterion anchors under Requirement 147 and loses only the ones under the now-gone Requirement 148. (5) ROADMAP row 565 was the one live, non-`*done*` queue row whose entire subject was the mirror road (the public prover edition's two example-run-record ledger shapes) — closed via `scripts/rotate-doc.py --close-row 565` with the reason named on the row, the same shape as the same day's row-618/619 closes. Every other ROADMAP hit for "mirror" or "sync-mirrors" is either an already-`*done*` historical row (597, 608, 610, 611, 612) or an unrelated use of the word "mirror" (the CI-gates mirror, rows 171/511/553) — left untouched. (6) `guardrails/gate-red-proofs.json`, `guardrails/ci-mirror.json`, and `scripts/check-registry.json` were checked for mirror-road entries and carry none — the prior day's commits (037e2329 and its siblings) had already cleared `guardrails/rule-census.json`'s editions/product-prover entries, and these three registries never held any. (7) The `rule-census.json` rebaseline (`python3 scripts/rule-census.py --json guardrails/rule-census.json`) moved byte and finding counts down for five files, not only `spec/public-text-rules.md` — `ARCHITECTURE.md`, `TEST_MATRIX.md`, `ROADMAP.md`, and `PRODUCT_SPEC.index.md` all shrank too, as a direct consequence of the same range's edits; every recorded figure fell, none rose, consistent with the ratchet's one-way rule, and `check-doc-findings-bound.py` runs clean after it. (8) `NEXT_STEPS.md` names `scripts/sync-mirrors.sh` three times in its dated "LIVE STATE (2026-08-13)" digest, describing a run six days before this range; it was left untouched — it is a superseded status snapshot the digest's own convention replaces wholesale on the next update, not a place this task's instructions named for editing, and JOURNAL.md, `docs/MEASUREMENTS.md`, and `docs/handovers/2026-08-06-adoption-movement-handover.md` are dated history of the same kind.
Blocking: none

Root: the coordinator's decision — cut the mirror-sync road whole — rested on five stated pieces of
evidence, all independently re-verified here (finding 1) before anything was removed. The road once
worked for real: JOURNAL.md's 2026-07-13 entry ("the CI mirror-sync arm turned on with a deploy key,
no owner action") records a deploy key registered on `happysasha18/product-prover`, a real SSH clone
and an "up to date" report proven by deed before that push, and names the same push's version bump as
the mirror's first real write; ROADMAP row 597 (2026-08-12, closed) still finds "the public mirror
`happysasha18/product-prover` existed" a month later. The release-history and public-edition machinery
(Requirement 148, INV-181; Requirement 304, INV-303) were built to serve that live mirror. The v5.0.0 decoupling
then moved product-prover (and later text-audit) out to their own canonical source repositories,
installed inward by `scripts/install-external-skills.sh` rather than shipped outward by the sync — the
opposite direction of traffic — and every skill folder left in `skills/` picked up a `-pack` suffix that
no mirror target ever matched again. The mechanism kept running, kept reporting "skipped," and kept
being maintained (ROADMAP row 597's own fix landed 2026-08-12) for a destination that no longer existed
to receive it.

`scripts/sync-mirrors.sh`, its `sync-mirrors` CI job, its non-blocking pre-push tail, and the three
tests that guarded its publish-source selection, release-history generation, and autosync wiring are
gone whole (commit 7b2980df). Requirement 148 and Requirement 304 (INV-181, INV-303) — the spec's own
statement of what a standalone mirror carried — retire with their one live consumer, their numbers
left empty per the standing retired-law convention rather than renumbering every later citation
(commit aea805d2). Requirement 147 and Requirement 198 each lose the one clause that was about the
mirror road specifically and keep the rest, since neither requirement's subject was the mirror road
alone (attribution offering in general; the shared-rules-live-once law in general). ROADMAP row 565,
the sole live queue item whose whole subject was the mirror road, closes with the rest (commit
969186db), and the size ratchet is re-baselined to the leaner shipped state (commit d7158520).

Not touched, on the explicit boundary this task was given: `scripts/install-external-skills.sh` and
the external-source road it carries (live, unrelated); `tests/test_ci_mirror.py` and
`guardrails/check-ci-mirror.sh` (gate-parity mirroring, a different "mirror" than this road, confirmed
by reading both before leaving them alone); every file under `skills/`, including the one stale
reference found there (finding 2), left for the coordinator's own word since editing under `skills/`
is blocked here for a separate, standing reason; and every `docs/prover/` record, which is this
project's history and stays exactly as it read on the day it was written.
