# Prover record — 2026-09-09 the stand-down that can no longer fire

PUSH-REVIEW

Mode: closure
Range: a7a5c1f..4234849
- 4234849 Gate a picks its record by what the record covers
Files read: PLAN.md (row q-830), .live-spec/checkpoints/q-830.md, .live-spec/readers/q-830-reader.md, guardrails/check-prover-record.sh, guardrails/pre-push, guardrails/README.md, guardrails/install.sh, guardrails/check-config-health.sh, docs/prover/README.md, matrix/product-prover.md, spec/guardrails-freshness.md, scripts/plan_checks.py, .github/workflows/gates.yml, JOURNAL.md, tests/test_guardrails.py, tests/test_push_review.py, tests/test_record_is_judged_by_reach_not_by_date.py, tests/test_deletion_only_push.py, skills/product-prover-pack/SKILL.md, docs/prover/2026-09-07-the-door-reads-the-record-and-where-that-read-goes-soft.md, docs/prover/2026-09-08-the-time-oracle-changes-what-it-proves.md
Checks run: the row's own acceptance command, verbatim at this commit (`PYTHONPATH=tests python3 -m unittest -q test_record_is_judged_by_reach_not_by_date && ! grep -q 'TODAY\*.md' guardrails/check-prover-record.sh && ! grep -q 'a record dated today exists' docs/prover/README.md`) — exit 0; `python3 -m unittest -v test_record_is_judged_by_reach_not_by_date` — 6 passed; `python3 -m pytest -q -k "GateA_ProverRecord" tests/test_guardrails.py` — 14 passed; `python3 -m pytest -q tests/test_push_review.py` — 23 passed; `python3 -m pytest -q tests/test_deletion_only_push.py` — 15 passed; `python3 -m pytest -q tests/test_guardrails_unit.py tests/test_guardrail_fixture_proofs.py tests/test_gate_common_table_rows.py tests/test_matrix_reference.py tests/test_prover_doc_homes.py tests/test_review_record_class.py tests/test_review_modes.py` — 89 passed; `python3 -m pytest -q tests/test_spec_parts.py tests/test_requirement_shape.py tests/test_index_generated.py tests/test_row_id_uniqueness.py tests/test_retroactive_gate.py tests/test_scaffold_guardrails.py` — 80 passed, 1 skipped; `bash guardrails/check-config-health.sh` — exit 1, "installed hook drifted from source: pre-push"; `bash guardrails/check-prover-record.sh --push docs/prover 2026-09-09` at this commit before this record landed — exit 1, 44.9 KB of output; and the two gate scripts, the one this push sends and the one origin/main holds, driven side by side over throwaway repositories built for the purpose (the inbox carve-out, the case-or-space carve-out, the recordless class with and without a committed record on file, an old committed record edited in the working tree alone, and a record staged and never committed).
Findings: The row's stated outcome is that the two arms which decide anything refuse exactly what they refused before. Both do: driven over throwaway trees, a record naming another range is refused, a record older than a PRODUCT_SPEC.md change is refused, and a record named for the 8th carries a push on the 9th. The acceptance command passes verbatim. What the row did not hold is the third arm the candidate list feeds — the recordless stand-down — and two paths the diff touches. Three blocking items below.

Beyond them, three things the read found and does not block on. First, the candidate list can hand the field arms a record other than the one written for the push: it is every committed record, walked in reverse filename order, and the match is a plain substring of the whole body, so a quoted hash counts. Over the last forty commits of origin/main, three single-commit ranges are named by more than one committed record; 186dc4a..18f41e1 is named both by the 2026-09-07 record written for it and by the 2026-09-08 record whose own range is a superset and which quotes those hashes in its prose besides. Reverse order picks the later one, so the Mode, field and Blocking arms then judge a document written for a different push. Nothing was found that matched a range it did not cover, and a superset record does cover a subset range, so no push passes on a review it never had — but the cross-day pairing was impossible while the list was one day wide. Second, the gate judges the working-tree bytes of a candidate, not the committed ones: `cat "$rec"` at guardrails/check-prover-record.sh:363. An old committed record whose committed body says nothing about the push, edited in the working tree and never committed, carries the push on the script this range sends (exit 0) where origin/main's script refused it by date (exit 1) — and a record placed with `git add` and never committed carries the push on both scripts alike (exit 0 either way), because `git ls-files` reads the index. The hole is therefore not new, and the same tree with a today-dated record proves origin/main's script equally open to it; what is new is that any of the 525 records on file can now host it rather than only one written today. The comment this diff adds at guardrails/check-prover-record.sh:234 states the opposite as settled — that an uncommitted record is refused above, by name, before this — and the staged-record run falsifies it, since the on-disk arm runs only where the candidate list came back empty. Third, guardrails/README.md:78, inside the bullet this diff rewrote, still says the record carries its five fields; the gate reads six, Mode among them.

Blocking: three, each closed in the landing this record covers. The reviewer wrote none of the change and none of the repairs; what each `stands:` said is kept as written, and the `closed:` beside it is the holder's, added after this read.
- The recordless-class stand-down can no longer fire in this repository. stands: the arm sits inside `if [ ${#candidates[@]} -eq 0 ]` at guardrails/check-prover-record.sh:169, and the candidate list is now every committed date-named record — 525 of them here — so the branch is unreachable on any tree that holds one. Two throwaway repositories differing only in whether a committed record sits in docs/prover, each pushing a range whose single commit touches docs/skill-review/ alone: origin/main's script exits 0 with the stand-down by name in both, this range's script exits 0 without the record and exits 1 with it, refusing the push as unreviewed since it was last edited. The row's own outcome sentence says the arms refuse exactly what they refused before, and this one refuses more. SPEC R226 criterion 6 still names the exception, guardrails/pre-push:118 still tells a reader the recordless class is one of the two stand-downs that exist, and tests/test_deletion_only_push.py still holds the marker at guardrails/check-prover-record.sh:188 against that criterion, so the enumeration now guards code no push can reach. The fixture that would have caught it, tests/test_push_review.py::test_a_recordless_class_range_stands_down, plants its record as docs/prover/scratch-note.md — a name the new date regex skips — so the candidate list stays empty there and the test passes over the defect. Fix: judge the recordless class before the candidate list is consulted, or on the range rather than on the emptiness of that list, and give the fixture a date-named record so it fails when the arm dies. closed: the arm is hoisted out of the empty-candidates branch it could no longer reach. Its condition was always about the RANGE, and the guard it sat behind meant "no candidate" only while a candidate had to carry today's date. It now runs on the push road against the range alone, ahead of the record demand. The fixture that stayed green plants a dated record before the base, so the candidate list is non-empty when the stand-down is judged; a prover-only range is answered by this arm now rather than by the range arm below it, and `test_a_range_of_record_commits_alone_stands_down` reads the message that actually fires.
- Gate m refuses this very push. stands: `bash guardrails/check-config-health.sh` exits 1 with "installed hook drifted from source: pre-push". This range edits guardrails/pre-push's gate-a banner and guardrails/install.sh was never run after it, so .git/hooks/pre-push still prints the retired wording. That is the one line a person meets before any document, and both the JOURNAL entry this range adds and tests/test_record_is_judged_by_reach_not_by_date.py::test_the_wording_a_push_prints_and_the_two_documents_say_what_the_gate_does claim it now says what the gate does; the test reads guardrails/pre-push in the repository and never the installed copy, so it passes while the banner every push actually prints is the old one. PLAN.md's own trap list names the missing step. Fix: run `./guardrails/install.sh`. closed: `./guardrails/install.sh` ran, and `guardrails/check-config-health.sh` returns OK (installed hooks match their sources), so the banner the hook prints is the repository's.
- The refusal message now buries its own verdict. stands: the missing-record report loops over every candidate, and the candidate list went from at most a day's records to all of them. Run at this commit before this record landed, `bash guardrails/check-prover-record.sh --push docs/prover 2026-09-09` printed 44.9 KB — the FAIL line on the fourth line, then 525 lines each saying one record names none of the pushed range. That is what a person now meets inside a pre-push hook when the gate refuses. The comment this diff adds beside the passing message says listing every committed record would bury the verdict and that the count and the newest stand for the set; the reasoning was applied to the message nobody has to read and withheld from the one they do. Fix: report the near misses only, or the count alone, on the refusal path too. closed: the report names the three newest records it walked and counts the rest, which is the reasoning the passing message already carried.


## After the read — what the holder changed

The three above are closed inside the landing this record covers, each repaired where it stood. One
non-blocking finding of this record is closed with them, because it was the same widening: the field
and range arms read a candidate with `cat`, so a record committed long ago, edited in the working
tree and never committed, carried a push. The retired date filter had refused such a record by its
name, and nothing refused it after. Both arms read `git show HEAD:<record>` now — the committed
bytes. `guardrails/README.md`'s "five fields" is corrected to six in the same landing.

One non-blocking finding stays open and is not repaired here: a range can be named by more than one
committed record, and the gate takes the first that names it — three of the last forty single-commit
ranges have two such records, one of them a superset record quoting the hashes in prose. It stands
in this record for a later read to pick up.
