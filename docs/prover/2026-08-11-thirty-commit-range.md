# Prover record — 2026-08-11 thirty-commit range

PUSH-REVIEW

Range: e82da27..68ee8b9
- 68ee8b9 Re-splice the rebuilt index row into the spec's embedded Reference table
- ee70d75 Rebuild the spec index after the INV-220 citation removal
- 7afe7ba Close the second push review's blocking gate and its two residue findings
- dbb5794 Close the repairable findings from the thirty-commit push review
- 5dba714 Add the structured Verdict line the repaired skill-review gate parses, to two same-day records
- 5e80ff4 The afternoon's movement closes into the resume file and the journal
- 391ba41 Repair the five recorded debts from the 2026-08-09/10 push reviews
- c9a4088 Carry product-prover's newer skill work into its public edition
- 2954a77 Split the 36-word Fable-economy sentence and ledger the parallel-suite temp contention
- 06cf3ae Merge the two review records a push carried into one, read by one gate (D4)
- 78a47a5 Stamp plan-v3 with execution statuses, record D5 yes and the rule-23 post-action
- b46b3d6 The sitting's second pass: D3 ruled keep, three items stay open on his questions
- fa6d6a9 The R7 escort inventory lands: 41 escorts traced, 21 his-word, 11 derived, 9 invented
- a829e8b Retire the answer-first Stop-hook arm: 3,095 runs, zero catches (D8, his word 2026-08-11 10:23)
- 4f2aa7f The transcript sweep's three gaps close, and the session is ready for its wipe
- ba94bbc The D2 fix lives in the plan instead of the chat, and the resume separates his direct words from derivations
- 5248db3 The local-commit count becomes a command instead of a number that goes stale
- cef7870 One resume sentence comes back under the cap
- 50358bb The sitting's first pass lands in the records, and the campaign turns to cutting over repairing
- d9a4d88 R1b: the last two pages place the product spec with the host, and the run-from-root falsehood goes
- 7f0831d The resume block records phase R closed, with its two queued follow-ons
- 781205e Four pointers left behind by the pin sweep come home, and the review that proved the night's two commits has its own file
- d7d73e1 R1: every path a skill names says which tree it lives in, and the review that proved it has its own file
- 713ceb0 R3: every rule's enforcing machine and recorded fires are on one page, and the ledger work waits as a decision row
- 92a5438 Two resume sentences come back under the cap
- 91ac677 The resume block carries the night's phase-R landings and what stays open
- 3915e95 R6: the architecture-pointer gate proves a pin against its own line, and eight rotten pointers come home
- cfe3cff The plan-delta guard moves into the hook's source and stands down where it has no script, and the resume block reads clean
- 87ffce6 R5 lands its finding in the plan: stale line citations become pinned snapshots and section names
- d11331f R5: the 53 rules outside the shared rulebook are priced, and the counting convention is named
- 66aa265 R2: the rulebook's self-repetition is measured, and the reclaimable weight is named
- 5a361fd The numbers table carries the same measured baseline as the batch test, on the same delta page
- 54442ec The plan's batch-test baseline comes back to what the fresh check measured, with its delta page
- 6c448a4 The plan's third revision lands beside its base with its line-by-line sweep, and one plan becomes the standing order

Files read: PRODUCT_SPEC.md, ARCHITECTURE.md, NEXT_STEPS.md, TEST_MATRIX.md, README.md, docs/prover/README.md, docs/prover/2026-08-11-architecture-pin-sweep.md, docs/PROGRESS.md, guardrails/check-prover-record.sh, guardrails/check-doc-findings-bound.py, guardrails/check-tree-counts.py, guardrails/pre-push, guardrails/language-rules.json, guardrails/judge-hooks.json, guardrails/hook-red-proofs.json, guardrails/gate-red-proofs.json, guardrails/progress-baseline.json, scripts/install-pack-hooks.sh, scripts/install-session-hooks.sh, scripts/rule-census.py, hooks/hedge-scan.py, hooks/lean-orchestrator-scan.py, tests/test_push_review.py, tests/test_affirmation_arm.py, .live-spec/culling-plan-v3-2026-08-10.md, .live-spec/escort-inventory-R7-2026-08-11.md, .live-spec/r5-rule-prices-2026-08-11.md, .live-spec/r3-rule-fires-2026-08-11.md, .live-spec/r2-repetition-2026-08-11.md

Checks run: first pass at 5dba714 — `python3 guardrails/check-doc-findings-bound.py` — FAIL, exit 1, NEXT_STEPS.md carries 3 findings against a recorded zero; `python3 scripts/rule-census.py NEXT_STEPS.md` — 2 long (longest 27 words), 1 style, 0 register, 3 in all; `python3 -m pytest -q` — 1 failed, 2,483 passed, 2 skipped, exit 1, the failure being tests/test_doc_findings_bound.py::TestDocFindingsBound::test_the_real_repository_passes; `python3 guardrails/check-tree-counts.py` — OK, 3 of 3 rows matched; `bash guardrails/check-prover-record.sh --push` — FAIL on the range arm, this record being the thing that closes it; `grep -oE -- '-- gate [a-z]{1,2}:' guardrails/pre-push | sort -u | wc -l` — 29; `git rev-list --count origin/main..HEAD` — 30; `python3 scripts/rule-census.py` over the 122 live documents — 4,874 open findings, 21 documents at zero. Second pass at dbb5794: `python3 guardrails/check-doc-findings-bound.py` — OK, exit 0, 122 live documents, 22 held at zero, none above its record; `python3 scripts/rule-census.py NEXT_STEPS.md` — 0 findings, longest sentence 25 words; `bash guardrails/check-config-health.sh` — FAIL, two installed hooks drifted from source; `bash guardrails/check-skill-review.sh` — OK; `bash guardrails/check-pin-drift.sh` — OK, 209 pins; `python3 guardrails/check-tree-counts.py` — OK; `python3 -m pytest -q` — 3 failed, 2,481 passed, 2 skipped, exit 1, in 346.69s; `git rev-list --count origin/main..HEAD` — 31.

Findings, second pass: dbb5794 closes the first pass's blocking finding and most of its repairable ones, and introduces one new blocking finding of its own. The census it regenerated is clean in the direction that matters — every changed `long`, `style` and `total` value falls (1,865 to 1,863, 101 to 99, 38 to 37) and only `bytes` values move both ways, so no document's recorded ceiling was raised and no ratchet relaxed. Its edits to the two hook sources, however, were made in the repository alone, and the installed copies under the personal layer still carry the old wording, so the parity gate now refuses the push. That is finding 10 below, and it blocks.

Findings: the range is thirty commits across ninety-eight files, and its two structural moves are the D4 merge of the second push-review gate into this one and the D8 retirement of the answer-first Stop-hook arm. Both retirements are wired correctly — no registry, hook roster, gate roster or CI mirror still points at the retired script or the retired record home, and the negative assertions in tests/test_push_review.py hold the retirement in place. The push nevertheless fails its own gate aa, and it fails deterministically, outside pytest. That is the one blocking finding. Eight further findings are recorded below without blocking, ranked by the cost of leaving them.

1. Blocking. Gate aa refuses the push. `python3 guardrails/check-doc-findings-bound.py` exits 1 with `FAIL (doc-findings-bound): NEXT_STEPS.md was repaired to zero and now carries 3 finding(s).` The gate is wired at guardrails/pre-push line 233 and sets `fail=1`, so the push road cannot pass while the finding stands. The census names the three: two sentences past the 25-word cap, the longest at 27 words, and one style finding. Walking the range shows where it entered — 6c448a4 carried 6 findings and was repaired, 91ac677 carried 2 and was repaired, 50358bb and 78a47a5 each carried 1 and were repaired, and 5e80ff4 carried 3 and was not. 391ba41's message records a green suite, which was true when it was written; 5e80ff4 broke it two commits later and no suite ran after. This is not the known cross-process contention signature in .live-spec/PROBLEMS.md, which shows only leaked-temp assertions: the gate script reproduces the red on its own, with no suite running beside it.

2. Non-blocking, but it rides in the same repair. The same gate run reports three documents whose recorded ceiling now sits above what the text measures: PRODUCT_SPEC.md recorded 1,865 and measures 1,863, docs/language-rule-coverage.md recorded 101 and measures 99, docs/language-rules.md recorded 38 and measures 37. The gate prints them as fallen rather than failing on them, and names its own repair, `python3 scripts/rule-census.py --json guardrails/rule-census.json`. Owner: whoever repairs finding 1, in the same commit.

3. Non-blocking. docs/PROGRESS.md is stale against the tree it publishes. Lines 9 and 201 read 4,876 open writing findings against a measured 4,874, and line 202 reads 22 documents at zero against a measured 21. Four per-document rows carry the drift, NEXT_STEPS.md among them, at 0 against its actual 3. guardrails/progress-baseline.json carries the same pair of stale numbers. Line 5 also reads that the page compares against a run on 2026-08-11 while the page itself was generated on 2026-08-11, so the delta compares the run to itself. Owner: regenerate the page and its baseline after finding 1 lands, since the repair moves the numbers again.

4. Non-blocking. .live-spec/culling-plan-v3-2026-08-10.md line 73 still reports D4 as in progress by a worker. D4 landed in this range, at 06cf3ae. NEXT_STEPS.md line 31 already records it as executed with the gate and requirement retired, so the two live surfaces disagree, and the plan is the standing order and the page he reads. Owner: the plan's keeper, before the next showing.

5. Non-blocking. The same plan's numbers table, lines 21 and 22, counts 31 blocking checks at the start and 30 today, and 6 dated real catches out of 31. The tree measures 29 gate letters, since gate ac was retired inside this very range. One of the six dated catches is gate ac itself. .live-spec/r3-rule-fires-2026-08-11.md line 30 repeats the same 31. Owner: the plan's keeper, with the same edit as finding 4.

6. Non-blocking. Four shipped surfaces still describe the retired answer-first arm in the present tense. scripts/install-pack-hooks.sh line 5 advertises installing it, though its own JUDGE_FILES roster no longer carries the file; hooks/hedge-scan.py line 34 and hooks/lean-orchestrator-scan.py line 28 both cite it as a live sibling; scripts/install-session-hooks.sh line 15 and tests/test_affirmation_arm.py line 8 name the deleted test file as existing. TEST_MATRIX.md lines 615 and 616 anchor two live rows on the retired arm's test level while M-401 itself is stamped retired on line 603. PRODUCT_SPEC.md line 3104 still lists INV-220 among the nets R135.3 rests on, though INV-220 now reads that the system shall carry no such Stop-hook arm. The installer header is the worst of these, being an adopter-facing surface. Owner: a sweep commit for the D8 retirement.

7. Non-blocking. .live-spec/r5-rule-prices-2026-08-11.md pins 53 rules by file and line. All 53 land correctly at its own commit d11331f; at HEAD 15 land and 38 have rotted, because 391ba41, c9a4088 and d9a4d88 edited the skill bodies later in this same range. 87ffce6 named exactly this class of defect and repaired it in the plan only, not in R5's own table. check-pin-drift.sh reads ARCHITECTURE.md alone, so .live-spec pages carry no guard. The rule count and the byte arithmetic still reconcile; it is the pins that moved. Owner: either re-pin the table or stamp it as a dated snapshot; the second is cheaper and is what 87ffce6 chose for the plan.

8. Non-blocking. .live-spec/r2-repetition-2026-08-11.md's file-size table totals 417,600 bytes against a measured 421,025, with 10 of 12 per-file values stale. The plan's rulebook volume row, line 23, reads 73,578 bytes against 73,903 returned by the plan's own stated command; the whole 325-byte drift is the profile file, which grew today. That row is the campaign's target measure. Owner: the same keeper, at the next measurement.

9. Non-blocking. Three counts in the range have no reachable source or read one short. The figure of 3,095 runs with zero catches now ships in PRODUCT_SPEC.md, TEST_MATRIX.md, docs/language-rule-coverage.md, guardrails/language-rules.json and attic/MANIFEST.md, but guardrails/net_meter.py names its log as .live-spec/net-meter.jsonl and no such file exists on the machine; the only cited source records the decision rather than the measurement. Separately, 3915e95's message and the journal say eight rotten pointers where the diff touches nine — six moved to a new line and three kept their line with a reworded label — a discrepancy the range's earlier record, docs/prover/2026-08-11-architecture-pin-sweep.md line 24, already states correctly. Last, .live-spec pages speak of nine working skills where skills/live-spec-base/SKILL.md defines ten, two near-identical sets under one name. Owner: the number is already shipped in the spec, so it wants either a recoverable log or a sentence saying the count is a decision-time reading.

What held. The retirements are clean where it counts: guardrails/judge-hooks.json, hook-red-proofs.json and gate-red-proofs.json carry no trace of the retired arm or of gate ac, the fixture directory is gone, the pre-push roster measures 29 letters and guardrails/README.md line 12 says 29, and check-every-gate-can-fail.py and check-ci-mirror.sh both pass. The escort inventory's 41/21/11/9 reconciles once the five cross-section duplicates are removed. README.md's 6,456 and 5,218 line counts, the 35 shared rules, the 53 rules outside the rulebook, and all eight spec size measures on docs/PROGRESS.md reconcile against the tree. 5248db3 left no other hardcoded commit count anywhere in the resume, plan or queue pages. PRODUCT_SPEC.md and ARCHITECTURE.md were touched by five commits in this range and hold together where they were touched, with the single exception recorded as finding 6.

10. Blocking, and new in dbb5794. Gate m refuses the push. dbb5794 reworded one comment line in hooks/hedge-scan.py and one in hooks/lean-orchestrator-scan.py, repairing the present tense finding 6 named. The repair landed in the repository only. The installed copies under the personal layer still carry the old wording, and the parity gate compares the two, so `bash guardrails/check-config-health.sh` now reports `installed hook drifted from source: hedge-scan.py` and the same for lean-orchestrator-scan.py. The gate is wired at guardrails/pre-push line 158 as gate m and sets `fail=1`. `diff ~/.claude/hooks/hedge-scan.py hooks/hedge-scan.py` returns exactly the one reworded line, and the same for the other hook, so the drift is this commit's and nothing else's. Two suite tests carry it, tests/test_config_health.py::TestConfigHealth::test_this_repo_installed_hooks_match_source and ::TestPermissionPathHealth::test_real_personal_settings_stands_down_or_passes. The gate names its own two repairs: re-run scripts/install-pack-hooks.sh so the installed copies match, or declare the difference in guardrails/local-overrides.json with a reason and the installed file's fingerprint. The first is the right one here, an override being for a difference kept on purpose, and this one is an oversight.

Second-pass dispositions of the first pass's findings. Finding 1 closed by dbb5794: NEXT_STEPS.md measures 0 findings, longest sentence 25 words, and gate aa exits 0. Finding 2 closed: guardrails/rule-census.json regenerated, all three recorded ceilings lowered to what the text measures. Finding 3 closed: docs/PROGRESS.md and guardrails/progress-baseline.json now read 4,871 findings and 22 documents at zero, matching the gate's own count of the live set. Finding 4 closed: the plan's line 73 reads D4 executed at 06cf3ae. Finding 5 closed: the plan's gate-count row now reads 31 at the start, 30 on 10.08, 29 after D4, and the tree measures 29. Both plan edits are rooted in a new delta page, .live-spec/plan-v3-delta-2026-08-11-4.md, as the plan-delta guard asks. Finding 7 closed as queued: ROADMAP.md row 588 carries the R5 pin rot with a done-when, which is the disposition the finding asked for. Finding 6 partially closed, and finding 9 partially closed — the residues are findings 11 and 12. Finding 8 stands untouched.

11. Non-blocking, the residue of finding 6. dbb5794 repaired the six surfaces the finding named one by one, and left the class. TEST_MATRIX.md rows M-459 and M-460 carry the identical construction the repaired rows carried, naming the retired arm in the present tense: `the same text-in/decision-out level the scissors, hedge, and answer-first arms take (M-420, M-401)`. PRODUCT_SPEC.md line 3104 still lists INV-220 among the nets R135.3 rests on, though INV-220 now reads that the system shall carry no such arm. Owner: the same sweep, run as a class this time — grep the tree for the arm's name and read every live hit, rather than the enumerated list.

12. Non-blocking, the residue of finding 9. The unsourced figure is gone from PRODUCT_SPEC.md's requirement and user story, which was the surface that stated it as a bare fact. It survives in TEST_MATRIX.md line 603, guardrails/language-rules.json line 2630, docs/language-rule-coverage.md line 870, attic/MANIFEST.md line 46 and JOURNAL.md line 2631. Every surviving mention names DECISIONS.md as its source, so each carries its provenance and none asserts the number on its own authority. Recorded rather than pressed: the underlying meter log is still not on the machine, so the figure remains a decision-time reading wherever it appears. Owner: whoever next touches the meter, if a recoverable log is wanted.

13. Non-blocking, and new in dbb5794. The regenerated docs/PROGRESS.md is stale against the very commit that ships it. The page as committed states PRODUCT_SPEC.md at 702,715 bytes and 117,798 words; PRODUCT_SPEC.md at that same commit measures 702,701 bytes and 117,796 words. The gap is exactly the two occurrences of the figure the same commit dropped from the requirement stub, so the report was generated before the spec edit and committed after it. The finding-count half of the page, 4,871 across 122 documents with 22 at zero, does reconcile. Owner: regenerate the page last, after every other edit in the commit, or regenerate it once more now. Noted with it: running the suite rewrites docs/PROGRESS.md in place through tests/test_progress_report.py, so a full run leaves that tracked file modified in the working tree. It is modified now for that reason and for no edit of this reviewer's; this reviewer is barred from restoring it, so it wants a look before the next commit is staged.

Checks run, third pass at ee70d75: `bash guardrails/check-config-health.sh` — OK, exit 0, installed hooks match their sources; `python3 guardrails/check-doc-findings-bound.py` — OK, exit 0, 122 live documents, 22 held at zero; `python3 guardrails/check-index-generated.py PRODUCT_SPEC.md PRODUCT_SPEC.index.md` — OK, exit 0, 398 of 398 rows matched, committed index equals a fresh build; `python3 scripts/build-index.py PRODUCT_SPEC.md -o <scratch>` then compared against both copies; `wc -c`/`wc -w` on PRODUCT_SPEC.md against docs/PROGRESS.md; `python3 -m pytest -q` — 2 failed, 2,482 passed, 2 skipped, exit 1, in 346.18s.

Why the third pass ran the whole suite rather than the named gates. The offer was to spot-check, on the ground that the previous round's reds were addressed and each gate script reproduces alone. Every named gate did pass — m, aa and x all exit 0, and the two matrix rows and the R135.3 citation read correctly. The blocking defect below is caught by none of them. It is held by a suite test, so a targeted run would have returned three greens and shipped a spec that contradicts itself. Twice already in this range a repair has broken something the repairing commit did not look at, which is the argument for the wide net over the narrow one.

14. Blocking, and new in ee70d75. The generated index exists in two places and they have drifted. `PRODUCT_SPEC.index.md` line 279 reads `| INV-220 | R231.1 |`, and a fresh `python3 scripts/build-index.py PRODUCT_SPEC.md` agrees with it. The same table spliced into the spec under `## Reference`, at PRODUCT_SPEC.md line 8101, still reads `| INV-220 | R135.3, R231.1 |`. 7afe7ba correctly removed INV-220 from R135.3 — the body at line 3104 now reads `[INV-221, INV-203, INV-173]`, and INV-220's own criterion at line 5539 says the system shall carry no such arm — and ee70d75 rebuilt the standalone index to match, but nothing re-spliced the copy inside the spec. So the spec's own cross-reference sends a reader from INV-220 to a criterion that no longer carries it. tests/test_formal_index.py::TestFormalIndex::test_committed_index_equals_embedded_table holds exactly this and is red: `PRODUCT_SPEC.index.md and the spec's embedded ## Reference table drifted`, first differing element 277. Its docstring states the law — the two are one artifact in two places, and a drift between them is a hand edit under INV-258. Gate x passes over it by construction, since it compares the body against the standalone file and never against the embedded copy. The push road runs the suite at gate b and CI runs the full suite, so the red blocks the push both ways.

Third-pass dispositions. Finding 10 closed by 7afe7ba: the installed hooks were brought back into parity through the installer and one direct copy, and `check-config-health.sh` exits 0 reporting that installed hooks match their sources. Finding 11 closed: TEST_MATRIX.md rows M-459 and M-460 now read `the same text-in/decision-out level the scissors and hedge arms take, and the retired answer-first arm took`, and PRODUCT_SPEC.md line 3104 no longer cites INV-220. Finding 13 closed: docs/PROGRESS.md was regenerated after the spec edit this time and reads 702,692 bytes and 117,795 words against a tree measuring exactly 702,692 and 117,795. Finding 8 stands untouched, and finding 12's residue stands as recorded — every surviving mention of the figure names DECISIONS.md, so none asserts it on its own authority.

Checks run, fourth pass at 68ee8b9: `python3 -m pytest -q` — 1 failed, 2,483 passed, 2 skipped, exit 1, in 347.23s, the single failure being the prover-record freshness test this record closes; `python3 -m pytest -q tests/test_formal_index.py` — 4 passed; `python3 guardrails/check-index-generated.py PRODUCT_SPEC.md PRODUCT_SPEC.index.md` — OK, exit 0, 398 of 398, committed index equals a fresh build; `python3 guardrails/check-doc-findings-bound.py` — exit 0; `bash guardrails/check-freeze.sh` — exit 0; `bash guardrails/check-config-health.sh` — exit 0; `python3 guardrails/check-size-ratchet.py PRODUCT_SPEC.md` — OK, 184.9 bytes per criterion at or below the recorded bound 207.2; `sed -n '8101p' PRODUCT_SPEC.md` and `grep '^| INV-220' PRODUCT_SPEC.index.md` — both read `| INV-220 | R231.1 |`; `wc -c`/`wc -w` on PRODUCT_SPEC.md against docs/PROGRESS.md.

The fourth pass ran the whole suite again, for the reason the third pass did. Three rounds running, a repair has left something its own commit did not look at, and twice the thing left behind was invisible to every gate named in the brief. The suite is the only net that has caught all of them.

15. Non-blocking, and the third occurrence of one class. docs/PROGRESS.md is stale against the tree again. The committed page reads PRODUCT_SPEC.md at 702,692 bytes and 117,795 words; the file measures 702,684 and 117,794. The gap is exactly the eight characters and one word 68ee8b9 removed from the embedded table row. This is the same defect as finding 13 — a page derived from the spec, regenerated before the spec edit rather than after it — and it has now happened at dbb5794, been repaired at 7afe7ba, and returned at 68ee8b9. No machine holds it: the only reader of the page, tests/test_progress_report.py, regenerates it rather than comparing the committed copy against a fresh run, which is why the suite stays green over it. Two things follow. The page is already correct in the working tree, since this pass's suite run regenerated it, so the repair is to commit that file rather than to rerun anything. And a defect that recurs three times inside one push wants a gate rather than a fourth hand repair — the cheapest is to have the progress test compare the committed page against a fresh build the way gate x compares the index, so the next stale page reds instead of shipping. Owner: the page's keeper, with the gate as a queued row.

Fourth-pass dispositions. Finding 14 closed by 68ee8b9: the embedded `## Reference` row and PRODUCT_SPEC.index.md both read `| INV-220 | R231.1 |`, tests/test_formal_index.py passes 4 of 4, and gate x still reports 398 of 398 against a fresh build. Findings 8 and 12 stand as recorded. Finding 15 is new and open.

The state of the tree this record covers. Two files sit uncommitted beside the 34 commits: this record, and docs/PROGRESS.md, which the suite run rewrote in place. Neither is staged by this pass except the record itself, staged by name. The PROGRESS.md modification is the repair finding 15 asks for and is left for its keeper to commit, since this reviewer edits no file but this record.

Blocking: none.

Superseded blocking findings, kept for the record.
- Gate b and CI refused this push at ee70d75: the generated index drifted between its two homes. ee70d75 rebuilt PRODUCT_SPEC.index.md after 7afe7ba removed the INV-220 citation from R135.3, but left the same table as spliced under `## Reference` in PRODUCT_SPEC.md, so line 8101 still routed INV-220 to R135.3 while the rebuilt file and a fresh build both routed it to R231.1 alone. closed by 68ee8b9: the row was re-spliced, and the test that held it is green.
- Gate m refused this push at dbb5794: the two hook comment lines dbb5794 reworded in the repository were still the old text in the installed copies, so `bash guardrails/check-config-health.sh` reported both as drifted and guardrails/pre-push line 158 set fail. closed by 7afe7ba: parity was restored through the installer, with one direct copy for the library hook the installer does not place, and the check now exits 0 reporting that installed hooks match their sources.

The first pass's blocking finding, kept for the record. Gate aa refused the push at 5dba714: NEXT_STEPS.md was cleared to zero findings and carried three. closed: dbb5794 split the two over-cap sentences and lowered the shouted word, and `python3 guardrails/check-doc-findings-bound.py` now exits 0 with 122 live documents and 22 held at zero.
