# Prover record — 2026-08-20 matrix-parts

PUSH-REVIEW

Range: ca67f37b..3cfae0a9
- 3f0430ab TEST_MATRIX.md gains the reader machinery a future split needs, with no row moved
- 35bc12e8 The test matrix's twenty-three architecture-node blocks move out of TEST_MATRIX.md into matrix/*.md, and the core carries a Parts map in their place.
- a7d829a6 docs/test-matrix-format.md now describes the core-plus-parts matrix, not the old single file
- 1775a15f docs/test-matrix-format.md names the freeze command, the level ladder's home, and rendered level, and tests/conftest.py drops its stale pre-split comment
- 8b02f5b0 The split's last two matrix reads route through conftest's shared read()
- 89b56ec4 Matrix part files end with one newline
- 3cfae0a9 Matrix part files have no trailing blank lines
Files read: PRODUCT_SPEC.md (matrix glossary and requirements); ARCHITECTURE.md (test-author responsibility and matrix derivation); TEST_MATRIX.md; TEST_MATRIX.index.md; matrix/*.md; docs/test-matrix-format.md; guardrails/specformat.py; guardrails/check-matrix-reference.py; tests/conftest.py; tests/test_matrix_reference.py; tests/test_traceability.py; tests/test_doc_rotation.py; tests/test_clean_context_review.py; guardrails/pre-push.
Checks run: `git diff --check origin/main..HEAD` — clean after finding and removing trailing blank lines in 22 part files, then three residual blank lines in three parts. `python3 guardrails/check-matrix-reference.py TEST_MATRIX.md matrix/*.md TEST_MATRIX.index.md` — OK; 540 of 540 rows scanned, committed Reference equals fresh build, 400 anchors agree. `python3 -m pytest -q tests/test_matrix_reference.py tests/test_traceability.py tests/test_doc_rotation.py tests/test_clean_context_review.py` — 233 passed, 1 skipped. A local full-suite diagnostic stopped at the first unrelated failure: `tests/test_config_health.py::TestConfigHealth::test_this_repo_installed_hooks_match_source` sees the installed personal `pre-push` behind the source's new matrix-reference invocation. With `LIVE_SPEC_SCRATCH=1`, config-health stands down and `tests/test_worker_restore.py::TestTheGateIsArmedWhereItSaysItIs::test_the_gate_runs_against_this_machines_own_transcripts` instead reds an earlier 2026-08-19 worker transcript containing `git checkout -- TEST_MATRIX.md`. Neither failure names a changed matrix line; both are unresolved local-state findings, not claimed green here.
Findings: (1) The split preserves the matrix's one logical body: the core's ordered Parts map names each of the 23 architecture-node files, `guardrails/specformat.py` joins that ordered list for consumers, and `tests/conftest.py` appends the generated Reference exactly once for existing `read("TEST_MATRIX.md")` callers. (2) The generated reference check is the decisive loss/drift proof: all 540 body rows and all 400 anchors still agree with the committed generated map. (3) The initial delivery had trailing blank lines in 22 new parts, with three parts retaining a second blank line after the first formatting pass; two explicit formatting commits remove them and leave `git diff --check` clean. (4) No product requirement, architecture responsibility, matrix criterion, test level, anchor, or generated Reference entry was changed; the delivery changes storage and reader wiring only. (5) The old installed hook invokes the matrix-reference checker with an obsolete argument shape. Synchronising it is a personal/global-state operation and has not been done in this branch. (6) The worker-restore gate's transcript finding is a real historical unsafe command, not an environment excuse; repairing that mechanism belongs to its separately isolated branch and is intentionally not folded into this matrix delivery.
Blocking: installed pre-push stands: it must be synchronised from the now-delivered source before a normal local push can be certified; this record does not authorise changing it. Worker-transcript finding stands: an earlier worker used a destructive checkout; it is outside this delivery and must be repaired on its isolated restoration branch. No matrix-specific blocking finding remains.
