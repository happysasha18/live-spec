# Prover record — 2026-08-20 ci-mirror-partial-extraction

PUSH-REVIEW

Range: 9870c7b8..c751abfe
- c751abfe Gate u refuses a broken read instead of turning it into a verdict about the tree
Files read: guardrails/check-ci-mirror.sh, tests/test_ci_mirror.py, guardrails/pre-push, .github/workflows/gates.yml, guardrails/ci-mirror.json, guardrails/gate-red-proofs.json, guardrails/rule-census.json, scripts/check-registry.json, guardrails/check-skill-loadability.sh
Checks run: Red-first, against the unpatched script: `python3 -m pytest -q tests/test_ci_mirror.py` — 3 failed, 14 passed, the three new cases failing with the exact strings the 2026-08-19 gate b logs carry ("gate d runs in ... absent from", "gate e runs in ... absent from", "carve-out 'c' in ci-mirror.json names no local pre-push gate"). After the repair, the same command — 17 passed. Focused: `python3 -m pytest -q tests/test_ci_mirror.py tests/test_every_gate_can_fail.py tests/test_gates_manifest.py tests/test_traceability.py tests/test_guardrails_unit.py tests/test_check_registry.py tests/test_config_surface.py tests/test_rule_census_ratchet.py tests/test_prose_gate.py` — 354 passed. `bash guardrails/check-ci-mirror.sh` on the real tree — OK, exit 0, unchanged verdict. Full suite: `python3 -m pytest -q` — 3 failed, 2477 passed, 53 skipped in 19:04, the nested scratch suite inside gate b's own meta-test green. Each of the three failures was reproduced on a pristine origin/main worktree (/private/tmp/live-spec-man/wt, 9870c7b8, clean) and is older than this branch: tests/test_config_health.py twice (the installed design-reviewer skill under ~/.claude/skills holds a neighbouring branch's copy) and tests/test_worker_restore.py's scan of this machine's own transcripts. Diagnosis evidence, taken before the repair: 961 checksum samples of guardrails/pre-push, .github/workflows/gates.yml and skills/text-audit-pack/SKILL.md across a full nested suite showed no change to any of the three, and that same nested suite ran 2458 passed, 71 skipped, 0 failed — so the short letter lists came from the reads, not from the tree.
Findings: (1) THE DEFECT. guardrails/check-ci-mirror.sh built all three of its evidence lists — local gate letters off pre-push, CI gate letters off gates.yml, carve-outs off ci-mirror.json — with shell pipelines ending in `|| true`. That construct suspends the script's own `set -euo pipefail` for exactly that line, so a pipeline that failed outright or was cut off part-way handed an empty or short list to the comparison below, which then read it as fact. The output was not an error but a false red naming whichever gate letters the broken read had dropped, textually identical to a real drift finding. Two such verdicts were observed on 2026-08-19 in two gate b runs over one unchanged tree, naming different gates each time. (2) THE REPAIR. Each read is now taken with its status: a required list whose pipeline fails, or which comes back empty, stops the gate by name and says which file it could not finish reading. The carve-out read keeps the empty answer that is genuinely meaningful — a repo may declare no carve-outs, which jq reports as an empty list on a clean exit — so there the read must succeed while the result may be empty; an unreadable or malformed ci-mirror.json still stops the gate. Nothing was added that retries, counts, thresholds, or hooks, no gate was added, and the verdict on a tree whose reads complete is byte-identical to before. (3) RED-FIRST PROOF SHAPE. The three cases substitute `grep` on PATH so one pipeline breaks while every input file stays intact and readable — a stage failing outright, a stage cut off mid-stream yielding all but two lines, and that same cut applied to the pre-push read so a live carve-out reads as stale. They are deterministic: no timing, no load, no retry, no threshold. A fourth test pins the intended empty case so the repair cannot later be tightened into refusing a repo that carves nothing out. (4) WHAT THIS RECORD DOES NOT CLAIM. The 2026-08-19 gate b runs also produced one failure of a different gate — check-skill-loadability.sh reading text-audit-pack's frontmatter as carrying no description. That is plausibly the same class, a text extraction accepted without checking that it completed, but that script carries no `|| true` and its exact mechanism was NOT reproduced. It is deliberately left untouched here; it needs its own reproduction before any repair. (5) STANDING ENVIRONMENT RED, not this branch's. Gate m reds on this machine because ~/.claude/skills/design-reviewer holds a neighbouring branch's copy of that skill; it reds identically on a pristine origin/main checkout, and clearing it means writing the personal layer, which is outside this hotfix.
Blocking: none
