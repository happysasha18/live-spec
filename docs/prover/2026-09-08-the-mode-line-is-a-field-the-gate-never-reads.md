# Prover record — 2026-09-08 the mode line is a field the gate never reads

PUSH-REVIEW

Mode: global
Scope: the push review path — `skills/product-prover-pack/SKILL.md`, `docs/prover/README.md`, and `guardrails/check-prover-record.sh`, the three documents that together decide what every future review of this pack reads and what it may block on. The scope is owed by the second trigger the row's own Global review section names: the accepted row changes a critical cross-cutting surface, the shared release path.
Range: 4b87ff28..8ca060ec
- 8ca060ec The two pins the new sections moved, and the skill's own review record — both refused by the push chain after this read, both caused by the reviewed edit inserting sections into the skill body, and both repaired in that commit rather than left for a later one
- d23e2214 The gate reads the mode line the record shape names — the repair this record's one blocking finding names, written after this read and recorded on that finding as closed
- b63662bc A push review runs in one of two named modes — q-827
- 4b87ff28 The range's own closure record (base)
Files read: skills/product-prover-pack/SKILL.md, docs/prover/README.md, guardrails/check-prover-record.sh, tests/test_review_modes.py, tests/test_push_review.py, scripts/plan_checks.py, PLAN.md (the q-827 block), .live-spec/checkpoints/q-827.md, git show b63662bc
Checks run: the row's own recorded acceptance command from CHECKS["q-827"], verbatim — exit 0; PYTHONPATH=tests python3 -m unittest -v test_review_modes — 7 tests, OK; a red-prove of that file's planted-record harness, two mutated records driven through the real gate (a record with its `Findings:` line dropped, and a record naming a range that is not the tree's) — rc 1 on both, each with the gate's own FAIL line, so the harness is a live run and not a vacuous pass; python3 -m pytest -q tests/test_push_review.py — 1 FAILED, 22 passed; the same file on the pre-change tree, a `git clone --no-hardlinks` into a scratch directory checked out at 4b87ff28 — 1 passed, so the red is this range's own; python3 -m pytest -q over the nine test files that read the two changed documents (test_prover_doc_homes, test_review_record_class, test_prover_adapter_contract, test_readme_stance, test_enumeration_reads_as_list, test_finding_kind, test_clean_context_review, test_minor_gate_reconciliations, test_traceability) — 254 passed, 3 skipped; bash guardrails/check-prover-record.sh on the work road — OK on presence and both freshness arms; the DOD digests of PLAN.md, of its `**DOD hash.**` line and of the checkpoint's `DOD:` anchor recomputed with the kernel's own whitespace-collapsing sha256 — all three aae6a3df, matching the current Done-when text.
Findings: The row meets four of its five Done-when clauses in the named surface. The two modes are named and only two (`## Closure review`, `## Global review` in the binding). Closure's stated reach is the row's definition of done, its acceptance command, the diff of the row and the paths that diff touches; its blocking set is material failure alone; everything else it finds becomes one inbox deposit and opens no row. Global's two triggers are both stated — the owner's request, and the row itself changing the base pack, the installer, CI or admission, or the shared release path — with the scope named outright and the reach held to reproducible material failure of that surface, never a repository audit. Both modes are proved through the gate itself: tests/test_review_modes.py plants a record of each shape in a throwaway repository and runs guardrails/check-prover-record.sh --push against it, and the mutations above show that harness fails when the record is wrong, so the proof is a run rather than a reading of the prose. The two modes are self-consistent enough for a future reviewer to run without asking a question: which mode is owed is decidable from the trigger list alone, and what blocks is the same four-item material-failure set in both the binding and the record README. The fifth clause is where the change falls down, and it is the one blocking finding below.
Blocking: one.
- Done-when clause four — "the record shape carries the mode it ran and the scope a global run names, the push gate reads a record of either mode" — is delivered by breaking a standing law of the very surface it edits, and the breakage is a red test in this range. `docs/prover/README.md` puts `Mode: closure` inside the record's shape block, but `guardrails/check-prover-record.sh` reads only Range, Files read, Checks run, Findings and Blocking, and this range does not touch it. The repository already holds that those two lists must agree: `tests/test_push_review.py::test_the_house_format_names_only_fields_the_gate_reads` parses the gate's `for field in ...` list and the README's shape block and asserts they are equal. It fails at HEAD — `documented=['Blocking', 'Checks run', 'Files read', 'Findings', 'Mode', 'Range'] parsed=['Blocking', 'Checks run', 'Files read', 'Findings', 'Range']` — and passes on the pre-change tree at 4b87ff28, so this commit is its cause. The same fact makes two statements in the scope false: `guardrails/check-prover-record.sh` states in its own header that "docs/prover/README.md holds the same shape for a person writing one, and holds no field this script leaves unread", and `docs/prover/README.md` states below the block that "Those are all the fields, and the gate reads every one of them" — with `Mode:` in the block, the gate leaves a documented field unread and both sentences are untrue as shipped. The commit message's line "Ran: the row's own acceptance, plus 292 passed and 3 skipped over the five files that read the changed documents" is how it escaped: tests/test_push_review.py is a file that reads docs/prover/README.md and it was not among the five. closed: the change's author took the first of the two repairs named below, in the commit that carries this record. `Mode` is now a field the gate's own list reads, and a record naming no mode, naming a mode that is not one of the two, or naming global with no `Scope:` is refused by name — three tests in `tests/test_review_modes.py` drive the gate against a planted record of each shape. `tests/test_push_review.py::test_the_house_format_names_only_fields_the_gate_reads` is green again, and its own record fixtures carry the mode, since a record without one is a record no push would carry. The two sentences the finding named are true again: the documented block and the gate's list agree, and the gate leaves no documented field unread. Two repairs close it and either is small: add `Mode` to the gate's field list so the gate reads what the README documents, or move the `Mode:`/`Scope:` lines out of the fenced shape block into the prose beside it, so the block again names only fields the gate parses. The first is the one that makes clause four literally true.
