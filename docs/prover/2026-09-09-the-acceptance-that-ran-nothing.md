# Prover record — 2026-09-09 the acceptance that ran nothing

Prover skill version: product-prover 1.6.0, the installed copy under `skills/product-prover/`, read
beside the pack bindings `skills/product-prover-pack/SKILL.md` (v6.1.4). The reviewer wrote none of
this change and made no edit to the tree in either of its two rounds; every mutation it tried was on
a scratch copy of the pack.

PUSH-REVIEW

Mode: closure
Scope: the accepted row q-834 and nothing else — its definition of done, its own recorded acceptance
command at this commit, the diff of the pushed range, and the paths that diff touches read directly,
which are `scripts/task-admission.py`'s acceptance path, `guardrails/run_modes.py` and
`guardrails.config.json`'s `run_modes`, `tests/test_closure_kernel_bypasses.py` and
`tests/test_task_admission.py`, `scripts/install-pack-hooks.sh`, `guardrails/check-push-reach.sh`,
`tests/test_release_6_1_1.py`, and the release stamping and prose that carry them.
Range: d671bd79..05fa7b1a
- 05fa7b1a The skill-creator read of the closing kernel's changed bullet — the review gate a
  second gate asked for, written after this read and by another reviewer; it changes no code
- 0aa429a0 6.1.4 — a close rests on the acceptance the row was admitted with
- d671bd79 (base) The record of the read that turned this landing back three times
Files read: PLAN.md (rows q-833 and q-834), .live-spec/checkpoints/q-833.md, .live-spec/checkpoints/q-834.md, .live-spec/readers/q-834-reader.md, scripts/task-admission.py, scripts/plan_checks.py, guardrails/run_modes.py, guardrails.config.json, guardrails/check-acceptance-rerun.py, guardrails/check-close-receipt.py, guardrails/check-push-reach.sh, scripts/install-pack-hooks.sh, tests/test_closure_kernel_bypasses.py, tests/test_task_admission.py, tests/test_release_6_1_1.py, tests/test_statement_validation.py, skills/build-pipeline/SKILL.md, skills/build-pipeline/references/accepted-work-execution.md, VERSION, MIGRATION.md, JOURNAL.md, docs/queue-archive/2026-09-09-abandoned-q833.md
Checks run: the row's own acceptance command verbatim (`python3 -m pytest -q tests/test_closure_kernel_bypasses.py tests/test_task_admission.py >/dev/null 2>&1 && test "$(cat VERSION)" = "6.1.4" && grep -q '^### 6.1.4' MIGRATION.md`) — exit 0, 107 tests collected and run; each of that key's three arms broken deliberately on a scratch copy and shown red — the handed-command refusal disabled (2 tests red), the whole run-mode block disabled (4 tests red), VERSION moved, the chapter heading removed; the abandoned row's own key driven to show it collects nothing (`PYTHONPATH=tests python3 -m unittest -q test_closure_kernel_bypasses` — Ran 0 tests, exit 0); the real acceptance path driven across thirty-two throwaway trees covering a handed command, the recorded command verbatim and padded, blank commands, `LIVE_SPEC_RUN_MODE` at manual, row, release, integration, an unknown string, empty and whitespace, `LIVE_SPEC_PUSH_FULL` set and empty, both named together, a tree with no run-mode reader, a reader raising on import, a reader with a syntax error, a config missing `run_modes`, missing a mode, missing a mode's `decides_verdict`, corrupt, absent, and one recording `row` as deciding no verdict; the CLI path for each; the two re-homed tests mutated at the code they cover to show each still reds; the whole table of recorded keys read for the vacuous shape; `python3 -m pytest -q tests/test_closure_kernel_bypasses.py tests/test_task_admission.py tests/test_run_modes.py tests/test_version_is_one_fact.py` — 132 passed; `python3 -m pytest -q tests/test_release_6_1_1.py tests/test_install_session_hooks.py tests/test_chat_law_hook.py tests/test_routing_preamble_hook.py tests/test_style_gate_kit.py` with `hooks/__pycache__` present — 63 passed against the repaired installer and 4 failed in one file alone against the installer as it stood at HEAD.
Findings: The acceptance path carries no default branch and no second road. `verify` refuses a command handed beside the recorded one, hard-sets the run to that one command, and refuses to write a receipt under any mode the tree's own contract records as deciding no verdict; the contract is loaded out of the tree that holds the plan, mirroring the loader `acceptance_key` already uses. Across every environment shape driven, a close was reachable exactly where it should be and the refusal named the fault it met: a typo in a mode name is answered as a typo, a contract that cannot answer is answered as a contract, a reader that is absent is named apart from one that will not load. No hook was added, no second list of tests, and no new runner: execution is still the single `plan_checks_core.run_key` call site. `close` still requires a passed receipt, a matching definition of done, a matching acceptance anchor and a matching tree hash, and `guardrails/check-acceptance-rerun.py` reads no receipt at all — it re-runs the recorded key itself. The two tests re-homed onto recorded keys still red at the code they were written for; what one of them lost is proof about a mix of one passing and one failing check, a path the change makes unreachable, so the lost coverage covers dead code. Five things stand outside this row's reach and none blocks: a closed row of this pack, q-829, rests on an acceptance of the same vacuous shape that abandoned q-833, and the anchor that fixes an acceptance at admission gives it no repair in place; a recorded key that pins a release literal goes red at the next release, which four rows now do; `verify` consults the contract only where a mode is named; `close` reads the receipt's exit codes and never that the receipt names only the recorded command; and the chapter arm's `grep -q '^### 6.1.4'` is a prefix match. All five ride one deposit at `inbox/2026-09-09-notes-beside-q834.md`, opening no row.
Blocking: four across two rounds, all reproducible, all closed before this commit. The reviewer wrote none of the change and none of the repairs; each `stands:` is the reviewer's own and the `closed:` beside it is the holder's, added after that round's read.
- The row's own recorded acceptance ran no tests at all. stands: `tests/test_closure_kernel_bypasses.py` is a pytest module of bare `test_` functions and the key drove it with `python3 -m unittest`, which collects none of them; `PYTHONPATH=tests python3 -m unittest -v test_closure_kernel_bypasses` reports `Ran 0 tests` and exits 0, and the key's `>/dev/null 2>&1` hides that line. What survived was a `grep` for a phrase that also appears in the file's own comments. The row's fourth done clause was therefore unmet by the path that decides the close, and this is the row's own subject matter. closed: the acceptance is anchored at admission and nothing may move it, so the row could not be repaired in place. q-833 was abandoned with that reason recorded and archived off the plan page; q-834 carries the same work with a key that drives both test files with pytest, proved here to collect 107 tests and shown red on each of its three arms.
- The change left two existing tests red and their coverage unre-homed. stands: `tests/test_task_admission.py::test_a_receipt_whose_command_failed_is_a_failed_verdict` and `::test_the_verifiers_own_check_may_spawn_the_probe` both hand `commands=[...]` differing from the recorded key and now die inside the new refusal. The second proved that the re-entry breaker does not bind the verifier's own checks, and with a handed command refused that coverage had no home. A third red in the same run, `tests/test_work_board.py::TestRealTree::test_the_day_count_holds_every_row_that_closed_today`, fails identically at HEAD with the five modified files restored and is unrelated. closed: the fixture takes the row's recorded acceptance as a parameter, so both tests record the check they are about — a failing one, and the probe-spawning one — and verify with nothing handed. Each was mutated at the code it covers to show it still reds.
- A malformed contract escaped as a refusal about a route, and three refusals named the wrong fault. stands: `decides_verdict` was called outside the `try` that catches `ValueError`, so a config missing a mode's own `decides_verdict` raised `KeyError` and surfaced as `the route names no 'decides_verdict'` on a run that has no route — the same traceback-instead-of-verdict shape the 2026-09-08 journal records repairing elsewhere. Beside it: an unknown mode name was answered by sending the caller to repair `guardrails.config.json`; a tree carrying a `run_modes.py` that raises on import was told it carries none; and the no-verdict refusal described the manual run whichever mode had been named. closed: the typo, the unreadable contract and the absent-versus-unloadable reader each have their own sentence, the no-verdict refusal names the mode that was asked for, and four cases cover them.
- The 6.1.4 chapter's headline said less than its own body. stands: it opened "Host action: none, unless something of yours passes `--command` to `verify`", while a host that names a run mode and carries no readable contract is now refused too — and `adopt/install-scaffold.sh` never overwrites a config a host already has, so a tree adopted before the four modes existed carries the reader with no `run_modes` key. The chapter said this four paragraphs later. closed: the headline names both shapes that owe an action and says plainly that a host naming no mode and passing no `--command` sees nothing new.

## What this read did not cover

The scope was named before the read and did not grow with it. The spec, the architecture and the
matrix were not read; nothing in this range touches them beyond the version stamps, which
`tests/test_version_is_one_fact.py` holds. The full pytest suite was not run here: the owner named
this row's final acceptance as its recorded key plus the real acceptance path, and CI's gate b runs
the whole suite over this commit. Two things ride in the reviewed commit that the row's own read did
not cover, and both are named in the commit that carries them: the installer's bytecode fix, which
was found by reading CI's own failure on the previous push and is proved by its own test with the
cache present, and the pin of `tests/test_update_watcher.py` into the scoped-run set, which the
previous landing owed and which the suite itself demanded by name.

The range also carries a second review, `docs/skill-review/2026-09-09-build-pipeline.md`, written
by the skill-creator reviewer after this read and covering the closing kernel's changed bullet by
its own craft's standards. Its four folds land in that commit and touch prose alone; the row's
recorded acceptance was re-run after them and still returns 0.
