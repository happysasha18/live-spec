<!-- ARCHIVED 2026-08-17, the owner's word. This record was written on an unmerged branch and
     carried into the tree when that branch was deleted, so the reasoning it holds outlives the ref.
     It reviews a range this tree never took, it discharges no gate, and it authorizes no push. -->

# Prover record — 2026-08-13 the reconciliation range: the sweep that repaired the gates and left one module red

PUSH-REVIEW

Range: be4e4f0..HEAD. `origin/main` resolves to be4e4f0, so be4e4f0 is the base and the two commits
below are what the push would send. The commit carrying this record names no change of its own and
falls under the exemption `guardrails/check-prover-record.sh:225-227` writes for a record-only commit.

- d835ecc The tree stops pointing at a body that left it: the temporary external-prover reconciliation
- 8f18a8f The decoupling meets its adversarial read: nineteen findings, sixteen of them blocking

Root: his word of 2026-08-13 at 15:06 — make the tree pass after the prover decoupling. This review
was run by a separate reader with clean context, holding no part of either commit, briefed to find
reasons to refuse the push rather than reasons to allow it. The brief named the temporary skip debt
as already declared in the range's own commit message and journal, and asked whether the declaration
is honest rather than whether the debt exists. Every number below was measured at this seat against
the tree; nothing is carried from the range's own text without a run behind it.

Files read: `docs/prover/README.md` whole; `docs/prover/2026-08-13-push-range-3.md` whole as the
shape example; `docs/prover/2026-08-13-decoupling.md` lines 1-80 and 430-559;
`guardrails/check-prover-record.sh` lines 200-340; `ARCHITECTURE.md` the range's diff and lines 78,
132-140; `README.md` line 74 and its diff; `JOURNAL.md` the range's whole entry;
`scripts/rule-census.py` the range's diff at lines 85-95; `guardrails/rule-census.json` the range's
diff; `.github/workflows/gates.yml` lines 5-115; `.gitignore` line 45;
`skills/product-prover-pack/SKILL.md` lines 10-20 and 55-70; `skills/live-spec-base/SKILL.md` lines
61-67, 332, 473, 587 and 624-632; `skills/test-author/SKILL.md` lines 163-183; `PRODUCT_SPEC.md`
lines 1082, 2415-2424, 2515-2546 and 4280-4292; `ROADMAP.md` rows 611-622 and the seven rows naming
the departed path; `NEXT_STEPS.md` lines 1-30 and its two mentions of the range's subjects;
`TEST_MATRIX.md` line 708; `evals/test-author.md` lines 24 and 33; and under `tests/`, the whole
range diff plus the bodies of `test_prover_doc_homes.py`, `test_restructure_merge_gate.py`,
`test_paired_transition.py`, `test_class_hunt.py`, `test_declared_laws.py`,
`test_full_pass_coverage_record.py`, `test_audit_point_folds.py` and `test_traceability.py` at their
skipped functions.

Checks run: each with its result, every one at this seat.

- `git log --oneline origin/main..HEAD` — 2 commits, 8f18a8f and d835ecc. `git diff --stat` over the
  range — 31 files, 761 insertions, 45 deletions.
- `bash guardrails/check-pin-drift.sh` — OK. 209 pins: 66 line pins, 137 file-level pins, 6
  unlabelled. The r5 arm passed at 53 range pins. This is the gate the range claims it repaired.
- `python3 guardrails/check-tree-counts.py` — OK, 3 of 3 rows matched, 0.1 s.
- `python3 guardrails/check-doc-findings-bound.py` — OK, 124 live documents, 22 held at zero, none
  above its record, cap 25.
- `bash guardrails/check-freeze.sh` — SPEC-FREEZE GREEN, 3 files. No frozen document is in the range.
- `python3 guardrails/check-landing-next-steps.py` — OK. Read for finding 5 below: the gate stands
  down because it classifies neither commit as a landing.
- `python3 -m pytest` over the 30 test modules whose text names the prover — **4 failed, 321 passed,
  56 skipped**. Every failure is in `tests/test_prover_doc_homes.py`.
- `python3 -m pytest tests/test_prover_doc_homes.py -q` alone — **4 failed, 1 passed in 0.06 s**, so
  the failures are the module's own and not an artifact of the selection.
- `python3 -m pytest tests/test_transition_payload_lens.py -q` alone — 3 passed, 1 skipped. The
  leaked-temp-artifact teardown error the selection reported does not reproduce, so it belongs to
  running a subset and not to this range.
- `git ls-files skills/` against `ls -d skills/*/` — 11 tracked skill directories against 12 on
  disk. `git check-ignore -v skills/product-prover` — `.gitignore:45`. The twelfth is the installed
  external clone.
- `cat skills/*/SKILL.md skills/*/references/*.md | wc -l` — 6,320, and `cat skills/*/SKILL.md | wc -l`
  — 5,082, both matching what README now publishes. The same two commands over the tracked set alone
  — 5,483 and 4,245. The whole difference, 837 lines in each figure, is the untracked clone.
- A sweep over the range's test diff, parsing each changed module for the enclosing function of every
  added skip — 56 skipped test functions across 25 modules, matching the commit message's count.
- The same sweep grouping the skip messages — 51 carry one shared reason and 5 carry their own,
  naming the version stamp, the pack lists, the base pin, the eval set and the installer wiring. All
  56 name the cause, his word of 15:06, and what is pending. None is bare.
- A sweep resolving each skipped function's file reads through its module helpers and class
  attributes — 23 of the 56 also assert over files this repository still tracks. The counts are in
  finding 2.
- `grep -rl 'skills/product-prover/'` over `tests/` — 28 modules read the departed path; 25 carry the
  named skip and 3 do not.
- `sed -n` over the two repointed pin targets — `skills/product-prover-pack/SKILL.md:15` is the
  heading `## Mode names` above the pipeline-to-prover table, `:65` is the pin-map row
  `| INV-72 | Unwritten seams; ...`, and `:60` is the row `| INV-114 | How to write findings |`. The
  fourth repointed pin, `skills/live-spec-base/SKILL.md:628`, is the heading `## The settings ladder`.
  Each lands on the line carrying its own label, exactly rather than within tolerance.
- `git diff` over `guardrails/rule-census.json` — the two entries for the departed body are gone, the
  bindings page enters measured, and the hand-raised entry carries a dated reason naming the commit
  that caused the rise, 8fbd70c, and stating that the prose repair is owed separately.
- `grep -n` over `.github/workflows/gates.yml` — the job runs `actions/checkout@v5`, installs pytest,
  then runs the suite and the gates, `check-tree-counts.py` among them at line 112. No step installs
  the external skill.
- `git show origin/main:README.md` — the base publishes 6,492 and 5,254, and `git ls-tree origin/main
  skills/` holds no `product-prover`. So the published counts were already unmeasurable on a fresh
  clone before this range.
- `grep` over `ROADMAP.md` for the decoupling's subjects — no row. Seven rows name the departed path;
  five are `*done*` and rows 618 and 619 are `*queued*`.

Findings: six. Two block and four do not. The four gates the range claims green are green, the two
pins spot-checked land exactly, the census raise is declared with its reason, and all 56 skips carry
a named reason rather than a bare one. What the range does not carry is a suite that runs, and a
statement of how far its skips reach.

**The two that block.**

1. **The suite is not green, and the commit message states that it is.** `tests/test_prover_doc_homes.py`
   returns 4 failed and 1 passed, in 0.06 seconds, reproducible on its own. The module reads
   `skills/product-prover/SKILL.md` at line 10 and carries no skip. Twenty-eight modules under
   `tests/` read that path; twenty-five carry the range's named skip and three do not —
   `test_node_fitness_test.py`, `test_product_prover_standalone_release.py` and this one. The first
   two pass. d835ecc's message says "All 26 modules that read the removed path run green", which is
   wrong in the count and wrong in the verdict. The four failures are the decoupling's own class: the
   external canon does not carry the boundary sentence, the kind-block split, the record-path line or
   the lifecycle lead-in that the module pins. One of them,
   `test_the_record_path_the_skill_prescribes_is_the_one_the_gate_demands`, was written the night
   before in 1fabca9 to hold the prover-record path against the gate that reads it — the contract
   this very record ships under, red on the tree the push would send. The range's stated purpose is
   to let the gates and the suite run again after the decoupling. Gate b refuses on this tree, so the
   purpose is unmet and the range's own evidence for having met it does not survive one command.

2. **The declared reach of the skips is narrower than their real reach.** Both the commit message and
   the journal describe what the skips defer as prover-shaped: each skipped test "asserts on wording
   the standalone release stripped, on a file this repository stopped shipping, or on the v5.0.0
   stamp", and what is lost is "the suite's proof that the review method still carries its lenses".
   That is true of the cause of each red and false of the reach of each skip, because a skip takes
   the whole test with it. Twenty-three of the fifty-six also assert over files this repository still
   ships: `PRODUCT_SPEC.md` in seventeen of them, `skills/build-pipeline/SKILL.md` in five,
   `skills/spec-author/SKILL.md` in four, `skills/design-reviewer/SKILL.md`, `TEST_MATRIX.md`,
   `skills/live-spec-base/SKILL.md` and `README.md` in two each, and `install.sh`, `OVERVIEW.md`,
   `adopt/ADOPT.md`, `.claude-plugin/plugin.json`, `skills/publish/SKILL.md`,
   `skills/test-author/SKILL.md`, `skills/communicator/SKILL.md` and
   `skills/feedback-intake/SKILL.md` in one each. The clearest instance is
   `tests/test_restructure_merge_gate.py`, whose three skipped tests each walk a `HOMES` tuple of
   `PRODUCT_SPEC.md`, the departed body and `skills/build-pipeline/SKILL.md`: skipping the test drops
   the two surviving homes along with the departed one, and the requirement they hold, R184, keeps no
   witness. `tests/test_traceability.py::test_install_sh_installs_and_backs_up` is the widest single
   loss — the whole fresh-home proof of `install.sh` goes quiet, and nothing about `install.sh` moved
   in this range. The debt itself is declared, dated and owned, which is why this finding is not that
   the skips exist. It is that a person reading the declaration would price the debt as the prover's
   and it is also the spec's.

**The four that do not block.**

3. **The pinned skip-set has no home in the tree.** `skills/test-author/SKILL.md:165` states the law
   the pack teaches: green means zero failures and the skip list is exactly the expected, pinned set,
   and an unexpected skip is a failure in a quieter colour. `PRODUCT_SPEC.md:2424` names that part of
   the method the skill must hold, and `TEST_MATRIX.md:708` covers the skill's presence with a string
   check. No file anywhere pins the expected set, and no gate reads a skip count. Before this range
   the gap cost nothing, because the suite carried few skips. After it, green carries fifty-six, and
   a fifty-seventh appearing by accident would read the same as today. The requirement is met as
   written — the skill does hold the method — so nothing here is a breach, and that is the point: the
   law is stated where a reader learns it and enforced nowhere.

4. **The published counts are a property of this machine, and the repository's own CI is not this
   machine.** README publishes 6,320 and 5,082 and tells the reader to count them with two commands.
   On a tracked checkout those commands return 5,483 and 4,245, and gate ad reds. The range declares
   this plainly in the commit message and the journal, names it finding 2 of the decoupling record,
   still open, and refuses to pretend the number is a property of what the repository ships. That is
   honest, and this finding does not dispute it. What neither text names is the environment that
   reds: `.github/workflows/gates.yml` checks out, installs pytest and runs `check-tree-counts.py`
   with no step that installs the external skill, so every push turns the repository's own gate job
   red. The base is already red the same way, on 6,492 against 4,245, so this range does not create
   the red — it moves it and leaves it. A reader of the declaration meets a contributor without the
   clone and not the gate chain the pack runs on itself.

5. **The resume file describes a tree that no longer exists.** `NEXT_STEPS.md` opens by saying it is
   written for a session starting with clean context, and its one live-state block is stamped
   2026-08-13 03:30 — before v5.0.0, before this range. It reports the evening pass pushed and the
   prover skill finished, and says nothing of the decoupling, the sixteen open findings, the
   fifty-six skips, or the installer the published counts now depend on. The gate that owns this,
   `check-landing-next-steps.py`, passes by name, because it classifies neither commit as a landing.
   So the file is stale by the machine's own permission, and the next session with clean context is
   the one that pays: it will read that the prover lives in this tree, and meet a gate that reds
   until it runs an installer no document in front of it names. The journal carries all of it, and
   the journal is dated history rather than live state.

6. **Nineteen findings and fifty-six skips have no queue row.** `ROADMAP.md` is untouched by this
   range and carries no row for any of the decoupling's findings; the whole set lives inside
   `docs/prover/2026-08-13-decoupling.md`, which nothing sweeps and no gate reads for open work. Seven
   rows still name the departed path, and two of them, 618 and 619, are `*queued*` against a footprint
   that left the repository — row 618's whole acceptance is a split of a body another repository now
   owns. The decoupling record names those two at its line 440 as an acknowledged gap owing a re-read.
   Recorded here because the re-read did not happen in this range, and because the working queue is
   where this pack looks for what is open.

**Classes swept with nothing found.** The four gates the range claims: each re-run at this seat,
unpiped, each green on its own exit code. The repointed pins: all four land on the line carrying
their own label, and the pin gate's tolerance is not doing the work. The skip messages: all
fifty-six named, five of them written specifically for their own cause, none bare and none blaming a
tool. The census: the two departed entries gone, the bindings page entering measured, the hand raise
from 68 to 69 carrying a dated reason, the commit that caused it, and the statement that the prose
repair is owed. The commit message against the tree: every claim checked resolved true except the
one in finding 1. Frozen documents: none in this range. The record shipped by 8f18a8f: its marker and
five fields are present, its blocking items each read `stands:` with a repair, and its own arithmetic
survived the checks made against it here.

Verdict: REFUSE, on finding 1. The range repairs three red gates honestly and declares its debts in
detail, but it does not deliver the state it was asked for, and it reports a green that a
six-hundredth of a second disproves.

Blocking: two, and neither is closed in this range.
- Finding 1, four red tests in `tests/test_prover_doc_homes.py` and a commit message claiming every affected module green — stands: the module reads the departed path, carries no skip, and returns 4 failed and 1 passed on its own in 0.06 s. Closes when the module's four assertions are re-pointed at the external canon or skip-guarded by the same named form the other twenty-five modules use, the other two unguarded modules are confirmed by name rather than by luck, and the count in the range's record reads 28 rather than 26.
- Finding 2, the skips' declared reach stopping short of the surfaces they silence — stands: 23 of the 56 also assert over tracked files, `PRODUCT_SPEC.md` in 17 of them, and no text in the tree says so. Closes when the tree's own declaration states the reach with its counts, so the person who owns the repair prices it against the spec's lost coverage and not the prover's alone.

The four non-blocking findings carry no push weight on their own. Findings 3 and 5 are the two worth
a row before the next session starts, since both are paid by whoever reads this tree next.
