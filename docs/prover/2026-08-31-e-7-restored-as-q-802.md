# Prover record — 2026-08-31 E-7 restored as q-802

PUSH-REVIEW

Range: 16b1a300..5d165bbc
- 5d165bbc E-7 was never q-54's: restore it as its own row, q-802
- 91894c1f Prover record: the target-ownership correction holds, but E-7's new home in q-54 does not
- f24871da Close the target-ownership gap for real: three tags were satisfied, not orphaned
- 058aa31e Correct the target-ownership diagnosis: the anchors are real, not orphaned
- b3bd531e The resume file catches up with tonight's landings and heals three
- 8da47015 Board: q-801 and q-55 marked done; q-386 checked honestly and stays open
- 66d31da8 q-55 lands: a joining project keeps its files as they were found
- 130a67e6 q-801 lands: release 6.1.0, a new project starts on one list
- 29db22d2 q-531 lands: a split or restructure is proved to have lost nothing
- 07463f9d A joining project keeps its files as they were found
- 8613bee5 Prover record: the one-list teaching, read against the tier law and the pins
- 176b1cef The row-format page still carries the retired document's name — a Blockers line
- cee24867 The matrix row for the footprint note follows the template to its new name
- e87c6656 Release 6.1.0: the one-list teaching, and the nothing it asks of a host
- ccb0773e The separate queue template retires, and its readers follow the one list
- e1769937 A new project starts on one list, and the skills stop naming a queue file
- 6d0257ac The board reads q-531 from its own command rather than a typed mark
- 7585e7e8 A split or restructure is proved to have lost no word and no mark (q-531)

The first sixteen commits (7585e7e8..f24871da) were already adversarially reviewed by
`docs/prover/2026-08-31-target-ownership-correction.md` (F1–F5, one blocking: F4). `91894c1f` is
that record's own commit. My own independent work concentrates on the one new commit this range
adds since then, `5d165bbc`, which was written in direct response to F4.

Files read: docs/prover/README.md, docs/prover/2026-08-31-target-ownership-correction.md (in full,
to recover F4's exact wording rather than trust the new commit's paraphrase of it), `git show
5d165bbc` (full diff), `docs/queue-archive/rotated-ROADMAP-2026-07.md` row 468, `PLAN.md`'s new
q-802 row in full, its neighbouring q-54 row in full, its q-55 row in full, `spec/doc-order-
generated.md` Requirements 247 and 248, `tests/test_traceability.py`'s `TARGET_ROW_OWNERS` map and
`TestDesignSyncWiring`, `tests/test_tasks_parser_finds_every_task.py` (to confirm the probe's task
count is self-derived from `PLAN.md` rather than hand-maintained), `bash scripts/state-probe.sh`
output, `board.html` mtime.

Checks run:
- `python3 -m pytest -q tests/test_traceability.py` — 185 passed, 2 skipped. Green after the
  `E-7` → `q-802` re-point; confirms `TARGET_ROW_OWNERS` still agrees with the spec's own-line
  `[target]` markers and that `q-802` reads as an open row in `PLAN.md`.
- `python3 -m pytest -q tests/test_config_health.py` — 34 passed. Checked as the adjacent file most
  likely to parse `PLAN.md` structurally; no `plan_checks`-style regression from the new row.
- Full suite not re-run here, per the orchestrating session's instruction: it ran clean at 2,656
  passed / 6 skipped earlier in this range, and this commit touches only `PLAN.md` and
  `tests/test_traceability.py`.
- `bash scripts/state-probe.sh` — ran clean against the new HEAD (`5d165bbc`). No new alarm tied to
  this commit. The four standing alarms (director eval staleness, `NEXT_STEPS.md` age, the
  `live-spec-p2` worktree branch, the `tlvphotos` version skew) all predate this commit. `board.html`
  carries an mtime (23:12) matching the commit itself — not stale, `render-board.sh` not needed.
- `grep -o "\*\*Group:\*\* [^·]*" PLAN.md | sort -u` — confirmed `q-802`'s group, "Method
  reliability", is an existing group already used by other rows, not invented for this one.
- Read `tests/test_tasks_parser_finds_every_task.py::test_probe_summary_accounts_for_every_declared_task`
  — the probe's row count is derived by parsing `PLAN.md`'s own `## Tasks` section at run time, not
  hand-maintained anywhere, so `q-802` needs no separate registration for a "how many open rows"
  figure; none exists as a static number to fall out of date.

Findings: one finding, closing F4; one non-blocking note carried forward.

**G1 — row 468 says exactly what the commit claims, read directly rather than trusted.**
`docs/queue-archive/rotated-ROADMAP-2026-07.md` row 468 (the 2026-07-23 landing-time audit) reads
verbatim: "The twelve marked anchors and their owning rows: E-6, E-7, E-10, INV-17, A-6 → row 55;
E-18 → row 93; ...". `E-7` sits in row 55's group alongside A-6/E-6/E-10/INV-17, exactly as
`5d165bbc`'s commit message and the new `q-802` row both state, and never in row 93's (design-sync's)
group. This matches `PLAN.md`'s own `q-55` row, whose Source line independently corroborates the
same five-anchor list ("restored 27.08 ... found to be the only owner of five promises the spec
still makes (E-6, E-7, E-10, A-6, INV-17)"). No discrepancy found.

**G2 — `q-55`'s closure is honestly narrowed to `A-6` alone, as claimed.** `q-55`'s "Done, landed
31.08, narrowed first" text confirms the row was cut back to one real case — a joining project's
starting-state commit, closing only the `A-6` half of its original five-anchor promise. `E-7`'s
larger promise (the declared-scope snapshot machinery) is not mentioned anywhere in the closure
text, so it genuinely fell out with no row when `q-55` closed. `5d165bbc` restoring it as its own
row rather than silently letting the `[target: E-7]` tag go stale is the correct move, and follows
the same shape `q-437`'s restoration already used (an anchor whose owning row closed without
building it) — both rows carry that same phrase and reasoning verbatim.

**G3 — `q-802`'s acceptance criterion is honest, scoped to `E-7` alone, and does not overbuild.**
Requirement 247 carries five acceptance lines. Lines 1–4 are tagged `[E-7]` (baseline advances only
at declared delivery; undeclared surface reds the check; the snapshot folder
`.live-spec/snapshot/` git-tracked with one manifest line per surface; a heavy-byte surface's
content held outside git with only its manifest line and hash tracked). Line 5 is tagged `[A-6,
E-8]` — already built, per `q-55` closing `A-6`. `q-802`'s Acceptance text states exactly lines 1–4's
promise (the manifest shape, one baseline advance over a fixture delivery, an undeclared surface's
baseline left untouched) and does not fold in line 5's adoption/narrowing promise, which is not its
anchor's to own. Nothing invented beyond what Requirement 247 already asks of `E-7`.

**G4 — `q-54` is correctly left untouched, still scoped only to onboarding-profile fields.** `q-54`'s
own row ("New projects learn who they're building for") still reads exactly as it did before this
commit: Acceptance names only the field leg — `project.kind`/`project.layers`/`project.proofs` in a
joining project's own profile — with no mention of design-sync or snapshot anywhere in its Source,
Closes, Absorbed, or Acceptance text. `5d165bbc` diffs only add the new `q-802` block and edit the
one `TARGET_ROW_OWNERS` line for `E-7`; `q-54`'s own text is byte-for-byte unchanged.

**Non-blocking note carried forward — `E-18`'s mapping to `q-54` still rests on the same
undemonstrated reasoning F4 named for `E-7`, and this commit does not touch it.** F4's own text
(`docs/prover/2026-08-31-target-ownership-correction.md`, lines 90–113 and its Blocking item)
explicitly says the `q-54` re-pointing problem "affects the pre-existing `E-18` mapping too," since
`E-18`'s `q-54` ownership rests on the identical, never-corrected claim ("`q-93` folded into `q-54`
on 2026-08-28") that `q-54`'s own acceptance text never substantiates for design-sync in either
direction. `5d165bbc` leaves `E-18: "q-54"` unchanged rather than touching it, which is the
compliant reading of F4's closing instruction ("before either anchor's row is touched again") —
untouched is not mis-touched. It is mitigated in practice, more than `E-7` was: `TestDesignSyncWiring`
(`tests/test_traceability.py`, class docstring "SPEC E-18, row 93 pack-side half: the switch +
channel lines are wired; the machine stays [target]") independently verifies `E-18`'s actual
wired pieces — the base-defaults switch, the communicator channel line, and the spec's own honest
`[target: the machine; the wiring is live]` marker — directly against the live files, not through
`q-54`'s acceptance text. So `E-18` does not currently share `E-7`'s silent-orphan exposure the same
way. But if `q-54` closes on its field-leg acceptance alone (as tonight's own hostile review already
flagged it as a likely-next narrowing candidate), `TARGET_ROW_OWNERS`'s `E-18` entry would need the
same re-pointing decision `E-7` just received, and nothing today forces that to happen — it rests on
a future reader noticing. Per this session's brief, this is not something this record fixes; it is
named here so it is not lost.

Blocking: none — F4 is closed for `E-7`, the anchor it named.
