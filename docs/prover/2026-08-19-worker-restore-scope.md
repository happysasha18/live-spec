# Prover record — 2026-08-19 worker-restore-scope

PUSH-REVIEW

Range: f6fa13a8..RANGEPLACEHOLDER
- 0b88ffbf Worker-restore counting start moves to 2026-08-18T21:48:00Z, carrying 28 findings
- 354bbb04 The record names the pushed range by its hash
- 0bbcbd58 Prover record for the worker-restore-scope range
- c9672c0b TEST_MATRIX.md gains one row for the worker-restore gate's own-session scoping
- c9e98308 Worker-restore gate: an unplaceable cwd now checks effective_dir before defaulting to own
Files read: guardrails/check-worker-restore.py, guardrails/README.md, tests/test_worker_restore.py, ROADMAP.md, TEST_MATRIX.md, DECISIONS.md, docs/test-matrix-format.md, docs/roadmap-format.md, scripts/build-matrix-reference.py, guardrails/check-matrix-reference.py, docs/prover/README.md, docs/prover/2026-08-05-day-of-readability-repairs.md, docs/prover/2026-08-14-candidate-repair.md, docs/prover/2026-08-17-slimdown-pin-renumber.md, docs/push-law.md, guardrails/check-tests.sh, guardrails/gate-red-proofs.json, the transcripts under /Users/sashaabramovich/.claude/projects for the eight worktrees named in ROADMAP row 624, and /private/tmp/live-spec-split/wt's own `git log` (read-only) to verify the concurrent commit `56f179a9`
Checks run: `python3 -m pytest tests/test_worker_restore.py tests/test_traceability.py -q` — 286 passed, 1 failed (`TestTheGateIsArmedWhereItSaysItIs::test_the_gate_runs_against_this_machines_own_transcripts`, explained under Findings, unrelated to this delta and not fixed by it). `python3 guardrails/check-worker-restore.py` — before the delta: 16 own findings (15 ran, 1 declined) and 11 neighbour notices over the default 24h window; after: 12 own findings (11 ran, 1 declined) and 15 neighbour notices — the four `exhibition-engine-integrate` false positives moved to the neighbour section, the twelve real findings in this project's own worktrees stayed red. `python3 guardrails/check-matrix-reference.py TEST_MATRIX.md` — OK, 536/536 rows matched, 400 anchors agree. The full suite was not run (owner's word: narrow runs only on this pass). Second pass, after the counting-start move: `python3 -m pytest tests/test_worker_restore.py tests/test_traceability.py tests/test_authority_anchor.py tests/test_formal_index.py tests/test_measurement_law_homes.py tests/test_opening_decision_sweep.py -q` — 330 passed, 0 failed, including `test_the_gate_runs_against_this_machines_own_transcripts` now green. `python3 guardrails/check-worker-restore.py` (default 24h window) — OK, exit 0, "2 findings stamped before the counting start 2026-08-18T21:48:00Z stand as history and red nothing". `python3 guardrails/check-worker-restore.py --all` — OK, exit 0, "37 findings stamped before the counting start 2026-08-18T21:48:00Z stand as history and red nothing" (the 28 named in ROADMAP row 624 plus 9 older ones already history under the previous start). Neither run shows any own finding stamped on or after 2026-08-18T21:48:00Z — no violation happened after the new start.
Findings: (1) `is_own_session` read only a session's recorded `cwd`; an owner's home directory (unplaceable in any repository) fell to the fail-safe default and reddened four `git stash` findings whose commands had actually `cd`ed into `/Users/sashaabramovich/exhibition-engine-integrate`, a real neighbouring checkout. The gate now takes one further look, only when `cwd` itself answers nothing: if the finding's `effective_dir` (already computed by `classify`, already printed as `ran in`) exists on disk and git places it in a different, nameable repository, the finding is carried as that neighbour's notice instead. Verified narrow: a `cwd` the key can already place is never second-guessed, an UNKNOWN or unplaceable `effective_dir` still reds, and a sibling worktree of this same repository (built with a real `git worktree add`/`remove` in the new fixture) still reds — the trap the narrowing had to not fall into. Three new tests in `tests/test_worker_restore.py` (`TestTheGateJudgesThisProjectsOwnSessions`) prove the three shapes. (2) The refinement is documented in all three places law lives for this gate: the gate's own header docstring, `guardrails/README.md`'s worker-restore section, and one new ROADMAP row (623, `*done*` same day, beside row 479) — plus one new `TEST_MATRIX.md` row (M-546, beside M-477, citing the same `[INV-299]` parent per `docs/test-matrix-format.md`'s "one spec fact may project into several rows") covering both yesterday's cwd-based own/neighbour split (`b4d00f8e`) and today's `effective_dir` refinement as one law read at two dates, per the coordinator's own word that they are one law and not two rows. The Reference table was rebuilt with `scripts/build-matrix-reference.py` rather than hand-edited. (3) Named plainly, not glossed over: `test_the_gate_runs_against_this_machines_own_transcripts` stays red after this delta, and for a real reason — this machine's transcripts carry twelve honest worker-restore findings inside this project's own worktrees (`live-spec-split/wt`, `live-spec-morning/wt`, `live-spec-cull2/wt` among them) over the last 24 hours, none of them touched by this branch. This package does not repair those findings and was never scoped to: its whole job was the four false positives whose `cwd` was an unplaceable home directory, and those four are the only ones this delta moved. The twelve real ones stay exactly as red as they were before this branch started, and clearing them needs either time (they age out of the 24h window) or the owner's word. This was written before the counting-start move below; it is superseded by finding (4), kept here unedited under the no-history law rather than deleted. (4) The coordinator's own word carried this record's scope past the four-worktree fix: reading `guardrails/check-worker-restore.py --all --counting-from 2026-08-12T06:06:00Z` (the old start) found not twelve own findings but twenty-eight, every one stamped before the latest of them — so no counting-start value carries the twelve into history without also carrying the other sixteen (six from 2026-08-13, one from 2026-08-15 with `ran in: UNKNOWN` — the fail-safe default's own proof case, reddening correctly — five from 2026-08-17, four more from 2026-08-18 in worktrees the first pass never named). All twenty-eight are named in `ROADMAP.md` row 624 and `DECISIONS.md` (2026-08-19), never folded into a bare count. Reading three prior prover records (`docs/prover/2026-08-05-day-of-readability-repairs.md` finding 13, `docs/prover/2026-08-14-candidate-repair.md`, `docs/prover/2026-08-17-slimdown-pin-renumber.md`) turned up a second finding: this same test's red had surfaced and been called "environmental" three times since 2026-08-05 without ever being traced to its source, which is exactly the class of silent tolerance that let sixteen of the twenty-eight go unrecorded. The counting start moves to `2026-08-18T21:48:00Z`, one minute past the latest of the twenty-eight and no narrower — verified: neither the default-window nor the `--all` run shows any own finding on or after that stamp. This move fixes no habit; it only stops mis-recording the past. ROADMAP row 624 stays open until the separately-built pre-shell hook is installed on this machine by the owner.
Blocking: none
