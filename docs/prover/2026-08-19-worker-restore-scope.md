# Prover record — 2026-08-19 worker-restore-scope

PUSH-REVIEW

Range: f6fa13a8..RANGEPLACEHOLDER
- c9672c0b TEST_MATRIX.md gains one row for the worker-restore gate's own-session scoping
- c9e98308 Worker-restore gate: an unplaceable cwd now checks effective_dir before defaulting to own
Files read: guardrails/check-worker-restore.py, guardrails/README.md, tests/test_worker_restore.py, ROADMAP.md, TEST_MATRIX.md, docs/test-matrix-format.md, docs/roadmap-format.md, scripts/build-matrix-reference.py, guardrails/check-matrix-reference.py, docs/prover/README.md
Checks run: `python3 -m pytest tests/test_worker_restore.py tests/test_traceability.py -q` — 286 passed, 1 failed (`TestTheGateIsArmedWhereItSaysItIs::test_the_gate_runs_against_this_machines_own_transcripts`, explained under Findings, unrelated to this delta and not fixed by it). `python3 guardrails/check-worker-restore.py` — before the delta: 16 own findings (15 ran, 1 declined) and 11 neighbour notices over the default 24h window; after: 12 own findings (11 ran, 1 declined) and 15 neighbour notices — the four `exhibition-engine-integrate` false positives moved to the neighbour section, the twelve real findings in this project's own worktrees stayed red. `python3 guardrails/check-matrix-reference.py TEST_MATRIX.md` — OK, 536/536 rows matched, 400 anchors agree. The full suite was not run (owner's word: narrow runs only on this pass).
Findings: (1) `is_own_session` read only a session's recorded `cwd`; an owner's home directory (unplaceable in any repository) fell to the fail-safe default and reddened four `git stash` findings whose commands had actually `cd`ed into `/Users/sashaabramovich/exhibition-engine-integrate`, a real neighbouring checkout. The gate now takes one further look, only when `cwd` itself answers nothing: if the finding's `effective_dir` (already computed by `classify`, already printed as `ran in`) exists on disk and git places it in a different, nameable repository, the finding is carried as that neighbour's notice instead. Verified narrow: a `cwd` the key can already place is never second-guessed, an UNKNOWN or unplaceable `effective_dir` still reds, and a sibling worktree of this same repository (built with a real `git worktree add`/`remove` in the new fixture) still reds — the trap the narrowing had to not fall into. Three new tests in `tests/test_worker_restore.py` (`TestTheGateJudgesThisProjectsOwnSessions`) prove the three shapes. (2) The refinement is documented in all three places law lives for this gate: the gate's own header docstring, `guardrails/README.md`'s worker-restore section, and one new ROADMAP row (623, `*done*` same day, beside row 479) — plus one new `TEST_MATRIX.md` row (M-546, beside M-477, citing the same `[INV-299]` parent per `docs/test-matrix-format.md`'s "one spec fact may project into several rows") covering both yesterday's cwd-based own/neighbour split (`b4d00f8e`) and today's `effective_dir` refinement as one law read at two dates, per the coordinator's own word that they are one law and not two rows. The Reference table was rebuilt with `scripts/build-matrix-reference.py` rather than hand-edited. (3) Named plainly, not glossed over: `test_the_gate_runs_against_this_machines_own_transcripts` stays red after this delta, and for a real reason — this machine's transcripts carry twelve honest worker-restore findings inside this project's own worktrees (`live-spec-split/wt`, `live-spec-morning/wt`, `live-spec-cull2/wt` among them) over the last 24 hours, none of them touched by this branch. This package does not repair those findings and was never scoped to: its whole job was the four false positives whose `cwd` was an unplaceable home directory, and those four are the only ones this delta moved. The twelve real ones stay exactly as red as they were before this branch started, and clearing them needs either time (they age out of the 24h window) or the owner's word.
Blocking: none
