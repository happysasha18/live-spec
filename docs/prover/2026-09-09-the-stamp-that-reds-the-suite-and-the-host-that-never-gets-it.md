# Prover record — 2026-09-09 the stamp that reds the suite, and the host that never gets the change

Prover skill version: product-prover 1.6.0, the installed copy under `skills/product-prover/`, read
beside the pack bindings `skills/product-prover-pack/SKILL.md` (v6.1.2). The reviewer wrote none of
this change and none of the code it touches.

PUSH-REVIEW

Mode: global
Scope: the owner asked for a read of this release's operational-observability surface and nothing
wider — `guardrails/check-acceptance-rerun.py`'s live output, `scripts/task-admission.py`'s
recorded-duration reader (`recorded_duration`, `comparable_durations`, `_estimate`) and its
`history` command, and the release stamping that carries them: `VERSION`, `guardrails.config.json`'s
`core_version`, `.claude-plugin/plugin.json`, the skill version stamps, and MIGRATION.md's 6.1.2
chapter.
Range: 36350f5..7dca40db
- 7dca40db 6.1.2 — a run that says what it is doing, and the duration a close recorded
- 36350f5 (base) The record's range reaches the language repair
Files read: PLAN.md (row q-831), .live-spec/checkpoints/q-831.md, .live-spec/readers/q-831-reader.md, guardrails/check-acceptance-rerun.py, guardrails/run_modes.py, guardrails/pre-push, guardrails/check-prover-record.sh, scripts/task-admission.py, scripts/plan_checks.py, scripts/plan_checks_core.py, scripts/check-pack-update.sh, adopt/install-scaffold.sh, adopt/install-style-gates.sh, tests/test_live_progress_and_recorded_history.py, tests/test_release_6_1_1.py, tests/test_acceptance_rerun_reach.py, .github/workflows/gates.yml, VERSION, guardrails.config.json, .claude-plugin/plugin.json, MIGRATION.md, JOURNAL.md, PRODUCT_SPEC.md (heading), the fourteen skill frontmatter stamps under skills/, docs/prover/README.md, skills/product-prover-pack/SKILL.md
Checks run: the row's own acceptance command verbatim at this commit (`PYTHONPATH=tests python3 -m unittest -q test_live_progress_and_recorded_history && test "$(cat VERSION)" = "6.1.2" && grep -q '^### 6.1.2' MIGRATION.md`) — exit 0, 14 tests; `python3 -m pytest -q tests/test_acceptance_rerun_reach.py tests/test_run_modes_install.py` — 26 passed; `python3 -m pytest -q tests/test_task_admission.py tests/test_report_estimates.py tests/test_statement_validation.py tests/test_work_board.py tests/test_checkpoint_mechanism.py` — 169 passed; `python3 -m pytest -q tests/test_release_6_1_1.py tests/test_closure_kernel_bypasses.py` — **1 failed, 47 passed**; `python3 scripts/task-admission.py verify q-831 --by push-reviewer` — red, "no open checkpoint to accept" (the row is closed, its recorded receipt stands at 36350f5); `python3 scripts/task-admission.py history` over the real tree for Guardrails, Core and an unknown group; `_estimate` driven directly for a group with two recorded rows, a group with none, and a synthetic single-row group; `recorded_duration` driven over eleven hand-built checkpoints (malformed number, comma decimal, trailing text, unknown unit, negative, zero, two trail lines, an unparseable sheet, an open sheet, "not recorded"); the real rerun gate driven over four throwaway git repositories under the session scratchpad — three passing targets, a mixed run with one failing target and one ended by the emergency stop, an empty composition, and twelve targets over eight jobs with chatty children — and the same four ranges driven against origin/main's copy of the gate side by side to compare verdicts; `adopt/install-scaffold.sh` run into a throwaway host already carrying the two vendored files; `scripts/check-pack-update.sh` run against a manifest pinned at the real 6.1.1 contents of every vendored file.
Findings: The live output holds on every path I could drive it down, and the reader gives back only what a close recorded. Every target prints a start line and a finish line, including the one that fails and the one the emergency stop ends; the composition line always precedes the first start; the counts are produced in the single thread that consumes the futures, so "n of m finished, k to come" cannot race; the child processes are captured, so the only writers to stdout are the lock-held `say` calls and no line interleaved across four runs of twelve concurrent targets. Verdicts are unchanged: driven side by side with origin/main's copy of the gate over the same trees, the exit codes and every summary line agree, the sole difference being the "run mode" line moving from the closing report to the head of the run. The emergency stop still takes no part in the verdict — the stopped row reports "stopped before it returned a verdict" and the red came from the failing acceptance alone. Nothing the owner barred is present in substance in either half: no duration is read or compared, no threshold, no host load, no clock in any verdict, no polling, no watcher, no new process, no second store (the reader takes the line the close already wrote), no new hook, no new CI step and no new queue — the only addition to the concurrency is a print lock, and `EMERGENCY_STOP_SECONDS` is untouched at 300. The reader answers off the delivery trail alone: touching a checkpoint file moves nothing, a close that wrote "actual not recorded" contributes nothing, another group is not read, an unparseable sheet yields nothing, and `_estimate` still behaves both ways — Guardrails now reports 12 to 36 minutes off q-830 and q-831's own trails, a group with no recorded row falls back to the route's stated range, and a route with neither still refuses by name. A row closed twice cannot carry two trails: `_write_delivery_trail` drops any existing `estimate ` line before appending, so the reopen-and-close path leaves exactly one. Both repairs the verifier's own run produced are right and leave nothing dangling: `_span` has exactly two units, so recomputing both ends in hours when they straddle ninety minutes is the whole of the correction and `_estimate`'s older form is correct for the same reason (its high end is already hours-valued in that branch); the removed runtime composition check leaves no orphan — `composition` and `finished` both still feed the line that prints, and no test names the removed check. Three things stand outside the two blocking items and I do not block on them. `recorded_duration` (scripts/task-admission.py:603) crashes with an uncaught ValueError where the trail's number is not a float — `estimate 3–5 hours → actual 1.2.3 hours`, or a bare `.` — and that traceback would escape through `comparable_durations` into the admission of the next row in the group; the same function already answers "no duration" to every other unreadable form, including an unreadable sheet, so the guard stops one line short of where it was going. No writer in this tree produces such a line, which is why it is not a failure of the shipped path. Second, `tests/test_live_progress_and_recorded_history.py:295` is a bare function in a unittest module, so the row's acceptance — which runs `python3 -m unittest` — never executes it; the stamp is checked by the acceptance's own two shell arms instead, and the function repeats the literal version pin that produced the first blocking item below. Third, JOURNAL.md's 6.1.2 entry says the closing line is "checked against the same list rather than asserted", while its own third paragraph and the code comment at guardrails/check-acceptance-rerun.py:409 say the runtime comparison was removed because it could never fire; the line does read two counters off the real list, but nothing compares them, and the two sentences read against each other. One more thing true outside this scope is deposited at `inbox/2026-09-09-notes-beside-q831.md`, opening no row.
Blocking: two, both reproducible, both closed in the landing this record covers. The reviewer wrote none of the change and none of the repairs; each `stands:` is kept as written and the `closed:` beside it is the holder's, added after this read.
- The release stamp reds the suite that CI runs on every push. stands: `tests/test_release_6_1_1.py::test_the_pack_is_stamped_and_carries_its_host_chapter` asserts `VERSION` reads `6.1.1` (line 333) and that `guardrails.config.json`'s `run_modes.release.core_version` reads `6.1.1` (line 337). This range moves both to 6.1.2, so `python3 -m pytest -q tests/test_release_6_1_1.py` returns 1 failed, 47 passed at 7dca40db and passed at 36350f5. `.github/workflows/gates.yml:64` runs `python3 -m pytest -q` over `testpaths = tests` on every push, so this push arrives red on gate b. The row's own acceptance could not see it: it runs one module under `unittest`, and the row's verification command re-runs the receipt, not the suite. The defect is a class, not a line — a per-release test file that pins the literal version reds at the next stamp, and this landing added a second one at `tests/test_live_progress_and_recorded_history.py:295` that will red at 6.1.3. Fix: whatever the holder decides for the 6.1.1 file, the pinned assertion wants to be about the stamp's internal agreement (VERSION, `core_version`, `.claude-plugin/plugin.json` and the skill frontmatter all reading the same number) rather than about one literal that a later release invalidates. closed: `tests/test_release_6_1_1.py` no longer pins a version literal at all. That every version surface agrees with VERSION is one fact with one home, `tests/test_version_is_one_fact.py`, which was green throughout; the 6.1.1 file now holds what 6.1.1 shipped, and its chapter assertion, which is history and stays true. The second literal this landing added is gone the same way: the new file asserts MIGRATION.md carries a chapter for whatever VERSION reads, so it cannot red at 6.1.3. A claim about THIS release belongs in the row's own recorded acceptance, which is where 6.1.2's stamp is checked.
- MIGRATION.md's 6.1.2 chapter tells a host it needs to do nothing, and a host that does nothing gets none of this change. stands: the chapter opens "Host action: none required" and says "A host that vendors this gate gets the output by pulling the pack; there is nothing to configure and nothing to turn on" (MIGRATION.md:499 and :508). Both of the files this release changes are vendored into a host tree by `adopt/install-scaffold.sh` — `guardrails/check-acceptance-rerun.py` and `scripts/task-admission.py` are two of the five `VENDOR_MODES` pairs at adopt/install-scaffold.sh:61-67 — and that loop skips a file the host already has unless `--force` is given (adopt/install-scaffold.sh:75). Driven into a throwaway host carrying both files, the installer printed `skip (exists, use --force to overwrite)` for exactly those two and vendored everything else. So a host on 6.1.1 that pulls the pack keeps running its 6.1.1 copies: it sees no live output from the gate that actually runs on its pushes, the command the chapter advertises — `python3 scripts/task-admission.py history "<group>"` — does not exist there, and the estimate reader the chapter says is fixed goes on returning the file-stamp artefact the chapter describes as past. The machinery that would tell such a host names the wrong road besides: with a manifest pinned at the real 6.1.1 contents, `scripts/check-pack-update.sh` reports both files stale and offers `bash <pack>/adopt/install-style-gates.sh --force`, an installer whose vendor set (adopt/install-style-gates.sh:97-107) contains neither file, so nothing anywhere names the road that works. Fix: say in the chapter what a host that vendors these two files runs to get them — `bash <pack>/adopt/install-scaffold.sh --force`, with the standing warning that a forced re-vendor overwrites host edits to those files — or state plainly that a host on the vendored copies stays on 6.1.1 behaviour until it re-vendors. closed: the chapter names the command and stops calling the host action none — `cd <your host root> && sh <pack>/adopt/install-scaffold.sh --force`, with `--force` named as what replaces the two copies a tree already holds, and with the note that it vendors into the directory it is run from. Proved on a throwaway host carrying stale copies of both files: the run printed `vendored:` for each, and `history` exists in the vendored reader afterwards. Handing the installer a path instead is refused as an unknown flag, which is how the first attempt at this proof silently did nothing, so the chapter says where to stand.

## What this read did not cover

The scope was named before the read and did not grow with it. The skill bodies, the spec and the
architecture were read only where the stamping touches them; every other surface of the pack, and
every path the diff does not touch, was left alone. `tests/test_guardrails.py` was not run — it
stashes the working tree — and no full `pytest` run was attempted, since it hangs on this machine;
what is claimed about the suite above rests on the one file that reds, run on its own and named
with its line numbers.
