# Prover record — 2026-08-12 the rule 30 cut

PUSH-REVIEW

Range: dfa9f57..d07f2d0
- d07f2d0 The records land: D2 executed in every home, the plan wears the done mark
- 3866a6c Rule 30 leaves the rulebook, its number a hole: his 21:22 word (D2)
- 99481e0 The plan carries status emojis, its page a styled render: his 22:28 word, insert-only
- 391ba4e The plan-v3 sitting closes: his 21:22 acceptance and D2 word, the range pushed

Four commits across 27 files, 212 insertions and 138 deletions. The range's one structural move is
D2: rule 30, the rule that minted a push gate from any machine-verifiable quality, is cut from the
shared rulebook on his word of 2026-08-11 21:22.

Files read: `skills/live-spec-base/SKILL.md`, `PRODUCT_SPEC.md`, `PRODUCT_SPEC.index.md`,
`ARCHITECTURE.md`, `TEST_MATRIX.md`, `ROADMAP.md`, `README.md`, `NEXT_STEPS.md`, `DECISIONS.md`,
`MIGRATION.md`, `docs/PROGRESS.md`, `.live-spec/culling-plan-v3-2026-08-10.md`,
`.live-spec/plan-v3-delta-2026-08-12.md`, `skills/build-pipeline/SKILL.md`,
`skills/communicator/SKILL.md`, `skills/communicator/references/words.md`,
`guardrails/pre-push`, `guardrails/check-tree-counts.py`, `guardrails/check-prover-record.sh`,
`guardrails/check-freeze.sh`, `guardrails/check-config-health.sh`,
`guardrails/check-doc-findings-bound.py`, `guardrails/check-index-generated.py`,
`guardrails/progress-baseline.json`, `tests/test_compaction_discipline.py`,
`tests/test_request_classifier.py`, `tests/test_tree_counts.py`, `tests/test_published_counts.py`,
`tests/test_clean_context_review.py`, `tests/test_resume_rederive.py`,
`docs/prover/2026-08-11-thirty-commit-range.md`.

Checks run: `python3 guardrails/check-tree-counts.py` — FAIL, exit 1, three published-count faults on
`README.md`; `python3 guardrails/check-doc-findings-bound.py` — OK, exit 0, 122 live documents, 22
held at zero, one fallen ceiling named; `python3 guardrails/check-index-generated.py PRODUCT_SPEC.md
PRODUCT_SPEC.index.md` — OK, exit 0, 398 of 398 rows matched, committed index equals a fresh build;
`bash guardrails/check-freeze.sh` — OK, exit 0, three guarded documents match their baseline;
`bash guardrails/check-config-health.sh` — OK, exit 0, installed hooks and skill copies match source;
`bash guardrails/check-prover-record.sh --push` — FAIL, exit 1, no `docs/prover/2026-08-12*.md`, which
is the gate this record closes; `grep -cE '^[0-9]+\. \*\*' skills/live-spec-base/SKILL.md` — 34;
`grep -oE '^[0-9]+\. \*\*'` over the same file — heads 1 to 29 then 31 to 35, the hole at 30;
`git show origin/main:skills/live-spec-base/SKILL.md | wc -c` — 66,435 against 65,496 at HEAD;
`wc -l` on the two edited skill bodies at both ends of the range; `cat skills/*/SKILL.md
skills/*/references/*.md | wc -l` — 6,443; `cat skills/*/SKILL.md | wc -l` — 5,205;
`wc -c PRODUCT_SPEC.md` — 702,784 and `wc -w` — 117,814, both against `docs/PROGRESS.md`; a tree-wide
grep for `rule 30`, `base rule 30` and `thirty-five` over every live surface; a scripted read of
99481e0's diff testing its insert-only claim. The orchestrator's full run on this tree: 2,484 passed,
2 skipped, 0 failed.

Findings: the cut itself is clean and its arithmetic reconciles to the byte. The rule is gone whole,
the hole at 30 is deliberate and held by a real negative assertion, every citation of the cut rule on
a live surface was chased down, and the derived numbers the seat published about the cut are true.
The push nevertheless fails one of its own gates, deterministically and outside pytest, and the
resume file contradicts the range's own closing commit. Those are the two blocking findings. Five
further findings are recorded without blocking.

1. Blocking. Gate ad refuses the push. `python3 guardrails/check-tree-counts.py` exits 1 with three
   faults, all on `README.md`: the generated `skills-lines` block differs from a fresh build, and the
   two reproduction commands the block prints beside its figures return numbers the block does not
   state. The committed block reads 6,456 lines under `skills/` and 5,218 lines of skill bodies. The
   tree returns 6,443 and 5,205. The drift is exactly 13 lines on both figures, and all 13 belong to
   this range: `skills/live-spec-base/SKILL.md` went 733 lines to 722 when rule 30 was cut, and
   `skills/build-pipeline/SKILL.md` went 674 to 672 when the same commit dropped the rule's
   restatement from the compaction bullet. No other skill file changed line count, so the block was
   green at `origin/main` and 3866a6c reddened it by not re-running `python3
   scripts/gen-tree-counts.py`. The gate is wired at `guardrails/pre-push` line 240 and sets
   `fail=1`, so the push road cannot pass. `README.md` states the consequence in its own words in the
   very block that is stale: a push of this repository is refused where either command disagrees with
   the number printed here.

   Why the green suite is not evidence against this. The full run reports 2,484 passed and 0 failed
   on this tree, and gate ad is red on the same tree at the same moment. `tests/test_tree_counts.py`
   carries 39 tests and `tests/test_published_counts.py` carries 3, and every one of them exercises
   the gate against a fixture tree or asserts the gate's wiring into `pre-push` and the CI mirror.
   Neither file carries an arm that runs the gate over the real repository. So the suite proves the
   gate works and never asks whether this tree passes it. A reviewer who read the suite log alone
   would ship a repository whose README publishes a count the tree does not carry.

2. Blocking. `NEXT_STEPS.md` contradicts the commit that closes the range. d07f2d0's message reads
   that D2 is executed in every home. The commit touches the plan, its delta page, `DECISIONS.md`,
   `JOURNAL.md` and `ROADMAP.md`. It does not touch `NEXT_STEPS.md`. That file's LIVE STATE block,
   stamped 2026-08-11 22:17 and headed as written for a session starting with clean context, still
   reads at line 22: execute D2, cut rule 30 from the rulebook. The cut landed at 03:17 the next
   morning, five hours after the stamp, and the resume file was never moved. A fresh session opening
   this repository is told as its first step to do work the tree already holds, and the same block's
   list of what was executed today names D8, D4, D5 and R7 while omitting the one cut this range
   exists to make. The resume file is the surface a clean-context session reads first, so the
   contradiction lands on the reader the range was written for.

3. Non-blocking. The plan disagrees with itself about its own acceptance. Line 3 of
   `.live-spec/culling-plan-v3-2026-08-10.md` records the plan as accepted on his word of 2026-08-11
   21:22. Line 89 still heads phase 2 as waiting for that same acceptance. 99481e0 passed over this
   line and stamped a waiting mark on it, so the emoji pass wrote a status the file's own opening
   line had already retired. The same commit's insert-only claim is sound — a scripted read of its
   diff finds no removed line without an emoji-only counterpart among the added lines — which is what
   makes this a missed cell rather than a rewrite. The plan is the standing order and the page he
   reads, so the two live cells wanting one answer is worth the point edit. Owner: the plan's keeper,
   with the delta page the plan-delta guard asks for.

4. Non-blocking, and already owned. `MIGRATION.md` line 125 still states rule 30 in the present tense
   as one of the two things a host inherits by adopting 2.0.0: a quality a machine can verify is
   enforced by a gate. `DECISIONS.md` line 531 records this debt in the same range, naming a
   MIGRATION.md chapter owed at the next release because a host that adopted 2.0.0 still carries
   rule 30. The surface is adopter-facing and the record is honest about it, so this is noted rather
   than pressed. Owner: whoever writes the next release chapter.

5. Non-blocking. One recorded ceiling now sits above what the text measures.
   `python3 guardrails/check-doc-findings-bound.py` exits 0 and prints
   `fell: skills/build-pipeline/SKILL.md — recorded 256, measured 255`, the drop being the sentence
   3866a6c removed from the compaction bullet. The gate names its own repair,
   `python3 scripts/rule-census.py --json guardrails/rule-census.json`. Owner: whoever repairs
   finding 1, in the same commit, since that repair moves `README.md` and not this figure.

6. Non-blocking, and the fourth occurrence of a class the previous record named. `docs/PROGRESS.md`
   line 5 reads that since the last run on 2026-08-12 the totals changed by zero, on a page generated
   on 2026-08-12. The page compares the run against itself, so the delta sentence carries no
   information. The byte and word halves do reconcile this time, and exactly: the page states
   `PRODUCT_SPEC.md` at 702,784 bytes and 117,814 words, and the file measures 702,784 and 117,814.
   The staleness that reddened three commits of the previous range did not recur here. Owner: the
   page's keeper, with the gate the previous record's finding 15 already proposed.

7. Non-blocking. One guard was loosened wider than the cut required. Before this range
   `tests/test_request_classifier.py` asserted the rulebook's numbered heads equal
   `range(1, len(heads) + 1)`, so a repeated or skipped number reddened. It now asserts only that the
   heads open at 1 and climb strictly. The relaxation is needed — the hole at 30 is deliberate, and
   the docstring says so and dates it to his D2 word — but strictly-climbing admits a gap of any size
   at any position, so a future rule number dropped by accident passes as silently as this one passes
   by design. The rule count itself stays honest, since it derives from the heads present and the
   description's `thirty-four` is asserted against it in three files. Owner: the rulebook's keeper, if
   the hole is ever worth pinning to the one number it is.

What held. The cut is complete where completeness was owed, and its numbers are true. The rulebook
carries 34 numbered heads, 1 to 29 and 31 to 35, and its frontmatter description reads thirty-four,
asserted from `tests/test_clean_context_review.py` and `tests/test_resume_rederive.py`. The byte claim
in `DECISIONS.md` verifies exactly: 66,435 at `origin/main` against 65,496 at HEAD. No live surface
anywhere in the tree still reads thirty-five — the only survivals are in `JOURNAL.md` and in dated
records under `docs/prover/`, `docs/audits/` and `attic/`, where they are history and belong. Every
live citation of the cut rule was chased: `PRODUCT_SPEC.md` R132.3 is inverted rather than deleted and
now reads that no gate is minted from checkability alone, re-anchored to INV-108; `TEST_MATRIX.md`
M-313, M-421 and M-422 dropped the citation; `ROADMAP.md` row 424 re-pointed to INV-239; four
guardrail scripts and one test docstring lost the reference without losing their sentence. The
surviving `rule 30` strings on live surfaces are all deliberate references to the cut itself, plus
findings 2 and 4.

The guard test is real, not vacuous. `test_base_rulebook_carries_no_generator_rule` asserts the rule's
opening sentence is absent from the flattened body, then parses the heads and asserts 30 is not among
them while 29 and 31 are. `test_a_check_is_not_born_from_checkability` pairs a positive assertion on
the new criterion with a negative one on the old wording, so a revert reds in both directions. Neither
is anchored on comment text.

The freeze law held. `PRODUCT_SPEC.md`, `ARCHITECTURE.md` and `TEST_MATRIX.md` all changed on purpose
in 3866a6c, and `bash guardrails/check-freeze.sh` exits 0 over three files. The baseline JSONs under
`.spec-freeze/` carry an 03:17 mtime, matching the commit, and none appears in the range's diff, which
matches `DECISIONS.md`'s claim that the re-run came out byte-identical.

Two defect classes the previous range shipped did not recur. The generated index has one answer in
both homes — `check-index-generated.py` reports 398 of 398 and the embedded Reference table carries
the same INV-108 row as the standalone file, both rebuilt in the same commit as the criterion that
moved them. And `check-config-health.sh` exits 0, so the installed skill copies were synced the same
night the rulebook shrank, as `DECISIONS.md` states.

`ROADMAP.md` row 589's account of the range checks out against the diff. It names three missed live
citations found at acceptance — two matrix rows and queue row 424 — and the diff carries repairs to
exactly M-421, M-422 and row 424. Its amended line, that the worker's second report matched the tree,
is consistent with everything measured above.

Blocking:
- Gate ad refuses the push: `README.md`'s generated skills-lines block publishes 6,456 and 5,218 while
  the tree returns 6,443 and 5,205, the 13-line drift on both figures being this range's own cut.
  stands: the repair is `python3 scripts/gen-tree-counts.py` followed by a commit of the rebuilt
  block, and this reviewer edits no file but this record.
- `NEXT_STEPS.md` still names executing D2 as the next session's first step, contradicting d07f2d0's
  claim that D2 landed in every home. stands: the LIVE STATE block wants a fresh stamp from the seat
  that owns it, and this reviewer edits no file but this record.

Verdict: blocked. Two blocking findings stand, both repairable in one commit. Neither touches the cut
itself, which is sound in the tree, sound in its arithmetic and sound in the guard that holds it.
