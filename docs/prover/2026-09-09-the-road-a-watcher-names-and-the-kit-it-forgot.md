# Prover record — 2026-09-09 the road a watcher names, and the kit it forgot

Prover skill version: product-prover 1.6.0, the installed copy under `skills/product-prover/`, read
beside the pack bindings `skills/product-prover-pack/SKILL.md` (v6.1.3). The reviewer wrote none of
this change and none of the code it touches, and made no edit to the tree in any of its three
rounds; every mutation it tried was on a scratch copy of the pack.

PUSH-REVIEW

Mode: closure
Scope: the accepted row q-832 and nothing else — its definition of done, its own recorded
acceptance command at this commit, the diff of the pushed range, and the paths that diff touches
read directly, which are `scripts/check-pack-update.sh`'s
manifest arm, the three installers under `adopt/` whose vendor arrays it now reads,
`tests/test_update_watcher.py`, and the release stamping and prose that carry them: `VERSION`,
`guardrails.config.json`'s `core_version`, `.claude-plugin/plugin.json`, the skill version stamps,
MIGRATION.md's 6.1.3 chapter, JOURNAL.md's entry, and the two archived inbox notes.
Range: baa97a08..1881ced1
- 1881ced1 6.1.3 — the watcher names the installer that actually re-vendors the file
- baa97a08 (base) The scoped record's range reaches the repairs it caused
Files read: PLAN.md (row q-832), .live-spec/checkpoints/q-832.md, .live-spec/readers/q-832-reader.md, scripts/check-pack-update.sh, adopt/install-scaffold.sh, adopt/install-status-view.sh, adopt/install-style-gates.sh, tests/test_update_watcher.py, scripts/plan_checks.py, VERSION, guardrails.config.json, .claude-plugin/plugin.json, MIGRATION.md, JOURNAL.md, inbox/handled/2026-09-08-notes-beside-q829.md, inbox/handled/2026-09-09-notes-beside-q831.md
Checks run: the row's own acceptance command verbatim (`PYTHONPATH=tests python3 -m unittest -q test_update_watcher && test "$(cat VERSION)" = "6.1.3" && grep -q '^### 6.1.3' MIGRATION.md`) — exit 0, 16 tests; `python3 -m pytest -q tests/test_update_watcher.py` — 16 passed; `python3 -m pytest -q tests/test_version_is_one_fact.py tests/test_release_6_1_1.py tests/test_live_progress_and_recorded_history.py tests/test_update_watcher.py tests/test_scaffold_install.py` — 63 passed; the real `scripts/check-pack-update.sh` driven from scratch directories over hand-built manifests in more than twenty shapes across three rounds — the five run-mode keys alone, the style-gate set alone, the status-view set alone, all three kits at once, a claimed key beside an unclaimed one, keys no installer vendors, and the old-pin-nothing-stale branch pinned at the real sha256 of each file for status-view only, style-gate only, scaffold only, all three, and an all-unclaimed manifest; all three real installers run into one scratch host and each of the 25 manifest keys they wrote driven individually through the watcher, its road compared against the array feeding that installer's own `cp` loop; the empirical road test — a host file corrupted and the road the watcher printed actually run, for `guardrails/run_modes.py`, `scripts/task-admission.py`, `scripts/render-board.sh`, `scripts/state-probe.sh` and `scripts/spec-freeze.py`; all eight status-view destinations corrupted and `adopt/install-status-view.sh --force` run over them; `dash adopt/install-scaffold.sh --force`; and the array reader attacked on a mutated copy of the pack with `adopt/` unreadable, `adopt/` absent, one installer unreadable, an array reshaped to one line, and a non-path string inside an array.
Findings: The sorting now carries no default branch. Every manifest key is mapped to the installer whose own vendor array states it, the stale keys are grouped by installer and one road printed per installer, and a key no installer claims prints a line saying so and takes no road. Driven over the 25 keys three real installers actually write, every attribution matched the array that feeds that installer's copying loop, with zero unclaimed and zero misattributed; the roads were then run against corrupted host copies and restored the files in every case tried. `install-style-gates.sh` pins `scaffold/guardrails/*` keys it never vendors, saying so in its own comment, and the watcher correctly routes those to `install-scaffold.sh`. The `not stale` branch, which named one fixed installer whatever a host had adopted, now names the kits the manifest's own keys belong to. No mutation of the reader produced a wrong road: an unreadable `adopt/`, an absent `adopt/`, an unreadable installer and a one-line array all degrade to "no road is named", and an over-matching parse harvests inert junk that can never equal a real manifest key. Three residual nits stand outside the definition of done and none blocks. The unclaimed line's wording is false where the cause is an unreadable `adopt/` rather than an unclaimed key, safe in effect and theoretical for this pack. The array parse depends on each array closing with `)` at column 0, and `test_every_adopt_installer_is_a_road_the_watcher_can_name` re-implements the same regex, so it proves the routing wiring and cannot catch a mis-parse. `scripts/task-admission.py` and `scripts/checkpoint.py` are vendored by two installers and attribute to `install-scaffold.sh` by alphabetical first-wins; both roads restore them, proved by the corruption test. Two facts true outside this row's reach were also confirmed unchanged by it: `install-style-gates.sh --force` with no doc argument is a silent no-op on a host whose gated doc is not `PRODUCT_SPEC.md`, and a bare re-run resets `gated_docs` and `tier` to the defaults.
Blocking: four across three rounds, all reproducible, all closed before this commit. The reviewer wrote none of the change and none of the repairs; each `stands:` is the reviewer's own, and the `closed:` beside it is the holder's, added after that round's read.
- The first repair was the same defect one kit over. stands: the sorting read `adopt/install-scaffold.sh`'s `VENDOR_MODES` array and sent every key that array did not claim to the style-gate road by default. `adopt/install-status-view.sh:51-59` keeps its own `VENDOR` array of eight keys and writes them into the same manifest at :162. The reviewer ran that real installer into a scratch host, aged the manifest it wrote, and got `re-install road: bash <pack>/adopt/install-style-gates.sh --force` for `scripts/render-board.sh` and `scaffold/status-view/state-probe.sh`, an installer whose vendor set contains neither — follow the advice, nothing changes, the watcher repeats it tomorrow. closed: the arm now lists every `adopt/install-*.sh`, reads each one's `VENDOR*` arrays, and maps each key to the installer that states it; an unclaimed key is said out loud. Proved over all 25 keys three real installers write, and empirically by corrupting a host file and running the road the watcher named.
- MIGRATION's chapters name their repair command under `sh`, which cannot run it. stands: `adopt/install-scaffold.sh` is `#!/usr/bin/env bash` and uses `set -o pipefail` and bash arrays; under dash it exits at line 19 with `set: Illegal option -o pipefail` before copying anything. Every other command in MIGRATION.md says `bash`; the 6.1.2 chapter said `sh` and the 6.1.3 chapter had copied it. closed: both chapters say `bash`. Reproduced here: `dash adopt/install-scaffold.sh --force` gives the same line.
- The 6.1.3 chapter named one repair for the two kinds of host it identifies. stands: the chapter says a host stale on the run-mode five and a host stale on the status-view kit were both misrouted, then gives `bash <pack>/adopt/install-scaffold.sh --force` alone. Demonstrated on a three-kit scratch host: `scripts/render-board.sh` and `scripts/state-probe.sh` were still stale after that command — the exact experience the chapter documents the end of. closed: the chapter names both roads, with the status-view kit's files listed, and says the watcher names the right one itself from this release on. The reviewer then corrupted all eight status-view destinations and ran the second command: all eight restored. The six-file list was derived from the installer's own copying loop and found right; the two omitted keys are shared with the scaffold kit and named in the paragraph above.
- The landing record claimed a test case the suite did not carry. stands: JOURNAL.md and the archived q-831 note both enumerated "all three at once" among the cases driving the real script, and no such test existed — `test_mixed_stale_keys_propose_both_reinstall_roads` stales two kits. closed: the case was added rather than the claim trimmed, together with the old-pin case; both records now say seven cases and each of the seven was matched to a test by name and line.

## What this read did not cover

The scope was named before the read and did not grow with it. The skill bodies, the spec, the
architecture and the matrix were not read; nothing in this range touches them beyond the version
stamps, which `tests/test_version_is_one_fact.py` holds. The full pytest suite was not run: the
owner named this row's final acceptance as its recorded `test_update_watcher` plus the real
installer and watcher path, and CI's gate b runs the whole suite over this commit. The version
literals that reddened the 6.1.2 push were checked for by hand across `tests/` and every one that
remains is a history assertion about a chapter that stays true.

One thing rides in the reviewed commit that this read did not cover, and it carries no code: the
board state of the next row, q-833 — its row in PLAN.md, its checkpoint, its reader's record and
its acceptance key in `scripts/plan_checks.py`. It was admitted and taken up after this landing was
verified, and its own work lands under its own record.
