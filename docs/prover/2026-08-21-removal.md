# PUSH-REVIEW — removing the checks that guarded only this project's own shape

Range: 5e23c086..e61b29b — one commit reviewed, `e61b29b`. Base `5e23c086`.

Files read: the full diff — 76 files deleted, 43 edited, 2 added; `guardrails/pre-push`
before and after, section by section, to confirm that every removed letter took its whole
section and no surviving letter lost a line; `.github/workflows/gates.yml` the same way;
each removed script's own header, to take its stated purpose from its own words rather than
from a summary; every file that named a removed file, found by grep and re-grepped after.
`PRODUCT_SPEC.md` and `ARCHITECTURE.md` were read only at the lines this change touches.

Checks run: `bash -n guardrails/pre-push` and `bash -n guardrails/check-tests.sh` — clean.
`gates.yml` parses. `pytest tests/ --collect-only` — 2355 tests, no collection error.
`pytest tests/test_traceability.py tests/test_skill_count_agrees.py tests/test_version_is_one_fact.py`
— 199 passed. `pytest tests/test_progress_report.py` — 18 passed. Two full local suite runs
were made and agreed with each other; the full suite is authoritative in CI on this push.

Findings: five.

**F1 — the previous push landed with CI red, and this record says so.** `5e23c086` passed
all 29 gates and failed the suite in CI. Seven tests: the pack counts its own skills in
seven places and the twelfth skill was registered in none of them, and one test holds that
every skill carries the pack's version, which `director` did not. Both were the author's
omission. Fixed in this commit. Worth keeping in the record because not one of the 29 gates
noticed, and the check that did notice is the one that stays.

**F2 — two files on the removal list were kept, on evidence against the list.**
`guardrails/attic/check-listener-tripwire.py` is loaded at runtime by
`guardrails/route_agent_transport.py`, which was not on the list; it is not an orphan
whatever its own docstring says. `guardrails/spec-ratchet.json` is read by
`check-size-ratchet.py`, also not on the list, which fails hard without it. Removing either
would have broken a live path rather than retired a dead one. The instruction to stop and
report rather than delete on doubt is what caught both.

**F3 — the removal broke one script, and it was cut rather than stubbed.**
`scripts/progress-report.py` imported the deleted census as a module. Its census-dependent
rows now print "not stated" and its ranking table returns nothing, because the source is
gone and there is no other. The dead parameters that fed it were deleted outright. A stub
returning empty values to make a test green is the dead compatibility this package exists
to remove, and it was refused.

**F4 — the removal's own edit tripped a surviving check, which is the point of it.** A
rewritten line in the architecture's budget table used the "X, not Y" construction the
prose lint forbids. Caught by `scripts/spec-style-lint.py`, which stays, and rewritten. A
check that catches its own demolition crew is a check earning its place.

**F5 — six skill files were edited without an independent review, and that is stated
rather than implied.** Recorded in `docs/skill-review/2026-08-21-skills-after-the-removal.md`.
The edits delete pointers to deleted files and add one line to five closing lists. An
adversarial pass over 150 KB of unchanged prose to certify that would be exactly the
ceremony this package removes; the record names the check as the weaker one it is.

Blocking: none

Twenty-two gate letters remain, and the push path is now: commit, then
`GIT_DIR=... GIT_WORK_TREE=... git push`, which runs those 22 in order and refuses on the
first red. What each one prevents is one sentence in the commit message. Gates a and s stay
until the Director executes, by decision and not by oversight — the mandate retires that
family once the new path replaces the old, and today those two records were the only place
a discarded run and this session's own errors remained written down.
