# From tlvphotos — the pack owes a session a thrift rule

**Root.** Alexander's words in the tlvphotos window, 2026-08-07 14:14: "плиз работай так чтобы
контекст беречь и с другой стороны максимально эффективно расходовать токены. не в счет качества, но
просто не транжирь. это должно быть записано. если не записано то отпиши лайвспек."

**Harvested 2026-08-07 into ROADMAP row 584.** He asked whether the pack already carries the rule,
and it does not: the row opens the thrift discipline and the narration law's answer to it. The file
carries his own words relayed by that window, so it was read as his wish rather than as an agent's
message; the earned-message gate reads the filename alone for that door, which is ROADMAP row 585.

He asks whether the pack already carries this rule. If it does, this note is a pointer to where the
rule failed to reach a session. If it does not, it is a request for one.

**What he is asking for.** A session that spends its context and its tokens deliberately: reading the
narrowest slice that answers a question, keeping working notes out of the human's chat, batching
independent tool calls, and reporting the finished answer rather than every intermediate step. He was
explicit that the depth of the work itself stays where it is — the saving comes out of process.

**Where it bit today.** A cost measurement in this project went through three instrument designs
before one of them measured anything, and each failed design was reported to him as it happened. The
work was right; the narration of it was expensive. Separately, a morning report handed him six
addresses when one would have done, which is filed in the sibling note
`2026-08-07-from-tlvphotos-focus-and-draft-cost.md`.

**The tension the pack should settle.** The narration law asks a session to write a line whenever a
step passes or the work turns, and the personal profile asks for work shown as it goes. Read
literally, both push toward more chat. His instruction pushes the other way. A rule that says which
turns are worth a line — and which are a session's own business — would settle it.

**Where the evidence sits.** `~/tlvphotos`, this session's transcript; `lab/cost-bench.py` and
`docs/research/2026-08-07-device-budget.md` are the artifacts the three attempts produced.
