# Rotated ROADMAP rows — 2026-08-27 provenance purge

> Removed on Alexander's direct instruction, 2026-08-27 ("нафиг выпиливай из очереди про машинерию... давай чисти все"), following a provenance audit that found 113 of 221 open ROADMAP.md rows carried no instruction from him. Two buckets: **Bucket C — an agent's own idea** (38 rows found, 37 removed here — row 471 was withdrawn from this bucket on his word the same day, that the per-movement time-estimate work stays in the queue, and it was never removed) and **Bucket D — machinery about machinery, a check guarding another check** (75 rows found, all considered). Of the combined 113, 3 carried the status *held for Alexander* and were left standing (527, 529, 536 — his own open questions), and 15 more named a real incident that already happened rather than a hypothesis of what a check could do wrong, and were left standing under the same rule (399, 479, 490, 503, 504, 517, 525, 531, 542, 552, 586, 589, 596, 605, 624). This file carries the 94 rows actually removed, verbatim, unmodified from their live form — their status cells still read *queued*, *deferred*, or *far*, since they were not landed, declined, or superseded in the normal editorial sense; they were struck from the queue by the owner's order.

| # | Wish (plain words) | Class | Status | Decision / acceptance |
|---|---|---|---|---|
| 55 | (prover 2026-07-05-architecture, F1) Snapshot machinery gets its own queue row — SPEC S-0 promises every [target] is owned by a row, and the snapshot (E-7, A-6: baseline of the last accepted run, declared-scope diff) had none; design pairs with the guardrails scaffold (row 3) which consumes its diff | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: snapshot design spec'd, D-3 decided with it — MET; baseline advance wired to *landed* — MET (the law names the transition and the declared-scope asymmetry); declared-scope diff feeds the guardrails — OPEN: the seam is named and the E-6 check clause written, and the mechanical slice that reads the diff has no owner anywhere, so this row carries it · Reopened 2026-07-27: the close was wrong and the target-ownership gate said so — A-6 still carries its promised tag, and a promised tag owes an open row; the mechanical slice named as riding another row has no open owner either, so this row carries it again |
| 69 | product-prover SKILL.md: extract the category table + glossary into references/ per skill-creator sizing guidance | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Source: skill-creator eval; deferred · groups as one queue bundle with rows 467 — two skill files grown past the recommended size, owing a references/ split |
| 197 | **A live-but-busy worker is never declared dead** (night re-prove F3 on the liveness checks): the two checks' quiet verdict is conservative on ambiguity — a checkpoint showing an in-progress long operation (a render, a suite run) means ALIVE until the worker's own reply or a finished-marker; the watch windows are read from the brief's longest named operation | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: INV-76 + base rule 6 state the conservative verdict, red-proven |
| 198 | **A dead worker's output is not yet a finished worker's output** (night re-prove F4): the dead verdict clears the FILES to touch, never the WORK as done; completeness is read from the worker's own checkpoint finished-marker, and an unconfirmed output walks the verify ladder [INV-46] before integration | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: INV-76 states dead≠done with the completeness check, red-proven |
| 199 | **The real-device walk row has one defined representation** (night re-prove F5): a human-walk marker in the matrix template that the coverage gate counts as covered-at-real-device-level and the suite lists as owed-to-human — neither a blocking red nor a forbidden silent skip [INV-77, INV-80, E-15] | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: template + INV-77 state the marker, a string test pins it, red-proven |
| 302 | **The prover-record guardrail test reads live git state instead of a fixture** (found during row 301's verify, 2026-07-13): `tests/test_guardrails.py::TestGateA_ProverRecord::test_missing_record_fails` runs `check-prover-record.sh` with the default cwd (the real repo) rather than an isolated `_init_repo(tmp)` like its siblings, so whenever the real HEAD is an inbox-only commit the script's remote-deposit carve-out fires and the "must-fail" assertion flips — a red that tracks the repo's commit history, not the code under test | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: `test_missing_record_fails` builds its own scratch repo (a spec change, no record) and asserts returncode 1 regardless of the real repo's HEAD; suite green |
| 307 | **Cross-cite the async edge-completeness face to the loading/error facet** (MINOR-gate audit R2, 2026-07-13): the async face of edge completeness (pending/arrived/failed) sharpens the spec-author empty/error/loading facet; the facet points at it, but INV-138's spec clause could cross-cite that facet for one-home hygiene | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: INV-138's clause cross-cites the loading/error facet it sharpens · groups as one queue bundle with rows 308, 309, 425, 426 — small point fixes left over from two earlier one-time audits |
| 308 | **Complete the product-prover node's carried-versus-owned wiring note** (MINOR-gate audit R3, 2026-07-13): ARCHITECTURE's product-prover node lists its carried lenses, but the wiring note may not name every law it carries as against the ones it owns | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the product-prover node's wiring note names each carried-versus-owned law completely |
| 309 | **Add a `project.design-principles` row to the defaults table** (MINOR-gate audit R4, 2026-07-13): the setting exists (INV-136) but the base defaults table carries no `project.design-principles` row | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the defaults table carries a `project.design-principles` row |
| 332 | **Lift the ship-the-shape / host-owns-the-instance split to its own home** (1.8.0 MINOR-gate design review, 2026-07-15, finding 2): the split "the pack ships an identical body → centralize [INV-158]; the body is host-specific → ship the shape and the host owns the instance" now recurs across INV-125 (no DOM of its own), INV-136/139 (kind principles the product instantiates), E-26 (the removal scanner), and INV-158 (the harness, the centralize case). E-26 was reframed to name the discriminator and cite the family (322/329 landing), so the split is STATED, but it has no single declared home the way the forward-binding law [INV-159] and the suite-honesty class [INV-160] now do. Consider minting the split its own invariant so each site cites one root rather than re-deriving the contrast. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: either the split is minted as one invariant every site cites, or a dated note agrees it stays stated-in-E-26-and-cited |
| 389 | **A remote consumer of a private contract has no read grant** (the 2026-07-17 audit's finding 8): INV-187 sends the consumer to read "over git when remote" and cites [INV-112], whose home defines a PUSH grant on the deposit path; no read grant is defined anywhere in the spec. tlvphotos is a private repo and the promoter↔site pair is the expected first real consumer [row 385], so the first contract read across machines has no stated road. The remote-seat law grows its read arm: what a consumer needs, where the grant is recorded (the host profile, beside the push grant [INV-82]), and the honest failure a grantless consumer gives, naming the grant it lacks [INV-67]. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the read grant stands beside the push grant with its recording home — MET (INV-232, `trust.read-grant` in the host profile beside INV-82); a grantless remote read fails naming its grant, red-proven — MET; the real cross-machine read stays OPEN, field-gated on rows 385/247 |
| 401 | **Receiving the same message twice does no harm** (found 2026-07-17 while stating the transport law: every message already carries a stable identifier, and the half the tree states nowhere is what happens when that identifier arrives a second time): the receiving sweep keys by the message's own identifier and records what it has already processed, so a replayed deposit yields a stated no-op. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the law stands (INV-202); the receiving sweep keys by the message identifier and records it; a replayed fixture deposit produces no second row, red-proven. |
| 425 | **Rows 422 and 423 carry an invalid Class** (found 2026-07-19 folding the E-35 / INV-239 / T-24 prover pass, docs/prover/2026-07-19.md): both rows are marked Class `method`, which sits outside the queue's size vocabulary — bug / small / surface / large, one vocabulary with the spec [SPEC T-11, INV-12] — so their owner reclassifies each to a valid size. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: rows 422 and 423 each carry a size from {bug, small, surface, large} in their Class column, chosen by their owner. |
| 426 | **T-24's provisional reference in row 398's done-when is reallocated when row 398 lands** (found 2026-07-19 folding the E-35 / INV-239 / T-24 prover pass, docs/prover/2026-07-19.md): row 398's done-when borrows the code `T-24` as a fixture label for its routing-preamble deposit, a label minted before T-24 was assigned to the earned-auto-notification feature [ROADMAP 424]; when row 398 lands, that fixture reference is re-pointed at row 398's own artifact so the two never collide on one code. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: row 398's done-when no longer borrows the T-24 code as a fixture label — the reference reallocated to row 398's own identifier at its landing. |
| 428 | **A class clause for the after-the-fact user-tell family** (design review, docs/design-review/2026-07-19.md REC-1, 2026-07-19): the design review found eleven standing members of one undeclared class — every agent action the user did not watch, surfaced to the user after the fact — sharing one role and one register yet with their report home decided ad hoc per member (taste choice [SPEC INV-31], tunable parameter [INV-70], economy shed [T-19], footprint re-classification, substance removal [INV-109], step stand-down [INV-22], plan-vs-prototype diff [INV-30], escalation [INV-192], wrong referral [INV-225], deposit-tell, decline-tell [T-24]); recommend spec-author add a class-level clause naming the class and its members, stating the two shared properties (the plain report register with no bookkeeping numbers as content, the user named by role never a person [INV-28]), and the one home-keying rule the members do not yet share explicitly — home keyed by cadence, a landing-time choice or record in the landing report [INV-31] and a status-cadence agent-channel message-lifecycle event in the status report [INV-27] — after which the prover's uniformity check holds the rule for every future sibling [INV-125]. | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a class clause naming the after-the-fact user-tell family, its two shared properties, and its cadence-keyed home rule has landed on the human's word, and the prover's uniformity check holds the home rule for the class [INV-125]. · groups as one queue bundle with rows 432 — two small gaps in how the pack classifies findings for the right judge or report route |
| 432 | **The hedge paraphrase class needs a text-semantic judge, distinct from the action-trace judge** (a fresh-seat audit residual, 2026-07-20): INV-238's hedge gate is a literal net, and its prose defers the whole paraphrase class — a hedge in any phrasing — to a semantic judge; that class is a phenomenon of the reply's words, so the action-trace conduct judge [INV-241] cannot hold it. The residual is a text-reading semantic judge, a chat-surface generalization of the register judge or an extension of it, that reads the reply's meaning for an offering-hedge in any phrasing. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when INV-238's paraphrase-class deferral points at a judge that actually reads the reply text for the hedge meaning, or the promise is narrowed to what the literal net catches. |
| 435 | **The skill-copy arm misses an executable-bit-only drift** (surfaced by the conduct-audit stories 2-3 adversarial audit, 2026-07-20): `diff -rq` compares content, so an installed skill script identical in bytes with a lost `+x` bit passes green. Exposure is low — skills load as markdown plus referenced files and the installer preserves mode — so this is a robustness nit. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when the skill-copy arm also compares the executable bit, or the mode is declared out of scope with a stated reason. |
| 446 | **Every gate declares its reach** (inbox 2026-07-21-from-tlvphotos-gate-reach-defects.md): ten field defects share one shape — a gate reports green after reading a subset of its subject (an anchor regex blind to 93 of 249 codes, hardcoded core-doc filenames, a truncation at column 110, no way to state "no CI here"). The pack adds a declared-reach convention: a gate's green line states what it read (files, rows matched of rows total), and the suite asserts reach where countable. | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when the vendored gates print their reach, the ten reported defects are fixed or fenced, and a reach assertion guards the description-field and shipped-language gates · groups as one queue bundle with rows 513, 514, 515, 541, 546, 580, 585, 588, 599, 607 — automatic checks that report green on work they should have failed |
| 447 | **Axes taxonomy and migration chapters catch up** (inbox 2026-07-21-from-tlvphotos-axes-adoption-field-report.md): the keyboard gets a pole in the input-capability taxonomy, a host gets a field recording that it runs ahead of the pack's increment schedule, and MIGRATION.md gains chapters 3.1–3.6 (its list stops at 3.0.0 while VERSION reads 3.6.0). | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when the taxonomy names the keyboard's pole, the ahead-of-schedule field exists, and MIGRATION.md charts every shipped release · Third leg, unread until the 2026-07-27 inbox sweep: the release text still says the touch-parity gap stands open on the reporting host, and that host closed it on 2026-07-14, so the correction rides this row. |
| 448 | **Scope negotiation gets its measure** (found by cold reading, row 445 pilot): the spec proposes cutting scope or splitting stages when a request is larger than its worth, and names no judge or measure for effort against benefit. Define who weighs a request's effort against its stated benefit and by what, and write it into the scope-negotiation clause. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when the clause names the judge and the inputs and the cold-reader panel reads it without a finding · groups as one queue bundle with rows 449, 450, 451, 452 — five small gaps found by one cold-reading pilot |
| 449 | **The quick-win mark gets its measure** (found by cold reading, row 445 pilot): the mark's effect is stated (a waiting quick win may run ahead of larger queued items), and the reading of "low effort, immediate value" has no stated basis. Define the classifier's inputs for the mark. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when the mark's clause names its inputs and the panel reads it clean |
| 450 | **Disagreement outcomes get entry routes** (found by cold reading, row 445 pilot): of the three-source disagreement outcomes, only the bug item carries a named entry route; the spec-fix and restructure items have owners and no route from the closed five-route set, and "restructure" shares no word with the route "refactor". Name both routes or extend the route set. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when each disagreement outcome names its route and the route vocabulary has one word per concept |
| 451 | **The taste category gets a definition** (found by cold reading, row 445 pilot): the ask-the-person versus decide-myself boundary leans on "taste" with no definition; the deferral re-test law (INV-152, base rule 29) already defines the boundary by derivability — an answer no artifact holds is the person's. Fold that definition into the spec clause that uses "taste", so the word carries its measure. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when "taste" is defined in one home by the derivability test and the panel reads it clean |
| 452 | **One step sequence, stated once** (found by cold reading, row 445 pilot): the pipeline's step list and the document chain's list read as two differing sequences with no stated relation. State the one sequence and the relation (documents are stations of the same walk), in one home. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when the two lists resolve to one stated sequence and cross-references point at it |
| 465 | **text-audit's lint-fix and vocabulary rules interact** (docs/evals/2026-07-23-text-audit/scores.md): the first real eval found that fixing a contrast-by-denial frame at the lint step can insert a partial definition of an audited coined term, which then mutes the cold reader's coined-term finding to non-blocking. Close the interaction — either the step-1 lint fix may not introduce defining content for a term still under audit, or the vocabulary fallback flags the coined term before any rewording. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when the text-audit skill states the rule that closes the interaction and its eval proves the coined-term finding survives a lint-step rewrite |
| 466 | **A future conversion proves claims, not only codes** (method improvement drawn from row 445): a per-unit zero-drop check counts code tokens, so a claim whose code survives elsewhere can vanish while every code still resolves — the exact class that cost row 445 the zones-may-overlap decision and the final fourteen restorations, each caught late by the content-test suite. A future format conversion adds a claim-level drop check, with the row-445 census-and-mapping Part-3 depth (each code's carried sentences tracked, not only its presence) as the worked instance to generalize from. | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when the conversion recipe carries a claim-level drop check that reds a lost sentence whose code survives, red-proven on a seeded drop, and the method doc names the Part-3 mapping depth as its input |
| 467 | **spec-author's body size and the text-audit duplication fold** (skill-rewrite/SKILL-CREATOR-REVIEW.md, R2 and R1, deferred as a milestone-fold): spec-author is the one skill body meaningfully over the ~500-line ideal with everything inline (676 lines). Move the facet sweep or the compose-across-axes section to references/ the way build-pipeline moved its tables, and confirm the comprehension-gate pointer to text-audit left no restated loop behind (the review's B1, R1, N1 fixes already landed in its fix section). | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when spec-author reads near the 500-line ideal with its long sections in references/ and no sibling restates the text-audit loop |
| 472 | **A done-claim states its remainder up front** (row 470, symptom 3: three episodes of a landing announced whole with the remaining pile in the same breath or later): the report shape where remaining work stands beside or before the done sentence; a Stop-hook arm on the done-then-remainder ordering plus the communicator rule sharpening. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when the communicator names the shape, the hook reds a fixture reply whose remainder trails its done-claim, and a done-with-remainder-up-front reply passes |
| 473 | **A cross-project finding deposits the session it is found** (row 470, symptom 5: the 07-21 findings shipped 07-22 18:38 in one batch, one file repaired by the receiver): the sending-side twin of the inbox deposit protocol (INV-249) — a conduct-judge member reading the action trace, a reply naming a cross-project finding with no inbox write that session flagged. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when the member stands in the conduct law, a fixture trace with an undeposited finding reds, and a deposited-same-session trace passes |
| 474 | **The judge chain runs once per reply** (row 470, extra pattern: ten near-identical judge sub-sessions spawned for one outgoing reply on 2026-07-22): dedupe the Stop-hook judge chain by reply hash so one reply buys one judge run — cost and noise carry no added verdict. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when repeated Stop firings on one reply spawn one judge run, proven on a fixture, and the hook meter shows the duplicate class gone |
| 475 | **The enforcement-coverage registry: every agreed working rule names its net, the net's reach, and where it is armed** (row 470, the class finding — partial execution is the reminder-dependent-capability class: where a machine exists and reads its true subject the rule holds, where it is absent, mis-aimed, or uninstalled the rule decays): one standing table (rule → net → reach → armed-where), seeded from the evidence digest's own table, with a check that reds a working rule carrying no net and no stated no-net reason; the rule-side sibling of row 446's per-gate reach line. | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when the registry exists with every profile and base working rule rowed, the check red-proves on a seeded netless rule, and each tlvphotos symptom maps to a registry row |
| 483 | **Pre-existing architecture-doc reader stumbles the row-456 cold read surfaced** (the two-reader cold-read panel at the row-456 conversion, findings pre-dating the conversion): four spots in ARCHITECTURE.md that a first-time reader stops on, none introduced by the format conversion — the Feature-coverage prose calls guardrails an infra node "outside this layer" while the F-prototype coverage row lists it as an implementer inside that layer; two different gates both carry the letter "gate x" (the index-prose gate INV-218 and the generated-index gate INV-258/259); the Decisions section calls the Formal index "retired" while notes and a gate name still read "Formal-index"; and the producer→consumer seam names spec-author on both sides, a self-seam in a table defined as where two nodes meet. Each is a real doc inconsistency the conversion left standing rather than widened. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when each of the four is either resolved in ARCHITECTURE.md or recorded as an agreed non-problem with its reason, and the cold-read panel returns zero blocking findings on the affected sections |
| 498 | **The cold reader gains a craft lens for text that must persuade** (deposit from the promoter window, 2026-07-24): the reading audit today asks one question — does a stranger understand this — and a piece of persuasive prose can be perfectly understandable and still do no work. Five craft failures pass the comprehension read untouched: an abstraction with no concrete image beside it, a claim with no example carrying it, a teaser that argues its point instead of naming what the piece delivers, an empty qualifier standing where a fact belongs, and an opener that hedges instead of landing. The lens is gated by the text's kind, so a specification is read for comprehension alone and a message, a page, or a campaign text is read for both, its craft findings reported as their own severity beside the places a reader stops. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the text-audit skill states the lens, its kind gate, and the five patterns with a worked example each; a persuasive text with all five passes the comprehension read and reds the craft lens; and a specification section reds neither |
| 499 | **The front door states the reach its shipped checks actually have** (deposit from the promoter window, 2026-07-27): the README tells a reader that a check goes red when a shipped behaviour has no requirement behind it, and the four scripts a host installs catch something narrower — a registered surface missing or empty, an uncited anchor, a user-facing change with no test change, and a contradiction between two documents. The scaffold's own README states that boundary honestly, so the front door contradicts the shipped artifact it describes. The fix rewrites the claim to what the scripts do and decides, by name, whether a host leaving the surface-discovery setting empty goes red or stays silent. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the README's claim matches the four scripts' real reach word for word with the scaffold's own statement, the empty-setting case is decided by name, and a reader of both files meets one story · groups as one queue bundle with rows 500, 501 — whether the pack's own front page tells the truth about what its checks actually catch |
| 500 | **A warning nobody counts and a cap that only loosens** (deposit from the tlvphotos window, 2026-07-21, the two findings its size-gate report carried beside the one that landed): the style reading emits hundreds of warnings that no gate reads, so a channel exists whose whole output is advisory and unmeasured; and the debt cap freezes today's number without ever tightening it, so a document that improves keeps its old permission. Both are the same shape as the size ratchet that landed from the same report, and both stand outside it. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the warning channel either gains a counted floor that ratchets or is declared advisory by name with its reason, and the debt cap lowers itself when the measured number falls |
| 505 | **The claim commit is required to carry one row and is only checked to carry one file** (found 2026-07-27, three rows flipped to in-work in a single lane claim and the gate passed it): the lane-open script states the law in its own message — the claim commit carries one row's delta [INV-39] — and the check behind that message reads the staged FILE LIST alone, so staging the queue file with any number of row changes satisfies it. The message a gate prints and the fact it checks are two different things, and a reader of the message believes the stronger one. The build: the check reads the staged diff of the queue file and reds when more than one row line changed, naming the rows it found; where the message and the check disagree, the check moves to the message rather than the message weakening. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a claim staging two changed rows reds with both row numbers named, a single-row claim passes, and the red proof covers both directions |
| 508 | **A check that reads text for a person fires where no person reads** (found 2026-07-27 ~21:29: a helper run doing a file edit received the mid-turn chat check's report on text it never wrote, treated it as something injected from outside, and carried on): the check stands before every tool call and reads the turn's messages. A helper run has its own turn and its own messages, and nobody reads them; its whole output returns to the session that dispatched it. So the check spends its work where its subject is absent, and it hands a helper a report the helper cannot act on. The helper's own reading of that report is the sharper half: it decided the report came from outside its task and ignored it, which is the right call and also the sign that the report had no business arriving. The build: the check reads whether its turn belongs to a session a person is reading, and stands down by name where it does not; the same question binds every check of this family, so the answer belongs beside them rather than inside this one. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a helper run receives no report from the checks that read text for a person, a session a person reads still receives every one, the stand-down says which turn it read, and a red proof covers both directions |
| 512 | **The rotation road reaches one document, and the others have none** (found 2026-07-28 while giving the matrix room for tonight's rows): `scripts/rotate-doc.py` reads the queue's table shape alone — a bare-integer row id and the closed vocabulary landed, decided, declined, superseded — and refuses every other document by name, which is the honest behaviour of a tool that will not guess a shape. A matrix rotation needs the tool to read the `M-NNN` row-id pattern and to treat *retired* as the archivable signal, since *built* and *todo* both describe coverage that is live. The matrix carries no *retired* row today, so this row asks for a road the tool does not build rather than moving a queue of rows that wait. The measurement behind it: 472 body rows, 431 built, 41 awaiting a test, none retired, so the ceiling was raised to 530000 with that count recorded as the reason. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the tool reads the matrix's row shape and its archivable status word, a rotation of a seeded retired row moves it to the archive and leaves every live row standing, the rotation gate reads the matrix as well as the queue, and a red proof covers a rotation that would move a live row · groups as one queue bundle with rows 520, 531, 552, 600, 603, 621 — one fact or count written in two places with no machine keeping the copies in step |
| 513 | **A checker whose reach is a whole file holds one surface's rule over another surface's text** (found 2026-07-28 in the first blind read of the rules reference, and stated inside the rule home already): the rule that a normative sentence writes in the third person governs the numbered requirements and acceptance criteria. The rule that explanatory text addresses the reader as `you` governs the prose a person reads. `scripts/spec-style-lint.py` reads a whole file and holds the first rule over both, so a Context paragraph or a User Story line reds for a word its own surface requires. The resolution the rule home now carries: a surface belongs to a BLOCK, and one file carries several — a spec's numbered criteria are one surface, and the same file's paragraphs are another. The build narrows the checker's reach to the blocks its rule governs, and the reach line says which blocks it read. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the checker reads the numbered blocks for the third-person rule and the prose blocks for the reader-address rule, its reach line names both, a red proof covers a second-person sentence inside a criterion and a third-person sentence inside explanatory prose, and a silent proof covers each rule's own surface |
| 514 | **A rule points at the place that states it by a line number, and an edit moves the line** (found 2026-07-28: the language-defects document shrank from 357 lines to 219 in one repair, and eleven of the rule home's pins overshot the file's end): the gate caught every one, so nothing passed silently, and the cost is that every edit of a pinned document earns a re-pinning pass. A pin that names a heading, a rule id, or a quoted phrase survives an edit that a line number does not. The build: the pin carries a stable handle beside its path, the gate resolves the handle and reports the line it found, and a handle that resolves nowhere reds with the phrase it looked for. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a pin names a stable handle, the gate resolves each handle to a line and reds on a handle it cannot find, a document edit that moves a pinned line leaves the gate green, and the eleven pins repaired by hand tonight carry handles |
| 515 | **The gate over skill reviews reads the wrong record and freshens every one at once** (found 2026-07-28 while writing the records tonight's skill change owes): `guardrails/check-skill-review.sh` reds a push whose diff changes a skill body with no committed review record, which is the law it exists for. Its reading has two faults. It stops at the first committed file under `docs/skill-review/` that carries the marker, a `Verdict:` line and the skill's name, so its green named a record from 2026-07-17 as the match for a change made tonight; a record written for the change at hand is never the one it points at. It measures freshness over the whole review directory, so a commit touching any file there freshens every record for every skill. Together those two make the gate pass on a record that describes another change. The build: the gate matches a record to the change by the commit that carries it, reads freshness per skill against that skill's own last content commit, and names in its green which record it matched and which commit it read. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a seeded record for another skill leaves the changed skill red, a record committed for the change at hand turns it green and is named in the verdict, a commit touching an unrelated record leaves the stale skill red, and the red proof covers both faults |
| 516 | **A page states two clean cold readings as its bar, and nine readings show the bar is not approached** (found 2026-07-28 over one night of readings on `docs/language-defects.md`): the rule holds a changed section back until two consecutive readings return nothing that blocks a reader. Nine readings ran, and the count of blocking stops went 11, 8, 12, 6, 5, 5, 6, 5, 8. Each round repaired every stop the last reading named, and each new reader found a different set. Part of every reading's count sits on text the previous round added, because a repair that answers a question adds words and a fresh reader walks the text by their own path. The page went 219 lines to 353 to 340 across those rounds, and two rounds of the nine came out shorter than they started. What the owner decides is how much reader friction a text may carry when it is shown. Two shapes stand ready: count a stop as blocking when two consecutive readers both stop there, which measures the text rather than one reader's path; and show a text with its remaining stops listed at its head as stated limits, so the residue is visible to the reader who meets it. The rule as written also has no exit for a text that never reaches zero, so a writer holds it forever. | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the bar states what counts as a blocking stop and how many readers must agree; a text that carries remaining stops has a stated way to ship with them named; the rule states its exit; and the nine readings recorded under `docs/language-reads/` are cited as the evidence the new bar was set against |
| 518 | **A second full statement of the writing rules ships inside the communicator skill** (found 2026-07-28 while reviewing the audit skill's own repair): `skills/communicator/references/writing-register.md` is 136 lines carrying 26 rules written out in prose, and the same rules stand in `guardrails/language-rules.json`, which every page and every checker is built from. The rule file lists that reference among the places that stated its rules before it existed, so the duplication is recorded and still shipping: a rule edited in the one file never reaches the communicator's copy, and a host reading that copy is held to whatever it said on the day it was written. The repair is the mechanism the audit skill just took: print the rules into the file from the one home between markers, and let `guardrails/check-language-rules.py` refuse a drift. Its own reason for standing apart wants stating first — the communicator's register may cover surfaces the human-prose roster does not, and any rule it holds that the one file lacks is folded in before the file is replaced. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: every rule that reference states stands in `guardrails/language-rules.json`, the reference prints its rules from there between markers, the gate refuses a hand edit inside that block with a proof recorded both ways, and no file outside the one home states a writing rule in its own words |
| 519 | **A heading in every skill shouts one word** (found 2026-07-28 while lowering the prover skill's count against the rule home): eleven skill bodies open their boundary section with `## When NOT to use`, and the capitals rule reads that word as emphasis rather than as a name the project defined. The rule's own carve-out list takes a defined term, an acronym, a document name, or a closed-vocabulary value, and this word is none of them. The repair is one heading, worded the same in every skill, that names the boundary in ordinary case. It is a pack-wide rename, so it waits for a pass that touches every skill rather than riding one skill's repair. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: no skill body carries a capitalised word for emphasis in a heading, and the census counts no caps finding on that line in any skill |
| 520 | **One ceiling law is written out four times, once per record it governs** (found 2026-07-28 while repairing the duplicate pair the redundancy check raised at the findings-ratchet landing): four requirements state the same rule over four different records. Requirement 245 held the growable-document byte bounds, Requirement 280 the bytes-per-criterion ratchet, Requirement 297 the criterion-readability counts, and Requirement 302 the document findings record. Requirement 245 retired 2026-08-18 (gate z and its four byte ceilings, cut as an unrequested bound); Requirement 297 retired 2026-08-19 (the criterion-readability gate, an invented word-cap ceiling in the same class), leaving two standing: 280, 302, each saying that no run raises its recorded number, that a fall is written back, and that a raise is a hand edit stating its reason. The glossary carries the same law once more, in its recorded-count entry. The wording ran close enough that the redundancy check once read a pair as a duplicate, and the floor was raised by hand to admit it; that pairing retired with Requirement 297. The build: one requirement carries the ceiling law with the record as its parameter, and every other statement points at that home. | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: one requirement states the ceiling law once with the record as its parameter, the other requirement and its glossary entry carry a pointer to that home, and the gate behind that record reads its raise rule from the one home |
| 521 | **A parked item is re-tested before it is shown** (handed in from the tlvphotos window 2026-07-28, off two items on that window's waiting board: one question the session later answered itself, and one the shipped product had already answered): a waiting board is a durable file that outlives the session that wrote it. Every item on it claims that no artifact in the tree answers it, and that claim decays as the product grows. The shipped net asks one question of an item, whether its text names a human-only fact, and that question's answer never changes. An item that passed on the day it was written passes on every run after. The net's reach is `NEXT_STEPS.md` and `docs/decisions/*.md`, so `WAITING.md` and the decision record's open region go unread. Its predicates fire on `reserved for the owner` and on `TBD`, and they pass silently over `waits for your word` and over `waits for his word`. The build: every item re-tests against the tree as it stands before a person sees it, an item some artifact now answers clears with that citation, and an item that names no human-only fact is itself the finding. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when the read-time re-test stands in one home and the board-owning skills cite it, each parked item carries the date of its last re-test, the net reaches `WAITING.md` and the decision record's open region, the second-person and `waits for` forms fire, and a fixture board holding an item the tree already answers reds before the fix and passes after |
| 523 | **Two records one word apart hold different things** (raised by the prover pass on Requirement 303, 2026-07-28): the handoff note of Requirements 25 and 128 records a background worker's id, the files its brief lets it write, and the three liveness checks a resuming session runs. The session handover of Requirement 303 is the file a closing session leaves for the next one. A reader meets both inside one document and tells them apart by one word, and the glossary defines only the second. The repair gives the worker's record a name a reader cannot read as the session's, sweeps every home that states it — the two requirements, the base rulebook's rule 6, the architecture's owns cells, and the matrix rows — and gives the chosen name its own glossary entry. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the worker's record carries a name that reads as its own, that name holds a glossary entry, and no home of either law still states the old one · groups as one queue bundle with rows 524, 528, 587 — what a closing session leaves for the next one, and making sure it is left correctly |
| 524 | **The handover directory grows one committed file per session with no ceiling** (raised by the prover pass on Requirement 303, 2026-07-28): every session close commits a session handover under `docs/handovers/`, and no rule says when one may leave. The resume file is held to its no-redundancy digest law (the 100-line cap struck 2026-08-07, row 576), and each growable working document carries a declared byte ceiling, while this directory stands beside them holding neither. The repair states the directory's retention rule: how long a handover stays live, where a retired one goes, and which check reads the rule. The rotation mechanism the queue and the split documents already run is the road here, so this row builds no machinery of its own. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: `docs/handovers/` carries a stated retention rule with one home, a retired handover sits in an archive that keeps it findable by name, and a check reads the rule |
| 526 | **The findings gate scores a reading that refused as zero and calls it a fall** (found 2026-07-29 while repairing the record check): `guardrails/check-doc-findings-bound.py` reads each document through the census and handles a file it could not open, and a row carrying a refused reading keeps its zero counts. So the gate prints a fall for a document whose check never ran, and it passes. The census write path now refuses that zero, so the hollow floor cannot be stored. The gate still reports the false fall on the push, and an operator reading it lowers a real ceiling on the strength of a reading that never happened. Row 525 owns the reading that produces no count at all. This row owns the gate's own read of a refusal. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the gate names a refused reading, refuses the push on it, and a fixture whose reading refuses reds before the fix and passes after · Stage 3 C2 note 2026-08-12 (`.live-spec/stage3-verdicts-2026-08-12.md`, gate aa): the smallest close treats a refused reading as red instead of scoring it zero, inside the existing script |
| 528 | **A handover can name another lane's transcript and the check never notices** (found 2026-07-29 while repairing the two holes in the extractor): a closing session names its own identity to the extractor, and nothing reads whether it named its own. Gate ab reads a handover's three lines of provenance for their shape alone. So a handover naming another lane's transcript passes green, and that lane's decisions reach the next session as this lane's own. The arm is cheap now: the handover's file name carries the session identity, the transcript path carries the same identity, and comparing the two is one read. This row is the sibling of the finding on the same requirement that the record left open. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the check reds a handover whose named transcript carries an identity other than the handover's own, a criterion states that arm, and a fixture handover naming another lane's transcript reds before the fix and passes after |
| 530 | **A person's own words red the language check that reads for machine dialect** (found 2026-07-29 while writing the night's decisions into the record): the decision record keeps what a person said character for character, because a paraphrase of a decision is a decision changed. One of those quotes carries a pack term he spelled in his own alphabet, and `scripts/preshow-register-lint.py` refuses the file for it. The check reads every line of a document as the project's own voice, and a quoted line belongs to whoever spoke it. The precedent is next door: `scripts/check-shipped-language.py` already declares a region mark and a fenced block for exactly this case, so the repair teaches the register check the same mark rather than inventing a second one. The other road, editing his words, is closed by the law that a quote is kept as it was said. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the register check passes over a marked quoted region, the decision record reads clean, a fixture carrying a leak outside such a region still reds, and the two checks name the same mark · Stage 3 C2 note 2026-08-12 (`.live-spec/stage3-verdicts-2026-08-12.md`, gate i): the smallest close exempts block quotes and fenced quotations from the alphabet test while the personal-name test keeps its full reach |
| 532 | **Gate aa compares the document bytes it already measures** (found 2026-07-30 by an outside audit, `/Users/sashaabramovich/context-slimdown/reports/spec-future-text-coverage.md`): gate aa (`guardrails/check-doc-findings-bound.py`) walks every live document at push and compares its defect count against a recorded ceiling in `guardrails/rule-census.json`. That record already carries a `bytes` field for all 115 documents, and `scripts/rule-census.py` measures it on every run at line 243, but no code reads it back — the gate's own comparison, at lines 196–204, runs over `total` alone. Adding the same ratchet shape over `bytes` — may fall, may hold, never rises without a written reason — reuses the measurement, the record, the wired gate, and the raise-with-a-reason shape the `reason` field already carries. The audit names it the change most worth making: one comparison, no new instrument. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the gate reads the `bytes` field beside `total`, a byte count that rises with no recorded reason reds the push, a fixture proves the red and the silent pass on a held or falling count, and finding 1 of the source audit is closed · Stage 3 C2 note 2026-08-12 (`.live-spec/stage3-verdicts-2026-08-12.md`, gate aa): the smallest close compares the `bytes` field already present in the record alongside `total`, inside the existing script · groups as one queue bundle with rows 533, 534, 543, 550, 551 — a batch of small defects found by one outside review pass in late July |
| 533 | **Text-birth stages get guard coverage** (found 2026-07-30 by the same outside audit, `/Users/sashaabramovich/context-slimdown/reports/spec-future-text-coverage.md`): the audit mapped eighteen stages where new project text is born and found eleven carry no guard at all — among them every report, every audit note, every brief, every handover, every page shown in chat, and every file created outside the tree; the pipeline skill that carries a change from request to commit names no readability check at any step. Three size brakes remain and each still misses the general case (a fourth, gate z's four byte ceilings, retired 2026-08-18: its own flaw named here, sitting far below the docs' limits and staying silent through any plausible rewrite, was the case this row makes for cutting it): the density ratchet on the spec bounds fatness per rule and not total size; the 500-bytes-per-criterion brake sits in no gate chain and no suite check; the findings ratchet at gate aa counts defects and never bytes, so a rewrite that adds a quarter more text while holding the defect count flat still passes clean. Underneath both gaps sits one asymmetry the audit names directly: Requirement 24 taxes every removal with a justification line and asks nothing of an addition, so the cheapest path through the rules is the one that grows the document — any brake built here needs a permitted way down or writers meet a ceiling with no exit. | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: each of the eighteen mapped stages carries a guard or a stated reason it needs none, at least one brake bounds total growth rather than per-rule density, Requirement 24's removal-only tax is answered with a stated cost or exemption for addition, and findings 2, 3, and 7 of the source audit are each closed or answered in their own line |
| 534 | **A dozen one-line source defects ship from the communicator and text-audit skills** (found 2026-07-30 across the communicator and text-audit source pilots, `~/context-slimdown/reports/communicator-audit-sweep.md` and its sibling sweeps `~/context-slimdown/reports/communicator-read-residue.md` and `~/context-slimdown/reports/text-audit-audit-sweep.md`): a batch of one-line source defects, each fixable without a ruling. `guardrails/weak-words.json` holds 28 words where the text-audit skill calls it "the fuller list" — *depends*, *related*, *handles*, *based on*, and *corresponds to* are missing. `CAPS_ALLOW` in `scripts/spec-style-lint.py` blesses five defined labels and misses two more of the same kind, `RETIRED` and `SYNTHETIC`. Row 544 below opens a host-level allowlist extension point for the same set; if that row lands first, its mechanism supersedes hardcoding these two labels straight into `CAPS_ALLOW`, and this row's fix moves them through the extension point instead. Printed commands and paths across the text-audit skill point at the wrong target: the self-check section names `preshow-register-lint.py` and never `rule-census.py`, so a writer who follows it exactly sees clean and still reds at push; the census command is printed with no `FILE` argument, so run as written it measures the whole repository instead of the audited text; the `references/` path is written as relative to the repository root where the files really sit under `skills/text-audit/references/`; the four structure checks print live-spec's own three document names with no note that they apply only where an audited project keeps the same three; and the closing-rule line states one cold-reading cycle where the rule is two consecutive clean reads. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the five words are added to `weak-words.json`, `RETIRED` and `SYNTHETIC` are added to `CAPS_ALLOW`, each printed command and path in `skills/text-audit/SKILL.md` runs as written and reaches its stated target, and every one-line item catalogued in `~/context-slimdown/reports/communicator-audit-sweep.md` and its sibling sweeps under this kind is fixed or named as out of scope |
| 535 | **`rule-census.py` carries two scoring bugs** (found 2026-07-30 during the same pilot, `~/context-slimdown/reports/communicator-audit-sweep.md`): its register column reads 0 for every one of 109 recorded documents, because `preshow-register-lint.py`'s JSON record carries no `errors` field and `run_lint` reads zero even when the lint itself errors — a planted register leak during the base-skill rewrite passed the census and was caught only by running the lint directly, so every census register verdict is vacuous until this is fixed. Separately, the census applies one word-cap to every surface: rule r08 gives spec prose a 35-word sentence cap against ordinary prose's 25, and the census scores a 29-word spec criterion as over-cap while a 31-word sentence elsewhere lands with zero style or register findings — the text-audit skill's own page contradicts the script it ships. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the census reads the lint's real error count for the register column, a fixture register leak reds the census before the fix and passes after, the census applies the spec's 35-word cap to spec surfaces and the 25-word cap elsewhere (or one cap is chosen and stated for all surfaces), and a fixture spec criterion between 26 and 35 words scores clean |
| 538 | **Six language-rule line-pins re-point at the rewritten profile** (found 2026-07-30 by the same dependency sweep, `attic/inbox-2026-07-30-context-audit-from-fable.md` section 7, item 2): `guardrails/language-rules.json` pins line numbers into the personal profile at `~/.claude/playbook/personal/profile.md`, which the same night's rewrite flattened to about 74 lines. `tests/test_language_rules.py::test_gate_stays_silent_on_the_real_file` reds on the stale pins for rows r01, r13, r47, r49, r50, and r51. This row's first act is a freshness re-check: the cited test passes clean at today's HEAD, so the row starts by checking which of the six pins still point at a stale line before re-pinning any. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: rows r01, r13, r47, r49, r50, and r51 are re-pinned to their lines in the rewritten profile, `scripts/gen-language-consumers.py` is re-run and its output committed, and `tests/test_language_rules.py::test_gate_stays_silent_on_the_real_file` passes |
| 539 | **Eighteen source rulings left standing in live-spec-base and build-pipeline** (found 2026-07-30 across three repair passes over the live-spec-base and build-pipeline rewrites, `attic/inbox-2026-07-30-live-spec-base-leftovers.md` items 34–51, continuing the numbering from `attic/inbox-2026-07-30-communicator-source-findings.md`): defects the rewrites could not close without inventing a rule or touching shipped identifiers. Among them: rule 24 sends a reader to two settings, `project.layers` and `project.proofs`, that the Package defaults table carries no rows for; rule 29 breaks the pack's own one-name lint at two lines; `guardrails/check-one-name.py` passes clean over both skill bodies at today's HEAD, after the readability rewrites, so this row's first act is a freshness re-check of which of items 34 through 51 still stand before any further ruling; rule 14 and rule 26 each claim to be the canonical home for a law the other also states, with rule 4's one-home law giving no way to tell which wins; the settings table drops its dotted scope prefix for three rows out of sixteen with no stated reason; six of the file's cited ROADMAP rows have rotated to the archive and the citations still point at the numbers no longer in `ROADMAP.md`; rule 3's one-name-per-thing law is broken by four names for one role — lead, orchestrator, seat, senior — and by "the human" surviving in three passages `test_class_hunt.py` and `test_request_classifier.py` pin literally, so a rename needs the tests re-pinned in the same change; rule 31's "owner" ambiguity (filed as item 32 of the sibling message and carried in row 536 above) had its rewrite's derived sentence removed and stands open; and build-pipeline carries four more of the same kind — an undefined "size" antecedent in the footprint rule, a cadence cited but never defined outside the spec glossary, a follow-on row promised with no row number named, and a spec-backed literal (INV-153's control count of four) sitting unmarked in prose where a near-miss phrasing already reded the suite once. Item 52 of the same message, the sync-skills overwrite hazard, is resolved and opens no row here: the installed and repo text-audit copies were independently verified byte-identical on 2026-08-05 13:20, both carrying mtime Aug 5 12:07. | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: each of items 34 through 51 is closed with a stated ruling or a built fix, a rename touching "the human", "backlog item", or a ROADMAP row citation carries its test re-pin in `test_class_hunt.py` and `test_request_classifier.py` in the same commit, and `guardrails/check-one-name.py` passes clean on both skill bodies |
| 540 | **A cross-document consistency check exists** (found 2026-07-30, item 53 of `attic/inbox-2026-07-30-live-spec-base-leftovers.md`, evidence at `~/context-slimdown/reports/consistency-check-coverage.md`): the text-audit skill's method is a cold-reader pass over one document read straight through, which finds a single-spot readability break but cannot compare two distant passages against each other. An unlisted name drift, a contradiction between two rules, a term redefined partway through a document, and a stale pointer whose target no longer backs its claim have no rule among the pack's 63 and no checking script. The finding's own proof: the rewritten live-spec-base carried two sentences giving opposite instructions on one topic, and a term redefined to mean two different things in two places, and both passed every gate — the breaks surfaced only when an adversarial audit was commissioned by hand. The context-slimdown campaign's own consistency pass — a term-definition registry swept across an edited file, a pointer-target verification, and a contradiction check over edited units — is offered as a starting shape. It is not a rule to adopt as written. | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a new rule class states what a cross-document consistency check covers, a script implements it and is wired into the suite, a red proof reproduces the live-spec-base contradiction and the two-meaning redefinition tonight's audit found by hand, and the rule's reach line states which documents and which rule classes it read |
| 541 | **The pin gate proves a pin against its own line** (found 2026-08-05 by the prover pass recorded in `docs/prover/2026-08-05-architecture-pointer-catchup-recheck.md`): `guardrails/check-pin-drift.sh` accepts a pin when any label word of four letters or more sits within a 51-line window of the pinned line. A pin pointing 70 lines away from its sentence reads clean. The pass found 29 stale pins across three skill files under a green gate. One pin labelled rule 20 landed on the opening line of rule 19. The pins were re-pointed the same day, so this row carries the gate alone. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the gate reds a pin whose target line no longer carries the pin's label, a red-proof fixture drives the gate to a non-zero exit, and the gate's reach line names the files it read |
| 543 | **Two chat-check scripts sit disabled with no decision on their permanent shape** (found 2026-08-05 while repairing gaps the morning inbox harvest left, `attic/inbox-2026-07-30-context-audit-from-fable.md` section 6): `hooks/register-judge-collect.sh` and `hooks/register-judge-report.sh` each carry `exit 0` on line 2 of the installed copy. The third script this row named, `hooks/midturn-chat-scan.py`, carried no such line at all — it was wired and firing the whole time the row said it sat disabled — and it was retired on 2026-08-17 for a different fault, so the row now covers two scripts. A dated comment marks each one disabled on Alexander's word, 2026-07-30, and calls the disable temporary. The same message's section 5 names the open question. One judged verdict per turn end could replace a headless judge session run every turn, alongside a pre-show lint for pages only. That proposal still awaits his word. No row owns making the call or re-arming the scripts. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the two scripts' permanent shape is decided and the decision is recorded, each script either runs again with its `exit 0` line removed or carries a dated tombstone stating it is retired and why, and `scripts/install-pack-hooks.sh` registers whichever survives exactly once |
| 544 | **The style lint's acronym allowlist has no host extension point** (found 2026-08-05 in the promotion campaign window, `attic/inbox-2026-08-05-from-promoter-caps-lint-has-no-host-acronym-list.md`): `CAPS_ALLOW` in `scripts/spec-style-lint.py` is a fixed set written into the script, built from live-spec's own file names, mode names, and status values. A host in another domain has no way to add its own acronyms to it. A hand edit to a vendored copy is overwritten at the next `install-ratchet.sh --force` refresh. The same lint run against one promotion campaign's media pack returned fifteen style errors. Thirteen of them were ordinary domain acronyms — `MIT`, `DJ`, `LUFS`, `BS`, `LFO`, `FFT`, `XML`, and `MIDI` among them — and only two named a real defect. `guardrails/check-weak-words.py` already carries the shape this needs: a JSON file beside the script, `weak-words.json`, plus a `WEAK_WORDS` environment variable naming another path. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: `scripts/spec-style-lint.py` reads a host-level allowlist file when present, using the same JSON-file-plus-environment-variable shape as `check-weak-words.py`, a fixture proves a host word clears the caps-shout rule once listed there, and a fixture unlisted word still reds |
| 545 | **The 10 July hole is still open, and the shipped install section walks an adopter straight into it** (found 2026-08-05 by the promotion campaign window replaying the repository's own configured discovery pattern against its own artifacts, `attic/inbox-2026-08-05-from-promoter-the-10-july-hole-is-still-open.md`; the same window's cold-reader install walkthrough is `attic/inbox-2026-08-05-from-promoter-shipped-install-leaves-the-10-july-hole-open.md`): `guardrails.config.json` sets `surface_discovery_pattern` to `<section id=\"([^\"]+)\"`, but the rendered artifacts it scans, `README.md` and `OVERVIEW.md`, are Markdown and carry no `<section` tag at all — the pattern matches zero ids in either file today, so the `if discovery:` branch runs, finds nothing, and passes on any content, exactly as it did on 10 July when the pattern was `null`. The fix the original probe record prescribed named the pattern only as an example; the example was taken as the value, which moved the failure one layer deeper and dressed it as closed. `scaffold/guardrails/check_completeness.py` still ends on an unconditional pass line asserting that rendered content exhibits nothing unregistered, whether or not the discovery branch ran or matched anything — the same false assurance the 10 July record named, never repaired. The shipped install section compounds it: it names five config keys to fill in and omits four more that decide what the checks can see — `user_facing_globs`, `render_command`, `rendered_artifacts`, and `surface_discovery_pattern` — so an adopter who follows the page exactly reproduces this same hole. Two more of the same class: the seeded `user_facing_globs` default (`src/**/*.py`) reds any project without a `src/` directory, and `adopt/ADOPT.md` sends an adopter to create `SURFACE_REGISTRY.md` while the seeded config reads `SURFACES.md`, so the two documents disagree and the first push reds on a dead path. The install section also omits the base-ref precondition (a non-`main` default branch, or a repo with no remote, reds its first push) and states rule-body line counts that were already stale within the day they were measured. | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the discovery pattern (or the artifacts it scans) is changed so a planted fake surface in a rendered artifact reds the check, `check_completeness.py`'s passing message states only what it actually verified, a discovery pattern that matches zero ids across every artifact reds as a configuration finding, the install section names all nine config keys including the four load-bearing ones, `ADOPT.md` and the seeded config agree on the registry file's name, the base-ref precondition is documented, and the stale line-count claim is replaced with the two commands to run |
| 546 | **The rotation gate's two default reaches disagree, so a correct tree reds under its own default invocation** (found 2026-08-05 by the tlvphotos window, running `guardrails/check-doc-rotation.py` with no arguments on a tree where nothing was wrong, `attic/inbox-2026-08-05-from-tlvphotos-gate-default-reach-reds-a-correct-tree.md`): the gate's documents scanned for manifest lines default to `["ROADMAP.md"]` alone, while its archives scanned for orphans default to the wider glob `docs/queue-archive/rotated-*.md`. An archive belonging to any document outside that one-item default — the reproduction used `NEXT_STEPS.md`'s rotated archive — is reported as orphaned even though its manifest line exists and is correct, because the run never looked at the document that owns it. Run the way the push gate actually runs it, `--doc NEXT_STEPS.md --doc JOURNAL.md`, the same tree passes clean. The class beneath the instance: a gate scanning one set for claims and a wider set for evidence, with the two defaults chosen independently, produces a verdict that is only meaningful when the two reaches match, and nothing in the gate checks that they do — the finding is filed apart from row 531's manifest-shape gap and stands whichever manifest form that row lands. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the gate states its own reach before it runs, a run whose archive glob can surface archives belonging to a document outside `--doc` either widens the document set to match or fails naming the mismatch rather than naming the tree broken, and a fixture with an out-of-reach document proves the red before the fix and the honest failure or the correct pass after |
| 547 | **The restore check reaches the commands a main thread runs** (found 2026-08-05 by the adversarial review of the day's push): `guardrails/check-worker-restore.py` reads the transcripts of spawned workers alone. A command run by the orchestrating session itself reaches the same tree and passes unread. The day's own case: a worker's `git stash` swept three files outside its brief, and the reading that followed found nothing, because the wipe reached the tree through a path the check never opens. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the check either reads the orchestrating session's own commands or its reach line names that ground as out of scope, a red proof drives the case, and the reach line states which transcripts it read |
| 548 | **The starter guardrails a new project copies are measured too** (found 2026-08-06 by the adversarial review of the budget-rule push): the census returned the twelve template files on the ground that `README.md` sends a person starting a project to them. That same page sends the same reader to `scaffold/guardrails/README.md`, which the census still passes over as a record directory. It carries nine findings nobody measures. It is the one reader entry point named in `README.md` that no check reads. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: every reader entry point `README.md` names is measured or its exclusion states why, `scaffold/guardrails/README.md` carries a record entry, and the census run reports the new file count |
| 551 | **A transcript drops a short paste, and rule 35's opening half runs on no trigger** (found 2026-08-06 by the tlvphotos window, `attic/inbox-2026-08-06-from-tlvphotos-transcript-loses-pasted-text.md`): the owner pasted five lines, and the transcript kept only the placeholder `[Pasted text #1 +5 lines]`. The turn's `message.content` carries no body and no `pastedContents` field, and `~/.claude/paste-cache/` held nothing from that day. `scripts/session-extract.py` can only be as complete as the transcript it reads, so a pasted turn becomes a silent hole — the same source row 550 points a decision's quotation at. Rule 35's opening half (SPEC INV-302: read the prior extract, list the owner's decisions, check them against `DECISIONS.md` and `NEXT_STEPS.md`) carries no gate; row 522 recorded it as a discipline the seat holds alone, since an opening writes no committed artifact. This session skipped the extract and planned work for eight darkroom items `NEXT_STEPS.md` called unbuilt, and five of the eight were already built on a parked branch from the prior session. The message names the `UserPromptSubmit` hook, which already fires the six session laws, as one place a first-prompt trigger could live. | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a placeholder paste with no matching paste-cache body is marked as a hole in the extract, never passed over in silence; a fixture with an unrecoverable placeholder and no matching paste-cache file drives the mark; rule 35's opening half runs from a real trigger, so a seat's own memory stops being the only thing carrying it; and a fixture opening a fresh session against a prior extract with an undischarged decision proves the comparison ran |
| 553 | **The suite-in-suite test proves the runner without re-running everything** (found 2026-08-06 when the suite passed its stated wall-time): one test copies the tree and runs the whole suite inside itself to prove `guardrails/check-tests.sh` reports green and red correctly. It costs 282 seconds of the suite's 456. It fires only when a diff touches gate machinery, so an ordinary day runs near 174 seconds and a day that edits a gate pays the whole price. The proof it owes is that the runner reports correctly, which a narrow selection of real tests can carry. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the meta-test proves the same two verdicts against a real but narrow selection, the suite's measured wall-time with the meta-test firing sits under 300 seconds, the budget row in `ARCHITECTURE.md` moves down to the fresh figure, and a red proof still drives the failing branch · Stage 3 C2 note 2026-08-12 (`.live-spec/stage3-verdicts-2026-08-12.md`, gate b): the smallest close runs this meta-test in the CI mirror alone (`.github/workflows/gates.yml` already re-runs the suite) and drops it from pre-push, taking 282 of the suite's 456 seconds off every push without opening the pre-push gap; the seat declined to remove gate b outright on this same page (see DECISIONS.md) · **Re-measured 2026-08-12 15:58**: `tests/test_guardrails.py` now costs **640.59 s** of the full run's 1,159.75 s, measured by `/usr/bin/time -p python3 -m pytest tests/test_guardrails.py -q` on a quiet machine. The 282-seconds-of-456 figure above is from 2026-08-06 and is stale by more than a factor of two; this file alone is 55% of every full run, and it is why the wall-time budget row was re-set from 800 s to 1,280 s on the same day. That budget rose again to 1,410 s at 20:08 the same evening, on load spread alone: the same tree measured 1,159.75 s and 1,281.39 s inside the same gate set. A ceiling raised by machine noise is repaid by this row alone. The row's own acceptance figure of 300 seconds is now the deepest cut in the queue · groups as one queue bundle with rows 554, 622, 491 — the test suite's time budget answers to machine load, a separate cost from the code itself, plus a request for suite-organizing guidance |
| 559 | **The reading panel is held by a machine** (found 2026-08-06 by the reading-bar design, `scratchpad/design-D-reading-bar.md`): the spec carries an open hole naming no panel size and no supplier for the cold readers, and the last round ran two readers both loaded with the project's rules, so the round measured what those rules already say. Thirty-eight readings across three documents have never produced a single reader pass at zero. The repair states what counts as a stop that blocks — the reader shows the act they attempted and could not carry out — and puts a gate over the reading record that reds a round whose two readers came from the same brief. Door: feature · kind: infra · footprint: `guardrails/` + `docs/language-reads/` + `PRODUCT_SPEC.md` + the audit skill · map: guardrails node | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the spec's open hole is closed with the panel size and the supplier of each reader stated, a stop counts as blocking only where the record carries one of the four named acts, the gate reds a round record whose two readers cite one brief and names both, a stated test decides a blocking stop from a suggestion, and a red-first proof drives each arm. Non-goal: changing the standard itself, which stays two consecutive rounds at zero blocking findings from both readers |
| 560 | **The template files a new project copies are named for what they are** (found 2026-08-06 by the founding walk, which had to sidestep the drift; the rename was offered to Alexander twice and never answered): the surface registry a new project copies carries two names in the shipped tree — `adopt/ADOPT.md` and `docs/adoption.md` call it `SURFACE_REGISTRY.md`, while `scaffold/guardrails/guardrails.config.example.json` seeds `SURFACES.md` and this repository's own root and config carry `SURFACES.md`. One artifact under two names is a defect the format family already names. The founding walk sidesteps it by taking the config's value, so nothing blocks on the answer. This row is parked on Alexander's word because the choice of name is his to make. Door: refactor · kind: infra · footprint: `adopt/` + `docs/` + `scaffold/` + the root registry · map: attach node | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: one name stands for the surface registry across the shipped procedure, the adoption guide, the scaffold config seed and this repository's own root, every consumer moves in the same delivery, and the one-name check carries the retired name as an alias so a stale mention reds. Non-goal: renaming any other template in the same pass |
| 561 | **The settings ladder names where the pack's own tree lives** (found 2026-08-06 by the setup routing card, which needs the value and has nowhere to record it): the card offers to write the resolved pack path onto the person's own profile as a `pack.tree` line, and the settings ladder in `live-spec-base` carries no such row, so nothing reads it. A later run therefore resolves the tree from scratch every time. Door: feature · kind: skill · footprint: `skills/live-spec-base/SKILL.md` + the routing card · map: base-rulebook node | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the settings ladder carries a `pack.tree` row with its default and the rung that may set it, the routing card reads that line before its ordered reads and says so, a pack that does not carry the row says so aloud when a person sets the line, and a test reads the row off the shipped ladder. Non-goal: making the pack tree a required setting, since an unset value still resolves through the card's six reads |
| 562 | **The spoken-entry phrases get their real scoring run** (push review 2026-08-06, blocking finding 3): `evals/build-pipeline.md` Scenario C names ten phrases and its method — one fresh session per phrase, the phrase as the whole first message, score which skills load. No run exists, every cell reads to be scored, and matrix row M-518 now says the loading is unscored. A simulated matcher would measure a different thing, so the run needs real sessions with the real loading mechanics. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the ten cells carry scored verdicts from real sessions, the run's date and method stand in the eval file, and M-518's parenthetical points at the recorded run · groups as one queue bundle with rows 563, 564, 565 — four small loose ends from one push review |
| 563 | **A published count outside the declared registry is invisible to the counts gate** (push review 2026-08-06, finding 4): the spec states the law over every published count of this tree, and the gate can only read `guardrails/tree-counts.json`. Counts shipped this day outside it: the fourteen-template count in `docs/adoption.md`, the eleven-row copy table count in `adopt/START.md`, and the description-length ladder in the eval and the two review records. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: either each such count joins the registry with its measurement, or the spec narrows the law to registry-declared counts and names the boundary, and a sweep lists the counts left outside with the reason each stays |
| 564 | **The founding-walk tests re-implement the walk's readers** (push review 2026-08-06, finding 5): tests/test_setup_entry.py carries its own found() and resolve_pack_tree() beside the shipped routing card, and the six-reads test asserts six numbered items plus two strings, so a reorder of the card leaves both green. The proof should read the shipped card as the driver. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the test derives the walk's steps from the shipped card text, a reorder of the card's reads turns a test red, and the duplicated readers are gone from the test file |
| 578 | **Suite temp artifacts that carry no suite prefix escape every sweep** (adversarial review 2026-08-07, recommendation R5 on the temp-folder leak's wider class). Several tests create default-named temp files beside the proven kill window, and both the kill-window sweep and the session leak check filter on the suite's prefix list, so such an orphan would be silent. One bounded kill call also runs with no sweep pair. Force suite temp artifacts onto the prefix list or the test framework's own temp path, and give the unswept kill call its sweep. | bug | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a hygiene check reds a suite temp artifact outside the prefix list; the named call carries its sweep; the review's cited files are converted |
| 579 | **The landing-refresh heal road gets a counter** (adversarial review 2026-08-07, recommendation R11). Nothing bounds heals per push range, so the strict same-commit law could quietly become a same-push law. The checker counts heals per range, and a range where every landing healed and none refreshed same-commit reds, or the count lands in the push report. | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the counter exists with its red or report road; a fixture proves the all-healed range is caught |
| 580 | **The skill-review gate matches stale records by word alone** (found 2026-08-07 10:52 by the delta reviewer). The gate accepts any committed record file that contains the skill's name as a word and postdates the skill's last commit, so four changed skills passed on old, unrelated records covering none of the day's deltas. A review record must name the change it covers, and the gate must match the record to the delta it reviews. | bug | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a fixture proves an unrelated fresh record no longer clears a changed skill; the day's real records are what the gate names; the gate's matching rule is stated in its header |
| 585 | **The earned-message gate refuses the form its own home teaches** (found 2026-08-07 at this inbox sweep). The gate reads past every fenced block before it reads any field, and `inbox/README.md` prints the birth block inside a fence. A sender copying that template verbatim writes a fenced `Lived:` block, and the gate then reports a message naming neither birth. The system-dialogs deposit of 2026-08-07 did exactly that. Two more deposits the same day carried his own relayed words with no birth field, and the reserved owner door reads the filename alone, so a wish of his relayed by a window is read as an agent's unearned message. Four real deposits have now been taken in on their substance over a gate that reported each one unearned, the first on 2026-07-28. Intake 2026-08-07 — door: bug · kind: infra · footprint: `guardrails/check-earned-message.py` and `inbox/README.md` · map: the intake door | bug | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a deposit written to the home's printed template passes; a relayed owner wish has a door the gate reads; a fixture proves each red before the fix and the pass after; the home and the gate print one form · Stage 3 C2 note 2026-08-12 (`.live-spec/stage3-verdicts-2026-08-12.md`, gate n): the smallest close reads the birth fields before stripping fences, or strips only fences that carry no birth field; the same page recommends retiring the gate instead and sends that question to Alexander's desk (see DECISIONS.md), so this repair waits on his ruling |
| 587 | **Sessions close without writing a handover and nothing notices** (found 2026-08-09 by the adversarial review of the day 2 cuts, `docs/prover/2026-08-09-culling-day2-cuts.md` finding 10). Row 558 carried this observation and was declined on 2026-08-09, because day 2 withdrew the gate it asked to extend. The observation itself stands. Three sessions closed with no handover written, and the newest file under `docs/handovers/` is dated 2026-07-29. Base rule 35 still asks a fresh agent to write the handover at every close. A decline that answers the mechanism leaves the failure with no owner. Intake 2026-08-09 — door: bug · kind: infra · footprint: base rule 35 and the session-record step · map: beside the withdrawn gate | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the missing handovers since 2026-07-29 are accounted for, and either the close step earns an owner that notices a session closing with none, or the queue records that the failure is accepted and held by attention alone |
| 594 | **Every stage-2 batch edits a skill body, and the push gate then demands a fresh skill-creator review record** (found 2026-08-12 by the batch-1 push review, `docs/prover/2026-08-12-batch1-range.md` B2; the same gate fired on the D2 push hours earlier). The plan's S1–S5 recipe names no skill-creator review, so the escort is re-discovered at every push. Intake 2026-08-12 — door: bug · kind: method · footprint: the stage-2 recipe, `guardrails/check-skill-review.sh` | bug | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | One-line answer: the batch closing (S5) gains a standing line — run the skill-creator review and commit its record before the push — recorded here so the recipe edit itself waits his «принято» on the plan text · groups as one queue bundle with rows 601, 606, 617 — gaps in the campaign recipe used to trim the pack's own text |
| 599 | **The pin guard passes a stale pin on any four-letter word inside its window** (found 2026-08-12 by the push review of `e8900d9..493f094`, finding 6). `guardrails/check-pin-drift.sh` proves a line pin by looking for one naming word from the pin's label within five lines of the pinned line, and it bars a furniture list of twenty words such as `rule`, `step`, `gate` and `table`. Seven pins into `skills/live-spec-base/SKILL.md` stood five lines short and every one reported green, held by `this`, `when`, `work`, `live`, `session`, `SPEC` and `defaults`. The guard's own header already names this failure mode from the 2026-08-05 pass that found 29 stale pins standing green on furniture, so the list is the known-weak part and it was never widened. The seven pins are repaired in `46de0a6` and `2d14354`; the guard that let them stand is this row. Intake 2026-08-12 — door: bug · kind: infra · footprint: `guardrails/check-pin-drift.sh` · map: beside the pin guard's own furniture list | bug | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a pin whose label's only in-window word is a common word reds, proven on a fixture carrying each of the seven words above, and the whole tree's pins re-verified under the tightened rule |
| 600 | **Every shortening batch changes a skill body, and the published line counts are rebuilt by nobody** (found 2026-08-12 by the push review of `e8900d9..493f094`, finding 2; the same gate reddened on the two ranges before it). `README.md` publishes the line counts under `skills/`, and gate ad (`guardrails/check-tree-counts.py`) refuses a push where the published figure and the tree disagree. A stage-2 batch changes a skill body by construction, so the figure drifts on every batch, and the repair — `python3 scripts/gen-tree-counts.py` — has no home in the S1–S5 recipe. Three ranges running, the same gate reddened on the same file, and each was repaired by hand at the push. Intake 2026-08-12 — door: small · kind: infra · footprint: `.live-spec/culling-plan-v3-2026-08-10.md` step S5 · map: beside S5's closing measures | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: S5's closing measures name the tree-counts rebuild alongside the volume measure and the freeze re-baseline, and the batch after it closes with gate ad green without a hand repair |
| 601 | **A batch's reached size is held by no machine, and the next batch may flip what the last one locked** (found 2026-08-12 by the push review of `e8900d9..493f094`, findings 3 and 11). Batch 1 locked rule 7: `tests/test_compaction_discipline.py::test_rule7_batch1_locked_its_level` holds the body under 5,477 bytes and asserts a restored sentence. Batch 2 shipped no lock at all for rule 32, which fell 2,205 to 1,449 and may grow back with every gate green. And batch 2 inverted one of batch 1's own assertions four hours after it was written, turning an `assertNotIn` into an `assertIn` — lawful, since queue row 595 asked for the sentence back, and it shows the lock is no ratchet. Rule 7's byte bound is also set at its pre-rewrite size, so it leaves headroom under a bound the rewrite already beat. Intake 2026-08-12 — door: small · kind: infra · footprint: `tests/test_compaction_discipline.py`, `.live-spec/culling-plan-v3-2026-08-10.md` step S5 · map: beside batch 1's own lock | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: S5 states what a lock is — the reached byte figure and the requirement sentences the rewrite carried — every closed batch carries one, and a later batch that changes a locked assertion says so in its own verdict line |
| 603 | **The rulebook's rule count is asserted by two hand-written literals and derived by nothing** (found 2026-08-12 by the push review of `e8900d9..493f094`, finding 9). Row 593 closed on the premise that the count is derived and asserted. It is neither. `skills/live-spec-base/SKILL.md` line 4 carries the words thirty-four in its description string, and the only two assertions on it are hard-coded literals riding unrelated tests, `tests/test_clean_context_review.py:70` and `tests/test_resume_rederive.py:56`. Neither counts the numbered heads. The tree carries 34 of them today, so the number is right by coincidence. Row 593's gain is real — three copies became pointers — and the next cut still costs three hand edits against four. Intake 2026-08-12 — door: bug · kind: small · footprint: `tests/`, `skills/live-spec-base/SKILL.md` frontmatter · map: beside row 593 | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: one test counts the numbered heads in the body, allows the retired numbers as holes, and reds when the frontmatter word disagrees; the two literals then read that test's own source |
| 604 | **The worker-restore gate can print the wrong outcome for a finding, though never the wrong verdict** (found 2026-08-12 by the push review of `e8900d9..493f094`, finding 13). `guardrails/check-worker-restore.py` reads `toolDenialKind` off the transcript record while mapping that one value onto every `tool_result` block inside it. Where one record carries several tool results and only one was refused, every finding in that record reads as declined. The exit code is unharmed, because all three outcomes red. What is harmed is the typed line a machine reads and the ranking that puts executed findings first, so a command that ran can print last under a sentence saying nothing was discarded — the error leans toward under-alarm. Beside it, `_normalize_stamp` drops a trailing offset, so a `+03:00` stamp would compare against a `Z` counting start three hours out; today's transcript stamps are all `Z`, so that half is latent. Intake 2026-08-12 — door: bug · kind: infra · footprint: `guardrails/check-worker-restore.py` · map: beside row 598 | bug | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the outcome is read per tool-result block, a fixture record carrying one refused and one executed command prints one of each, and the offset case is either handled or refused by name |
| 606 | **A field no code writes and no code reads still publishes a stale figure** (found 2026-08-12 by the push review of `e8900d9..493f094`, finding 8). `4dfdc66` removed the run-to-run comparison from `scripts/progress-report.py`, and the removal is clean everywhere except one place: `guardrails/progress-baseline.json:34` still carries a `last_generation` block reading a date, a total of 4,866 findings and 22 documents at zero. Nothing writes it and nothing reads it, so it will sit there reading like live state to anyone who opens the file. Beside it, `docs/PROGRESS.md` was regenerated at 08:39, before the rulebook edits of 09:15 and 09:32, so its own published figures are pre-batch-2. Intake 2026-08-12 — door: small · kind: infra · footprint: `guardrails/progress-baseline.json`, `docs/PROGRESS.md` · map: beside the progress page's generator | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the dead block is gone from the baseline file, and the progress page is regenerated once the prose it describes has landed |
| 607 | **The skill-review gate matches a record by any mention of the skill's name anywhere in it** (found 2026-08-12 at the push gate, this range). `guardrails/check-skill-review.sh` walks the committed records for one carrying the `SKILL-REVIEW` marker, a `Verdict:` line, and the skill's name as a whole word — `grep -qw "$name"` over the whole body. A name mentioned once in passing satisfies it. This push proved it live: the gate reported `docs/skill-review/2026-08-12-build-pipeline-2.md` as the fresh record for all three of `build-pipeline`, `communicator` and `live-spec-base`, because that record mentions communicator once while quoting a sentence and live-spec-base once while naming a file path, and it sorts first in `git ls-files` order. The gate's freshness half is careful — each candidate is judged on its own commit, after the 2026-08-09 stale-match finding — and its identity half is a bare word search. One record can therefore green-light every skill it happens to name, which is the whole point of the gate defeated. The push it passed is sound, because all three records exist and each was written for its own skill. Intake 2026-08-12 — door: bug · kind: infra · footprint: `guardrails/check-skill-review.sh` · map: beside the freshness walk it already holds | bug | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a record is matched to a skill by a declared field it carries — the `Skill:` line the records already write — rather than by a word anywhere in the prose, a fixture where one record names a second skill in passing reds for that second skill, and the pack's own records are re-verified under the tightened rule |
| 613 | **Skill bodies send a host project to two files the install never gives it** (found 2026-08-12 by the tlvphotos window, `inbox/2026-08-12-preshow-lint-script-missing.md`). `skills/text-audit/SKILL.md`, `skills/communicator/SKILL.md` and `skills/text-audit/references/rewrite-meaning-check.md` tell a reader to run `python3 scripts/preshow-register-lint.py <path>` from the project root before showing an artifact; `skills/spec-author/SKILL.md` tells a reader to check a candidate word against `guardrails/spec-coinages.json` before writing it. Both files live in this repository alone. `install.sh` copies the skill directory into `~/.claude/skills` and nothing else, so a host project receives the instruction and neither file. The tlvphotos window met this at 18:20 preparing a page for its owner: `git log --all --diff-filter=A` there returned nothing for either path, and the check was done by hand instead. A check that cannot run is a gate that never reds, so the instruction either stops a showing forever or is walked past in silence. Intake 2026-08-12 — door: bug · kind: infra · footprint: `install.sh`, `scripts/preshow-register-lint.py`, `guardrails/spec-coinages.json`, the four bodies above · map: beside the install walk | bug | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a host project installed by `install.sh` alone can run every check its installed bodies name, or those bodies state what a reader does when the tool is absent and stop naming a path the install never delivers; whichever shape lands, one host tree proves it by running the named command from its own root |
| 614 | **The author's own skill states no enumerate-or-ride duty** (found 2026-08-12 by the adversarial review of the prover change, which left no file of its own; the record that describes it is `docs/skill-review/2026-08-12-product-prover-2.md`, finding 6). Row 609 places INV-226 with the spec-author, and `skills/spec-author/SKILL.md` carries no paragraph asking whether a member set is closed. The strings `enumerate-or-ride`, `member set` and `open-ended` return no hit in that body. On this pack's own spec the law still has a net, because `tests/test_instance_enumeration_keying.py` holds that the three named laws cite the class. A project adopting the pack receives the skill and none of that net. The prover runs no sweep for this law because the author owns it. The author's skill never asks the question. Intake 2026-08-12 — door: feature · kind: skill · footprint: `skills/spec-author/SKILL.md` · map: beside the law-writing guidance | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the spec-author skill asks whether a general law's member set is closed and names both answers. A fixture law that keys the choice by feel reds against that check. |
| 615 | **The class line has no reach outside its own test module** (found 2026-08-12 by the adversarial review of the prover change, which left no file of its own; the record that describes it is `docs/skill-review/2026-08-12-product-prover-2.md`, finding 5). The prover now owes one class line in every persisted record. `tests/test_class_hunt.py` holds that duty against fixtures it builds in memory. No gate and no script reads a real record for the line. The records already on disk under `docs/prover/` predate the rule, so a gate over all of them would red the whole history at once. A duty whose only witness is a fixture is a rule the tree never checks. Intake 2026-08-12 — door: feature · kind: infra · footprint: `guardrails/check-prover-record.sh`, `tests/test_class_hunt.py` · map: beside the prover-record gate | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a gate reads the class line in every prover record written after a stated date and reds one that carries none. A record written before that date passes untouched. |
| 616 | **A status word thirteen live rows carry stands outside this queue's own vocabulary** (found 2026-08-12 by the adversarial review of the prover change, which left no file of its own; the record that describes it is `docs/skill-review/2026-08-12-product-prover-2.md`, finding 7). This file's preamble names the live status vocabulary: queued, ready, in-work, deferred, far. Thirteen body rows carry `done` in the status cell, three of them written by this change. Nine more carry `landed`, and `docs/roadmap-format.md` declares that word by name: "The word *landed* therefore names a transition a row makes, and a body row never carries it as a status." The same document promises a lint that reds "a row whose status is outside the closed set or missing its date". So nine body rows break a declared law under a lint that stays quiet, and the closed vocabulary the format family promises is open in fact. Two answers: the vocabulary gains the two closing words with their meanings, or the rows lower to a word already declared. Intake 2026-08-12 — door: bug · kind: infra · footprint: `ROADMAP.md`, `docs/roadmap-format.md` · map: the status vocabulary in the preamble | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: every status cell in the live body reads a word both the preamble and `docs/roadmap-format.md` declare. A fixture row carrying an undeclared status word reds. |
| 620 | **Every pack skill pins the base skill's version in prose, and two skills forbid exactly that** (found 2026-08-13 by Anthropic's skill-creator review of the prover, `docs/skill-review/2026-08-12-product-prover-2.md`). Ten of the eleven skill bodies open on the token `` `live-spec-base` (v4.3.0) ``, and the eleventh is `live-spec-base` itself, whose frontmatter is that version's one home. `skills/spec-author/SKILL.md` line 718 lists "pinning a drifting version number in prose" among the shapes to catch, saying "current version: vX.Y" in a header always goes stale. `skills/build-pipeline/SKILL.md` lines 477-478 says no doc pins a drifting version number in prose, and it sends a reader to the version's one home. The copies hold today by machine: `scripts/stamp-versions.py` rewrites all ten at every bump, and `tests/test_traceability.py::test_skills_inherit_base_pin` reds a body whose pin is stale. The stated law and the shipped practice still read opposite, and a reader of either skill learns a rule the pack breaks ten times over. Intake 2026-08-13 — door: bug · kind: skill · footprint: the eleven `skills/*/SKILL.md` openings, `scripts/stamp-versions.py`, `tests/test_traceability.py` · map: the base-pin line each skill opens on | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: the pack's own bodies and its stated rule agree, either by each opening pointing at the version's one home with no number in the prose, or by the rule naming the stamped pin as its one declared exception beside the machine that holds it. A planted stale pin still reds |
| 621 | **A generated table has two homes and no machine carries an edit from one to the other** (found 2026-08-13 by the pass's fourth gate-chain refusal). The reference table of codes and their locations lives twice: as the committed file `PRODUCT_SPEC.index.md`, and as a copy embedded in `PRODUCT_SPEC.md` under its Reference heading. `scripts/build-index.py` refuses to write into its own input by design, so it writes the file and leaves the embedded copy to a hand splice. `tests/test_formal_index.py` reds once the two disagree, which caught this pass at 01:45 after criterion R67.4 landed; the same class costs a full gate-chain run each time, and this pass paid it twice with the published tree counts. The same shape sits under queue row 600, where a batch's recipe carries no rebuild step. Intake 2026-08-13 — door: bug · kind: infra · footprint: `scripts/build-index.py`, `PRODUCT_SPEC.md`, `PRODUCT_SPEC.index.md`, the batch recipe · map: beside the generated-artifact rebuilds | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: a session that changes a criterion runs one command that leaves every generated copy in step, and the recipe names that command where the change happens. The two copies drifting still reds, and the run that repairs them takes no second gate chain |
| 622 | **The suite budget measures the machine's load, and it refused three pushes on that reading** (found 2026-08-13 by the pass's sixth gate-chain refusal). `guardrails/check-suite-budget.sh` reads the wall-time on the pytest tail line and compares it against the figure in `ARCHITECTURE.md`. Across the 2026-08-12 evening pass the same suite measured 1,221.81 s, 1,281.39 s, 1,304.65 s, 1,605.37 s, 1,559.15 s, 1,387.88 s and 1,451.77 s, a 31% swing on trees whose code differs by under a second of test time. Three of those runs refused a push, and each refusal cost a further 25-minute chain. The ceiling has risen 800 to 1280 to 1410 to 1780 inside one day, every step on load rather than on code, which is a ceiling teaching a person to ignore it. Two shapes for the answer: read processor time in place of wall-clock, which a neighbouring process cannot inflate, or measure the budget only on a run the session declares quiet and let a loaded run report its figure without refusing. Intake 2026-08-13 — door: bug · kind: infra · footprint: `guardrails/check-suite-budget.sh`, the wall-time row in `ARCHITECTURE.md` · map: beside the suite-budget gate | small | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: two runs of one unchanged tree, one on a quiet machine and one under load, give the gate the same verdict, and the ceiling in the row falls only when the suite itself gets faster. A planted slow run still reds |
| 750 | **TEST_MATRIX.md is one 477-kilobyte file, and every read or edit of it pays that whole size** (found 2026-08-19 by the cut-matrix investigation recorded in `docs/prover/2026-08-19-cut-matrix-stop.md`, checked against PRODUCT_SPEC.md's own proven split). The file carries no declared ceiling to justify it by — gate z and `guardrails/doc-bounds.json` were removed 2026-08-18 as "every invented ceiling goes" (commit `2e2f167c`), so this wish owes no urgency and no gate forces it; it is asked for readability and edit cost alone, the same reason the spec was split, and a session picking this up should not invent itself a deadline the tree no longer states. PRODUCT_SPEC.md was already cut this way: a 50-kilobyte core plus thirty `spec/*.md` parts, joined back by `guardrails/specformat.py`'s `spec_paths`/`read_document`, one committed `PRODUCT_SPEC.index.md` standing in for the code-to-location table the core no longer embeds, and one reader node in `tests/conftest.py` so every caller still sees one document whether it is one file or thirty. The investigation checked four claimed risks against the matrix rather than trusting them, and found three real in some degree and one flatly wrong: (1) *true* — `scripts/build-matrix-reference.py` and `guardrails/check-matrix-reference.py` each read exactly one file; the spec's sibling scripts already read a core-plus-parts list through `specformat.spec_paths`/`read_document`, so the plumbing exists already and the matrix needs wiring rather than invention. (2) *overstated* — of 62 test files naming TEST_MATRIX.md, 44 already route through `tests/conftest.py`'s shared `read()`/`read_flat()`; only nine still open the file by path directly and need converting: `test_class_hunt.py`, `test_config_health.py`, `test_cross_surface_policy.py`, `test_gesture_overlay_parity.py`, `test_founding_set_version.py`, `test_impact_analysis_entry.py`, `test_paired_transition.py`, `test_prose_gate.py`, `test_scenario_entry_exit.py`. (3) *false as a blocker* — the INV-xxx anchors a matrix row trails are plain text read per row wherever that row physically sits; a file split does not touch them. (4) *false* — no byte ceiling governs either TEST_MATRIX.md or ARCHITECTURE.md today, so there is no size-based reason to prefer one over the other. *The block risk that turned the stop*: the matrix's `## Reference` is documented (`docs/test-matrix-format.md`) as *embedded* in the body, and `specformat.read_document` concatenates a named core's own full text before any of its parts — so a Reference left embedded in a shrunk core would sit ahead of every part, and the twenty-three node blocks appended after it would land after the Reference too, breaking the parser that expects the Reference last. The spec hit this identical shape and solved it by moving its Reference out to its own committed file (`PRODUCT_SPEC.index.md`) and re-synthesizing it at read time, appended after the whole concatenation (`tests/conftest.py`'s `_with_reference_tail`); the matrix needs that same move, and making it is a change to `docs/test-matrix-format.md`'s own documented shape, which is itself subject to the format family's comprehension gate — mechanical lints, then a panel of cold readers, passing only after two consecutive reads return zero blocking findings — a review loop one agent's single pass does not honestly close alone. Intake 2026-08-19 — door: refactor · kind: infra · footprint: `TEST_MATRIX.md`, `scripts/build-matrix-reference.py`, `guardrails/check-matrix-reference.py`, `tests/conftest.py`, `docs/test-matrix-format.md`, the nine test files named above, and — the same machinery, following behind — `ARCHITECTURE.md` · map: beside row 621 (a generated table's two homes) and rows 390/392 (the size ceiling this wish no longer owes) | surface | *declined* 2026-08-27 — not the owner's ask; struck by his provenance order of 27.08 | Done when: TEST_MATRIX.md's body stands in `matrix/*.md` parts behind a shrunk core carrying a `## Parts map`, `scripts/build-matrix-reference.py` and `guardrails/check-matrix-reference.py` read core-plus-parts the way their spec siblings do, the generated Reference lives in its own committed file synthesized at read time, `tests/conftest.py`'s shared reader covers TEST_MATRIX.md the way it already covers PRODUCT_SPEC.md so the nine direct-open tests convert with no other test touched, `docs/test-matrix-format.md`'s Reference-location change has cleared the comprehension gate's two consecutive clean cold reads, the full suite and `bash guardrails/pre-push` run green, and ARCHITECTURE.md is cut by these same steps in its own following delivery. Not done by: shrinking the file without the reader node, leaving the Reference embedded in the core, or skipping the cold reads because the ceiling that used to force this work is gone |
