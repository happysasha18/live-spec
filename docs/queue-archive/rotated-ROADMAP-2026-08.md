# Rotated ROADMAP rows — 2026-08

> One calendar month's closed rows gather here; the live queue stays lean and these rows stay grepable by number. A row's presence here certifies its terminal exit — landed, decided, declined, or superseded stands in its status cell.

| # | Wish (plain words) | Class | Status | Decision / acceptance |
|---|---|---|---|---|
| 571 | **A clean tree reads green at any hour** (cost audit repair b; the after-midnight reds fired their third time at the 00:20 run): the record-freshness check demanded a record dated today, so a clean tree red after midnight until the day's first review record existed. The work road now accepts the newest committed record while it stays fresh for the guarded documents. The push road keeps demanding today's record, per his recorded line. | bug | landed 2026-08-07, the night session's repair commit | Done when a clean-tree run after midnight exits green — met: the 01:15 run shows the after-midnight reds gone |
| 573 | **The suite stops paying for itself twice on ordinary runs** (cost audit repair a): the scratch self-run guard re-fired on every work run while any unpublished commit touched the checks folder, 134 seconds each time. A green self-run now records the checks' content digest and skips while the digest stands. Any changed byte of the machinery re-fires it; a missing record runs it. | bug | landed 2026-08-07, the night session's repair commit | Done when an ordinary run with unchanged machinery skips the self-run by name and a machinery edit re-fires it — met by the digest tests |
| 558 | **The opening read leaves an artifact, and a push from a session that wrote none is refused** (found 2026-08-06 reading the handover chain): `guardrails/check-handover-provenance.py` judges the handovers that exist and says nothing about the ones that do not, so three sessions closed without writing one and nothing noticed. The newest handover under `docs/handovers/` is dated 2026-07-29. The opening read has no committed artifact at all today, which is why no gate holds it. This row gives the opening read an artifact of its own and extends the gate from the shape of a handover to its presence. Door: feature · kind: infra · footprint: `guardrails/` + `docs/handovers/` + the resume discipline · map: guardrails node | surface | declined 2026-08-09 (day 2 row 2.2: gate ab and `guardrails/check-handover-provenance.py` retired — no gate left to extend) | Done when: the opening read writes a dated artifact naming what it read and the state it found, a session's closing handover is required of every session that wrote to the tree, the gate reds a push whose range carries commits from a session with no handover and names that session, a red-first proof drives the new arm, and the counting start is stated so history before the rule reds nothing. Non-goal: judging whether the writing session was genuinely fresh, which stays with the session's discipline |
| 602 | **The migration chapter still sends a host to the rule that was cut** (found 2026-08-12 by the skill review `docs/skill-review/2026-08-12-live-spec-base-3.md`, finding 7, and carried forward from the review before it). `MIGRATION.md:125`, in the 2.0.0 chapter, tells a host the method rule is rule 30 in `skills/live-spec-base/SKILL.md`. Rule 30 was cut whole on 2026-08-12 and its number retired, and the rulebook's head now says so in plain words, which sharpens the contradiction: the rulebook states the number is dead while the migration chapter still walks a host to it. A host that adopted 2.0.0 carries rule 30 today. Intake 2026-08-12 — door: bug · kind: infra · footprint: `MIGRATION.md` · map: the chapter the next major release owes | bug | landed 2026-08-13, the C-K1 migration-chain repair | Done when: the 2.0.0 chapter names the rule by what it says, with the number dropped, and the next major release's own chapter tells a host carrying rule 30 what to do with it — met: the 2.0.0 chapter item 2 names the rule by what it says with the number dropped, and the 5.0.0 chapter step 3 tells a rule-30-carrying host what to do |
| 522 | **A session's record is read at both ends by an agent that did not live it** (the owner asked for this as a standing process on 2026-07-28, after a session wrote its own handover from memory and named a settled question as waiting for him): `scripts/session-extract.py` writes the person's own turns from one transcript into one compact file, and that file goes to a scratch directory since a transcript holds private conversation. At the close, a fresh agent writes the session handover from that extract. A standing gate once reded a handover naming no transcript, no extract and no writer; it retired 2026-08-09 for no real catch on record (`attic/check-handover-provenance.py`), so this half stays a discipline the seat holds too. At the open, a fresh agent lists every decision the person made with its timestamp, compares the list against `DECISIONS.md` and `NEXT_STEPS.md`, and reports to the session lead whatever is missing from both. | surface | *landed* 2026-07-28 | Done when: Requirement 303 states the three parts — MET; rows M-483, M-484 and M-485 carry them, M-484 now retired with no owning test since its gate found no real catch — MET; the extract runs over a real transcript — MET (8 human turns, 1554919 bytes read into 53546); the opening step stays a discipline the seat holds, since a session's opening writes no committed artifact for a gate to read — MET |
| 549 | **The never-bend criteria carry a test row of their own** (found 2026-08-06 by the same review): the budget rule added four criteria to the never-bend list of `PRODUCT_SPEC.md`, riding codes the ladder already owned. The matrix rows for those codes enumerate the older never-bend items and name neither the standard the work is held to nor the fresh clean-context agent. Deleting all four criteria today leaves the suite green. | small | *landed* 2026-08-06 | Done when: a matrix row names the four criteria, a test fails when any one of them is removed, and the row states which code it rides |
| 555 | **A count this repository publishes about its own tree is built from the tree** (found 2026-08-06 by the number pass over the pack's two front pages): four counts on `README.md` and `guardrails/README.md` were false — thirteen gates against twenty-nine on the push hook, 6,328 lines under `skills/` against 6,370 measured, 5,178 skill-body lines against 5,216 measured, and eighty worker runs against a command whose reading moves between two runs minutes apart. Each was typed by a person and went stale as the tree grew. The repair gives every published count of this tree a declared measurement in `guardrails/tree-counts.json`, a generated home on its page, and a push gate that re-measures it. Door: feature · kind: infra · footprint: `guardrails/` + `scripts/` + the two front pages · map: guardrails node | surface | *landed* 2026-08-06 | Done when: the registry declares four counts with the ground each carries, `scripts/gen-tree-counts.py` fills both generated blocks, gate ad is wired on the push hook and mirrored in CI with a red-first proof, the four wrong counts are repaired on the two pages, and the reproduction command published beside each number returns that number [INV-305, R306]. Non-goal: a number a session writes into chat, a number in a rendered artifact, and a count inside a shipped skill body, each of which stays unheld here. Landed and pushed 2026-08-06 |
| 556 | **The pack records what each of its runnable files is** (found 2026-08-06 by the host-scripts review, which caught two checks running green over the wrong tree): a skill body tells an agent to run a file by path, and some of those files judge this pack's own document set and mean nothing in a host project. Nothing on the page says which is which. The repair is one machine-readable record at `scripts/check-registry.json` — kind, name, kit, root, reach and needs per runnable file — with a gate over it, and the correction of `skills/text-audit/SKILL.md`, which today sends a host project's agent to run two checks that measure this pack's documents. Door: feature · kind: infra · footprint: `scripts/` + `guardrails/` + one skill body · map: guardrails node | surface | *landed* 2026-08-06 | Done when: the registry carries an entry per runnable file a skill names and per file those entries read, gate ae is wired on the push hook and mirrored in CI with a red-first proof, a pack-only check named in a skill body reds by file and line, a declared root disagreeing with the code reds by entry and line, and the text-audit body names no check that measures this pack's own documents [INV-306, R307]. Non-goal: the resolver, the wrapper command, and the vendored set, which the host-root contract's later rows carry. Landed and pushed 2026-08-06 |
| 557 | **A spoken sentence sets a project up on the pack** (Alexander's standing ask that the pack run in plain words, read against the tree on 2026-08-06): the pack promised that a person says what they want and the right setup walk runs, and no installed skill description carried the setup sentences, so an agent had to be handed a procedure file by hand. There was also no founding walk for an empty directory. The repair puts the setup entry on one skill description, ships a routing card that resolves the pack's own tree, and adds `adopt/START.md` for a fresh project. Door: feature · kind: skill · footprint: `skills/` + `adopt/` + `docs/` + `evals/` · map: attach node | surface | *landed* 2026-08-06 | Done when: one installed description carries the setup phrases and `spec-author` names it as the earlier door, the routing card names all three walks and carries six ordered reads ending in one action, `adopt/START.md` copies only templates that ship and is safe on a half-done tree, the request-kind table places all three setup sentences, and a founding runs on a throwaway tree to a green scaffold suite [INV-307, R308]. Non-goal: making `pack.tree` a recognised setting, which row 561 carries. Landed and pushed 2026-08-06 |
| 569 | **A work block is grounded in his sight before it runs** (Alexander 2026-08-07, 00:17, 00:42, 00:46). Sessions entered long work blocks he could not connect to any request of his. Before a block starts, the session names the request it serves, the pipeline step, the demanding rule, and the estimate. Every report line opens with the same root: his dated request, a standing instruction of his, or a stated reason. Machinery — an alarm, a gate, a schedule — is never a root. A step outside the announced plan stops and announces itself first. The board's per-task plan is the plan's home, and the delivery report accounts each block against it. Law 7 of the session-rules reminder already carries the rule to every prompt. | surface | *landed* 2026-08-07 | Done when: a session transcript shows every block opened by its root line; a step outside the plan shows its stop; the delivery report names each block's plan line |
| 572 | **The commit fence re-arms on the session's own commit** (problem ledger, second occurrence 2026-08-07 00:29). The fence records the last seen commit, so a session's own second commit reads as a foreign move and blocks. Each multi-commit session pays a hand refresh. The ledger names the fix: re-arm on the session's own successful commit, keeping the teeth against another window. The design must keep two windows honest about each other's moves. | bug | *landed* 2026-08-07 | Done when: one session lands two commits with no manual refresh, and a commit from another window still blocks; the ledger entry flips to SOLVED |
| 574 | **The suite leaks one temp folder per full run** (found at the 00:20 run). Eleven leaked folders from past days sat in the system temp dir, removed 2026-08-07 with their names in the journal. Each full run leaves one livespec-test-agent-inbox folder behind, and the leak check pins the blame on whatever test runs last. The leak reproduces only in full runs; the class runs in isolation clean. Find the true leaker and close it. | bug | *landed* 2026-08-07 | Done when: three consecutive full runs leave the temp dir unchanged; the leak check stays armed |
| 577 | **The landing-refresh law gains a heal road** (found 09:47 at the seat's own acceptance run; same class as the record-freshness work road, row 571). The law says a landing commit refreshes the resume file in that same commit. Three of the night's landing commits missed it, and the checker's only stated fix is amending history, which fabricates a record. The strict law stands as the norm; the checker learns to accept a later commit in the same push range that touches the resume file and names the missed landing by its commit id, so the miss stays visible and the range heals honestly. | bug | *landed* 2026-08-07 | Done when: fixtures prove the heal passes, a missing heal still reds, and a heal before its landing does not count; the law's home states the heal road; the night's three missed landings are healed by one named commit |
| 618 | **The prover's body runs to 1,079 lines with no reference directory** (found 2026-08-13 by Anthropic's skill-creator review of the prover, `docs/skill-review/2026-08-12-product-prover-2.md`). That review's sizing guidance puts a skill body near 500 lines. Three sibling skills already hold the pattern, in `skills/build-pipeline/references/`, `skills/communicator/references/` and `skills/text-audit/references/`. The public edition split its own Phase 3e into `editions/product-prover/reference/stress-lenses.md` and reads 801 lines. Four self-contained blocks are the candidates: Phase 3e at lines 545-953, the review modes at 288-416, the glossary at 1042-1079, and Phase 0's architecture lens at 431-461. Row 69 asked for a split of two of them in one line and stands deferred, and its revisit trigger — the next edit to this skill — fired on 2026-08-13. The trap: `guardrails/check-doc-findings-bound.py`, gate aa, reds a live document carrying no entry in `guardrails/rule-census.json`. So each new reference file is measured into that record in the landing that creates it, or the push reds. Intake 2026-08-13 — door: refactor · kind: skill · footprint: `skills/product-prover/SKILL.md`, a new `skills/product-prover/references/`, `guardrails/rule-census.json` · map: the prover node's own body | surface | declined 2026-08-19 (product-prover decoupled to its own repository in v5.0.0 — skills/product-prover/ and its references/ are no longer this repository's to build; the split, if it still matters, is that repository's own row now) | Done when: the prover's SKILL.md reads under the sizing guidance, each extracted block sits in one reference file the body points at by name and by the phase that reads it, every extracted file carries its own entry in `guardrails/rule-census.json`, and gate aa passes on the tree that ships them |
| 619 | **The internal prover has drifted from its public edition on five points, under a guard that reads timestamps** (found 2026-08-13 by Anthropic's skill-creator review of the prover, `docs/skill-review/2026-08-12-product-prover-2.md`). The two copies share most of their body, and the edition has been repaired in five places the internal copy never received. Its glossary triggers named slash commands nothing registers. The word "seam" carried sixteen uses and no definition in the word list. The restructure gate asked for an exact comparison by hand, where the edition asks the project to write down the command it ran. Phase 5 lists five closing blocks against the edition's six, the sixth being one line for each strength that survived the pass. Phase 4 scopes domain language to user-facing surfaces, where the edition scopes it to every string the document promises to emit. `scripts/sync-mirrors.sh` guards the pair by modification time alone, so an edition newer than its skill publishes and content drift stays invisible to it. The 2026-08-13 landing repaired the first two by hand, which is the shape of the problem: five point fixes leave the sixth drift for the next reader to find. Intake 2026-08-13 — door: feature · kind: skill · footprint: `scripts/sync-mirrors.sh`, `skills/product-prover/SKILL.md`, `editions/product-prover/SKILL.md` · map: beside the mirror-publish walk | surface | declined 2026-08-19 (both copies this row guarded are gone from this repository — skills/product-prover/ decoupled to its own repository in v5.0.0, and its editions/product-prover/ mirror retired 2026-08-19 as dead weight; no drift is possible between two copies where only one still exists here) | Done when: a standing check reads a skill and its edition for content drift and names each passage carried by one copy and absent from the other, the three remaining drift points are settled in whichever direction that check reports, and a planted drift reds it |
| 565 | **The two example run records show two ledger shapes** (push review 2026-08-06, finding 7): run 1 carries a per-finding ledger table; run 2 collapses it into one row. The edition's page points a reader at both records as the shape to expect, so the examples answer one question two ways. | small | declined 2026-08-19 (the mirror road this row lived on is cut: `editions/product-prover/` — the edition and its two example run records this row asks to reconcile — was retired 2026-08-19 as dead weight, before this row's own closing, so there is no edition page and no pair of records left to carry either shape) | Done when: the edition states which record shape a run owes, both example records carry it, and the comparison record notes the repair |
