# Archived off PLAN.md: a row that names no outcome this project can reach

Date: 2026-08-28. His word at 00:53 that night governs the run this came out of: carry the plan to
the end, archive anything blocked or unclear, add no machinery without a reason.

The criterion, and it is narrower than "hard to write": a row leaves the board when nobody can say
what finished would look like *here*, and nothing in this tree could ever settle it. A row whose
criterion is merely difficult to state stays and gets one written. Only one of the twenty-seven open
rows failed the narrow test.

Nothing is lost. The row below also stands in full, in its original wording, as row 596 of
`docs/queue-archive/rotated-ROADMAP-2026-08-27-merged-into-plan.md`. If its cause is ever fixed
where the cause actually lives, it comes back as a task with a definition of done rather than as the
dated note it is here.

## Index

One line for the archived row, findable by its own number — the pointer a reader who greps the
live list for that number follows to get here.

| # | Wish (plain words) | Class | Status | Decision / acceptance |
| --- | --- | --- | --- | --- |
| 596 | Personal settings never leak into a worker's task | archive | declined 2026-08-28 | this repository cannot reach the cause, which lives in the owner's personal layer, so no state here could go from undone to done; the row stands as the dated record of a cost already paid, in full under its heading below |

---

### ⬜ Personal settings never leak into a worker's task — id: q-596
**Group:** Worker & data safety · **Priority:** normal
**Source:** found 2026-08-12 — text from the owner's personal layer surfaced inside four workers' results and cancelled one command.

**Acceptance:** One-line answer: this repository cannot fix its own cause; the fix belongs to the owner's personal layer, and this row stands as the dated record and evidence of the cost — four workers saw the text, one command was cancelled and retried

**Why it left the board, 28.08.** The row's own acceptance says it: this project cannot reach the
cause. The text that leaked belongs to the owner's personal layer, which lives outside this tree and
which this project's own plan says it does not touch. So there is no state here that could change
from undone to done, and no command or reading in this tree could tell the difference. What the row
really was is a dated note of a cost already paid — four workers saw the text, one command was
cancelled and retried — and a note belongs in the archive rather than on a list of work.

Nothing about the incident is being disputed or forgotten. The four workers all treated the text as
data and carried on, which is what their briefs ask of them, so no worker acted on the embedded
instruction. The full account is at row 596 of the merged-queue archive named above.
