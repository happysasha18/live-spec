# Cold read of `skills/text-audit/SKILL.md` (with `references/reader-prompt.md` and `references/human-prose-rules.md`), read 2026-07-29

Stops: 43 — 6 blocking

1. **"Part of the **live-spec pack**"** — opening blockquote, "Part of the live-spec pack. The shared working rules live once…". A stranger cannot tell what a pack is: a directory, an installable bundle, a set of related skills, or a product name. The last section lists eleven named members but never says what the container is or how a reader gets it. *Guess:* a bundle of skills installed together into an agent. Non-blocking.

2. **"Four scopes settle a setting there: the session's live word first, then the host profile, the personal profile, and the package default."** — opening blockquote. Four terms arrive at once with no definition and no path to one: "the session's live word", "the host profile", "the personal profile", "the package default". A stranger also cannot tell which settings this ladder decides, or whether any of them changes how the audit runs. *Guess:* configuration overrides for things like language and verbosity, none of which touch the audit loop. Non-blocking.

3. **"Used on its own, this skill is plain advice a person applies by hand"** — opening blockquote. "On its own" names one side of a contrast whose other side is never stated. On its own as opposed to what — loaded with the base skill, run inside the pack, run by an agent rather than a person? *Guess:* on its own means without the rest of the pack installed. Non-blocking.

4. **"Those readers found new blocking terms on every pass, and the terms already repaired stayed repaired."** — "The loop came from the comprehension gate on spec sections…". This is the evidence the whole method rests on, and none of it is on the page: how many passes, how many readers, on what text, how many terms. The named file `docs/spec-format.md` is not one a reader of this page can open. *Guess:* a handful of runs over `PRODUCT_SPEC.md` sections during one project. Non-blocking.

5. **"A reading record carries every stop and any check that did not run, and by hand it sits beside the audited text."** — "The loop came from the comprehension gate…". Two stops in one sentence. "By hand" is used as a named mode roughly two hundred lines before "The by-hand mode" defines it. And "any check that did not run" points at a set of checks no earlier sentence has introduced. *Guess:* "by hand" means the no-agent mode, and the checks are the ones listed much later under the spec-section section. Non-blocking.

6. **"One person may hold the auditor role and own the text, and the cold reader is never either of them."** — "The roles and the words this skill uses". I read this twice. "Either of them" can point at the two roles (auditor, owner) or at the one person who holds both. On the first reading it seemed to say a cold reader cannot be a person. *Guess:* the cold reader is neither the auditor nor the text's owner. Non-blocking.

7. **"Every rule binding either surface is then in force. That rule stands in the same file, in the note over the surface list."** — "**One rule settles the surface a text stands on.**". Blocking. "That rule" has no antecedent in its own sentence: the nearest candidates are the bolded rule in the heading sentence and the rules just said to be in force. Worse, the step a reader must actually perform — naming the text's primary surface — is delegated to `guardrails/language-rules.json`, a file this page never prints and never summarises. The document elsewhere promises the audit "works anywhere" through grep fallbacks, but a reader without that repository cannot carry out this step at all. "Either surface" also presumes exactly two surfaces, while an unpublished text has one. *Guess:* the surface list holds six named surfaces with a short description each, and the rule is the bolded sentence restated. Blocking.

8. **"The word cap arrives with the register: 25 words for a human-prose sentence, and 35 for a spec-body criterion (rule `r08`)."** — "The **register** of a surface is the set of writing rules…". The same fact is stated a second time under "The mechanical lints": "A sentence past the cap for its surface is a hit, and 15 to 25 words is the band a human-prose sentence aims at." A reader now holds two numbers (a 25 cap, a 15–25 band) and must work out that one is a limit and the other a target. "Arrives with" is also a vague relation — does the register set the cap, or merely accompany it? *Guess:* 25 is the hard cap and 15–25 the aim, and the second statement is a restatement. Non-blocking.

9. **"Load it when a human-facing text is about to ship and its clarity matters"** — "When it fires". The document names an act — loading — with no way to perform it. There is no command, no path, no mechanism. The heading calls this firing, a third word beside "load" and "trigger" in the same short section. *Guess:* an agent picks the skill up from its description when the user asks. Non-blocking.

10. **"Run every check a script or a grep settles, before a reader spends attention on the text."** — "The loop", step 1. I read this twice. A script settling a check reads as an odd predicate, and "settles" here seems to mean "can decide mechanically" rather than "resolves a dispute", which is how the word is used in items 7 and 8 of the same document. *Guess:* run every check a machine can decide. Non-blocking.

11. **"The five lints under "The mechanical lints" are that whole set."** — "The loop", step 1. The set is named by its count, so the sentence sends the reader to another section to learn what the five are. *Guess:* the five bullets in that section. Non-blocking.

12. **"A non-blocking finding waits: it queues for the person's taste call once the blocking ones are gone."** — "The loop", step 3. Queues where — a file, a list in chat, the reading record? No actor holds the queue. "Taste call" is not defined, though "Taste and voice stay with the person" appears earlier. *Guess:* the auditor hands the person a list at the end of the run. Non-blocking.

13. **"The loop ends when two consecutive reads return zero blocking findings."** — "The loop", step 4. This exact fact is already stated in the section's opening line ("It closes when two consecutive cold reads return zero blocking findings") and again in the skill's description block. Three statements of one rule inside one file. *Guess:* the repetition is emphasis, not a distinction. Non-blocking.

14. **"A section-sized run puts one definition and a handful of sentences in front of a reader."** — the paragraph after "The loop". "A handful" leaves the quantity open, and this is the sentence that sizes a run. The document's own rulebook bans exactly this: rule `r33`'s recorded case is `a few` → `state the exact quantity`. So the text breaks a rule it ships. *Guess:* five to ten sentences. Non-blocking.

15. **"The plan chose that size and gave two reasons for it"** — "Running it on a spec section". The set is pointed at by a count, so I stopped to look for the two reasons; they arrive in the following sentence, one clause each, unnumbered, and I had to match them back. A document is also named as the actor that chooses. *Guess:* the two reasons are the reader's holding capacity and the blast radius of a repair. Non-blocking.

16. **"Four things change there, against the human-prose run the sections above describe."** — "Running it on a spec section". Another set named by count only. "The human-prose run" is a name appearing here for the first time for something the earlier sections never called a run. *Guess:* the four bolded paragraphs that follow, and the human-prose run is the ordinary four-step loop. Non-blocking.

17. **"A rule binds a whole region of the file, so those two rules never both judge one sentence."** — "**A criterion and the prose around it take different rules.**". Blocking. "Region" is undefined and no sentence says where a region starts or ends. The reader has to decide, while repairing, which rule judges a given sentence — third-person-with-named-actor, or address-the-reader-directly — and the page gives no boundary test. A Context paragraph that quotes a criterion, or a criterion containing an aside, has no answer here. I also read the sentence twice: "never both judge one sentence" parses as a conclusion drawn from the region claim, but the claim does not obviously support it. *Guess:* a region is the block of lines belonging to a requirement's Context versus its numbered criteria, split by their headings. Blocking.

18. **"The third is four commands of its own"** — "Four checks run after the section is repaired…". The item is named by its position, so I had to count into the list below to learn which check is meant. *Guess:* the structure checks. Non-blocking.

19. **"Run the audited project's own suite command, whatever it is"** — first bullet of the four checks. The step names a check and then declines to name the way to run it. A reader holding this page alone cannot carry it out. *Guess:* look for the project's test command in its README or package manifest. Non-blocking.

20. **"the census, `python3 scripts/rule-census.py`. It counts findings per file … A count above the record fails, and the batch runs again."** — the fourth of the four checks. Blocking. Two questions stay open. First, whether these four checks belong to a spec-section run only: they sit under "Running it on a spec section", but the later section "This skill is held to the rules it lists" applies the census and its record file to this skill, which the document says is human prose. So a reader auditing a README cannot tell whether the census is owed. Second, no actor reads the total against the record, and "the batch runs again" has no meaning for a README, which has no batch. *Guess:* the census runs on any file recorded in `rule-census.json`, and the other three structure checks are spec-only. Blocking.

21. **"`bash guardrails/check-freeze.sh` — the three guarded documents match the frozen baseline"** — the third of the four checks. Both "guarded documents" and "the frozen baseline" are used here and defined eleven lines later, and "guarded document" is never defined at all — the reader has to infer the three from a command line further down. *Guess:* `PRODUCT_SPEC.md`, `ARCHITECTURE.md`, and `TEST_MATRIX.md`, from the freeze command below. Non-blocking.

22. **"`--compaction`"** — "The frozen baseline is the recorded map…". The reader is given a command to type with a flag nothing on the page explains, and no way to tell whether it is required or optional. *Guess:* it makes the freeze record smaller. Non-blocking.

23. **"At the push, `python3 guardrails/check-doc-findings-bound.py` runs the census comparison over every live document"** — its own paragraph. No actor: a stranger cannot tell whether they run this, or a hook runs it for them, and "at the push" does not say. "Live document" is undefined and is a second name for something earlier called a file with a record. *Guess:* a pre-push hook runs it automatically where the repository is set up. Non-blocking.

24. **"**The build test measures the work once the audit has closed.** … **The method's build-test evidence is owed.** No record stands behind any build test."** — the last two bolded paragraphs of "Running it on a spec section". Blocking. The document prescribes a build test in detail — hand the requirements to a fresh agent, count what it builds unasked, reach every requirement in the batch — and then says no such run has ever been recorded and that the skill states no build count. A reader cannot tell whether the build test is a step they owe or a proposal nobody has run. "Is owed" also names no one who is owed and no one who judges the debt, and "the method" is a term this page uses only here. *Guess:* the build test is optional and unproven, and the audit is complete without it. Blocking.

25. **"Grep fallback: list the nouns written with an initial capital and the nouns this project coined"** — the vocabulary lint. The fallback exists for the case where the repository is absent, yet it asks the reader to know which nouns the project coined — which is the very knowledge a stranger, and an outsider auditing someone else's text, does not hold. *Guess:* nouns that look like jargon and return nothing familiar. Non-blocking.

26. **"A project holding no such file gets one, carrying a `weak_words` list."** — the weak-word lint, third sub-bullet. No actor creates the file, no path says where it goes, and no shape says what the file looks like beyond one key. The key `weak_words` and the file `weak-words.json` also differ in punctuation, which cost me a second look. *Guess:* the auditor writes `guardrails/weak-words.json` beside the script, holding a JSON object with one `weak_words` array. Non-blocking.

27. **"Grep fallback: read for those four classes by hand."** — the style-and-register lint. The four classes are stated in prose above as a run of clauses, so I had to go back and count them out. *Guess:* the word cap, capitals for emphasis, the denial frame, and size-grading adjectives. Non-blocking.

28. **"No named thing appears under two names: a file, a script, a command, or a concept."** — the one-name lint. I read this twice. On the first pass the colon list read as the two names a thing might appear under; it is in fact a list of what counts as a named thing. *Guess:* the list enumerates named things. Non-blocking.

29. **"In this pack that means a fresh worker with the pack not loaded, reading the text from outside."** — "The cold reader". "Worker" is a third name for one thing the document has already called a cold reader and a fresh session, in a document that ships a one-name-per-thing lint. "From outside" also leaves its slot open: outside the pack, the repository, or the project? *Guess:* worker is the pack's word for an agent session, and outside means without the project's rules loaded. Non-blocking.

30. **"An undefined term the rest of the text leans on blocks."** — "Every finding is **marked blocking or non-blocking**." I read this twice: the sentence ends on a verb that had been used as an adjective two sentences earlier, so "blocks" first parsed as a noun. *Guess:* such a term counts as a blocking finding. Non-blocking.

31. **"That material is the source spec, the code, the recorded decision, or the author's own notes."** — "Where a fix comes from". "The recorded decision" arrives with a definite article and no home: a stranger cannot tell where decisions are recorded, or what to do when the project keeps none. *Guess:* a decision log or an architecture document, if the project has one. Non-blocking.

32. **"Four of those rules bind every sentence of this file"** — "This skill is held to the rules it lists". The companion sheet prints 48 rules binding human prose, and this sentence singles out four. A reader cannot tell whether the other 44 bind this file loosely, bind some sentences, or do not bind it at all — while the sentence just above says the skill obeys the register as a whole. *Guess:* all 48 bind, and these four are the ones the author found hardest to hold. Non-blocking.

33. **"## This skill is held to the rules it lists"** — the heading over that section. The heading promises a section listing rules; the body lists none and points at another file for the register, then spends most of its length on lint commands and a census record. *Guess:* the heading means the rules the skill points at rather than prints. Non-blocking.

34. **"Whoever changes this skill runs the register lint again. The same editor runs one cold-reader loop over the changed section before the skill ships."** — the closing lines of "This skill is held to the rules it lists". Blocking. "Again" names no first run, so a reader cannot tell whether a second run is being asked for or a first. And "one cold-reader loop" contradicts the loop this document defines, which closes only on two consecutive clean reads — so an editor cannot tell whether one read or two clean reads are owed before shipping a change. *Guess:* one full loop, meaning read-fix-read until two clean reads. Blocking.

35. **"A README, an article, or a piece of copy takes a bracketed query in the draft."** with **"it takes an inline `[GAP: what is missing]` note"** — "Where a fix comes from", and earlier "**A fix comes from the spec's own neighbours.**". The `[GAP: what is missing]` rule for spec sections is stated in full in both places. One fact, two homes. *Guess:* the second statement is the general rule and the first an early instance. Non-blocking.

36. **"The prompt's last instruction takes every other stop the reader met, so a class outside the five still comes back."** — `references/reader-prompt.md`, the paragraph before the pasted block. I read the block twice trying to find its last instruction. The block ends with a paragraph about relational words and new finds, but the instruction that closes the reporting shape — "Return the entries as a numbered list. If you stopped nowhere, say so in one line." — sits above it, so the pasted prompt reads as if it finished and then resumed. *Guess:* the last instruction is the sentence about a word the list does not name. Non-blocking.

37. **"At every relational word, ask the three questions and write which one is unanswered"** — inside the pasted block. The set is pointed at by a count before it is given; the three arrive after the colon in the same sentence, but the reader hits "the three questions" first with nothing to attach it to. *Guess:* the three that follow in the same sentence. Non-blocking.

38. **"A new slot-opening word joins the weak-word list, and the skill body's weak-word lint says which copy of that list takes the edit."** — the closing line of `references/reader-prompt.md`. No actor adds the word. And one thing carries three names across the two files: `weak-words.json`, "the fuller list", and "the weak-word list". *Guess:* the auditor adds it to `guardrails/weak-words.json`. Non-blocking.

39. **"This block prints 48 of the 62 rules the source carries."** — `references/human-prose-rules.md`, above the generated block. A reader holding these pages cannot check either number: the codes printed run from `r01` to `r71` with gaps, which suggests more than 62 ever existed, and no line says how many rules the block should hold. *Guess:* 62 live rules across all six surfaces, of which 48 bind human prose. Non-blocking.

40. **"Two of those pages carry more than this sheet does. `docs/language-rules.md` gives each rule with its examples… `docs/language-worked-example.md` walks one short document end to end"** — `references/human-prose-rules.md`, after the four-artifact list. "Those pages" points back at the four artifacts just listed, but only `docs/language-rules.md` is among them; `docs/language-worked-example.md` appears here for the first time. So the set claimed and the set delivered disagree, and I reread the list to be sure. "This sheet" is also a new name for what the same file calls the block. *Guess:* the worked example is a fifth page that the generator does not build. Non-blocking.

41. **"`the door` → `the entry point`"** — rule `r02`'s recorded case in `references/human-prose-rules.md`. The case is meant to teach a class, and a stranger cannot tell what "the door" named, so the repair teaches nothing. Compare `r03` and `r63`, whose cases carry enough context to read alone. *Guess:* "the door" was this project's coined word for some entry point in its pipeline. Non-blocking.

42. **"*Ask:* Was this sentence first written by someone who had never read this project's skills, working from a brief?"** — rule `r53` in `references/human-prose-rules.md`. Blocking. The file opens by saying these rules are the whole set a human-prose audit holds a text to, and the cold reader judges from the page alone. This rule cannot be judged from a page at all: it asks about the authorship history of a sentence, which is the one thing a cold reader is defined as not holding. A reader following these two files cannot tell who checks `r53`, when, or what evidence settles it. *Guess:* the auditor, not the cold reader, checks it, and only for text the project itself drafts. Blocking.

43. **"Explanatory text addresses the reader as `you` for what a person does, and names the component for what software does."** — rule `r25` in `references/human-prose-rules.md`. `SKILL.md` is explanatory text held to this register, and it addresses the reader as "you" once in its whole length ("When that repository is on your disk"); everywhere else a person's actions are given to "a person", "the auditor", "whoever changes this skill". A rule the package ships is broken by the document that ships it. *Guess:* the rule binds instructions to a human elsewhere, and skill bodies are treated as an exception nobody wrote down. Non-blocking.

Blocking entries: 7, 17, 20, 24, 34, 42.
