# Cold read of `skills/text-audit/SKILL.md` (with `references/reader-prompt.md` and `references/human-prose-rules.md`), read 2026-07-29

Stops: 53 — 8 blocking

1. **"It states the register it holds a text to"** — description block at the top of the file. A stranger reading top to bottom meets *register* here, and the file defines it only much later ("The **register** of a surface is the set of writing rules its text is held to"). Nothing in the description points forward to that definition. *Guess:* register means the style rules for that kind of text. *Non-blocking.*

2. **"Part of the **live-spec pack**"** — the indented note under the title. A stranger cannot tell what a pack is: a repository, an installable bundle of skills, a directory, a product. The note names a base skill and a file path but never says what the grouping is. *Guess:* a set of skills shipped together in one repository. *Non-blocking.*

3. **"Four scopes settle a setting there: the session's live word first, then the host profile, the personal profile, and the package default."** — same note. Four terms arrive at once with no definition here: *the session's live word*, *the host profile*, *the personal profile*, *the package default*. The relational verb *settle* leaves open: settle by what measure, and settle what setting? The file names a path to `live-spec-base`, so the definition exists elsewhere, but this file gives a stranger nothing to act on. *Guess:* a precedence order, with a spoken instruction beating a config file. *Non-blocking.*

4. **"This skill checks whether a stranger understands a text, and repairs the places where they stop."** — the opening line of the body, and again in "A reading record carries every stop". The word *stop* is the unit the entire skill counts, closes on, and records, and this file never defines it. The definition sits only inside the pasted block in `reader-prompt.md`, as a list of five classes, and the body never points at that list as the definition. *Guess:* a place where the reader paused, guessed, or reread. *Blocking* — the auditor cannot judge a returned report, or count findings, without knowing what counts as one.

5. **"The loop came from the comprehension gate on spec sections, recorded in `docs/spec-format.md`."** — paragraph beginning "The loop came from". Two problems in one sentence. *The loop* is used as if already introduced; the section that defines it ("The loop") comes fifty lines later. And the claim's ground sits in a file a stranger holds no copy of. *Guess:* "the loop" is the four-step audit cycle described later. *Non-blocking.*

6. **"Those readers found new blocking terms on every pass"** — paragraph beginning "Those readers found". *Blocking* is used here, and then in the loop's closing condition ("two consecutive cold reads return zero blocking findings"), long before the file defines it under "The cold reader". A stranger meets the exit condition of the whole method before the word that carries it. *Guess:* blocking means the reader could not go on. *Blocking* — the reader cannot judge when the audit ends, which is the one thing the loop turns on.

7. **"The findings reached zero only after two reads in a row returned nothing that blocks."** — same paragraph. A stranger cannot tell how many passes ran, over what text, or how many findings each pass returned. The sentence is the evidence for the whole method, and its numbers stay off the page. *Guess:* several passes over spec sections, with the count falling each time. *Non-blocking.*

8. **"Every reading is written to a dated **reading record** under `docs/language-reads/`, and `docs/language-defects.md` records what each one returned."** — same paragraph. Two files are said to hold what a reading produced. A stranger cannot tell what goes in which: the reading record already "carries every stop", so what is left for the defects file? *Guess:* the reading record holds one session's raw stops, the defects file holds the classes distilled from them. *Non-blocking.*

9. **"One person may hold the auditor role and own the text, and the cold reader is never either of them."** — "The roles and the words this skill uses". *Either of them* has two candidate antecedents: the two roles (auditor, owner), or the one person who holds both. Read the second way, the sentence forbids only that one person; read the first way, it forbids any auditor or owner. I read it twice. *Guess:* the cold reader is never the auditor and never the owner. *Non-blocking.*

10. **"**A surface is a kind of text.** One file carries several kinds."** — paragraph beginning "A surface is a kind of text", the second one. The same definition, in nearly the same words, stands two paragraphs earlier: "A **surface** is a kind of text. One file carries several kinds at once". A stranger rereads to check whether the second statement adds something, and it does not. *Guess:* an editing leftover. *Non-blocking.*

11. **"The auditor names the text's one primary surface, reading it against the surface list in `guardrails/language-rules.json`."** — same paragraph. A stranger cannot tell how the auditor decides which surface is primary when a file carries several kinds, which the file has just said it does. No rule, no tie-break, no example of the choice. *Guess:* pick the surface that most of the file's sentences belong to. *Non-blocking.*

12. **"The word cap arrives with the register: 25 words for a human-prose sentence, and 35 for a spec-body criterion (rule `r08`)."** — paragraph beginning "The **register** of a surface". The same fact is stated a second time under "The mechanical lints": "A sentence past the cap for its surface is a hit, and 15 to 25 words is the band a human-prose sentence aims at." The two do not read alike — one gives a cap, the other a band — and a stranger cannot tell whether a 20-word sentence is inside or outside the rule. *Guess:* 25 is the hard cap and 15–25 is the aim. *Non-blocking.*

13. **"a stop in one place stands for that class everywhere else"** — paragraph beginning "A **class** is the shape of a mistake". Two readings. Either the auditor fixes only the reported place and the class label travels with it, or the auditor must sweep every other instance of that class in the text. Nothing in the file settles which, and the loop's step 3 says only "Write each fix from the source". *Guess:* the auditor sweeps the class across the whole text. *Non-blocking.*

14. **"`guardrails/language-rules.json` is the file where the six surfaces, the three words above, and every rule of every register are edited."** — end of the same section. *The three words above* points at a set by its count and leaves the members for the reader to go and gather. I stopped and counted back: surface, register, class. *The six surfaces* does the same, though they are listed earlier. The file states this exact class as a defect elsewhere ("a set pointed at by a count ... with its members never given", `r07`). *Guess:* surface, register, class. *Non-blocking.*

15. **"Load it when a human-facing text is about to ship and its clarity matters"** — "When it fires". *Matters* is a judgment with no judge and no measure. Who decides that clarity matters here, and against what? The bullets underneath give four examples, and they read as the whole answer, which makes the clause do no work. *Guess:* the person asking decides. *Non-blocking.*

16. **"The five lints under "The mechanical lints" are that whole set."** — step 1 of "The loop", set against **"**The requirement-shape lint applies here.** Run `python3 guardrails/check-requirement-shape.py FILE` beside the other lints."** under "Running it on a spec section". The requirement-shape lint is already one of the five mechanical lints, where it is listed as "A spec section owes the requirements genre". The spec-section section then presents it as one of "Four things change there", as though it were an addition. A stranger cannot tell whether the spec run has five lints or six, nor whether this script runs once or twice. *Guess:* five lints, and the spec section merely turns on the one the README run skips. *Blocking* — step 1 says fix every hit before a reader runs, and the reader cannot tell which set of checks that is.

17. **"it queues for the person's taste call once the blocking ones are gone"** — step 3 of "The loop". *Taste call* is used without definition. A stranger cannot tell whether this is a formal step, a list handed over, or a note left in the file. The later sentence "The non-blocking ones go to the person as a list" answers part of it, far away. *Guess:* the person decides one by one which non-blocking findings to spend a rewrite on. *Non-blocking.*

18. **"The comprehension gate settled on two reads, and `docs/spec-format.md` records that pattern."** — step 4 of "The loop". The number two is the closing condition of the whole method, and its ground is a file the stranger does not hold. The page never says what was observed that made two the right number rather than one or three. *Guess:* a third read kept returning nothing, so two was taken as the point of stability. *Non-blocking.*

19. **"A section-sized run puts one definition and a handful of sentences in front of a reader."** — paragraph beginning "A section-sized run". *A handful* leaves the quantity open: relative to what, and by what measure? The printed rules in the companion file name this exact wording as the defect ("`a few` → `state the exact quantity`", `r33`), so the file breaks a rule it ships. *Guess:* five to ten sentences. *Non-blocking.*

20. **"A whole-page run puts every sentence of that page in front of one."** — same paragraph. *One* stands where the name belongs, and a stranger has to carry "a reader" in from the previous sentence. I read it twice, first taking *one* as one sentence. *Guess:* one reader. *Non-blocking.*

21. **"A spec section here is one requirement with its Context paragraph, its User Story, and its acceptance criteria."** — "Running it on a spec section". *Context paragraph*, *User Story*, and *acceptance criteria* arrive capitalized, as named parts of a defined shape, with no definition here and no path to one. The whole spec-section run then rests on them: the requirement-shape lint checks that "Context comes before criteria", and a stranger cannot run that check without knowing what a Context block is or where it starts and stops. *Guess:* a spec template with three labelled parts per requirement. *Blocking* — the reader cannot carry out the section's own checks.

22. **"a repair inside those lines cannot break a requirement a hundred lines away"** — paragraph beginning "Ten requirements at a time". The claim's ground is nowhere on the page. Nothing has said that requirements are independent of one another, and the file says the opposite elsewhere: a rewrite can break a test or one of the two maps. *Guess:* requirements do not cross-reference each other by text, only by code. *Non-blocking.*

23. **"Four things change there, against the human-prose run the sections above describe."** — paragraph beginning "A spec section stands on the spec-body surface". Four bold-led paragraphs follow, and then two more arrive in the same shape and the same section: "The build test measures the work once the audit has closed" and "The method's build-test evidence is owed". A stranger counting bold leads finds six and cannot tell which four the claim covers. *Guess:* the first four, with the last two standing outside the count. *Non-blocking.*

24. **"the map a script builds from the body criteria at freeze"** — the code-to-location table bullet. Two gaps. The script is not named, so a reader cannot run it. *Freeze* is used as a defined moment, and the file defines the frozen baseline several paragraphs later. *Guess:* the freeze is the run of `scripts/spec-freeze.py` named further down. *Non-blocking.*

25. **"whose rows pair one architecture node with one spec fact and pin the test level that covers it"** — the test matrix bullet. *Architecture node* and *test level* are used as defined terms of this method, with no definition and no path. A stranger cannot tell what a level is: a layer of the code, or a grade of test. *Guess:* level means how deep the test reaches, from string check to rendered pixel. *Non-blocking.*

26. **"Four checks run after the section is repaired, and each one reports what it read. The third is four commands of its own"** — paragraph introducing the check list. *The third* names a thing by its position, so I left the sentence, counted into the bullets below, and came back. The file states this class as a defect in its own printed rules (`r63`). *Guess:* the structure checks. *Non-blocking.*

27. **"Run the audited project's own suite command, whatever it is"** — first bullet of the four checks. A stranger cannot carry out this step from the page: no command, and no way of finding one. The three bullets beside it all carry exact commands, which makes the gap sharper. *Guess:* look for the project's documented test command and run that. *Non-blocking.*

28. **"`bash guardrails/check-freeze.sh` — the three guarded documents match the frozen baseline"** — third structure check. *The three guarded documents* points at a set by count without naming its members, and *frozen baseline* is used before the paragraph that defines it. I resolved both by reading ahead to `PRODUCT_SPEC.md ARCHITECTURE.md TEST_MATRIX.md`. *Guess:* those three files. *Non-blocking.*

29. **"runs the census comparison over every live document"** — paragraph beginning "At the push". *Live document* is used as a defined category and is never defined. A stranger cannot tell which documents are live: every markdown file, or only those with a row in the census record. *Guess:* every file carrying a row in `guardrails/rule-census.json`. *Non-blocking.*

30. **"**The method's build-test evidence is owed.**"** — the bold lead of the last paragraph in the spec-section run. The heading promises a subject the body then contradicts: the body says no build test was ever recorded. *Owed* also leaves its slots open — owed by whom, to whom, and by when? *Guess:* the method's authors owe a recorded build test, and none exists yet. *Non-blocking.*

31. **"Each word added this way is one more class the mechanical layer holds from then on."** — the weak-word lint bullet. *The mechanical layer* is a second name for the thing the file elsewhere calls *the mechanical lints*. The file's own last lint is "One name per thing", so it breaks a rule it states two bullets later. *Guess:* the same five lints. *Non-blocking.*

32. **"Which copy of that list a run reads: the `weak-words.json` sitting beside the `check-weak-words.py` that ran."** — the weak-word lint bullet. I read this twice. It opens as a question written without a question mark, and its answer arrives after a colon with no verb between them. *Guess:* the script reads the JSON file in its own directory. *Non-blocking.*

33. **"Context comes before criteria, each criterion carries one trigger and one response, and every judgment names a judge and a measure."** — the "A spec section owes the requirements genre" lint. The same three checks are written out word for word under "Running it on a spec section" ("It reads three things nobody would ask of a README. Context comes before criteria..."). One fact, two homes, in one file. *Guess:* one of the two is a leftover. *Non-blocking.*

34. **"A sentence past the cap for its surface is a hit"** — the "Style and register" lint. *Hit* is defined here by use, and the file already gave the reader an instruction that turns on it much earlier: "Run the mechanical lints, and fix every hit." *Guess:* a hit is one flagged line from a lint. *Non-blocking.*

35. **"**One name per thing.** No named thing appears under two names: a file, a script, a command, or a concept."** — the last mechanical lint. The file breaks this rule in its own sentences: the mechanical lints are also "the mechanical layer" (entry 31), the cold reader is also "a fresh worker" (entry 37), and the companion file adds a third name for the rules source (entry 49). A stranger who trusts the rule and then meets the second names stops. *Guess:* the rule is meant for the audited text, not for this file. *Non-blocking.*

36. **"so no reader spends a finding on a class a machine already owns"** — the closing line of "The mechanical lints". *Spends a finding* and *a machine owns a class* both put the action into a figure of speech, and the actor is a bare "a machine" rather than a named script. The file states four rules it holds itself to, one of them "no coined metaphor carries the meaning". *Guess:* the lints already catch that class, so the reader should not report it. *Non-blocking.*

37. **"In this pack that means a fresh worker with the pack not loaded, reading the text from outside."** — "The cold reader". The file's own definition earlier says "Inside the pack a cold reader is a fresh session". *Worker* and *session* are two names for one thing, and a stranger cannot tell whether a worker is something else — a subagent, a separate process. *Guess:* the same thing. *Non-blocking.*

38. **"An undefined term the rest of the text leans on blocks."** — "The cold reader". I read this twice and first parsed *leans on blocks* as a unit. The verb *blocks* lands at the end with no marker that it is the main verb. *Guess:* such a term is a blocking finding. *Non-blocking.*

39. **"A README, an article, or a piece of copy takes a bracketed query in the draft."** — "Where a fix comes from". The spec form gets an exact mark, `[GAP: what is missing]`, and this one gets a description with no form. A stranger cannot tell what to type. *Guess:* the same bracketed note, in whatever wording suits. *Non-blocking.*

40. **"Inventing an answer to close a finding is the one move this skill forbids."** — same section. The claim that this is the only prohibition sits against other prohibitions the file states: the cold reader repairs nothing, a rewrite never moves a mark a machine reads, the author is never their own cold reader. *Guess:* it is the gravest prohibition rather than the only one. *Non-blocking.*

41. **"Four of those rules bind every sentence of this file"** — "This skill is held to the rules it lists". The sentence before it says the skill obeys the human-prose register, which the companion file prints as 48 rules. A stranger cannot tell whether the other 44 bind this file too, or whether four were singled out for emphasis. *Guess:* all of them bind it, and these four are the ones the author watched hardest. *Non-blocking.*

42. **"The same editor runs one cold-reader loop over the changed section before the skill ships."** — the last line of "This skill is held to the rules it lists". *One cold-reader loop* reads two ways. A loop, as this file defines it, closes only on two consecutive clean reads, so "one loop" could mean the full cycle. It could equally mean one read. *Guess:* the full cycle, so at least two reads. *Blocking* — an editor cannot tell what to do before shipping a change.

43. **"The prompt's last instruction takes every other stop the reader met"** — `references/reader-prompt.md`, the paragraph beginning "The prompt names five stop classes". *The last instruction* names a thing by its position, and inside the block the ordering fights the description: "Do not fix anything... Return the entries as a numbered list. If you stopped nowhere, say so in one line." reads as the closing instruction, and another paragraph follows it. I had to read the block twice to work out which instruction was meant. *Guess:* the closing "A word the list above does not name..." sentence. *Non-blocking.*

44. **"Do not fix anything. Report only where you stopped and why. Return the entries as a numbered list."** — inside the pasted block in `reader-prompt.md`. The block never says where the report goes. The skill body says every reading is written to a dated reading record under `docs/language-reads/`, and by hand "it sits beside the audited text", but the prompt handed to the reader carries neither instruction. A stranger running the pack's own procedure cannot tell whether the reader writes that file or the auditor does. *Guess:* the reader returns text in the session and the auditor files it. *Blocking* — the record is a named output of the method, and the only prompt that produces it never asks for it.

45. **"A word the list above does not name, that still stopped you, is a real find — report it and note that it is new."** — inside the pasted block. Every other stop must be marked blocking or non-blocking; this one is told only to be marked new. A stranger cannot tell whether a new-class find also takes the blocking mark, and the loop's exit condition counts blocking findings. *Guess:* mark it both ways. *Non-blocking.*

46. **"a relational word — depends, related, handles, based on, corresponds to, proportional, larger, sufficient, appropriate, fast, easily"** — inside the pasted block, set against the same eleven words listed in the skill body's weak-word lint. The same list lives in two files, and the skill's own printed rule says one fact lives in one home with every other place pointing at it (`r56`). A stranger who adds a word to one copy cannot tell that the other exists. *Guess:* the body's copy is authoritative and the prompt's copy must be edited by hand to match. *Non-blocking.*

47. **"This block prints 48 of the 62 rules the source carries."** — `references/human-prose-rules.md`, the paragraph beginning "This block prints". The skill body says of this same file: "The human-prose register is printed **in full**". A stranger cannot tell whether they hold the whole register or three quarters of it, and the two files disagree in plain words. *Guess:* the 48 are the whole human-prose register, and the other 14 bind other surfaces, so both statements are meant to be true. *Blocking* — the audit's whole standard is this list, and a reader cannot trust that they hold all of it.

48. **"Two of those pages carry more than this sheet does. `docs/language-rules.md` gives each rule with its examples... `docs/language-worked-example.md` walks one short document end to end"** — `human-prose-rules.md`, near the top. *Those pages* points back at the four artifacts just listed, and `docs/language-worked-example.md` is not among them. A stranger counting back finds the referent missing. *Guess:* the worked example is a fifth page, built by something else. *Non-blocking.*

49. **"Every case the class was built from lives in the rule home."** — `human-prose-rules.md`, the paragraph beginning "Each entry names the class of mistake". *The rule home* is used as a defined term and defined nowhere in any of the three files. It is also a third name for the thing the same file calls `guardrails/language-rules.json` and *the source*. The term recurs where it carries an instruction: rule `r61` tells a writer to "enter that class in the rule home". *Guess:* `guardrails/language-rules.json`. *Blocking* — a writer is told to write into a place the documents never name.

50. **"The case is written text on the left and its repair on the right."** — `human-prose-rules.md`, same paragraph. Many entries below break this shape: their right side carries an instruction rather than repaired text — "name the actor and say plainly what it does" (`r20`), "name the judge and the measure" (`r32`), "state the exact quantity" (`r33`), "delete" (`r15`). A stranger cannot tell whether those are repairs written in shorthand or a different kind of entry. *Guess:* the author fell back to instructions where no single repair fits. *Non-blocking.*

51. **"a long flat run of peer items at one level (`r45`) ... A long run of peer items is gathered under headed parents."** — `human-prose-rules.md`, the rule list. The list stating this rule is itself 48 peer bullets at one level with no headed parents over them. A stranger reading the rule and then looking up at the page stops. *Guess:* generated blocks are exempt. *Non-blocking.*

52. **"`The system shall refuse each of the four faults below. - a branch behind main's tip; - a lane with no open row; - a lane past the cap.`"** — the recorded case under `r64` in `human-prose-rules.md`. The rule says parallel items become "a bulleted or numbered list ... one item per line", and its own repair runs all four items along one line. The rule is broken by the text that demonstrates it. *Guess:* the generator flattens newlines inside a case. *Non-blocking.*

53. **"the harness task panel on the human's screen" and "conversation runs in the human's pinned language"** — the recorded rules `r52` and `r18` in `human-prose-rules.md`. *Harness* and *pinned language* stand as defined terms of this project, in the one file a cold reader is handed as the standard, with no definition and no path to one. The same file's rule `r67` forbids exactly this. *Guess:* the harness is the tool running the agent, and a pinned language is the language chosen for a given person. *Non-blocking.*

Blocking entries: 4, 6, 16, 21, 42, 44, 47, 49.
