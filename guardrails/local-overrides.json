{
  "note": "An installed hook whose local copy is deliberately different from the repository source. The repository stays the version everyone gets; a machine may run a changed copy for a stated reason and for a stated span. Each entry pins the fingerprint of the installed file it covers, so the moment that file changes again the entry stops matching and the health check reds. An entry buys silence for exactly the copy it names.",
  "hook_overrides": [
    {
      "hook": "chat-law-hook.sh",
      "installed_sha256": "f8e28a6e925c5905149ff66d690fc7aa58c8a03f502c2377fbd383b0a9d5a249",
      "reason": "The repository now carries the shrunk hook the owner asked for on 2026-08-17, so the two copies speak the same line and print the same reminder. They differ in one comment line of the header: the installed copy points a reader at the pre-shrink text through a backup file on this machine, which means nothing to anyone else, so the repository copy points at this repository's own history instead. The behaviour is identical; only that pointer differs.",
      "added": "2026-08-17",
      "review_by": "2026-10-01"
    }
  ]
}
