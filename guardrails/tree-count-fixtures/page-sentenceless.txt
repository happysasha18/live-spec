# The fixture page that lost the sentence its template renders

This page is page.txt with the sentence home's number changed by hand: it says six lines where the
fixture tree holds five. The generated block is untouched, so the drift arm passes and the sentence
arm is the one that reds. A test copies it over page.txt and requires the gate to red.

<!-- generated:count:fixture-lines — scripts/gen-tree-counts.py owns the block below -->

The fixture tree holds 5 lines of text across its files. Count them yourself with `cat tree/*.txt | wc -l`; a run of the gate over this fixture is refused where that command disagrees with the number printed here. The number decides how much text a fixture run reads, and it is read against the two files this fixture tree ships. It holds at five lines, and the direction to hold is down: when it rises, every fixture run reads more text than the case needs.

<!-- /generated:count:fixture-lines -->

Six lines stand in the fixture tree.

More than three lines stand in the fixture tree.
