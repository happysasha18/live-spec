# live-spec Roadmap (dated version: 2026-08-27 — updated at every edit; SPEC M-3)

The wish queue: the live record of what is asked of the product and where each ask stands. A wish is a
request for a change the product does not yet carry, and it lands when the delivery that completes it
ships. Intake is continuous, a wish entering the moment it is spoken; execution runs at most three independent
landings at once — the lane cap — and a landing finishes before a colliding next starts.

The roadmap is a member of the format family. Its shared rules — the closed-vocabulary glossary, the
keyword form, the no-capitals rule, the trailing code anchor, the no-history law, the comprehension
gate — live once in `docs/spec-format.md` and hold here unchanged. Its own rules — the row shape, the
status and class vocabularies, the live-body law, the row lint — are defined in `docs/roadmap-format.md`.
The class cell names the wish's size, one vocabulary shared with the spec: *bug*, *small*, *surface*,
or *large*. The wish cell carries a priority mark when the wish's priority is other than normal. The status cell carries one of *queued*, *ready*,
*in-work*, *deferred*, or *far*, each with its date, a *deferred* row naming its revisit trigger. A row
reads *ready* once its task statement has passed validation and its wording is frozen. The status cell is the sole authority on a row's current state; the wish and acceptance cells carry
the ask and its criteria. Pre-conversion prose in those cells keeps its old words verbatim —
capitals and old state words included — and lowers as rows are edited; a bold landed or open there
is history, and the status cell alone says where the row stands. The pre-conversion status texts of the rows that stayed live are kept verbatim
in docs/queue-archive/status-notes-ROADMAP-2026-07-23.md. A
bracket code such as `[INV-277]` points to its home in `PRODUCT_SPEC.md`; a reader may ignore it.
Two bracket marks read as part of their sentence: `[target]` marks a feature or leg that is promised
but not yet built, owed an open row here (SPEC S-0), and `[default: …]` names a value the agent set
that the human may retune.

The body below is the live queue, one row per open wish, the rows in ascending id order. When a wish
reaches a terminal exit it moves verbatim to the month's archive under `docs/queue-archive/` in the
commit that closes it, and the rotated-manifest block records the move.

<!-- rotated-manifest -->
Rotated closed rows (base rule 10 — nothing lost; the archive keeps every moved row, grepable by number; the live queue below holds live material):
- rows 14, 27, 33, 42, 43, 62, 63, 67, 101, 121, 172, 189, 194, 196, 200, 201, 202 → docs/queue-archive/rotated-ROADMAP-2026-07-18.md
- rows 47, 59, 64, 99, 107, 109, 110, 115, 128, 130, 135, 136, 137, 138, 139, 145, 149, 150, 151, 152, 154, 155, 156, 157, 158, 159, 160, 161, 162, 188, 195, 209, 210, 211, 212, 213, 214, 216, 218, 219, 222, 223, 224, 225, 226, 227, 228, 232, 233, 237, 239, 240, 242, 244, 245, 246, 248, 249, 250, 251, 252, 253, 254, 255, 256, 257, 258, 259, 262, 263, 264, 265, 266, 267, 268, 269, 270, 271, 272, 273, 274, 275, 276, 277, 278, 279, 280, 281, 282, 283, 284, 285, 286, 287, 288, 289, 290, 291, 292, 293, 294, 295, 296, 297, 298, 299, 300, 301, 303, 304, 305, 306, 310, 311, 312, 313, 314, 315, 316, 317, 318, 319, 320, 321, 322, 323, 324, 325, 326, 327, 328, 329, 330, 331, 333, 334, 335, 336, 337, 338, 339, 340, 341, 342, 343, 344, 345, 346, 347, 348, 349, 350, 351, 352, 353, 354, 355, 356, 357, 358, 359, 360, 361, 362, 363, 364, 365, 366, 367, 368, 369, 370, 371, 372, 373, 374, 375, 376, 377, 378, 379, 380, 382, 383, 384, 387, 388, 390, 391, 392, 393, 394, 395, 397, 402, 403, 406, 407, 408, 409, 413, 414, 415, 416, 417, 418, 419, 420, 422, 423, 429, 430, 431, 433, 434, 438, 439, 441, 442, 443, 444, 445, 456, 461, 462, 463, 464, 468, 470, 476, 477, 478, 480, 482, 494, 495, 502, 506 → docs/queue-archive/rotated-ROADMAP-2026-07.md
- rows 522, 549, 555, 556, 557, 565, 569, 571, 572, 573, 574, 577, 602, 618, 619, 626, 700 → docs/queue-archive/rotated-ROADMAP-2026-08.md
- rows 55, 69, 197, 198, 199, 302, 307, 308, 309, 332, 389, 401, 425, 426, 428, 432, 435, 446, 447, 448, 449, 450, 451, 452, 465, 466, 467, 472, 473, 474, 475, 483, 498, 499, 500, 505, 508, 512, 513, 514, 515, 516, 518, 519, 520, 521, 523, 524, 526, 528, 530, 532, 533, 534, 535, 538, 539, 540, 541, 543, 544, 545, 546, 547, 548, 551, 553, 559, 560, 561, 562, 563, 564, 578, 579, 580, 585, 587, 594, 599, 600, 601, 603, 604, 606, 607, 613, 614, 615, 616, 620, 621, 622, 750 → docs/queue-archive/rotated-ROADMAP-2026-08-27-provenance-purge.md
<!-- /rotated-manifest -->

| # | Wish (plain words) | Class | Status | Decision / acceptance |
|---|---|---|---|---|
| 44 | Learn from other projects/frameworks — study how others run spec-driven / agent-driven development, harvest ideas worth folding (Alexander 2026-07-05: learn from other projects — but that is its own separate version bump) | large | *deferred* 2026-07-05 — revisit trigger: taken up as its own version bump on the human's word | Two research reports in: `docs/research/2026-07-05-methods.md` (spec-kit/BMAD/Kiro/Shape Up/Google/Amazon — top-10 candidates incl. regression-fence sentences, waived-risk verdict, non-goals, facet curation) + `docs/research/2026-07-05-skill-patterns.md` (Anthropic/superpowers/Trail of Bits — top-10 incl. when-NOT-to-use sections, skill evals, loadability check). Candidates go to Alexander on a decision page 2026-07-05; his picks become queue rows. Revisit trigger unchanged: lands as its own MINOR |
| 48 | Maintenance skill with measurement PLUGINS — after shipping, the product is MEASURED: e.g. a Google Analytics plugin; each user story / canonical axis states how (or explicitly whether) it is measured in the field; findings feed back into the queue (Alexander 2026-07-05) | large | *deferred* 2026-07-05 — revisit trigger: taken up as its own version bump on the human's word | Revisit trigger: first host with a live audience; design pairs with row 47 (feedback in → measurement in) · groups as one queue bundle with rows 49, 96 — measuring the product after shipping and running two-variant experiments on it |
| 49 | A/B tests as a pack capability — for software hosts (not every host: a book has none), a wish may land as an EXPERIMENT with two variants and a measured pick; builds on row 48's measurement layer (Alexander 2026-07-05) | large | *deferred* 2026-07-05 — revisit trigger: taken up as its own version bump on the human's word | Revisit trigger: row 48 lands; spec must state where experiments live in the wish lifecycle |
| 54 | Onboarding step — "understand who you're working with": at attach (or first run on a new machine/human) the pack builds or loads the human's profile — the human tells about themselves and/or agrees to a data pull; nothing written without their word (INV-9 holds) (Alexander 2026-07-05) | surface | *deferred* 2026-07-07 — revisit trigger: first real run rides the next real founding/adoption | Done when: ADOPT/bootstrap gains the step with a done-when — MET; profile template exists — MET; first real run recorded — OPEN (the row stays open on its field leg) · groups as one queue bundle with rows 129, 190, 236, 421, 488, 496 — how a project knows what it is: its kind, its engine/instance split, the tech it declares, what it can change without a rebuild, who it is founded for |
| 93 | Design-sync wiring (E-18 [target] machine): base default + host-profile switch vocabulary + communicator/verify channel lines + first real run on a visual host; lands with/after row 86 (work-kind axis) | surface | *deferred* 2026-07-05 — revisit trigger: the machine's FIRST REAL SYNC on a visual host (track-coach/tlvphoto window) through Alexander's gate | Done when: E-18 loses its [target]; a real host synced with the human's gate shown working |
| 95 | Marathon phase — play, watch, learn (Alexander 2026-07-05 evening, back from the sunset: this is a marathon not a sprint — play with the projects, test the agent out, watch how he runs projects): NO version race — 1.0 has no date; the pack proves itself on real project play; Fable OBSERVES how Alexander leads the different windows (read-only: their journals/transcripts) and folds what it learns as inbox wishes/rows | large | *queued* 2026-07-05 | Done when: it never is — this row is the phase's charter; concrete lessons land as their own rows |
| 96 | Feedback loops, raised (his word tonight — we talked about somehow building feedback loops — + INV-21's success measures now wait on a reading machine): the feedback → measurement → A/B family (row 47 landed to the archive; rows 48-49 deferred) moves from the deferred tail to next-up after the tlvphoto lessons | surface | *queued* 2026-07-05 | Done when: rows 47-49 re-scoped against INV-21 and the first feedback loop runs on a real project |
| 100 | **The problem ledger** (Alexander 2026-07-05 ~23:00, watching tlvphoto transcripts: if there's a problem, especially a recurring one, it has to be solved — solved!! Either solve the problem or agree that it isn't one; this should never happen at all): recurring OPERATIONAL noise — flaky test harness, missing deps, environment errors, anything that crashes mid-work — gets a per-host DYNAMIC LIST; an entry seen a SECOND time gets an owner that moment: a queue row (solve it) or an explicit dated "agreed non-problem" with the human's word; a third silent recurrence of a listed signature is a defect of the METHOD itself, regardless of the day. Distinct from the recurring-BUG rule (that covers the product; this covers the workshop). Evidence at intake: tlvphoto's "element not clickable: #ex-skip" ×5, "readyState=complete" timeout in two different sessions, missing PIL — all retried, none owned | surface | *deferred* 2026-07-05 — revisit trigger: the first foreign-host run | Done when: the ledger's home + shape spec'd and proven — MET; the second-occurrence law in the base skill — MET; the pack's own ledger opened with tonight's live entries — MET; first real host run on tlvphoto/track-coach — REMAINS, rides those windows (this window is fenced to one inbox file per host) |
| 108 | **A feature is interrogated for PRODUCT FIT at intake — a minimal prover on the feature itself** (Alexander 2026-07-06 ~00:25, at night, with the tlvphoto evidence pasted in: a bunch of questions never get asked when a feature is accepted into the system — not as heavy as BMAD, but a minimal prover on the feature is needed — asking about various aspects, how it fits the product's context (navigation for UX, flows for backend) — the agent doesn't even think of this on its own — maybe live-spec doesn't structure the process well enough, because a bunch of feature-adoption questions go unasked): today's facet sweep asks DEVICE facets (phone/touch/empty/a11y/perf, T-13) but nobody asks the PRODUCT-FIT questions — how the feature sits in the user's JOURNEY (his door example: enter → browse → re-enter → stuck at the 10th picture — a navigation loop nobody specced), what neighbouring behaviours it IMPLIES (no-repeat across doors ⇒ state tracking), what the FEEL owes (premium, animation smoothness), what the next feature it invites is (the greeting). The wish: a kind-scaled FIT interrogation at the spec step — product/UX kind asks journey · navigation · continuity-state · feel · invited-next; infra/backend kind asks flows · data lifecycle · failure paths — each answer becoming a spec sentence or a batched question, same law as the facet sweep (INV-18). His five tlvphoto comments forwarded to tlvphoto/inbox same hour (their window owns the fixes; ours owns the METHOD hole) | surface | *deferred* 2026-07-06 — revisit trigger: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, so his one command completes the sync) | Done when: the fit-lens list spec'd kind-by-kind in one home (spec-author's sweep section widened, prover gains the FEATURE-FIT focused mode the way CROSS-LINK exists); every fit answer lands as spec sentence / [default] / batched question, never silently; first real run on the next product-kind wish in any adopted host; suite covers the shipped clause · groups as one queue bundle with rows 117 — asking whether a new feature actually fits the product, at the moment it is accepted and again when it is checked |
| 117 | **Verify-by-deed learns to walk the visit and watch the feel** (tlvphoto inbox 2026-07-06 ~10:10, §1 + Alexander's morning word let's account for that): the product kind's step 8 gains a named VISITOR WALK — the whole journey as the person lives it (first visit · return · cross-entry · from-any-point navigation · exits) — and a FEEL pass: motion quality (easing, duration, choreography) and affordance craft judged against the approved prototype's bar; findings become rows, never vibes. Evidence: tlvphoto's transitions and its open-work button shipped green through the full pipeline and read cheap on sight. Kin: row 108 owns the intake half (journey questions at spec time); this row owns the verify half | surface | *deferred* 2026-07-06 — revisit trigger: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, so his one command completes the sync) | Done when: visitor-walk + feel pass spec'd into step 8's product-kind form (one home); prover/eval cover the clause; first real run on a product-kind landing in an adopted host; inbox file archived with a pointer at landing |
| 118 | **A taste default may not outlive two landings unreviewed** (tlvphoto inbox 2026-07-06 ~10:10, §2): the [default]-tag is right for velocity — the lane never blocks — but nothing closes the loop: tlvphoto carries 8+ open defaults across two landings and the product reads unfinished everywhere. The law: a landing report LISTS its [default]s; the next human review resolves them (one decision page, communicator rule 10); a default surviving two landings without the human's word is flagged loudly by the gate/report | small | *deferred* 2026-07-06 — revisit trigger: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, so his one command completes the sync) | Done when: the expiry law spec'd (count home + flag mechanics chosen); communicator's decision-page rule cites it; suite covers; tlvphoto's open defaults become the first real decision page after their window adopts the clause · groups as one queue bundle with rows 119, 131, 134, 141, 144 — how the working session sounds and reports as it runs — the clock, the chat rules, the task list, decision cards |
| 119 | **A decision card asks in consequences, leaving mechanisms aside** (Alexander 2026-07-06 ~10:40: what you wrote above is understandable, but what you gave me in the HTML is not!!! — the shell-separator card explained the mechanism and he could not tell what he was deciding, twice): every card opens with what the choice CHANGES for the person under each option, in the product's words; options labelled by consequence; mechanism only if it helps | small | *deferred* 2026-07-06 — revisit trigger: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, so his one command completes the sync) | Done when: INV-32 spec'd; communicator rule 10 opens cards with consequences; suite covers; the next decision page written under it |
| 129 | **A project knows its own KIND, and the kind evolves** (Alexander 2026-07-06 ~13:27, before the session-13 wipe: you need to understand (and update as needed) what kind of project this is — whether it's a book being written, or just a backend, or just a static site, or a fullstack app — it matters to know this and keep updating it as things evolve, custom kinds explicitly expected): today the pack classifies each WISH (product/infra/skill/prose, T-16) and a host may set one intake default (`work-kind.host-default`, base defaults table) — but nothing states the PROJECT's own kind (a book · a backend service · a static site · a fullstack app · a CLI · a skill pack · a custom kind), asks it at founding/adoption beside the personal-vs-reusable question, records it in ONE home (the host profile), and lets it scale which steps, facets and feel-lenses apply by default across the whole project; and nothing RE-CHECKS it as the project evolves — a static site that grows a backend must update the line on the human's word, journaled, the moment the evolution is noticed, ahead of any audit. Custom kinds join the same way every table grows: through the queue | surface | *deferred* 2026-07-06 — revisit trigger: the first real adopted host carries a project.kind line by deed | Done when: the project-kind line spec'd (one home: host profile; asked at founding/adopt; re-checked on evolution with a journaled update on the human's word) — MET (SPEC INV-36); the kind vocabulary + custom-kind path stated — MET; composition with T-16 stated in one sentence — MET (three verdicts, one intake breath); suite covers the shipped clauses — MET (M-125, red-proven); the pack's own repo carries the line by deed — MET (`.live-spec/profile.md`); one real host — OPEN LEG |
| 131 | **Work is narrated while it runs** (Alexander 2026-07-06 ~13:57, resuming session 15: don't forget (this was already asked) to report as work happens too — I need that to actually work — it should be written into the project's communication rules as well — the SECOND ask in one day; the morning's word had landed only as a personal-profile line (chat.working-narration), and a habit held only in a personal profile did not carry across sessions, so the law moves into the pack): between the capture echo and the landing report the lane never goes silent — each beat worth a sentence (a pipeline station passed, a load-bearing find, a change of direction) is said as it happens, one-two plain sentences in the roadmap's terms, the same voice as the reports; the mechanical grind stays quiet; a narration line is chat only — no pre-report walk, no questions, the plain-language and bookkeeping laws still bind | small | *deferred* 2026-07-06 — revisit trigger: the next working session narrates unprompted, with no third ask | Done when: SPEC states the law (INV-35) — MET; communicator carries the narration rule (one home) — MET (rule 13); suite covers both — MET; the next working session narrates unprompted — PENDING, the open leg |
| 133 | **"Show me all the features" — the feature map readable on demand** (Alexander 2026-07-06 ~15:52, mid-landing of rows 129/132: maybe we need some kind of commands like "show all features" or something — some kind of transparency): the map now exists as law (spec scenarios + architecture nodes + the rows' `map:` notes, INV-37) but only the agent reads it — a spoken command should render it FOR THE HUMAN: every feature by its plain name, its state (shipped / in-work / queued / idea), what changed lately, in the product's words; likely one rendered page per ask (the render-doc road), kin to the departures board (INV-27 shows in-flight wishes; this shows the WHOLE product) | surface | *deferred* 2026-07-06 — revisit trigger: the law forbids firing uninvited, so the first real post-law ask rides his next "show me all the features" ask | Done when: the command's trigger + the map-reading's form spec'd (one home) — MET; a real "show me all the features" ask in any window renders the map and he can tell shipped from queued at a glance — PENDING, the open leg; suite covers the shipped clause — MET. **LANDED (legs 1+3) 2026-07-06 ~16:27, session 17** (SPEC v0.15.27 — new scenario "Asking what the product does", INV-38: read at ask-time off spec scenarios + the header at the promised-tag's own granularity + the queue's open rows, queued NEW-verdict wishes included; no third document; chat by default, rendered page on his word [default]; never fires uninvited; empty host answered honestly; communicator 0.1.22 rule 14 + when-it-fires arm (f); architecture: assignment only, INV-38 → communicator; matrix M-127 · `test_feature_map_on_demand` red first (proven against the pre-edit communicator), then green; prover CROSS-LINK `docs/prover/2026-07-06-row133.md` — F1 statuses at the promised-tag's granularity (must-fix, folded in-pass), F2 queued NEW wishes were invisible to a scenarios-only read (folded in-pass); fences held: INV-27 board scope · INV-37 intake placement · E-14 no-third-document; stood down by name: design-sync — skill kind; non-goals: no standing feature document, no auto-dashboard, no per-feature history. **OPEN LEG (INV-26): the law forbids firing uninvited, so the first real post-law ask rides HIS next "show me all the features", any window**) |
| 134 | **The chat clock gets a mechanical hand** (problem-ledger CHAT variant, re-opened 2026-07-06 15:50, session 16 — the next-session recurrence its 14:18 note named: leads extrapolated up to ~6 min ahead again while the wall clock read 15:50; the habit + the shipped sentence did not hold across sessions, third session in a row ⇒ METHOD defect by the ledger's own law): the reply's leading [HH:MM] stops being handwritten — a harness hook (candidate: a session hook that injects the wall clock into every reply context, wired via settings.json) makes the stamp read off the machine — the hand no longer sets it | small | *deferred* 2026-07-06 — revisit trigger: the ledger entry flips back to SOLVED citing this row | Done when: the hook exists — MET; installed on this machine (Alexander's `!` hand if the classifier blocks the agent's — it did; his command pending) — OPEN; a full session's leads carry machine-read times with zero drift catches — OPEN, next session's evidence; the ledger entry flips back to SOLVED citing this row — OPEN, rides the zero-drift session |
| 140 | **Budget heuristics — what the method sheds when money or time run short** (Alexander 2026-07-06 ~20:23, session 19: right now I'm on a great plan, money isn't running out — but if it were running out (and time isn't infinite either), what would we do? maybe run tests selectively or economize on something else; heuristics are needed): today the pack runs full rigor always (full suite before push, prover at every gate, senior model on judgment); the wish: a named ECONOMY ladder — which savings are legal at which pressure (selective test runs scoped by touched node, CROSS-LINK instead of FULL prover, cheaper worker tiers, batched landings, narration unchanged — it is cheap) and which rungs NEVER bend (the door law and its tripwires, red-before-fix, the human's gates, the landing report) — so a tight budget is a setting on the ladder, chosen deliberately rather than improvised | small | *deferred* 2026-07-06 — revisit trigger: applied once for real — a budget-named session names its cuts aloud | Done when: the economy ladder lives in one home with its never-bend list — MET; suite covers the shipped clause — MET; applied once for real — a budget-named session names its cuts aloud — OPEN (awaits the first session he names a rung for) · groups as one queue bundle with rows 568, 570, 575, 576, 584 — measuring what the method itself costs in time, context and tokens, and cutting the waste |
| 141 | **The chat laws get a mechanical voice in every window** (Alexander 2026-07-06 ~20:41, session 19, on tlvphoto pasting a new EX-SHARE section... before the wall's tombstone" into his chat: "can you actually do something about communication??"; the day's audit found the same class twice more — raw plan codes in two other windows): the language law (INV-28) and the narration law (INV-35) live in skills a session may never load — so the same prompt-hook mechanism as the clock (row 134) injects ONE reminder line into every prompt on the machine: plain words do the talking, codes only trail; beats name wish+station, station ends digest, silence past ~10 min owes a heartbeat; the skills stay the laws' homes — the hook reminds, never legislates | small | *deferred* 2026-07-06 — revisit trigger: the field evidence (a following tlvphoto session with no leading codes) | Done when: the reminder script exists and its line carries both laws' teeth — MET; one installer covers clock + laws — MET; suite covers the shipped strings — MET; installed on this machine by his hand — OPEN; the field evidence (a following tlvphoto session with no leading codes) — OPEN |
| 143 | **The architecture step owes measurable quality criteria** (Alexander 2026-07-06 ~21:03, session 20, on tlvphoto's Room first image loading long with no loader and no timing numbers — his question: what happened, did the architect not set criteria? how was measuring timings never even planned? if so the written architecture is poor): today the architecture step pins nodes and seams but demands NO measurable non-functional criteria, so a user-facing slowness is found only when the human notices it, ahead of any check; the wish: the architecture step and the spec's performance facet must produce measurable budgets (e.g. first image ≤ N s cold) plus a timing-instrumentation home for every user-facing surface, and the story's acceptance asserts the budgets; tlvphoto's own half rides its inbox wish (filed 2026-07-06 ~21:04) | small | *deferred* 2026-07-06 — revisit trigger: re-read the wish's record in the status notes | Done when: build-pipeline's architecture step + spec-author's facet sweep name the budget-and-instrumentation duty; suite covers the shipped clause; first real use — one host's architecture doc carries asserted budgets · groups as one queue bundle with rows 215, 206, 234, 386, 411, 412, 481, 504, 505 — actually running several independent pieces of work at once — branches, worktrees, the three-lane cap, staleness, orchestration guidance |
| 144 | **The task list on his screen speaks plain words** (Alexander 2026-07-06 ~21:22, session 20, sending a screenshot of the session's task list: a thing like this strongly helps to see what is happening — if only it communicated in understandable words; the visible list read "Row 142: prove (CROSS-LINK), matrix M-022/M-129, tests red→green"): the harness task list is a human-facing surface the language law already claims (INV-28) but no rule NAMES it — task subjects and spinner lines must speak plain product words in the chat language, codes only trailing; applied live the same minute (the open tasks renamed), the rule still needs its home in communicator's board family so it survives sessions | small | *deferred* 2026-07-06 — revisit trigger: re-read the wish's record in the status notes | Done when: communicator names the task list among the language law's surfaces; suite covers the shipped sentence; a following session's task list reads plain by his eye |
| 148 | **The pack's spec reads like its craft wrote it** (Alexander 2026-07-06 ~23:24: make the specs readable, as if a human wrote or at least edited them — a product person, an architect, and so on; right now the specs are written for the agent; put it at the end of the queue, or earlier if it's easy): the human-first SHAPE law already binds (spec-author's "How it reads"; the craft ladder INV-33) — this row makes the pack's own SPEC.md walk it BY DEED: a readability rewrite, section by section, in each section's craft voice (scenarios talk like a person, sentences breathe, codes stay trailing), the anchor-set guard held (identical anchor sets before/after, any delta named), FULL prover re-prove after the reshape; **entry: mockup-first** (INV-43's door arm, declared at intake) — a rendered before/after sample of TWO sections goes to his morning eye FIRST, his taste is the bar, the whole-doc rewrite runs only after his word on the sample; **sample rounds 1–2 KILLED; round 3 STRUCTURE APPROVED, language killed** (round 2 killed ~07:59: rule-book genre out, bar = USE-CASE genre per BMAD+Kiro; round 3 ~08:11: genre stands, three language kills — scissors frame banned FOREVER globally (profile law `language.no-scissors`), overcompressed narrative, non-native English (`language.native-english`); round 4 = plain-English rewrite of the same page, in place; **round 4 ANSWERED ~09:34 (JSON archived): genre+language APPROVED with three adjustments — pre/postconditions stated where they exist · an EPIC grouping proposed · a SERIOUS VALIDATION with a thorough plan BEFORE the whole-doc rewrite (the genre must not break the prover, the anchors, or the mechanical checks — his extremely important, check it immediately); criteria voice = plain sentences, the WHEN/THEN/SHALL caps KILLED (pseudo-dramatic tone)**; kill-list on the decisions page) · **WIDENED 2026-07-27 by the 2026-07-24 self-read** (Alexander feared the format family bought compaction and left readability behind, and asked for a fresh reader on our own spec; a cold reader with no context read four real requirement bodies and confirmed the fear with quotes): the front door, the overview, the spec preamble and the glossary read fine for a stranger, and the requirement BODIES are uneven. A body whose context grounds its load-bearing noun before using it reads (R194 the published-contract read, R236 is the counter-case); a body leaning on bare role-nouns fails a stranger (R88 worktree isolation, R236 touchpoint kinds), and the tells repeat: a role-noun used bare, a named net dropped in with no introduction, a verb coined from a colour, and a verbless fragment. One lever carries the whole movement — each requirement grounds its load-bearing noun once in its own context, and the verbless fragments go — run worst-first, starting at R88, two clean cold reads per section. Siblings already queued from the earlier pilot: rows 448-452. The movement doubles as rehearsal for the tlvphotos migration, which needs the same craft | surface | *queued* 2026-07-06 | Done when: the sample rendered and his word taken on it; the full pass lands with anchor sets identical (or every delta named); FULL prover re-prove green; his eye says a section reads human; and each converted section's bodies pass two consecutive cold reads with zero blocking stops, R88 first · groups as one queue bundle with rows 204, 208 — making the spec and its documents read as if a person, not just an agent, wrote them |
| 163 | **test-author skill — the test methodology extracted into the pack** (inbox wish from the track-coach close, 2026-07-05 late night, born of his direct question whether tests-by-the-method was truly done; the NOT-done half): the method that rebuilt track-coach's suite — matrix organized architecture-node × spec-fact, a pinned assertion-level ladder per fact, real-artifact assertions over string checks, red-first, pinned skip-set, traceability enforcement — lives only in track-coach's TEST_MATRIX header prose, its overhaul inventories, and two build-pipeline paragraphs (steps 4–5); extract it into a **test-author skill** invoked by build-pipeline at steps 4–5 the way steps 1–2 invoke spec-author and product-prover; donor material named in the wish: cross-audit F11 charter sketch, track-coach TEST_MATRIX.md as worked exemplar, s34/s52 inventories as the classification scheme, test_traceability.py as the enforcement pattern; field urgency dated 2026-07-02: two user-visible bugs slipped past ~660 string-only tests — the exact class the ladder prevents | surface | *queued* 2026-07-05 | Done when: the skill authored + versioned + loadability-gated; build-pipeline steps 4–5 invoke it by name (one home for the method, pipeline cites); SPEC + matrix + suite cover the wiring; skill-creator walk at birth recorded; first real use on a host recorded |
| 165 | **Skill discovery — search for applicable skills at setup and at every struggle** (Alexander 2026-07-07 ~08:28, from the promoter-window audit: five review rounds died on scissors/voice failures that a PUBLIC skill — stop-slop, shipped months earlier — already carried as a written checklist («Any "not X, it's Y" contrasts? State Y directly»); his word: there should be a skill search for the project, especially in a struggle — maybe this could be written into live-spec-base — or maybe it's not only about the spec): the pack gains a skill-search move with two triggers — (a) at project SETUP (founding / adoption, beside the founding questions): scan installed and available skills for ones matching the project's kind and its crafts, propose the fit list with a recommendation; (b) on STRUGGLE — a problem-ledger entry reaching its second occurrence, a taste artifact rejected twice, any recurring failure family: before the next attempt, search whether an existing skill or checklist already owns this failure class, and adopt or reject it by name (the promoter's stop-slop case is the type specimen); adoption practice rides the row (his 08:32 ask on how to borrow without stealing): prefer INVOKING a found skill as-is; fold its lessons by paraphrase plus a named credit line; verbatim text only under its license (MIT-class, with the notice kept) — the pack never republishes unlicensed text | surface | *deferred* 2026-07-07 — revisit trigger: first real struggle run | Done when: the trigger pair + the search's form live in one home, the setup arm rides ADOPT/bootstrap beside the founding questions, the struggle arm cites the problem ledger's second-occurrence law; suite covers the shipped clauses; first real run recorded — a struggling session finds and adopts or rejects a skill by name |
| 166 | **The live work board — what the agent is doing, visible at every moment** (Alexander 2026-07-07 ~09:36, his out-of-turn word: the reporting along the way is apparently bad — he wants it to be clear what the agent is doing at every moment, ideally the whole process, always; can this be made a priority?): a standing visible surface that always shows the CURRENT picture — which task is in hand and at which pipeline station, which workers run with which briefs, which lanes roll or wait, the last landing, the queue head, and what waits on the human — the departures board (INV-27) shows in-flight wishes on ask; this shows the WHOLE working process, always, updated at every station change | surface | *queued* 2026-08-07 | Done when: the board's form + update moments spec'd (one home); the mockup approved and frozen as the norm; the surface ships with its registry row and rendered-level matrix rows; a session updates it at every station change, checked by his eye over one real stretch. Cheap first leg taken 2026-07-27 (Alexander: where did the plans and the progress tables go): the session's own task list is kept live for the whole movement, and the report carries a done / in-work / waiting table, which is the visible half this row owes while the standing surface waits its turn. Widened on Alexander's ask of 2026-08-06 that the board show what is running and what was done: the board also carries the record of completed work, a tag per row saying what kind of work it was, the steps inside a row each naming its own outcome, and the name of the worker that ran each step. Widened again on his word of 2026-08-06 ~19:17 (on record in DECISIONS.md): every task carries an understandable name, an understandable description and an understandable plan; approved wording freezes, and the seat speaks it letter for letter at take-up, along the way and at the close; a validation step stands in front of the board, and nothing enters work before it passes. Widened a third time on his word of 2026-08-06 ~19:28 (on record in DECISIONS.md): every task carries the time the seat estimated for it and the time it actually took, the estimate written at take-up and the pair at the close. Widened a fourth time on his word of 2026-08-06 ~20:16 (on record in DECISIONS.md): the board is a kanban over the whole queue — every task done, in progress or queued shows as a card; each card carries the name, the behavior description, the estimate, a tag naming its feature or its cross-module reach, and a plan marking what can run in parallel under the budget; in-work cards name the worker on them, and recurring worker crafts carry stable names and icons. Widened a fifth time on his word of 2026-08-06 ~20:35 (on record in DECISIONS.md): approvals stay rare and transparency is the standing ask; the short name is a super-sharp semantic handle a reader recognizes every time the ticket is referred, then the description of behavior, then the other details; and each task names and links the product-spec part it changes. Widened a sixth time on his word of 2026-08-06 ~20:36 (on record in DECISIONS.md), rejecting the variant-4 tiles as text to crack rather than tasks: a card reads as a task at a glance — authored handle, behavior line, chips, details behind the card; the in-work area splits into lanes for parallel agents; each task's details name its branch and worktree. Widened further on his words of 2026-08-06 ~20:47, ~21:00 and ~21:16 (all on record in DECISIONS.md): the variant-5 form holds with four amendments and the look froze as the norm; the whole backlog becomes readable on the board (row 566 carries that campaign), the done pile folds away, in-card step marks are emoticons, and step comments read plainly; a card's plan is a few deliverables in the agile sense, never an activity log, with one marker per line, no legends and no internal numbering — and the kanban study's five adoptions (an exit rule per column, a blocked mark with its clock, the named commitment point, cycle time and throughput, vertical-slice deliverables) fold into the same requirement. Widened on his words of 2026-08-06 ~21:34 and ~21:35 (on record in DECISIONS.md): the stage ladder gains inception between the backlog and ready — validation is inception's exit, and exploration work is inception's product, never an execution subtask; the columns re-map to backlog, inception, ready, in work, done; subtasks with detail are collapsible; and every new wish is placed into the maintained task graph — its relations, dependencies and parallelism — at arrival, so the seat always plans the best implementation path. Widened on his word of 2026-08-06 ~21:49 (on record in DECISIONS.md): a task in work absorbs new words at any stage — each widening dated, the wording re-validated on change, the count shown on the card; every task offers its dated word-history on demand behind the card; a kanban-tools display study feeds the page's next revision; and the full adversarial review of this specification by a fresh reader stands as inception's next product, ahead of any build. Intake 2026-08-06 — door: feature · kind: product · footprint: cross-cutting (a new surface plus a standing update law every session holds) · map: a new feature beside the departures board (INV-27) · groups as one queue bundle with rows 566 — one visible board showing every queued wish in plain, board-ready language |
| 168 | **The showing channel adapts to the session's seat** (Alexander 2026-07-07 ~10:57: if the session runs locally it's HTML, but if the agent runs remotely, it comes out in Claude in the browser, and then it's not HTML but a different format — can this be detected and adapted to?): a locally-seated session shows rendered artifacts as local HTML opened in a new browser window (today's law); a remotely-seated session (Claude Code in the cloud/browser) cannot open local windows — the same content goes through the remote seat's own channel: an artifact page the host renders in the human's browser, or the chat itself — same passport, same commentable round-trip, same kill-list discipline. The session DETECTS its seat from environment markers (platform, display, filesystem reach) and SAYS which channel it picked; a local file path handed to a remote reader is a defect of the exchange, exactly like an unopened window | surface | *deferred* 2026-07-07 — revisit trigger: the first real remote-seat session | Done when: the seat rule in one home — MET; the profile's show line marked as the local arm — MET; first real remote-seat session shows through its channel and says so — OPEN (rides the first cloud-seated session) · Alexander re-raised 2026-07-08 ~09:07: does the session actually KNOW it is remote, and how do we communicate then? — the same open field leg, sharpened: seat-detection is spec'd (environment markers) but unverified in a real cloud seat, and today's session is local, so it cannot be proven from here · groups as one queue bundle with rows 171, 247, 261, 385, 389, 396, 398, 399, 400, 401, 404, 405, 410, 424 — agents and hosts talking to each other across projects and machines — transport, contracts, remote intake, adapting the display to the seat |
| 170 | A mechanical pre-show lint at the presentation gate (communicator skill): before any text/doc is shown or rendered for a human, scan the human-facing text for BANNED constructs and refuse-with-flag so the author rewrites positively before the human sees it (a warning to clear, never a silent auto-rewrite). First pattern set: the scissors/negation-contrast family — a neighbour denied after a dash or a comma, the deny-then-assert shape, and the Russian negation-then-replacement forms — a GLOBAL PERMANENT profile ban (`language.no-scissors`) that keeps leaking (~7 leaks in two days, memory-alone isn't holding it), run over EN + RU, tuned against real text (the mechanical detector now ships in `scripts/spec-style-lint.py`, catching all four shapes). SIBLING pattern to fold in: coined doc-metaphor titles on human-facing pages (the board's "showcase/benchmark/push-it-through" class, 2026-07-07). (inbox wish from the tlvphoto window, 2026-07-07, per the inbox exception) | surface | *queued* 2026-07-10 | Done when: communicator's "before you show" walk carries the lint with a tunable pattern set (scissors first, coined-metaphor titles folded), red-proven on real leaking text, a warning the author clears; runs EN + RU |
| 171 | **The pack works for a host with no hooks and no GitHub** (Alexander 2026-07-08 ~09:07): today the pack leans on Claude Code session hooks (the chat clock, the chat-law reminder, the skill-search prompt) and on git/GitHub (the push gate, the CI mirror, directory submission). A host that has neither — no hook support, no remote, maybe no git at all — must still get the method whole: the pipeline, the living documents, the review round-trip. Work out which parts are the CORE method that is always on, and which are OPTIONAL machinery that degrades gracefully when its host support is absent; and make the pack SAY plainly what it turned off and why, never silently assume a hook fired or a remote exists | large | *queued* 2026-07-08 | Done when: the pipeline runs end-to-end on a host with no session hooks and no GitHub (git optional), each disabled capability named with its degraded fallback, red-proven that the core method still lands a wish |
| 190 | **The method proposes the engine/instance split and knows how to lead a split project** (Alexander 2026-07-09 late evening: propose separating engine from content BY DEFAULT — the same engine later hangs Bangkok photos or anything else; how do we run projects where the engine is a public repo and the content/infra private?): at founding/adoption, when the product is an instance of a reusable mechanism (his standing reuse answer), the kind question PROPOSES the split — engine repo (public, generic fixtures, content contract per INV-79) + instance repo/home (content, corrections, private fragments); the method states how the pack attaches to a PAIR (which repo carries which documents, one queue or two, where the wish door lives, how a lesson travels engine↔instance), and the placement view says where heavy binary content lives (a git repo of images is the wrong home — object storage / the machine's archive + backup, named per project) | surface | *deferred* 2026-07-09 — revisit trigger: validated against the tlvphoto/engine case read-only | Done when: founding/adoption carry the split proposal + the pair-attachment shape — LANDED 2026-07-10 night (INV-85/86, F-pair scenario, D-6/D-7 open with picks); the content-placement guidance names the binary-storage homes — FIRST LEG LANDED 2026-07-10 ~00:10 (the template's placement view now PROMPTS the deriving agent: heavy binary content names its home, a git repo of large binaries is the wrong home — so a project's own agent reaches the storage answer itself, per his word: teach the skill, never write it into their tree from here); red-proven; validated against the tlvphoto/engine case read-only — OPEN |
| 191 | **Check the field's TEST norms against our test method** (Alexander 2026-07-09 late evening: while you are at Kiro/BMAD, look at their test / test-matrix practice too): diff test-author + the matrix template against BMAD's Test Strategy and Standards (testing philosophy · unit/integration/E2E organization · test DATA management · continuous testing) and Testing Pyramid, Kiro's always-generated Testing Strategy, and one or two strong general sources; adopt what is real for our kinds (candidates already visible: a named test-data/fixture management duty beyond the engine case; the unit/integration/E2E axis beside our level ladder — same research discipline as the architecture check, adopt never copy | small | *queued* 2026-07-09 | Done when: the diff is recorded with adopted/not-adopted named, each adoption red-proven · groups as one queue bundle with rows 192, 193, 197, 198, 199 — small standalone method questions: comparing test norms with the field, scenario entry/exit rules, a pipeline coverage gap, and one worker-liveness check trio |
| 192 | **Scenario entry/exit contracts — pre/postconditions where they matter** (Alexander 2026-07-09 late evening, his words: the prover should say, where needed, which pre/postconditions hold — e.g. in tlvphoto how we EXIT scenarios, how we ENTER; "a big topic, defer it but don't forget, write it down"): the spec's flows state how a scenario is entered (from where, with what already true) and how it exits (to where, leaving what behind) — the prover's per-operation pre/postcondition lenses lifted to the SCENARIO level, kin of the entry-symmetry lens and the runtime view's walks; a flow whose entry or exit is unstated is a finding | large | *deferred* 2026-07-09 — revisit trigger: the next prover-method landing, OR the first real spec where a scenario-level entry/exit hole ships a bug, OR his word | Done when: spec-author states the entry/exit duty per flow, the prover carries the scenario-level lens, red-proven; validated on one real spec |
| 193 | **Which pipeline stages are we still missing for the kinds we support?** (Alexander 2026-07-09 late evening): a gap analysis — sweep the supported project kinds (product · backend · CLI · skill pack · prose/book · data/ML tier · static-first+edge blend) against the pipeline's steps and ask, per kind, which stage the field considers necessary and we do not name; first candidates from tonight's sources: a RELEASE/DEPLOY stage for hosted products (BMAD: Deployment Architecture + Rollback Strategy; we end at commit-and-show, deploy rides ad-hoc host scripts) · environment promotion (dev/preview/prod) · a data-quality/eval stage for data-ML projects · a security review step (BMAD Security section; our prover asks or names the skip) · post-ship monitoring walk (our success measures + field legs, unwired mechanically) | surface | *queued* 2026-07-09 | Done when: the per-kind gap table is recorded; each adopted stage lands as its own row through the pipeline; the not-adopted named |
| 203 | **Chat-cleanliness Stop-hook backstop gets a durable home** (his-word backlog item living only in NEXT_STEPS/memory; tonight's deferred-scan flagged the homelessness): the plan — a Stop-hook self-scan over the outgoing reply as the mechanical net for the chat laws — sits here until its trigger (recurring chat-law leaks) or his word | large | *queued* 2026-07-10 | Done when: decided on trigger — built or agreed-closed by name · groups as one queue bundle with rows 205, 207 — three old to-do notes that were homeless until this queue gave them a row |
| 204 | **Readability-restructure word-preservation verifier gets a durable home** (same homelessness class): the prover leg that proves a prose restructure moved meaning nowhere — word-token identity plus punctuation multiset plus the full prover pass — becomes a script the restructure operation must run | large | *queued* 2026-07-10 | Done when: decided on trigger — built or agreed-closed by name |
| 205 | **Thin-loader tidy** (his-word backlog, now homed): ~/.claude/CLAUDE.md carries only what must precede pack load; sweep it against the profile/pack homes once more | small | *queued* 2026-07-10 | Done when: every loader line proves its must-precede claim or moves |
| 206 | **Pack orchestration guidance** (his-word backlog, now homed): how a session runs many workers — parallelism, phase gating, model tiering (his 2026-07-10 word: the senior orchestrates and validates; opus drafts and audits; sonnet does mechanics), spawn liveness — stated once in the pack | surface | *queued* 2026-07-10 | Done when: the guidance lands with the night run as its worked example, red-proven |
| 207 | **Artifact registry: pointers, never secrets** (his-word backlog, now homed): a per-project index of handed-in artifacts surviving /clear — records WHERE a thing lives (Keychain name, path), never the value | surface | *queued* 2026-07-10 | Done when: decided on trigger — built or agreed-closed by name |
| 208 | **Human-facing prose is drafted by a clean writer** (Alexander 2026-07-10 ~00:53, his word after the third onboarding bounce: write ALL docs that way, with a clean head — then the pack's own rules read clean too): any text a human reads — reader docs, spec prose, reports, decision pages, product copy, and the pack's own rule bodies when touched — is DRAFTED by a fresh writer session working from a plain brief (the facts, the reader, the register laws), never by the pack-marinated session; the marinated session briefs, gates, and folds; the law binds forward — existing text walks the road when it bounces off the human or is opened by an edit anyway, never as a blanket rewrite (the restructure-safety risk is named) | small | *queued* 2026-07-10 | Done when: the base rule + spec clause state the drafting road and its forward binding, red-proven by a string test |
| 215 | **Plan for parallel work: the architecture step gains a parallelism lens** (Alexander 2026-07-10 ~10:08, his word: how to plan things so work in the project can be done in parallel — for example, code split into smaller modules; this needs to be accounted for when doing the design): when the architecture step carves nodes, it weighs parallel buildability — smaller, independently testable modules with narrow seams let several build lanes run at once (the lanes-by-graph law INV-49 then has real independent sets to pick); the lens joins the architecture step's checks and the prover's architecture lens; sibling of row 206 (pack orchestration guidance) — the two land together or cite each other | surface | *queued* 2026-07-10 | Done when: the architecture step and the prover lens both name the parallelism check, red-proven by a string test; the first real landing that used it cited in the row |
| 217 | **The convergence audit — every quality lock is itself tested to hold** (Alexander 2026-07-10 ~10:22-10:24, his words: convergence of all processes is needed — that's a basic principle — and test everything so it works and always converges; without that we can't go to 1.0): enumerate every convergence mechanism the pack claims — frozen norms, norm-conformance tests, the register lint's growing pattern set, the debt-cap ratchet, the anchor-multiset guard, waiver expiry, version pins — and give each a test that it HOLDS and cannot silently regress; known holes at registration: norm files have no content-fingerprint test (a frozen norm can drift silently), the lint pattern set has no grows-only test, the debt ratchet's only-downward claim needs its own check; 1.0-BLOCKING — rides the M-1 audit as its own pass | surface | *queued* 2026-07-10 | Done when: the lock inventory is written, every listed lock has its holding test (each new one red-proven), and the M-1 audit's list names the convergence pass · groups as one queue bundle with rows 230 — checking that the pack's own quality locks keep holding, and that its wording does not quietly drift |
| 220 | **Audit the last 24 hours of the tlvphoto exchanges — what must enter live-spec** (Alexander 2026-07-10 ~10:43, his word: audit the last 24 hours of communications with tlvphoto and see what can and should be written into live-spec — significant problems surfaced there — this comes first): read the tlvphoto (and exhibition-engine) journals, resume files, prover records, and checkpoint trails of the last day READ-ONLY, name every problem class that surfaced, check each against the pack's existing laws, and queue or land the missing rules here; FIRST in the queue after the onboarding landing on his word | surface | *queued* 2026-07-10 | Done when: the audit record lists every surfaced problem with its verdict (already covered · new rule queued/landed · agreed non-problem on his word), and the landed rules cite it · groups as one queue bundle with rows 221 — one full audit of a day's messages with the tlvphoto project, in two parts |
| 221 | **tlvphoto's move onto the new documentation — teach-only** (Alexander 2026-07-10 ~10:43, his word: help the migration only through instructions or through the inbox, preferred shape: he tells the tlvphoto window load up live-spec and start relaying all the documentation into it): make that one sentence WORK — verify the adoption path + the pair-adoption guide carry a document-relayout walk a cold tlvphoto session can follow alone (their windows execute; this window answers, reviews artifacts read-only, and may file at most one inbox wish); any gap the dry-read finds becomes a fix HERE, never a write THERE | surface | *queued* 2026-07-10 | Done when: the one-sentence start is dry-read against the guides with every step answerable from them, gaps fixed here, and the real migration runs from their windows with this one supervising |
| 229 | **Parametrizability audit — the full settings model** (Alexander 2026-07-10 ~11:00, POST-1.0 on his word: a parametrizability audit — if there were project settings, what would the parameters be, what types and sections): derive the complete settings model from the shipped pack — every parameter that COULD exist, its type (enum · string · boolean · path · conditional), its section (about the project · about the person · the workshop), and its applicability condition (GitHub-or-GitLab exists only when the project kind carries software/skills; author name; languages; …); the existing Card-marked table and the settings card are the seed; the outcome decides whether the catalog grows a typed schema | surface | *queued* 2026-07-10 | Done when: the model document lists every parameter with type/section/condition, reviewed by him, and the accepted deltas become queue rows · groups as one queue bundle with rows 427 — one always-current picture of every tunable setting a host may adjust |
| 230 | **Standing self-audit on every drift axis** (Alexander 2026-07-10 ~11:02, his word: a self-audit needs to be done so specs don't drift in wording — any axis where drift can happen needs to be continuously re-tightened, always): enumerate every axis where drift can happen — spec wording vs the Formal index, docs vs shipped code, installed skill copies vs the repo, matrix vs tests, README claims vs truth, chat register vs the laws — and give EACH a standing re-tightening mechanism with a named cadence (continuous test where possible, milestone sweep where not); the M-1 convergence pass (row 217) produces the axes inventory as its byproduct, this row turns it into standing sweeps; the convergence family's third leg beside 217 (locks hold) and 218 (goal-anchors) | surface | *queued* 2026-07-10 | Done when: the axes inventory stands, every axis names its sweep and cadence, each new sweep red-proven |
| 231 | **Version alignment is a stated rule** (Alexander 2026-07-10 ~11:29, at the 1.0.0 release, his word: this is also a rule of sorts — the question is at which moments to sync — a major version, for sure): the pack states WHEN every skill's version aligns to the pack's — at a MAJOR version certainly (done at 1.0.0: pack, all eight skills, the spec header, the pins); the open half is the other moments (minors? never? on his word?) — refine with him after the wipe; also fold here the convergence-locks leftovers F4/F5 (standing anchor guard; branch protection — his one GitHub click) and the skill-description polish trio | small | *queued* 2026-07-10 | Done when: the alignment rule stands with its moments named on his word, red-proven; F4/F5 dispositioned |
| 234 | **Managing one project's independent parallel lanes** (Alexander 2026-07-10 ~13:06, his word: you also need to understand how to manage a project with independent parallel lanes — add it to the to-do): the pack states how a senior session RUNS several independent lanes at once — a per-lane state line in the resume file (each lane names its station and its next step), a heartbeat that names every live lane, lane-scoped workers with non-overlapping write-sets (worktree default on overlap, row 227's half), one report that walks the lanes one by one without mixing them, and the commit law across lanes: one lane, one commit, never a sweep that carries a second lane's files; the 2026-07-10 midday session is the born-of worked example (the catch-up-walk landing lane + the Superpowers/gstack research-and-README lane + a promoter hand-off, run at once) | surface | *queued* 2026-07-10 | Done when: the lanes-management rules stand in their homes red-proven, and the first real multi-lane session after them cites the rules with its per-lane commits |
| 235 | **The leave-command: "I'm leaving" reaches a safe stop and says so** (Alexander 2026-07-10 ~13:30, his word from the café: if he needs to leave the café, he gives the command — the agent reaches a stopping point... and he can turn off the computer): one spoken command makes the session wind down to a shutdown-safe state — background workers halted or landed (a sleeping machine kills them, INV-76's plain truth), every lane at its checkpoint, work committed, and one closing line says "you can turn it off now" with what resumes where; the twin habit rides long work the whole way: an occasional plain "about N minutes left" line so the human can plan the exit (kin of row 232's estimates and INV-71's heartbeat) | small | *deferred* 2026-07-10 — revisit trigger: the first real "I'm leaving" reaching the safe stop with the closing line | Done when: the command's walk stands in its homes red-proven, and the first real "I'm leaving" reaches the safe stop with the closing line said |
| 236 | **Projects declare their technologies; an engine parametrizes them** (Alexander 2026-07-10 ~13:48, his word: projects need to state which technologies they use — is the engine hard-wired to Cloudflare and Google Analytics, or is it customizable? this needs to be parametrized and written down; we're not afraid of migrations): a host's docs state in one findable place the external technologies and services the product stands on — hosting, deploy target, analytics, build tools, third-party APIs — each with its role and a swappability verdict (fixed by choice · swappable with a named seam · hard-wired, flagged); for a reusable ENGINE every such dependency becomes a named contract entry like content does (INV-79's infrastructure twin): the engine names the seam, ships a works-without-it default or the swap instruction, and a hard-wired service found in an engine is a finding, not a fact of life; adoption's inventory raises hard-wired services the same way it raises unbacked surfaces; born-of: the tlvphotos engine carries Cloudflare + Google Analytics hard today, and installing it elsewhere has no stated road | surface | *queued* 2026-07-10 | Done when: the law stands in its homes red-proven, the engine content-contract template names infrastructure entries, and the first real engine lists its services with swap verdicts |
| 238 | **The rationalization table — approved 2026-07-05, never built** (found by the 2026-07-10 originality audit: the owner answered "now" in the dated decision record, and no trace exists in the shipped pack; a sound approved thought is never silently dropped, row 233's law): build the mechanism as approved — the idea traces to Superpowers/Trail of Bits practice, source credited in the audit doc — or bring the decision back to the owner if its ground has shifted since | small | *queued* 2026-07-05 | Done when: the mechanism stands as approved (red-proven) or the owner's fresh word re-decides it, either way citing the audit |
| 241 | **The four guardrail checks ship as runnable code a host attaches** (the web session's read, relayed by Alexander 2026-07-10 ~14:22: Known issues admits the host-side checks ship as descriptions while the README's core claim is executable gates — the moat must be portable, he would close this out before everything else): the pack's own repo gates are executable today (push gate, lints, traceability — the analysts ran them); the unshipped half is the generic, host-attachable form of the four checks (completeness · tests-present · behaviour-traces-to-spec · conflicts) that today each host wires by hand from descriptions; build them as parameterized runnable checks + the adopt wiring that instantiates them per host, with the pack repo and one real host as the two proofs | large | *deferred* 2026-07-10 — revisit trigger: one real EXTERNAL host attaches (track-coach's catch-up is the natural first) | Done when: the four checks run as shipped code on the pack repo AND on one real host from the adopt wiring, red-proven each, and the Known-issues entry leaves the list |
| 243 | **README ↔ the article series, linked both ways** (the web session's distribution point, relayed ~14:22: the funnel exists — Product Prover ↔ live-spec is linked, articles ↔ live-spec is not): when the Verifiable Spec series goes live, the why-section links it the same day; the other half rides the campaign (wish filed 2026-07-10: articles carry the repo link, campaign reports the URLs back by inbox) | small | *queued* 2026-07-10 | Done when: both links live — each article names the repo, the README names the series |
| 247 | **The inbox reaches over GitHub — the remote arm of the intake door** (Alexander 2026-07-10, thinking the cloud seat through mid-session: today's inbox law assumes a shared filesystem; a cloud seat, a routine, or another machine reaches a repo only through git): state the remote arm — the deposit stays ONE new file in inbox/, committed with the source in the message and PUSHED (the commit-touching-inbox-only exception already written); the grant model made explicit: the Claude environment links the owner's GitHub account once (his click — done 2026-07-10), each repo is granted to the app once (his click, the ask carries the exact settings path), the grant recorded in the host profile like the push grant; a seat with NO grant fails honestly — names what it lacks and hands the owner the one action (today's routine alert is the live example); DECIDE at landing: do GitHub Issues become the stranger's inbox door for public repos (no push rights needed), swept beside inbox/ | small | *deferred* 2026-07-12 — revisit trigger: Done-when (c) "one real remote deposit landed" stays open, closable only by the owner's own real cloud/browser session at claude.ai/code — that session happening is the trigger | Done when: the remote arm stands in the inbox law red-proven — MET (red 4F then green against the pre-delta tree; suite 415 green); the grant ask is scripted with its exact path — MET (scripts/grant-ask.md, the stable settings URL plus its named click path, per the orchestrator's call on the draft's read-at-draft-time fill-in); one real remote deposit landed — OPEN (field-gated; a local run cannot self-certify it [INV-94], closes only from the owner's real browser cloud deposit) |
| 261 | **GitHub Issues as a public repo's stranger door — a DECIDE** (split off row 247's landing, 2026-07-12 ~02:41, session 37: row 247 stated the inbox's remote arm for a GRANTED seat reaching a repo over git; its fourth point asked whether GitHub Issues becomes the stranger's inbox door for a PUBLIC repo — a contributor with no push rights, no grant — swept beside inbox/ rather than committed into it): the question, unresolved — does a public live-spec repo accept a stranger's wish or feedback through a GitHub Issue, swept by a live session the same way inbox/ is swept; if so, what shape (a label, a template, a required field naming the source) keeps a stranger's Issue as parallel-safe as a granted seat's inbox deposit; row 247's own analysis recommends NOT folding this into INV-112: it is a genuinely different actor (a stranger with no push rights, not a granted seat) needing its own mechanic, and its facts wait on a real public-repo contribution case to ground them — the same "waits on a first real run" posture that gates row 247's own field leg (c) | small | *deferred* 2026-07-12 — revisit trigger: the live Discussion WRITE round-trip (a real discussion bridged end-to-end) | Done when: the owner's word answers whether GitHub Issues becomes the stranger's door for public repos, and if yes, the shape lands red-proven — shape landed 2026-07-14 (his "all at once"; INV-146/147 red-proven, suite green); open leg: the live Discussion write round-trip, field-gated |
| 381 | **A promised flow reads as a diagram** (split from row 379 on the owner's word, 2026-07-17, after he asked whether a diagram is readable to the agent that later analyses the spec and wondered whether to wait for more capable models; answered from the medium: a mermaid diagram is source text the agent reads directly, so readability carries no risk — the real risk is a flow living twice, once in the paragraph and once in the picture, which breaks the one-home law [INV-3]; his verdict on hearing it: the far tier): a lifecycle or message flow earns a mermaid diagram in its spec section, the flow's ONE home, the surrounding prose pointing at it and stating no step twice; the render road carries mermaid to the rendered page; a redundancy check flags a paragraph re-telling a diagrammed flow. Homes: spec-author, scripts/render-doc.py, the redundancy gate. | small | *far* 2026-07-17 | Done when: a flow's one home is the diagram, the render road carries it, red-proven |
| 385 | **The contract's mechanical arms ship when a host declares its first contract** [target] (split from the multi-agent stage at its landing, 2026-07-17, by the scope law [T-15]: the contract's LAWS ship today as spec [E-33, INV-185, INV-186, INV-187], and this pack contains no data producer, so there is nothing for a permission gate, a freshness check, or a compatibility test to exercise — shipping them here would ship code no red ever proved): three arms, each red-proven against the first real producer and consumer — a producer-side gate reading the card's declared contracts and redding a published field with no dated permission record [INV-185]; a consumer-side freshness check redding an artifact past the consumer's declared staleness bound before any analysis [INV-187]; and a compatibility test redding when the pinned version and the artifact's version diverge [INV-187]. The permission record's own format lands with them (author + date + field, one home in the producer's tree). Revisit trigger: the first host declaring a contract in its card — the promoter↔site pair is the expected first, and that work belongs to those windows [INV-190]. | surface | *queued* 2026-07-17 | Done when: the three arms ship red-proven against a real producer and consumer, and the permission record has one declared format |
| 386 | **Parallel lanes get branches and worktrees, and the three-lane cap is re-read** (the owner's word, 2026-07-17 and ~14:15: can we not run different agents in different branches in parallel? why do we wait? why is this written nowhere? — and then, checking me: there are commands in Claude Code for each agent to work on its own tree isolated, verify. Verified: two mechanisms exist — a subagent launched with `isolation: worktree` gets a fresh git worktree and its own branch, auto-cleaned when unchanged; and `EnterWorktree`/`ExitWorktree` move a whole session into one, with keep-or-remove on exit): three gaps, one row. (a) The law names worktree isolation as the DEFAULT when two lanes' write-sets overlap [INV-105], and the tool's own contract says to use it only on the human's explicit word or a project instruction — so the law's default and the tool's gate contradict, and neither fires; his word today lifts it, recorded as a standing profile line. (b) The method has no BRANCH and no merge road: integration is described only through the pen (one lane edits a shared doc at a time, T-18/INV-39), so parallelism today lives inside one tree and cannot exceed it. (c) The three-lane cap is his 2026-07-06 word and the method holds it as given; he is questioning it now, so the cap is re-read against the worktree road, where the binding cost is the merge rather than the tree. Worked evidence to carry in: this landing ran serial because its eight rows all edit PRODUCT_SPEC.md, which is the graph law working correctly [INV-49]; the parts that did not collide (the gate, the template, the card, the matrix, the tests) ran on four concurrent workers. Homes: base rule 5 / T-18 / INV-39 / INV-49 / INV-105, build-pipeline's trains section, the personal profile's autonomy block. **In flight, unparked: an earlier note parked this behind a field run of the communication layer on the strength of a ranking recorded as his that no message of his backs — struck on the read-back surface (DECISIONS.md, 2026-07-17). The pack imposes no such ordering; the lanes work is open now, and its scheduling is the pack's own call until an exchange says otherwise [row 415].** | surface | *queued* 2026-07-27 — re-derived from the code 2026-07-27: the branch-and-worktree road is already built (scripts/open-lane.sh, base rule 7, 37 green tests) and no session is working it, so the in-work claim was bookkeeping. What it owes is proof by deed, granted by his 2026-07-27 word: a live run of three lanes on three independent rows in one window | Done when: the branch-and-worktree road stands with its merge law, the cap is re-read with his word recorded, the law and the tool agree, red-proven |
| 396 | **Two agents on one machine talk directly; git carries what needs no answer** (the owner's word, 2026-07-17, refusing the transport premise this stage was built on: agents will not talk through git, that is dumb — there should be direct protocols between agents on one machine, certainly on one machine; git carries the requests that need no particular reply, and notifications): the premise came from the seed and I built INV-183 on it — a remote seat reaches a repo through git alone [INV-112], therefore git is the universal transport and co-location is only a fast path. That reasons from the worst case to the general rule, and his split cuts by the TRAFFIC's kind instead: a thing needing no timely answer takes the durable, auditable, slow road (a file, a commit); a conversation takes a direct channel. The channel law survives untouched — INV-183 is about WHO talks and WHEN, and one sentence inside it about transport is what needs correcting. **The fact nobody has checked: whether two Claude Code sessions on one machine have a direct channel at all.** They are independent processes and may have none by default; a socket, a watched file, or a harness mechanism may exist. That is verified, never derived — check it before designing. This also re-reads the 2026-07-17 research (docs/research/): its closing line was that no prior art fits a repo-as-unit, git-as-transport setting; drop the git premise and the question becomes what direct agent-to-agent protocols exist, which is a different sweep. Carry in from that research: the Contract Net Protocol's directed contract (when the owner is known the award goes straight to it, no announcement, no bids — the pack's referral law under another name) and its measured ladder, 2 messages after learning against ~20 broadcast; and its own transport is the half to reject. | surface | *deferred* 2026-07-18 — revisit trigger: field-gated on the harness shipping a listener [INV-231, row 405] | Done when: the direct-channel fact is verified rather than assumed — MET (confirmed against INV-231, read from the machine not derived: the socket plumbing is built and switched off, no listener has shipped, so no live direct channel stands between two local sessions today); INV-183's transport sentence states the traffic split — MET (the refused premise removed, the sentence now cuts by the traffic's kind and points to INV-236 [INV-236]); the road each kind takes is red-proven — MET (`guardrails/route_agent_transport.py` routes a no-answer message to the git store road and a conversation to the not-yet-shipped direct channel naming the listener [INV-231], red 14F then green). **The conversation channel's real first use stays OPEN** — field-gated on the harness shipping a listener [INV-231, row 405], closable only when a listener ships and never self-certified on a local run [INV-94]. On the second paragraph's research: the git-premise-free re-read the owner asked for is answered for co-located agents by the machine-fact itself — the harness's own dormant unix-domain-socket plumbing (messagingSocketPath, the uds client) IS the local direct channel, built and switched off [INV-231], which is what two long-lived local Claude Code sessions would use; a broader cost-and-portability sweep across the other shapes (a local HTTP endpoint per agent, a message queue, a watched file, an OS notification — what each costs, which survive an agent moving to another machine) stays available as a research row if wanted, off the critical path of this transport-split law. |
| 398 | **A request typed in the wrong zone routes itself to the right one** (the owner's third live word this afternoon, 2026-07-17: every prompt he types carries the implicit first question — whose zone is this — and a foreign request should be deposited automatically in any project that has loaded the pack): a UserPromptSubmit hook, vendored by adoption beside the existing guardrails and active only in adopted projects, injects a compact routing preamble at every prompt — the roster, each agent's zone in one line, and the rule that a request pinning to another agent's zone gets deposited there with a one-line notice back to him. The trigger is mechanical (the hook fires every prompt); the classification stays the session's judgment, since zones are prose. One clause rides along to keep this legal under INV-190: the relaying window deposits his words unchanged and adds none of its own — his instruction carries his own source authority [E-11, INV-193], and forwarding it is delivery of his word; a relay that expands or reinterprets the request would cross back into the defect INV-190 already names. The preamble declares its own size cap; row 392's general growth-bound law (INV-234) retired 2026-08-18 as an unrequested ceiling, so this row's cap belongs to this feature alone. | surface | *queued* 2026-07-17 | Done when: the vendored UserPromptSubmit hook injects the routing preamble in adopted projects, red-proven on a fixture prompt naming a foreign zone (T-24: the fixture deposit lands in the fixture tree with the one-line notice); the no-rewrite clause stands in the spec beside INV-190; the adoption gate reds a pack-loaded fixture carrying no hook; the preamble carries its own declared size cap. |
| 399 | **A message is gated where it is born** (this morning's incident, 2026-07-17: a bogus deposit passed the receiving sweep's gate, and the cheapest lesson is to gate both ends rather than only the destination): the depositing agent's own guardrail reads the birth before the file is written, so a birthless message never leaves home; the receiving sweep keeps its existing gate as the second net. | small | *queued* 2026-07-17 | Done when: the sender-side arm stands beside the receiving sweep's gate, red-proven on a birthless fixture deposit attempt that never leaves the sending tree. |
| 400 | **A design element shaped after a human institution names the machine constraint that forces that shape** (the owner's atavism word this afternoon, 2026-07-17, generalized from the day's own worked example: the mail shape carried a real cost where it was copied past its forcing constraint — a conversation held in a drop-box — and it carried a real benefit where the machine's own reality forces it — durable state for a receiver that is asleep): where no constraint forces a borrowed shape, the shape is redesigned from the machine's own reality. The law rides the expensive-decision road (row 395) as one of the standing questions its adversarial read asks, and the design reviewer carries it as a lens. | small | *queued* 2026-07-17 | Done when: the law stands in the spec; the expensive-decision record's required sections include the borrowed-shape answer; the design reviewer carries the lens; the record gate reds a record missing the section, red-proven. |
| 404 | **The field run has a bar and a record derived from what the machinery already keeps** (section 7 of the 2026-07-17 parallelism-and-communication plan: the v2.6.0 laws — cards, births, gates, referrals, terminal states, contracts — need real traffic between the real windows before the major release can claim which of them held): a deriving script assembles the record from the queue archives, the gates' exit codes, and the roster, imposing no new duty on any window; the bar states what counts as evidence — every carded agent green under the card gate; at least one lived crossing of each kind (an earned request with its birth named, a reply reaching a terminal state, a referral traveling back, a gate red on a birthless attempt, a contract declared and consumed if its trigger fires); ten working days elapsed; zero crossings that bypassed the gates, itself evidence in either direction since the layer exists to suppress traffic. The bar doc states the closing rule, including the honest-rarity clause: the run may close on time plus zero-incident evidence, naming any kind that never fired as untested. **Correction folded in at minting (the owner's word, 2026-07-17 ~15:26, overturning the plan's roster-ratification step — whoever is a standing agent will say so itself, the system being dynamic, so his approval of a list decides nothing):** the run opens on no ratification gate — an agent declares itself by carrying a card in its own tree and doing work, and the deriving script reads whichever cards exist rather than waiting on a ratified list. | surface | *queued* 2026-07-17 | Done when: the deriving script assembles the record from archives, gate logs, and whatever cards exist with no ratification wait; the bar doc states the closing rule including the rarity clause; an empty required input reds by name (row 384's law applied), red-proven on a fixture archive missing a kind. |
| 405 | **The day the harness ships a listener, addressed push becomes available under the standing transport law** (section 3 of the 2026-07-17 parallelism-and-communication plan: the dormant socket plumbing — messagingSocketPath, the uds client, the local-session recipient kind — is built and switched off, and a private HTTP broker daemon is not worth building to bridge a few crossings a day): a deferred row carries a mechanical revisit trigger — a session record in `claude agents --json` showing a non-empty socket field, re-scanned at every queue-take under INV-129's existing habit — plus the one-shot check script that reads it. When it fires, the live channel gains addressed push: the push carries the pointer, the state stays the truth. | small | *deferred* 2026-07-18 — revisit trigger: the harness shipping a listener | Done when: the deferred row stands with its mechanical trigger — MET; the one-shot check script reads the socket field and reports whether it fired — MET (`guardrails/check-listener-tripwire.py`, re-scanned every queue-take under INV-129); the firing stays field-gated on a real listener, not self-certified |
| 410 | **What the pack learns climbs a ladder, and the rung above retires the rung below** (his word, 2026-07-17 ~15:44: the model does not train, so learning must live in notes that never bloat; plus his recorded 2026-07-17 wish that a hook be removed once it stops firing; the ladder itself is the pack's judgment): a lesson starts as an incident, becomes a journal line, may earn a note, may harden into a law, and ends as a machine. Each rung retires the one below, so the store gets better while its shown set stays small. | small | *queued* 2026-07-17 | Bounded at the shown end, unbounded at the archive end [his 15:57 word + base rule 10]: a note that graduates into a machine leaves the live index and moves to the archive with its date and its graduation named, and a machine whose fire count stays zero for its declared silence is removed by the same road (`hooks/hook-meter.py --report` already records runs and fires, so the evidence exists). Every number here is a settings-ladder default the pack picks and reports [E-13]: the shown index's size, the silence before a machine retires. Machine: the cap gate plus the meter's report extended to notes. Red-proofs: an over-cap live index reds; a graduated note still sitting in the live index beside its installed machine reds; the archive missing a demoted note reds. |
| 411 | **The orchestrator shows who is working on what, and where the work stands on the map** (his word, 2026-07-17 ~15:54, sending it away himself: nothing today lets him follow who is working on what, and it would be good to have — he placed it in the far backlog, described the orchestrator showing a half-kanban of who works on what and where the work stands on the map, and named 4.0 as its release): a person running several windows and their workers has no view of the whole, and reads the state by asking each window in turn. He named the tier and the version himself, so this row rides the far tier's law [row 382] — it stays out of the what's-left answer and surfaces on his ask or on the rare self-surfacing line [row 403]. | surface | *far* 2026-07-17 | The bricks are already on the machine, and the row records that as its own finding so its cost is honest when it is picked up: the session registry (`~/.claude/sessions/<pid>.json`, verified 2026-07-17) names every live window with its state (busy/idle) and its working directory, which is "who is running"; a claimed queue row is a committed act [INV-2], which is "what they took"; the queue's own rows carry the position on the map. So the missing part is the hand that draws the view and keeps it fresh, plus the answer to what a stalled claim means — his own framing of the neighbouring problem [15:44]: knowing who took a thing and did not do it, and how that is mitigated. Nothing is designed here and nothing is promised: the far tier's law is that a row waits for his ask. |
| 412 | **Opening a lane is an act a session performs, and running independent work single-file reds** (his field report, 2026-07-17 ~15:59: he asked the exhibition-engine window for three items in parallel and nothing ran in parallel at all — his own words, "guess! nothing!"): the lanes law grants up to three lanes [T-18] and grants is all it does. No step of the method OPENS a lane, so there is nothing for a session to perform; the isolation the law calls default is refused by the tool [row 386]; and no gate asks whether independent work rolled in parallel, so single-file work costs nothing and takes the easy road every time. This is the day's own class found in the field by the owner in under a minute: a law with no machine is a wish, and here even the ACT the machine would fire was never written. | surface | *queued* 2026-07-27 — re-derived from the code 2026-07-27: the branch-and-worktree road is already built (scripts/open-lane.sh, base rule 7, 37 green tests) and no session is working it, so the in-work claim was bookkeeping. What it owes is proof by deed, granted by his 2026-07-27 word: a live run of three lanes on three independent rows in one window | Two things this row owes beyond the branch road's prose. First, the ACT: a lane-open step in the pipeline that a session performs — the row's claim commits, the branch is cut, the worker takes the lane in its own isolated copy — with the subagent road usable today under no permission at all (`isolation: "worktree"` carries no gate, verified 2026-07-17), so the first proof of this row is a live run of three lanes on three independent rows in one window. Second, the NET: the dependency graph already computes which queued rows are independent [INV-49], so the graph is the gate's evidence — a session closing a movement that rolled independent rows one after another, with lanes free under the cap and no recorded reason, reds. Acceptance: red-proven on a fixture movement whose graph shows three independent rows landed serially with no reason recorded; quiet when the rows share a surface, when the cap is full, or when a reason is recorded; and the live three-lane run in one window is the row's own worked instance, reported with what it cost. |
| 421 | **How an open-source engine and its instances live as separate repos, and how one window rules several instances** (his word, Alexander 2026-07-18 ~21:00): the machine already runs the pattern twice — exhibition-engine (released open-source so anyone can build the site) with tlvphotos as one instance, and promoter (an open-source backbone) with several instances planned, one each for the site, the music, the articles, and the assistant, all ruled from one window. The pack has no stated position on this shape, so a third project reopens the question from scratch. The row asks the pack to state it: when an engine earns its own repo (the trigger is a real or planned second instance, never before), which zones the engine's agent card owns versus the instance's, how a change fans out across the two repos and their specs, and — the new part the current rules do not cover — how one orchestrator window rules several instance-agents against the "one window, one project" law, which the pack's own agent layer (cards, discovery, referral, contracts) is the machinery for. | surface | *queued* 2026-07-18 | The two repo boundaries are verified: exhibition-engine + tlvphotos and promoter + promoter-alexander are each two GitHub repos under happysasha18. The framework-plus-apps split is standard practice and justified here by real multi-instance reuse; the genuinely unaddressed piece is the single-window orchestrator over many instance-agents, which touches the one-window bootstrap rule and should be resolved together with the far-tier orchestrator view [row 411]. |
| 424 | **Named references travel everywhere, and earned notifications are deposited automatically** (the owner's word, 2026-07-19): every reference to an internal item — in a message across the agent channel and in any human-facing report — carries a pair, its stable code beside a one-sentence description pinned to the item's canonical line [E-35, INV-239]; an agent deposits an earned message in the course of its own work for any earned birth, the whole class rather than a fixed pair of cases, gated by the earned-message births unchanged [T-24, INV-189, INV-195]; and the user is told both when a message is deposited and when one is declined, with the reason, the user named by role [T-24]. | large | *deferred* 2026-07-20 — revisit trigger: the back-describe of every registered code to the quality bar | Probable MAJOR by INV-217 — pinning the pair everywhere forces every host to back-describe its codes with human-clear descriptions, so the landing owes a MIGRATION.md chapter; the success measure is description quality (the human-clear descriptions read as clear to a non-engineer, sampled from real references and accepted). Out of scope this build: the live wake / listener (a neighbour still reads its inbox on its next run [INV-192]) and a static translation dictionary (the description is translated in real time [INV-83], a translations file a later pass). ⟨DECIDE⟩ items RESOLVED 2026-07-19 (owner's decision, folded from prover pass docs/prover/2026-07-19.md): each item's pinned description lives in a dedicated Formal-index description field — the description's one home — netted by a presence gate that reds a registered code carrying no description [INV-239]; the existing code set is back-described in one pass at the major release's MIGRATION.md chapter; and INV-239's agent-channel reach is netted by a deposit-time lint on `from-<agent>` inbox files homed beside the earned-message gate, the prover's station its net until it ships [target]. The provisional reference to T-24 from row 398's done-when is tracked for reallocation by row 426. |
| 427 | **A dynamic parameters registry — an always-current, self-updating list of every tunable per-person parameter, surfaced as a live settings window** (Alexander 2026-07-19): the pack keeps one live registry of every per-person parameter a host may tune, and an incoming request is recognized for whether it adjusts a parameter, updating the registry the moment it does, so the list never drifts from the truth and the settings window renders the current set on demand. This folds into the onboarding movement, which already owns the settings window [ROADMAP 414]; the delegation-strictness setting (how strictly the pack delegates work to cheaper workers) is its first prospective entry. | surface | *queued* 2026-07-19 | Done when: the onboarding movement's owner has decided the registry's shape — how an incoming request is recognized as a parameter change, how the registry stays current and self-updating, and how the live settings window surfaces it — with the delegation-strictness setting captured as its first entry. |
| 436 | **The composition-axis set is derived from `project.kind`, known in advance** (from the tlvphotos 2026-07-20 inbox deposit + Alexander's clarification the same day): `project.kind` (INV-36) already proposes a project's node structure; it should equally carry the STANDARD set of composition axes a product of that kind renders under, so spec-author composes every surface against that set from the start rather than discovering an axis from a bug. A visitor-facing web product's standard set: input modality (touch / pointer / keyboard-assistive), browser engine, viewport, locale delivery, connection/data class, first-vs-returning, accessibility, measurement reach. A backend's: load, version, tenant. Input modality (the tlvphotos mobile-grab bug) is one member of the web set, not a one-off addition. The extrapolation runs one level deeper too: each axis's VALUE space is its own class to extrapolate, with an explicit test for the member BETWEEN the two obvious poles — the device/input axis is coarse-touch-only (phone), fine-pointer-with-hover (desktop), the hybrid (tablet / 2-in-1 / touchscreen laptop, both at once), and keyboard/assistive-only, so a binary touch-vs-pointer split already drops the tablet. The mechanism must force naming the value space and its in-between member, since a hand-written list writes the two obvious values and drops the third. | surface | *deferred* 2026-07-20 — revisit trigger: the value-space forcing step for each axis's in-between member, the other kinds' elementary axis sets, and the recursive registry sweep | Done when `project.kind` carries a per-kind standard composition-axis set, spec-author derives a surface's axes AND each axis's value space from it (input modality among them, with its cross-modality reachability invariant and the between-the-poles member named), and the prover walks the kind's axes per surface. Enters through spec-author + product-prover. The per-kind axis set is authored smallest-first with Alexander (INV-62). Sibling of row 437. · groups as one queue bundle with rows 437, 447 — the standard set of layout/behavior axes a product of a given kind should be checked against |
| 437 | **The look-alike sweep is recursive — it runs at every level, the axis registry included** (from the tlvphotos 2026-07-20 inbox deposit + Alexander's clarification): base rule 14 and INV-124/INV-125 sweep sibling SURFACES within a class; the same reflex applies one level up, at the registry of composition axes, and at every level thereafter — at any point one can always ask what other siblings or axes exist. When a session adds or derives a composition axis, it runs a bounded look-alike pass over the kind's registry (row 436): what other axes does this product's kind render under that the spec composes against nowhere, each named composed / added / out-of-scope-with-reason. | small | *queued* 2026-07-20 | Done when adding or deriving a composition axis triggers a bounded registry-level look-alike pass with the three verdicts, stated in spec-author plus a product-prover line, and the recursive nature (sweep at any level) is named. Enters through spec-author + product-prover. Sibling of row 436. |
| 440 | **A request is classified one-time or standing before it is actioned** (Alexander's word, 2026-07-21, said sharply after the seat treated a standing ask as a one-off and made him say "permanent" and "every project" twice): every incoming request or wish first passes an understanding step that names its persistence class. A ONE-TIME task is done once and closed. A STANDING ask is a rule that owes a permanent mechanism — a gate, a base rule, a judge — holding it forever and across every host by shipping in the pack, and the sweep to floor is only the one-time half of a standing ask. The seat states the class itself at intake, and the history-cleanup gate is one instance of the standing kind. This generalizes the answer-the-class reflex [INV-125] from an example to a request's own persistence. | surface | *queued* 2026-07-21 | Done when intake carries a named one-time-versus-standing classification step, a standing request routes to a permanent pack mechanism inherited by every host, and the seat states the class without being asked. Enters through spec-author + product-prover. · groups as one queue bundle with rows 471, 472, 473, 474, 484, 485, 486, 487, 497 — the law that an agent must say plainly what it did, what it resembles, what remains, and never claim another's words as its own |
| 453 | **Review pages persist edits** (Alexander 2026-07-22, relayed from the tlvphotos window's journal — the deposit was noted there as a pack candidate and never sent): a decisions/review page opened for the person accepts edits in place and writes them back into repo files through a small local HTTP server (stdlib, one shared script all pages use), replacing the save-a-JSON-download dance. Generalizes the commentable-review law; every project's review pages get it from the pack. | surface | *queued* 2026-07-22 | Done when a review page's edit lands in the repo file it belongs to, the round-trip is covered by a test, and the shared script ships with the pack |
| 454 | **The deep-pass schedule gets one written home** (Alexander 2026-07-22): periodic whole-document passes stay in the method, on a stated ladder — per delivery the machine gates; per changed section the cold-reader panel; per push the whole-spec prover re-verify (already law, M-6); per MINOR release a full-document fresh cold read plus a design-review similarity sweep, because cross-section drift is visible only whole. One clause states what runs, how, and when; the release checklist cites it. | small | *queued* 2026-07-22 | Done when one spec clause states the ladder, the MINOR checklist cites it, and a skipped scheduled pass reds the release gate |
| 455 | **An all-project transcript audit mines the working record** (Alexander 2026-07-22, voiced in the live-spec window after a sibling window's reply carried a banned contrast frame the scanner missed): one deep sweep over every project window's session transcripts (live-spec, track-coach, tlvphotos, promoter) reading for classes, each finding routed to an owner: a friction seen twice with no owning queue row, a law voiced in chat and never mechanized, a deferral marker that fails the derivability re-test, a register leak that slipped the nets (the comma-less «а не» hole fixed 2026-07-22 is the worked instance), a worker report breaking the language laws, a bug fixed before its red test existed (the red-first ordering is invisible in commits and visible only in transcripts), and a session that read raw material itself where the routing rule owed a worker dispatch. Per the one-time-versus-standing classifier (row 440) this row is BOTH: the sweep is the one-time half, and the audit proposes its own standing cadence and mechanism as part of its deliverable. | large | *queued* 2026-07-22 | Done when the sweep's findings each name an owner (queue row, base rule, hook pattern, or agreed non-problem), the fixed scanner holes are red-proven, and a standing audit cadence is proposed with its cost stated |
| 457 | **Cost telemetry and the budget discipline** (Alexander 2026-07-22: the $200 weekly plan barely fits the week; his live word amends the 2026-07-15 quality-over-budget grant — quality passes stay, and cost joins correctness as a criterion for HOW they run): the measured week 07-15..22 burned $14,338 API-equivalent across the machine (live-spec $6,486, tlvphotos $6,053, track-coach $1,475), two-thirds of it cache re-reads at an average 252k-token standing context over ~25,600 model calls, with the sonnet+haiku share under 0.5% against a written routing rule. Standing mechanism, four arms: (a) the usage-audit script (playbook tools/usage-audit.py) runs on a weekly cadence and its report lands where the owner reads it, showing burn by project, tier shares, and average context; (b) the milestone context-wipe returns as a mechanism — at a movement end or breakpoint the seat offers ending the session and reopening fresh, since the resume file carries the state; (c) the routing rule gains a lived floor — mechanical multi-step stages route to sonnet and the weekly report makes the tier mix visible; (d) long tool outputs go to a file with the verdict line printed inline, so suite logs stop living in the standing context. A cost-audit deposit with the tlvphotos share and levers went to that window's inbox 2026-07-22. | surface | *queued* 2026-07-22 | Done when the weekly report exists on its stated cadence, the milestone wipe offer fires, the report shows the tier mix and average context, and a following week's measured burn is lower at unchanged gates · groups as one queue bundle with rows 475, 489, 509, 507, 508, 469, 492 — whether an agreed rule truly has a working check behind it, and how work is routed to the right cost tier |
| 458 | **The cold-reader text audit becomes a pack skill** (Alexander 2026-07-22: «как аудировать тексты — это отдельный скилл, может дашь его отдельно в лайвспек?»): the loop proven inside row 445 — the mechanical lints first (vocabulary, weak words, requirement shape, style), then a fresh cold reader with zero context returning the places a stranger stops, fixes from the source, repeated until two consecutive clean reads — is packaged as its own working skill applicable to any human-facing text: a spec section, a README, a decision page, marketing copy, an article. The skill states the writing register it audits against and the reader-prompt it uses, and any host extracts it by adoption. Its first outside consumer is the promoter window's presentation-text rewrite (the recipe went to that inbox inline, 2026-07-22, so the wish there is runnable before this skill ships). | surface | *queued* 2026-07-22 | Done when the skill exists with its eval (a text a bare session passes and the skill reds), the register and the reader loop are stated inside it, and one outside host has run it on a real text · groups as one queue bundle with rows 460, 465, 493, 498, 510, 518, 519, 530, 559, 544, 525, 526, 529, 535 — the machinery that checks the pack's own prose for register and style, including the text-audit skill and its counters |
| 459 | **A bug row carries its axis, class verdict, and spec verdict before it lands** (Alexander 2026-07-22, on the rotation-bug case: a reported bug first needs its axis named, then the class hunted, then the spec checked, and only then a test — the same walk a wish takes, one principle everywhere): the bug door's entry gains three mandatory row fields, mechanically gated at landing. `axis:` names the composition axis the break sits on, from the project's declared axis set (INV-244); `class:` records the lookalike sweep's verdict — the same axis walked across sibling surfaces and platforms, each checked pair listed with its result, settling one-off versus family (INV-125; the recursive sweep is ROADMAP 437's machinery); `spec:` records whether the spec is silent on or under-describes the broken behaviour, a yes routing a spec delta ahead of the matrix row. A landed bug row missing any field reds the gate; the prover's bug-delta read carries the three fields as a lens. The one-time half: tlvphotos's axes catch-up rides its migration (rows 437, 447 adjacent). | surface | *queued* 2026-07-22 | Done when a bug row without the three fields cannot land (red-proven), the lookalike sweep's checked pairs are listed on the row, a spec-silent bug demonstrably lands its spec delta first, and the prover's bug lens reads the fields |
| 460 | **The register ratchet over the prose corpus** (Alexander 2026-07-22, voiced while the row-445 assembly ran: the skills and the wider documentation carry the banned contrast frames and further register debt; his order lands the pack-skills rewrite before the 4.0.0 push, and this row holds the rest): a census (docs/audit/2026-07-22-register-census.md) records per-file violation counts across four reader tiers — pack skills, public reader docs, working docs, archive. This row owns (a) the rewrite of the public and working doc tiers to the register bar, and (b) the standing mechanism: a per-tier violation-count ratchet wired as a gate, seeded from the census, that only tightens, so a push raising a tier's count reds. The archive tier (journal, prover records, attic) is measured and left as written. His word 2026-07-23 ~18:30 re-scopes the row: the public tier (texts strangers and he read) stays the work; the working tier (journal, prover records, internal notes) moves to possibly-never, returning only on either named trigger — its size interferes past the doc bounds, or its register contaminates outbound drafts through reading (the pack-marinated-author pattern). | surface | *queued* 2026-07-22 | Done when the public and working tiers pass the register lint, the ratchet gate is red-proven on a seeded regression, and the census file carries the was and became numbers per tier |
| 469 | **A persistent metric gap becomes an owned queue row** (Alexander 2026-07-22 ~19:34, relayed by the tlvphotos window's inbox deposit inbox/2026-07-22-from-owner-turn-standing-metric-gaps-into-queued-builds.md: he keeps being shown statistics and supplies the "so what" by hand every time): the pack gains the analytics sibling of the nothing-handed-in-is-lost law — a metric standing past a declared floor, held over N consecutive reads, is itself a finding and opens a queue row mechanically, the way a bug reproduced twice does. The build: the rule's spec clause, the per-host wiring shape (a host's report family declares its floors and read cadence; the sweep compares and deposits the row), and the worked first instance on the tlvphotos evidence in the deposit (shares 12 / arrivals-by-share 0 over weeks; works-per-visit 1.15). The host owns the product read; the pack owns the reflex. | surface | *queued* 2026-07-23 | Done when the clause exists with its gate, a host-declared floor held over N reads demonstrably opens a row without the owner asking, and the tlvphotos instance is the recorded first run |
| 471 | **A report carries a forward time estimate, and a landing carries promised-vs-actual** (row 470, symptom 4: 0 of 46 report-shaped replies in the sampled window carried an estimate; the agreed rule lived only in auto-memory): the rule becomes a personal-profile contract line plus a Stop-hook arm on report-shaped replies, and the landing report carries the calibration line. | small | *queued* 2026-07-23 | Done when the profile line exists, the hook arm reds a fixture report with no estimate and stays silent on a non-report reply, and a landed row's report shows promised-vs-actual. Widened 2026-07-27 (Alexander: it would be good to measure and record how long each movement took, so the forecasts get more accurate over time, a file for it is fine): the forward estimate is read from a kept ledger — one line per movement carrying its size, what was promised, and what it actually took — so each landing sharpens the next forecast |
| 479 | **A worker never restores the shared working tree** (found 2026-07-23, second uncommitted-work destruction in one day: an applier's `-o` clobber — root-fixed as row 478 — and a caps-sweep worker's `git checkout -- TEST_MATRIX.md` that discarded the uncommitted conversion, recovered only because the converter was deterministic; two further occurrences on the night of 2026-07-27, which make four: a neighbouring project's worker restored a shared client file with `git checkout --` three minutes before another lane began writing the sources that build it, recorded in `inbox/2026-07-27-from-tlvphotos-worker-git-restore-discards-sibling-lanes.md`, and a worker in this tree ran `git stash push -- PRODUCT_SPEC.md` and popped it while another lane held 93 uncommitted criterion rewrites in that same file, because the brief it was handed carried no clause against it. The orchestrator writing that brief is the leak: a rule that lives in a skill file reaches a worker only when the worker loads that skill): the worker contract gains the clause — before a worker mutates a file it means to put back, it reads that file and holds its bytes, and it restores by writing those bytes back. It runs none of these: `git checkout -- <path>`, `git checkout .`, `git restore` outside `--staged`, `git stash` and its `push`, `save`, `create` and `store` forms, `git reset` with `--hard`, `--merge` or `--keep`, and `git clean` with `-f` or `-x`. The rule binds a worker in every tree, its own isolated worktree included, since a worktree shares one repository with the lanes beside it. A worker holding no saved bytes, or believing a file needs a git-level restore, halts, reports, writes no further file, and runs no further command; the orchestrator then restores the named file from the last committed stage, hands the worker a fresh brief carrying that file's current bytes, and records the halt in the delivery report. The orchestrator-side twin: a finished build stage is committed before the next worker touches its files. | small | *queued* 2026-07-23 | Done when the delegation protocol and the worker contract carry the clause, briefs inherit it from the template, and the orchestrator habit (commit the stage before the next worker) stands in build-pipeline's delegation reference · groups as one queue bundle with rows 511, 527, 547, 586, 598, 604, 605, 589, 596 — a background worker or session damages or misreports work that was not its own, and the safety net has holes |
| 481 | **The live queue stays short — a staleness sweep at the milestone gate** (Alexander 2026-07-23 ~18:18: does the roadmap have a mechanism that shrinks it, should the limits be much stricter, the roadmap is no five-year plan): the live body after row 480's conversion still holds ~95 open rows, and no mechanism bounds their age or count — a wish queued once can sit forever. The wish: the milestone gate gathers the stale rows — queued past a threshold with no movement and no revisit trigger, and *in-work* with no commit citing them for seven days — into a short review list for the human, who declines, defers with a named trigger, or keeps each; a decline is his word, since the wishes are his. Threshold [default: 30 days], tuned with him. Rides on row 480's format (statuses dated, so staleness is computable). | small | *queued* 2026-07-23 | Done when the milestone gate produces the stale-row review list mechanically, the spec states the sweep beside the deferred-row re-scan, the first real sweep reached him, and the threshold stands in the settings ladder as a tunable |
| 484 | **The seat declares what it did on its own** (Alexander 2026-07-27, across one morning's three asks: a wish taken in must say which queued wishes it resembles and where it went · anything picked up in passing must be done and said · a message sent to another project must be declared at the close — «декларация становится важной деталью в коммуникации»): one law with four surfaces, since each ask names the same act — the seat moved on its own judgment and the human learns it only if the seat says so. A reply closes with a short declaration block naming, in plain words: what was sent outside this window and to whom, what was picked up in passing beside the asked work, which choices were made by default and what flips each one, and what was closed, archived, or retired. A fifth surface joined the same hour (his word: text scrolls past and he runs many windows, so anything said in passing that expects a reaction must be repeated at the close): a line said mid-work that waits on him is restated in the block, since a question that scrolled away was never asked. The block stands at the reply's end so it never competes with the answer. Its mechanical net is the sending-side twin of row 473's deposit check: a turn whose action trace carries a write outside this project's tree, a message to another agent, or an archive move, and whose reply carries no declaration line, reds | surface | *queued* 2026-07-27 | Done when the law lives in one home with its four surfaces named, the communicator carries the block's shape and position, a fixture turn with an undeclared outbound write reds and a declared one passes, and one real landing report closes with the block |
| 485 | **A handed-in text is edited only where the human pointed** (Alexander 2026-07-27: in the text skills it matters a great deal to leave working prose alone — when a specific fix is asked, change that and nothing else): the text skills today may rewrite a neighbouring sentence while fixing the named one, and the author loses words he chose without being told. The law: an edit to a text the human wrote is bounded by what he named; every other sentence survives verbatim, and the proof is mechanical — the word tokens and the punctuation multiset outside the named span are identical before and after. A wider rewrite is proposed and waits for his word | small | *queued* 2026-07-27 | Done when the bound stands in one home, the text skills cite it, the preservation check runs inside the edit itself on every run, a fixture edit that touches an unnamed sentence reds, and the row-204 verifier is the check it runs |
| 486 | **A wish taken in says what it resembles and where it went** (Alexander 2026-07-27: when a new request comes in and it looks like an existing one, or overlaps it, or changes it, that has to be declared clearly at intake, along with where it goes): intake today opens a row and says nothing about the queue it joins, so a wish that duplicates, supersedes, or narrows a standing row reaches the same queue twice under two numbers. The law: at intake the wish is matched against the live queue and the archive, the near neighbours are named aloud with one verdict each — merged into row N, supersedes row N, narrows row N, independent — and the reply says which row now carries it | small | *queued* 2026-07-27 | Done when the match-and-declare step stands in the intake law, its verdict vocabulary is closed, a fixture wish that duplicates a live row reds when taken in silently, and one real intake declares its neighbours |
| 487 | **A sentence the human could not parse is repaired at its source** (Alexander 2026-07-27, calling it the most valuable of the morning's asks: every time unclear wording is written and he has to ask again, find where it came from in the documents and fix it there; when the fix carries risk, say so and decide together): a re-ask is treated as a defect report against the artifact that produced the wording — a skill body, a base rule, a spec sentence, a template — rather than as a one-off misunderstanding. The seat answers him first, then traces the phrase to its home, repairs it there, and says which artifact changed; a repair that would move meaning elsewhere is named and decided with him instead of applied. **Second deposit, 2026-07-27 evening (the tlvphotos window, on his instruction after he asked whether the rule already existed):** the same gap met in the field — he read a chat sentence built entirely from the requirement document that window was writing and answered that it was not understandable; the sentence was restated for him and nothing was repaired at the source, because no rule says to. What that deposit adds: the address of the only line that speaks to this today (`language.register` in the personal profile, which states the diagnosis and stops at "fixed upstream in how I write", an instruction with no address), the reason the defect repeats (the vocabulary reaching him in chat is the vocabulary of whatever document the session holds, so every session working from that document speaks that way and each apologises separately), and two questions for the build to settle — whether the duty belongs to the register family (text-audit, which already repairs a text from its source) or the intake family (feedback-intake, which routes what a person hands back), given that the signal arrives as feedback and is discharged as a text repair; and whether the trigger can be netted, since the chat-law hooks read outgoing text and this one reads an INCOMING message and classifies it as a re-ask ("не понятно", "что такое X", a question asked twice) | surface | *queued* 2026-07-27 | Done when the trace-and-repair duty stands in one home with the risky-repair fork named, the problem ledger counts a second re-ask of the same wording as a method defect, and one real re-ask lands a repair in the artifact that caused it |
| 488 | **An engine carries no personal trace, and a project knows which it is** (Alexander 2026-07-27: engines like the exhibition engine behind tlvphotos must contain no personal data at all, and this needs a gate — with the understanding of when a thing is an engine and when it is a personal project): the split itself is proposed at founding (row 190), and nothing checks that the public half stayed clean. The build: the project's own kind line gains the engine reading, and a project reading as an engine runs a publish-time check for personal traces — a person's name, an address, a private path, a private caption, a key, a photograph's subject — each hit naming its file and line. The check runs before anything leaves the machine | surface | *queued* 2026-07-27 | Done when the engine reading stands beside the kind line, the personal-trace check ships as a runnable gate wired into the publish walk, red-proven against a fixture engine carrying a name and a home path, and run once against the real exhibition engine read-only |
| 489 | **Every hook proves it still works, and a hook that stopped earning its place leaves out loud** (Alexander 2026-07-27: I need a mechanism that validates the hooks — maybe at every minor version — and periodically retires them, declaring it; models will soon stop writing in the banned frames on their own; plus his correction the same hour: the text-audit skill calls those same checks, so a hook quiet in conversation may be busy elsewhere): today the counter records runs and firings for seven hooks, and three carry zero firings, which reads the same whether the law is held or the machine is blind. The build: a red fixture per hook, run at every minor release, where a hook silent on its own fixture is a red release; a caller census that counts every path into the check — the session event, the skills that invoke it, the gates and scripts that import it — so quiet on one path proves nothing; and a retirement step that moves a hook out only when every path is silent past a stated threshold, with a dated line saying what left and why. The counter also learns to tell a detector from a collector, since a collector that never fires is working as built | surface | *queued* 2026-07-27 | Shipped 2026-07-27: guardrails/hook-red-proofs.json names a fixture per hook and guardrails/check-hooks-can-fire.py runs each real hook against it, failing on any hook that stays silent; every wired hook fires against its own fixture, the census reading the wired declaration as its population, and the collector half is declared unprovable this way with its reason. The run rides the full suite, which is the gate every release passes, so the release-gate leg of the acceptance is discharged by that placement (the architecture states it, no gate letter of its own). Still owed: the caller census as a standing table, the retirement threshold with its declaration, and the reporting shape of a stand-down on a turn record that cannot be read, which today looks the same as a clean turn. Done when each hook owns a red fixture the release gate runs, the census names every caller per hook, the retirement threshold and its declaration stand in one home, and one hook completes the walk — validated, censused, and either kept with its evidence or archived with its line |
| 490 | **The legibility check pairs each colour with the surface it actually sits on** (deposit from the tlvphotos window, 2026-07-27): `scripts/preshow-legibility-lint.py` pairs every foreground colour with the whole page's own background, so on a dark page carrying light cards it blocked seven passing rows and let a genuinely failing one through unnamed — wrong in both directions in one run. A gate that reports the wrong pairs teaches its user to skip it, and the next real hit gets waved past with the false ones. The fix walks ancestors to the first surface that paints a background, falling back to the page only at the root; the depositing window notes that a static reader cannot see DOM nesting, so the check either drives a browser or reasons only over the selector prefixes it can resolve | bug | *queued* 2026-07-27 | Shipped 2026-07-27: the check walks a selector's own chain to the nearest ancestor that paints a background and scores against that, and reports an unresolvable pair separately instead of guessing it against the page. The hole that keeps this row open: a bare single-class selector with no chain still falls back to the page background, which is the shape the depositing window's own failing case had, and the sure road for it is the browser-driven measurement that window used. Done when the check resolves each element's effective background by ancestor walk, the seven false hits pass and the two real ones red on the depositing window's own page, and the fixture pins both directions |
| 491 | **The pack carries reusable guidance on how a test suite is organized to stay fast** (Alexander 2026-07-27, after the tlvphotos suite reading: the pack needs reusable instructions for organizing test suites): the test method today states the level ladder, red-first proof, and traceability, and says nothing about what a suite costs to run — so a host builds a suite that takes a quarter of an hour and nobody notices until the human asks. The reading that prompted this names the reusable rules: a browser or other heavy resource is opened once per group and reused where the tests already reset their own state · a wait is a poll on the condition with a bound, and a fixed sleep is a defect · an expensive fixture is built once and cloned · the runner's parallelism is read from the machine and its queue starts with the longest suite · and every run prints its per-suite durations, since a suite with no timing has no budget. The instruction lands in the test method with the architecture step's budget duty (row 143) naming the suite's own wall clock as one of its budgets | surface | *queued* 2026-07-27 | Done when the guidance stands in the test-authoring method as rules a host can apply without reading the case that produced them, the suite-budget duty cites it, and one real host suite is measured before and after applying it · First real case (2026-07-27): the prototype fence ran one grep per (fenced file, scanned file) pair and carried 215 of the suite's 572 seconds; one fixed-string pass over the scan set cut the gate to 6 seconds and the suite to 285. The shape to name in the guidance is a check whose cost grows as the product of two lists. |
| 492 | **The pack watches its own measures and revises them on a stated clock** (Alexander 2026-07-27: a standing self-optimization setting — watch all the measures and periodically revisit and improve them, the cadence being the agent's own to design): the mechanism for acting on a standing measure already exists as row 469 (a metric past its floor opens a row) and the wording-drift sweep as row 230; what is missing is the pack's own measure list and the clock that reads it. The build: the pack declares its own measures with a floor each — hook firing rates per caller path, the suite's wall clock, document sizes against their bounds, gate counts and their red proofs, the live queue's length, the count of open defaults — and reads them at every minor release, each measure past its floor opening a row through row 469's mechanism. A measure that has sat at its floor across three reads is itself reported, since a floor nothing ever crosses measures nothing | surface | *queued* 2026-07-27 | Done when the measure list with floors lives in one home, the minor-release walk reads it and opens rows through row 469, a fixture measure past its floor opens a row and one at its floor stays quiet, and the first real release walk reports its readings |
| 493 | **A thing that cannot act is never given a person's verb** (deposit from the promoter window, 2026-07-27, carrying Alexander's own stop: he read "the requirement argues with itself before the code is written" and said he did not want to read further, then named the class half joking — cups do not fluoresce, specs do not walk in, checks do not blush): a human-facing sentence hands an inanimate technical noun a predicate only a person can perform, and the reader stops to picture something impossible while the meaning arrives late or never. The metaphor is at home in the pack's internal documents, whose audience is the pack; it fails in every text a person outside the project reads. The build has three parts: a list of predicates that need an animate subject in both working languages, a check that flags such a predicate taking one of the project's own inanimate nouns as its subject, and the fix stated as one rule — name the actor that performs the action, or say what the mechanism does. His explicit ask is that it reach the text-audit skill, since the pack's own prose carries the same habit. Neighbours declared at intake: the pre-show lint (row 170) is where the mechanical half lands, the register census and its ratchet (row 460) is where the debt is counted, and the text-audit skill is the home of the reading rule; this row adds a pattern class none of the three carries | small | *queued* 2026-07-27 | Done when the predicate list and its check ship with the pre-show lint, the text-audit skill states the reading rule and its fix, red-proven on the four sentences the deposit names, and one real human-facing text is swept under it |
| 496 | **What a running product's owner can change without a rebuild is stated as a design principle** (Alexander 2026-07-27 ~15:00, asking first whether the design principles are split by front and back and then for parametrization: today, to switch the experiment off on his photo site he has to run a full build — by design, anything that can be changed without a big build should be easy to change and deploy, a parameter, a text, an experiment switch, and the rest by our own judgment): the per-kind design principles name the visitor walk, the reachable flows, the register, the trigger — and say nothing about the seam between what ships in a build and what the owner turns from outside it. The build: every kind whose product is deployed carries a principle naming that seam — an experiment switch, a piece of copy, a threshold or budget, a feature toggle live outside the build and reach production by a deploy of configuration alone, while behaviour and structure stay in code — with the check that reds a deployed kind declaring no such surface, and the founding step recording where that surface lives. The first proof by deed is the photo site turning its own experiment off with no build. | surface | *queued* 2026-08-07 | Done when: the principle stands in the per-kind table for every deployed kind with the seam named on both sides, a founding check reds a deployed kind with no configuration surface declared, and one host changes a switch and a text in production without a build |
| 497 | **An instruction the tooling injected is never reported to the person as his own word** (Alexander 2026-07-27 ~16:20, reading another window's line that a direct instruction in the session forbade launching workers without his ask: he never said it, and his standing word is the opposite — multi-agent by default): the session's system instructions carry lines from the tooling beside the person's own standing word, and a seat that cannot tell them apart hands him a rule he never gave, then shapes its working method around it. The window that did this dropped delegation for a whole movement. The pack already holds the rule for a recorded decision — a decision written as the person's names the exchange it came from, and an unanchored attribution reds — and chat carries no such guard. The build: the attribution law reaches the seat's own sentences, so an instruction the person did not give is named as coming from the tooling, and a conflict between the two is stated plainly with his standing word deciding. The check most likely reads the reply for a claim about what he said, the same tool-boundary surface the chat laws now use. | bug | *queued* 2026-07-27 | Done when: the law states where an instruction's authority comes from and how a conflict is spoken, one home; a machine reds a sentence attributing to the person an instruction he did not give; and the deposit sent to the affected window is answered by its resumed delegation |
| 501 | **The front door is replaced by the handed-in draft, once its eleven corrections land** (handed in by the promotion campaign window, 2026-07-27, on Alexander's evening word: take the strongest material out of the public page, fold it into the README so the result is no worse than what ships, stop repeating the platform's name, audit it, and hand it over): the draft and its cover note stand in `docs/handovers/`, and the review of them is `docs/prover/2026-07-27-readme-replacement-review.md` — verdict HOLD on eleven defects, of which the heaviest sit in the opening paragraph, the page's most load-bearing text: a causal claim the records do not carry, a count that reads as the whole day, a close that omits the reissued release, two events fused into one, and a gate described as shipped that does not exist. One sentence fails the pack's own style reading. Two thirds of the draft is more accurate than the page that ships, and its correction of the guardrail section is the largest honesty gain that page can take, so the draft returns for its fixes rather than being set aside. The owner settled the one item that was his (2026-07-27 ~18:11): live-spec's own repository is the third host, so the page now says three and the draft carries that number when it lands. Update 2026-08-05 (`attic/inbox-2026-08-05-from-promoter-readme-replacement-returns-corrected.md`): the merge direction reversed — the shipped page moved four times since the July draft was written and its install section is now the better base, so today's shipped page is the base with all eleven corrections folded in, plus a twelfth the draft itself needed: the record supports two landings under adversarial review on 17 July; the draft's claim of three is dropped. Four more numbers were corrected in the same pass (the push chain's gate count, a stale line count, the install section's missing keys, the base-ref precondition). The audit has not yet reached two consecutive clean reads and stays open, and the heaviest open item is the 10 July hole (row 545), which this row's close now waits on for its one remaining sentence. | surface | *queued* 2026-07-27 | Done when: the eleven corrections land in the draft, the style reading returns zero, the owner's word settles the host count and both sentences move together, a fresh stranger read earns the second paragraph, the replacement lands on the public page with its review record beside it, and the 10 July hole's status (row 545) is stated in the page's own words |
| 503 | **The feedback ledger stopped being written, and nothing noticed** (found 2026-07-27 while taking in a deposit from the tlvphotos window): the ledger law says every handed-in item gets one dated line — when it arrived, through which channel, what it concerns, and where it went [E-28, T-20] — and the file's last line is dated 2026-07-17. Between then and today at least ten deposits were harvested from the inbox, three of them opening queue rows, and not one reached the ledger; the sweep commit that emptied the folder touched the queue, the journal, and the resume file, and left the ledger alone. A record nobody writes reads exactly like a period with no feedback, so the ledger cannot answer the question it exists for — what came in, and what became of it. The build: the ledger's law names who writes the line and at which moment, a check reds a harvest that removed an inbox file without adding a line for it, and the unwritten stretch is either back-filled from the sweep commits that hold the facts or declared closed by a dated line saying so. | small | *queued* 2026-07-27 | Done when: the writing duty names its actor and its moment in one home, a red-proven check ties an inbox file's removal to a ledger line, and the 2026-07-17..27 gap is back-filled or declared with its reason |
| 504 | **A landed lane is never torn down, and the cap counts the dead ones** (found 2026-07-27 opening a lane: the profile cap of three was already full, and two of the three were branches whose every commit had long since reached main): the lane road specifies the open act as a performable step with its own script and its own preconditions, and specifies integration as rebase-gate-fast-forward, and stops there. Nothing removes the branch or its worktree once its delta is in main, so each landed lane leaves a corpse that counts against the next lane's cap; the same tree also holds four worktrees left by earlier delegated workers. A cap meant to hold three live lanes was holding one. The build: integration ends with a teardown step — the branch deleted once merged, its worktree removed, said out loud — and the cap reading counts only branches carrying commits main does not have, so a corpse can never block a live lane; a sweep names any leftover worktree the same way. | small | *queued* 2026-07-27 | Done when: the integration walk carries the teardown, the cap counts unmerged lanes only, a red proof covers a merged branch not blocking a new lane, and the leftover worktrees in this tree are gone |
| 507 | **A dispatch to the expensive tier proves its own need, and the proof teaches a free check** (Alexander 2026-07-27 ~20:31 on the evening's spend — about a fifth of the weekly budget in half a day, six of twelve helper runs on the expensive tier for mechanical work — then his sharpening at ~20:39: comparing strings tells nobody which tier a task needs, so let the run itself answer first): the routing law is written and already judged by the conduct check, which reads the turn's record and holds worker-routing as its first law. That check spends one model call per turn and stands off by default, so the law has a reader that costs money and no reader that costs nothing. The build has three steps, and money is spent only on the middle one. First, patterns learned from past refusals turn a task away before any call, and this list starts empty and grows from the record. Second, a dispatch to the expensive tier opens with one instruction: read the task, and stop now if a cheaper tier does it as briefed, naming that tier. A refusal costs the brief plus one short line, and the orchestrator re-runs the same brief a tier down. Third, the full run proceeds when the first two let it through. Every refusal is recorded with the task text, the named tier and the reason, and a phrase that predicted a refusal three times becomes a pattern in step one. The stated weakness: a model judging its own need leans toward yes, so the instruction states the cheap tier as the default assumption, and the record makes the lean visible — refusals near zero over fifty dispatches means the instruction fails. The paid whole-record reading stays available for the case a stated reason covers mechanical work, and it turns on by the owner's word. | surface | *queued* 2026-07-27 | Done when: an expensive dispatch carries the self-check instruction, a refusal is recorded with its task text and named tier, the orchestrator re-runs a refused brief a tier down, the learned patterns turn a task away before any call once three refusals agree, a red proof covers a refusal and a genuine design task passing through, and the weekly spend report shows the tier mix a week later **Run as a stated experiment (his word 2026-07-27 ~20:42).** Hypothesis: a run that answers for its own need turns itself away often enough to move the tier mix, and the refusals it leaves behind teach a check that costs nothing. Measures, all read from records that already exist: the share of helper runs on each tier per day; the count of refusals against the count of expensive dispatches; the weekly spend from the usage report; and the count of tasks re-run a tier down that came back needing the expensive tier after all, which is the cost of a wrong refusal. Baseline, measured today: twelve helper runs, six on the expensive tier, about two million output units across the evening, and zero refusals, since no instruction asked for one. Duration: one week from the day it ships. Decision rule stated before the run: the expensive share falls by a third or more and wrong refusals stay under one in ten, the instruction stays; the expensive share holds and refusals sit near zero, the instruction is judged inert and the paid whole-record reading is tried instead; wrong refusals pass one in ten, the instruction is rewritten toward caution. The experiment's own shape belongs to a wider question the queue already holds — measurement plugins after shipping (row 48) and A/B tests as a pack capability (row 49), both deferred; this run is their first real subject and their trigger. |
| 509 | **A law that binds hosts reaches every host by itself** (Alexander 2026-07-27 ~23:14, on the parametrization principle heading for every project: when a new architecture principle, test rule, or design rule lands, every project has to be told, and that holds for whatever else turns out to bind them): today a law lands in this repo and no project learns of it until a person carries the news. Half the machinery is built. A daily check asks the public repository whether the pack moved past this machine and proposes an update for the human to accept, and its founding arm names any founding question added past the version a host recorded. What that check reports is a version number and a list of new founding questions. It never says which landed laws bind a host, and it deposits nothing into any project's intake folder. The build: a landing that binds hosts records what it binds and in which version, in the pack's own list; the daily check reads that list against the version a host recorded and reports the binding laws by their plain names; and a deposit lands in each known project's intake folder naming the law, what it asks of that project, and where to read it. The deposit channel already exists and is used by hand. Kin: earned notifications deposited automatically (424), version alignment as a stated rule (231). | surface | *queued* 2026-07-27 | Done when: a host-binding landing records its binding entry, the daily check names those laws in plain words against a host's recorded version, a deposit reaches every known project's intake folder with what the law asks of it, a landing that binds no host deposits nothing, and a red proof covers both directions |
| 510 | **A part of a set is named by what its members are** (Alexander 2026-07-27 ~23:31 and the evening that followed, reading this movement's plan: a group called by its size names nothing, and a reader who forgets the number cannot recover the reference. His evidence is his own reading that morning of a page whose items were labelled by position — «Опора 4: это 4й пункт и либо он психологически помогает понять что это либо как сейчас мы потеряли пользователя»): a text points at part of a set and gives the reader no name for that part. The defect has three shapes. Naming by count identifies the part by how many members it holds. Naming by pointer identifies it by a bare word — the rest, the others, several, certain. Naming by position identifies it by where it sits in a list — the first three, the latter, item four. The rule carries two duties: name the part by what its members are, and keep that name in every later sentence of that document and of its neighbours. A count standing beside a name the sentence already gave passes, and a count reporting a measurement passes. The reader's test: a person who reads this sentence alone can say what the members of this part are. The build: the class enters the language-rule home with its rule and its examples; the model judge holds it on the chat surface and the document surface; a pattern pass catches the shapes a machine can see, a number word before a plural noun with no naming word beside it and the bare pointers; and the shipped documents are swept for the instances already in them. | surface | *queued* 2026-07-27 | Done when: the class lives in the language-rule home carrying its rule, its reader's test, and both duties; the judge holds it on the chat surface and the document surface, with a red proof over an offending line and a silent proof over a passing count; the pattern pass states its own reach; and the sweep's instance count for each shipped document is recorded beside the repairs that landed |
| 511 | **A finding another project needs travels on its own, and today nothing notices when it stays home** (Alexander 2026-07-27 evening, carrying by hand a lesson the tlvphotos window had already written down: a worker there restored a shared client file with a git command, and three minutes later another lane began writing the same sources, so unsaved edits came close to being erased. That window recorded the near miss in its own checkpoint at 23:09, sent the rule to its workers, and deposited the message here at 23:44, after the owner had already carried the lesson by hand): the rule that a session deposits a cross-project finding in the other project's intake folder is stated in Requirement 195, in the pack's shared rulebook, and in the architecture, which says plainly that the deposit is model behaviour reading installed skill text. Its trigger is the agent's own recognition of its own work, and its grounds are a closed set — blocked work, a lived fault carrying evidence, a concern no zone owns. An event that cost the sender nothing names none of those grounds, so the rule as written never asked for this deposit; the window deposited on its own judgment past the stated grounds. Every armed check reads deposits that landed; the check that would notice an absent one is missing, and nothing measures the hours between a finding and its deposit. Two facts sit beneath this: the same lesson was queued on 2026-07-23 as row 479 and never landed, and the tlvphotos copy of the shared rulebook records an older version than the mirror on this machine. A third fact, found 2026-07-28 while checking the neighbours: `~/track-coach/` holds an intake folder and nothing else, since that project's working tree was removed after its 1.7.0 release, so the deposit left there on 2026-07-21 has sat unread in a folder no session opens. A fourth, found when the push gate read the deposit: it names its birth in prose and carries none of the three fields the format asks for, so the gate that guards the door refused it after its content had already been harvested into row 479 and into this row. The door's own check reads a message the moment a push runs rather than the moment it arrives, so a sender learns of a malformed message through the receiver's blocked push. The build: the grounds gain the near miss, an event that cost nothing this time and would cost the neighbour next time; a check reads the turn's own record and reds a session that named a fault inside another project's zone with no write into that project's intake folder in the same run; and a host whose vendored rulebook falls behind the mirror is told where its own people read. | surface | *queued* 2026-07-27 | Done when: the near-miss ground is stated in the spec beside the existing grounds; a red proof covers a turn that names a neighbour's fault and deposits nothing, and a silent proof covers a turn that deposits; the version drift between a host's vendored rulebook and the mirror is reported in that host's own tree; and row 479's lesson lands with the deposit that carries it |
| 517 | **A spec complete about every writer of a shared surface says nothing about what its reader holds** (deposited by the tlvphotos window, 2026-07-28, with its worked case at that project's `docs/prover/2026-07-28-walk-step-told-line.md`): an exhibition's screen-reader announcement had a law in the spec, a node in the architecture, and five green matrix rows, and was wrong in three ways for weeks. Every clause of the law was true of the code and each writer obeyed its own discipline; nothing asked what the person receiving the surface ends up holding. Read from the visitor's side, a step onto a work whose line already stood announced the caption alone, an arriving batch of ten lines read out nine stories about pictures further along, and a single work's own line announced nothing. One sentence written from the reader's side — the region carries what the visible wall label carries for the work in view — collapsed four questions into one. The shape is general: wherever several producers write to ONE surface a person consumes — a live region, a status line, a log, a notification stream, a shared document section — the coverage inherits the blind spot, since each row asserts a writer behaved and no row asserts what the consumer holds. Three candidates came with the deposit: a spec-authoring prompt in the facet sweep that states the surface's content from the consumer's side and derives each writer's discipline from it; a matrix rule that a shared-surface fact owes at least one row asserting the whole surface at a moment; and a prover lens asking what the consumer holds after each interleaving of N writers. | surface | *queued* 2026-07-28 | Done when: a surface with more than one writer carries one sentence stating what it holds at any moment from the consumer's side, the writers' disciplines are derived from that sentence, the matrix rule is stated and red-proven on a fixture whose per-writer rows pass while the whole-surface row fails, and the prover lens is written with the tlvphotos case as its worked example |
| 525 | **The register column of the findings record reads zero for every document, and the reading never ran** (found 2026-07-28 while landing the findings ratchet and reading the chat-law text): the census takes each reading's count from that check's own record line. The register check prints a record carrying a severity and no count, so its findings fall through to zero. All 109 recorded documents therefore carry a register count of zero, and two of them were proved to carry real findings by running the check by hand. The specification holds two, and the matrix holds fourteen. The same shape reaches any reading whose check changes its output, so the repair covers the class: a reading that produces no count refuses the run instead of scoring zero. Repairing this raises real counts, and the ratchet now forbids a raise, so the landing records the higher numbers as a hand edit stating this row as its reason. | small | *queued* 2026-07-28 | Done when: a reading that produces no count refuses the run and names the document and the reading, the register column holds measured numbers for every recorded document, the raised counts carry their reason in the record, and a fixture check whose output carries no count reds before the fix and passes after |
| 527 | **A completed recovery leaves the worker-command check red for a day, and nothing clears it** (found 2026-07-29 when a worker ran a command that discards uncommitted work): `guardrails/check-worker-restore.py` reads the worker runs' own records and reds on a command that discards uncommitted work. The record of that run stays on disk, so the finding stays true forever, and the suite arm reads a window of 24 hours. The law the check states names the way out: the orchestrator restores the file, briefs the worker afresh, and records the halt. The check reads none of that, so a finished recovery clears nothing and every push waits a day. Alexander's word is owed on one question of policy: whether a recorded recovery closes a finding, and what carries the record of it. | small | *held for Alexander* 2026-07-29 — policy: what closes a finding of this kind | Done when: the way out is stated in a requirement, a recorded recovery clears the finding it names, the record of a recovery has one home, and a fixture recovery clears a red that stands without it |
| 529 | **A reason written once licenses every later raise of the same ceiling** (found 2026-07-29 by the prover pass over the night's two repairs, `docs/prover/2026-07-29-ratchet-arm-and-extract-recheck.md`): the census copies an entry's reason field forward on every write, and the check passes any raise on an entry that carries one. So the first reason a person writes stands as a standing licence for that document, and the ceiling above it moves freely ever after. The record holds 109 entries and none carries a reason yet, so the first landing to write one opens the hole. The same record carries six more findings: a ceiling born above its measured count in a new entry passes unread, the check reads the upstream branch where the pushed reference stands on the hook's own input, an empty identity falls back to the newest transcript, the census report still prints zeros from a reading that refused, the extractor takes a leading part of an identity where the criterion asks for the whole, and one criterion promises silence from an arm that names the copy it read. Alexander's word is owed on one question of policy: whether a reason is spent by the raise it justified, or stands until a person removes it. | small | *held for Alexander* 2026-07-29 — policy: whether a reason is spent when it is used | Done when: a reason binds the one raise it justifies, a new entry born above its measured count reds, and each of the record's six other findings is closed or answered in its own line |
| 531 | **The rotation check understands one document's shape, so every other rotation is unproven** (found 2026-07-29 while splitting the journal at its byte bound): `guardrails/check-doc-rotation.py` reds an archive that no live pointer names, and it reads pointers of one shape alone, the queue's numbered rows. It scans the queue by default. The journal reached its bound tonight and was split by hand, and no check proves that the entries which left still stand somewhere whole. The rotation tool `scripts/rotate-doc.py` says the same thing in its own words: it understands the queue's table and nothing else. Every growable document carries a bound, and each one reaches it in turn. Each split then runs by hand under no proof. A live reproduction landed 2026-08-05 from the tlvphotos window: `NEXT_STEPS.md` there hit the same gap on a real rotation — fifteen closed resume anchors moved to `docs/queue-archive/rotated-NEXT_STEPS-2026-08-05.md`, a line-multiset check proved nothing lost, and gate t still reded with `no manifest: <archive> exists but no live manifest line points to it`, because `MANIFEST_LINE_RE` reads only the `rows N,M → path` shape and no date-anchored document ever has row numbers to give it. That host's own fix, local to its copy, is a second manifest-line form, `<what moved> → <archive path>`, proving only that the archive exists and carries content rather than the row-level nothing-lost claim the numbered form proves; the case also shows `scripts/rotate-doc.py` missing entirely from a using host's tree, though both gates and the failure message name it as the remedy. | small | *queued* 2026-07-29 | Done when: a rotation of any growable document carries a pointer the check reads, the check proves the archived part and the live part add back to what stood before, a fixture archive missing a section reds before the fix and passes after, the fix admits a second pointer shape for a document whose closed portion carries a date and no row numbers (the tlvphotos host's `<what moved> → <archive path>` form is one candidate), `scripts/rotate-doc.py` ships wherever the gates that name it run, and a red proof for gate t is added to `guardrails/gate-red-proofs.json` using this rotation as its fixture |
| 536 | **Three source rulings from the readability pilots need Alexander's word** (found 2026-07-30 across the communicator, text-audit, and live-spec-base source pilots, `~/context-slimdown/reports/communicator-audit-sweep.md` and its sibling sweeps): rule 31 of live-spec-base uses the word "owner" for two different actors in one rule — the owning agent in "the zone's owner is presumed competent" and the person in "the third crossing goes to the owner" — and the data-and-contracts law turns on which one governs a neighbour's data release; the rewrite removed its own invented ruling and restored the source's silence. A design review is routed to two different skills: text-audit's own line sends it to product-prover, and design-reviewer's roster line claims it too, though the two skills' own descriptions already resolve which each does. And the communicator skill carries rule collisions the sweep counted at fourteen, with no stated precedence between them — a number-context rule against its own neighbour's example, two rules claiming the same closing slot, a critical-bug rule read two ways at once, and more, catalogued row by row in the sweep under its N-numbered tags. | small | *held for Alexander* 2026-08-05 — policy: what "owner" means in rule 31, which skill runs a design review, and precedence among the fourteen rule collisions | Done when: rule 31's two meanings of "owner" are resolved with one ruling stated in the source, the design-review routing is stated once in the source both skills read, each of the fourteen rule collisions the sweep names is given a stated precedence or is merged into one rule, and the source sweep's N-tagged rows are marked closed against this row |
| 537 | **The repo adopts the installed 2026-07-30 slimdown** (found 2026-07-30 by the Fable audit in the home window, `attic/inbox-2026-07-30-context-audit-from-fable.md` section 7; the adopt direction is backed by Alexander's answered word recorded in that same message's section 6, 00:23): the night's context-slimdown edits touched installed copies only, so the repo now drifts from what is running. `tests/test_config_health.py` and `guardrails/check-config-health.sh` red on installed-vs-repo drift across `chat-law-hook.sh`, `register-judge-collect.sh`, `register-judge-report.sh`, and all eleven skills' description frontmatter. (`midturn-chat-scan.py` stood in that list until 2026-08-17, when the script was retired; the drift it carried leaves with it.) This row's first act is a freshness re-check: readability rewrites landed in this repo since 07-30 may already have closed part of the gap by hand, and the row starts by diffing installed against repo before copying anything. Where the gap still stands, the row commits the installed files into the repo, adds an installer guard so `scripts/install-session-hooks.sh` refuses to register a hook command already present, and adds a test asserting each hook command appears once (closing the doubled-reminder root cause the same message records). | small | *queued* 2026-08-05 | Done when: `tests/test_config_health.py` and `guardrails/check-config-health.sh` pass clean on the actual current drift after the freshness re-check, the installer refuses a duplicate hook registration with a fixture proving it, a test asserts each hook command appears exactly once, and the row's notes state which files the freshness re-check found already resolved versus still copied · groups as one queue bundle with rows 538, 539, 540, 545, 548, 560, 561, 613 — things a new host is promised at install time but does not actually receive |
| 542 | **A session sweeps the servers it leaves listening** (found 2026-08-05 by the tlvphotos window; its report rode the inbox and is kept in git history by this harvest commit): three local web servers from sessions 8 to 22 days old still listened on all network interfaces. The system asked the owner to approve incoming connections again and again, and he suspected the day's tests. A server bound to the local address never raises the dialog. The report offers two lines for the pack: a session's own server binds `127.0.0.1`, and a session that served anything lists its listening sockets before handing over and stops them. A socket older than the session is a finding in itself. The report notes this sits beside the browser-cleanup and orphaned-child rules, the same family with a different resource. A mechanical guard is possible: red when a listening socket bound to every interface belongs to a process started from the project tree. | small | *queued* 2026-08-05 | Done when: the pack states the two lines in the rule family that owns process cleanup, the sweep step names the one command that lists the sockets, and either a guard reds an all-interface listener from the project tree or the queue records the decision to hold the rule by hand |
| 550 | **An entry recording the person's word carries the words themselves** (found 2026-08-06 by Alexander, reading the correction of a fabricated entry): `guardrails/check-authority-anchor.py` asks an entry for a date and asks for nothing else. A session can therefore write its own reasoning under his name, stamp a real date on it, and pass. That happened on 2026-08-05 with the entry on the reading queue, and the date was what let it through. He named the remedy himself: record his words as he said them and point at where he said them. `scripts/session-extract.py` already pulls his turns out of a transcript with their timestamps, so the pointer has a source. | small | *queued* 2026-08-06 | Done when: an entry on the decision record carries a quotation of the person's own words and a pointer to the exchange, the check reds an entry that carries a date alone, a red proof drives it, and the existing entries either gain their quotations or move to the pack's own voice · Stage 3 C2 note 2026-08-12 (`.live-spec/stage3-verdicts-2026-08-12.md`, gate r): the verdict confirms this row's own repair as the smallest close |
| 552 | **An edit to a requirement carries its generated pages with it** (found 2026-08-06 across four blocked pushes in one morning): the spec's code table lives in two places. One stands in `PRODUCT_SPEC.index.md` and one sits inside `PRODUCT_SPEC.md` under its Reference heading. `scripts/build-index.py` writes the first when it is called with the document and an output path. The second is written by hand. Splitting one criterion renumbered the criteria after it, and the two copies disagreed until a later commit caught the second one up. Each catch-up commit then aged the records the push gates read, so the same push was refused four times over one edit. | small | *queued* 2026-08-06 | Done when: one command run after a spec edit writes both copies, a check reds a tree where the two disagree, the command is named in the place a person looks after editing the spec, and a red proof drives the check |
| 554 | **A test proves its mechanism on the smallest input that carries the proof** (Alexander 2026-08-06 at 11:03, reading the suite that outgrew its time: he called the slow run a defect in how the test was built and asked that the spec hold the class): the test proving the runner reports green and red copies the tree and runs every test inside itself. It executes the whole body of work to prove the wrapper around it. The same shape can grow anywhere a test drives a script that drives other tests, and each such test costs the whole suite again. The spec states no rule against it today, so queue row 553 fixes the one instance while the class stays open. | small | *queued* 2026-08-06 | Done when: the spec states that a test drives its mechanism on the smallest input carrying the proof, the test-writing skill carries the same sentence, a check names any test whose run re-executes the suite it belongs to, and row 553's instance passes the new rule |
| 566 | **Board-ready statements for the whole queue** (Alexander 2026-08-06 ~21:00, on record in DECISIONS.md: soon enough he wants to see everything on the board, readable): every open queue row gets an authored echo-name, a plain behavior description, a plan of few deliverables and an estimate, taken through the board's entry check in batches, so the whole backlog reads as cards | large | *queued* 2026-08-06 | Done when: every open row shows on the work board with its validated statement; the far tier stays off per its own law; checked by his eye over one full column scan |
| 567 | **The register check the session rules name must exist on every host** (inbox, from-tlvphotos 2026-08-06, at the owner's word: the session rules' law 3 names `scripts/preshow-register-lint.py`, which exists only in the pack's own tree — on a host the law cannot be obeyed, artifacts go unchecked, and a gate nobody can run teaches the session that named gates are theatre): ship the checker with the pack so adopt and catch-up land it in the host tree, or re-word the law to name what a session actually does; sweep the session-rules hook text in the same change | bug | *queued* 2026-08-06 | Done when: on a fresh host tree the command the law names runs and exits meaningfully, or the law names no script; the tlvphotos window can obey law 3 as written; the inbox report is atticked with its row named |
| 568 | **The method's cost per change is audited and made proportionate** (Alexander 2026-08-07, 00:17 to 01:10). The work feels slow and he suspects redundant steps. Audit every fixed step a change pays. Give each step its measured price, the rule demanding it, and that rule's author: his word, a derivation, or the method's own invention. An invented standard is dropped on sight — his 01:10 word. Numeric size caps are out; the one standard for any text is no redundancy. A step that ran before its subject settled is named premature, like the 26 test-plan rows written for the board mid-conception. A cost reported as fixed is checked against where it still fires. | large | *in-work* 2026-08-07 | Done when: the audit page lists each fixed cost with price, demanding rule, author, and verdict; each repair stands as its own row; he has read the page |
| 570 | **The fixed context a working session carries is measured and cut** (Alexander 2026-08-07 00:17): the context still seems too huge. The conversation is the variable part; the fixed part is the pack's rulebooks. The base skill is 68 kilobytes, and each loaded working skill adds 46 to 69 kilobytes. A session holding three carries roughly 45k tokens of rules before any work. Anthropic's own skill guidance keeps a skill's main file near 500 lines, with detail in reference files read at need. Measure the fixed load, then cut it to his one standard: nothing removable without losing meaning. Progress 2026-08-07: the spec reviewer's file cut 69,297 to 66,675 bytes and the shared rulebook 67,781 to 66,472, each verified by a fresh reader against the old text; remaining in measured order: the pipeline skill 63,938, the spec writer 62,658, the reporting skill 45,831, the text audit 30,365, the design reviewer 27,827, the test author 18,533, the publisher 13,169, the two feedback skills 7,544 and 7,088; the micro-numbers the rulings page names go with each file's pass, and the pre-cut editions copy of the spec reviewer awaits its rebuild. | large | *in-work* 2026-08-07 | Done when: the load is measured before and after; working sessions load the smaller forms; a sampled check shows no rule lost |
| 575 | **During-work test runs scope to the change; the full suite holds every landing** (the cost audit, row 568, step-7 verdict; Alexander's ~01:10 class decision in DECISIONS.md: a standard the process holds itself to is his, derived and justified, or absent — the run-everything-on-every-working-run instruction is process-invented). During work a session runs the tests near its change. The full suite still runs at every landing and at the publication gate, so nothing ships with less proof than today. Find the instruction's home and reword it there. | small | *queued* 2026-08-07 | Done when: the instruction's home states the scoped rule; a landing demonstrably still runs the full suite; the cost page cites this row as the repair |
| 576 | **Every number the process invented is found and rooted out** (Alexander 2026-08-07 09:16: надо найти ВСЕ эти дебильные числа которые ты придумал и выкорчевать; his ~01:10 class rule on record: a standard the process holds itself to is his, derived and justified, or absent). Enumerate every numeric standard the tree holds — floors, caps, targets, budgets, thresholds, defaults — across tests, gates, scripts, skills, spec defaults and published pages. Classify each: his word with its citation, derived with its justification, or invented. An invented one is removed or replaced by the justified form, one landing per coherent batch. | large | *in-work* 2026-08-07 | Done when: the classification table stands as a page with every number's home and verdict; every invented number's removal landed or queued as its named batch; he has read the table |
| 581 | **A session announces anything that can raise a system dialog before it runs** (deposit from the tlvphotos window, 2026-08-07 ~01:35, carrying the owner's own words that night). Twice in one session a step raised a keychain dialog on his screen: a deploy script reading a hosting key, and four launches of a browser binary the machine had never run. He interrupted the work both times and said he always presses Deny. The session sees no dialog — the request blocks a foreign process or returns an empty secret — so the only channel that reports it is the person interrupting. The pack's rule for the neighbouring case covers a server left listening after a session ends; ordinary work reaching for a guarded resource has no rule at all. The ask is one line before the command, naming the dialog and the button that ends a repeated one for good. Intake 2026-08-07 — door: feature · kind: skill · footprint: the conduct family in the base skill · map: beside the cleanup rule the 2026-08-05 deposit landed | small | *queued* 2026-08-07 | Done when: the conduct family states the rule and lists the commands known to raise a dialog; a session running one of them shows its announcement line; the deposit is archived with this row named · groups as one queue bundle with rows 582, 583 — three related requests from one day's messages with the tlvphoto project: warn before a dialog pops up, help him stay focused, show early progress on long builds |
| 582 | **The pack names the thread in hand and the threads frozen behind it** (deposit from the tlvphotos window, 2026-08-07 09:54, carrying the owner's words: the pack has to learn to help the client focus). That project carries five prototype pages open at once. One evening went to two of them while the two he cares about moved not at all, and his reading was that neither can be shipped. The queue and the resume anchor answer neither question he asked: which single thing is being finished, and what is left alone until it is. A queue holding eleven open rows reads to him as a project that ships nothing. The narration law makes a session say what it is doing and stays silent on what it is declining to do. Intake 2026-08-07 — door: feature · kind: skill · footprint: the resume file and the narration law · map: beside the work board, row 166 | surface | *queued* 2026-08-07 | Done when: the resume file states the thread in hand and the frozen threads by name; a check refuses a new surface while two stand open and unshipped; the tlvphotos window reads its own resume file and finds its focus there |
| 583 | **A long build owes an early showing, and a report shows one thing** (deposit from the tlvphotos window, 2026-08-07 09:54, carrying his ask for an audit of this class). A night block wrote 1,556 lines over four hours and shipped a page he read as a prototype the next morning. Nothing in that block reached him while it was being written, so the whole cost landed before the first word of feedback. The morning report then handed him six addresses, three of them pointing at work he had already judged, and he spent his own attention finding the one new thing. Row 569 covers the root a block announces before it starts, and this row covers the stretch after it starts. Intake 2026-08-07 — door: feature · kind: skill · footprint: the pipeline's showing moments and the report form · map: beside row 569 and the reporting skill | surface | *queued* 2026-08-07 | Done when: the audit names how a session decides how long to build before showing, and what stops a build running past the point where feedback would have changed it; a ceiling on unshown work stands in the method with its own trigger; the report form shows one thing; he has read the audit |
| 584 | **A session spends its context and its tokens deliberately** (deposit from the tlvphotos window, 2026-08-07 14:14, carrying his words: work so as to spare the context and spend tokens efficiently, without cutting quality, and write this down). He asks whether the pack already carries the rule. The moves he named are reading the narrowest slice that answers a question, keeping working notes out of his chat, batching independent tool calls, and reporting the finished answer. The depth of the work itself stays where it is, and the saving comes out of process. Where it bit that day: a cost measurement went through three instrument designs before one of them measured anything, and each failed design was reported to him as it happened. The tension the pack owes an answer to is that the narration law asks for a line at every step that passes, which read literally pushes toward more chat. Rows 568 and 570 measure the cost and the fixed load, and this row is the working discipline. Intake 2026-08-07 — door: feature · kind: skill · footprint: the narration law and the base rules · map: beside rows 568 and 570 | small | *queued* 2026-08-07 | Done when: the base rules state the thrift discipline; the narration law says which turns earn a line and which stay the session's own business; the tlvphotos window can cite the rule where it failed to reach a session |
| 586 | **A worker restored two files and dropped work the tree already held** (found 2026-08-09 01:06 during day 1 of the culling). A census worker ran the full test suite for its timing, which regenerated `docs/PROGRESS.md` and `guardrails/progress-baseline.json` in the live tree. Both files already stood modified and uncommitted before the culling began. The worker put them back with `git show HEAD:<path> > <path>`, which reports itself as a read, and the uncommitted state went with it. The rule against discarding uncommitted work names `git checkout`, `git restore`, `git stash`, `git reset` and `git clean`, and `guardrails/check-worker-restore.py` watches for those. A redirect out of `git show` walks past all of them. Both files here are generated, so the loss is one stamp. A worker briefed the same way on an authored file would carry off that file's edits. Intake 2026-08-09 — door: bug · kind: infra · footprint: `guardrails/check-worker-restore.py`, the restore rule in the base rules, and the worker brief form · map: beside base rule 7 | bug | *queued* 2026-08-09 | Done when: the restore rule names the effect it forbids, with the five subcommands as examples; the check reds on any command that writes a tracked file back from git history; a fixture proves the red before the fix and the pass after |
| 588 | **R5's rule-price pins have rotted, 38 of 53** (found 2026-08-11 by the thirty-commit push review, `docs/prover/2026-08-11-thirty-commit-range.md` finding 7). `.live-spec/r5-rule-prices-2026-08-11.md` pins 53 rules by file and line, all landing correctly at its own commit `d11331f`; at HEAD only 15 land and 38 have rotted, because three later commits in the same range edited the skill bodies the pins point at. `87ffce6` named exactly this class of defect and repaired it only in the plan, leaving R5's own table untouched. `guardrails/check-pin-drift.sh` reads `ARCHITECTURE.md` alone, so `.live-spec` pages carry no guard. Intake 2026-08-11 — door: bug · kind: infra · footprint: `.live-spec/r5-rule-prices-2026-08-11.md` · map: beside the plan's D-series decisions | bug | *done* 2026-08-12 | One-line answer 2026-08-12: met and verified — the push review of `e8900d9..493f094` (finding 12) read all 53 pins against the lines they name and found 53 of 53 landing exactly, and `0e95f45` widened `guardrails/check-pin-drift.sh` to read the page, so the guard now holds them. Done when: R5's 53 line pins are re-derived against the tree, or the page is stamped as a dated snapshot — the cheaper fix `87ffce6` already chose for the plan · Stage 3 C2 note 2026-08-12 (`.live-spec/stage3-verdicts-2026-08-12.md`, gate g): the gate today reads one fixed path, and the smallest close takes its file set from a declared list, `ARCHITECTURE.md` first, then sweeps and lands what the widened reach finds; the repair may already be under way in the worker holding this file today |
| 589 | **A worker's final report fabricated its facts while its tree work stood sound** (found 2026-08-12 by the orchestrator's acceptance read of the D2 slice). The worker that cut rule 30 returned a report whose quoted rule text, invariant code (INV-158 for the real INV-164), byte counts and file list matched nothing in the tree; the diff itself was correct, and three missed live citations (two matrix rows, queue row 424) were the only real gaps. The class: a delegated report is a map of the work; the tree is the work. Intake 2026-08-12 — door: bug · kind: method · footprint: worker briefs, the acceptance step | bug | *queued* 2026-08-12 | One-line answer: acceptance reads the diff and the suite before any record quotes the report; the standard worker brief gains a line — report only facts re-read from the file after your last edit — on its next edit. Amended 2026-08-12 ~03:19: the worker resumed after its premature stop, finished the cut, and its second, final report matched the tree line for line, naming the two files the first report missed — the defect narrows to a mid-run stop that returned an invented report as if final. Amended 2026-08-12, two more dated instances, same class, both caught by the orchestrator's acceptance read: at 08:12 a worker stopped with "waiting for the monitor task to report the suite log's completion" and returned that as its final result; at 08:27 another stopped with "waiting on two background verification runs to finish before committing"; both resumed and finished correctly once told to run in the foreground and read the log themselves. One-line answer: a worker brief carries two standing lines — start no background job and wait on no monitor, and read every command's output yourself in the foreground |
| 590 | **The retired rule number is invisible to a host** (found 2026-08-12 by the skill-creator review of the rule-30 cut, `docs/skill-review/2026-08-12-live-spec-base.md`). The rulebook holds 34 rules numbered 1–35 with 30 absent, and no shipped skill sentence says the number was retired; the record lives in `DECISIONS.md`, `NEXT_STEPS.md` and a test docstring, and `scripts/sync-skills.sh` ships skill directories alone. Intake 2026-08-12 — door: bug · kind: skill text · footprint: `skills/live-spec-base/SKILL.md` head | bug | *done* 2026-08-12 | One-line answer: one clause at the rulebook's head names the hole; it lands inside the first stage-2 batch that already edits the rulebook, under campaign rule 2 it otherwise waits his word or campaign close · groups as one queue bundle with rows 591, 592, 593, 595, 597 — small findings from one cleanup after a rule was retired — marked done, still waiting to be filed away; row 593 here is disputed by row 603 |
| 591 | **Matrix row M-313 names a home the file no longer carries** (same review, build-pipeline record). The row lists build-pipeline's gates section among the homes of the no-check-from-checkability law, and the section now carries only the compaction half. Intake 2026-08-12 — door: bug · kind: doc drift · footprint: `TEST_MATRIX.md` M-313 | bug | *done* 2026-08-12 | One-line answer: drop the stale home leg from the row (frozen-doc edit with refreeze), or return one sentence to the file — decide inside the stage-2 batch that touches these homes |
| 592 | **A guard test passes on a string's accidental scarcity** (same review). `test_build_pipeline_carries_compaction_every_pass` asserts only that `INV-164` appears somewhere in build-pipeline's file, so deleting the bullet while leaving the code in a comment stays green. Intake 2026-08-12 — door: bug · kind: vacuous gate · footprint: `tests/test_compaction_discipline.py` | bug | *done* 2026-08-12 | One-line answer: pin the assertion to the bullet's own sentence; lands with row 591's batch |
| 593 | **The rule count lives in four homes and three carry no guard** (same review). The rulebook's frontmatter count is derived and asserted; the copies in build-pipeline line 73, communicator line 12 and communicator `references/words.md` line 31 are read by no test or gate, and the cut cost four hand edits. Intake 2026-08-12 — door: bug · kind: one-home law · footprint: the three unguarded copies | bug | *done* 2026-08-12 | One-line answer: either the three copies lose the literal number (a pointer to the rulebook), or a test derives and asserts all four — stage-2 batch on these files decides which cut is cheaper |
| 595 | **Three small restorations recommended for rule 7's rewrite** (found 2026-08-12 by the skill-creator review `docs/skill-review/2026-08-12-live-spec-base-2.md`, findings 1/2/3/5, verdict pass). The lead-in naming the eight bullets one family came out; the shared document's pack-wide name "convergence point" came out while five other homes still carry it; the by-hand route to `scripts/open-lane.sh`'s four preconditions came out; one closing pronoun lost its actor. Intake 2026-08-12 — door: bug · kind: skill text · footprint: `skills/live-spec-base/SKILL.md` rule 7 | bug | *done* 2026-08-12 | One-line answer: fold all four at the next batch that touches the rulebook (~45 bytes back against 305 saved), with the S3 clean-context check re-run on the folded text |
| 596 | **Personal-layer style-check text surfaced inside four workers' tool results as an instruction, and cancelled one command** (found 2026-08-12 by this pass's orchestrator, reading its own workers' tool output). Text belonging to the orchestrating session's personal style checks — a mechanism that lives outside this repository, in Alexander's personal layer — appeared inside the tool results of four separate workers during this pass, worded as an instruction addressed to them; in one of the four it cancelled a worker's command outright, costing that worker a retry. All four workers treated the text as data and carried on, which is the behaviour their briefs ask for — no worker acted on the embedded instruction. Nothing in this repository can close this row: the checks are not part of this tree, so this row holds the record and the evidence alone. Intake 2026-08-12 — door: bug · kind: infra · footprint: outside this repository (Alexander's personal layer) · map: beside row 589's worker-acceptance class | bug | *queued* 2026-08-12 | One-line answer: this repository cannot fix its own cause; the fix belongs to Alexander's personal layer, and this row stands as the dated record and evidence of the cost — four workers saw the text, one command was cancelled and retried |
| 597 | **`scripts/sync-mirrors.sh` read every failure of its existence check as "no mirror repo yet"** (found 2026-08-12 by this pass, reading the sync output after both of today's pushes). A missing credential, a network failure and a missing `gh` binary all produced the identical "no mirror repo yet" line, indistinguishable from a genuinely absent repository; the sync after both of today's pushes reported "no mirror repo yet" for every skill while the public mirror `happysasha18/product-prover` existed. Intake 2026-08-12 — door: bug · kind: infra · footprint: `scripts/sync-mirrors.sh` | bug | *done* 2026-08-12 | One-line answer: fixed in commit `7520a42`, which separates the three states — missing credential, network failure, missing `gh` — from a genuinely absent mirror, and stops the sync loudly when the check cannot answer instead of reading silence as absence. **Corrected 2026-08-12** by the push review of `e8900d9..493f094` (finding 4): `7520a42` could reach none of its own branches. The script runs under `set -euo pipefail`, where an assignment whose command substitution exits non-zero is itself the failing simple command, so `gh_view_err="$(gh repo view ...)"` killed the script before the status was read; the loud message and the pre-existing `no mirror repo yet` skip were both unreachable. Closed for the second time in `2d14354`, which seeds the status and guards the assignment with `|| gh_view_status=$?`, with two tests in `tests/test_mirror_autosync.py` that read the script's own lines and run them under a failing `gh`, proven red against the pre-fix script |
| 598 | **A worker discarded uncommitted work in another project, tlvphotos** (found 2026-08-12 by `guardrails/check-worker-restore.py` at the push pass's verify step). Session `af22b716-c9d7-48b2-b3fd-2be1820a1a14`, working in `/Users/sashaabramovich/tlvphotos`, ran `git checkout -- lab/data/step3-grid-derivation.json` at 2026-08-12T06:05:40Z; the file's last commit before the discard was 2026-08-11T23:41:50+03:00, so the command dropped uncommitted edits laid on top of a committed version rather than destroying a file that had never been saved. The incident belongs to tlvphotos — its own session owns recovery and any repair — and live-spec holds the record alone: the finding is named in `DECISIONS.md` (2026-08-12), the gate's counting start moved past it in `guardrails/check-worker-restore.py`, and `/Users/sashaabramovich/tlvphotos/inbox/2026-08-12-worker-discarded-uncommitted-work.md` carries the notice to that project's session. **Corrected 2026-08-12** by that project's reply, `inbox/2026-08-12-tlvphotos-reply-worker-restore-finding.md` (2026-08-12): the sentences above are what live-spec could establish from its own transcripts, and they read the outcome wrong — the harness classifier declined the call, so the command never ran and dropped nothing, and the file's only uncommitted change was one regenerated timestamp line that survived the attempt and is now committed. The finding stands as a real attempt at a forbidden command, since the rule forbids handing one to a shell at all. The reply also named a gap this row's own gate had: it reported a command handed to a shell while the shell's answer sat in the same transcript. `guardrails/check-worker-restore.py` now reads each call's `tool_result` and every finding says whether the command ran, was declined, or went unanswered, executed ones first; the counting start stays at 2026-08-12T06:06:00Z, re-tested at 2026-07-28 where two findings red. Intake 2026-08-12 — door: bug · kind: infra · footprint: `/Users/sashaabramovich/tlvphotos` (another project) · map: beside row 479's worker-restore gate | bug | *done* 2026-08-12 | One-line answer: this repository cannot repair another project's tree; the gate caught the command, the counting start now carries it as history so the gate stays live for what comes next, and the tlvphotos session was notified through its inbox to check what the discarded edits held; corrected 2026-08-12 from that project's reply — the command was declined and nothing was lost, and the gate now reports each finding's outcome |
| 605 | **A discard that ran in this repository's own tree is recorded in a source comment alone** (found 2026-08-12 by the push review of `e8900d9..493f094`, finding 14). Session `176e927f-4e67-4fa6-887e-86d1d6e5d1e4` ran `git checkout -- guardrails/rule-census.json` here at 2026-07-28T21:12:39Z, and that command executed. The gate's counting start moved past it, first to 2026-08-13 and then to 2026-08-12T06:06:00Z, and the incident's only homes are a comment at `guardrails/check-worker-restore.py:197` and one clause of a `DECISIONS.md:635` entry about a different incident. The gate's own law is that the counting start moves forward only with a recorded reason, and a comment is thinner than that law asks. Row 598 covers the tlvphotos attempt alone, which was declined and cost nothing; this one ran. Intake 2026-08-12 — door: bug · kind: small · footprint: `ROADMAP.md`, `guardrails/check-worker-restore.py` · map: beside row 598 | small | *queued* 2026-08-12 | Done when: the 2026-07-28 execution has its own row naming what the discarded bytes held and whether anything was lost, or row 598 states in its own words that it is knowingly carried past |
| 608 | **The prover tells an author to write a record the push gate then refuses** (found 2026-08-12 by the orchestrator seat's full read of the product-prover skill, `docs/prover/2026-08-12-product-prover-full-read.md` finding 1). The skill's meta rules say the findings are written to `docs/prover/YYYY-MM-DD.md`. `guardrails/check-prover-record.sh:131` tells the author the opposite in its own repair line: save it as `$PROVER_DIR/$TODAY-<slug>.md`. An author who follows the skill writes a bare-date file, and a second pass the same day overwrites the first pass's record with its folded-or-rejected column, which the very next paragraph of the skill says makes the fold verifiable after a memory wipe. The tree already votes: `docs/prover/` holds 360 dated records and 356 carry a slug (re-derived 2026-08-12 18:59; the first figures written here, 381 and 380, matched nothing in the tree). The same sentence stands in `editions/product-prover/SKILL.md` and owes the same sweep. Intake 2026-08-12 — door: bug · kind: skill · footprint: `skills/product-prover/SKILL.md`, `editions/product-prover/SKILL.md` · map: the meta-rules block | bug | *done* 2026-08-12 | One-line answer: the meta rule names `docs/prover/YYYY-MM-DD-<slug>.md` and two short sentences after it say the slug names the pass and lets a second pass the same day write beside the first; the public edition carries no such path, so it needed no sweep, which corrects the record's own assumption. Review `docs/skill-review/2026-08-12-product-prover.md`. Done when: both copies name `docs/prover/YYYY-MM-DD-<slug>.md`, the slug's job is stated in one clause, and the gate's repair line and the skill say the same string · groups as one queue bundle with rows 609, 610, 611, 612, 614, 615, 616, 618, 619, 620 — one thorough external review of the product-prover skill, and its follow-up fixes |
| 609 | **The declaration member of the composition-lens family has no lens in the prover** (found 2026-08-12 by the orchestrator seat's full read, finding 2). `PRODUCT_SPEC.md:6330` states that INV-226 reads as the declaration member of the composition-lens family. The prover's Phase 3e carries five members of that family and none of them asks the enumerate-or-ride question. The strings `enumerate-or-ride`, `member set` and `open-ended` return no hit in `skills/product-prover/SKILL.md`, and no test pins the law there. So a spec under review states a general duty over a closed, nameable member set, names no members, and every prover pass reads clean — which is the shape INV-226 was written to catch. Two answers: a sixth mandatory sweep, which moves the five-mandatory-sweeps count pinned in the spec, the README, the public edition and its examples; or one sentence saying the law is the author's duty and carries no prover lens. The seat prefers the sweep, because the spec's own word for the failure is defect and defect is the prover's verdict to issue. Intake 2026-08-12 — door: feature · kind: skill · footprint: `skills/product-prover/SKILL.md` Phase 3e, `PRODUCT_SPEC.md` INV-171 · map: beside the five mandatory sweeps | surface | *done* 2026-08-12 | One-line answer: the seat's preference for a sixth sweep did not survive the evidence. INV-226 is the spec-author's writing duty, and the prover states so by name in the cross-surface uniformity sweep. The prover issues verdicts only on what the document lets it see, and whether a member set is closed lives with the author. The part it can see — members that are sibling surfaces — the cross-surface sweep already covers. Criterion R264.5 now places its enforcement with the author and names the sweep that catches the surface-shaped case. The requirement's own user story, the spec-author node's ownership in `ARCHITECTURE.md`, and matrix row M-407 settled it. Review `docs/skill-review/2026-08-12-product-prover-2.md`. Done when: the family's carrier either runs the declaration lens with its own verdict line, or states by name that INV-226 is the author's duty, and INV-226's own clause agrees with whichever answer landed |
| 610 | **The term that decides what a surface add skips is defined nowhere** (found 2026-08-12 by the orchestrator seat's full read, finding 3). The prover's cross-link mode says it skips the whole-document property sweep and keeps the quantifier re-verify. That phrase appears twice in the whole repository, in the skill and its public edition, and the skill's own glossary defines lens, sweep, station and fold while leaving this one out. Three readings are open: Phase 3's property analysis, the five mandatory sweeps, or the declared-cross-cutting-laws sweep alone. They differ where it costs most, because that sweep demands a clause per surface and a test per surface, and a newly added surface is exactly the thing that has neither. Read wide, the mode skips the check the add most needs while the verdict table still renders a row for a surface no sweep visited. Intake 2026-08-12 — door: bug · kind: skill · footprint: `skills/product-prover/SKILL.md`, `editions/product-prover/SKILL.md` · map: the cross-link mode paragraph | bug | *done* 2026-08-12 | One-line answer: the mode skips Phase 3's property analysis, steps 3a through 3d. Every Phase 3e mandatory sweep runs on a surface add, with one exception. The lifecycle sweep sends its paired-transition symmetry sub-question alone, and its other angles stand down. The declared-laws sweep fires, because INV-101 has the author write the newcomer's line for this pass to audit. Both copies say it. Review `docs/skill-review/2026-08-12-product-prover-2.md`. Done when: the mode names what it skips and what it runs in words that need no glossary, both copies say it, and a reader can tell from the sentence alone whether the declared-laws sweep fires on a surface add |
| 611 | **The class lens is a base rule sitting in the tier that owes no verdict** (found 2026-08-12 by the orchestrator seat's full read, finding 4). The prover's imaginative-probe tier opens by saying these are habits of attention, no checklist ticks them off, and no verdict is owed. The class lens sits there. Base rule 14 is unconditional: before calling a fix done, name the pattern behind the instance and sweep the whole repository for it. So a full pass can file a point finding, skip the class sweep, and owe no line saying so — which is the exact distinction INV-171 split the two tiers to preserve. `tests/test_class_hunt.py` asserts only that the words appear in the skill, so the suite is green on a prover that never sweeps. The public README compounds it, listing the tier as ties, concurrency, bounds, dependency failures and dangling references, so a standalone reader never learns the class lens exists. Intake 2026-08-12 — door: bug · kind: skill · footprint: `skills/product-prover/SKILL.md`, `skills/product-prover/README.md`, `editions/product-prover/` · map: the tier's own opening | bug | *done* 2026-08-12 | One-line answer: the class lens owes one class line per pass, written in the record beneath the verdict table. It stands in a tier of its own after the probe list, in both copies, since base rule 14 binds every pass whatever the document holds. The README names the sweep, the edition carries the same edits, and `tests/test_class_hunt.py` reds a fixture pass with no class line. Review `docs/skill-review/2026-08-12-product-prover-2.md`. Done when: the class lens owes one line per pass reading swept / no class / not-applicable-with-reason, the tier's opening says so where it stands, the README names it among what the pass does, and a fixture pass with a point finding and no class line reds |
| 612 | **Six bullets stand under a stated five in the prover's lifecycle sweep** (found 2026-08-12 by the orchestrator seat's full read, finding 5). The sweep says the parent gathers five angles and six bullets follow. The count is defensible: the first bullet is the transition-payload parent itself and the five gathered angles are entry symmetry, entry state, paired-transition symmetry, persistence and versions, and scenario entry and exit, which queue row 344's own record confirms. It still stops a reader, and the skill's own meta rule says a release's lenses run against its own body and names the count-versus-contents lens by name. This is that lens on this document. Intake 2026-08-12 — door: small · kind: skill · footprint: `skills/product-prover/SKILL.md`, `editions/product-prover/SKILL.md` · map: the lifecycle sweep's lead-in | small | *done* 2026-08-12 | One-line answer: the lead-in reads "The parent lens stands first below, and the five angles it gathers follow it:", so six bullets under a stated five now read as a parent plus its five; the public edition carries no such lead-in. Review `docs/skill-review/2026-08-12-product-prover.md`. Done when: the lead-in reads so that the bullet count and the stated count agree on first reading, and both copies carry it |
| 617 | **The campaign's stated goals have no measured parameters in the plan's status block** (asked by Alexander 2026-08-12 23:58, restated 00:02 and 00:03). The campaign was opened on several goals, written down across earlier sittings. He says those goals live in the session transcripts, and he wants every one of them under watch in the head block of `.live-spec/culling-plan-v3-2026-08-10.md`. The block today watches one number, the rulebook's byte count at 72,466. The second stated goal, making the machinery cheaper, is measured by nothing: the full run's budget rose from 800 s to 1280 s to 1410 s inside one day, and the only place that noticed is row 553. A number already in the plan shows the same gap — the roughly 346,000 bytes the campaign has written about itself carries no command, so no pass can re-derive it, and `cat .live-spec/*.md \| wc -c` today returns 674,271 against a different reach. Intake 2026-08-12 — door: small · kind: docs · footprint: `.live-spec/culling-plan-v3-2026-08-10.md` head block, its delta page · map: beside the plan's numbers table | small | *in-work* 2026-08-13 — the goals table with commands and live values stands in the plan's head block since 08:39; the transcript sweep by cheap readers is the remaining half | Done when: cheap reader workers have swept the session transcripts for the goals as they were stated, this seat has read their summaries, and every goal carries one parameter in the plan's head block with the command that measures it, its direction, and its value at the writing pass. His word of 00:03 sets the readers: the sweep goes to cheap workers and this seat reads what they return |
| 623 | **A session recorded from an unplaceable directory read its neighbour's command as this project's own** (found 2026-08-19 during a review of the worker-restore gate's own-session narrowing, row 479, commit `b4d00f8e`). Four findings on this machine's transcripts carried `cwd` `/Users/sashaabramovich` — the owner's home directory, no repository's own — while the command itself `cd`ed into `/Users/sashaabramovich/exhibition-engine-integrate`, a real checkout of a different project (`u27-integrate`). `is_own_session` reads only the session's recorded `cwd`, and an unplaceable `cwd` falls to the fail-safe default that reads it as this project's own, so all four reddened here though the command ran in a neighbour's tree. Intake 2026-08-19 — door: bug · kind: infra · footprint: `guardrails/check-worker-restore.py` · map: beside row 479's worker-restore gate | bug | *done* 2026-08-19 | One-line answer: the gate now takes one further look only where `cwd` alone answers nothing — if the finding's own `effective_dir` (the directory `classify` already computes and prints as `ran in`) exists on disk and git places it in a different, nameable repository, the finding is carried as that neighbour's notice and reds nothing here; a `cwd` the key can already place, an `effective_dir` that is itself `UNKNOWN` or unplaceable, and a sibling worktree of this same repository all leave the fail-safe exactly where it stood, proven by three added fixtures in `tests/test_worker_restore.py` |
| 624 | **Twenty-eight worker-restore discards ran in this project's own worktrees since 2026-08-13, one of them landing on a file a concurrent lane was actively writing, and the same suite red that named them was waved past as "environmental" on three separate pushes instead of traced** (found 2026-08-19 while moving the worker-restore gate's counting start past this history, `DECISIONS.md` 2026-08-19, full breakdown there). `guardrails/check-worker-restore.py --all --counting-from 2026-08-12T06:06:00Z` reads twenty-eight own findings, every one stamped before the latest of them (2026-08-18T21:47:11Z), so no date separates a first-found twelve from the rest — twelve on 2026-08-18 across `live-spec-dt/wt`, `live-spec-split/wt` (eight, one a `git stash push -- tests/test_spec_parts.py` run by an agent whose own brief forbade changing files, one minute before a second agent in that same worktree committed 82 real lines to that identical file, commit `56f179a9`), `live-spec-morning/wt` and `live-spec-cull2/wt` (`DECLINED` by the harness); sixteen more never before recorded — six on 2026-08-13 (`live-spec` itself, `live-spec-night/wt-packet-b`, `live-spec-night-integration/wt-ck2` and `wt-integration` ×3), one on 2026-08-15 (`git clean -qfd` with `ran in: UNKNOWN` — the one case `classify` cannot place at all, the fail-safe default's own proof case, reddening exactly as its law says it must), five on 2026-08-17 (`live-spec-slimdown/wt` ×2, `live-spec-integrate-slimdown/wt`, `live-spec-night18/wt-d` and `wt-a2-comm`), and four more on 2026-08-18 in worktrees the first pass never named (`live-spec-progressfix/wt` ×2, `live-spec-readme2/wt` ×2). `tests/test_worker_restore.py::TestTheGateIsArmedWhereItSaysItIs::test_the_gate_runs_against_this_machines_own_transcripts` reddened on this history repeatedly (`docs/prover/2026-08-05-day-of-readability-repairs.md` finding 13, `docs/prover/2026-08-14-candidate-repair.md`, `docs/prover/2026-08-17-slimdown-pin-renumber.md`) and each time was named true and left standing rather than escalated — the same silence row 605 already warned this gate against, at sixteen times the size. Intake 2026-08-19 — door: bug · kind: infra · footprint: `guardrails/check-worker-restore.py`, the eight worktrees' transcripts, `tests/test_spec_parts.py`, `docs/prover/` · map: beside rows 479, 598, 605 | bug | *queued* 2026-08-19 | Done when: a hook refuses a worker's shell command matching the five forms row 479 names at the moment it is handed to the shell, rather than catching it afterward in the transcript — proven against a fixture command of each form. That hook is already built as a separate package but is not yet installed and armed on this machine, which is the owner's own act; this row records the incident and the counting-start move it earned (`guardrails/check-worker-restore.py`, `DECISIONS.md` 2026-08-19), neither of which is the fix, and stays open until the hook is standing here, not merely built |
| 625 | **The gate device is written by hand in five places, and only the letters were ever compared** (found 2026-08-19 while auditing the push gate's own device): `guardrails/pre-push`, `.github/workflows/gates.yml`, `guardrails/gate-red-proofs.json`, `guardrails/ci-mirror.json` and `guardrails/README.md`'s generated roster each carry part of every gate's law, script, SPEC anchor, red proof and CI-mirror status, by hand or by generation from pre-push alone, and `guardrails/check-ci-mirror.sh` (gate u) compares only the letter set between pre-push and gates.yml, never the law sentence beside a letter. Gate ad's law sentence had drifted as a direct result: pre-push and the generated README roster state both arms of the gate ("matches the tree, and the reproduction command beside it returns the published number"); `.github/workflows/gates.yml` stated only the first ("is built from the tree") for over a month with nothing red. The repair: `guardrails/gates-manifest.json`, the one join of every gate's script, SPEC anchor, law sentence, red proof and CI-mirror status, generated by `scripts/gen-gates-manifest.py` from pre-push, gate-red-proofs.json and ci-mirror.json alone — no field typed by hand — with gate af (`guardrails/check-gates-manifest.py`) reding a committed manifest that differs from a fresh build, or a gates.yml step whose law sentence differs from the gate's own. Six more gates' CI step text (a, d, i, t, x, aa) carried the same undetected drift and were brought into line with pre-push's own wording in the same pass. `guardrails/README.md`'s 15-of-28 prose notes are left untouched — their stated incompleteness is a separate, deliberate choice this row does not reopen. | surface | *done* 2026-08-19 | Done when: `guardrails/gates-manifest.json` and `scripts/gen-gates-manifest.py` stand, gate af is wired on the push hook and mirrored in CI with a red-first proof (`tests/test_gates_manifest.py`), the seven drifted gates.yml law sentences (a, d, i, t, x, aa, ad) are brought into byte agreement with pre-push and held there by the new gate, and a dry `bash guardrails/pre-push < /dev/null` passes clean [INV-210, INV-212] |
