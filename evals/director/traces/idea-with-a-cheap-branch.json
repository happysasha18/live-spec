{
  "id": "idea-with-a-cheap-branch",
  "acts": [
    "instruction",
    "idea"
  ],
  "creates_work": true,
  "shelves_idea": false,
  "attaches_to_existing_work": false,
  "work_items": 1,
  "dimensions": [
    "product value and behaviour",
    "the user's path and the design"
  ],
  "specialists": [
    "Developer"
  ],
  "reasoning": "This is a conditional request stating both branches in one breath, exactly the pattern the skill names under 'A conditional request states both branches, and both are real': 'if that's also possible, let's do it' is the instruction branch, 'if not now, note it for later' is the idea branch, and per that rule both stay named as acts but the outcome is exclusive — the request is taken as work if the condition holds, or shelved as an idea if it does not, never both. The scenario's situation line establishes that the condition holds: the workers display is already built and live in another window, so satisfying the request here means reusing that same display rather than building a new one — it is possible, so the Director takes it as accepted work now rather than filing the idea half, per 'take the work if the condition holds.' This is new work (an instruction naming a goal nothing already covers), not a correction to work in flight, so it opens a fresh checkpoint rather than attaching to an existing one. It touches product behaviour (a new thing becomes visible/true) and the user's path and design (an interface/interaction addition — seeing all workers here too), and needs a Developer to wire the existing display into this window; nothing here changes a spec's stated behaviour beyond what it already describes, so no spec author, and the reuse of an already-designed display means no new design-reviewer judgment is required.",
  "scenario": "idea-with-a-cheap-branch",
  "skill_version": "6.1.0"
}
