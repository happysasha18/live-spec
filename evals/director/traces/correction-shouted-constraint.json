{
  "id": "correction-shouted-constraint",
  "acts": [
    "correction"
  ],
  "creates_work": false,
  "shelves_idea": false,
  "attaches_to_existing_work": true,
  "work_items": 0,
  "dimensions": [
    "product value and behaviour",
    "architecture, data, integrations, operations"
  ],
  "specialists": [
    "Developer",
    "Architect"
  ],
  "reasoning": "This lands mid-implementation of the transition engine, and it forbids the approach that implementation is currently taking rather than reporting a fact or naming a future possibility. Per the skill: 'Telling work in flight to stop one approach and take another is a correction to its method, not a halt either — the work keeps running toward the same goal, only how it gets there changes.' There is no 'from now on' or 'always' language, so this is not a standing rule/decision either — it is scoped to the transition engine work underway. As a correction it attaches to the existing checkpoint for the transition engine rather than opening new work: 'A correction attaches to work, not to a queue. When a message changes something already in flight, find that work and change it... never answer a correction by creating a second row.' It touches product behaviour (what the transitions must be) and architecture (how the engine computes them, since ruling out static transitions forces a different mechanism), so the developer continues building under the new constraint and the architect is pulled in given the boundary/mechanism shift the constraint implies.",
  "scenario": "correction-shouted-constraint",
  "skill_version": "6.1.0"
}
