{
  "id": "correction-shouted-constraint",
  "acts": [
    "correction"
  ],
  "creates_work": false,
  "shelves_idea": false,
  "attaches_to_existing_work": true,
  "work_items": 0,
  "dimensions": [
    "product value and behaviour",
    "the user's path and the design"
  ],
  "specialists": [
    "Developer",
    "Design reviewer"
  ],
  "reasoning": "The situation (mid-implementation of the transition engine) plus the message (a constraint about how transitions must behave) match the Correction row: 'changes the goal or the constraints of work in flight.' Per 'A correction attaches to work, not to a queue', this finds the transition-engine work already in flight and changes its constraint, so it attaches to that existing checkpoint. Tone/shouting does not change the act — what makes this a correction is that it changes an in-flight goal's constraint, not that it was shouted; it is not a halt, since the work keeps running toward the same goal, only a constraint on its output changes. Because it changes what the transition engine must produce, it touches product value and behaviour and the user's path and design, calling for the Developer to replan and continue, and the Design reviewer since the correction is about the coherence of the experience.",
  "scenario": "correction-shouted-constraint",
  "skill_version": "6.1.0"
}