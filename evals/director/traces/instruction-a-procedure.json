{
  "id": "instruction-a-procedure",
  "acts": [
    "instruction"
  ],
  "creates_work": true,
  "shelves_idea": false,
  "attaches_to_existing_work": false,
  "work_items": 1,
  "dimensions": [
    "architecture, data, integrations, operations"
  ],
  "specialists": [
    "Developer"
  ],
  "reasoning": "The whole message is one imperative procedure -- stop the perf server, relaunch it bound to 0.0.0.0:8790 from the named project, confirm the bind, then report the Mac's real Wi-Fi IP -- and the skill's own halt rule says exactly this shape is not a halt: 'stop the server said in the middle of a procedure names a step of that procedure -- it is an instruction, part of the work, not a halt,' so 'Останови текущий сервер' is a step inside the instruction, not a separate halt act. Under 'Taking acts apart is not the same as splitting a goal. One instruction stays one piece of work even when it will touch six files... Steps toward a single result are not separate work,' stopping, relaunching, verifying the bind, and naming the IP are all steps toward one observable outcome (the perf server correctly serving on 0.0.0.0:8790 and its real address known), so this is a single instruction and a single new work item, not attached to any work already described as in flight. It touches the 'architecture, data, integrations, operations' dimension only, since the fix is entirely about a server's network binding and no product behaviour, spec, or design surface changes. Per the specialists table, 'Developer | something must be built | the agent itself' covers running and verifying this fix directly; nothing here calls for Architect-level design change, a spec author, or an independent verifier beyond the Director's own re-check against the stated observable outcome (confirmed 0.0.0.0 bind, real IP named).",
  "scenario": "instruction-a-procedure",
  "skill_version": "6.1.0"
}
