{
  "id": "instruction-a-procedure",
  "acts": [
    "instruction"
  ],
  "creates_work": true,
  "shelves_idea": false,
  "attaches_to_existing_work": false,
  "work_items": 1,
  "dimensions": [
    "architecture, data, integrations, operations"
  ],
  "specialists": [
    "Developer"
  ],
  "reasoning": "One turn, one act. \"Stop the server\" here is not a halt: a halt is about the session's own work in progress, and this names a step inside a procedure the person is asking for — stop, restart bound to 0.0.0.0 from the named directory, confirm the binding, then report the machine's real Wi-Fi address. The later clauses are steps toward a single result, not separate work: none of them could ship or be cancelled on its own, and \"а не 127.0.0.1\" and \"не предполагаемый\" are constraints on the verify step rather than acts of their own. Nothing was in flight for this to correct, so it opens new work rather than attaching. The work touches architecture, data, integrations, operations — a listening socket's bind interface and the host's network address — and nothing else: no behaviour the spec describes changes, no interface, no document becomes wrong. It needs the developer only; verifying the bind and reading the actual address are plain re-checks against the observable outcome, so no independent verifier is called.",
  "scenario": "instruction-a-procedure",
  "skill_version": "6.1.0"
}