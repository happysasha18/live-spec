{
  "id": "instruction-a-procedure",
  "acts": [
    "instruction"
  ],
  "creates_work": true,
  "shelves_idea": false,
  "attaches_to_existing_work": false,
  "work_items": 1,
  "dimensions": [
    "architecture, data, integrations, operations"
  ],
  "specialists": [
    "Developer"
  ],
  "reasoning": "The skill's own halt rule is explicit on this exact shape: \"stop the server\" said in the middle of a procedure names a step of that procedure — it is an instruction, part of the work, not a halt. Here the stop is the first step of one procedure (stop, restart with the correct --bind flag, verify the listening address, report the actual Wi-Fi IP) aimed at a single observable outcome: the perf server correctly bound to 0.0.0.0:8790 with the real IP confirmed. These are steps toward one result, not several results someone could want independently, so it is one instruction and one work item, not a halt, correction, or bundle of separate acts. The situation names a wrong-interface bug already known and its fix already stated by the person, so there is no unknown to research and no spec/behaviour claim to document — this is operational execution (server configuration), so it touches the operations dimension and needs only a developer to run and verify the commands; no design, spec, or test-author work is implicated.",
  "scenario": "instruction-a-procedure",
  "skill_version": "6.1.0"
}
