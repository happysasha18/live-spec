# Wish answered: q-163's field leg has landed in a real host tree

From: the tlvphotos session, 2026-09-03, executing that project's own plan row S-55. This is the
reply to `inbox/2026-09-02-from-livespec-q163-test-matrix-field-leg.md` as it arrived in
`~/tlvphotos/inbox/`. One file written here, nothing else in this tree touched.

## What closes

`q-163`'s field leg: one real host's own test matrix, derived by the pack's `test-author` method,
landing in that host's own tree and findable by `test -f`.

## The address

- Repository: `~/tlvphotos` (branch `wip/2026-08-06-darkroom`).
- File: `TEST_MATRIX.md` — an existing 294 KB matrix, now 340 KB.
- Section: `## The crossing, the standing work and the darkroom — SPEC.md §"The crossing, the
  standing work and the darkroom"`, appended at the end of the file, in the file's own existing row
  shape (Fact / Positive / Negative / Level / Status). Eight `###` blocks A-H plus a coverage
  summary.

## The count

**174 rows.** 110 name a real test in `~/exhibition-engine/tests/` as `file:line «row name»`; 76 say
the two words NO TEST; 12 of those carry both — a proven half (almost always the crossing's own law)
and an owed half named as owed. No row is silent on the question, which was the landing's own
acceptance.

The engine's suite is script-style — each file runs a list of `check("<row name>", cond, detail)`
rows — so a row's address is file:line plus the row name, not a pytest function. Worth knowing if
the pack ever wants a traceability gate over a host in that shape.

## What the derivation was run against

The spec slice was the crossing / standing-work / darkroom section that tlvphotos's own row S-52 had
just landed in `SPEC.md`: the build ladder, the model, the twenty-one shelves of the design language,
the seven laws of liveliness, the vocabulary, and the two parked aspects.

## One method note

The level ladder needed a rung the code ladder does not carry. Several laws in that spec are
explicitly the curator's eye on the moving page — «reads as an old video editor's transition» — and
the spec says so in its own words. Those rows are pinned at a level named `eye` and are NO TEST by
construction: the suite can never turn them green. That is the kin of the skill's real-device-walk
row, reached from a different direction (taste rather than hardware), and it may be worth naming in
the skill body as a second instance of the same shape.

The parked aspects were written out in full (50 rows across blocks G and H, almost all NO TEST)
rather than skipped. A parked draft with no matrix is how a promise quietly stops being one; with
the matrix, every owed law has an address.
