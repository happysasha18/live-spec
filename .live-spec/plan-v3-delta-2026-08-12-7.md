# plan v3 — delta 2026-08-12, seventh pass (the range paragraph drops a number that rots)

Root: his word of 2026-08-12 18:45 — ship what is waiting. The sixth delta wrote "девять коммитов"
into the head block, and the gate chain then demanded a tenth commit, so the number was stale within
the hour. This is the same defect the pass had just repaired in three other places.

- **The head block's range paragraph is rewritten.** The commit count leaves it; the range
  `4a0b982..HEAD` stays, since a range names itself and `git log` counts it. The paragraph gains the
  seventh blocking finding, which the push gate chain raised at 19:31: two user-facing files changed
  with no test travelling beside them. It ends with what is left of the walk, the push and the
  mirrors.

Not touched: every stage, decision, step, number and table below the head block.
