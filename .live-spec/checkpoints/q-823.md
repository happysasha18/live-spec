# One instruction travels the whole path — ticket, worker, independent acceptance, delivery, done, and a new session continuing it
Status: closed
Owner: pipeline

## DONE

T3 correct, T4 block, T5 unblock, T6 park, T7 close, T8 reopen, T9 abandon and the T2 hold half are subcommands of scripts/task-admission.py, each with one test proving the legal move and one proving the refusal (tests/test_task_admission.py, 21 new tests, all red before the code existed).
checkpoint.py gained reopen_checkpoint, T8's half; abandon reuses the module's own placeholder rule so the halt's reason survives the close.
Admission refuses a ticket carrying no context pointers and renders them as the row's own **Context pointers.** paragraph, the shape PLAN rows already use.
task-admission.py brief hands a worker the ticket entry plus the checkpoint's NEXT byte for byte; the test diffs it against both sources read independently.
All 36 Director fixtures carry their operation beside their acts and evals/director/check.py grades it, skipping a trace recorded before the field existed.
Three matrix rows added (M-637 worker brief, M-638 blocker names its cause, M-639 the remaining transitions), M-630's owning cell now names its three tests, and tests/test_traceability.py::TestStateMachineFactsAreTraced walks all eight section-7B facts from the contract's own words to a built row to a test really defined under tests/.
scripts/plan_checks.py carries q-823's acceptance key, 0.34s.
Reopened 2026-09-06 night against a false close: the eval exited 1 and the two independent recordings on file shared three operation reds. The halt fixture's expect.operation is now T9 — the producers were right, needless machinery is dropped rather than parked — and skills/director/SKILL.md gained one classification sentence for a far-backlog idea (no ticket, not even a queued one) and one for a failure already diagnosed with its repair in flight (a correction on the running work, not a new blocker). 21,977 to 22,548 bytes, no execution rule added.
All 36 scenarios re-recorded twice by fresh Opus producers against the edited skill: 34 of 36 in evals/director/traces, 33 of 36 in evals/director/recordings/2026-09-06-pair-6, shared reds: 0. Both recordings are kept in the tree; check.py gained --pair, and q-822's and q-823's keys read that intersection instead of a score.
RECEIPT: {"by": "clean-context verifier", "at": "2026-09-06T16:56:11", "tree": "84cda0b3f89f8d510f4820b7c542f5b23a0ec7ef", "head": "e65ae0ee96c9fd5b3717adad3ed696db69145988", "dod_hash": "1e44dbb01a40023cb1314e449330e6a11dad3fd4640915e7fc4106522b8a6373", "surfaces": [], "checks": [["python3 -m pytest -q tests/test_director_route_end_to_end.py", 0], ["python3 -m pytest -q tests/test_task_admission.py tests/test_checkpoint_mechanism.py tests/test_traceability.py tests/test_statement_validation.py", 0], ["python3 -m pytest -q \"tests/test_director_scenarios.py::test_the_grader_fails_a_wrong_operation\" \"tests/test_director_scenarios.py::test_every_scenario_names_its_operation\"", 0], ["PYTHONPATH=tests python3 -m unittest -q test_traceability.TestStateMachineFactsAreTraced", 0], ["test \"$(grep -cE \"^def (correct|block|unblock|park|close|reopen|abandon|worker_brief)\" scripts/task-admission.py)\" = 8 && grep -q \"def reopen_checkpoint\" scripts/checkpoint.py", 0], ["test \"$(grep -c \"\\\"operation\\\": \\[\" evals/director/scenarios.json)\" = 36", 0], ["test \"$(ls evals/director/traces/*.json | wc -l | tr -d \" \")\" = 36 && test \"$(ls evals/director/recordings/2026-09-06-pair-6/*.json | wc -l | tr -d \" \")\" = 36", 0], ["S=$(shasum -a 256 skills/director/SKILL.md | cut -d\" \" -f1); test \"$(grep -l \"\\\"skill_sha256\\\": \\\"$S\\\"\" evals/director/traces/*.json evals/director/recordings/2026-09-06-pair-6/*.json | wc -l | tr -d \" \")\" = 72", 0], ["python3 evals/director/check.py --pair evals/director/traces evals/director/recordings/2026-09-06-pair-6 | grep -q '^shared reds: 0'", 0]], "verdict": "passed"}
Reopened: closed without an acceptance receipt was not true — grep -rl '^RECEIPT: ' .live-spec/checkpoints/ named only q-816.md; .live-spec/checkpoints/q-823.md carries no RECEIPT line, so the close was a textual claim rather than a state transition against evidence — settled by the receipt at 2026-09-06T16:56:11
estimate 6–10 hours → actual not recorded
divergence: no lane decision was recorded at take-up

## IN PROGRESS

(nothing)

## NEXT



## DECISION SHEET

Goal: One instruction travels the whole path — ticket, worker, independent acceptance, delivery, done, and a new session continuing it
Observable outcome: an instruction given in one session becomes a ticket, is worked, is accepted by someone other than its producer, delivers, closes, and is picked up by a fresh session with no spoken recap — every state change written by code that refuses the illegal move, rather than by a hand editing two files
Definition of done: the six transitions no code runs today are run by code and refused on bad input: T4 marks blocked only with one of the three named reason kinds and refuses a reason that merely restates difficulty; T5 clears a block only against a named fact and lands in hand or queued by whether a holder is named; T6 parks by clearing the holder and leaving the checkpoint open with NEXT; T7 writes the PLAN.md mark after closing the checkpoint, in that order, and a re-run over an already-closed checkpoint only rewrites the mark; T8 reopens the same id against a named false condition and never a copy; T9 abandons by clearing IN PROGRESS and NEXT with the halt's reason as their last line and closing the checkpoint in the same step. T3's queued half rewrites a queued ticket's goal and done in place, touching no checkpoint. A ticket carries its context pointers and admission refuses one that carries none. A worker's brief equals the ticket entry plus its checkpoint's NEXT with no paraphrase. Every Director verdict fixture carries its operation beside its acts and the grader reads that field. Each of the eight state-machine facts in the contract's section 7B has one TEST_MATRIX row reading built against a real test, including the two that have neither today — a worker gets the exact text, and a real blocker names its cause.
Verification: python3 -m pytest -q tests/test_task_admission.py tests/test_checkpoint_mechanism.py tests/test_director_route_end_to_end.py tests/test_director_scenarios.py tests/test_traceability.py, green, with the eight section-7B facts each traced from a TEST_MATRIX row marked built to a test named on that row; plus python3 evals/director/check.py --all reading the operation field on every fixture
Project: live-spec
Scope: Turnkey productization
Source: the owner's own package 2, second in his numbered list at `.live-spec/next-phase-prompt-turnkey-productization.md:80` — one vertical path on live-spec: instruction, ticket, worker, independent acceptance, delivery, done, and a new session continuing it — detailed by the product contract he had proven before any code started, `.live-spec/turnkey-contract-composed.md` sections 2, 4 and 7. Opened 2026-09-06 after a piece-by-piece read of that contract against the tree found the path built in three places and unbuilt in nine; q-816 had been waiting since 2026-09-03 on a package-2 row that did not exist, which is what the read was looking for.
