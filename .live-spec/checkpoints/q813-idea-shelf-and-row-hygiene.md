# A queued row always carries a real reason to stay open; an idea gets its own shelf
Status: open
Owner: director

## DONE

(nothing yet)

## IN PROGRESS

(nothing yet)

## NEXT

(nothing yet)

## DECISION SHEET

Goal: build the already-specced-but-unbuilt idea shelf (spec/message-first-read.md Requirement 315, INV-320, E-37 -- marked [target], zero implementation anywhere per the requirement's own admission), formalize a real distinction for PLAN.md's own open ## Tasks rows (a row stays open only for a checkable reason: actively worked, or blocked on a NAMED external trigger a person could verify fired -- never 'if he asks again', which is not checkable and is exactly what makes a row idea-shaped instead of task-shaped), and sweep the current board applying that distinction, moving what's genuinely idea-shaped off Tasks. Root cause tonight: owner's own words, 2026-09-03 ~10:08 -- he does not want tasks sitting open with no real reason; either a real reason or it was an idea and belongs on a shelf; asked me to think through how to formalize this in the spec rather than pattern-match row by row; reminded me who this pack is built for (a solo owner who dumps thoughts and wants the system to sort real work from passing ideas on its own, PLAN.md's own opening goal) should set my state of mind doing this. Outcome: IDEA_SHELF.md exists at repo root (root-level standing doc, same tier as PLAN.md/DECISIONS.md/JOURNAL.md/NEXT_STEPS.md), append-only, each entry dated with the person's own wording verbatim, no identifier/priority/estimate per Requirement 315's own text; director/SKILL.md's idea-act handling writes to it and says so in one line (this closes E-37/INV-320's [target] tag for real, not just in prose); PLAN.md gains one short paragraph in its own '## Words used here' or law section naming the real bar for staying in ## Tasks (an active reason: in-hand, or a named checkable trigger -- never a hope that circumstances change). Dimensions: architecture (a genuinely new mechanism, a promised-but-never-built spec target), documentation (PLAN.md's own house rules), quality (a sweep of real content, judgment-heavy). Known: q-811 is the clear, already-identified case -- its own text admits 'nobody has asked for that larger feature again since 08-06', its 'revisit trigger' is not checkable ('a real ask for it' requires HIM to re-raise it, which is circular, not an external fact). q-811 also carries an approved, fully-designed spec chapter (spec/work-board.md Requirement 309, 18+ criteria, an approved sketch frozen at docs/norms/work-board.html) -- richer than a bare passing thought, so my own recommendation (not yet his word, present it plainly and let his own read of this session's report catch it if wrong per his own 'потом поговорим'): retire Requirement 309 to attic as a declined-for-now spec chapter with a manifest line explaining exactly why and how to revive it (a fresh design conversation, not a git-resurrection, since a prototype/declined design holds no rights per this pack's own rule 16 spirit), drop its INV-308/INV-67 [target] tags since nothing is promised once nobody's building it, and put ONE line on the new idea shelf in his own original words ('one page I can just look at instead of asking the agent' / the July-August board asks) so it's findable if he raises it again. Unknown: whether any OTHER open row is similarly idea-shaped under the new bar -- must be swept and reported, not assumed absent; for anything ambiguous, report to me rather than deciding unilaterally, per his own explicit instruction tonight ('если непонятно уточнять'). Risk: retiring an approved spec chapter is reversible (git history + attic manifest + one shelf line preserve everything) but is a real content judgment; flagged plainly in the report rather than buried in a diff. Specialist: opus-tier worker (judgment: which rows are idea-shaped, how to word the new house rule, how to retire a spec chapter properly through this pack's own doc-rotation/attic convention). Evidence: a new test proving IDEA_SHELF.md exists and a scripted or director-described idea gets appended correctly with no identifier; tests/test_traceability.py's TARGET_ROW_OWNERS updated (drops INV-308/INV-67 if retired, confirms E-37/INV-320 either gets a real implementation-backed close or an honest note); full suite green. Next: dispatch worker with this brief plus spec/message-first-read.md Requirement 315, spec/work-board.md Requirement 309, PLAN.md's own q-811/q-385 (as the contrast case -- q-385 HAS a real checkable trigger and should stay exactly as a Tasks row), and attic/MANIFEST.md's existing convention as primary sources.
