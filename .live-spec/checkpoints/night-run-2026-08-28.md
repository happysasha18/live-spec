# The 28.08 run — resume point

Owner's word, 28.08 00:53: run the plan to the end without asking him; archive anything
blocked or unclear; report against the canonical plan; add no machinery without a reason;
push and deploy are pre-authorized on green; close with an adversarial review and repair
whatever it finds. His word at 11:48 ordered the board cut to the genuine remainder. His
word 31.08 18:32: a hostile Opus read of the remaining board, since surviving one cull is
not the same as being right — its findings are below and drive what NEXT actually is.

Read `bash scripts/state-probe.sh` first; this file carries only what the probe cannot.

## LIVE STATE, 31.08 20:08 — read this before anything else if resuming

**That merge finished and went out. `6cbec19d..16b1a300` is pushed; origin/main carries it.**
The full suite ran alone on the pushed tree — 2,637 passed, 5 skipped, none failed — and the
gate chain printed "All gates green — push allowed." A fresh-context reader refused the merged
range first and returned nine blocking findings; all nine are repaired inside the pushed range,
and the record is `docs/prover/2026-08-31-three-lanes-land-and-a-read-refuses-them.md`.

**The board as it stands: 24 rows open.** Twenty-one are the seat's own work — `q-531`, `q-801`,
`q-55`, `q-581`, `q-576`, `plan-10` (the "## Steps" checks version), `q-437`, `plan-14`, `plan-15`,
`q-453`, `q-163`, `q-489`, `q-591`, `q-536`, `q-54`, `q-48`, `q-235`, `q-398`, `q-751`, `q-386`,
`plan-9`. Three wait on Alexander's own read and no command stands in for it: `q-800`, `q-166`,
`q-501`. Three of the twenty-one already have a finished lane sitting in a worktree, named below.

**Worktree branches, their status, and what to do with each:**

Merged and pushed. The three this pass owned are already deleted, branch and worktree, with
`git branch -d` and `git worktree remove`: `worktree-agent-a63c0baffdcc79480` (director eval),
`worktree-agent-a99bbbe59f59a4143` (q-527), `worktree-agent-ab76a36eb666259f3` (q-497).
Still standing and merged, so safe to delete: `worktree-agent-ab6c64014d1fca16e`
(prover pin → 1.6.0), plus three old ones already merged before tonight's later work:
`worktree-agent-a4f4f7d99d1c91bfa` (locked by a live process since 26.08 — do not force),
`worktree-agent-a529cd9632dea5580` (locked), `worktree-agent-ae4718438fdb99475` (locked).

**Done, not yet merged — merge these three next, in one pass, same as every other
multi-lane merge tonight (read both sides on conflict, run the full suite alone, gate
chain, push):**
- `worktree-agent-a6437a7ec0f3d04d8` — q-531, the lossless-split proof (`scripts/nothing-lost.py`).
- `worktree-agent-a68d22353bd5a0dab` — q-801, release 6.1.0, one-list teaching.
- `worktree-agent-a540d919d0e8446e4` — q-55, narrowed to the one real case, locked.

**Obsolete — delete without merging:** `worktree-agent-a4a4ce65458443d7a`. It is the first,
partial director redraw (26 of 35) that a later branch (`a63c0baffdcc79480`, already
merged) fully superseded with an honest 35-scenario redraw plus a grading fix (34 of 35).
Merging this one too would fight the already-merged one. `git branch -D` and
`git worktree remove --force` once confirmed its content is fully subsumed.

## THE HOSTILE REVIEW OF THE REMAINING BOARD, 31.08 18:34 — drives all of NEXT

Asked by him directly: "из этих 20 в очереди точно все нужны?" A clean-context Opus
reviewed every remaining open row against the standing rule (no machinery without a real
incident) and sorted all twenty:

- **Solid (build as written):** q-531 ✅ done, q-801 ✅ done, `plan-10` (real: 5 done marks
  failed re-check 28.08), `q-591` (a matrix reference gone stale twice), `plan-9` (already
  deferred correctly, dry run proven, waiting on his word — not stalled).
- **Moot, close without work:** `q-386` (parallel branches proven live) — this is exactly
  what happened all night across a dozen worktrees merging cleanly; verify the equivalence
  test, mark it done, cite tonight's own history as the proof.
- **Archive, no work:** `plan-15` (promoter update — no owner word, no incident, idle since
  27.07), `q-453` (review-page edits — unsourced, nothing in the tree), `q-48` (post-launch
  tracking — the row names its own trigger, "the first host with a live audience", which
  does not exist yet), `q-751` (test-guards-something-real audit — plan-6 already ran this
  exact measurement 26.08 on 120 tests, found 16% dead, decided against removal; this row
  proposes re-measuring a settled question against his own rule against re-measuring for
  confidence).
- **Real but overbuilt — narrow before building, do not build as originally worded:** q-55
  ✅ done (narrowed to one commit at join, cut a throwaway-tree-walking framework), `q-581`
  (narrow to the rule stated once plus reaping the one stale server, not a command
  registry), `q-576` (the sweep already shipped — this is a report page to publish, half an
  hour, not a task), `q-437` (run the sweep by hand once over tlvphotos's declared axes;
  build a pass only if it finds a third case), `plan-14` (narrow to the one measured leg —
  printing unhandled inbox files — the rest is deferred on his own word), `q-163` (park with
  plan-9, only the field leg remains and it's on a deferred host), `q-489` (strike the
  "walk guardrails/ for an unfixtured check" leg — that is exactly the machinery `e61b29b7`
  removed on 21.08; keep only the per-check red-proof), `q-536` (re-scope against the
  post-`plan-16` tree first — one-home-per-rule may have already closed most of it),
  `q-54` (two of three legs done; the third waits on a real project joining — park, don't
  hold a board slot for it), `q-235` (the existing test asserts strings inside a skill body,
  not behaviour — narrow to actually running the wind-down once for real), `q-398` (strike
  the "declared size cap" — a threshold with no outside source, forbidden by his own
  standing rule; keep the preamble and the fixture).

**Method going forward: for every one of the eleven "narrow" rows, write the narrowed
acceptance into the row before dispatching a worker — do not hand the original wording to
a fresh agent and hope it self-narrows.** That is what happened with q-55 by accident
(caught only because the review ran before it finished) and should not be repeated.

**Correction, found 31.08 ~21:50, after the review closed: its q-55 finding was itself
wrong, on a methodology error.** It grepped `PRODUCT_SPEC.md` as a plain 315-line file and
concluded the row's cited anchors (E-6, E-7, E-10, A-6, INV-17) "appear nowhere." They do
appear — `conftest.read("PRODUCT_SPEC.md")` returns the 8,357-line body assembled from
`spec/*.md` parts, and a plain `cat`/`grep` on the file never sees that. `A-6` genuinely
matches what q-55 built. `E-6`/`E-7`/`E-10` look like they belong to a different feature
(design-sync's rendered-surface baseline and the surface registry), likely mis-mapped onto
`q-55` before tonight rather than broken by tonight's narrowing. Full diagnosis is in
`NEXT_STEPS.md`'s LIVE STATE, since it's exactly what blocks the current push
(`tests/test_traceability.py::TestTargetOwnership::test_targets_owned_by_open_rows`).
**This does not mean distrust the review's other ten findings** — each of those cited a
real commit, a real test, or a real dated word, checked independently; this was the one
finding built on a file-reading shortcut instead. Worth remembering: this pack's own spec
is generated/assembled, and reading it as a flat file is a trap that will recur.

## DONE

- **The board was cut from 162 rows to 63** (`829c6f39`). 94 folded rows rotated to
  `docs/queue-archive/rotated-PLAN-2026-08-28-folded-rows.md`, 5 rows with no owner and no
  acceptance archived, 3 tasks marked done against shipped code.
- **The 👁️ marks on `plan-9` and `plan-15` came off** (`137f691`). They were placed by the
  cull pass on a brief, never by his word, so `test_eyes_marker_traces_to_owner.py` was
  right and the marker was the false half.
- **Everything unpushed went out**: `e7fafcf..c714833d`, 20 commits, CI 33160556497
  success — the first green on main since 27.08. The first attempt went red on two defects
  invisible to local gates; both were repaired rather than reported.
- **Three repairs landed** (`c714833d..d69372c6`, CI 33169384826 success):
  `q-490` the legibility lint scored chainless selectors against the page background and
  now scores only what the stylesheet determines, reporting the rest as undetermined;
  `q-586` the worker-restore guard matched five command verbs while sixteen assembled
  write-back routes passed it, including the one its own refusal recommended, and now
  judges the write target across the pipeline; `q-567` `guardrails/install.sh` shipped
  hooks into host projects without the checks they call, so gates skipped in silence.
- **`plan-10` done** (`d69372c6..fdcdd9de`, CI 33172082443 success). Seven false done-marks
  resolved: two tombstoned (work shipped, a later decision deleted its subject — `e61b29b7`,
  `7b2980df`), two returned to open, one verified genuinely done, one given an honest line,
  one left ✅ against the brief with the argument recorded. Thirteen verification keys
  written; nine reading-verified rows carry a `file:line` instead. The class rule went into
  PLAN.md's rules: a closing task writes its check at the moment it closes.
- **The prover-description test was uncoupled** (`85b659d1`). It asserted a literal phrase
  from the external skill's frontmatter, which 1.4.2 dropped precisely to make discovery
  reliable, so it reddened on the improvement it protected. The CI pin moved `4503881` →
  `efe05fa` (v1.4.2); the old pin was an untagged merge commit carrying 1.4.0, so CI had
  been proving a state no release names.
- **`plan-11` landed** — one list, ROADMAP retired, `1b061a8b` pushed, tree clean.
- **Branch hygiene, on his word 15:38.** 81 branches and 6 agent worktrees closed across
  three passes: 60 fully merged, 8 test droppings (a scenario generator had been committing
  synthetic history into the live tree), 12 superseded with the main-side evidence cited per
  branch.
- **product-prover, his own instruction, separate repo** (`happysasha18/product-prover`):
  1.4.1 positions Code mode in the README and ships its code-lenses resource through the
  validator; 1.4.2 shortens the frontmatter description from ~1,500 characters to 498 and
  splits the README hero. Both pushed. Tags `v1.4.0`, `v1.4.1`, `v1.4.2` created and
  released; no release pages existed before today.
- **Every open task now says what finished looks like** (`1b061a8b..d097e41f`, CI
  33187731075 success). 21 rows carry a command that decides done; 5 name a person's read
  and what would convince them; `q-596` was archived as naming no outcome this project can
  reach. **PLAN.md's own bar at lines 45-49 was amended**: it had demanded a command of every
  queued task while the same file provided twice for the opposite. The amended sentence is
  in §Blockers, since it changes his 27.08 wording.
- **A product review package was written for him**, read-only, pinned at `1b061a8b`:
  `~/livespec-product-review-2026-08-28.md`, rendered and opened. Its sharpest findings:
  the path from a message to the Director exists only as an instruction to a model; the
  board is a static render; no gate reads a task, so the independent done-check is absent.
- **The adversarial review ran** over `e7fafcf..7159fed6` and produced 12 confirmed findings
  plus a suspected list. Four repair lanes ran against it in isolated worktrees.

- **The four repair lanes are merged, re-reviewed, repaired again, and pushed.**
  `7159fed6..48816231`, 30 commits, CI 33377145003. The merge resolved five conflicts by
  reading both sides; the pass that made it found nine more defects in its own merged result
  and repaired them (`03acd21`, `c7c4ab6`), then died mid-sentence on an account limit.

- **The 31.08 pass finished it and got it out.** The half-finished record edit was reverted
  rather than completed — it had widened the 28.08 record's range header to claim commits
  written after that record closed — and a record dated today covers the whole push instead
  (`docs/prover/2026-08-31-the-merged-run-goes-out.md`).

- **Two fresh-context readers went over the pushed range and found sixteen more things; ten
  are repaired** (`2540ef8f`), each red-proven against the code that shipped it.
  - The worker-restore guard let fifteen destructive routes past. git's pre-command options
    were stepped over by a list of five names, so `git --no-pager checkout -- foo` passed —
    and `guardrails/check-worker-restore.py` carried the same list, so the retrospective arm
    saw nothing either and the act had no gate anywhere. A redirection standing before the
    command word hid the read half. `exec > foo` and `{ … ; } > foo` split the pair across
    two pipelines of one command. `if`, `while` and `until` were missing where `do` and
    `then` already stood. An append onto a path the same command empties is a restore.
    Three honest commands that were being refused — `git restore -S`, `-h`, `--help` — pass.
  - The legibility lint had three regressions this range introduced. A painting rule the
    reader cannot match was stepped over, so the ancestor walk ran past the element's own
    surface and printed a clean pass over an unreadable card, and a red over a legible one.
    The gradient ground took the first matching rule in document order while the reader
    beside it ranked by specificity.
  - `✅` and `✅️` are one mark on the board and two strings to every reader, and this plan
    already writes `👁️` with a selector. The mark is normalized where it is parsed, in both
    homes. The test written to catch the two board readers disagreeing still carried the
    two-tag regex its sibling outgrew on 28.08, so its reach over the new state was zero.
    And the Blocked column told the owner he was holding up rows whose command simply fails.
  - Six narrownesses stand with their reason in the record, among them one this pass tried
    to close and backed out of: teaching the rotation gate's ambiguous-row arm the heading
    shape reds forty rows the tree holds on purpose.

- **Branches and worktrees from the run are closed.** `repair/adversarial-3-findings` and
  the four `worktree-agent-*` branches deleted with `git branch -d`, their worktrees removed.
  `p2-change-classifier` and the two locked worktrees are untouched.

- **The three lanes are merged, reviewed, repaired and pushed.** `57ec6d85..a27e1332`, 17
  commits. Three conflicts, all in files two lanes had appended to on the same afternoon:
  both journal entries kept, both sets of blocked-list findings kept, and the resume file's
  one status block replaced by the later lane's, as that file's own rule says, with the
  earlier lane's paragraph carried into it.

- **`plan-16` done** — every rule of the pack has one place it lives, and a check keeps it
  there. **`plan-12` done** — the spec says what stands behind each rule it states, the
  first read has its own requirements and node, the feature roster went from seventeen names
  to twelve, and two new gate faults ride the format family with red proofs. **`q-800` waits
  on his eyes** — the report is written and every step it prescribes has already run from
  the playbook's own window.

- **The two loose ends `plan-12` could not reach from a worktree are closed.** The decision
  sheet has its ordering line — which open piece of accepted work runs next and why that one
  — written from the main tree with `scripts/sync-skills.sh` in the same breath, ten skills
  refreshed, `tests/test_config_health.py` 34 passed. And `q-437`, checking for similar
  cases at every level, is back on the board as its own open row: it had been folded into
  `plan-12` when the board was cut and never worked, so the spec's promise was about to be
  owned by a finished task.

- **The merge is where the defects were, and two fresh readers found nine.** Sixteen line
  pointers named the wrong line after the merge — each lane was right in its own tree, and
  only one of the sixteen reddened the pin gate; the other fifteen passed on its two-line
  tolerance. The one-home check could be switched off with no red anywhere, because its arms
  run once per rule over a table and an empty table gives skips. The check keeping a feature
  name off an unbuilt scenario read a leg's promised marker as the scenario's, and fifteen of
  the nineteen markers in the spec are leg-level. The proof that the opening probe tells a
  stale score from a fresh one survived the comparison being reversed. `plan-12`'s acceptance
  command read three function names out of two test files. The roster count was wrong in
  three live documents. Records:
  `docs/prover/2026-08-31-three-lanes-merged.md` and
  `docs/skill-review/2026-08-31-decision-sheet-ordering-line.md`.

- **Suite, gates, CI.** Full suite alone on the merged tree, twice: 12 failed before the
  repairs, 6 after — 2,610 passed, 5 skipped. All six trace to one cause outside this
  project (see NEXT). The gate chain ran end to end and printed "All gates green — push
  allowed."

- **Two of the three lane worktrees and branches are closed**, with `git branch -d` and
  `git worktree remove`. The third could not be: see NEXT.

- **The last three lanes this pass owned are merged, refused by a fresh reader, repaired and
  pushed.** `6cbec19d..16b1a300`, 19 commits, CI run 33417833605. One conflict, in the resume
  file's live-state block, where two lanes had each written a landing paragraph under its own
  heading: both kept under one dated heading, with the director eval's re-measurement beside them.
  - **`plan-2` done, and correcting its board text was half the work.** The row claimed 33 of 35
    with two scenarios accepted by the owner. Both halves were false. The 33 came from a partial
    re-record that left twenty-six scenarios graded against a skill version that had already moved,
    and nothing on that record carries his word — a session's own judgment had been written down as
    his. The row now carries the full re-draw read by both graders and names the one disagreement
    that stays open, `idea-for-another-project`.
  - **`q-527` done** — what counts as a cleared mistake is written once and asked of git afresh on
    every run, so nothing on disk records a clearing and no reader has to trust such a record.
  - **`q-497` done** — a sentence crediting a named person with an undated decision hard-blocks on
    every live page now, rather than on the two decision records alone.
- **A fresh-context read refused the merged range and was right nine times.** Two were the worst
  class, a check loosened rather than corrected. The director grader had stopped failing a turn
  that names an act where the fixture expects none, which is exactly what the thank-you case
  exists to catch, and the number justifying that change was published as six when it is two. Four
  more were in the way out for a cleared worker-restore finding, which counted a wildcard standing
  for the whole tree, a directory already gone from disk, a commit that deletes the file, and a
  commit merely amended afterwards as repairs; each is blocking again and each is held by a test
  that fails against the code as first written. One was the new attribution block reddening
  sentences that carry their date in this project's own form; it reads the whole line now. The rest
  were pointers and counts that drifted at the merge. All nine repaired before the push, in
  `1223b3f4`. Record: `docs/prover/2026-08-31-three-lanes-land-and-a-read-refuses-them.md`.

## NEXT (superseded by the LIVE STATE and HOSTILE REVIEW sections above — read those first;
most items below are already done or already answered up there. Kept as the historical
trail of how the run got here.)

1. **`q-801`** — a project joining the method is still handed the wish queue this one
   retired. Eleven files say so, and the row names all eleven. Nothing blocks it.
2. **`q-437`, back on the board** — checking for similar cases at every level, at the level
   of the set of variations a product of a kind is rendered under. Its definition of done
   names the case the landing writes and the command that will then decide it.
3. **Five rows whose finish is his own read**, and no command can stand in for it: `q-531`,
   `q-751`, `q-576`, and the two marked for his eyes, `q-166` and `q-501`. `q-800` joins
   them — its page is written and the work it prescribed has run, so what is left is
   his reading of the answer.
4. **Six entries in §Blockers, three of them new today.** Two of the three have since closed and
   are marked here so nobody chases them: the suite was red on five checks over the external
   reviewing skill's version, which the prover-pin landing settled, and the whole suite is green
   now; and "a question you ask never turns into a task" was ticked while its acceptance command
   failed, because the recorded runs were older than the skill they grade — the full re-record of
   all thirty-five put the runs back in front of the skill and the command passes again. What
   stands is the idea shelf: promised in the spec and owned by no row, because the check that
   demands an owner reads a marker by the line above it and this one sits under a heading — the
   same argument that put `q-437` back on the board.
5. **The older questions still stand**: whether the amended bar for a queued task holds, what
   is left of two rows that went off the board while a worker still had them, and two rules of
   his that live only in the retiring working agreement and entered no skill in `plan-16`'s
   landing. Two pointers inside `PLAN.md` name lines that moved today and could not be
   corrected, since that page's own rule allows a session only a status and a §Blockers edit.
6. The product review package is still the agenda for the next real movement, once he has
   read it.
7. One seat-level note, three times earned now: two writers in one tree collide. This pass hit it
   twice more. A reviewing agent ran two full suites in this same tree while the merge lane's own
   suite was reading it, and the merge lane wrote its record while its suite ran; the run that
   proves a push has to be the only one in the tree, and nothing today enforces that.

## NOT TOUCHED, DELIBERATELY

- `~/live-spec-p2` / `p2-change-classifier`: one commit off v5.0.0 while main is 6.0.0, and
  PLAN.md's settled section forbids what it builds. Dropping it is his one-line call.
- Two locked agent worktrees, `a529cd9632dea5580` and `ae4718438fdb99475`.
- **`a4f4f7d99d1c91bfa`, the one-home lane's own worktree.** Its branch is merged into pushed
  main and its tree is clean, so nothing is lost, but it carries a lock naming a `claude`
  process that has been running since 26.08 and is alive. Removing it needs a force, and
  unlocking it would act against a live process's claim on the path. Left standing; one
  `git worktree unlock` then `git worktree remove` closes it once that session is gone.
- `skills/product-prover/`: its own repository, released three times today. The local clone
  stands three releases ahead of the commit this project's build pins, which is why five
  checks are red here and green on the server.
- `q-166`, `q-501`: marked for his eyes, and their Source lines do trace to his word.
- The tlvphotos pack version gap (2.7.0 against 6.0.0): another window's work.
- `q-568`: closed on another task's measurement, and the cost-audit page its own acceptance
  named was never produced.
