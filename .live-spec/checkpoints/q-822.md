# A session starts light — opened at reopen 2026-09-06; the row predates checkpoints
Status: closed
Owner: pipeline

## DONE

LANES: plan expects 2 steps side by side; lane decision runs 1; divergence: the plan expected 2 side by side and the lane decision runs 1
RECEIPT: {"by": "clean-context verifier", "at": "2026-09-06T17:02:31", "tree": "f31f22009af0e484d860542516f50178a185b6dd", "head": "e65ae0ee96c9fd5b3717adad3ed696db69145988", "dod_hash": "6c23b2082ea38b6d77f14f756b6f720fc7a5d99b695a24a9373fbcef414eec26", "surfaces": [], "checks": [["python3 -m pytest -q tests/test_front_door_boundaries.py tests/test_next_steps_boundary.py tests/test_task_admission.py tests/test_director_scenarios.py", 0], ["grep -q '^## Route contract' skills/director/SKILL.md && ! grep -q '^## Execution' skills/director/SKILL.md && ! grep -q 'scripts/checkpoint.py' skills/director/SKILL.md && grep -q 'scripts/checkpoint.py' skills/build-pipeline/references/accepted-work-execution.md", 0], ["! grep -qE '(q|plan)-[0-9]+|^### (✅|🔄|⬜|⛔|🔁)' NEXT_STEPS.md templates/NEXT_STEPS.template.md", 0], ["python3 evals/director/check.py --all > /dev/null", 0], ["python3 evals/director/check.py --all | grep -q '^shared reds: 0'", 0], ["test \"$(ls evals/director/traces/*.json | wc -l | tr -d \" \")\" = 36 && test \"$(ls evals/director/recordings/2026-09-06-pair-6/*.json | wc -l | tr -d \" \")\" = 36", 0], ["S=$(shasum -a 256 skills/director/SKILL.md | cut -d\" \" -f1); test \"$(grep -l \"\\\"skill_sha256\\\": \\\"$S\\\"\" evals/director/traces/*.json evals/director/recordings/2026-09-06-pair-6/*.json | wc -l | tr -d \" \")\" = 72", 0], ["test \"$(bash scripts/state-probe.sh | grep -o \"required context.*bytes\" | grep -o \"[0-9]*\" | tail -1)\" -lt 83596", 0]], "verdict": "passed"}
Reopened: the done was false — closed without an acceptance receipt; evidence: no RECEIPT line existed; the row predates the kernel — settled by the receipt at 2026-09-06T17:02:31
estimate 8–14 hours → actual not recorded
divergence: the plan expected 2 side by side and the lane decision runs 1

## IN PROGRESS

(nothing yet)

## NEXT



## DECISION SHEET

Goal: A session starts light. The row closed before checkpoints existed, so this sheet carries the row's own goal and nothing the closed work recorded.
