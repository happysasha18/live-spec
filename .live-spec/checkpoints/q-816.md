# The work board — worker lanes, timing, who's on what — builds once package 2 gives it a real trigger
Status: closed
Owner: pipeline

## DONE

scripts/render-board.sh grew from the four-column pseudo-kanban into the work board Requirement 309 describes, in place: one generator, one page, one path, stdlib only, no server and no poll. Builder, opus.
The whole queue stands in four columns — awaiting validation, ready, in work, done — every row in exactly one, its column read off the status its own row records.
The in-work column splits into one lane per build lane the profile's own lanes.cap allows; each busy lane names its row's branch and worktree, read from git branch lane/* and git worktree list, and the free lanes read as free. Builder, opus.
A card's face reads echo-name, then the description of the behaviour, then its chips — placement, door, spec anchor, time, craft and tier, session — and every other detail sits behind the fold.
Given-vs-actual time is read off each checkpoint's own stamps. The estimate half has no artifact to read: no ticket field, checkpoint line or delivery report records one, so every card says so in those words instead of printing an invented number. Builder, opus.
The waiting region renders WAITING.md and keeps no list of its own; the done column reads this month's docs/queue-archive/ beside the plan's own closed rows, older months behind a fold.
The board refuses to render until SURFACES.md carries its registry row, red-proved by a tree whose registry has none.
tests/test_work_board.py: 21 tests, one per built matrix row, all red-proved against the renderer as it stood before the landing. Checker, opus.
matrix/work-board.md: every row reads built naming its test — 28 of them. M-531 to M-535 landed the same day with the statement-validation check, so the datum they were waiting on, a statement's time estimate, is the row entry's own field. M-649 and M-650 carry Requirement 309 criteria 44 and 45, built into the statement floor 2026-09-06.
Four neighbouring tests re-aimed at the new markup (the canon comparison, the id sweep, the failing-done-mark arm, the installed-host title), and q-166's acceptance key re-aimed at the columns Requirement 309 names.
Two defects the tests caught and the renderer now handles: a lane branch checked out in another worktree is listed by git with a leading "+", not "*", so every open lane read as none; and a deferred row was counted but its card dropped off the page.
LANES: plan expects 3 steps side by side; lane decision runs 3; divergence: none
EVIDENCE 2026-09-06 16:40: incident — acceptance keys q-816/q-166 rendered the board inside a check; the renderer evaluates keys; a 14-deep cascade hung six hours and a second one drove load past 160. Root fix: keys are static; plan_checks_core.evaluate refuses re-entry under LIVE_SPEC_EVALUATING (exit 3); red-first tests in tests/test_plan_is_not_executable.py (depth 3 without the breaker, 1 with it). Attached as a correction to this row, no new row.
RECEIPT: {"by": "clean-context verifier", "at": "2026-09-06T16:46:53", "tree": "e2ff24e14b0f2f22b5eda0ca192e8f519de32138", "head": "e65ae0ee96c9fd5b3717adad3ed696db69145988", "dod_hash": "b3e3ca4cff8d1b01ab02b8b6d2258398dab156e66692981bf87d323c5ec5bd64", "surfaces": ["https://happysasha18.github.io/live-spec/board.html", "board.html"], "checks": [["python3 -m pytest -q tests/test_statement_validation.py", 0], ["grep -q \"<h2>Done</h2><span class='count'>3 today</span>\" board.html && grep -q \"<h2>In work</h2><span class='count'>0 of 10 lanes busy</span>\" board.html && grep -q 'id=\"card-q-816\"' board.html && python3 -c \"import sys;s=open(chr(98)+chr(111)+chr(97)+chr(114)+chr(100)+chr(46)+chr(104)+chr(116)+chr(109)+chr(108),encoding=chr(117)+chr(116)+chr(102)+chr(45)+chr(56)).read();w=s.split(chr(60)+chr(100)+chr(105)+chr(118)+chr(32)+chr(99)+chr(108)+chr(97)+chr(115)+chr(115)+chr(61)+chr(39)+chr(99)+chr(111)+chr(108)+chr(32)+chr(105)+chr(110)+chr(119)+chr(111)+chr(114)+chr(107)+chr(39)+chr(62),1)[1].split(chr(99)+chr(111)+chr(108)+chr(32)+chr(114)+chr(101)+chr(97)+chr(100)+chr(121),1)[0];sys.exit(0 if chr(99)+chr(97)+chr(114)+chr(100)+chr(45)+chr(113)+chr(45)+chr(56)+chr(49)+chr(54) in w else 1)\"", 0], ["test \"$(grep -c \"^- lanes.cap: 10 \" \"$HOME/.claude/live-spec/profile.md\")\" = 1", 0], ["grep -q \"^\\*\\*Statement.\\*\\* Echo-name: The work board.\" PLAN.md && grep -q \"^\\*\\*Validation.\\*\\* 2026-09-06 · floor: passed · reader: passed · echo-name placed: yes · status: ready\" PLAN.md && grep -q \"^\\*\\*Frozen at take-up 2026-09-06.\\*\\*\" PLAN.md", 0], ["grep -q \"^LANES: plan expects 3 steps side by side; lane decision runs 3; divergence: none$\" .live-spec/checkpoints/q-816.md", 0], ["grep -q \"| work-board | https://happysasha18.github.io/live-spec/board.html |\" SURFACES.md && test \"$(grep -c \"^ *\\[target\\]$\" spec/work-board.md)\" -eq 0", 0], ["export LSB=${TMPDIR:-/tmp}/live-spec-board-verify.html; curl -sS -o \"${TMPDIR:-/tmp}/live-spec-board-verify.html\" -w '%{http_code}' https://happysasha18.github.io/live-spec/board.html | grep -qx 200 && test \"$(grep -c 'https://happysasha18.github.io/live-spec/board.html' \"${TMPDIR:-/tmp}/live-spec-board-verify.html\")\" = 1 && grep -qE 'built [0-9][0-9]:[0-9][0-9] [A-Z][A-Z]+, ' \"${TMPDIR:-/tmp}/live-spec-board-verify.html\" && ! grep -qE '/home/runner|/Users/|github/workspace|_site' \"${TMPDIR:-/tmp}/live-spec-board-verify.html\" && grep -q 'lanes: not judged here' \"${TMPDIR:-/tmp}/live-spec-board-verify.html\" && grep -q 'id=\"card-q-816\"' \"${TMPDIR:-/tmp}/live-spec-board-verify.html\" && grep -q '<b>Statement.</b> Echo-name: The work board.' \"${TMPDIR:-/tmp}/live-spec-board-verify.html\" && grep -q 'validation 2026-09-06 · floor: passed · reader: passed · echo-name placed: yes · status: ready' \"${TMPDIR:-/tmp}/live-spec-board-verify.html\" && grep -q \"<h2>Done</h2><span class='count'>3 today</span>\" \"${TMPDIR:-/tmp}/live-spec-board-verify.html\" && grep -q \"waits until it's actually asked for</b> <span class=\\\"st\\\">declined\" \"${TMPDIR:-/tmp}/live-spec-board-verify.html\" && grep -q 'is caught automatically</b> <span class=\"st\">declined' \"${TMPDIR:-/tmp}/live-spec-board-verify.html\" && grep -q \"building for</b> <span class=\\\"st\\\">terminal state not recorded\" \"${TMPDIR:-/tmp}/live-spec-board-verify.html\" && grep -q 'to the newer version</b> <span class=\"st\">terminal state not recorded' \"${TMPDIR:-/tmp}/live-spec-board-verify.html\" && python3 -c \"import re,sys;s=open(__import__('os').environ['LSB'],encoding='utf-8').read();w=s.split(chr(60)+chr(100)+'iv class='+chr(39)+'col inwork'+chr(39)+chr(62),1)[1].split('col ready',1)[0];sys.exit(0 if 'card-q-816' in w else 1)\"", 0]], "verdict": "passed"}
estimate 4–8 hours → actual not recorded
divergence: none

## IN PROGRESS

(nothing)

## NEXT

(nothing — criterion 8's stable link is built: the board answers at https://happysasha18.github.io/live-spec/board.html, published from this repository's own scripts/render-board.sh by .github/workflows/pages.yml, and spec/work-board.md's target markers retired in e0a0f8f6, so tests/test_traceability.py::TestTargetOwnership::test_targets_owned_by_open_rows runs green)

## DECISION SHEET

Goal: The work board — worker lanes, timing, who's on what — builds once package 2 gives it a real trigger
Observable outcome: one page at one stable link answers, without asking the session: what is being done now, who runs each thing, what was done, and how long each took against what it was given
Definition of done: spec/work-board.md Requirement 309's criteria minus the retired auto-refresh heartbeat — a card per task read from the plan's own parser, one lane per build lane the cap allows, given-vs-actual time per row read off the records that already carry stamps, per-agent attribution by craft name and icon, and one published link — each with a test named on its matrix row in matrix/work-board.md, and spec/live-status-reporting.md Requirement 310 criterion 10's announcement home standing as the board's own per-task plan
Verification: bash scripts/render-board.sh writes board.html, and python3 -m pytest -q tests/test_work_board.py runs green over the matrix rows M-519..M-544
Project: live-spec
Scope: Board & visibility
Source: restored 2026-09-03 correcting q-813; the owner's 2026-09-02 12:46 word at .live-spec/turnkey-contract-composed.md:305 keeps Requirement 309 and the rest of matrix/work-board.md, scheduled to build after package 2. q-823 closed 2026-09-06, which fired this row's trigger.
