# Stage-2 batch 1 — rule 7, the concurrent-edit fence — 2026-08-12

Root: the accepted plan (his «принято» 2026-08-11 21:22), stage 2 recipe S1–S5; the queue runs
by price (his word 2026-08-09 11:22).

## Batch choice

The price table `.live-spec/r5-rule-prices-2026-08-11.md` covers the nine working skills alone.
For the shared rulebook the price source is the day-1 census. Its top rule by body bytes is rule
31 (6,067) — and rule 31 carries Alexander's open word on the two senses of "owner" (queue rows
536, 539, open since 2026-08-05). A rewrite before his word would either decide the question
silently or preserve a known defect, so batch 1 took the second rule by price. Rule 31 becomes
batch 2 on his word; its S1 inventory is ready (`.live-spec/s1-rule-31-2026-08-12.md`).

## Verdict lines, one per step

- **S1 (inventory, Sonnet).** Rule 7, the concurrent-edit fence, body `skills/live-spec-base/SKILL.md`
  lines 170–211 at 5,477 bytes re-measured against the live tree. Nine requirements, each quoted
  with its addressee; surfaces in eight categories (17 pinned test files, 12 spec codes, matrix
  rows M-244/M-476/M-477, architecture pins, five other skills, guardrails and scripts). Three
  rotted pointers found at the edges and excluded with reasons. Page:
  `.live-spec/s1-rule-7-2026-08-12.md`.
- **S2 (rewrite, Opus).** Body 5,476 → 5,171 bytes (−305; the frozen clause 6 held byte-for-byte
  at 1,566). Over-cap sentences in the rewritten prose 4 → 0; five more stand inside the byte-frozen
  worker-restore clause, untouched by design, so the whole rule reads 9 → 5. The file's census
  findings 74 → 70. All
  nine requirements carried, all twelve codes kept in place, every test-pinned literal phrase
  survives. One sentence dropped whole (the pointer at a script's own header); its facts stand in
  the script.
- **S3 (clean-context check, fresh Sonnet).** Verdict: accept. Nine of nine requirements mapped
  to carrying sentences by quote; no prescription weakened to advice, no threshold or default
  lost; zero new coined terms; zero contrast frames; twelve of twelve codes found.
- **S4 (surfaces, seat).** Eighteen architecture line pins reshifted for the three-line
  shortening (the map S2 pre-verified on a tree copy); pin gate green, 209 pins checked.
  Installed copy synced same hour (`scripts/sync-skills.sh`: live-spec-base updated).
- **S5 (closing measures).** Rulebook volume by the repaired command: opening 73,578 → closing
  72,929 bytes — the batch's mechanical test passes (the fall is −649: −305 this batch's rewrite,
  −939 the D2 cut, +595 profile growth recorded 2026-08-11–12 in the profile's own home). Full
  run on the closed tree: 2,485 passed, 1 skipped, 0 failed. Gate count stands at 29.

## Who the rule reaches

Every session and worker that writes shared files: the fence before each write and commit, lanes
under the single pen with the profile cap, brief-time write-set disjointness, the worker
tree-restore prohibition (five homes, byte-frozen), the prior-context worker law, the stable
session identity for the pen tie-break.

## Carried forward

- Rule 31 batch waits his word on rows 536/539.
- `tests/test_worker_restore.py` lost the INV-298 code from its own text and
  `guardrails/check-worker-restore.py` cites INV-218 — the pinned-test map rotted a code; found
  by S1, left for the stage-2 batch that touches those files.
- The measure of the day-1 census pointer at rule 7 predates the rule-30 cut; the S1 page pins
  the live lines.
