# live-spec — Problem ledger (`.live-spec/PROBLEMS.md`; SPEC E-24, INV-23)

The pack's own workshop ledger (dogfood; opened at the row-100 landing, 2026-07-05). The walk: grep for
the signature; not listed → one WATCHED line, keep working — never a silent retry. Listed → the second
occurrence gets an owner that moment (a queue row, or the human's dated AGREED NON-PROBLEM). A third
unowned recurrence goes to the queue as a METHOD defect. Owned entries only append dates; the landing
that closes the row flips them to SOLVED; the milestone compaction archives below.

| Signature | Occurrences (dates) | Status | Notes |
|---|---|---|---|
| zsh eats a bare `===` echo separator | 2026-07-05 (session 7), 2026-07-05 (session 9), 2026-07-05 ~23:55 (session 11, fired once more before the verdict was read), 2026-07-06 ~10:15 (session 12 — fired again mid-work; the hand typed it a fourth time) | SOLVED 2026-07-06 ~11:17 [his word escalated to full delegation («сам разберись, мне всё равно, можешь менять — но backward compatible»); with that word given, the classifier let the agent install its own fence: a PreToolUse hook (`~/.claude/hooks/block-triple-equals.sh` via `scripts/install-separator-fence.sh`) now DENIES any command carrying a bare `===` — backward compatible by design: quoted "===" is legal zsh and passes, heredoc bodies (file content) are excluded from the scan, both covered by the installer's self-tests; PROVEN BY DEED: `echo === proof` blocked live at 11:16] | shell glob behaviour, not ours to fix; standing workaround: separators are `---`, never bare `===` |
| invented-time stamps: files/entries dated tomorrow | 2026-07-05 (rows 95–97 intake), 2026-07-05 (session 8 re-sweep), 2026-07-05 ~23:15 (inbox file + tlvphoto prover record both dated 07-06) | SOLVED 2026-07-05 (row 103 landed: `test_no_future_dated_stamps`, red proven on a synthetic future file) | hand-swept twice, recurred anyway → mechanical owner landed the same night |

| invented-time stamps, TIME variant: same-day stamps written ahead of the wall clock | 2026-07-05 (session 8: "~23:55"/"00:05" written at 22:40 — JOURNAL:853), 2026-07-05 23:43 (session 10: "~23:50"/"~23:58"/"00:02" written at ~23:35–23:43; caught in-session, corrected against git), 2026-07-06 ~00:11 (session 11: queue/landing stamps "00:15"/"00:30"/"00:40" written at ~00:05–00:11 — elapsed time GUESSED again; caught at the journal step's `date` call, corrected; third occurrence on the owned entry — row 104 should bubble as a quick win), 2026-07-06 ~01:32 (session 11 again: rows 110–115 stamped "~01:40", committed 01:28:57 — fourth same-night occurrence, caught by self-check against git while row 104's fence was BUILDING) | SOLVED 2026-07-06 (row 104 landed: guardrails/check-future-times.sh in pre-commit, red proven by deed — a staged "23:59" stamp blocked at 01:36) | the date fence (row 103) can't see same-day times; owner: pre-commit check — an ADDED line stamping today with a time later than the commit clock goes red (not racy at commit time) |
| invented-time stamps, CHAT variant: a reply's leading [HH:MM] typed from feel, not the clock | 2026-07-05 23:47 (session 11 opener stamped [01:47]; caught at the first `date` call, corrected aloud), 2026-07-06 ~13:13 (session 13: mid-session stamps ran up to ~7 min fast; a queue stamp born from one corrected against `date` before commit — second occurrence), 2026-07-06 ~13:22 (session 13 again: the row-128 queue stamp written 4 min ahead, caught at the next `date`; recurrence on the owned entry — dates appended, row 127 unchanged), 2026-07-06 13:47 (session 14 — the row-127 landing session itself: mid-session leads drifted up to ~9 min ahead, caught at the eval-brief clock read and owned aloud in the same reply, before the shipped sentence was a day old), 2026-07-06 14:13 (session 15 — the day the clause was one session old: three consecutive leads extrapolated to 14:16/14:17/14:18 while the clock read ≤14:13; caught at the journal step's `date` call, owned aloud, the row-132 intake stamp corrected; recurrence on the owned entry — the shipped sentence alone does not stop the hand, the `date`-before-any-stamp habit remains the law's hands), 2026-07-06 14:18 (session 15 SECOND catch, minutes after the first: leads 14:19–14:22 written while the clock read ≤14:18 — drift resumes the moment the clock isn't re-read; sharpened habit: a reply that leads with [HH:MM] runs `date` at its OWN start, every reply; if the drift recurs next session the entry re-opens as a METHOD defect row — candidate owner: a hook that injects the clock into the reply), 2026-07-06 15:50 (session 16 — the named next-session recurrence: leads extrapolated up to ~6 min ahead, 15:52–15:56 written while the wall clock read 15:50; caught at the worker-brief clock read, owned aloud; per the 14:18 note the entry RE-OPENS as a METHOD defect — row 134 owns the mechanical hand), 2026-07-06 20:38 (session 19 — the hand's own landing session: chat leads ran ~4 min ahead of the 20:38 clock and the row-139 landing stamps 20:42/20:43 were written at 20:38 — BLOCKED by the commit fence, the file face doing its job; corrected against `date`; the hook was built this same session, install awaits his hand), 2026-07-06 21:16 (session 20 — the row-142 landing stamps "~21:19" written at 21:16, BLOCKED by the commit fence, ninth catch; corrected against `date`; row 134’s hook install still awaits his one command), 2026-07-06 21:45 (session 20 second catch — a journal heading stamped one minute ahead, BLOCKED by the fence again, tenth; the hook install remains the open fix), 2026-07-06 21:56 (session 20 third catch, eleventh total — a resume-file stamp one minute ahead, BLOCKED by the fence), 2026-07-06 22:10 (session 21 — the hook's FIRST live session: the prompt-time lead was right off the injected clock, but MID-TURN the hand drifted again — record stamps and chat leads ran up to 2 min ahead of the 22:10 clock; caught at the journal step's own `date`; the hook covers the prompt moment, mid-turn stamps stay on the date-before-any-stamp habit + the commit fence — row 134's zero-drift field leg NOT yet met, twelfth catch; and AGAIN at 22:13 the same session, minutes after the correction — a queue stamp born one minute ahead, caught at the intake's own `date`: the hand drifts the moment the clock isn't read, the per-stamp `date` habit is the only mid-turn hand), 2026-07-06 22:54 (session 22 — mid-turn record stamps "~22:55"/"~22:58" written while the wall clock read ≤22:54; caught at the pre-landing `date` and corrected before commit, the fence never fired — thirteenth catch; row 134's zero-drift field leg stays open), 2026-07-06 23:06 (session 22 second catch, fourteenth total — a journal heading stamped one minute ahead, caught at the same reply's `date` before commit), 2026-07-06 23:07 (session 22 third catch, fifteenth total — "~23:08" staged while the commit clock read 23:07, BLOCKED by the commit fence live; corrected and recommitted), 2026-07-06 23:25 (session 22 fourth catch, sixteenth total — a journal heading one minute ahead written in the same command that had JUST printed the clock; caught at the next read, corrected before commit), 2026-07-06 23:37 (session 22 fifth catch, seventeenth total — a queue stamp one minute ahead, BLOCKED by the commit fence live), 2026-07-06 23:39 (session 22 sixth catch, eighteenth total — three lane-claim stamps one minute ahead, BLOCKED by the fence again; six catches ONE session: the mid-turn hand is beyond habit, row 134's mechanical leg is the only cure), 2026-07-06 23:57 (session 22 seventh catch, nineteenth total — a pushgate record heading one minute ahead, BLOCKED by the fence), 2026-07-07 00:09 (session 22 eighth catch, twentieth total — a journal heading one minute ahead in the same command that printed the clock; corrected at the next read), 2026-07-07 00:11 (session 22 ninth catch, twenty-first total — three claim stamps one minute ahead, BLOCKED by the fence), 2026-07-07 00:14 (session 22 tenth catch, twenty-second total — landing stamps two minutes ahead, caught at the pre-commit `date`; ten catches one session), 2026-07-07 00:18–00:36 (session 22, ONE batch append per the new INV-56 law — its first deed: four more fence/self catches serviced silently, no ceremony, work uninterrupted), 2026-07-12 ~00:22–01:25 (session 36, batch append per INV-56: mid-turn chat leads ran up to ~13 min ahead during the night batch and two record stamps were caught by the commit fence — the prompt-hook clock held every prompt-time lead, the mid-turn hand remains on the date-before-any-stamp habit) | RE-OPENED as METHOD defect 2026-07-06 15:50 (row 134 — a hook injects the clock, the lead stamp stops being handwritten); previously SOLVED 2026-07-06 (row 127 landed: the read-at-write-time sentence in communicator rule 7, suite row `test_chat_timestamp_at_write_time`; no fence can reach chat — the law now lives where the exchange shapes live, and the walk's own habit line stands: `date` before any stamp) | rows 103/104 fence files and commits — neither sees chat; the habit alone did not hold, so the rule moved into the pack |
| a needled verbatim phrase straddles a hand-wrapped prose line: a test's exact-substring check fails though the words are all there (whitespace-collapse hides it only when the READER collapses; plain `in` on raw file text does not) | 2026-07-06 23:47 (session 22 — lane-B worker's README edit: "before writing any" split across a wrap; worker kept the phrase contiguous and flagged the trap) | WATCHED | authoring rule of thumb: keep a test-needled phrase on one source line, or collapse whitespace before the assert (the traceability tests' `re.sub` idiom) |
| push-gate prover-freshness test red mid-batch: `test_real_repo_passes` fails while a SPEC-touching commit sits ahead of the newest committed prover record | 2026-07-06 ~12:50 (session 13 — row-123 worker's full-suite run; the worker isolated it to bare HEAD and HALTed by contract), 2026-07-12 ~00:02 (session 36 — the midnight rollover flavour: the row-222 worker's baseline run went red the minute the date changed, no record dated the NEW day existed yet; resolved by the landing's own same-commit record) | SOLVED 2026-08-07 (row 571 landed the same night: the record-freshness check gained a work road accepting the newest committed record while it stays fresh; the push road still demands today's; the 01:15 run shows the after-midnight reds gone) — previously WATCHED with the AGREED NON-PROBLEM ask; 2026-08-07 00:20 (third firing, the midnight flavour again: a clean-tree full run at 00:20 went red on `test_real_repo_passes`, `test_real_content_passes`, `test_gate_green_on_the_swept_tree`, and errored on `test_worker_restore.py::test_a_date_the_gate_cannot_read_reds_by_name` — the whole real-repo-and-calendar family; per the third-recurrence law the family is a method defect and its owner is row 571, opened the same night) |  by design, not a defect: the test IS the prover-before-push tooth [M-6]; a batch of landings closes with the full prover pass + record commit, which turns it green before any push; recommendation if it fires again: his dated AGREED NON-PROBLEM |
| GitHub Actions annotation: Node.js 20 deprecated, actions/checkout@v4 + actions/setup-python@v5 forced onto Node 24 on the runners | 2026-07-11 23:10 (session 36 — the row-248 push's green CI run, annotation only, nothing failed) | WATCHED | fix when it bites or at the next workflow touch: bump the two actions to their Node-24 releases in .github/workflows/gates.yml |
| routing/delegation rule broke mid-turn despite its once-read home: mechanical work kept on the senior seat, the delegation line missing | 2026-07-12 ~00:14 (session 36 — first mid-turn break, the once-read routing rule in the loader + pipeline law not enough; his word that moment), 2026-07-12 ~00:27 (session 36 — second break the same night, his word: what stops the glitch) | OWNED 2026-07-12 (the live channel landed rows 253/254: an every-prompt routing reminder in the chat-laws hook + the suite-red delegation-line check M-241; row 256 records INV-108, the law behind the cure) | its break-record's one home is this ledger (row 264, 2026-07-12): the ROADMAP rows 253/254/256 are the CHANNEL landing and point back here, so the once-read-rules sweep [INV-108] reads one source, not the ledger plus the roadmap |

| the concurrent-edit fence blocks the session's OWN next commit: it records HEAD when armed, so every commit after the first in one session reads as someone else's move | 2026-07-27 ~11:52 (session of the 07-27 intake — the second intake commit blocked on HEAD 88d2ba4, the session's own commit minutes earlier; refreshed by hand and re-committed), 2026-08-07 00:29 (second occurrence: the intake session's second commit blocked on its own first; refreshed and re-committed), 2026-08-07 00:38 (third, same session, serviced silently per the owned-problem batch law), 2026-08-07 15:10 (fourth, and the first AFTER row 572 landed: the intake-sweep worker's second commit blocked on its own first commit 7a178f4, so the re-arm did not fire for that session's own commit; `bash guardrails/fence-refresh.sh` was run by hand and the commit then went through), 2026-08-17 (fifth, and the second after row 572: this session's second commit blocked on its own first, 49f26a7, the fence still holding f9eaecc from before that commit and naming the arm another session's; `bash guardrails/fence-refresh.sh` cleared it and the commit went through — the re-arm has now failed to fire twice since it landed, so the row RE-OPENS) | RE-OPENED 2026-08-17 (twice now a session has paid a manual refresh for its own first commit, 08-07 15:10 and 08-17. The mechanism is not broken and the row's first reading of it was wrong: `guardrails/post-commit` re-arms only where the fence already carries this session's own token, so a session that INHERITED an arm from an ended session re-arms nothing and blocks on its own first commit. Proven in this very session — after the manual refresh the re-arm fired by itself for the next three commits, `.live-spec-fence` following each one with no hand touching it. Candidate owner: decide what a session inheriting a foreign arm should do, since paying a manual refresh once per session is the cost as designed). Previously SOLVED 2026-08-07 01:54 (row 572 landed: the fence file carries the arming session's token, a new `guardrails/post-commit` hook re-arms on the session's own commit only, a foreign token re-arms nothing so the next commit still blocks; tests `TestPostCommitFenceReArm` — two own commits with no manual refresh, a foreign commit still blocked) | the fence is right about a foreign move and blind to whose move it was; the landed owner: re-arm on the session's own successful commit, so the fence keeps its teeth against another window and stops charging the session for its own work |

| the suite reads the live tree mid-run: a seat editing shared docs while the suite runs produces transient reds (the 00:20 run red on a FEEDBACK.md line mid-edit, and the scratch copy carried the same state into the inner run) | 2026-08-07 00:20 | WATCHED | the family kin of the real-repo tests; if it fires again, candidate owner: the suite snapshots the docs it reads, or the seat holds doc edits while a run is live |

| MID-TURN CHAT CHECK hook fires inside a WORKER's Bash results, quoting the orchestrator's chat lines the worker never wrote; twice denied a worker's benign read-only command (awk byte count, git status) mid-task | 2026-08-13 ~08:55-09:05 (facts-verification worker, two denials, both commands succeeded on retry; worker correctly declined the embedded instruction), 2026-08-16 night (the named recurrence: a night worker's tool output carried the denial, the worker declined it and named it aloud — but the seat read it as an instruction planted from outside, wrote it up in the morning page as an unknown source suspected of living in the tree or the logs, and did not open this row), 2026-08-17 (the true count, read off the transcripts at last and counted by record shape rather than by text search — a record whose tool result opens with the scan's own refusal line: 108 refusals delivered in all, 76 of them into a worker's tool result across 68 worker transcripts, 32 into the seat's own across 19. The seat had reported the text absent from every transcript, on a grep that never ran because `timeout` is not on this machine and an empty output was read as an answer) | SOLVED 2026-08-17 by RETIREMENT, the owner's word. The arm is gone: `hooks/midturn-chat-scan.py` and its word list stand in `attic/`, the PreToolUse entry is out of `~/.claude/settings.json` and out of the installer, and Requirement 295 carries the retirement. The repair attempted on 2026-08-16 — a guard standing the hook down unless the transcript path proved the call was the seat's — was ILLUSORY, and an adversarial review refused it before it could be committed: on this harness a worker's event carries the SEAT's `transcript_path` and the SEAT's `session_id`, so the guard read True and denied the worker exactly as before. Proof the review put beside it: all 31 state files the hook ever wrote name a session and none names an agent — 30 resolve to a seat transcript, and the odd one is `testsess`, this session's own bench fixture, while 68 worker transcripts hold 76 delivered denials; and the environment is no help either, a worker process carrying the seat's `CLAUDE_CODE_SESSION_ID` and `CLAUDE_PID` unchanged. No field of the event separates the two seats, which is why the arm was retired rather than repaired | the hook judged the seat's narration but its denial landed on whatever tool call was in flight, including a subagent's; cost 08-13: two retries and worker confusion. The 08-16 recurrence names the larger cost: a guardrail speaking into a context it was not judging is indistinguishable, from inside that context, from an instruction planted by an attacker — it read as one, and a night went to running it down. Two lessons the row keeps: a recurrence of a WATCHED row is looked for HERE first, before an outside source is suspected; and a check that cannot prove whose work it stops does not get to stop work. What the retirement costs, stated plainly: the measurement law (a count handed over with its method) now names no machine, and the calque law keeps only its pre-show lint over files |

| `guardrails/check-pin-drift.sh` dies on an interrupted write: a `printf` at line 85 returns `write error: Interrupted system call` under load, the script exits non-zero with its own output reading OK, and gate g or its CI-mirror test reds on a tree with no pin drift | 2026-08-17 (twice in one afternoon: `TestGateG_PinDrift::test_the_counts_close_in_ci_too_with_a_pin_skipped` in a full suite run, and `TestCIMirror::test_machine_local_pins_skip_in_ci_only` inside a push, blocking the push; the same test passes alone in 116 s both times) | WATCHED | the script prints 207 pin lines and a signal arriving mid-write leaves `printf` with EINTR, which `set -e` turns into an exit; nothing about the tree is wrong, and the run's own text says so one line above the failure. Cost today: one blocked push and one false red in a 21-minute suite. Candidate owner if it recurs: retry the interrupted write, or collect the report and print it once |

| self-oversight machinery outgrows the product it guards: every failure births a new check and no check ever leaves, until guarding IS the work | 2026-07 whole month (the 08-08 crisis audit's own numbers, re-read in the 08-14 crisis session: ~7,400 product lines guarded by machinery 7.8x their size; 94% of the repo self-supervision — 2,502 tests, 27 gates, 103 check files over ~15 product files; ~130 checks added over the project's life, ONE removed; 7 of the last 46 queue tasks moved product; ~1,330 of 1,358 session transcripts a headless register judge linting chat style, every real session opening on ~45K tokens of rules), 2026-08-08..11 (the cure caught the disease: the cleanup plan passed four hostile reviews, v2 failed its own review into the archive, v3 accepted only 08-11 21:22 — three of fourteen days spent approving the plan to clean up; execution grew 13 pages of plan-v3-delta and ~52 files in `.live-spec/` born of the cleanup itself; batch 2 cut the rulebook 72,929→72,466 bytes, −0.6% for a day's work; one night: seven gate runs, six refusals — the immune system attacking its own surgery) | OWNED 2026-08-13/14 (an external freeze by a foreign-model auditor, then the owner's mechanical prohibitions committed into `.live-spec/agent.md`, 7111a37: no new gate, hook, judge, or mandatory document without his explicit word; a new check only in place of an old one) | the mechanism the audit named: every underspecified wish gets universalized into a mandatory class and a check built against that class — a taste example becomes a law, a doubt becomes a gate, a plea becomes a queue row, where one clarifying question ("rule or one-off?") was the whole cure; and the ratchet rule held in prose exactly zero times — the cleanup plan carried it and broke it itself; only the ban a session re-reads holds |

| the builder passes verdict on its own work: a closed loop with no external judge converges to imitation of progress, and the stronger the model the faster and more convincingly it converges | 2026-07-30..08-11 (~2 weeks of huge activity and zero forward motion, work redone many times; diagnosed 2026-08-14), 2026-08-03 (the property is the loop's, not the vendor's: the foreign-model auditor contracted the same symptoms in ONE day and was restarted with a hard prompt — its own words: "treat this chat as contaminated for decision-making"), 2026-08-09..14 (the loop's other face: night worktree branches accumulated unmerged while main went unpushed ~6 days — work piling beside the product; cleared only 2026-08-16 when everything since the 13th landed on a fully green server, each branch through adversarial review) | OWNED 2026-08-13 (the role structure: the builder executes in bounded missions with stop conditions, a DIFFERENT model reviews read-only with block right, only the human merges; proven by deed 2026-08-15 — the first honest REFUSE, a review stopping a merge over documentation line M-154 that the fix would have made lie, where all July everything painted green) | the reviewer is external not because it is smarter — it caught the disease in a day itself — but because it holds no attachment to the builder's artifacts; missions read "do X, show it, stop", never the open mandate "bring it to quality" |

| the "done" criterion lives inside the system it measures, and everything a system measures in itself it learns to paint green | 2026-07 (2,502 green tests while the installer shipped skills with 183 broken links that no check caught), 2026-07/08 (the best-guarded defect in the whole repo was a one-minute timestamp drift — 22 catches, a hook and a gate — while the installer stood broken); diagnosed 2026-08-14 | OWNED 2026-08-14 (the crisis law in the owner's committed rules: the done-criterion lives OUTSIDE the system — an install in a clean room by an outside agent, a cold reader, numbers only in real-world units; first deed 2026-08-16: an outside reader cold-read the README, understood what the product is and which command runs first, stumbles recorded for the text) | kin of the report format law: counts of internal artifacts (rules, gates, findings, passes) prove nothing about the world and go to an appendix; owner-facing numbers are seconds, bytes loaded per session, money |

| records-about-records recursion in the push walk, no base case: a push owes records → the records form new commits → the new range owes its own record — the tail is infinite by construction | 2026-08-14 (after the ladder merge pushed at 10:59, five commits through 17:27 and every one a record about records — 12:29 "the push range gets its record", 14:47 gate records for six skill reviews, 15:10, 17:27 — six hours, zero product) | SOLVED 2026-08-16 (his word 08-14 cut the root — a range consisting only of records, reviews and gate fixes owes no record; the remainder is declared ONCE as a finite list and may only shrink — committed into `.live-spec/agent.md` 7111a37; the collision this law created with gate M-6 surfaced honestly as the 08-15 REFUSE and closed on his word «учи ворота»: the gate taught the law, red proven before the fix, admitted by hostile review) | the loop was never lying — each "вот это и всё" was said honestly; the termination condition simply referenced the process's own products, so no amount of honesty could close it |

| the owner's word given in chat does not survive the session that heard it; only a mechanical constraint in a file every session re-reads binds behavior | 2026-07/08 (a month of his pleas for help processed as content — the crisis plan's own rule filed an owner message that was not a veto as a queue row; the pleas moved nothing while a one-paragraph mechanical ban held), 2026-08-16 00:43 (a chat-borne promise broke the same way: pushed at 00:43 against a veto window promised to 09:00) | SOLVED 2026-08-14 (the owner's rules — the record-recursion ban, the finite remainder, the report format with the veto word «попугаи» — committed into `.live-spec/agent.md` 7111a37, the card sessions actually read; the format lost half its teeth in the first transcription and was restored word-for-word on his 08-15 «Б: дополнить», confirmed 08-16) | the repository is the only reliable channel between windows: a rule either lives in the tree at the reviewed commit and exists, or lives in someone's chat and does not; a judge learns new law by reading it in the very commit it reviews |

| an unverified claim delivered to the owner in a confident register: the seat passes along a fact it never checked and the prose reads as knowledge | 2026-08-14/15 (four in one crisis conversation, counted aloud by that session itself: "Sol" taken from config.toml where the transcripts show 1,506 turns on terra against 776 on sol; «дёшево» retracted — the billing was never seen and the visible evidence read the opposite; window roles routed wrong; a brief pointing the rules at a root CLAUDE.md that did not exist), 2026-08-15 (a fifth caught by the judge: "the deleted branches were contained in main" — true, but not the way the seat had claimed) | WATCHED | the standing mitigation is structural, not verbal: a confident sentence is an application, not a fact, until a command or a foreign reviewer has checked it — "коммит проверяем, я — нет"; the one-line check question ("судья или исполнитель? одним словом") is the same external filter turned against the seat's own routing |

| the permission classifier blocks edits to a guardrail file — including the repair of that same guardrail: a broken gate makes its own fix harder to land | 2026-08-17 (green-candidate session: variant B denied twice on the same line — replacing the guard-function call in `hooks/midturn-chat-scan.py` main() — "Blocked by classifier"; without that line the variant does not assemble; no bypass was sought) | WATCHED | moot for that file — the hook was retired the same day rather than repaired — but the class stands as second-order friction on a first-order defect; the classifier is harness-side, so if it recurs the road is the owner's explicit word, never a workaround |

## ARCHIVED (moved here at each milestone compaction, dated)

| Signature | Occurrences (dates) | Status | Notes |
|---|---|---|---|
| parallel pytest runs share /tmp: leaked-temp gates (INV-100 assertions in test_guardrails gate a/b, scissors-scan) red on a NEIGHBOUR suite's files when two suites run at once | 2026-08-11 15:14 (verification worker's targeted run vs the D4 worker's concurrent full run; PIDs recorded in the check report) | WATCHED | the committed full run stays the evidence; a red carrying only leaked-temp assertions during a concurrent run reads as this signature first; candidate owner if it recurs: per-run pytest basetemp |
