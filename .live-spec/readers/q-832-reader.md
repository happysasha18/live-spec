What is to be done: Change the daily watcher so it sorts each stale file to the installer whose own vendor set actually carries it, and prints the scaffold re-install road or the style-gate re-install road only for the manifest that matches, proven by tests and recorded in VERSION and MIGRATION.md.
Why: For five files the watcher currently names an installer whose vendor set does not carry them, so a person who follows the advice still holds the same stale files and hears the same advice again tomorrow.
How long: 12–36 minutes.
Echo-name placed: Watcher names the vendoring installer.
