# Handover, 2026-08-09 — the culling's first day, and what it actually established

Root: his order of 2026-08-09, 12:25, before a context wipe. This page holds what the tree does not
already say, so the next session reads one page instead of a night of history.

## Bottom line

No executable plan stands today. Three verdict lists were drawn, and fresh review overturned every
one of them. Two rows of the first list executed before their review: the review of the first read a
landed commit, and the review of the second read the working tree before its commit. Every other row
of all three lists was withdrawn with nothing executed. One check was removed and stays removed. One
check was removed and came back on his word. No rule was cut.

(Corrected 2026-08-09 13:05 by the two-hour check, which read the git log and found the earlier
wording — "all three were overturned before anything executed" — false for those two rows.)

What does stand: a corrected map of the machine, four pieces of work that need nobody's permission,
and five decisions only he can make. One of those five decides whether any of this lasts.

## What the tree holds now

Seventeen commits, `fb1e9d7` through `1f7fca7`, all local. Nothing is pushed. The suite is green at
2492 tests. Thirty checks run on every push.

Landed and standing:
- day 1's four starting measures, `.live-spec/day1-measures-2026-08-09.md`, each with its command;
- the three censuses of rules, checks and delivery, `.live-spec/day1-census-*.md`;
- the queue page for his pen, `.live-spec/day1-queue-for-striking.md`, 209 open rows;
- the handover-provenance check removed, with its whole tail;
- the architecture-pointer check restored on his word of 11:22, with queue row 541 back live;
- queue rows 586 and 587, both opened by findings during the night;
- the rulebook measure's command repaired to count reference pages beside the body.

## Numbers this session got wrong, and their corrected values

The next session must not inherit these mistakes.

| claim made | what is true |
|---|---|
| the rulebook is 35 rules | 88 rules: 35 shared, 53 inside the nine working skills |
| the rulebook is the machine | 314 invariants, 551 matrix rows, 203 test files, 30 checks stand beside it |
| the suite pays 451 s for running itself inside a check | 451 s is what 2 500 tests cost; the double payment was already fixed on 2026-08-07, queue row 573 |
| the install leaves 219 references pointing at nothing | that counts the short install path, which the readme itself calls incomplete |
| the rulebook a session reads is 45 000 tokens | 18 400 tokens for the shared rules and the profile; 45 000 counts a working skill as well |
| base rule 8 protects nobody outside | it guards the installed copies two other projects run on |
| base rules 19, 20 and 28 reach only this project | each rule's own opening sentence names a host or an adopting project |

## What the reviews established, and where the records sit

Four adversarial reviews ran, each by a fresh agent, each before execution.

- `docs/prover/2026-08-09-culling-day2-cuts.md` — the two removals. Four blocking findings. It
  overturned the ground for removing the pointer check.
- `docs/skill-review/2026-08-09-live-spec-base.md` — the rulebook edit inside the second removal. It
  refused two drafts before passing the third.
- `docs/prover/2026-08-09-redrawn-rule-verdicts.md` — the second verdict list. Five blocking
  findings. It showed the rulebook measure moves when text relocates.
- `docs/prover/2026-08-09-culling-plan-v2.md` — the recompiled plan. Seven blocking findings. It
  showed the plan's whole first phase rests on him.

Findings worth carrying forward:
- moving a rule's cases to a reference page keeps the reach of a procedure and loses the reach of a
  prohibition, since nothing prompts a session to open the page; base rules 7 and 31 are prohibitions
  and hold a quarter of the rulebook between them;
- one review record cannot satisfy both review gates, since each reads a different folder with a
  different rule, so merging them edits a gate under a freeze that only he lifts;
- most rules reach a stranger or his reports, so deletion is a small lever.

## What he said in chat that no document holds

- **Chat runs in Russian** from 2026-08-09 11:26, and reports stay short and plain. He read the
  night's reports as sprawl.
- **The architecture-pointer check is essential**, his word at 11:22. The restore is his call
  carried out.
- **The cut is ordered by what makes every change expensive**, his word at 11:22, and it covers the
  whole machine rather than one file.
- **Days and sessions are being confused.** The plan counts days; the work runs in batches. Six
  hours held day 1, day 2 and the opening of day 3. The rule that stops work after two days without
  a moved measure means nothing as written.
- **Outside users are the goal**, so a stranger's run is a test of whether the work is done.
- **He asked for rule usage statistics long ago**, and the instrument exists: queue row 391 landed on
  2026-07-17 and row 489 continues it. `~/.claude/hooks/hook-meter.py --report` prints runs and
  fires per check. Read on 2026-08-09: `answer-first-scan.py` shows 0 fires over 3 095 runs and
  `register-judge-collect.sh` shows 0 over 3 118, the second explained by being switched off since
  2026-07-30. His law says retirement is his call.
- **He wants rules to retire themselves as models improve.** The runs-and-fires law exists for
  checks and reaches no rule today. Extending it is the mechanism he described.

## This session's own failure rate, and the three fixes

Three verdict lists, three overturns. Four of the first five workers stalled or died.

Both causes are known and two fixes are already proven:
1. **Verdicts were drawn from summary tables.** A verdict row is invalid without two pieces of
   evidence: the rule's own opening sentence quoted, and the name of who the rule reaches.
2. **No verdict executes without a fresh review.** Score so far: three wrong lists, three caught.
3. **Worker briefs now forbid background jobs and waiting.** Before that line, four of five workers
   stalled; after it, every worker returned clean.

## What can proceed with nobody's permission

1. Fix the architecture-pointer check so it proves a pointer against its own line, queue row 541.
   Today it accepts a pointer when any matching word sits within a 51-line window, and a pass on
   2026-08-05 found 29 stale pointers green under it.
2. Measure how much of the rulebook repeats itself. He asked for the redundancy figure and no one
   has it. Base rule 4 gives the ground: one home per fact, so any repeat is already a defect.
3. Extend the runs-and-fires law from checks to rules, under his landed row 391.
4. Repair the install so every path a skill names says which tree it lives in.
   `.live-spec/day3-opening-2026-08-09.md` holds the reading.

## What waits on his word

1. **Base rule 30** — any property a machine can verify becomes a blocking check. This is the engine
   behind thirty checks. Without an answer the machine regrows the day the freeze ends, so this one
   decides whether the campaign leaves anything behind.
2. **Base rule 23** — a behaviour that breaks twice earns a check of its own.
3. **A carve-out from the deletion bookkeeping** during the campaign, since five artifacts per
   removed item is what makes removal expensive.
4. **A gate edit** merging the two review records into one review per push.
5. **Verifying the plugin install path**, which means installing the pack into his own setup.
6. **Retiring `answer-first-scan.py`**, silent over 3 095 runs, by his own law.

## Plans on file

`.live-spec/culling-plan-2026-08-08.md` is his frozen plan and stays the record of what was ordered.
`.live-spec/culling-plan-v2-2026-08-09.md` is the recompiled one, unfit to execute as reviewed. Its
phase A rests entirely on the decisions above. Its phases C, D and E stand unreviewed by anyone and
untouched.
