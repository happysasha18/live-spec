# The culling campaign's stated goals, put under watch

Root: row 617 (Alexander, 2026-08-12 23:58, restated 00:02 and 00:03). He said the plan's status
block watches one number and misses the rest of what the campaign was opened to fix; he wants
every stated goal under watch with the command that measures it. This page is that sweep. It reads
the campaign's own founding text (`.live-spec/culling-plan-2026-08-08.md`), the day-1 measures
page, the v3 plan, ROADMAP rows 166/553/568/569/570/622, and JOURNAL.md 2026-08-07 through
2026-08-13, then re-runs every command that still has one and pastes the raw output taken this
morning, 2026-08-13 ~08:38.

Every row below names where the goal was said, in his words or the plan's own words, dated. Where
a command exists, it was run this morning and the output is pasted next to it. Where none exists,
the row says so and proposes the nearest measurable proxy, marked PROPOSAL.

## The table

| # | Goal, in his words, dated | Command that measures it today | Value now (raw, 2026-08-13 ~08:38) | Earliest recorded value | Notes |
|---|---|---|---|---|---|
| 1 | **Rulebook volume in bytes.** The founding plan (08.08, his orders 21:35–22:08): "Проект оброс машиной самопроверки... 45 000 токенов правил". The v3 plan's own status block already watches this one. | `{ find skills/live-spec-base -name '*.md' -not -name 'README.md' -print0 \| xargs -0 cat; cat ~/.claude/live-spec/profile.md; } \| wc -c` (the plan's own appendix command) | **72466** | 73,645 bytes, 2026-08-09 00:40, commit `fb1e9d7` (`.live-spec/day1-measures-2026-08-09.md`) | Matches the plan's head block exactly (72,466). Already under watch — the one goal row 617 says the block already carries. |
| 2 | **Per-session context load, in tokens.** Same founding sentence as #1: "45 000 токенов правил, которые каждая сессия читает перед работой" is the number the whole campaign was sized against. Corrected 2026-08-09 10:53: the repaired command returns 73,503–73,645 bytes, and "a token here is four bytes" (`.live-spec/day1-measures-2026-08-09.md`), giving the "18,400 tokens" figure the plan cites at line 60. | Same command as #1, divided by 4 (`.live-spec/day1-measures-2026-08-09.md`, "Measure 2", "divided by four") | 72466 / 4 = **18116.5** (≈18,117 tokens) | 73,645 / 4 = 18,411.25, rounded to "18 400" in the plan and `NEXT_STEPS.md@e82da27:28-29`; measured 2026-08-09 | The 45,000-token figure the campaign was opened against was never the founding command's own output — it came from a session also loading a working skill (rows 570, JOURNAL 2026-08-07). The plan itself flags this as never re-approved against the corrected figure. |
| 3 | **The full test run gets cheaper, not more expensive.** Founding plan (08.08): "Первая порция особая — она удешевляет сами проверки... и каждый следующий день становится дешевле." Restated as complaint by Alexander 2026-08-07 00:17 (row 568): "The work feels slow... he suspects redundant steps." Row 617 itself: "the full run's budget rose from 800 s to 1280 s to 1410 s inside one day, and the only place that noticed is row 553." | `python3 -m pytest -q` (`.live-spec/day1-measures-2026-08-09.md`, Measure 3) | **Not re-run this pass.** A fresh run costs 20–27 minutes on this machine per the last seven recorded runs, and this brief forbids background jobs and waiting loops; running it foreground would blow past the tool's timeout. Flagging this rather than guessing a number. | 447.22 s, 2,502 tests, all green, 2026-08-09 00:3x (`.live-spec/day1-measures-2026-08-09.md`) | **Caveat on record (row 622, ARCHITECTURE.md §Quality budgets, 2026-08-13 03:03):** this number reads machine load, not campaign progress — the same tree measured 1,221.81 s and 1,605.37 s in one evening, a 31% swing with under a second of code difference. Latest recorded runs (2026-08-12 evening, `ARCHITECTURE.md`): 1,221.81 s, 1,281.39 s, 1,304.65 s, 1,605.37 s, 1,559.15 s, 1,387.88 s, 1,451.77 s. Ceiling history in the same row: 800 s → 1,280 s → 1,410 s → 1,780 s, each rise on load, not code (row 622, row 553). `tests/test_guardrails.py` alone costs 640.59 s of any run (row 553) — the one code term that actually dominates. **PROPOSAL:** until row 622 lands, watch `tests/test_guardrails.py`'s own wall-time (`/usr/bin/time -p python3 -m pytest tests/test_guardrails.py -q`) instead of the full suite — it is the term the campaign can actually move by cutting rules, and it is not noise-dominated the way the full-suite figure is. |
| 4 | **Number of blocking checks on the push chain.** Founding plan (08.08): "31 проверка перед каждой публикацией." Plan's own Numbers table: "Блокирующих проверок: 31 на старте, 30 на 10.08, 29 после D4 (11.08)." | `grep -oE -- '-- gate [a-z]{1,2}:' guardrails/pre-push \| sort -u \| wc -l` (`.live-spec/day1-measures-2026-08-09.md`, Measure 4, same method `.live-spec/day1-census-checks.md` used) | **29** | 31, 2026-08-09 00:4x (`.live-spec/day1-measures-2026-08-09.md`) | Matches the plan's own "29 after D4" line exactly. D2 (rule 30's repeal) stops new gates from being born automatically; this count is the direct proof the stop is holding. |
| 5 | **Count of rules in the shared rulebook.** Plan's Numbers table: "Правил в охвате кампании: 88; из них 35 живут в общем своде." D2's own text: "правило 30 вырезано из свода... коммит `3866a6c`." Requested explicitly as a candidate for this sweep. | `grep -cE "^[0-9]+\. \*\*" skills/live-spec-base/SKILL.md` (numbered rule entries in "## The shared rules") | **34** | 35, 2026-08-09 (`.live-spec/day1-census-rules.md`, "thirty-five rules in the body") | The file itself notes rule 30's number is retired and stays open (`skills/live-spec-base/SKILL.md:110`), so 34 live rules span numbers 1–35. One rule gone since day 1, none cut by the batch process itself yet (batches 1 and 2 shortened rules 7 and 32, they did not remove them). |
| 6 | **Bytes the campaign has written about itself, against the rulebook body it is cutting.** `NEXT_STEPS.md@e82da27:16-17` (historical, cited by the plan's Numbers table): "Text the campaign wrote about itself: about 346 000 bytes. The rulebook it is cutting holds 66 435." Row 617 flags this exact gap: no command can re-derive 346,000. | `cat .live-spec/*.md \| wc -c` for the self-written side; `wc -c skills/live-spec-base/SKILL.md` for the target body | **674271** (self-written side); **64728** (target body, `skills/live-spec-base/SKILL.md` today) | ~346,000 bytes self-written vs. 66,435 target body, dated on or before 10.08 (`NEXT_STEPS.md@e82da27:16-17`, a snapshot the live file no longer carries — `NEXT_STEPS.md` was rewritten 2026-08-10, commit `6c448a4`) | This is the exact gap row 617 names: the original 346,000 figure was never tied to a command, so `cat .live-spec/*.md \| wc -c` cannot be checked against it — it may have counted a different file set (root docs, `docs/`, or both, not just `.live-spec/`). Today's 674,271 and the historical 346,000 are not the same reach and should not be read as one trend line. **PROPOSAL:** adopt `cat .live-spec/*.md \| wc -c` as the one going-forward command for this goal, starting today's 674,271 as its new baseline, and say plainly that the pre-10.08 figure is orphaned. |
| 7 | **How far a stranger's install gets** (the founding plan's fourth measure, alongside #1–#4, though not named among row 617's explicit candidates). Day-1 measures: "219 references point at nothing, 123 distinct targets, across 33 of 66 installed files." | No single shell command — the day-1 census simulated a real install into a throwaway `$HOME` and hand-scanned every installed file for dangling references (`.live-spec/day1-census-delivery.md`, "Method" section) | **Not re-run.** Reproducing it means installing the pack into a fresh throwaway home, which is a multi-step simulation, not a one-line command, and sits outside this brief's read-only, no-new-writes scope. | 219 dangling references, 123 distinct targets, 33 of 66 files, 2026-08-09 (`.live-spec/day1-census-delivery.md`) | **PROPOSAL:** this goal needs its census method turned into a runnable script before it can sit in the status block on the same footing as #1–#6; until then it stays qualitative, re-run by a fresh worker repeating the day-1 method by hand. |

## Goals I found but could not put in the table above

- **Row 569 — "a work block is grounded in his sight before it runs"** (Alexander 2026-08-07, 00:17, 00:42, 00:46). This is one of the same night's four complaints, and it is genuinely dated and genuinely his word — but it is not a goal of the culling campaign. It was answered and landed the same day (2026-08-07), a day before the campaign's founding order (2026-08-08 22:17), via law 7 of the session-rules reminder. The culling plan's own text never adopts it as one of its measures. I left it out of the table rather than force it into a byte-or-second row it does not fit; it has no numeric target and its "done" state is a behavioral property (does every block open with a root line), not something `wc` or `grep -c` proves.
- **Row 166 — the live work board** (Alexander 2026-07-07 ~09:36, resurfaced as field evidence 2026-08-07 00:17). Same situation: real, dated, his word, but a different initiative with its own ROADMAP row and its own "Done when" line, not a stated goal of the culling campaign's plan text anywhere I found. Folding it into this page would misattribute a large unrelated feature to the culling campaign's status block.

Both of the above are complaints from the same 00:17–01:26 night that opened rows 568/569/570/166 together (JOURNAL.md 2026-08-07, 00:17–01:26). Only 568 (redundancy/slowness → row 3 above) and 570 (context size → rows 1–2 above) are actually taken up inside the culling campaign's own founding text; 569 and 166 were routed to their own rows and closed or continued on their own terms.
