# Дельта плана v3 — 11.08.2026, ~03:00

Каждая строка — один изменённый кусок плана и его корень.

- Тест партии (этап 2): точка отсчёта объёма свода 73 645 → 73 578 байт. Корень: свежая двухчасовая сверка 11.08 02:56 пересчитала объём починенной командой и получила 73 578; разница в 67 байт — предложение, вырезанное коммитом `0ef204e` (день 2, снятие проверки происхождения передач) уже после замера дня 1. Приказ Александра 08.08 22:17 держит сверку, его закон от 07.08 держит числа с проверяемым происхождением.
- Приложение, строка «Объём свода»: то же исправление того же числа по тому же корню, второе из двух мест, где точка отсчёта записана.
- Таблица «Числа», строка «Объём свода»: третье место того же числа, тот же корень — сверка 11.08 02:56; строка теперь несёт измеренное 73 578 и держит замер дня 1 как историю.

Вторая порция, ~03:10 — корень один на все куски: находка R5 (страница `.live-spec/r5-rule-prices-2026-08-11.md`) — ссылки плана на строки `NEXT_STEPS.md` устарели после перезаписи статусного блока коммитом `6c448a4`, класс «гниющий указатель». Исторические цитаты получают снимок `@e82da27`, живые правила цитируются разделом.

- Вывод п.1: `NEXT_STEPS.md:16-17` → снимок `@e82da27`.
- Вывод п.2: `NEXT_STEPS.md:25-27` → снимок `@e82da27`.
- Таблица, строка «88 правил»: `:15` → снимок.
- Таблица, строка «вырезано 0»: `:15` → снимок.
- Таблица, строка «346 000»: `:16-17` → снимок.
- Таблица, строка «18 400 токенов»: `:28-29` → снимок.
- Таблица, строка «прогон»: `:82` → раздел «Rules you must not break».
- D1 и заголовок этапа 2: `:29-32` → снимок.
- S5: `:84-86` → раздел «Rules you must not break»; строка про два списка файлов: `:81` → тот же раздел.
- Приложение: строки «прогон», «88 правил», плюс новая строка, объясняющая снимки `@e82da27` и команду их чтения.

Третья порция, 11.08 ~10:35 — корень: слова Александра 11.08 10:23, первый проход присеста.

- Этап 1, новый абзац состояния присеста: D1/D4/D8 — да, D7 — позже, D2 — согласие по сути с исполнением после понятного решения, D3/D5/D9 — открыты его вопросами, D6 — снят в прежнем виде; направление «выпиливать, а не чинить». Корень: его слова 10:23, записаны в DECISIONS.md той же минутой.
- Этап R, новая строка R7 (опись обязательной обвязки с происхождением каждого требования): корень — его слова 10:23 «надо понять все такие лишние элементы… выпиливать а не чинить»; заменяет прежний смысл D6.

Четвёртая порция, ~10:58 — корень: проверка воспроизводимости резюме перед стиранием контекста (его заказ 10:23) нашла, что однофразное решение по D2 жило только в чате.

- Блок присеста, строка D2: решение записано в сам план одной фразой (удалить правило 30 срезом с ревью; новые проверки после этого — только через правило 23 или его слово). Корень: находка проверки — чистая сессия не нашла бы, что предъявлять на «понял, делай».
