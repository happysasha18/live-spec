# Hostile review record — culling plan, 2026-08-08

Reviewer: fresh-context agent (Fable), read-only over /Users/sashaabramovich/live-spec. Subject: culling-plan-2026-08-08.md (pre-fold draft). All findings below were sustained by the orchestrating session and folded into the plan the same evening.

## BLOCKING

B1. Stage 4's green-run acceptance was self-blocking: cutting or rewriting any rule reds three test families the draft never named — 103 test files assert verbatim skill-body sentences; traceability ties spec ↔ architecture ↔ matrix ↔ tests; the freeze gate blocks push on anchor drops in the three frozen docs. Fold: new stage 4d (per-rule five-surface sweep + baseline re-freeze), calendar recheck moved to verdict day.

B2. Verdict granularity mismatch: census 1b was per file while stage 4 cuts individual rules, so rule fates never received his word. Fold: 1b and the verdict are per rule.

B3. Stage 2's page format contradicted shipped law (far tier carries no trigger, SPEC INV-222; queue-row lint guards ROADMAP format) and the page's relation to ROADMAP was undefined. Fold: the page is a separate view built from ROADMAP; tiers follow the existing law (deferred = trigger, far = none); his pencil lands back into row states mechanically.

## MAJOR

M1. 4a/4c "disjoint files" was false — test_guardrails.py holds many gates' tests in one file. Fold: arms serialized (4b+4d → 4a → 4c), briefs name file lists (execution rule 7).
M2. The one-month catch window was a self-invented numeric standard, and rules don't "catch defects". Fold: last-catch dates printed, he draws the line; rules judged by what they protect.
M3. The "move piece into skill" disposition had no executing stage, and 1c goes stale after 4b. Fold: moves execute in 4b; mechanical 1c re-count after the cut.
M4. Installed copies (~/.claude/skills) and the two host projects were unaddressed during a week of cuts. Fold: copies frozen until the cut ends, one sync in 5a with both hosts' check runs.
M5. The plan cured the stock and left the flow — the moratorium expired with no successor regime. Fold: the standing regime (a new check enters only in place of an old one) is a named decision item at stage 6.
M6. 5b's boundary rested on the worker's self-report. Fold: the stranger run executes under a home directory where the pack tree physically does not exist.
M7. PRODUCT_SPEC.md (704KB) had no census and no verdict. Fold: spec blocks enter 1b coarsely, tied to the unit they formalize; unbound blocks get their own verdict mark; a full spec pass is a stage-6 candidate.

## MINOR

m1. Stage 0 accepted on the log's last line — the tail-reading anti-pattern. Fold: exit code zero plus persisted log.
m2. current-order.md had no reader. Fold: NEXT_STEPS.md gains a pointer line (edit of an existing file, no new hook).
m3. Discoveries "wait" with no owner. Fold: discoveries land only as queue rows; nothing enters work before his closing read.
m4. "Read in one sitting" named no reader. Fold: a fresh clean-context agent's timed read published as a fact at stage 4, plus his own sitting at stage 6.

Dismissed by the reviewer itself (checked and found sound): stage 1/2 parallelism; the census completeness check (units enumerable by command); stage 4a's refusal of a target gate count; calendar-slip handling.

---

# Round 2 — structure review of the second edition (iterations + pulse)

Fresh-context Fable agent, read-only. Verdict: 1 BLOCKING, 4 MAJOR, 4 MINOR — all folded into the third edition.

- B1. Per-batch green runs collided with frozen installed copies: an existing gate diffs skills/ against ~/.claude/skills and reds on drift, so the first skill rewrite made every "green run" unreachable. Fold: touched skills/hooks sync at the end of the same iteration; the two host projects' check runs stay one final pass.
- M1. The pilot's per-unit price (gates) does not transfer to prose rules whose cost is verbatim-needle re-sewing. Fold: the pilot adds two-three real rules from the heaviest skill body.
- M2. The fixed five sweep surfaces were wrong for gates (CI yml, red-proof registry, both pre-push copies, gate README, tree counts). Fold: surfaces come from each census row.
- M3. Delivery work of iteration 2 was partially invalidated by later batches with only a re-count scheduled. Fold: delivery is a sweep surface of every batch.
- M4. The run-cheapening batch was test-machinery smuggled under a rules verdict. Fold: it is a named batch whose digest states what scoped runs skip.
- m1. The stop rule was vacuous (any measure improvable by one more cut) and misfired at the endgame. Fold: each iteration pre-declares the measure it must move; stop judges the declared one.
- m2. Frozen wording vs calendar recheck. Fold: mechanics/priorities/rules frozen; dates are estimates, moves named day-of.
- m3. His total adjudication volume untotaled. Fold: total verdict-row count published on prep day; superseded further by the owner's 21:55 word — verdicts are the orchestrator's, he rules only on taste/policy rows.
- m4. "27 gates" wrong against the tree (31 lettered). Fold: counts come from the census, no number frozen.

# Round 3 — drift review (why will THIS plan drift like the previous ones)

Fresh-context Fable agent, read-only, primed with the journal's drift record. Verdict: the primary drift vector is plan-death-by-replacement via mid-plan orders, and most bindings were session self-grading. Folds:

- One cut = one commit carrying its verdict-row ID; the verdict page commits first; resumability is the diff between verdict marks and git log; a mid-flight session death loses at most one unit.
- The pulse and the marks-vs-commits check move to a fresh acceptance agent; the seat never grades itself.
- The "blocking red" exception narrowed: a batch repairs red only on its own surfaces; foreign red goes to the evening digest by name.
- A rule binding the owner's own channel during the culling (messages that are neither veto, taste mark, nor "стоп просев" get one queue line and a one-line reply) — reserved as a decision only he can speak, listed at approval.

# Round 4 — final pass on the third edition

Fresh-context Fable agent, read-only. Verdict: approvable after four MAJOR one-line repairs; no BLOCKING; all five priority ranks HOLD under attack; six fold spot-checks landed.

- MAJOR-1: rounds 2-3 records were missing beside the plan — this appendix is the cure.
- MAJOR-2: the rules census now also collects each unit's surfaces (test files asserting its text, matrix rows, spec anchors).
- MAJOR-3: iteration exit is green modulo foreign red deferred and named by the digest.
- MAJOR-4: the four pulse reproduction commands are written into docs/plans/current-order.md on prep day.
- MINOR: batch size derives from the pilot price (a semantic group may span batches); the closing page shows the queue with his pencil if it happened; the "scoped runs" wording de-jargonized.
