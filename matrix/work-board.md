### [node: work-board [target]]

The board itself is built (q-816, 2026-09-06): `scripts/render-board.sh` renders it, and `tests/test_work_board.py` carries one test per built row below, each red-proved against the renderer as it stood before that landing. The statement-validation check is built beside it the same day: `scripts/task-admission.py` derives a statement at admission, puts it through a mechanical floor and a clean-context reader before any take-up, freezes its wording at take-up, and writes the given-vs-actual and the lane divergence into the delivery trail — `tests/test_statement_validation.py` carries one test per row from M-531 to M-535, each red-proved against `scripts/task-admission.py` as it stood at 4a1579d0. q-816's acceptance is the whole of Requirement 309 minus the retired heartbeat, never five rendering criteria; the earlier reading here — that no artifact in this tree records a statement's estimate — is answered rather than waited on, since the estimate now stands in the task's own row entry as a range with the basis it rests on, and where no comparable closed row gives one the basis says so in those words. The work board's own source file is `PLAN.md`: the row entry holds the statement and its validation record, keyed by the row's id, and the queue row keeps its own cells. The rows read the recorded state rather than any named column, so the stage ladder's own settling moves no row's wording.

The auto-refresh heartbeat is retired on the owner's 2026-09-02 12:46 word (`.live-spec/turnkey-contract-composed.md:304`). NO ROW here retires whole for it: M-540 and M-542 each carried the heartbeat as one clause beside a clause that stands, and each row's own text already records which half went. Their tests prove the standing half and prove the retired half as an ABSENCE — no timer, no meta refresh, no poll on the page.

| ID | Fact (from spec) | Test level | Owning test | Status |
|---|---|---|---|---|
| M-519 | The work board is the rendered status page the pack already promises, one rendered surface under one name, held as one source file in the host's tree and published from that file at one stable link the person opens from any device; never a second rendered status surface standing under a second name, and never a board held anywhere but that one source file [INV-308, INV-71, INV-67] | string | `test_m519_one_surface_one_source_file_one_stable_link` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing); the stable-link half by `tests/test_board_publish.py` — the Pages workflow renders with this repository's own `scripts/render-board.sh` and publishes that one file at `https://happysasha18.github.io/live-spec/board.html` (red-proved 2026-09-06 against HEAD 4a1579d0, which carried no workflow), and the published bytes themselves by `tests/test_work_board.py::TestFreshClone`, which renders a real `git clone` of this tree in records-only mode — the shape of the runner's own checkout (red-proved 2026-09-06 against HEAD de542335, whose page named the checkout's path and stamped every row with the checkout's minute) | *built* |
| M-520 | One work board stands per host project, every row naming the session that wrote it, and a session's write passes the concurrent-edit fence like any shared-document write, a blocked write re-reading the board and re-applying its own row; never another project's work merged into a host's board, and never a board write that skips the fence [INV-308, INV-11] | string | `test_m520_one_board_per_project_every_row_names_its_session` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-521 | The board stands whether or not anyone asks, leads with the one-line identifier every opened artifact carries, and is registered in the host's surface registry `SURFACES.md` — with the marker text the completeness check reads and the spec anchors it answers to — before it renders; never a board rendered ahead of its registry row, and never a page opened without its identifying line [INV-308, E-10, INV-51] | string | `test_m521_identifier_line_and_registry_before_it_renders` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-522 | The board's form follows the frozen norm `docs/norms/work-board.html`, cited by the requirement's own norm pointer from the approval on, and shows rows in feature language with the work in hand and a timestamped feed; never a form drifting from the frozen norm without a recorded landing, and never a second copy of the approved form standing beside it [INV-308, INV-43] | string | `test_m522_form_follows_the_frozen_norm_and_carries_a_timestamped_feed` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-523 | The whole queue stands on the board in columns, one column per recorded state, and every row the page shows stands in exactly one column, an open row's column read off the status its queue row records; never a queue row dropped from the page in silence, and never a row standing in two columns at once [INV-308, INV-277] | browser-computed | `test_m523_every_row_stands_in_exactly_one_column` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-524 | The far tier stands down by name with its rows opening only on the person's request, the *queued* rows below the runnable head collapse into a stated count that opens on the person's act, and the *deferred* rows show as a stated count alone with each revisit trigger behind an expand; never a collapsed row dropped in silence, and never the far tier's rows standing open unasked [INV-308, INV-222, INV-223] | DOM-text | `test_m524_far_tier_named_head_collapses_deferred_counted` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-525 | The in-work column splits into lanes, one for each build lane the lane cap allows, a lane holding no row reading as free, and a parked row stands in the in-work column marked parked and naming the row that preempted it, returning to the work in hand once that row clears; never a lane count diverging from the cap, and never a parked row cleared off the board [INV-308, T-9, T-18] | browser-computed | `test_m525_lanes_match_the_cap_free_lanes_read_free_parked_row_kept` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing), beside `TestFreshClone::test_no_lane_is_judged_where_the_heartbeats_and_the_cap_do_not_live` — off the machine that holds the work neither the heartbeats nor the cap exist, so the page names no lane rather than printing a figure (red-proved 2026-09-06 against HEAD de542335, which published "1 of 3 lanes busy" off a checkout's own mtimes) | *built* |
| M-526 | A card reads as a task at a glance: the echo-name first as the recognition hook, then the description of the behaviour, then the chips, every other detail held behind the card in a details layer, with a placement tag chip on every row read from the queue row's own map and footprint notes and a named link to the part of the product spec the task changes; never technical detail standing on the card's face, and never a board row without its placement tag and its spec link [INV-308, INV-28, INV-37, INV-128] | DOM-text | `test_m526_card_reads_as_a_task_at_a_glance` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-527 | An in-work row shows the steps of its own statement's plan with a mark beside the one pipeline stage the row now stands at, and names its branch and worktree in the row's details, both read from the lane's own claim commit and checkpoint; never a stage mark composed from narration rather than read from the record, and never a branch or worktree name typed onto the row rather than read from the lane [INV-308, E-34, INV-69] | DOM-text | `test_m527_in_work_row_shows_its_plan_its_stage_and_its_lane` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-528 | The board's waiting region renders the waiting board `WAITING.md` and keeps no list of its own, so one clearing rule and one gate hold every waiting item, and a row whose progress waits on a waiting-board item is marked waiting while standing in the column its own state names; never a second waiting list kept on the board, and never a waiting row moved out of the column its state names [INV-308, INV-206] | DOM-text | `test_m528_waiting_region_renders_the_waiting_board` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-529 | Each running step names its worker by a fixed craft name and icon with a muted tier note beside it, the seat naming that craft in the worker's brief at spawn and the movement's checkpoint carrying the craft, its icon and the logged tier while the step runs, a mid-flight tier change updating it, and the board reading a running step's worker there; never a craft guessed for a step whose record names none, and never which worker runs which task left unreadable across the in-work column [INV-308, INV-33, INV-69] | DOM-text | `test_m529_each_running_step_names_its_worker_or_says_it_is_unnamed` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-530 | The fixed craft set and its icons live in the board's own source file as their one home, the names read as display names of the pipeline's craft standards with the skill names kept internal; never a second home for the craft set, and never an internal skill name shown on a card [INV-308, INV-137] | string | `test_m530_the_craft_set_has_one_home_and_no_skill_name_reaches_a_card` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-531 | Every task carries a statement holding its echo-name, its description, its plan and its time estimate, kept with its validation record in the work board's own source file keyed by the queue row's id, the queue row keeping its five cells; never a statement or a validation record held outside that source file, and never a queue row grown a sixth cell to carry one [INV-309, INV-277] | string | `test_m531_a_statement_holds_its_four_fields_in_the_rows_own_entry` (red-proved 2026-09-06 against `scripts/task-admission.py` as it stood at 4a1579d0, which carried no statement half at all: the name the test calls did not exist) — the statement stands in the row's own `PLAN.md` entry, the checkpoint carries no copy of it, and the row keeps its five cells | *built* |
| M-532 | No task enters work before its statement passes validation — the mechanical floor of echo-name, description, plan and estimate each present with the register check clean, then a clean-context reader carrying no project vocabulary who answers what is to be done, why, and how long from the statement alone and later places the echo-name on its own — a failed statement rewritten and validated again, its task staying out of work until it passes; never a task taken up on a statement that has not passed, and never a reader carrying project vocabulary standing in for the clean-context one [INV-309] | string | `test_m532_no_task_enters_work_before_its_statement_passes_validation` (red-proved 2026-09-06 against `scripts/task-admission.py` as it stood at 4a1579d0, which carried no statement half at all: the name the test calls did not exist), beside `test_m532_a_statement_that_fails_the_floor_is_rewritten_and_stays_out_of_work` and the three reader refusals — a record missing an answer, a question the reader cannot answer, and a reader that places another name | *built* |
| M-533 | A passed validation is read as approval and writes the task's queue row status to *ready*, dated like every other status, the gate-and-reader road standing as policy in `DECISIONS.md` with the person's word on a single task as the exception; never a per-statement route to the person standing as the road, and never a status written without its date [INV-309, INV-277] | string | `test_m533_a_passed_validation_writes_the_dated_ready_state` (red-proved 2026-09-06 against `scripts/task-admission.py` as it stood at 4a1579d0, which carried no statement half at all: the name the test calls did not exist) | *built* |
| M-534 | A passed statement's wording freezes and is spoken letter for letter at take-up, along the way and at the close, the person's re-wording taken and frozen anew, and a task's estimate or plan revised only before take-up with validation run again and the statement frozen anew, an estimate held as it stands once the task is taken up and any overrun stated plainly at the close; never a frozen statement paraphrased when the task is spoken, and never an estimate revised after take-up [INV-309, INV-93] | string | `test_m534_the_wording_freezes_at_take_up_and_a_later_change_is_refused` (red-proved 2026-09-06 against `scripts/task-admission.py` as it stood at 4a1579d0, which carried no statement half at all: the name the test calls did not exist), beside `test_m534_a_change_before_take_up_sends_the_statement_back_through_validation` | *built* |
| M-535 | The statement's plan lists the steps ahead in the order they run, its parallel mark read as the plan's expectation with the take-up lane decision deciding what actually runs together and a divergence recorded plainly in the delivery report's trail, and the steps running together inside one task are bounded by the same lane cap that bounds build lanes; never a plan's parallel mark overriding the lane decision, and never a divergence between the two left off the trail [INV-309, INV-49, T-18] | string | `test_m535_the_plans_expected_parallel_steps_meet_the_lane_decision_at_take_up` (red-proved 2026-09-06 against `scripts/task-admission.py` as it stood at 4a1579d0, which carried no statement half at all: the name the test calls did not exist), beside `test_m535_the_divergence_and_the_given_against_the_actual_land_in_the_trail`, `test_a_plan_whose_steps_run_out_of_order_fails_the_floor` and `test_a_take_up_past_the_lane_cap_is_refused` — the cap half of this fact, red-proved 2026-09-06 against `scripts/task-admission.py` at 7993fa9b, which read no cap at all and took `hold --lanes 9` under a cap of three (the adversarial read of 2026-09-06) | *built* |
| M-536 | The statement's estimate is the one estimate every other surface cites, written onto the board row when the task is taken up, the actual standing beside it on the same row at the close, and the landing's own settling of estimate against actual extended onto the board; never a second estimate cited from anywhere but the statement, and never a closed row showing an estimate with no actual beside it [INV-310, INV-93] | DOM-text | `test_m536_the_row_carries_the_time_it_was_given_beside_the_time_it_took` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-537 | Every closed task keeps its row on the board rather than being cleared, the done column read from the month's archive file under `docs/queue-archive/` with the current month standing by default and an older month opening on the person's ask, each closed row showing its own terminal state — landed, declined, superseded, or decided — and tagged with its door read from the archived row's intake note; never a closed row cleared off the board, and never a done column built from anything but the archive [INV-311, INV-276, INV-134] | string | `test_m537_closed_rows_are_kept_and_the_done_column_reads_the_archive` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-538 | The delivery report carries a trail over the plan's steps — each step's outcome, the worker tier or role that ran it, and the step's share of the task's time — and a closed row's step trail is drawn from that report; never a step trail composed on the board itself, and never a closed step standing with no outcome named [INV-311, INV-103] | DOM-text | `test_m538_a_closed_rows_step_trail_is_drawn_from_the_record` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-539 | The board updates at every pipeline stage change, at take-up, at a worker's spawn and finish, at a landing, and at every state that waits on the person, and the board file's update rides inside the landing's own commit; never a stage change that leaves the page standing stale, and never a board update landing outside the commit of the landing it records [INV-312, INV-71] | string | `test_m539_the_page_never_stands_stale_against_the_record_it_reports` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-540 | A board update delays no stage it touches; never a stage held back by its own board update. The periodic auto-refresh heartbeat this fact once also carried retired 2026-09-03 on the owner's 2026-09-02 12:46 word (`.live-spec/turnkey-contract-composed.md:304`) [INV-312] | string | `test_m540_a_board_update_delays_no_stage` — the standing clause timed, the retired heartbeat proved as an absence (no timer, no meta refresh, no poll on the page) (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-541 | The board lays out in one column on a narrow screen with the work in hand at the top, every control reachable by touch and by keyboard, and the page holds the contrast the pack's pages hold; never a control reachable only by hover, and never a narrow screen pushing the work in hand below what a reader sees first [INV-313] | browser-computed | `test_m541_one_column_on_a_narrow_screen_with_the_work_in_hand_on_top` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-542 | An empty board says so and shows the queue's head in its place, and every board carries the stamp of the time it last updated so a reader judges its freshness; never an empty board reading the same as a broken one, and never a page standing with no freshness stamp for a reader to judge. The page's own five-second re-read this fact once also carried retired 2026-09-03 on the owner's 2026-09-02 12:46 word (`.live-spec/turnkey-contract-composed.md:304`) [INV-313] | DOM-text | `test_m542_an_empty_board_says_so_and_every_board_carries_its_stamp` — the standing clause proved, the retired five-second re-read proved as an absence by `test_m540_a_board_update_delays_no_stage` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing), the stamp's zone by `TestFreshClone::test_the_build_stamp_names_the_zone_it_was_read_in` — a bare "built 10:29" off a UTC runner reads as four hours stale to a reader on another clock (red-proved 2026-09-06 against HEAD de542335) | *built* |
| M-543 | The board writes no history the journal already owns and merges no other project's work into a host's board, the chat's departures board keeping its own report duty inside every status report and on ask, the narration whole, and the live status line unreduced; never a board row standing in for a beat the session owes in the chat, and never the board's arrival reducing a chat duty [INV-308, INV-27, INV-35, INV-71] | string | `test_m543_the_board_writes_no_history_and_takes_over_no_chat_duty` (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-544 | The board counts as working when the person answers four questions from the page alone over one real working stretch — what is now being done, who runs what, what was done, and how long each took against its estimate; never a question of the four the page leaves the reader to ask in chat, and never the board counted as working off a green suite with no such stretch read [INV-308] | DOM-text | the person's read over one real working stretch — human-verified, the honest level here weaker than a machine gate, flagged; its machine half is `test_m544_the_four_questions_are_answered_from_the_page_alone`, which proves each of the four questions has an answer standing on the page (red-proved 2026-09-06 against the renderer as it stood before q-816's landing) | *built* |
| M-649 | An activity that carries value only alongside others — writing the tests, say — is held outside a plan's deliverables, the statement's mechanical floor naming such a step and keeping the task out of work until the plan is rewritten; never a plan whose deliverable shows value only beside another one, and never a floor that passes such a plan in silence [INV-309] | string | `test_m649_an_activity_that_only_carries_value_alongside_others_is_not_a_deliverable` (tests/test_statement_validation.py; red-proved 2026-09-06 against `scripts/task-admission.py` at e65ae0ee, whose floor read the criterion nowhere), beside `test_m649_a_plan_whose_every_deliverable_stands_on_its_own_passes_the_floor` | *built* |
| M-650 | A plan's deliverables stay a handful, the most one plan holds standing at five and read from the criterion's own sub-item rather than chosen here, a plan past it failing the floor and staying out of work; never a deliverable count invented by the code, and never a plan of any length admitted because nothing counted its steps [INV-309] | string | `test_m650_a_plans_deliverables_stay_a_handful` (tests/test_statement_validation.py; red-proved 2026-09-06 against `scripts/task-admission.py` at e65ae0ee, which counted no steps at all — the six-step plan passed the floor) | *built* |
