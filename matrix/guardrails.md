### [node: guardrails [target]]

| ID | Fact (from spec) | Test level | Owning test | Status |
|---|---|---|---|---|
| M-168 | Pack-list parity: every list of the pack's skills (the spec working-skills sentence · the skills' closing lists · README table) names the same complete set, checked by a pure function over the real files; never a list missing a skill (the checker proven to catch the historic four-skill communicator footer) [INV-66] | string | `test_real_repo_lists_complete` + `test_stripped_copy_goes_red` (the never side runs the checker on the pre-fix footer) | *built* |
| M-082 | Architecture pins are names first, the :line a cache (E-14); the drift gate (g) turns file-missing/beyond-EOF red, and a label the pinned line does not carry within ±2 lines is red as well, with no strict flag to opt into. A label is judged by its naming words: document furniture — rule, step, gate, line, table, check, node, file, home, section, part, item, row, list, name, page, doc, entry — does not count while the label carries a word that names something, and a label carrying only furniture ("gates", "the rules") is proved by that furniture. A `:1` file-level pin is proved against the whole file, an unlabelled pin by the file's existence, and the green line names the files read, the unlabelled pins, named, and counts that close: line + file-level + unlabelled = pins checked, a machine-local pin absent on the host being skipped and named outside the count; never a rotten pin trusted silently [E-6] | string | `test_real_repo_passes` (gate g), `test_missing_file_fails`, `test_label_on_a_neighbouring_rules_line_reds`, `test_a_fabricated_label_on_the_right_rule_reds`, `test_a_pin_on_its_own_rules_line_passes`, `test_a_label_word_seventy_lines_away_reds`, `test_a_label_the_line_does_not_carry_reds_with_no_strict_flag`, `test_a_file_level_pin_is_proved_against_the_whole_file`, `test_a_sub_item_label_is_judged_by_its_own_words`, `test_a_label_of_generic_words_alone_is_proved_by_them`, `test_green_line_names_the_files_it_read`, `test_the_green_line_accounts_for_every_pin` | *built* |
| M-083 | The surface registry's preferred form is executable (a declared map inside a completeness-gate test, red both directions); the .md is the doc-only fallback; adoption never asks an executable registry to step back into a document [E-10] | string | clause presence: `test_spec_states_registry_and_pins`; machine rides row 55 | *built* |
| M-081 | Every shipped skill loads: frontmatter parses, name = folder, description + version present, a "Work that belongs elsewhere" section scopes it; a broken skill turns the push gate red; never an unloadable or unscoped skill shipped [E-6] | string | `test_real_repo_passes` (gate f), `test_broken_skill_fails`, `test_missing_skills_dir_fails` | *built* |
| M-060 | The guardrails run on the pre-push hook: completeness, tests-present, behaviour-traces-to-spec, declared-scope diff; never a red push [E-6] | string | first slice built (pack gates: prover record · green suite · anchor ownership · matrix coverage — `guardrails/pre-push` + `test_guardrails.py`); the four host-facing checks await the surface registry + snapshot (rows 55+) | *todo* |
| M-226 | The four host checks' shipping contract (INV-97): generic runnable code under scaffold/guardrails/, one host config (paths, globs, registry, render command), gate-contract output, honest failure (missing config = red attach-me; dead path = red; waivers declared in config), adopt-walk attachment with the pack repo as first host, measured acceptance (~15-min config-only attach, red-first on a planted defect per check); never a silent pass on an undeclared gap, never the contract's wording forked from the code's behaviour, never the pack's own attach leaving surface_discovery_pattern null (the external push-probe's hole, 2026-07-10), never the attach walk hiding the config step (it opens with it, the pre-config red named as by-design, the no-config fix line pointing at it — row 251) [INV-97] | string | clause presence: `test_contract_in_the_spec` + `test_waivers_are_declared` (red proven against the pre-delta tree, 2026-07-10); function rows M-228..M-231; own-attach lock: `test_own_attach_arms_the_discovery_pattern` (red proven on the nulled config, 2026-07-11); walk opening: `test_walk_opens_with_the_config_step` (red proven against the pre-edit walk, 2026-07-11) | *built* |
| M-228 | check_completeness.py: every registered surface's needle present and non-empty in the rendered content; with a discovery pattern, every discovered id registered (self-closing); never a registered-but-absent, registered-but-empty, or rendered-but-unregistered surface green [INV-97] | string | `test_completeness_green` + `test_registered_but_absent` + `test_registered_but_empty` + `test_rendered_but_unregistered` (red proven before the code existed, 2026-07-10, worker checkpoint) | *built* |
| M-229 | check_tests_present.py: a diff touching user-facing globs with no test change is red, naming the offenders; a file identical after normalizing version strings (the stamp script's mechanical output, held by the stamped-copy guard test) stands outside the subject; never a user-facing change pushed testless, and never a stamp-plus-real-edit file passed as a stamp [INV-97] | string | `test_change_without_test_red` + `test_change_with_test_green` + `test_non_user_facing_change_green` (red-first, same run) + `test_version_stamp_only_change_green` + `test_stamp_plus_real_edit_still_red` (red-first 2026-07-23) | *built* |
| M-230 | check_traces_to_spec.py: every registry surface cites >=1 anchor and every cited anchor lives in the spec; never an unanchored surface or a dead anchor green [INV-97] | string | `test_traces_green` + `test_unanchored_surface` + `test_dead_anchor` (red-first, same run) | *built* |
| M-231 | check_conflicts.py: duplicate anchors, invariant-without-matrix-row, resolved-but-live markers, surface-named-twice all red; honest ladder shared by all four (no-config = attach-me red, dead path = red, declared waiver visibly exits 0); never a silent pass [INV-97] | string | `test_conflicts_green` + `test_duplicate_anchor` + `test_invariant_without_row` + `test_resolved_but_live` + `test_surface_named_twice` + `test_missing_config_is_red_with_attach_me_line` + `test_dead_path_is_red` + `test_waived_check_is_visible_and_green` (red-first, same run) | *built* |
| M-061 | The surface registry is self-closing: a rendered-but-unregistered surface is red; never a trusted hand-list [E-10] | string | rides row 55 (registry executable form travels with the snapshot/guardrails family; the stale row-3 cite caught by row 64's mechanization — row 3 landed its pack slice and archived) | *todo* |
| M-062 | A host may mirror the same checks in CI — one source of truth, CI never redefines them [M-5] | string | lands at row 14 | *todo* |
| M-070 | The prototype fence is mechanical: a prod file referencing anything inside a prototype home turns the push gate red; never a green push with such a reference [INV-17] | string | `test_prod_reference_fails`, `test_real_repo_passes`, `test_narrative_mention_passes` | *built* |
| M-142 | The push gate scales to the diff's reach: a declared reach map (guardrails/check-push-reach.sh) decides gate b's scope mechanically from the diff's file list — prose-only diff (README/OVERVIEW/MIGRATION/LICENSE/docs research-reports-audit-decisions) stands the suite down by name, an infra-class diff rides the scoped middle road (M-344, row 362), any tested document (the spec, matrix, architecture, queue, skills — or anything new/unmapped) runs it whole, empty diff and missing base fall to full by construction; the cheap gates never scope; never a fast path for a tested document, never the script deciding by judgment [INV-45] | string | `TestGateReachMap` (the script RUN by deed on fixture file-lists — red proven bare) + `test_push_gate_reach_law` | *built* |
| M-344 | The reach map's scoped middle road (INV-45, row 362): a diff confined to the infra classes (guardrails/, scaffold/guardrails/, templates/, scripts/, hooks/, adopt/, guardrails.config.json, tests/test_*.py) exits scoped and lists the test files that name its changed files (one referrer level deep) plus the traceability net, the picked set and its reason logged; a changed infra file that no test names falls to full, a mixed diff falls to full; never a scoped run dropping the traceability net, never a file outside the declared classes riding the scoped road [INV-45] | string | `test_scoped_reach_guardrails_diff_exits_scoped` + `test_scoped_reach_unnamed_file_falls_full` + `test_scoped_reach_mixed_diff_falls_full` (red proven against HEAD — the script has no scoped verdict) | *built* |
| M-345 | The gate-machinery meta-test rides the same reach (INV-45, row 362): the suite-in-suite scratch runs (TestGateB_Tests) fire when the diff against the push base touches a gate-machinery file (guardrails/, scaffold/guardrails/, guardrails.config.json, .github/workflows/, tests/test_guardrails.py) and skip under their own named reason otherwise; an empty or unreadable diff runs them, conservative by construction; never the meta-test silently skipped on a gate-machinery diff, never a skip without its named reason [INV-45] | string | `test_meta_reach_fires_on_gate_machinery_diff` + `test_meta_reach_skips_off_class_with_named_reason` + `test_meta_reach_conservative_on_empty_diff` (red proven against HEAD — no reach classifier in the meta-test) | *built* |
| M-405 | The reach map's directory classes are host config (INV-224, ROADMAP 380): `guardrails/check-push-reach.sh` reads its infra directories, prose files, and referrer directories from `guardrails.config.json` under `reach_classes`, the committed defaults reproducing today's verdicts (a prose-only diff stands the suite down, a lone infra file scopes, an unmapped file falls to full); a fixture config that reclassifies a directory (`scripts/` classed as the product engine rather than helper tools) flips the reach verdict, proving the script reads the config and not a body constant; the conservative floor holds, a config naming no classes leaving every file unclassified so the whole diff falls to full, never a false-green scope; the hygiene net reads the referrer directories from the config's one home; never a class read from a second copy in the script body, never a reclassified directory that leaves the verdict unchanged, never a missing config that scopes instead of falling to full [INV-224] | string | `test_reach_reads_classes_from_config_flips_verdict` + `test_reach_default_config_reproduces_todays_verdicts` (red proven against HEAD — the script reads body constants, so the fixture config changes nothing) | *built* |
| M-346 | The suite wall-time budget's mechanical net (INV-41, INV-164, row 361). Retired: the watcher `check-suite-budget.sh` and its call site in `check-tests.sh` retired, reading the machine's load more than the code; the figure lives on in ARCHITECTURE.md's own budget table as a snapshot, watched by eye. Never re-armed without a fresh decision. [INV-45] | string | retired, no owning test | *retired* |
| M-348 | The scoped road's by-name discovery is guarded by a suite-hygiene net (INV-45, defends INV-40's reach-identity claim, row 366): a test that reads an infra directory (guardrails/, scaffold/guardrails/, templates/, scripts/, hooks/, adopt/) by os.walk/glob/Path.glob/rglob/listdir/iterdir/scandir rather than naming its files' basenames is invisible to check-push-reach.sh's basename grep, so every such enumerating test must appear in the pinned always_scoped block the script and the net read from one home (test_traceability.py pinned inside the block as the integrity rider); a test enumerating only non-infra dirs (skills/, docs/) stays free; always_scoped carries the integrity rider and stands open for the first enumerating-infra test. Never an enumerating-infra test outside the scoped set, never a second copy of the infra-dir list. [INV-45] | string | `test_enumerating_infra_tests_are_pinned` + `test_synthetic_enumerating_infra_test_reds_unpinned` (red proven against HEAD — the script carried no ALWAYS_SCOPED block, docs/prover/red-proof-2026-07-16-row366.txt) | *built* |
| M-466 | A deletion-only push carries no content (INV-290, row 502, history: docs/matrix-notes/guardrails.md#m-466): `guardrails/check-deletion-only-push.sh` reads the ref-update lines git feeds the pre-push hook's stdin (`<local ref> <local sha> <remote ref> <remote sha>`, verified against a real `git push --delete` run the same day) and reports deletion-only when every line's local sha is the all-zero id; `guardrails/pre-push` reads stdin once at the very top, before any lettered gate, and on a deletion-only read stands the whole chain down with one line naming the stand-down and its reason, deriving no reach and running no suite; a mixed push (any line carrying a real local sha) runs the chain exactly as an ordinary push; no ref-update lines read at all (a manual, tty-less invocation) is conservative and also runs the chain; never a gate letter for this check — a triage read ahead of the chain, the same placement `check-push-reach.sh` already holds inside gate b [INV-290] | string | `tests/test_deletion_only_push.py` — the checker in isolation (`test_all_deletion_lines_exit_0` + `test_all_content_lines_exit_1` + `test_mixed_lines_exit_1` + `test_no_lines_exit_1_conservative` + `test_blank_lines_between_real_lines_are_skipped` + `test_reads_real_stdin_when_env_var_unset`) plus `guardrails/pre-push` end to end (`test_deletion_only_push_finishes_in_seconds_with_a_named_standdown` + `test_content_push_falls_through_to_the_ordinary_chain` + `test_manual_invocation_with_no_stdin_is_unaffected`), red proven against the pre-edit hook (docs/prover/red-proof-2026-07-27-row502.txt: fed the same deletion-only stdin, the pre-edit hook printed its ordinary banner and gate a, then fell toward the full suite, timing out at the harness's bounded 15s) | *built* |
| M-145 | Gate hygiene: every blocking gate on red emits one typed failure line {severity, code, message, fix} beside its human lines; every check declares blocking or advisory (advisory never flips the exit); artifact-rebuilding scripts validate all outputs before writing any; home guardrails README, applied by deed to ≥1 shipped gate (the prototype fence); the reach decider exempt by name (a deliberate verdict); never a blocking red without its typed line on the converted gates [INV-47] | string | `TestGateHygieneContract` (`test_readme_states_contract` + `test_prototype_fence_emits_typed_failure` — the converted gate's real FAIL output parsed as JSON) | *built* |
| M-154 | The CI mirror is live and honest: `.github/workflows/gates.yml` runs the same gate scripts as the local pre-push (a second net; full set — the reach map stays a local optimization; fetch-depth 0 + author-day TZ so gate (a) reads real history and the real day), including the same test runner — both sides run `python3 -m pytest`, so the local net can never under-collect what CI runs (history: docs/matrix-notes/guardrails.md#m-154); the guardrails README carries the host guidance (copy, swap the test command on both sides in lockstep, never redefine a check); never CI redefining a check, never the local gate on a weaker runner than CI, never the second net scoped by reach, never a false red on a machine-local pin the running home directory lacks (noted + skipped wherever the file is absent; a present file is checked everywhere, so drift still reds) [M-5] | string | `TestCIMirror` (`test_workflow_ships_and_mirrors_the_gates` + `test_readme_carries_the_mirror_guidance` + `test_local_gate_uses_the_same_runner_as_ci` + `test_machine_local_pins_skip_in_ci_only`, red proven bare + by the first live run) + the real run watched on the landing push | *built* |
| M-106 | Time is read off the clock: no repo file name, journal entry heading, or ledger date sits later than the current clock — a future-dated stamp turns the suite (and so the push walk) red; prose quoting a past incident's wrong date stays legal; never an invented tomorrow shipped [INV-24] | string | `test_no_future_dated_stamps` (red proven by deed on a synthetic future-named file) | *built* |
| M-110 | INV-24's second arm at pre-commit: an added line carrying the adjacent stamp shape (today's date [~]time) with a time later than the commit moment goes red (commit clock the reference; check_today/check_now only for tests); a past time today, any time on another day's date, and a line mixing today's date with quoted other-moment times (F9, the fence's own first live run) all stay green; never a same-day future stamp reaching a commit, never the fence blocking a quoted past [INV-24] | string | `test_future_time_today_goes_red` (with `test_past_time_today_stays_green` + `test_other_day_time_stays_green` — the script run by deed on a staged synthetic repo) | *built* |
| M-123 | The chat face of the clock law — no fence can reach chat, so the arm is stated as law where the exchange shapes live (the sentence is carried by communicator rule 7): a human-facing timestamp — the [HH:MM] a reply leads with, any moment spoken to the human — is read off the clock at write time, never continued or extrapolated from an earlier stamp; quoting a past moment's recorded time stays legal; never an extrapolated stamp presented as the current time [INV-24] | string | `test_chat_timestamp_at_write_time` | *built* |
| M-132 | The chat clock's mechanical hand: `hooks/clock-hook.sh` exists, is executable, and when run emits the current machine time (HH:MM within a minute of now, plus today's date) together with the read-off-this-clock instruction — the output names "[HH:MM]" and says "never a continued or extrapolated stamp"; installed per machine as a prompt hook (a copy under ~/.claude/hooks + a UserPromptSubmit entry in the host's settings); never an invented or cached time in its output, never a silent install on a machine the human didn't put it on [INV-24] | string | `test_output_carries_current_machine_time` (with `test_output_carries_the_law` — the script run by deed; red-proven on the absent script first) | *built* |
| M-180 | The feature-coverage trace ships: live-spec's primary unit is its person-facing scenario, so each such heading carries an inline `[feature: F-x]` tag and ARCHITECTURE.md's Feature coverage table maps every unit to implementer node(s) + a real test; a per-project-type unit layer above the anchor matrix, its format's home the spec-author skill; never a feature layer that forks a second machine (it reuses anchor-ownership), never a promised scenario without its tag [E-29] | string | `test_every_scenario_carries_its_feature_tag` + `test_spec_and_spec_author_carry_the_format` (red proven against HEAD — no tags, no E-29, no coverage table there) | *built* |
| M-181 | The feature-coverage check is two-way and has teeth: every tagged unit resolves to a real implementer node and a real test, every promised scenario carries its tag, and the pure checker is proven to catch a dropped tag, an orphan coverage row, a fake node and a fake test name; never a green push with a broken feature trace, never a one-directional check that misses an orphan row [INV-73] | string | `test_feature_coverage_two_way` + `test_stripped_coverage_goes_red` (the never side runs the checker on a deliberately broken table) | *built* |
| M-260 | The shipped-language gate refuses Cyrillic and owner-name in the shipped set, file:line (INV-120, row 275): guardrails/check-shipped-language.sh (engine scripts/check-shipped-language.py) scans the shipped set — the delivery's own indexed text files, a staged file not yet committed included and a file belonging to no delivery left alone, minus the local-only diaries and fixture homes — and exits 1 reporting each offence as file:line, sparing deliberate user-language regions (a user-tagged fenced block or an inline marker) and allowlisted program-data and authorship-byline files; the machine covers the two mechanical offences and leaves a coined metaphor to the register lint; the name arm reads a declared alphabet held as allowlist data (ASCII English + program strings, the out-of-alphabet name patterns declared as data) rather than four hardcoded spellings, so the detector's own source names no person and covering a collaborator is one data line (ROADMAP 417) — every string it reds today still reds; proven on fixtures and wired into the pack's own pre-push (gate i) and CI mirror 2026-07-12 (row 279), the gate's own machinery and the prototype/ sketch home excluded from the scan; never a shipped requirement carrying an untranslated owner-language string or a personal name slipping out uncaught, never a person's name hardcoded in the detector's own code [INV-120] | string | `test_offence_fixture_fails_with_file_line` + `test_gate_reads_a_staged_file_and_leaves_the_rest_of_the_tree_alone` (both directions: a staged file's stray reds one commit before it lands, and an unstaged local file stays outside the scan) + `test_clean_fixture_passes` + `test_deliberate_user_language_region_is_spared` + `test_gate_wired_into_pre_push_and_ci` + `test_gate_green_on_the_swept_tree` + `test_declared_alphabet_is_data_driven_not_hardcoded_in_code` + `test_detector_source_names_no_person` + `test_owner_name_still_reds_under_the_alphabet_form` (the last three red-proven against the pre-delta hardcoded regex, 2026-07-17) | *built* |
| M-430 | A core spec names no foreign project and tells no dated incident (INV-245, ROADMAP 441): the shipped-language machine (scripts/check-shipped-language.py, gate i) gains a project-name arm scoped to the three core specs — PRODUCT_SPEC.md and ARCHITECTURE.md red on a bare foreign project name and on a project name standing within a character window of a calendar date, and TEST_MATRIX.md reds a dated-incident provenance turn while it permits the fixture-ledger's own kind names (a bare name with no adjacent date) and a project-name substring of a test-function name (word-bounded, so it never matches); the forbidden project names live as data in scripts/shipped-language-allowlist.json under `project_name_patterns`, so the detector's own source names no project and the arm stays inert for a package declaring none; the arm rides gate i's existing wiring (pre-push, CI mirror, gate-red-proofs proof i) and adds no gate letter; the dropped history of a reworded line moves to JOURNAL.md as a dated entry; never a foreign project name in the product spec or the architecture, never a dated incident in any core spec, never the fixture-ledger kind names or a test-function-name substring flagged, never a project name hardcoded in the detector's own source [INV-245] | string | `test_project_arm_reds_a_bare_project_name_in_a_core_spec` + `test_project_arm_greens_a_core_spec_stated_as_the_rule` + `test_project_arm_reds_a_project_name_beside_a_date_in_architecture` + `test_matrix_permits_the_fixture_ledger_and_a_test_name` + `test_matrix_reds_a_dated_incident` + `test_project_arm_inert_outside_the_core_specs` + `test_detector_source_names_no_project` (the three reds red-proven against the pre-arm engine, the whole-tree green after the sweep) | *built* |
| M-273 | The reverse direction of the feature-coverage check has teeth through a heading convention (INV-132, row 283 — prover finding F4, 2026-07-12): INV-73's "every scenario carries its tag" was not mechanically enforceable, since the checker could not tell an untagged new scenario from a legitimately-untagged machinery/rules/reference section; now every H3 heading in PRODUCT_SPEC.md carries either its `[feature: F-x]` tag or the explicit `[not a scenario]` marker, so an H3 carrying neither is unambiguously red — a forgotten scenario tag can no longer ship uncovered; the convention lives on H3 headings, sub-parts nesting under an already-tagged-or-marked heading; homes the feature-coverage clause + spec-author; the check reads the shipped spec, the red-proof seeds an untagged H3 into a copy; the recognizer accepts a numeric feature id (`F-12`) and a multi-feature heading (`[feature: F-a, F-b]`) as validly tagged while a truly untagged/unmarked heading stays red (INV-132 mechanism hardened, row 296); never an untagged, unmarked H3 shipping green, never a new machinery section passing without its marker, never a valid numeric-id or multi-feature tag reddened [INV-132] | string | `test_every_feature_tag_sits_well_formed_on_a_heading` + `test_the_checker_catches_the_seeded_defects` + `test_spec_states_heading_convention` + `test_spec_criteria_match_the_practiced_convention` + `test_formal_index_row` + `test_spec_author_carries_heading_convention` (re-aimed at the requirements format: the tag convention moved to `## Requirement` headings, the marker retired, the forgotten-tag net riding the feature-coverage two-way trace; seeded-defect red-proof kept) | *built* |
| M-328 | Pack hooks live as source in `hooks/` and install via `scripts/install-pack-hooks.sh` (INV-173): the canonical scan hook carries the universal English contrast patterns only, reads the personal overlay `~/.claude/hooks/scissors-personal.json` when present, and skips quoted demonstrations (guillemets, quotes, backticks, code fences); never a live hook whose only copy is the installed one, never a pattern-demo flagged as a violation [INV-173] | string | `test_canonical_hook_exists_with_universal_patterns_only` + `test_hook_skips_quoted_demos` + `test_hook_reads_personal_overlay` + `test_installer_dry_run_touches_nothing` | *built* |
| M-330 | The installed gate is the source gate (INV-175, inverted ROADMAP 417): `guardrails/check-config-health.sh` reds a missing or byte-drifted installed hook naming the fix, runs inside the suite (self-healing under a stale push gate) and as push-gate m, skips a CI checkout by name; the session-hook arm diffs the whole hook source directory (hooks/) against the installed set rather than a fixed basename list, so every shipped hook — register-judge*.sh and any future hook — is covered automatically with no edit to the check; a source hook with no installed copy reds as drift (history: docs/matrix-notes/guardrails.md#m-330), and a personal-layer overlay with no source is left alone; the commit fence stops a file both staged and carrying unstaged edits; never the installed copy silently under-running the source's gate list, never a hook going uncovered because a basename list never named it [INV-175] | string | `test_this_repo_installed_hooks_match_source` + `test_missing_installed_hook_reds` + `test_drifted_installed_hook_reds` + `test_matching_hooks_pass` + `test_ci_checkout_skips_by_name` + `test_staged_file_with_unstaged_edits_blocks` + `test_clean_staged_commit_passes` + `test_a_hook_beyond_the_old_three_names_reds_on_drift` + `test_a_sourced_hook_missing_from_install_reds_as_drift` + `test_an_installed_only_overlay_with_no_source_stays_silent` + `test_every_matching_source_hook_passes` (the sourced-missing-reds contract corrected 2026-07-17, red-proven against the pre-correction green skip) | *built* |
| M-397 | A permission rule that points at nothing is a dead rule, and config-health reads it (INV-216, ROADMAP row 406): a permission rule the harness enforces can name a filesystem path, and a rule whose path no longer exists enforces nothing while looking correct (history: docs/matrix-notes/guardrails.md#m-397); the arm `guardrails/check-config-health-perms.py`, wired into `guardrails/check-config-health.sh` (gate m), resolves every filesystem path named in a permission rule in the personal `~/.claude/settings.json` and the host's project settings and reds a rule whose path is absent, reads absolute and `~/` paths, strips a trailing glob to its literal ancestor, keeps a spaced path whole so an existing Google-Chrome-shape path does not false-red, and reports the count resolved so an unresolvable shape (a variable it cannot expand) is named not skipped; personal-layer, so it stands down by name on an absent settings file and reds a present-but-unreadable one, never a false pass; rides gate m's CI carve-out and known-red proof, adding no gate letter; never a dead rule passing, never a spaced existing path false-reddening, never a false pass on unreadable settings, never an unresolvable shape silently skipped [INV-216] | string | `test_dead_permission_path_reds` + `test_existing_permission_path_passes` + `test_space_containing_path_that_exists_does_not_false_red` + `test_absent_settings_stands_down_by_name` + `test_unparseable_settings_reds` + `test_reports_the_count_of_rules_resolved` + `test_unresolved_shape_is_named_not_reded` + `test_real_personal_settings_stands_down_or_passes` + `test_spec_states_the_law` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` (red proven against the pre-arm tree 2026-07-18: the arm absent from check-config-health.sh, spec/architecture/matrix carry no INV-216 — 8 red then green) | *built* |
| M-331 | A law extracted into a gate scans the whole tracked tree, retroactive by construction (INV-176): pre-gate debt is found the day the gate lands, an oversized backlog is absorbed by cap seeding (INV-172), and adoption + the catch-up walk run the current gate set backward over the host's tree; never a delta-only scan letting old debt hide until the next touch [INV-176] | string | `test_spec_states_the_retroactive_gate_law` + `test_gate_contract_carries_the_clause` + `test_adoption_runs_gates_backward` | *built* |
| M-383 | The net-liveness meter records every net's runs and fires and reads them into three readings, red-proven by deed (INV-202, row 391): the shared shape `guardrails/net_meter.py` — the pack-side arm of the personal layer's `hook-meter.py` — wraps a net transparently (stdin through, stdout re-emitted, the net's own exit code preserved) and logs one JSON line per invocation recording whether the run fired, and a `--report` reader aggregates runs and fires against the host's declared roster; a roster net with zero runs reds by name as a broken trigger and the reader exits non-zero (the roster being what lets a never-run net be named, since it writes no line), a net with runs at or over the declared window and zero fires is surfaced as a retirement candidate whose retirement is the human's call — printed but never reddening, never auto-retired — and everything firing reads live; the window is load-bearing, a net silent below it not yet a candidate; recording is proven by driving the real wrapper over a real net and reading back the two log lines' `fired` flags, the readings proven against synthetic records; never a zero-run net passing silently, never a silent net auto-retired or auto-red, never the wrapper altering the net's stdin/stdout/exit [INV-202] | browser-computed | `test_zero_runs_reads_broken` + `test_silent_over_window_is_a_retirement_candidate` + `test_silent_below_window_is_not_yet_a_candidate` + `test_firing_net_reads_live` + `test_report_reds_a_zero_run_net_by_name` + `test_report_surfaces_a_silent_net_without_reddening` + `test_report_reads_a_firing_net_as_live` + `test_a_firing_and_a_quiet_run_are_both_recorded` + `test_the_wrapper_is_transparent_to_the_nets_exit_code` + `test_spec_states_the_law` + `test_formal_index_row` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` (red proven against the pre-delta tree 2026-07-17: the shared shape absent, the module fails to load) | *built* |
| M-384 | The register judge holds the register class a literal list cannot, both surfaces (INV-203, rows 416+418): the mechanism `hooks/register_judge_core.py` hands the outgoing text and the law to the cheapest tier and takes back only the offences quoting the text verbatim — a hallucinated quote dropped, an unreadable answer / a missing binary / a non-zero exit standing the judge down with an error and no block; on the chat surface the four probed empty intensifiers («по-настоящему», «реально», «на самом деле», «действительно» <!-- user-language: judged register samples -->) pass the full literal scissors list untouched and red under the judge, a flat informative sentence passes both, and a turn holding many messages is gathered whole (every assistant message since the last human turn, not the last only) so an offence between two tool calls reds — the place fix as well as the kind; the law splits universal (scissors frame, shipped) against personal (empty intensifier + grading the reader, an overlay the personal layer owns [INV-173]); on the document surface the same judge is the pre-show gate's opt-in ceiling (off by default so the suite stays deterministic) and a Russian fixture carrying the list's own coinages as calques passes the literal list and reds under the judge; the pack installer ships the mechanism + arms; growth duty retracted in the lint docstring and INV-83; never a literal list trusted to hold a class, never a judge blocking on its own breakage, never the last-message-only read that ships an inter-tool violation, never a live model call in the deterministic suite [INV-203, INV-83, INV-173] | string | `test_intensifier_passes_the_literal_list` + `test_intensifier_reds_under_the_judge` + `test_flat_informative_sentence_passes_both` + `test_turn_gathers_every_message_not_only_the_last` + `test_turn_scopes_to_the_current_turn` + `test_hallucinated_quote_is_dropped` + `test_unreadable_shape_stands_down` + `test_code_fence_is_stripped` + `test_judge_stands_down_when_no_binary` + `test_judge_stands_down_on_nonzero_exit` + `test_universal_chat_law_ships_and_names_the_scissors_frame` + `test_personal_law_overlay_rides_the_personal_layer` + `test_chat_law_composes_universal_plus_personal` + `test_document_calque_passes_the_literal_list` + `test_document_calque_reds_under_the_judge` + `test_document_judge_is_opt_in_and_stands_down` + `test_document_law_ships` + `test_growth_duty_retracted_in_the_lint_docstring` + `test_growth_duty_retracted_in_the_spec` + `test_pack_installer_ships_the_judge_arms_unwired` (red proven against the pre-delta tree 2026-07-17: register_judge_core.py absent, the test module fails to import; the spec still carries the growth doctrine) | *built* |
| M-386 | Every point of contact with the person has a kind, and the kind decides what may be said there (INV-205, ROADMAP 413): each touchpoint declares its kind in the manifest `guardrails/touchpoints.json` — synchronous (the person present, the work waiting on him) or asynchronous (he reads on his own clock while the work rolls) plus whether the person opens the point himself — and the afforded traffic derives from those two (an interruption on a synchronous point, a teaching line on a point the person opens himself, waiting traffic on every point); the gate `guardrails/check-touchpoint-kind.py` reds a surface speaking in a kind its touchpoint lacks — a fixture surface interrupting from an asynchronous point reds, a fixture teaching on an asynchronous agent-pushed point reds, and the mirrors pass (an interruption on a synchronous point, a teaching line on the person-opened waiting list, a wait line on an asynchronous point), a malformed manifest reds and the clean repo passes; the frame is the home for its four instance rows, each declared with its kind — the waiting list [408] and the parked feedback question [409] asynchronous and person-opened, the release note offer [402] asynchronous and person-opened, the far-tier surfacing [403] asynchronous and agent-pushed; never an interruption from an asynchronous point passing, never a teaching line on a point the person did not open passing, never a genuine touchpoint reddening [INV-205] | string | `test_manifest_is_well_formed` + `test_manifest_declares_the_four_instances` + `test_manifest_names_each_instance_row` + `test_gate_reds_an_interruption_from_an_asynchronous_point` + `test_gate_reds_a_teaching_line_on_a_point_the_person_did_not_open` + `test_gate_passes_a_wait_line_on_an_asynchronous_point` + `test_gate_passes_an_interruption_on_a_synchronous_point` + `test_gate_passes_a_teaching_line_on_a_point_the_person_opens` + `test_gate_passes_the_clean_repo` + `test_gate_reds_a_malformed_manifest` + `test_gate_wired_into_pre_push` + `test_spec_states_the_law` + `test_formal_index_row` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` (red proven against the pre-delta tree 2026-07-17: manifest + gate absent, the spec/index/architecture/matrix carry no INV-205) | *built* |
| M-387 | Everything waiting for the person's eyes has a home that outlives the scroll (INV-206, ROADMAP 408): the board `WAITING.md` at the host root holds parked questions and unseen answers, the waiting-list touchpoint's surface [INV-205], rendered through `scripts/render-doc.py`; an item clears on his acknowledgement alone, never auto-expired; the shown set is bounded at twelve and the oldest demotes into the unbounded list whole and alive when a thirteenth arrives, a cleared or superseded item moving to the attic with a manifest line (base rule 10); his status answer prints what is in front of him plus one line naming the list's count and that it opens on request [ROADMAP 382]; the gate `guardrails/check-board.py` in the push chain reds three violations — a fixture board with a demoted item absent from the list reds (a silent loss), a fixture closing note omitting an item still open on the board reds, a fixture board showing thirteen items reds (over-cap) — and a genuine board plus a note naming every open item pass, the board keying its regions by markers so the human's wording stays free; never a demoted item vanishing from the board passing, never a closing report omitting a still-open item passing, never a thirteenth shown item passing, never a genuine board reddening [INV-206] | string | `test_board_declares_its_touchpoint` + `test_board_has_the_four_regions` + `test_gate_wired_into_pre_push` + `test_gate_reds_a_demotion_with_no_matching_line` + `test_gate_reds_an_over_cap_shown_set` + `test_gate_reds_a_closing_report_that_omits_an_open_item` + `test_gate_passes_a_genuine_board` + `test_gate_passes_a_closing_note_that_names_every_open_item` + `test_gate_passes_the_real_board` + `test_spec_states_the_law` + `test_formal_index_row` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` (red proven against the pre-delta tree 2026-07-17: gate reds the three fixture violations, and the test file reds 5 — pre-push unwired, the spec/index/architecture/matrix carry no INV-206) | *built* |
| M-388 | A decision recorded as the person's names the exchange it came from; a claim the pack reasoned out is written in the pack's own voice (INV-207, ROADMAP 415): a human's word is the one claim no gate questions, so a challengeable judgment recorded as his is a fabrication moved into the one slot nothing reaches (history: docs/matrix-notes/guardrails.md#m-388); the read-back surface `DECISIONS.md` (the decision-readback touchpoint the frame declares [INV-205], asynchronous and opened on request, rendered through `scripts/render-doc.py`) shows the person the decisions the pack believes he made so he strikes what he never said, a struck line retracted and kept with its note; the gate `guardrails/check-authority-anchor.py` reads the decision set — in a declared `DECISION-RECORD` surface every live on-record entry (a bullet with its wrapped continuation lines, or a prose paragraph carrying an authority-claim) carries a real calendar date, a genuine ~~strikethrough~~ retraction or a `<!-- record:struck -->` region skipped, an unanchored one or an impossible date reds; the standing push scan hard-blocks the decision records and also reaches the churny attribution surfaces the incident used (the resume file and the roadmap) as an advisory report that never fails the push, since a deterministic gate cannot tell live narration from a live fabrication — the tight detector drops the trigger-noun "word" and counts a same-day time as an anchor so the report does not drown, and the register judge (`--judge`) holds the class a literal list cannot; the gate catches only the unanchored case, and a fabrication carrying a plausible date passes it and is caught instead by the read-back where the person strikes what he never said; the fabricated lane-ranking is the corpus's first fixture, reddening unanchored and passing once struck, a genuine dated attribution passing, a genuine rule-language sentence passing, the person roster and role forms held as data so the gate names no person; never an unanchored decision entry passing, never a genuine strike or dated attribution reddening, never rule language reddening, never a typed "STRUCK" word silencing a live claim, never a note-region or above-marker bullet escaping the scan, never a wrapped-entry date on a continuation line reddening, never the risky-surface report failing the push [INV-207] | string | `test_readback_declares_its_touchpoint` + `test_readback_is_a_decision_record` + `test_template_ships` + `test_gate_reds_the_fabricated_ranking` + `test_gate_passes_a_dated_record` + `test_gate_passes_a_struck_record` + `test_gate_reds_unanchored_prose` + `test_gate_passes_dated_prose` + `test_gate_passes_rule_language` + `test_gate_passes_the_readback_surface` + `test_gate_standing_scan_hard_blocks_records_and_reaches_risky_surfaces` + `test_push_mode_reports_risky_surface_candidates` + `test_gate_passes_a_dated_fabrication_the_readback_is_the_defence` + `test_gate_rejects_an_impossible_date` + `test_evasion_typed_struck_word_does_not_silence_a_live_claim` + `test_evasion_note_region_bullet_is_still_scanned` + `test_evasion_paragraph_in_on_record_region_is_scanned` + `test_evasion_bullet_above_first_marker_is_scanned` + `test_wrapped_entry_date_on_continuation_line_passes` + `test_sweep_catches_widened_attribution_shapes` + `test_judge_mode_is_wired_and_advisory` + `test_gate_names_no_person_in_code` + `test_gate_wired_into_pre_push` + `test_spec_states_the_law` + `test_formal_index_row` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` (red proven against the pre-delta tree 2026-07-17: gate + config + surface + template absent, the spec/index/architecture/matrix carry no INV-207) | *built* |
| M-389 | A push that changes a skill's body carries the skill-creator review that change owes (INV-208, ROADMAP 419): a skill is instructions a model reads and a change to them can shift every loading session's behaviour, so Anthropic's skill-creator review is the read that catches a regression before ship (history: docs/matrix-notes/guardrails.md#m-389); the gate `guardrails/check-skill-review.sh` (gate s) reads the push range (the prover-record base ladder [INV-116]: `LIVE_SPEC_DIFF_BASE` then `origin/main` then `HEAD~1`) and finds every substantively-changed skill under `skills/` — substantive when a changed file carries an added or removed line that is not a version stamp; the version-frontmatter stamp `scripts/stamp-versions.py` writes (the `version:` line and the `live-spec-base (vX.Y.Z)` reference [INV-178]) is a machine copy of the one version fact and owes no review, so a stamp-only diff passes; for each substantively-changed skill a committed record under `docs/skill-review/` must name it, carry a `SKILL-REVIEW` marker and a `Verdict:` line, and be at least as new as the skill's last change; a body change with no record reds, a body change with its record passes, a pure version stamp passes; never a substantive skill change passing with no record, never a stamp-only diff reddening, never an untracked record counting, never a stale earlier review covering a later change [INV-208] | string | `test_gate_ships` + `test_review_dir_ships` + `test_template_ships` + `test_body_change_without_record_reds` + `test_version_stamp_only_does_not_red` + `test_version_stamp_with_baseref_does_not_red` + `test_body_change_with_matching_record_passes` + `test_stale_record_does_not_cover_a_later_change` + `test_record_must_be_committed_not_untracked` + `test_no_skill_change_passes` + `test_record_missing_verdict_reds` + `test_gate_wired_into_pre_push` + `test_gate_mirrored_in_ci` + `test_spec_states_the_law` + `test_formal_index_row` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` (red proven against the pre-delta tree 2026-07-17: gate + review-dir + template absent, the spec/index/architecture/matrix carry no INV-208, pre-push and CI unwired — 18 red then green; the hardcoded CI-mirror fixed-list check retired into gate u 2026-07-18, INV-212) | *built* |
| M-390 | The pack's own append-only working documents are split and rotated, and nothing rotated is lost (INV-209, ROADMAP rows 390 + 392): the docs grow with every landing until a guard's scan and a grep run slow (history: docs/matrix-notes/guardrails.md#m-390), so a fully-closed portion rotates out of the live file into a dated archive under `docs/queue-archive/rotated-<doc>-<date>.md` and the live file keeps only live material; the mechanism `scripts/rotate-doc.py` reads the existing `landed`/`decided`/`MET` signal in the status headline with no open marker (`queued`/`in-work`/`deferred`/an open field leg), halts rather than rotate a still-open row, moves the closed rows and leaves a manifest line naming which rows moved and where — base rule 10's superseded-file-moves-to-attic law applied to a document's own closed portion; nothing is lost since each rotated row keeps its own numbered table line in the grepable archive and the manifest maps every rotated number to its archive so a reader who greps the live file meets the pointer; the gate `guardrails/check-doc-rotation.py` (gate t, wired local + CI) reds four fixtures — a manifest claiming a row absent from its archive reds (content dropped), a `rotated-*` archive no manifest points to reds (no manifest), a row both live and rotated reds (ambiguous), and a row inside an archive whose status carries no terminal word (landed, decided, declined, superseded) reds by name, since a row still open there is reachable from no answer about what is left — and a clean rotation plus the real tree pass; never a dropped row passing, never an orphan archive passing, never a row findable twice passing, never a still-open row rotated, never a non-terminal row resting in an archive unnamed, never a clean rotation reddening [INV-209] | string | `test_gate_reds_a_rotation_that_loses_a_row` + `test_gate_reds_a_rotation_with_no_manifest` + `test_gate_reds_a_row_that_is_both_live_and_rotated` + `test_gate_passes_a_clean_rotation` + `test_rotated_row_is_findable_by_number_in_the_archive` + `test_gate_passes_the_real_tree` + `test_rotate_moves_closed_rows_and_leaves_a_manifest` + `test_rotate_halts_on_a_row_that_is_not_closed` + `test_rotate_output_survives_the_gate` + `test_gate_wired_into_pre_push` + `test_gate_mirrored_in_ci` + `test_spec_states_the_law` + `test_formal_index_row` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` (red proven against the pre-delta tree 2026-07-18: gate + mechanism absent, spec/index/architecture/matrix carry no INV-209, pre-push and CI unwired — 16 red then green); the terminal-word arm adds `test_a_queued_row_in_the_archive_reds` + `test_terminal_word_bolded_and_dated_passes` + `test_terminal_word_decided_passes` + `test_terminal_word_declined_passes` + `test_terminal_word_superseded_passes` + `test_stale_leading_narration_word_still_passes_when_terminal_word_stands_later` (red proven against the tree as it stood: the archived row 482 read *queued* with no terminal word and the arm named it) | *built* |
| M-391 | The CI mirror carried every local gate the push gate runs (INV-210, ROADMAP row 420 candidate 1). Retired: gate u (`check-ci-mirror.sh`) and its carve-out declaration retired. Never re-armed without a fresh decision. [INV-210] | string | retired, no owning test | *retired* |
| M-393 | Every gate carried a known-red proof: a gate that cannot fail is a check that looks at nothing (INV-212, ROADMAP row 420 candidate 3). Retired: gate w (`check-every-gate-can-fail.py`) and its registry `gate-red-proofs.json` retired. Never re-armed without a fresh decision. [INV-212] | string | retired, no owning test | *retired* |
| M-392 | The chat judges stayed wired: the installed settings.json listed every judge the pack runs (INV-211, ROADMAP row 420 candidate 2). Retired: gate v (`check-judge-listed.py`) retired; the wired-hook declaration `guardrails/judge-hooks.json` itself still ships and is read elsewhere. Never re-armed without a fresh decision. [INV-211] | string | retired, no owning test | *retired* |
| M-394 | A finished worker leaves no runaway child that burns unseen (INV-213, ROADMAP row 420 candidate 4): a descendant a completed worker/task left behind can keep burning a full core unnoticed (history: docs/matrix-notes/guardrails.md#m-394), and the run owns that descendant (its own process group or its own temp tree), so at a stopping point the run reports what it is, its processor share, and why it owns it; a Stop-time notice, never a push gate (a push gate runs long after the cores burn), so it takes no push-gate letter and is absent from pre-push and CI; the mechanism `guardrails/check-runaway-child.py`, its core `find_runaways` reasoning over process group, parent liveness, and processor share alone and reading no command/name field for its verdict, reports a descendant that is owned (process group or owned tree) and orphaned (owning parent no longer alive, reparented to init or absent from the live table) and burning (processor share at or above the host-settable threshold), the mechanism and group lead excluded; notice-first, reporting through the shared `guardrails/cleanup_notice.py` `runaway_notice` [INV-204] and ending no process (the report-only mechanism carries no killpg/pkill/terminate); live wiring a documented owner-run install step rather than an auto-wire into a running session's Stop hook; a bare-name match in a foreign process group under no owned tree fires never (the browser-kill lesson [INV-162]), a live-and-working child whose parent is alive fires never, an idle owned orphan below threshold fires never, the mechanism ends a process never [INV-213] | string | `test_notice_helper_emits_a_runaway_line` + `test_check_reports_through_the_cleanup_notice_module` + `test_owned_orphaned_burning_is_reported` + `test_live_owned_working_child_is_not_reported` + `test_below_threshold_is_not_reported` + `test_bare_name_match_never_fires` + `test_ownership_by_owned_tree_path` + `test_a_process_outside_the_owned_tree_is_not_owned` + `test_self_pids_are_excluded` + `test_orphaned_by_missing_parent_is_owned_and_reported` + `test_run_emits_a_notice_for_a_runaway_and_returns_zero` + `test_the_check_source_ends_no_process` + `test_cli_injected_table_reports_and_exits_zero` + `test_cli_clean_table_is_silent_and_exits_zero` + `test_not_wired_as_a_prepush_gate` + `test_not_a_ci_step` + `test_spec_states_the_law` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` (red proven against the pre-delta tree 2026-07-18: mechanism + `runaway_notice` absent, spec/index/architecture/matrix carry no INV-213 — 18 red then green, the 2 absence assertions green throughout) | *built* |

| M-399 | A check that looked at nothing is not a pass (INV-218, ROADMAP 384): a check whose input set is empty reports clean while testing nothing, and an empty input set is nearly always the defect — the parse broke or the source moved — so the sibling of the unexpected-skip law [INV-155] governs it: a check declares the input set it expects to be non-empty and an empty set reds by name in place of passing silently; the shared shape `guardrails/nonempty_input.py`'s `require_nonempty` reds by name naming both the check and the empty input set, and passes a non-empty one back; its named instance is the generated index's code → home promise: `guardrails/check-index-generated.py` (gate x since the requirements-format conversion) reds by name on an empty body through the same shared shape, while every committed-index code resolves to a body criterion; never a vacuous check passing green over an empty input set, never an index code with no body home passing the uniqueness check alone [INV-218] | string | `test_shape_reds_by_name_on_empty_input` + `test_shape_passes_a_nonempty_input` + `test_every_index_code_has_a_body_home` + `test_spec_states_the_law` + `test_formal_index_row` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` (red proven against the pre-delta tree 2026-07-18: shared shape + gate absent, the empty-input and absent-home fixtures red by name, spec/index/architecture/matrix carry no INV-218, pre-push and CI unwired — 27 red then green, the real spec's 308 anchors and the pack's substantive check green throughout) | *built* |
| M-400 | The card's gate reads the host's own tree (INV-219, ROADMAP 387): INV-184 declared a card-less host tree flagged where its siblings are flagged and deferred the mechanical gate to ROADMAP row 387 [target], a declared law with no net ranking as a broken invariant [INV-101]; the gate `guardrails/check-agent-card.py` (gate y), the sibling of the kind-with-no-layers flag [A-10], reads a host tree's root and reds by name when it carries no `.live-spec/agent.md` (the red proof of gate y), naming the missing card and the act that supplies it; the pack is its own first host [INV-97] and carries its own card, so the gate reads this tree and passes, the honest self-application keeping the gate off its own push; adoption's own document `adopt/ADOPT.md` names the card so a pre-law tree writes it at its catch-up walk [A-11], the duty binding forward [INV-159]; with the gate live INV-184's [target] comes off its Formal-index row and its prose; never a card-less host tree passing the gate, never a mission-bearing tree standing undeclared as complete, never INV-184's law standing with no mechanical net [INV-219] | string | `test_gate_reds_a_cardless_tree` + `test_gate_passes_a_tree_with_a_card` + `test_gate_passes_the_pack_own_tree` + `test_gate_wired_into_pre_push` + `test_gate_mirrored_in_ci` + `test_adoption_names_the_card` + `test_inv184_index_row_carries_no_target` + `test_inv184_prose_no_longer_rides_row_387_as_target` + `test_spec_states_the_law` + `test_formal_index_row` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` (red proven against the pre-delta tree 2026-07-18: gate absent, the card-less temp tree reds by name, ADOPT.md names no card, INV-184 still carries [target], spec/index/architecture/matrix carry no INV-219, pre-push and CI unwired — red then green, the pack's own card present throughout) | *built* |
| M-401 | The answer-first arm reds a lead-less wall (R231.1). Retired, DECISIONS.md (history: docs/matrix-notes/guardrails.md#m-401): the answer-first law itself is untouched, still stated in the personal profile and reminded by the chat-law hook, and the retired file's sole home is `attic/answer-first-scan.py`; never re-armed without a fresh decision [INV-220] | string | retired, no owning test | *retired* |
| M-410 | A question the pack parks for the person carries a recommended default, and the work rolls on (INV-229, ROADMAP 409): when a question's value is the person's input yet the work cannot wait on his free minute, the question is born onto the waiting list already carrying the default the work took, so the work proceeds on the recommendation (INV-4 — the lane never parks on a proposal) and a parked question with no default is a stalled question wearing a parked question's clothes, the finding this law names, distinct from the decision law [INV-152] which governs what an agent may not settle without him; the waiting-list gate `guardrails/check-board.py` carries the arm — a board item marked a parked question (a `[[park]]` tag) recording no default (a `default:` note) reds, a parked question naming its default passes — an arm extending the existing gate q, no new gate letter; an instance of the touchpoint frame [INV-205] consuming the declared `parked-feedback-question` classification (asynchronous, person-opened, riding the waiting list he opens); an unanswered parked question keeps standing (nothing lost [INV-206]), its default holding and the record saying the default stood unreviewed, an answered one routing through the intake road and closing forever; never a parked question with no default passing, never the arm minting a new gate letter, never the touchpoint frame re-opened here [INV-229] | string | `test_gate_reds_a_parked_question_with_no_default` + `test_gate_passes_a_parked_question_naming_its_default` + `test_spec_states_the_parked_default_law` + `test_formal_index_row_for_parked_default` + `test_architecture_owns_the_parked_default_invariant` + `test_matrix_row_covers_the_parked_default_law` (tests/test_board.py; red proven against the pre-delta tree 2026-07-18: the gate passes the parked-no-default fixture and spec/index/architecture/matrix carry no INV-229 — red then green, the arm reddening the no-default fixture and passing the with-default one) | *built* |
| M-411 | A worker's teardown reaps its own process group, and a stalled worker is caught by its idle output (INV-230, ROADMAP 393): INV-213 reports an owned runaway and ends nothing, and this lands the reap it deferred plus the idle-output detection habit. The reap `guardrails/reap_owned_group.py`'s `reap_owned_group` takes a numeric process group and reaps the run's own group by `os.killpg`, reporting what it ended through the shared cleanup-notice shape [INV-204], and refuses any group absent from the run's owned set or a non-numeric target so a program name is no legal reap target [INV-162] — red-proven by the row-417 broad-kill gate passing on the module; `find_idle_output_workers` flags a worker whose status reads "running" while its output mtime has gone idle [INV-76] and passes a fresh, finished, or output-less one; the idle habit stands in the worker contract [ACT-3]; a process-space habit at teardown taking no push-gate letter [INV-213]; never a reap of a group the run cannot prove it owns, never a kill by program name, never a fresh or finished worker flagged as stalled, never wired into the push chain [INV-230] | string | `test_reap_reports_through_the_cleanup_notice_module` + `test_reap_of_owned_group_is_named_and_scoped` + `test_reap_refuses_an_unowned_group_and_ends_nothing` + `test_reap_refuses_a_non_numeric_target` + `test_reap_source_names_no_process_by_name` + `test_reap_module_passes_the_broad_kill_gate` + `test_idle_output_running_but_stale_is_detected` + `test_idle_output_running_and_fresh_is_not_detected` + `test_idle_output_finished_worker_is_not_detected` + `test_idle_output_no_output_yet_is_not_flagged_here` + `test_reap_not_wired_as_a_prepush_gate` + `test_reap_not_a_ci_step` + `test_worker_contract_carries_the_idle_habit` (tests/test_reap_owned_group.py; red proven against the pre-delta tree 2026-07-18: module + spec/index/architecture/matrix absent — red then green, the not-wired absence assertions green throughout) | *built* |
| M-412 | A deferred row can carry a mechanical revisit trigger, and a one-shot check reads it (INV-231, ROADMAP 405): the tripwire for the day the harness ships a listener — `guardrails/attic/check-listener-tripwire.py` (parked, still runs from there — attic/MANIFEST.md 2026-08-18)'s `find_listeners` fires on a session record carrying a non-empty socket field and stays silent on an empty or absent one, the CLI redding (non-zero, a trigger-fired line) on a firing fixture and silent on an empty one, reading `claude agents --json` or the session registry through `LIVE_SPEC_AGENTS_JSON`; field-gated so today's listenerless harness leaves it quiet, returning the row to the runnable head on firing [INV-129]; rides the queue-take scan and the suite and takes no push-gate letter the way the far-tier check does [INV-222, INV-83]; never a fired state simulated as real, never fired on an empty socket field, never wired as a push gate, never a CI step [INV-231] | string | `test_nonempty_socket_field_fires` + `test_empty_socket_field_does_not_fire` + `test_alternate_socket_field_names_fire` + `test_cli_fires_on_a_nonempty_socket_fixture` + `test_cli_silent_on_empty_socket_fixture` + `test_cli_accepts_a_registry_dict_shape` + `test_not_wired_as_a_prepush_gate` + `test_not_a_ci_step` + `test_row_405_carries_the_trigger_wherever_the_row_now_lives` (tests/test_listener_tripwire.py; red proven against the pre-delta tree 2026-07-18: script + spec/index/architecture/matrix/roadmap trigger absent — red then green, the not-wired absence assertions green throughout; the row itself left the board on the owner's word of 2026-08-28 and the trigger assertion followed it to `docs/queue-archive/2026-08-28-q405-agent-messaging-stale-premise.md`) | *built* |
| M-479 | A document repaired to zero stayed at zero, and every other document's finding count moved down alone. Retired: gate aa and its record `guardrails/rule-census.json` retired. Never re-armed without a fresh decision. [INV-301] | string | retired, no owning test | *retired* |
| M-480 | The census stored no risen count, writing the record only when every measured document sat at or under its record (R302.9, R302.10). Retired: `scripts/rule-census.py` retired. Never re-armed without a fresh decision. [INV-301] | string | retired, no owning test | *retired* |
| M-481 | A reason written by hand survived the census rewriting the record (R302.11). Retired: `scripts/rule-census.py` retired. Never re-armed without a fresh decision. [INV-301] | string | retired, no owning test | *retired* |
| M-482 | Gate aa read the record against the copy the base commit held and reds a raised count carrying no reason (R302.11, R302.12). Retired: `guardrails/check-doc-findings-bound.py` retired. Never re-armed without a fresh decision. [INV-301, INV-218] | browser-computed | retired, no owning test | *retired* |
| M-486 | A raise committed before the push still reds, because the arm read the copy the remote already held (R302.12..R302.15). Retired: `guardrails/check-doc-findings-bound.py` retired. Never re-armed without a fresh decision. [INV-301, INV-218] | browser-computed | retired, no owning test | *retired* |
| M-487 | The census wrote no record from a reading that refused to run (R302.16). Retired: `scripts/rule-census.py` retired. Never re-armed without a fresh decision. [INV-301] | string | retired, no owning test | *retired* |
| M-417 | Which transport carries a message between two agents is decided by the traffic's kind (INV-236, ROADMAP 396): the router `guardrails/route_agent_transport.py`'s `route` sends a message needing no timely answer — a durable record read on the neighbour's own clock, or a notification — to the store road (one file the sender deposits and the receiver sweeps later [E-11, T-10], reachable while the receiver is not running and committed-and-pushed when remote [INV-112], the road that works today [ROADMAP 247]) and a conversation needing a live peer to the direct channel, its availability read from the machine through the listener tripwire's socket-field truth [INV-231, INV-194] so today's listenerless harness leaves it unavailable and the result names the listener it waits on; a fixture session record carrying a socket field flips the road to available, the proof the read is machine-driven and not hardcoded; it corrects INV-183's refused premise (the owner's word 2026-07-17) that git was every message's universal transport with co-location only a fast path, the cut now by the traffic's kind and the two-channel contract [INV-183] left whole; rides the suite and takes no push-gate letter the way the listener tripwire does [INV-231, INV-83]; never a no-answer message routed off the store road, never a conversation routed to an available channel while the harness ships no listener, never the direct-channel road reported available without a listener, never an unclassified message given a guessed route, never wired as a push gate or a CI step [INV-236] | string | `test_no_answer_message_takes_the_store_road` + `test_store_road_is_available_today` + `test_conversation_takes_the_direct_channel` + `test_conversation_road_is_unavailable_today` + `test_conversation_road_names_the_listener` + `test_conversation_available_when_a_listener_ships` + `test_route_rejects_an_unclassified_message` + `test_not_wired_as_a_prepush_gate` + `test_not_a_ci_step` + `test_spec_states_the_law` + `test_spec_corrects_inv183_transport_sentence` + `test_formal_index_row` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` + `test_roadmap_row_396_landed` (tests/test_traffic_transport.py; red proven against HEAD 50b9f2f 2026-07-18: module + spec/index/architecture/matrix/roadmap carry no INV-236 — 14 red then green, the two not-wired absence assertions green throughout; the router sending a no-answer message to the store road and a conversation to the unavailable direct channel naming the listener, a fixture listener flipping it available) | *built* |
| M-420 | The hedge gate reds a reply carrying an offering-hedge frame — an offer to do a derivable, git-reversible act only once the human gives a cue, held open rather than done and reported (INV-238): a literal-pattern Stop-gate on the seat's own reply text modeled exactly on the scissors scan [INV-173], reading the inline universal English pattern list plus an optional personal-overlay JSON, a quoted span/backticked span/code fence stripped before matching; does — reds a reply matching an offering-hedge pattern the way the scissors scan reds a denied-neighbour frame; never — reds a legitimate taste/policy/irreversible question that names its human-only fact [INV-152], and never reds a hedge phrase appearing only inside a quoted or fenced span; string level, the same text-in/decision-out level the retired answer-first arm's stdin-JSON test took [INV-220, M-401] [INV-238] | string | `test_every_hedge_fixture_fires` + `test_no_legitimate_question_is_flagged` + `test_quoted_or_fenced_hedge_is_not_flagged` + `test_stop_hook_blocks_on_a_hedge` + `test_stop_hook_is_silent_on_a_clean_reply` + `test_stop_hook_active_stands_down` + `test_reads_the_whole_turn_not_only_the_last_message` + `test_personal_overlay_patterns_load` + `test_pack_installer_ships_the_arm_unwired` + `test_classified_in_judge_hooks` + `test_spec_states_the_law` + `test_formal_index_row` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` (tests/test_hedge_arm.py; red proven against the pre-delta tree 2026-07-20: hedge-scan.py absent so the module import fails — red then green, 13/13 hedge fixtures fire (8 English + 5 Russian) and 0/6 legitimate/quoted fixtures falsely flagged) | *built* |
| M-431 | The lean-orchestrator arm warns a session that holds raw file content inline without dispatching a worker (INV-246): a Stop-hook soft signal `hooks/lean-orchestrator-scan.py` that sums inline raw file content across the whole session — the tool_result byte size for a main-thread Read or one of six literal Bash file-dump verbs (cat/head/tail/less/more/sed), a sidechain read never counted since it is the worker's own context (the Read is the main net; a dump via grep/awk/python/a shell loop escapes by construction [INV-203]) — counts main-thread Agent/Task dispatches, and fires the hedge scan's block-and-suppress shape [INV-238] when the cumulative inline content is at or over the threshold (default 50 KiB = 50×1024 bytes, tuned by the `~/.claude/hooks/lean-orchestrator-personal.json` overlay's `threshold_bytes`) and the dispatch count is zero; reads the whole transcript not the last message alone, reusing the conduct judge's transcript mechanism [INV-241]; off by default and opt-in, classified as a library entry in judge-hooks.json [INV-211] not wired into the pack's default settings.json; does — warns on a transcript hoarding raw inline content past the threshold with zero dispatch, and clears once a dispatch is present; never — fires on a lean turn under the threshold, on a session that dispatched at least one worker, on a sidechain (worker) read, or on a non-dump Bash result, and never blocks on its own breakage (unreadable payload/transcript stands it down silently); string level, the same text-in/decision-out level the hedge arm takes and the retired answer-first arm took [INV-238, INV-220, M-420, M-401] [INV-246] | string | `test_scan_counts_inline_read_bytes_and_dispatches` + `test_hoard_over_threshold_with_no_dispatch_fires` + `test_clean_lean_turn_stays_silent` + `test_below_threshold_stays_silent` + `test_a_dispatch_suppresses_the_warning` + `test_sidechain_reads_are_not_counted` + `test_bash_file_dump_is_counted` + `test_non_dump_bash_is_not_counted` + `test_measures_across_the_session_not_the_last_message` + `test_stop_hook_active_stands_down` + `test_unreadable_transcript_stands_down` + `test_malformed_payload_stands_down` + `test_personal_overlay_tunes_the_threshold` + `test_lowered_threshold_fires_on_a_small_read` + `test_classified_in_judge_hooks` + `test_spec_states_the_law` + `test_formal_index_row` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` (tests/test_lean_orchestrator_arm.py; red proven against the pre-delta tree 2026-07-21: lean-orchestrator-scan.py absent so the module import fails — red then green, the hoarding/bash-dump/lowered-threshold/whole-session fixtures warn and the lean/below-threshold/dispatched/sidechain/non-dump fixtures stay silent, both directions) | *built* |
| M-421 | The Formal-index description-field presence gate would have redded a registered code carrying an empty description, staying dormant until the back-describe migration armed it (INV-239, ROADMAP 424). Superseded: the requirements-format conversion gave the generated index a locations-only shape instead (M-445), and the gate this row planned retired unbuilt. Never re-armed without a fresh decision. [INV-239] | string | retired, no owning test | *retired* |
| M-422 | The agent-channel deposit-time description lint reds a `from-<agent>` inbox file that references a code with no description beside it (INV-239, ROADMAP 424): `guardrails/check-deposit-description.py` reads every `from-<agent>` inbox file the way the earned-message gate `guardrails/check-earned-message.py` already reads those same deposited files [INV-189], and reds when a referenced internal code appears with no non-empty description text beside it, judging presence alone and never the semantic match, undecidable on a translated, reformulated sentence and owned by the human net [INV-41, INV-83, INV-240]; does — reds a deposit that references a bare code with no description beside it, naming the code and the file; never — reds a reference that carries its description, and never a correctness or semantic-match check (presence only); string level, the file-read-and-exit-code level the earned-message gate beside it takes [INV-239] | string | `guardrails/check-deposit-description.py` + tests/test_deposit_description.py — `test_gate_ships` + `test_gate_reds_a_deposit_referencing_a_bare_code` + `test_gate_passes_a_deposit_carrying_the_description` + `test_gate_is_presence_not_a_semantic_match` + `test_gate_reads_only_from_agent_inbox_files` + `test_spec_states_the_law` + `test_formal_index_row` + `test_architecture_owns_the_invariant` + `test_matrix_row_covers_the_law` (code step builds red-first: the gate absent before this build, the bare-code deposit fixture reds and the described-reference fixture passes) | *todo* |
| M-426 | The conduct judge reds an act-level orchestration violation read from the turn's action trace (INV-241): `hooks/conduct-judge.py` walks the transcript to the last real human message, collects the assistant `tool_use` blocks in order as the action trace, renders them to quotable text, and hands them to `register_judge_core.judge` against the four-member orchestration law (worker-routing, lean-orchestrator, pull-unblocked-work-and-never-idle, classify-the-subtask), async in the register judge's two-arm shape with a verdict slot of its own and off by default; does — reds a trace carrying a known violation (a long artifact authored inline with no worker dispatch, an idle after a landing) under a stubbed model, and extracts and orders the trace, renders it quotable, skips an empty-trace turn, writes the distinct `.conduct.json` slot, folds the env strictness default, and drops a hallucinated quote; never — reds a clean trace (work dispatched, reads delegated), reds when the model binary is missing or broken, overwrites the register judge's slot, or ships wired on by default; string level, the function-and-stubbed-model level the sibling chat judges take [M-401, M-420], the live model verdict human-verified over a review window [INV-41] [INV-241] | string | tests/test_conduct_judge.py — `test_action_trace_extracts_tool_uses_in_order` + `test_action_trace_scopes_to_the_current_turn` + `test_action_trace_boundary_skips_tool_result_user_records` + `test_render_trace_is_quotable_one_line_per_call` + `test_empty_trace_skips_exit_zero_no_verdict` + `test_verdict_slot_is_the_distinct_conduct_slot` + `test_judge_stands_down_when_no_binary` + `test_hook_stands_down_on_broken_binary_never_reds` + `test_violation_trace_reds_under_a_stubbed_model` + `test_violation_reds_through_the_full_judge_call` + `test_clean_trace_draws_an_empty_verdict` + `test_hallucinated_trace_quote_is_dropped` + `test_orchestration_law_names_the_four_members` + `test_strictness_folds_the_env_default_into_the_law` + `test_conduct_hooks_are_classified_library_not_wired` + `test_render_carries_a_size_signal_for_authored_content` + `test_main_emits_the_conduct_block_end_to_end` + `test_main_stays_silent_on_a_clean_trace` + `test_law_3_and_4_carry_the_partial_evidence_note` (red-first proven: with hooks/conduct-judge.py moved aside the suite fails at collection with FileNotFoundError, and the block-emit test reds when conduct_reason is gutted — red then green, 19/19; a fresh-seat adversarial audit added the block-emit end-to-end test and the size signal before landing) | *built* |
| M-427 | The landing-refreshed-map gate reds a landing commit — one whose diff flips a ROADMAP.md row's status cell to the closed acceptance token `landed` — whose diff does not also touch NEXT_STEPS.md, unless a later commit in the same range heals it (INV-242): `guardrails/check-landing-next-steps.py` reads the pushed commit range through the same base ladder the other range checks use [INV-208], naming the one fix (refresh NEXT_STEPS.md in the landing commit); a miss is healed by a later commit that touches NEXT_STEPS.md and names the missed landing's commit id via the phrase `heals landing <shortsha>`, its committer timestamp not earlier than the landing's, one healer able to name several misses; does — reds a landing commit whose diff omits NEXT_STEPS.md and finds no valid heal, warns (does not red) a healed miss naming the landing, its rows, and the healer; never — stays green on a landing commit that did touch NEXT_STEPS.md, stays green on a non-landing (no row closed) commit, stays green on a row closed to declined/deferred/superseded (not a landing), never heals on a NEXT_STEPS.md touch that omits the phrase or names the wrong sha, never heals on a heal commit dated before its landing, and is never wired into the pre-push hook, riding the suite the way the far-tier report-shape check does [INV-222] and taking no push-gate letter since a–z are exhausted; string level, the script run over a fixture git range [INV-242] | string | `tests/test_landing_next_steps.py::test_reds_landing_commit_without_next_steps` + `tests/test_landing_next_steps.py::test_passes_landing_commit_that_touches_next_steps` + `tests/test_landing_next_steps.py::test_passes_non_landing_commit` + `tests/test_landing_next_steps.py::test_passes_decline_deferred_superseded_close` + `tests/test_landing_next_steps.py::test_checker_not_wired_into_pre_push` + `tests/test_landing_next_steps.py::test_heals_missed_landing_with_later_heal_commit` + `tests/test_landing_next_steps.py::test_missed_landing_with_later_next_steps_commit_not_naming_sha_still_reds` + `tests/test_landing_next_steps.py::test_heal_phrase_commit_dated_before_its_landing_still_reds` + `tests/test_landing_next_steps.py::test_one_heal_commit_names_two_missed_landings` | *built* |
| M-428 | The config-health check gains a skill-copy arm that reds a missing or drifted installed skill copy against the pack's skills source (INV-243): `guardrails/check-config-health.sh` diffs each installed skill tree at `~/.claude/skills/<skill>` against the pack's `skills/<skill>` source, naming the one fix (re-run `scripts/sync-skills.sh`), reading the whole skill source directory against the installed set rather than a fixed name list so every skill the pack ships is covered the moment it lands; does — reds a missing installed skill copy, reds a drifted installed skill copy; never — stays green when every installed skill matches the pack source, leaves a personal-overlay skill with no pack source alone, and skips the arm by name on a CI checkout with no installed skills; runs inside the suite and the push gate with the hook arm [INV-175]; string level, the shell check run over a temp tree [INV-243] | string | `tests/test_config_health.py::test_reds_missing_installed_skill` + `tests/test_config_health.py::test_reds_drifted_installed_skill` + `tests/test_config_health.py::test_passes_matching_skills` + `tests/test_config_health.py::test_ignores_personal_overlay_skill` + `tests/test_config_health.py::test_ci_skips_skill_arm` | *built* |

| M-435 | The requirements-format shape holds: preamble then glossary then requirements, each with Context, User Story, and criteria in named cases; each criterion trails its anchor, numbers continuously, states its response as lowercase-italic shall with no all-capital shout outside anchors and filenames; a malformed [gap: ...] line reds and an evaluative phrase names its judge; never a shapeless requirement, a bare criterion, or a shouted word passing [INV-250, INV-251, INV-252, INV-257] | string | `tests/test_requirement_shape.py` | *built* |
| M-436 | The spec body states today's behaviour only: a date, a recorded-word phrase, or a past-state narration in the body reds and moves to the journal; the preamble and glossary stay exempt; never a provenance sentence riding a criterion [INV-253] | string | `tests/test_no_history.py` | *built* |
| M-437 | Every domain noun the body uses has its glossary entry and every glossary entry is used; a dead entry or an unglossed noun reds; never a coined term standing undefined [INV-254] | string | `tests/test_vocabulary_check.py` | *built* |
| M-438 | One thing carries one name everywhere in the document; a registered alias appearing anywhere reds naming the canonical form, the pair list living in `guardrails/one-name-aliases.json`; never two names for one artifact passing [INV-255] | string | `tests/test_one_name_check.py` | *built* |
| M-439 | A weak relational word with an unfilled slot and no reference point and no [gap] line reds; the same word passes once its slot is filled; never a vague bound shipping unmeasured [INV-256] | string | `tests/test_weak_words.py` | *built* |
| M-440 | The code-to-location table is generated output built from the body criteria: a hand edit (committed differing from a fresh build), a body code the table misses, a table code no criterion carries, or an empty body reds by name; never a hand-kept index row passing [INV-258, INV-259] | string | `tests/test_index_generated.py` + `tests/test_build_index.py` | *built* |
| M-447 | The index builder refuses `-o` aimed at its own input document, exiting red with the document untouched; never the builder replacing the document with its generated table (the 2026-07-23 clobber) [INV-258] | string | `tests/test_build_index.py` | *built* |
| M-448 | The test matrix reads in the format-family member `docs/test-matrix-format.md` defines — artifact inventory, node-grouped criterion rows, generated Reference — after the conversion delivery; never a converted matrix restating a family law the spec format owns [INV-272] | string | `tests/test_traceability.py` | *built* |
| M-449 | The matrix Reference is generated output agreeing with the body rows: a hand edit, a body anchor the table misses, or a table anchor no row carries reds, the green line stating its reach; never a hand-kept Reference passing [INV-273] | string | `tests/test_matrix_reference.py` | *built* |
| M-602 | The architecture Reference is generated output agreeing with the nodes' owns fields: a hand edit, an owns anchor the table misses, or a table anchor owned by no node reds, the green line stating its reach; never a hand-kept Reference passing [INV-315] | string | `tests/test_architecture_reference.py` | *built* |
| M-450 | The matrix row lint reds a body row pinning no level or stating no never side, naming the row, and states its reach on the green pass; never a level-less or one-sided row surviving the suite [INV-274] | string | `tests/test_traceability.py` | *built* |
| M-451 | The queue reads in the format-family member `docs/roadmap-format.md` defines — a live-only body of five-cell rows in ascending id order above a monthly-file manifest — after the conversion delivery; never a converted queue restating a family law the spec format owns [INV-275] | string | `tests/test_traceability.py` | *todo* |
| M-452 | A row reaching a terminal exit moves verbatim with its delivery report to the current month's archive file in its closing commit, that file's single manifest line updated and the rotation gate proving nothing lost; never a body row whose status reads *landed*, and never a moved row found in neither the body nor its archive [INV-276] | string | `tests/test_doc_rotation.py` | *todo* |
| M-453 | The queue row lint reds a body row with other than five cells, an out-of-order id, a status or class outside the closed vocabularies, an undated status, or a *deferred* row naming no trigger, naming the row and stating its reach on the green pass; never a malformed row surviving the suite [INV-277] | string | `tests/test_traceability.py` | *todo* |
| M-441 | A spec delivery declares its delta: an undeclared disappearance, an undeclared appearance, an undeclared sharpen, a sharpen whose old sentence survives, an over-cap new criterion, or undeclared byte growth reds against the last freeze baseline; never a silent criteria-set change passing [INV-260, INV-261, INV-262, INV-263] | string | `tests/test_delta_classifier.py` | *built* |
| M-442 | The bytes-per-criterion ratchet governs PRODUCT_SPEC.md alone, seeded at the migration-end freeze in `guardrails/spec-ratchet.json`; a delivery above the recorded bound reds, a lower delivery lowers the bound, and no delivery raises it; never the recorded bound rising outside a Requirement-4 change [INV-264, INV-265] | string | `tests/test_size_ratchet.py` | *built* |
| M-443 | Each format gate's green line states its reach — the files opened and the rows matched of the rows scanned — never a bare green line with no reach stated; asserted per gate in its own test file [INV-269] | string | `tests/test_requirement_shape.py::test_green_line_states_reach` (siblings assert the same reach line per gate) | *built* |
| M-444 | Every format gate armed in the one conversion delivery, none before: each gate's suite test runs it on the live PRODUCT_SPEC.md (gate x also wired local + CI), the armed tests being the record of the arming event; never a format gate enforcing before the conversion delivery, never an armed gate red on the live document [INV-270] | string | `tests/test_requirement_shape.py::TestArmedOnTheRealSpec` (one armed test per format-gate file) | *built* |
| M-445 | The generated index carries locations only while the criteria and the glossary stay the authored home of every code's plain statement; never a table row carrying prose, never a bare code standing with no criterion home [INV-271] | string | retired, no owning test | *retired* |
| M-454 | The architecture reads in the format-family member `docs/architecture-format.md` defines — a preamble, then per-node `### [node: <name>]` sections each carrying responsibility, owns, and pins with an optional notes field, then the generated tables — a `[target]` node marking the tag in its heading; every spec anchor owned by exactly one node and the bond held both ways; never a node outside a section, never an anchor owned by two nodes or by none [INV-278] | string | `tests/test_architecture_format.py` (red proven against the pre-conversion table `ARCHITECTURE.md` at git HEAD: `archformat.parse_nodes` raises on the retired 4-column table, so every check reds) | *built* |
| M-455 | An owns entry cites its rule's spec home by anchor and restates no law, a sentence the spec lacked having moved into the spec clause in the same delivery, and the dated prover-record table having relocated to `docs/prover/architecture-prover-record.md`; never an owns cell restating the rule its anchor names, never a pin carrying a date or provenance, never a dated history table left in the architecture [INV-279] | string | `tests/test_architecture_format.py` (red proven against the pre-conversion doc: restated laws filled the owns cells, pins carried landing dates, the Prover-record table sat in ARCHITECTURE.md) | *built* |
| M-456 | One node reader `guardrails/archformat.py` reads the node shape — names, owned anchors, pins — for every consumer, and the node-growth counter's node list re-derives from it; never a consumer parsing the raw node shape on its own, never a hardcoded node-name list drifting [INV-280] | string | `tests/test_architecture_format.py` (red proven against the pre-conversion tree: consumers parsed the `## Nodes` table by hand and `crosscut_counter.PACK_NODES` was a hardcoded literal) | *built* |
| M-457 | The contrast-frame scan and the hedge scan each read every assistant message shown since the last human turn through the shared reader `hooks/turn_reader.py`, so a banned frame in an inter-tool narration line is caught the same as one in the final reply; never a mid-turn narration line going out unread by the contrast-frame or hedge scan [INV-281] | string | `tests/test_scissors_scan.py` + `tests/test_hedge_arm.py` (red proven: a contrast frame planted in a mid-turn narration line was missed while the scan read only the final message); the consumer list is netted by `tests/test_architecture_pins.py::test_turn_reader_pin_names_every_actual_importer`, which reads the real importers under `hooks/` and reds a pin naming fewer or more (the drift that ran three to five to six by hand inside one day) | *built* |
| M-458 | Every session hook carried a known-red proof, the hook-side sibling of gate w's registry (INV-212, M-393). Retired: `guardrails/check-hooks-can-fire.py` and its registry `guardrails/hook-red-proofs.json` retired. Never re-armed without a fresh decision. [INV-282] | string | retired, no owning test | *retired* |
| M-459 | A naked internal code in live prose reds (INV-28, base rule 2): the Stop hook `hooks/code-anchor-scan.py` reads every assistant message shown since the last human turn through the shared reader `hooks/turn_reader.py` (INV-281, M-457) and blocks the stop when a queue row named by its number in either working language, a bracket code the documents use, a document's own name run against a number, or a multi-letter code spoken with a space, stands outside a trailing anchor, asking for the naming to be repeated in plain words one message later; does — reds a leading naked code and passes a code sitting inside parentheses or square brackets, the lawful anchor; never reds a code inside a fenced block, an inline backtick span, or a quoted span, never reds a table row whose neighbouring cell carries the plain words, never reds a bare number with no naming word before it, never reds a naming word and its number on a line that carries a file name or a file word, which is a reference to a line inside a source file, never reds an ordinary word carrying a letter and a number in lower case since every code form is read in the capital shape the documents write it in, and never judges whether the plain words that replace the code are the right words; string level, the same text-in/decision-out level the scissors and hedge arms take, and the retired answer-first arm took (M-420, M-401) [INV-283] | string | `tests/test_code_anchor_scan.py` — `test_naked_russian_row_reference_reds` + `test_naked_english_row_reference_reds` + `test_naked_invariant_code_reds` + `test_trailing_anchor_in_parentheses_passes` + `test_bracketed_anchor_in_a_document_line_passes` + `test_table_row_passes` + `test_code_block_passes` + `test_clean_reply_passes` + `test_document_name_citation_reds` + `test_spaced_inv_code_reds` + `test_hash_row_reference_reds` + `test_uppercase_row_spaced_reds` + `test_uppercase_act_spaced_reds` + `test_uppercase_m_dash_code_reds` + `test_russian_file_line_reference_passes` + `test_parenthetical_anchor_wrapped_across_a_line_passes` + `test_lowercase_act_as_ordinary_word_passes` + `test_lowercase_b_dash_sample_passes` + `test_lowercase_f_16_passes` + `test_lowercase_c_4_wiring_passes` (both directions: the naked forms red — a bare row number in either working language, a bracket code, a document name run against a number, a multi-letter code spoken with a space — and the lawful shapes stay silent, ordinary lower-case words carrying a letter and a number among them, red-first proven for the case rule: "act 3", "b-2", "f-16" and "c-4" each blocked before the patterns were held to their capital shape) | *built* |
| M-460 | Empty validation aimed at the person reds: the Stop hook `hooks/affirmation-scan.py` reads every assistant message shown since the last human turn through the shared reader `hooks/turn_reader.py` (INV-281, M-457) and blocks the stop with a rewrite instruction when a validation phrase — telling the person he is right, praising his question or his intuition — survives the stripping of a quoted, backticked, or fenced span, matching an inline universal English tier plus an optional personal-overlay file a host tunes, installed by the setup walk beside the scissors scan (INV-173) and covered by config-health parity (INV-175), the wired-hook declaration (INV-211), and the net-liveness meter (INV-202); does — reds a validation line in the final reply and one in an inter-tool narration line alike; never reds a phrase demonstrated inside quotes, backticks, or a fenced block, never reds when the stop hook is already active or when the turn's record cannot be read, and never reds a paraphrase it does not list, which the register judge reads in meaning (INV-203); string level, the same text-in/decision-out level the scissors and hedge arms take, and the retired answer-first arm took (M-420, M-401) [INV-284] | string | `tests/test_affirmation_arm.py` (both directions: the universal English fixtures and the overlay fixtures fire, the plain factual reply, the agreement with a fact, and the quoted demonstration stay silent, and an already-active stop stands the hook down) | *built* |
| M-461 | A chat law is judged while the turn still runs (ROADMAP 495; history: docs/matrix-notes/guardrails.md#m-461). Retired, the owner's word 2026-08-17: the PreToolUse arm `hooks/midturn-chat-scan.py` is retired. The two laws it carried stand: the code-anchor law keeps its Stop-side scan, and the measurement law now names no machine. The retired files' sole home is `attic/midturn-chat-scan.py` with `attic/chat-calques.json`; never re-armed without a fresh decision [INV-285] | string | retired, no owning test | *retired* |
| M-462 | The public page's host count agrees with the architecture's own (ROADMAP 501, his word 2026-07-27): the front page told a stranger that two projects run under the pack while the architecture named three real hosts, and the contradiction stood on the page's own honesty claim; the architecture's count is the authority and the two sentences on the front page state it; does — reds when the front page's count and the architecture's differ, and reds an architecture stating more than one count, since the number has one home; never judges which projects those hosts are, which stays with the documents that name them; string level [INV-120] | string | `tests/test_host_count_agrees.py` — `test_the_architecture_states_its_host_count` + `test_the_front_page_states_the_architecture_count` (red proven against the page as it stood: architecture three, front page two) | *built* |
| M-465 | The setup walk installs every session hook the declaration names (ROADMAP row 506): `guardrails/judge-hooks.json` gains four generation fields per wired hook — the repo file it ships as, its plain command form, its event matcher, and the shipped data files it reads — plus the personal-overlay pattern it never touches; `scripts/install-session-hooks.sh` generates its own two hooks (clock-hook, chat-law-hook) from the declaration and chains to `scripts/install-pack-hooks.sh` for the rest; does — places every declared hook's file with every declared data file from a fresh HOME and wires the wired declaration, leaving the six opt-in hooks unwired, recognizes a hook already wired in any command form including a counter-wrapped one and leaves it standing, reports every personal overlay it finds and touches none, changes nothing on a second run; never installs a file the declaration does not name, never overwrites a personal overlay, never duplicates an already-wired entry [INV-289] | string | `test_every_declared_hook_ends_up_wired` + `test_every_declared_hook_command_matches_the_declaration` + `test_every_declared_data_file_is_installed_beside_its_hook` + `test_every_opt_in_hook_file_lands_unwired` + `test_no_undeclared_file_is_installed` + `test_rerun_changes_nothing` + `test_a_personal_overlay_already_present_is_left_untouched` + `test_a_meter_wrapped_existing_entry_is_recognized_not_duplicated` (tests/test_install_session_hooks.py; red proven 2026-07-27 against the pre-fix installer, which wired only two of the ten declared hooks) | *built* |
| M-468 | Every rule about this project's texts sits in one home with its own fields; never a rule stated twice [INV-292] | string | `tests/test_language_rules.py` | *built* |
| M-469 | The generator builds both consumers from that home; never a drifted consumer or a broken pin passing [INV-293] | string | `tests/test_language_rules.py` | *built* |
| M-470 | Each rule records its catcher and its arming point, or why none runs it; never a rule silent on both [INV-294] | string | `tests/test_language_rules.py` | *built* |
| M-471 | Each rule names its surfaces and takes a person's override beside the shipped default; every surface section of the generated page names each rule that binds it; the home records no rule whose verdict differs by surface, so the test holds the shipped shape and the one-entry-per-surface condition waits for the first rule that carries a differing verdict; never a surface-blind rule, never a surface section silent on a rule that binds it [INV-295] | string | `tests/test_language_rules.py::TestTheRuleHomeHoldsWhatTheRequirementStates::test_every_surface_names_every_rule_that_binds_it` | *built* |
| M-472 | A change to one rule is read against its relatives in one pass; the rule home records no change event and no relative-reading verdict, so nothing in the home says whether a pass happened and no test can read it — the prover's station holds this row until the home records the event; never one rule moved alone [INV-296] | string | held at the prover's station; `tests/test_language_rules.py` reads the home and finds no change record to assert against | *todo* |
| M-473 | A reader's finding enters the home as a class with its examples under it: every rule carries a class sentence, a rule carrying examples renders its class above them on the generated page, and the home itself states the class-over-examples law as one of its rules; never examples under no class, never the law absent from the home it governs [INV-297] | string | `tests/test_language_rules.py::TestTheRuleHomeHoldsWhatTheRequirementStates::test_every_entry_states_its_class_over_its_examples` + `test_the_class_law_itself_stands_in_the_home` | *built* |
| M-477 | A machine judges the worker result it is about to accept (the mechanical arm of base rule 7): `guardrails/check-worker-restore.py --run <exact-agent-jsonl>` reads that worker transcript and reds when Bash received a named discarding git form. The command reads the full exact run independently of dates, session placement, and neighbour classification. A red original run remains red in the forensic root scan. Recovery gives the worker a fresh brief; acceptance requires its fresh exact run to pass. Reports and plans are prose; `git restore --staged` and dry `git clean` pass. The rulebook, pipeline skill, delegation protocol, agent card and lane opener state one command list. Deterministic fixtures prove the verdict. The personal transcript root preserves forensic evidence outside suite acceptance. Later repair never accepts the original run, and prose never counts as a shell call [INV-299] | string | `tests/test_worker_restore.py` + `tests/test_worker_restore_run_scope.py` — exact bad run reds, clean sibling passes, forensic bad run remains red, neighbour cannot downgrade an explicit run, incompatible flags red, and no counting/resume threshold appears | *built* |
| M-546 | The worker-restore gate judges this project's own sessions, and a session it cannot place still reds (INV-299, narrowed 2026-08-18, refined 2026-08-19): `is_own_session` reads the session's recorded `cwd` first and alone — a directory that exists on disk and that git places in a different, nameable repository makes the finding a neighbour's, carried under its own heading as a notice rather than reddening, and every worktree of this repository answers this repository's one shared git directory, so a lane worktree reads as this project's own wherever it sits; a `cwd` the gate cannot place at all — gone from disk, no repository at all, or an owner's home directory that only launched the session — falls to the fail-safe default and reds, unless the finding's own `effective_dir` (the directory `classify` already walks any `cd` to and prints as `ran in`) itself exists on disk and git places it in a different, nameable repository, in which case that finding is carried as the neighbour's too; the `effective_dir` look fires only where `cwd` answered nothing, never overriding a `cwd` the key already placed, and an `effective_dir` that is itself `UNKNOWN`, unplaceable, or lands back in this project's own repository — a sibling lane worktree among them — leaves the fail-safe reddening exactly as before; does — carries a real neighbour's finding as a notice naming session, directory, command and outcome, reds a session the gate cannot place anywhere, and reds an unplaceable-`cwd` session whose command actually ran in a sibling worktree of this same repository; never a `cwd` the key already placed second-guessed by `effective_dir`, never an `UNKNOWN` or unplaceable `effective_dir` downgrading a finding to a neighbour's, never a sibling worktree of this project's own repository read as a neighbour's [INV-299] | string | `tests/test_worker_restore.py` — `test_a_neighbouring_projects_session_reds_nothing` + `test_the_neighbours_finding_is_carried_as_a_notice_and_not_lost` + `test_this_projects_own_session_reds_exactly_as_before` + `test_a_session_directory_gone_by_scan_time_still_reds` + `test_a_session_in_no_repository_at_all_still_reds` + `test_an_unplaceable_cwd_with_a_neighbours_effective_dir_reds_nothing` + `test_an_unplaceable_cwd_with_an_unknown_effective_dir_still_reds` + `test_an_unplaceable_cwd_with_effective_dir_in_a_sibling_worktree_still_reds` + `test_every_verdict_says_whose_sessions_it_judged` | *built* |

| M-601 | The worker-restore gate's own `git clean` catch reads git's dry-run guarantee, not only the force letter (INV-299, found 2026-08-19 — a sandbox worker of this very package's own session ran `git clean -fn` to prove the pre-shell hook against these forms, and the gate reddened the proof run itself): `-n` and `--dry-run` are read as one marker, bare or bundled with `-f`/`-x` into a combined single-dash cluster in either order (`-fn`, `-nf`, ...), and their presence anywhere among the flags — confirmed against a real `git clean` run, whichever position the marker takes and however many times `-f`/`--force` also appears — clears the command of the finding, since git itself removes nothing once the marker stands; a `--` path separator ends the scan, so a literal path is never read as a flag, and a form the scan cannot prove dry — a bare `-f`, a bare `-x`, an abbreviated `--dry` nobody asked it to recognise — still reds exactly as before, narrowing nothing else the gate already caught `git clean` on; never a live `-f`/`-x` clean passing on the marker's absence, never an abbreviated or unlisted spelling read as proof of dryness, never a spelling this cannot prove dry reddening the gate silently past its own doubt-resolves-to-red rule [INV-299] | string | `tests/test_worker_restore.py::TestGitCleanDryRunIsNeverAFinding` — `test_a_dry_run_form_passes` + `test_the_same_flags_without_the_dry_marker_still_red` + `test_dry_run_alone_with_no_force_or_x_still_passes` + `test_an_abbreviated_long_flag_is_not_trusted` (red proven 2026-08-19: reverting the gate's `git clean` branch to the bare `-f`/`-x` letter-check made ten of the twelve dry-form cases fail, naming the still-reddening command; restored and green) | *built* |
| M-494 | A push carries an adversarial review of the delta it sends, on the one record it already owes (INV-304): `guardrails/check-prover-record.sh --push` (gate a) reads the push range through its own base ladder [INV-116] — the declared base `LIVE_SPEC_DIFF_BASE`, then `origin/main`, then `HEAD~1` — takes the commits in that range that touch a file outside `docs/prover/`, and requires the committed record to name the base commit and every one of them; a commit that carries the record alone is exempt from the naming rule, since a record cannot name the commit that first ships it; where no record is found and the base is one a real push resolves (never the `HEAD~1` last resort), a range whose every commit touches only the owner's recordless class — the record directories alone, `docs/prover/ docs/skill-review/ docs/language-reads/` (narrowed twice: the owner's ruling of 2026-08-15 22:07 took `guardrails/` and `.github/workflows/` out, since a push touching gate machinery demands its record again; the adversarial refusal of 2026-08-15 23:41, findings F1–F3, took `.live-spec/`, `tests/` and `TEST_MATRIX.md` out, since enforcement machinery must never exempt itself) — stands down by name (the owner's word, agent card rule 1); does — reds a range with no committed record, reds a record older than the newest reviewed commit, reds a record naming another range, passes a committed record that names the pushed range, stands down an all-in-class range with no record on file, and reds a range whose one commit touches `guardrails/` alone, `tests/` alone, or `.live-spec/` alone; never a scratch record in the working tree counting, never a stale record covering a later commit, never a record written for another range passing, never a range holding record commits alone reddening, never a tree with no resolvable range reddening, never a second record demanded of one push, never a range carrying one file outside the recordless class standing down [INV-304] | string | `tests/test_push_review.py::test_range_with_no_record_reds` + `test_stale_record_does_not_cover_a_later_commit` + `test_record_naming_another_range_covers_nothing` + `test_a_fresh_record_naming_the_range_passes` + `test_untracked_record_does_not_count` + `test_a_range_of_record_commits_alone_stands_down` + `test_a_tree_with_no_range_stands_down` + `test_one_record_and_one_gate_carry_the_push_review` + `test_a_recordless_class_range_stands_down` + `test_a_range_with_one_out_of_class_file_still_demands_the_record` + `test_a_guardrails_only_commit_now_reds` + `test_a_tests_only_commit_now_reds` + `test_a_live_spec_only_commit_now_reds` (red proven against a scratch repository whose range carries no record) | *built* |
| M-495 | The record states what the review examined (INV-304): the record under `docs/prover/` carries the `PUSH-REVIEW` marker and five fields, each with a value — `Range:`, `Files read:`, `Checks run:`, `Findings:` and `Blocking:` — so a review that found nothing still says which files it read and which checks it ran, and the absence of findings rests on that coverage rather than on silence; `Blocking:` reads `none`, or lists one item per blocking finding, each carrying `closed:` with what changed or `stands:` with the reason it stands; the house format in `docs/prover/README.md` names those five and no field the gate leaves unread; does — reds a record short a field, reds a field left empty, reds an open blocking item, and passes a blocking item that is closed or explained; never an empty file satisfying the gate, never a blocking finding leaving the machine unexplained, never a documented field no gate parses [INV-304] | string | `tests/test_push_review.py::test_record_short_a_field_reds` + `test_open_blocking_finding_reds` + `test_closed_blocking_finding_passes` + `test_record_home_ships` + `test_the_house_format_names_only_fields_the_gate_reads` (red proven against a record naming the pushed range with its `Checks run:` field removed) | *built* |
| M-496 | The gate holds what a machine can hold and names the rest (INV-304): `guardrails/check-prover-record.sh` runs as gate a of `guardrails/pre-push`; the script's own comments, the requirement, and `docs/prover/README.md` each state that no script decides whether the review was adversarial, whether the files the record names were read, or whether the findings are the ones the delta deserved; does — states that limit on the green line and in all three homes; never a green line read as a judgment that the review was adversarial [INV-304] | string | `tests/test_push_review.py::test_the_limit_is_stated_in_every_home` + `test_gate_ships` + `test_spec_states_the_law` + `tests/test_guardrails.py::test_missing_record_fails` | *built* |

| M-498 | A published count answered to the command printed beside it (INV-305). Retired: gate ad (`guardrails/check-tree-counts.py`) retired. Never re-armed without a fresh decision. [INV-305] | string | retired, no owning test | *retired* |
| M-499 | A count inside a generated block was rebuilt from the tree (INV-305). Retired: `scripts/gen-tree-counts.py` and gate ad retired. Never re-armed without a fresh decision. [INV-305, INV-258] | string | retired, no owning test | *retired* |
| M-500 | A sentence home was judged and never written (INV-305). Retired: `scripts/gen-tree-counts.py` and gate ad retired. Never re-armed without a fresh decision. [INV-305] | string | retired, no owning test | *retired* |
| M-501 | A floor claim redded only below its floor (INV-305). Retired: gate ad retired. Never re-armed without a fresh decision. [INV-305] | string | retired, no owning test | *retired* |
| M-502 | The gate ran an argument list through a read-only allowlist (INV-305). Retired: `scripts/gen-tree-counts.py` and gate ad retired. Never re-armed without a fresh decision. [INV-305] | string | retired, no owning test | *retired* |
| M-503 | The gate measured the tree the push sends (INV-305). Retired: gate ad retired. Never re-armed without a fresh decision. [INV-305] | string | retired, no owning test | *retired* |
| M-504 | The gate redded over nothing and stated its reach (INV-305). Retired: gate ad retired. Never re-armed without a fresh decision. [INV-305, INV-218, INV-269] | string | retired, no owning test | *retired* |
| M-505 | Every runnable file a skill body named in command position carried a registry entry (INV-306). Retired: gate ae and `scripts/check-registry.json` retired. Never re-armed without a fresh decision. [INV-306] | string | retired, no owning test | *retired* |
| M-506 | A registry entry disagreeing with the tree redded (INV-306). Retired: gate ae retired. Never re-armed without a fresh decision. [INV-306] | string | retired, no owning test | *retired* |
| M-507 | A check entry carried a unique name and a kit derived from its reach (INV-306). Retired: gate ae retired. Never re-armed without a fresh decision. [INV-306] | string | retired, no owning test | *retired* |
| M-508 | A check's source was held against its declared reach (INV-306). Retired: gate ae retired. Never re-armed without a fresh decision. [INV-306] | string | retired, no owning test | *retired* |
| M-509 | A pack-only check left the host instructions (INV-306). Retired: gate ae retired. Never re-armed without a fresh decision. [INV-306] | string | retired, no owning test | *retired* |
| M-510 | A check declared how it learns which tree it judges (INV-306). Retired: gate ae retired. Never re-armed without a fresh decision. [INV-306] | string | retired, no owning test | *retired* |
| M-511 | The registry gate was wired and stated its reach (INV-306). Retired: gate ae, `scripts/check-registry.json`, and its named-checks script retired. Never re-armed without a fresh decision. [INV-306, INV-210, INV-212] | string | retired, no owning test | *retired* |
| M-547 | The gate device had one home (INV-210, INV-212): `guardrails/gates-manifest.json` joined every gate's script, SPEC anchor, law sentence, red proof and CI-mirror status. Retired: gate af, `guardrails/gates-manifest.json` and its generator retired. Never re-armed without a fresh decision. [INV-210, INV-212] | string | retired, no owning test | *retired* |
| M-548 | A CI mirror step could not silently restate a gate's law (INV-210, INV-212). Retired: gate u and gate af, the two gates that read and cross-checked the CI mirror's law text, both retired. Never re-armed without a fresh decision. [INV-210, INV-212] | string | retired, no owning test | *retired* |
