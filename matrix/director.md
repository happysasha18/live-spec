### [node: director]

The first read of a person's message, and what holds it. The rows are careful about what they claim. The graded scenarios are recorded runs against written expectations, so they hold the reading's shape rather than proving a live session went through the door; the row says so in its own words, and the replay row is what keeps a stale score from reading as a fresh one.

| ID | Fact (from spec) | Test level | Owning test | Status |
|---|---|---|---|---|
| M-603 | A message is read as one or more of the seven acts before anything answers it, opens a document or changes a file, the act read from what the person meant in the exchange rather than from the wording, and an unreadable act asked about before any work starts; never an act read off the wording alone, and never one act absorbing another [E-36, INV-4] | string | `tests/test_director_scenarios.py` | *built* |
| M-604 | Work starts only on an instruction, on a correction to work already running, or on a decision that changes such work, and a question, a musing, an idea or an observation is answered with no task, no document and no branch opened for it, the reading said back in one line in the person's own words; never a question that becomes a task because answering it took effort, and never work opened behind an act that asked for none [INV-316, INV-28] | string | `tests/test_director_scenarios.py` | *built* |
| M-605 | The grader that judges a recorded run against its written scenario is deterministic, calls no model, and can tell a wrong verdict from a right one; never a grader that also produces the answer it grades, and never one that passes every verdict put to it [E-36, INV-317] | string | `test_the_grader_fails_a_wrong_verdict` | *built* |
| M-606 | The first read stands on the reading skill's own text with no command, gate or hook putting a message through it, and the score a session's start prints is marked a replay whenever the recorded runs are older than the skill; never a claim of wiring the tree does not hold, and never a stale score printed as though it spoke for the skill as it stands [INV-317] | string | `tests/test_first_read_carrier.py` | *built* |
